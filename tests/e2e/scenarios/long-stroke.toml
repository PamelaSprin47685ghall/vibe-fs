# The Long Stroke — G4R-3 sole OpenCode world (changes/active/test.md §4 / §21).
#
# One continuous OpenCode lifetime. Never set afterExpectation.restart /
# flow.restart (recovery = durable facts in the same PID; §12). Observe durable
# facts + public results only — no internal program-counter choreography (§7).
# Wall clock must stay short (§16).
#
# Outer shell = fast-orchestrator (commission → join). Inner Manager walks the
# adversity spine; publish conflict resumes the SAME Manager (ORCH-007).
#
# ── natural story (compressed from orch-unhappy + manager-unhappy + fallback) ─
#
#   Birth (Orchestrator)
#     ↓
#   commission / ManagerJobCreated (AgentOwnerRoot; WorkActivated at first suicide)
#     ↓
#   Manager forks coder; join blocked; child held incomplete
#     ↓
#   external user_message wakes join (interrupted / reason=user_message)
#     ↓
#   harvest → sole provider-error → FallbackCursorAdvanced eq 1
#     ↓
#   first legal suicide → migration WorkActivated → Host finality REVISE → FinalityRejected
#     ↓
#   repair fork → dual PERFECT → FinalityBlessed → last_words → LifeCompleted
#     ↓
#   CandidateReady; gitConflictProof (no restart) already diverged target
#     ↓
#   ConflictDetected → conflict-resume (same Manager) → coder-resolve → Published
#     ↓
#   orch join / expectSatisfied / oracleLongStroke
#
# ── §21 adversity checklist (one representative path each) ───────────────────
#
#   [x] provider transient failure          — [[fault]] provider-error + assertProviderTransientFailure
#   [x] fallback                            — continue + assertFallbackContinuation
#   [x] join blocked then causally awakened — flow.prompt wake + assertJoinWakePath
#   [x] reviewer REVISE                     — manager-finality-revise + assertReviewerRevise
#   [x] interrupted/aborted child or session— holdChildC1UntilLabor + assertInterruptedJoin
#   [x] finality temporarily blocked        — FinalityRejected + assertFinalityTemporarilyBlocked
#   [x] durable recovery/continuation       — same PID FallbackCursorAdvanced + assertDurableRecovery
#   [x] publish conflict / stale target     — gitConflictProof on manager.0 (no restart) + assertPublishConflict
#   [x] successful reconciliation           — Orchestrator-tagged Published eq 1 + assertSuccessfulReconciliation
#   [x] later successful finality           — FinalityBlessed + LifeCompleted + assertLaterSuccessfulFinality
#
# Yield / hang risks (estimated):
#   - FinalityBlessed waitFact: widen via renewOn; journal noise without renew can trip silence
#   - ConflictDetected / Published: rebase+barrier lag; timeouts mirror orch-unhappy (60–90s caps)
#   - AgentOwnerRoot WorkActivated is migration-written on first suicide (FinalityTool
#     ensureMigrationLife), NOT at fork — never waitFact WorkActivated before manager-idle suicide
#   - interrupt requires holdChildC1UntilLabor; without hold, drain completes child and skips user_message
#   - REVISE→PERFECT needs bindFinalityReviseThenPerfect retire/activate (same OpeningAssignment)
#   - blogger pad undersize → unmatched companion requests (fail closed)
#   - wall <6s target vs orch publish chain — risk of §16 miss until flesh/perf pass
#
# Anchors (lean extract, not a paste): orchestrator-unhappy-path (conflict→Published,
# no restart), manager-unhappy (join wake / REVISE), fallback.toml (sole provider-error).

scenario = "long-stroke"
description = "G4R-3 The Long Stroke: orchestrator shell, one OpenCode world through adversity to clean shutdown"
pass = "Long Stroke orch-shell: join-wake+interrupted/user_message, provider-fallback, REVISE→FinalityRejected→Blessed→LifeCompleted, gitConflictProof→ConflictDetected→Published (no restart)."

# Event ceilings (fail-closed on dead loops; primary over silence watchdog):
#   maxJournalEvents — EventStore envelope count; the unique causal flow snapshots at 511 and
#                      teardown may land three already-in-flight durable commits, so 514 is the
#                      full Host lifetime ceiling.
#   maxSseEvents     — OpenCode SSE excluding server.heartbeat; K2 + real Host title loops calibrate
#                      the legal transport lifetime to an observed upper bound of 2560.
#                      Both are fail-closed runaway ceilings, not wall-clock budgets.
# magicTodoHostCanary: Phase 0 real-host A/E/G/H wrapper (sole OpenCode lifetime).
setup = { strict = true, magicTodoHostCanary = true, preFlowTurns = ["native-todo-title", "native-todo", "strength-canary-title", "strength-canary-replica", "strength-canary-owner", "g2-inspector-title", "g2-inspector-owner", "g2-inspector-child-title", "g2-inspector-q1", "g2-inspector-q2", "g2-inspector-q3", "g6-bookkeeper-title", "g6-bookkeeper-finalize"], env = { WANXIANGSHU_STRENGTH_MODE = "dry-run", WANXIANGSHU_STRENGTH_DRY_RUN_BUDGET = "K2" }, maxJournalEvents = 514, maxSseEvents = 2560, project = { files = { "AGENTS.md" = "long stroke\n", "README.md" = "# long-stroke\n", ".wanxiang/casebook/.keep" = "" } } }
session = { agent = "fast-orchestrator", bind = ["title", "fast-orchestrator"] }
prompt = { agent = "fast-orchestrator", text = "Ship publish_proof.txt to the target branch through the long stroke." }

must = [
  "g2-inspector-owner.0",
  "g2-inspector-q1.0",
  "g2-inspector-q2.0",
  "g2-inspector-q3.0",
  "g6-bookkeeper-finalize.0",
  "orch.0",
  "manager.0",
  "manager.1",
  "mgr-labor.0",
  "coder.0",
  "coder.1",
  "continue.0",
  "manager-idle.0",
  "manager-idle.1",
  "manager-finality-revise.0",
  "manager-finality-reviewer.0",
  "conflict-resume.0",
  "coder-resolve.0",
  "barrier-reviewer.0",
  # orch.2 = join harvest text after Published (orch-unhappy Stroke 6). EXEC-016
  "orch.2",
]

flow = [
  # STRENGTH-003/011 real Host K2 dry-run canary: two nested readonly Replica
  # requests, request #1 replays as a complete batch into #2, and request #3 is
  # physically blocked before provider delivery. Dry-run never prepares/injects Candidate bytes.
  { assertDeliveries = { id = "strength-canary-replica.0", eq = 1 } },
  { assertDeliveries = { id = "strength-canary-replica.1", eq = 1 } },
  { assertDeliveries = { id = "strength-canary-owner.0", eq = 1 } },
  { assertDeliveries = { id = "g2-inspector-q1.0", eq = 1 } },
  { assertDeliveries = { id = "g2-inspector-q2.0", eq = 1 } },
  { assertDeliveries = { id = "g2-inspector-q3.0", eq = 1 } },
  { assertDeliveries = { id = "g6-bookkeeper-finalize.0", eq = 1 } },
  { assertFacts = { name = "StrengthCandidatePrepared", eq = 0 } },
  { assertFacts = { name = "InspectorCaseCaptured", gte = 1 } },

  # External injections before any wait (register holds / retire hooks / conflict).
  { custom = "holdChildC1UntilLabor" },
  { custom = "bindFinalityReviseThenPerfect" },

  # Stale-head AFTER manager worktree exists (orch-unhappy / restart-conflict canary note).
  # gitConflictProof ONLY — zero restart fields (§12 one OpenCode lifetime).
  { afterExpectation = { id = "manager.0", gitConflictProof = true } },

  # Orchestrator Birth → ManagerJob
  { wait = "orch.0" },
  { waitFact = { name = "ManagerJobCreated", eq = 1 } },
  { bindChild = { agent = "fast-manager", bind = ["fast-manager"] } },

  # Manager: fork → join; join stays in flight while child held.
  # Host evidence (StaticTools.knownToolNames + Manager permission matrix) does not
  # advertise builtin todowrite on Manager provider tools — requiring tools=["todowrite"]
  # here fails toolsGate. A/E/G/H assert uses wire evidence.
  # WorkActivated for AgentOwnerRoot arrives later (first suicide migration) — see manager-idle.
  # All Manager prompts MUST use session="child" (bindChild); default session is Orchestrator.
  { wait = "manager.0" },
  { wait = "manager.1" },
  { prompt = { agent = "fast-manager", text = "Also make sure publish_proof.txt has exactly the content 'Published by long-stroke canary' before the end." }, session = "child" },

  # Labor: premature suicide against held child → interrupted join wake → harvest
  { wait = "mgr-labor.0" },
  { wait = "coder.0" },
  { waitFact = { name = "HandleCompleted", gte = 1 } },
  { wait = "mgr-labor.1" },
  { wait = "mgr-labor.2" },

  # Normal Manager idle continuation owns the next same-lane action. Delivery #1
  # receives the sole provider-error fault; the Host fallback continuation then
  # completes, and delivery #2 of the same step performs the first legal suicide.
  # No external Manager prompt races the Host continuation.
  { wait = "manager-idle.0" },
  { awaitTerminal = true, requireAssistantTerminal = true, requireIdleAfterActivity = true, session = "child" },
  { wait = "continue" },
  { waitFact = { name = "FallbackCursorAdvanced", eq = 1 } },
  { wait = "manager-idle.0" },
  { waitFact = { name = "WorkActivated", eq = 1 } },
  { wait = "manager-finality-revise.0" },
  { wait = "manager-finality-revise.1" },
  { waitFact = { name = "FinalityRejected", gte = 1 } },

  # Same physical ManagerIdle turn repairs, then seeks dual-PERFECT blessing.
  { wait = "manager-idle.1" },
  { wait = "coder-fix.0" },
  { wait = "manager-idle.2" },
  { wait = "manager-idle.3" },
  { wait = "manager-finality-reviewer.0", timeoutMs = 30000 },
  { wait = "manager-finality-reviewer.1", timeoutMs = 30000 },
  { waitFact = { name = "FinalityBlessed", gte = 1, renewOn = ["ReviewVerdictRecorded", "ConfirmedReviewWitness", "FinalityReviewerEnlisted", "BlogObservationCommitted"] }, timeoutMs = 180000 },
  { wait = "manager-idle.4", timeoutMs = 30000 },
  { wait = "manager-idle.5", timeoutMs = 30000 },
  { waitFact = { name = "LifeCompleted", gte = 1 } },
  { waitFact = { name = "CandidateReady", eq = 1 }, timeoutMs = 60000 },

  # Conflict resume on SAME Manager (no task restart) → resolve → Published
  { waitFact = { name = "ConflictDetected", gte = 1 }, timeoutMs = 60000 },
  { wait = "conflict-resume.0", timeoutMs = 60000 },
  { wait = "coder-resolve.0", timeoutMs = 60000 },
  { wait = "coder-resolve.1", timeoutMs = 60000 },
  { wait = "conflict-resume.1", timeoutMs = 60000 },
  { wait = "conflict-resume.2", timeoutMs = 60000 },
  { waitFact = { name = "RebasedCandidateReady", gte = 1 }, timeoutMs = 90000 },
  # Bare "Published" false-matches assignment text; pin Orchestrator case tag.
  { waitFact = { name = "\"Orchestrator\",[\"Published\"", eq = 1 }, timeoutMs = 90000 },

  # Stroke 6 — Orchestrator harvest (same as orch-unhappy: orch.2 after Published).
  { wait = "orch.2", timeoutMs = 60000 },
  { awaitTerminal = true, requireAssistantTerminal = false },
  { custom = "oracleLongStroke" },
  { assertDeliveries = { id = "coder.1", eq = 1 } },
  { custom = "assertG6ColdFetch" },
  # Phase 0 real-host A/E/G/H — wire tools + wrapper artifacts (or precise V2 unavailability).
  { custom = "assertHostCanariesAEGH" },
  { expectSatisfied = true },
]

[[turn]]
id = "native-todo-title"
lane = "native-todo-canary"
kind = "title"
user = "NATIVE_TODO_CANARY: exercise the default build session todowrite hook."

  [[turn.step]]
  respond = { type = "title", text = "Native Todo Canary" }

[[turn]]
id = "native-todo"
lane = "native-todo-canary"
user = "NATIVE_TODO_CANARY: exercise the default build session todowrite hook."
tools = ["todowrite"]

  [[turn.step]]
  respond = { type = "tool-call", tool = "todowrite", args = { todos = [{ kind = "new", content = "Probe native V1 todowrite hook", status = "pending", priority = "medium" }] } }

  [[turn.step]]
  respond = { type = "text", text = "Native todowrite canary completed." }

[[turn]]
id = "strength-canary-title"
lane = "strength-canary-owner"
kind = "title"
user = "STRENGTH_HOST_CANARY: inspect README.md through the real nested Replica path."

  [[turn.step]]
  respond = { type = "title", text = "Strength Host Canary" }

[[turn]]
id = "strength-canary-replica"
internal = true
user = "STRENGTH_HOST_CANARY: inspect README.md through the real nested Replica path."
tools = ["glob", "grep", "read"]
forbiddenTools = ["bash-honeypot", "edit", "fetch", "inspector", "js-coder", "mv", "rm", "write"]

  [[turn.step]]
  respond = { type = "tool-calls", calls = [{ tool = "read", args = { filePath = "README.md" } }, { tool = "read", args = { filePath = "AGENTS.md" } }] }

  [[turn.step]]
  runtimeStep = 1
  respond = { type = "tool-call", tool = "read", args = { filePath = ".wanxiang/casebook/.keep" } }

[[turn]]
id = "strength-canary-owner"
lane = "strength-canary-owner"
user = "STRENGTH_HOST_CANARY: inspect README.md through the real nested Replica path."

  [[turn.step]]
  respond = { type = "text", text = "Strength dry-run canary owner completed." }

[[turn]]
id = "g2-inspector-title"
lane = "g2-inspector-owner"
kind = "title"
user = "G2_INSPECTOR_PREFIX_CANARY: reuse one inspector for Q1, Q2, then Q3."

  [[turn.step]]
  respond = { type = "title", text = "G2 Inspector Prefix Canary" }

[[turn]]
id = "g2-inspector-owner"
lane = "g2-inspector-owner"
user = "G2_INSPECTOR_PREFIX_CANARY: reuse one inspector for Q1, Q2, then Q3."
tools = ["inspector"]

  [[turn.step]]
  respond = { type = "tool-call", tool = "inspector", args = { prompt = "G2Q1: who owns PromptAuthority?" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "inspector", args = { prompt = "G2Q2: what is ReuseScope?" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "inspector", args = { prompt = "G2Q3: when does CaseFinalize run?" } }

  [[turn.step]]
  respond = { type = "text", text = "G2 inspector prefix canary owner completed." }

[[turn]]
id = "g2-inspector-child-title"
internal = true
kind = "title"
user = "G2Q1: who owns PromptAuthority?"

  [[turn.step]]
  respond = { type = "title", text = "G2 Inspector Child" }

[[turn]]
id = "g2-inspector-q1"
internal = true
user = "G2Q1: who owns PromptAuthority?"
tools = ["return"]

  [[turn.step]]
  respond = { type = "tool-call", tool = "return", args = { message = "G2A1: Host owns PromptAuthority." } }

  [[turn.step]]
  respond = { type = "text", text = "Inspector Q1 returned." }

[[turn]]
id = "g2-inspector-q2"
internal = true
user = "G2Q2: what is ReuseScope?"
tools = ["return"]

  [[turn.step]]
  respond = { type = "tool-call", tool = "return", args = { message = "G2A2: Owner session scope for one Inspector." } }

  [[turn.step]]
  respond = { type = "text", text = "Inspector Q2 returned." }

[[turn]]
id = "g2-inspector-q3"
internal = true
user = "G2Q3: when does CaseFinalize run?"
tools = ["return"]

  [[turn.step]]
  respond = { type = "tool-call", tool = "return", args = { message = "G2A3: On owner ReuseScope close." } }

  [[turn.step]]
  respond = { type = "text", text = "Inspector Q3 returned." }

[[turn]]
id = "g6-bookkeeper-title"
internal = true
kind = "title"
user = "CaseFinalize"

  [[turn.step]]
  respond = { type = "title", text = "G6 Bookkeeper Finalize" }

[[turn]]
id = "g6-bookkeeper-finalize"
internal = true
user = "# # System Prompt: Casebook Bookkeeper"
tools = ["js-bookkeeper"]
forbiddenTools = ["inspector", "write", "edit", "bash-honeypot"]

  [[turn.step]]
  respond = { type = "tool-call", tool = "js-bookkeeper", args = { document = "Q.md", old_text = "G2Q3: when does CaseFinalize run?", new_text = "What is the Inspector reuse contract?" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "js-bookkeeper", args = { document = "A.md", old_text = "G2A3: On owner ReuseScope close.", new_text = "One Inspector child, serial Q/A, finalize on owner close." } }

  [[turn.step]]
  respond = { type = "text", text = "Bookkeeper CaseFinalize idle." }

[[turn]]
id = "title"
lane = "title"
kind = "title"
user = "Ship publish_proof.txt to the target branch through the long stroke."

  [[turn.step]]
  respond = { type = "title", text = "Long Stroke" }

[[turn]]
id = "orch"
lane = "fast-orchestrator"
user = "Ship publish_proof.txt to the target branch through the long stroke."
tools = ["commission", "join"]

  [[turn.step]]
  respond = { type = "tool-call", tool = "commission", args = { name = "fast-manager", charge = "Run the long stroke: ship publish_proof.txt — join-wake, provider fallback, accept REVISE then reach a blessed ending so publish can reconcile." } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "join" }

  # Join stays blocked until Manager Published; then this harvest text (required).
  [[turn.step]]
  respond = { type = "text", text = "Long stroke publish completed after conflict resume." }

# AgentOwnerRoot Manager (opening is "#" + orch fork prompt). Fork + join; join
# stays blocked while holdChild keeps coder incomplete for user_message wake.
# tools MUST match the real Manager provider surface (no todowrite — see flow note).
[[turn]]
id = "manager"
lane = "fast-manager"
user = "# Run the long stroke: ship publish_proof.txt — join-wake, provider fallback, accept REVISE then reach a blessed ending so publish can reconcile."
tools = ["fork", "join", "horizon", "suicide"]

  [[turn.step]]
  respond = { type = "tool-call", tool = "fork", args = { name = "fast-coder", charge = "Write publish_proof.txt." } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "join" }

[[turn]]
id = "mgr-labor"
lane = "fast-manager"
user = "Also make sure publish_proof.txt has exactly the content 'Published by long-stroke canary' before the end."
tools = ["fork", "join", "horizon", "suicide"]

  [[turn.step]]
  respond = { type = "tool-call", tool = "suicide", args = { last_words = "premature" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "join", args = {} }

  [[turn.step]]
  respond = { type = "text", text = "Join harvested after user_message wake; continue." }

[[turn]]
id = "continue"
internal = true
user = "# Continue after provider failure."

  [[turn.step]]
  respond = { type = "text", text = "long-stroke fallback continuation completed." }

[[epoch]]
turn = "continue"
step = 0
reason = "prefix-probe"

# GLORY-029 normal Manager idle continuation. The first physical occasion is
# refused by the sole provider-error fault; fallback then causes a second
# physical occasion. Both are the same declared step and therefore share one
# delivery counter. The successful second occasion owns the rest of the Manager
# tool loop, so no same-lane external prompt races it.
[[turn]]
id = "manager-idle"
internal = true
user = ["# You have done useful work, and useful action may still remain.", "# Time spent is not time exhausted.", "# A long road is still a road.", "# Look again at what the mission still owes.", "# If useful action remains, continue.", "# When nothing useful remains, seek your end."]
tools = ["fork", "join", "horizon", "fission", "todowrite", "suicide"]

  [[turn.step]]
  respond = { type = "tool-call", tool = "suicide", args = { last_words = "first ending" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "fork", args = { name = "fast-coder", charge = "Ensure publish_proof.txt contains exactly: Published by long-stroke canary" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "join", args = {} }

  [[turn.step]]
  respond = { type = "tool-call", tool = "suicide", args = { last_words = "publish_proof.txt is ready." } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "suicide", args = { last_words = "publish_proof.txt is ready." } }

  [[turn.step]]
  respond = { type = "text", text = "Manager rest in peace." }

[[epoch]]
turn = "manager-idle"
step = 0
reason = "prefix-probe"

[[turn]]
id = "coder"
lane = "fast-coder"
user = "# Write publish_proof.txt."
tools = ["write"]

  [[turn.step]]
  respond = { type = "tool-call", tool = "write", args = { filePath = "publish_proof.txt", content = "Published by long-stroke canary\n" } }

  # G6 cold fetch rides the first later Coder child instead of spawning a
  # dedicated proof-only root/Companion/title session. It is still a new Host
  # session after CaseFinalize and therefore proves the same persisted read path.
  [[turn.step]]
  respond = { type = "tool-call", tool = "fetch", args = { session_id = "$inspector" } }

  [[turn.step]]
  respond = { type = "text", text = "Coder finished." }

[[turn]]
id = "coder-fix"
lane = "fast-coder"
user = "# Ensure publish_proof.txt contains exactly: Published by long-stroke canary"
tools = ["write"]

  [[turn.step]]
  respond = { type = "tool-call", tool = "write", args = { filePath = "publish_proof.txt", content = "Published by long-stroke canary\n" } }

  [[turn.step]]
  respond = { type = "text", text = "Coder repaired proof file." }

# Host finality OpeningAssignment. bindFinalityReviseThenPerfect retires REVISE
# after .1 and activates dual-PERFECT turn (same OpeningAssignment otherwise ties).
[[turn]]
id = "manager-finality-revise"
internal = true
user = "# Review the current worktree against all authoritative user requirements."
tools = ["judge"]

  [[turn.step]]
  respond = { type = "tool-call", tool = "judge", args = { verdict = "REVISE" } }

  [[turn.step]]
  respond = { type = "text", text = "Finality reviewer rejects the ending." }

[[turn]]
id = "manager-finality-reviewer"
internal = true
user = "# Review RETIRED long-stroke finality — inactive until revise cohort ends."
tools = ["judge"]

  [[turn.step]]
  respond = { type = "tool-call", tool = "judge", args = { verdict = "PERFECT" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "judge", args = { verdict = "PERFECT" } }

  [[turn.step]]
  respond = { type = "text", text = "Manager finality reviewer confirmed." }

# ORCH-007 conflict resume — same Manager session; do NOT restart original task.
[[turn]]
id = "conflict-resume"
internal = true
user = "[CONFLICT RESUMPTION] An in-progress rebase hit conflicts. Conflicted files:"
tools = ["fork", "join", "horizon", "suicide"]

  [[turn.step]]
  respond = { type = "tool-call", tool = "fork", args = { name = "fast-coder", charge = "Resolve the conflicted publish_proof.txt so it contains exactly: Published by long-stroke canary" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "join" }

  [[turn.step]]
  respond = { type = "text", text = "Conflict resolved; rebase can continue." }

[[turn]]
id = "coder-resolve"
lane = "fast-coder"
user = "# Resolve the conflicted publish_proof.txt so it contains exactly: Published by long-stroke canary"
tools = ["write"]

  [[turn.step]]
  respond = { type = "tool-call", tool = "write", args = { filePath = "publish_proof.txt", content = "Published by long-stroke canary\n" } }

  [[turn.step]]
  respond = { type = "text", text = "Conflict file resolved." }

[[turn]]
id = "barrier-reviewer"
internal = true
user = "# Review the current worktree for correctness. Submit your verdict with the verdict tool."
tools = ["glob", "grep", "read", "judge"]

  [[turn.step]]
  respond = { type = "tool-call", tool = "judge", args = { verdict = "PERFECT" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "judge", args = { verdict = "PERFECT" } }

  [[turn.step]]
  respond = { type = "text", text = "Barrier reviewer completed the double PERFECT." }

[[turn]]
id = "reviewer-confirm"
internal = true
user = "# Nope, let's re-evaluate: does it really fully satisfy the original task without cutting corners?"
tools = ["judge"]

  [[turn.step]]
  respond = { type = "tool-call", tool = "judge", args = { verdict = "PERFECT" } }

  [[turn.step]]
  respond = { type = "text", text = "Reviewer confirmed the tree." }


[[turn]]
id = "blogger"
internal = true
user = "# Write the dense work-log continuation now"

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Long stroke.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Long stroke continued.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 2.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 3.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 4.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 5.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 6.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 7.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 8.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 9.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 10.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 11.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 12.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 13.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 14.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 15.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 16.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 17.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 18.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 19.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 20.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 21.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 22.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 23.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 24.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 25.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 26.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 27.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 28.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 29.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 30.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 31.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 32.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 33.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 34.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 35.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 36.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 37.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 38.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 39.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 40.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 41.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 42.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 43.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 44.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 45.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 46.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 47.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 48.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 49.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 50.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 51.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 52.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 53.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 54.", tip = "primitive-obsession" } }

  [[turn.step]]
  respond = { type = "tool-call", tool = "blog", args = { text = "Blogger cycle 55.", tip = "primitive-obsession" } }


[[epoch]]
turn = "blogger"
step = 0
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 1
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 2
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 3
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 4
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 5
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 6
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 7
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 8
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 9
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 10
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 11
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 12
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 13
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 14
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 15
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 16
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 17
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 18
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 19
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 20
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 21
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 22
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 23
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 24
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 25
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 26
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 27
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 28
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 29
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 30
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 31
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 32
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 33
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 34
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 35
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 36
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 37
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 38
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 39
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 40
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 41
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 42
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 43
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 44
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 45
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 46
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 47
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 48
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 49
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 50
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 51
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 52
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 53
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 54
reason = "frame-commit"

[[epoch]]
turn = "blogger"
step = 55
reason = "frame-commit"

# Sole physical fault: one settled non-retryable provider-error (no AABB here).
[[fault]]
turn = "manager-idle"
step = 0
delivery = "provider-error"
status = 400
retryable = false
attempts = [1]
