# 下班交接

基准：本班开始时 `master` 在 `82d5c2513`（相对 `origin/master` ahead 1，工作区干净）。结束时已把本批结构剪枝与交接文档提交并推送 `master`。

## 本班做了什么

继续 GAP-033（STRUCTURED-WORKFLOW-011..016）下的跨 subsystem 声明依赖收敛：先只读审计稀疏边与符号消费，再按分片并行剪未使用的 `ProjectReference`，聚焦 Fable 编译验证，失败时改挂真实窄合同而不是恢复宽伞引用。

### 结构结果

| 口径 | 上班前（含未推送的 DistillationSurface 提交后） | 下班时 |
|---|---|---|
| subsystems | 26 | 26 |
| compile shards | 213 | 213 |
| production sources | 700 | 700 |
| ProjectReference | 1873 | 1846 |
| shard DAG | 无环 | 无环 |
| 最大 subsystem SCC | 22 | 22 |

相对 `1873` 净减 **27** 条声明引用。SCC 未缩小；GAP-033 保持 **PARTIAL**。事实记录已写入 `requirements/structured-workflow/HOW.md` §3.2。

### 主要剪枝（均经符号复核 + focused Fable compile）

1. `process/opencode-tools-ptytool` ← 去 `execution-delegation-fact`
2. `process/foundation-temporal` ← 去 syncdelegate-model／composition-durable-projection／journal-promptfactcodec／provider-language（4）
3. `repository-programming/opencode-tools-filemutationtools` ← 去 outcome／durabilityport／bounded-parallel（3）
4. `knowledge/repository-knowledge-casebook-model` ← 去 durabilityport；补 `host-fatal-effect` + `host-tool-adapter`（`FatalProcess`／`ToolHostCodec` 真实直接消费）
5. `relay/mission-relay-reviewtool` ← 去 sessionquiescencegate
6. `work/mission-obligation-todo-magictodosemanticsurface` ← 仅去 bounded-parallel（保留 host-digest）
7. `authority/interaction-authority-fold`、`enforcer/enforcer-continuation`、`enforcer/execution-session-loopdetector` ← 各去 outcome
8. `authority/interaction-repair-interactionrepair` ← 去 outcome／ingresscodec／fallback-ledger／requestkind（4）
9. `change/git-integrationgate` ← 去 handle-surface／durabilityport／sharedstatesurface／bounded-parallel／provider-language／node-timing-adapter（6）；清理 `Host.fs` 未使用 `open`；**保留**仍消费的 `foundation-llm-facing`
10. `strength/strength-opencode-settings` ← 去 managedagent／eventstore-storetypes
11. `strength/strength-prediction-predictor` ← 去 managedagent／outcome／strength-policy；删除 7 处无符号 `open Wanxiangshu.Host.Contract`

上班前已存在、本班一并推送的提交：`82d5c2513` 将 DistillationSurface 从误挂 companion runtime 收窄（1874→1873）。

### 明确拒绝／回滚的假阳性

- `context-prefix-wire` 的 outcome／host-digest：仍用 `AttemptOutcome`／`HostDigest`
- `magictodo`／`requirementgrounding` 的 host-digest：仍用 `HostDigest.sha256Hex`
- `provider-languagesurface` 的 foundation-identity：仍用 `SessionId.create`
- 取消中的 worker 曾写坏 `context-prefix-wire.fsproj`（重复 PropertyGroup／误删引用），已 `git checkout --` 还原，未纳入提交

## 验证

已运行：

- `node scripts/checks/subsystems.mjs` → OK，1846 refs，SCC=22
- `node --test requirements/structured-workflow/tests/subsystem-boundaries.test.mjs` → 4/4
- `node scripts/check.mjs` → 各静态 gate 绿；requirement-trace 仍报既有 4 个 proof gap（ENF-015／ENF-016），与本批无关
- 代表性 focused compile：ptytool、foundation-temporal、filemutationtools、casebook-model（修复后 274）、reviewtool、magictodo、fold／continuation／loopdetector、interaction-repair、git-integrationgate、strength-settings、strength-predictor 均 OK

未运行：

- 全量 `npm run build`／`npm run format-build-test`
- Long Stroke／正式 semantic suite 全量

## 剩余缺口（下一班）

1. **GAP-033 仍 PARTIAL**：22 节点 SCC 未拆；优先真实知识归属迁移（HOW §3 重力井：`foundation-roles`、Identity cohorts、provider projection、Host／Delegation／Persistence 多原因分片），不要只追引用计数。
2. 继续稀疏边审计时必须 focused compile；传递闭包碰巧带上的符号要改挂窄合同，禁止伪解耦（动态加载／tag／复制实现）。
3. ENF-015／ENF-016 证明缺口仍 OPEN（capability-enforcement），不在本轮结构授权内自动关闭。
4. 并行剪枝时注意同一 fsproj／Git index 冲突；半截编辑要先还原再继续。

## Git

- 分支：`master`
- 本班提交：结构剪枝 + HOW 更新 + 本交接文件（与先前 ahead 的 DistillationSurface 提交一并 push）
- 推送：`origin/master`（无 force）

---

## 下一班追加（接续 GAP-033）

基准：`bce851f5f`，工作区干净，1846 refs，SCC=22。

### 做了什么

继续只读审计未覆盖的稀疏 SCC 边与大型 composition／persistence 分片；多数 1–2 边为真实 KEEP。对确认零符号消费的引用并行剪枝，并清理残留无用 `open Wanxiangshu.Host.Contract`。

### 结构结果

| 口径 | 上班前 | 下班时 |
|---|---|---|
| ProjectReference | 1846 | **1834** |
| 最大 subsystem SCC | 22 | 22 |
| subsystems／shards／sources | 26／213／700 | 不变 |

相对 `1846` 净减 **12** 条声明引用。GAP-033 仍 **PARTIAL**。HOW §3.2 已同步。

### 本班剪枝

1. `resources/resources-promptsurface` ← `foundation-roles`
2. `delegation/execution-fission-fold` ← `persistence-journal-promptfactcodec`
3. `dispatch/plugin-composition` ← `opencode-tools-filemutationtools`
4. `persistence/persistence-journal-promptfactcodec` ← `execution-delegation-fact`／`context-trace-cursor`／`context-trace-model`（3）；删 `ProjectionUpdate.fs` 无用 Trace open
5. `session-lifecycle/opencode-host-pluginruntimescope` ← `delegation-recovery-runtime`
6. `context/context-companion-companionfactfold` ← `strength-policy`
7. `provider/composition-turn-ordinaryturnworkflow` ← outcome／identityseed／chatadmission-intent／context-trace-capture（4）
8. 清理 12+1 处无用 `open Wanxiangshu.Host.Contract`（不含仍用 `ToolResultBound` 的 ToolHostCodec／Casebook Surface）

### 验证

- `subsystems.mjs` → 1834 refs，SCC=22
- `subsystem-boundaries.test.mjs` → 4/4
- `check.mjs` → 绿（ENF-015／016 既有 proof gap 仍在）
- focused compile：promptsurface、fission-fold、plugin-composition、journal codec、pluginruntimescope、companionfactfold、ordinaryturnworkflow、managedagentconfig、hostsignalbootstrap 均 OK
- 未跑全量 build／Long Stroke

### 剩余缺口

1. SCC=22 未拆；稀疏剪枝收益递减，下一班应优先重力井知识归属迁移。
2. ENF-015／016 仍 OPEN。
3. 剪枝后若 focused compile 暴露传递闭包碰巧提供的符号，改挂窄合同，禁止伪解耦。

---

## 再下一班追加（接续 GAP-033 · SharedState 拆分）

基准：`c82dc213b`，工作区干净，1834 refs，SCC=22，213 shards。

### 做了什么

1. **真实知识归属迁移：** 将 `SharedState` 从混装的 `host/opencode-host-providerrunbinding` 抽到新建 `host/opencode-host-sharedstate`；providerrunbinding 去掉 companion 依赖，只保留 ProviderRunBinding／Surface；SharedState 消费方改挂新分片。聚焦编译后为 `sessionquiescencegate` 补窄合同 `foundation-taskresult`（原靠传递闭包碰巧提供）。
2. **零符号剪枝（+清理无用 open）：** 去误挂的 providerrunbinding／foundation-roles／managedagent／host-signal-contract（host-digest 名）／host-session-contract／outcome／chatadmission-intent／requestkind／async-support 等；foldsurface 的 PluginScope 删 3 处无用 Delegation／Wait open。

### 结构结果

| 口径 | 上班前 | 下班时 |
|---|---|---|
| compile shards | 213 | **214** |
| ProjectReference | 1834 | **1821** |
| 最大 subsystem SCC | 22 | 22 |
| subsystems／sources | 26／700 | 不变 |

相对 `1834` 净减 **13** 条声明引用。GAP-033 仍 **PARTIAL**。HOW §3.2 已同步。

### 验证

- `subsystems.mjs` → 214 shards，1821 refs，SCC=22
- `subsystem-boundaries.test.mjs` → 4/4
- `check.mjs` → 绿（ENF-015／016 既有 proof gap 仍在）
- focused compile：sharedstate、providerrunbinding、sharedstatesurface、pluginruntimescope、companion runtime、strength-opencode-settings、sessionquiescencegate、foldsurface、durabilityport、modelroutingsurface、chatexecution-fold、sessionstartedatledger、fallback-ledger、providerprojectionsurface、delegation-host-adapter、delegation-recovery-runtime 等 OK
- 未跑全量 build／Long Stroke

### 剩余缺口

1. SCC=22 未拆；继续重力井归属（Role↔Persona 分拆、ordinaryturnworkflow 编排归属、HostSignalBootstrap／DurabilityPort 多原因分片等），勿只追 ref 计数。
2. Identity／provider／Host-Delegation 审计已列出更多 move candidates，下一班择一可独立验证的归属迁移。
3. ENF-015／016 仍 OPEN。

### Git

- 分支：`master`
- 本班提交：SharedState 分片 + 剪枝 + HOW + 本交接
- 推送：`origin/master`（无 force）

---

## 再下一班追加（接续 GAP-033 · Persona 拆分与重力井剪枝）

基准：`cd0ad1b49`，1821 refs，SCC=22，214 shards。

### 做了什么

1. **知识归属迁移：** 新建 `participant-persona` 分片，将 Persona 词汇（`Participant/Persona/{Catalog,ManagedCatalog,Identity,Surface}.fs/.fsi`）从 `foundation-roles` 中拆出，精简 `foundation-roles` 至基础 Role 合同；36 个仅消费 Persona 的消费者改挂 `participant-persona`；`prefix-wire` 补齐直接 `participant-persona` 依赖。
2. **重力井与多余引用剪枝：**
   - `hostsignalbootstrap`：仅剪去未直接引用的 `provider-system-transform` 与 `host-tool-adapter` 2 条引用；审计确认 `enforcer-guidance-tip` 必须保留（KEEP，`PairProgrammingThoughtSurface` 直接消费其 `ThoughtTransform` 符号）。
   - `ordinaryturnworkflow`（OTW）：剪去 `context-companion-fact`、`interaction-authority-fact`、`execution-fission-fact` 3 个未直接引用的 fact。
   - `interaction-attention-fold`：剪去未直接引用的 `intra-participant-parallelism/foundation-parallel`。
   - `casebook-bookkeeper`：剪去未直接引用的 `durabilityport`。
   - `casebook-lifecyclesurface`：剪去未直接引用的 `durabilityport` 与 `persistence-journal-promptfactcodec`，改挂实际消费其 `RuntimePath` 类型的 `change-integration/change-fact`。

### 结构结果

| 口径 | 上班前 | 下班时 |
|---|---|---|
| compile shards | 214 | **215** |
| ProjectReference | 1821 | **1815** |
| 最大 subsystem SCC | 22 | 22 |
| subsystems／sources | 26／700 | 不变 |

相对 `1821` 净减 **6** 条声明引用，新增 1 个 persona compile shard。GAP-033 仍 **PARTIAL**。HOW §3.2 已同步。

### 验证

- `subsystems.mjs` → 26 subsystems，215 compile shards，700 sources，1815 refs，shard DAG，largest subsystem cycle=22
- `subsystem-boundaries.test.mjs` → 4/4 通过
- `check.mjs` → 绿（ENF-015／016 既有 proof gap 仍在）
- 各受影响分片均经符号复核与聚焦编译通过
- 未跑全量 build／Long Stroke

### 剩余缺口

1. 最大 subsystem SCC=22 仍未拆开，GAP-033 保持 PARTIAL。
2. 下一刀继续重力井物理分片剥离，优先推进 `DurabilityPort` 等关键分片，禁止将引用数下降写成 SCC 已拆。
3. ENF-015／016 仍 OPEN。

### Git

- 分支：`master`
- 本班提交：Persona 分片 + hostsignal/OTW/attention/casebook 剪枝与窄合同改挂 + HOW + 本交接
- 推送：`origin/master`（无 force）

---

## 再下一班追加（接续 GAP-033 · DurabilityPort 4 路拆分与 SyncDelegateRole 正名）

基准：`a8d51cff8`，26 subsystems，215 compile shards，700 sources，1815 refs，SCC=22。

### 做了什么

1. **DurabilityPort 物理 4 路拆分（本班主刀）：**
   - 拆解混装了 Strength 持久化、Durable Fold、EventStore 日志写入器及 EventStoreHandle 的 `durable-events.strength-persistence-durabilityport` 重力井；
   - 新建分片 `durable-events.composition-durable-fold.fsproj`（`HostFactFold.fs/.fsi`，`Fold.fs/.fsi`）；
   - 新建分片 `durable-events.persistence-journal-eventstorewriter.fsproj`（`JournalEventStoreBoot.fs/.fsi`，`EventStoreJournalWriter.fs/.fsi`）；
   - 吸收归并：将 `EventStoreHandle.fs/.fsi` 移入 `durable-events.persistence-eventstore-storetypes.fsproj`（将 `Store`、`Dispose`、`Create` 设为 public 以便跨分片可见）；
   - 精简 `strength-persistence-durabilityport.fsproj`，仅保留 Strength 持久化类型（`DurabilityPort`、`Store`、`Durability`），其自身的 ProjectReferences 从 31 降至 7；
   - 重定向 ~17 个原消费分片至细粒度窄分片（如 `runtime.fsproj`、`canonicalintegrator`、`foldsurface`、`context-compression.runtime`、`plugin-composition` 等）。
2. **SyncDelegateRole 知识所有权正名与解环：**
   - 将 `SyncDelegateRole` 与 `delegateRoleToAttachment` 从 `Execution/Delegation/SyncDelegate/Model.fs` 移至新建分片 `session-ontology.execution-session-syncdelegaterole.fsproj`（`Execution/Session/SyncDelegateRole.fs/.fsi`）；
   - 切断环路边：`session-ontology.execution-session-association` 不再反向引用 `syncdelegate-model`（改为引用 `syncdelegaterole`）；
   - 在 `Model.fs` 中将 `SyncDelegateRole` 保留为类型别名 `Wanxiangshu.Execution.Session.SyncDelegateRole`，保持完全向下兼容；
   - 更新 `scripts/checks/session-ownership-matrix.json` 中 SyncInspector 与 SyncCoder 的 evidencePath，指向真实所有者 `src/Wanxiangshu/Execution/Session/SyncDelegateRole.fs`。
3. **经符号复核的稀疏引用剪枝（共 16 条）：**
   - 9 条安全剪枝：涵盖 `delegation-sync-runtime`、`ingresscodec`（4 条）、`chatadmission`、`managed-chat-execution`、`strength-predictor`、`delegation-host-adapter`；
   - 3 条 durability 伴随剪枝：`foldsurface`（去 `composition-durable-fact`）、`context-compression.runtime`（去 `host-session-contract`）、`plugin-composition`（去 `foundation-outcome`）；
   - 1 条经审计明确保留（KEEP）：`fallback-ledger` 对 `ingresscodec` 的引用（因需要 `ModelRouting` 类型）；
   - 3 条 HostSignal Stage 1 剪枝：在 `opencode-host-hostsignalbootstrap.fsproj` 上切断 `foundation-outcome`、`interaction-authority-identityseed`、`execution-failure-model`（经符号核实零使用，聚焦编译 32.8s 通过）；
   - 清理 `PluginScope.fs` 中 5 处未使用的 Strength 命名空间 open。

### 结构结果

| 口径 | 上班前 | 下班时 |
|---|---|---|
| subsystems | 26 | 26 |
| compile shards | 215 | **218** (+3) |
| production sources | 700 | **701** (+1) |
| ProjectReference | 1815 | **1818** (+3 净增，新分片必要接线 vs 16 剪枝与 DurabilityPort 31→7) |
| shard DAG | 无环 | 无环 |
| 最大 subsystem SCC | 22 | 22 |

GAP-033 保持 **PARTIAL**。事实记录已写入 `requirements/structured-workflow/HOW.md` §3.2。

### 验证

- `node scripts/checks/subsystems.mjs` → 26 subsystems, 218 shards, 701 sources, 1818 refs, SCC=22，exit 0
- `node --test requirements/structured-workflow/tests/subsystem-boundaries.test.mjs` → 4/4 通过，exit 0
- `node scripts/check.mjs` → 全部门禁通过，exit 0（仅既有 ENF-015/016 证明缺口仍在）
- 全部 14+ 个受影响分片均通过 focused Fable 聚焦编译验证
- 未运行：全量 `npm run build`／Long Stroke

### 剩余缺口

1. 最大 subsystem SCC 保持为 22，GAP-033 仍为 PARTIAL。
2. HostSignal Stage 2（叶子 surface 抽取）与 Stage 3（PluginBoot 移至 plugin-composition）排期至下一班。
3. OTW（OrdinaryTurnWorkflow）子系统重新分类至 dispatch。
4. ENF-015/016 证明缺口仍为 OPEN。

### Git

- 分支：`master`
- 本班修改：DurabilityPort 4 路拆分 + SyncDelegateRole 正名与解环 + 16 条剪枝 + HOW + 本交接

---

## 再下一班追加（接续 GAP-033 · OTW 子系统重分类与 HostSignal Stage 2 叶子 Surface 拆分）

基准：`bca46531d`，26 subsystems，218 compile shards，701 sources，1818 refs，SCC=22。

### 做了什么

1. **OTW 子系统归属修正（重分类至 dispatch）：**
   - `composition-turn-ordinaryturnworkflow.fsproj`（OTW）作为 turn observation 与 dispatch 调度的核心编排器，原归属于 `provider` 子系统系历史遗留；
   - 将其 `<WanxiangshuSubsystem>` 声明从 `provider` 修正为 `dispatch`；
   - 消除跨子系统反向编排倒挂，聚焦编译通过，各上游下游引用关系更加自洽。
2. **HostSignal Stage 2 叶子测试 Surface 归属迁移：**
   - **ChatParamsSurface**：从 `host-boundary.opencode-host-hostsignalbootstrap` 中抽离，移入其天然归属且已包含 `ChatParamsHook` 的 `execution-model-routing.opencode-host-modelroutingsurface` 分片中；
   - **JoinResultRendererSurface**：从 `hostsignalbootstrap` 中抽离，移入拥有 `JoinResultRenderer` 实现的 `delegation.execution-delegation-hostturnobservedsurface` 分片中，并补齐直接依赖 `foundation-roles`；
   - **ExplicitResumeSurface**：从 `hostsignalbootstrap` 中抽离，移入已编译 `ExplicitSessionResume` 的 `host-boundary.opencode-host-fissionhostsurface` 分片中。
3. **HostSignalBootstrap 零符号 ProjectReference 剪枝（2 条）：**
   - 剪除 `hostsignalbootstrap.fsproj` 中经符号复核完全未使用的 2 条声明引用：
     * `Wanxiangshu.Owner.managed-chat-execution.execution-session-chatexecution-fold.fsproj`
     * `Wanxiangshu.Owner.dispatch-protocol.composition-turn-reconcilesurface.fsproj`

### 结构结果

| 口径 | 上班前 | 下班时 |
|---|---|---|
| subsystems | 26 | 26 |
| compile shards | 218 | 218 |
| production sources | 701 | 701 |
| ProjectReference | 1818 | **1817** (净减 1 条：剪枝 2 条，`hostturnobservedsurface` 补 1 条 `foundation-roles`) |
| shard DAG | 无环 | 无环 |
| 最大 subsystem SCC | 22 | 22 |

GAP-033 保持 **PARTIAL**。事实记录已同步写入 `requirements/structured-workflow/HOW.md` §3.2。

### 验证

- `node scripts/checks/subsystems.mjs` → 26 subsystems, 218 shards, 701 sources, 1817 refs, SCC=22，exit 0
- `node --test requirements/structured-workflow/tests/subsystem-boundaries.test.mjs` → 4/4 通过，exit 0
- `node scripts/check.mjs` → 全部门禁通过，exit 0（仅既有 ENF-015/016 证明缺口仍在）
- `node --test requirements/interaction-authority/tests/chat-params-hook.test.mjs` → 7/7 通过
- `node --test requirements/provider-projection/tests/join-result-renderer-entry-comment.test.mjs` → 2/2 通过
- 受影响分片聚焦编译（`node scripts/compile-owner.mjs`）验证均通过：
  * `composition-turn-ordinaryturnworkflow.fsproj`
  * `opencode-host-modelroutingsurface.fsproj`
  * `execution-delegation-hostturnobservedsurface.fsproj`
  * `opencode-host-fissionhostsurface.fsproj`
  * `opencode-host-hostsignalbootstrap.fsproj`
- 未运行：全量 `npm run build`／Long Stroke

### 剩余缺口

1. 最大 subsystem SCC 保持为 22，GAP-033 仍为 PARTIAL。
2. HostSignal Stage 3（PluginBoot / PluginHostWiring 等移至 plugin-composition）留待后续推进。
3. ENF-015/016 证明缺口仍为 OPEN。

### Git

- 分支：`master`
- 本班修改：OTW 重分类至 dispatch + HostSignal Stage 2 三个叶子 Surface 抽取 + 2 条零符号剪枝 + HOW + 本交接

---

## 再下一班追加（接续 GAP-033 · HostSignal Stage 3 与稀疏剪枝）

基准：`bccee6d39`，26 subsystems，218 compile shards，701 sources，1817 refs，SCC=22。

### 做了什么

1. **PairProgrammingThoughtSurface 归属迁移：**
   - 将 `PairProgrammingThoughtSurface.fs/.fsi` 从 `host-boundary.opencode-host-hostsignalbootstrap` 抽离移入 `guidance-delivery.enforcer-guidance-tip`；
   - 为 `enforcer-guidance-tip` 补充直接依赖 `semantic-trace.context-trace-model` 与 `runtime-platform.digest`；
   - HSB 剔除 PPT 专用依赖（`enforcer-guidance-tip`、`guidelineprojection`、`context-trace-model`、`companion-fact`、`companionfactfold`）；
   - 符号复核确认保留 `companion-runtimesurface`，供 `HostCompactionObserver` 使用。
2. **HostSignal Stage 3 插件接线迁移至 PluginComposition：**
   - 将 Plugin Cluster B（`PluginHost.fs/.fsi`、`PluginBoot.fs/.fsi`、`PluginHostWiring.fs/.fsi`、`PluginSessionWiring.fs/.fsi`、`PluginRecoveryWiring.fs/.fsi`）从 `hostsignalbootstrap` 整体移入 `opencode-plugin.plugin-composition`（位于 `BloggerChronicleText` 之后、`PluginTransforms` 之前）；
   - `plugin-composition` 补齐所需依赖（包括 `delegation-host-adapter`、`statictools`、`managedagent`、`opencodeport`、`opencodeport-adapter`、`wait-diagnostic-adapter`、`host-root-workspace-runtime`、`session-attachment-contract`、`foundation-temporal-contract`、`process-foundation-temporal`、`syncdelegate-model`）；
   - 剪除 `plugin-composition` 中未使用的 `git-hook-sync`；
   - HSB 彻底剔除 Cluster B 专用及多余引用（`syncdelegate-model`、`wait-*`、`opencodeport*`、`statictools`、`managedagent`、`session-attachment-contract`、`foundation-temporal`、`host-root-workspace-runtime`）；
   - HSB 依赖改挂 narrow 的 `host-boundary.host-root-workspace-contract`（仅满足 `IRootWorkspaceReader`）；
   - 至此 HSB 仅编译单一核心入口 `HostSignalBootstrap.fs/.fsi`，且 `plugin-composition` → `hostsignalbootstrap` 依赖方向严格保持。
3. **独立符号复核稀疏剪枝：**
   - `delegation.execution-delegation-hostturnobservedsurface`：剪除 7 条未使用的 ProjectReference（`eventstore-surface`、`durable-fold`、`identityseed`、`handle-surface`、`wait-completion-mailbox`、`host-digest`、`ingresscodec`）；
   - `managed-session-lifecycle.opencode-host-pluginruntimescope`：剪除未使用的 `sharedstatesurface`；
   - `speculative-investigation.strength-opencode-settings`：剪除未使用的 `sessionquiescencegate`。

### 结构结果

| 口径 | 上班前 | 下班时 |
|---|---|---|
| subsystems | 26 | 26 |
| compile shards | 218 | 218 |
| production sources | 701 | 701 |
| ProjectReference | 1817 | **1809** (净减 8 条) |
| shard DAG | 无环 | 无环 |
| 最大 subsystem SCC | 22 | 22 |

GAP-033 保持 **PARTIAL**。事实记录已同步写入 `requirements/structured-workflow/HOW.md` §3.2。

### 验证

- `node scripts/checks/subsystems.mjs` → 26 subsystems, 218 shards, 701 sources, 1809 refs, SCC=22，exit 0
- `node --test requirements/structured-workflow/tests/subsystem-boundaries.test.mjs` → 4/4 通过，exit 0
- `node scripts/check.mjs` → 全部门禁通过，exit 0（仅既有 ENF-015/016 证明缺口仍在）
- 受影响 6 个分片全部通过验证
- 未运行：全量 `npm run build`／Long Stroke

### 剩余缺口

1. 最大 subsystem SCC 保持为 22，GAP-033 仍为 PARTIAL。
2. ENF-015/016 证明缺口仍为 OPEN。

### Git

- 分支：`master`
- 本班修改：6 个 fsproj（HostSignal Stage 3、PPT 迁移、稀疏剪枝）+ HOW.md + 本交接
- 提交并推送至 `origin/master`（无 force）

---

---

## 再下一班追加（接续 GAP-033 · 剥离 output 出环、RuntimeResources 抽取与零符号剪枝）

基准：`081aabc36`，26 subsystems，218 compile shards，701 sources，1809 refs，SCC=22。

### 做了什么

1. **剥离 `output` 子系统出环（SCC 22→21）：**
   - 将 `output-distillation.process-largegatesurface.fsproj` 的 `WanxiangshuSubsystem` 归属从 `output` 修正为 `process`（`WanxiangshuSemanticOwner` 保持 `output-distillation` 不变）；
   - 原先唯一导致 cycle 的 `process/opencode-tools-ptytool` → `output/process-largegatesurface` 边转变为 `process` 子系统内部引用；
   - `output` 子系统入度降为 0，成功从全局最大强连通分量中剥离，最大 subsystem SCC 成功由 22 降至 21；
   - 同步更新 `scripts/checks/authority-contracts.json` 中 `RequirePermit DistillationRuntime` 的 owner 为 `process`。
2. **Rank-2 resources 细粒度抽取：**
   - 新建分片 `cognitive-environment.resources-runtimeresources.fsproj`（归属 `resources` 子系统），承接从 `distribution.resources-enforcercatalogresource` 抽出的 `RuntimeResources.fs/.fsi` 编译；
   - 重定向 6 个仅需 `RuntimeResources` 的消费分片（`promptsurface`、`enforcer-repair`、`enforcer-guidance-tip`、`opencode-host-managedagentconfig`、`opencode-tools-executortoolsurface`、`strength-opencode-settings`），切断其对 `resources-enforcercatalogresource` 的引用；
   - 切断了 `promptsurface` → `enforcercatalogresource` 命名分片依赖，并消除了 `strength` → `enforcer` 经由 settings 的唯一跨子系统引用；
   - 为同时使用两者的 `provider-system-transform.fsproj` 补齐对 `resources-runtimeresources` 的显式引用；
   - 剩余遗留：`resources` ↔ `enforcer` 仍通过 `runtimeresources` → `enforcer-catalog*` 相互依赖；`resources` → `provider` 仍通过 `provider-language` 存在跨子系统引用。
3. **经严格符号复核的零符号剪枝（共 6 条）与格式收敛：**
   - `strength-persistence-durabilityport`：剪除未使用的 `foundation-outcome`；
   - `composition-turn-ordinaryturnworkflow`：剪除未使用的 `execution-session-recovery-model`；
   - `opencode-host-hostsignalbootstrap`：剪除未使用的 `wait-diagnostic-adapter`、`composition-durable-fact`、`session-attachment-runtime` 3 条引用；
   - `repository-knowledge-casebook-model`：剪除未使用的 `foundation-roles`；
   - KEEP 保留确认：`opencode-host-modelroutingsurface` 对 `fissionhostsurface` 仍需消费 `ExplicitResumeSuppression`，确认保留；
   - 格式清理：收敛 `modelroutingsurface` 与 `enforcercatalogresource` 的紧凑单行 XML 格式。
4. **后续设计就绪项：**
   - Rank-3（Language → runtime-platform）与 AgentJournal split 仅完成设计，未实施修改，留待下一班推进。

### 结构结果

| 口径 | 上班前 | 下班时 |
|---|---|---|
| subsystems | 26 | 26 |
| compile shards | 218 | **219** (+1) |
| production sources | 701 | 701 |
| ProjectReference | 1809 | **1809** (持平：新建分片依赖与剪枝相抵) |
| shard DAG | 无环 | 无环 |
| 最大 subsystem SCC | 22 | **21** (output 已剥离出环) |

GAP-033 保持 **PARTIAL**。事实记录已同步写入 `requirements/structured-workflow/HOW.md` §3.2。

### 验证

- `node scripts/checks/subsystems.mjs` → 26 subsystems, 219 shards, 701 sources, 1809 refs, SCC=21，exit 0
- `node --test requirements/structured-workflow/tests/subsystem-boundaries.test.mjs` → 4/4 通过，exit 0
- `node scripts/check.mjs` → 全部门禁通过，exit 0（仅既有 ENF-015/016 证明缺口仍在）

### 剩余缺口

1. 最大 subsystem SCC 保持为 21，GAP-033 仍为 PARTIAL。
2. `resources` ↔ `enforcer` 及 `resources` → `provider` 环路仍存。
3. Rank-3（Language → runtime-platform）设计就绪但单凭自身无法解环；AgentJournal split 设计就绪（约 52 处 retarget），留待后续实施。
4. ENF-015/016 证明缺口仍为 OPEN。

### Git

- 分支：`master`
- 本班修改：15 个已改文件 + 1 个新建 fsproj + HOW.md + 本交接
- 提交并推送至 `origin/master`（无 force）

---

## 再下一班追加（接续 GAP-033 · Rank-3 Language 下沉、AgentJournal 分拆、连续解三环至 SCC=18 与安全剪枝）

基准：`68ab2a8cc`，26 subsystems，219 compile shards，701 sources，1809 refs，SCC=21。

### 做了什么

1. **Rank-3 Language 合同与资源读取下沉到 runtime-platform：**
   - 新建 `runtime-platform.language.fsproj`（归属 `runtime-platform`，仅依赖 `distribution.assemblyinfo.fsproj`），承接 `Language.fs/.fsi` 与 `ProviderResources.fs/.fsi`；
   - `participant-provider-language` 移出上述 4 个源码文件，并引用 `runtime-platform.language`；
   - 将 `resources-promptresources`、`resources-runtimeresources`、`resources-enforcercatalogresource` 全部改挂 `runtime-platform.language`，彻底斩断 `resources → provider` 知识边；保留 `languagesurface` 对 `promptresources` 的反向真实测试消费。
2. **AgentJournal 运行时句柄独立分片拆分（治理持久化最大重力井）：**
   - 新建 `persistence-journal-agentjournal.fsproj`（归属 `persistence`），独立编译 `Writer.fs/.fsi`、`AgentJournal.fs/.fsi`、`SharedAgentJournal.fs/.fsi`，声明 9 条直接最小依赖；
   - `persistence-journal-promptfactcodec` 剔除上述 6 个文件，保留事实编解码及投影状态；
   - 严格审查 64 个消费分片：51 个仅需句柄的工程改挂 `persistence-journal-agentjournal`，4 个编解码/折叠工程保留 `promptfactcodec`，9 个双重消费分片挂载双依赖；消除了数十个下游对 FactCodec 内部细节的无谓依赖。
3. **剥离 `resources` 子系统出环（SCC 21→20）：**
   - 结合 Rank-3 切断 `resources → provider` 的成果，将组合 EnforcerRule 与审计资源的 `resources-runtimeresources.fsproj` 以及对 JS 暴露的 `resources-promptsurface.fsproj` 的 `WanxiangshuSubsystem` 修正归入 `enforcer`；
   - `resources` 在环内出度降为 0，成功剥离出全局最大环（SCC 21→20）。
4. **剥离 `repository-investigation` 子系统出环（SCC 20→19）：**
   - 将作为 ToolSpec 装配的 `opencode-tools-inspectortool.fsproj` 重分类至 `interaction`（`action-affordance` 工具面）；
   - 将已无领域依赖的 `participant-provider-language.fsproj` 重分类至 `runtime-platform`；
   - `repository-investigation` 仅保留 `semble-mcp` 纯接收汇，在 SCC 内出度归 0，成功剥离出环（SCC 20→19）。
5. **剥离 `repository-programming` 子系统出环（SCC 19→18）：**
   - 将其 3 个工具与运行时分片（`opencode-tools-codertool`、`opencode-tools-filemutationtools`、`runtime`）的子系统声明重分类至 `interaction`（其中 `codertool` 所有者设为 `action-affordance`）；
   - `repository-programming` 仅保留 `js-capability` 纯汇分片，在 SCC 内出度归 0，成功剥离出环（SCC 19→18）。
6. **经严格符号复核的零符号安全剪枝（5 条）：**
   - `composition-durable-fold`：剪除 `context-companion-facts` 与 `execution-fission-facts`；
   - `opencode-tools-bookkeepertool`：剪除 `foundation-roles`；
   - `casebook-lifecyclesurface`：剪除 `persistence-eventstore-storetypes`；
   - `execution-session-recovery-model`：剪除 `host-digest` 与 `foundation-outcome`；
   - 符号复核 KEEP 确认：`repository-programming.runtime` 保留 `host-fatal-effect`（`TransactionStore` 依赖 `FatalProcess`）；`provider-system-transform` 保留 `enforcercatalogresource`（依赖 `composeBloggerSystemPromptFor`）。

### 结构结果

| 口径 | 上班前 (`68ab2a8cc`) | 下班时 | 净变化 |
|---|---|---|---|
| subsystems | 26 | 26 | 0 |
| compile shards | 219 | **221** | +2 (`runtime-platform.language` + `persistence-journal-agentjournal`) |
| production sources | 701 | 701 | 0 |
| ProjectReference | 1809 | **1824** | +15 (新增 2 个分片前置依赖及双依赖消费方，抵扣剪枝后净增) |
| shard DAG | 无环 | 无环 | 保持严格无环 |
| 最大 subsystem SCC | 21 | **18** | **-3** (连续剥离 resources、repository-investigation、repository-programming) |

GAP-033 保持 **PARTIAL**。事实记录已同步写入 `requirements/structured-workflow/HOW.md` §3.2。

### 验证

- `node scripts/checks/subsystems.mjs` → 26 subsystems, 221 shards, 701 sources, 1824 refs, SCC=18，exit 0
- `node --test requirements/structured-workflow/tests/subsystem-boundaries.test.mjs` → 4/4 通过，exit 0
- `node scripts/check.mjs` → 全部门禁通过，exit 0（仅既有 ENF-015/016 证明缺口仍为 OPEN）
- 全部受影响的定向分片聚焦编译均验证通过
- 未运行：全量 `npm run build`／Long Stroke

### 剩余缺口

1. 最大 subsystem SCC 保持为 18，GAP-033 仍为 PARTIAL。
2. 下一刀候选已就绪：`relay`（reviewtool / suicidetool 迁移工具面）、`strength` 或 `requirements`。
3. ENF-015/016 证明缺口仍为 OPEN。

### Git

- 分支：`master`
- 本班修改：74 个已改文件 + 2 个新建 fsproj + HOW.md + 本交接
- 提交并推送至 `origin/master`（无 force）

---

---

## 再下一班追加（接续 GAP-033 · 剥离 relay 与 requirements 出环至 SCC=16、零符号剪枝与漏报依赖补齐）

基准：`d140e9503`，26 subsystems，221 compile shards，701 sources，1824 refs，SCC=18。

### 做了什么

1. **剥离 `relay` 子系统出环（SCC 18→17）：**
   - `Mission/Relay/OpenCode/` 三个宿主集成分片的 `WanxiangshuSubsystem` 移出 relay：`mission-relay-reviewtool`、`mission-relay-suicidetool` 归 `interaction`（ToolSpec 可供性，由 capability-enforcement.opencode-host-managedagentconfig 消费）；`mission-relay-narrative-transform` 归 `dispatch`（plugin transform 阶段，由 opencode-plugin.plugin-composition 调用；与既有 `relay-retirement.composition-turn-workflow` 归 dispatch 的先例一致）。
   - relay 只余领域核心（Contract/Fold/Assessment/Projection/Retirement/WorkspaceSnapshot），SCC 内出度归 0。
   - 只改现行权威字段 `WanxiangshuSubsystem`；legacy `WanxiangshuSemanticOwner`/locality 保持原值（STRUCTURED-WORKFLOW-016：legacy 字段只作兼容读取，不参与 verdict）。
2. **剥离 `requirements` 子系统出环（SCC 17→16）：**
   - `requirement-grounding.host-fact` → `persistence`（durable host fact 词汇与 grounding projection state，被 composition-durable-fact/-fold/-projection 与 promptfactcodec 消费）；
   - `requirement-grounding.opencode-host-requirementgrounding-runtime` → `interaction`（面向模型的 grounding runtime/gate/transform 及 Surface，由 managedagentconfig 与 plugin-composition 消费）；
   - requirements 只余纯 model/catalog 分片，SCC 内出度归 0。
3. **独立编译暴露的漏报依赖补齐（2 处，均为改动前既存）：**
   - `mission-relay-narrative-transform` 独立编译缺 `PromptDispatcher` → 补 `dispatch-protocol.dispatch-runtime`，并补直接使用的 `interaction-authority-fact`、`interaction-authority-ledger`；
   - `context-companion-fact` 独立编译缺 `LlmFacing`（`Context/Companion/Blogger/Toml.fs/.fsi`）→ 补 `dispatch-protocol.foundation-llm-facing`；
   - 两者补齐后独立 Fable 编译通过（fingerprint `2087d81b6d49`、`3407b794d9e9`）。subsystem 标签不参与编译，这些失败在改动前就存在，只有 standalone focused compile 能暴露。
4. **经符号复核的零符号剪枝（4 条）与引用移位（1 条）：**
   - `repository-programming.runtime` 的 `host-fatal-effect` 引用零使用（provider 公共符号 `FatalProcess`/`kill`/`trip` 逐名核对），真实消费者 `opencode-tools-filemutationtools`（`TransactionStore.fs` 两处 `FatalProcess.trip`）反而漏报 → 引用从前者移除、补到后者；
   - 剪除 `interaction-authority-ledger`、`delegation-sync-runtime`、`execution-delegation-syncdelegate-model` 三条 `participant-persona` 引用（以 `.fsi` 公共符号逐名核对为零使用；命中的 `role`/`roleLabel`/`origin` 均为消费方本模块自有符号）；
   - 五个受影响分片独立编译全部通过（`077c60862481`、`4c54f590ce87`、`e10c2f546bda`、`0566c6359dfd`、`efb267cc6a0e`）。
5. **审计方法警示（零符号审计刀部分误报）：**
   - 审计刀 7 个候选中 `mission-workrecord-materialize-runtime → host-digest` 为误报（`Materialize.fs` 实际 3 处 `HostDigest` 使用，且其 provider 描述错误）；本批所有剪枝都在 provider `.fsi` 公共符号表上逐名复核后才执行，低置信 2 项未采纳。
   - 结论：零符号候选必须以 provider 公共签名逐名核对，不能只按 `.fs` 文本命中判断。

### 结构结果

| 口径 | 上班前 (`d140e9503`) | 下班时 | 净变化 |
|---|---|---|---|
| subsystems | 26 | 26 | 0 |
| compile shards | 221 | 221 | 0 |
| production sources | 701 | 701 | 0 |
| ProjectReference | 1824 | **1825** | +1（-4 零符号剪枝，+1 真实消费者补齐，+1 漏报依赖补齐，+3 漏报闭包补齐） |
| shard DAG | 无环 | 无环 | 保持严格无环 |
| 最大 subsystem SCC | 18 | **16** | **-2**（relay、requirements 出环） |

GAP-033 保持 **PARTIAL**。事实记录已同步写入 `requirements/structured-workflow/HOW.md` §3.2（第 8 条）。

### 验证

- `node scripts/checks/subsystems.mjs` → 26 subsystems, 221 shards, 701 sources, 1825 refs, SCC=16，exit 0
- `node --test requirements/structured-workflow/tests/` → 264/264 通过，exit 0
- 受影响行为套件（relay-context-projection / relay-assessment / relay-incumbency / relay-retirement / requirement-grounding）→ 55/55 通过，exit 0
- `node scripts/check.mjs` → 全绿，exit 0（仅既有 ENF-015/016 proof gap）
- 独立 Fable 编译：relay 三片（`198b1daa1d4c`、`30c42de88f7d`、`2087d81b6d49`）、requirements 两片（`39458fa41d13`、`f60386db1274`）、剪枝/移位五片（见上）全部 exit 0
- 未运行：全量 `npm run build`、Long Stroke、全表独立编译普查（见下）

### 剩余缺口

1. 最大 subsystem SCC=16，GAP-033 仍为 PARTIAL；下一刀候选 `process`、`work`、`strength`，但都需真实知识边界（停止用改标签冒充，见 AGENTS.md 警告）。
2. **独立 compile closure 尚未系统闭合（本班给出精确清单）**：本批先实证两处漏报依赖（narrative-transform、context-companion-fact）并修复；随后对全部 221 个 production shard 各跑一次 standalone focused compile（并发 6，702s）：**213 通过、8 失败**，全部 missing-declaration 且均为本批前既存——`change-facts`（`Mission`/`WorkspaceSnapshotId`/`QualityCertificateId`）、`delegation-pty-adapter`（`Strength`）、`dispatch-model-contract` 与 `session-attachment-runtime`（`Host`，同源 `Composition/Turn/{Program,Observation}.fs`）、`sphinx-host-adapter` 与 `sphinx-runtime`（`Foundation`/`CanonicalJson`）、`eventstore-merge-runtime`（`CanonicalEventCodec`）、`repository-investigation-semble-mcp`（`TaskValue`）。修复方式按本班先例：为每个直接使用的符号补最窄 provider 引用并重跑该分片独立编译，禁止动态回捞或复制实现。
3. ENF-015/016 证明缺口仍为 OPEN。

### Git

- 分支：`master`
- 本班修改：10 个 fsproj + HOW.md + 本交接
- 提交并推送至 `origin/master`（无 force）

---

## 再下一班追加（接续 GAP-033 · 全表独立编译闭包补全 8/8、sphinx 重力井拆分；SCC 16→17 为真相值）

基准：`1c82aee29`（26 subsystems，221 shards，701 sources，1825 refs，SCC=16）。

### 做了什么

1. **关闭上一班普查的 8 个 missing-declaration 分片（逐个 focused compile 通过）**：
   - 真实漏报 → 补最窄 provider：`change-facts`（补 `relay-incumbency.mission-relay-core`）、`durable-convergence.eventstore-merge-runtime`（补 `persistence-eventstore-processeventlog`）、`repository-investigation.repository-investigation-semble-mcp`（补 `foundation-taskresult`）。
   - 经编译证明的遗留 open 清理（**不加引用**，删除死 open）：`Execution/Delegation/Fork/Host/Pty.fs:19` 的 `open Wanxiangshu.Strength`、`Composition/Turn/{Program,Observation}.fs` 的 `open Wanxiangshu.Host`、`OpenCode/Host/SphinxMcpConfig.fs:8` 的 `open Wanxiangshu.Foundation`。
   - 全部 fingerprint：`6d658993f69e`、`5ba3c992efa8`、`a633203104c0`、`c44ce09283ea`、`0806e4a5c24c`、`a37a61737881`、`de6882e1831b`。
2. **sphinx 重力井拆分（93 文件单体分片 → 纯领域内核 + 服务/持久化集成）**：
   - 新建 `Wanxiangshu.Owner.epistemic-reasoning.sphinx-serve-runtime.fsproj`，承接 `Sphinx/{GenericDurability,McpServer,Surface}.{fsi,fs}` 共 6 个文件；领域分片只保留 `sphinx-mcp`/`runtime-platform.digest`/`foundation-canonical-json`/`foundation-identity`/`sphinx-event-vocabulary-contract`。
   - 触发原因（证据）：只有这 6 个文件需要 persistence 的 `EventEnvelope`（`GenericDurability.fs:27-31`）、`IEventStore`/`AppendError`（`McpServer.fsi:8-10`）、`CanonicalIntegrator.create`（`McpServer.fs:1221`）、`EventStore.createLocal`（`McpServer.fs:1224`）；而 persistence 的 `Persistence/EventStore/CanonicalIntegrator.fs` 反向持有 sphinx 知识（`open Wanxiangshu.Sphinx`、`SphinxIntegration`/`SphinxGenericIntegration` 规则、第 253-254 行注册；`EventVocabulary.fs:36-40` 枚举 5 个 `SphinxEventTypes.*`）。`scripts/lib/owner-compile.mjs` 拒绝 shard 级 ProjectReference 环，**直接补声明不可行，必须先拆**。
   - 编译证据：`sphinx-runtime` `274675fa13b3`、`sphinx-serve-runtime` `db6978431264`、消费方 `canonicalintegrator` `1f62c475c962`。
3. **零符号剪枝 7 条（编译证明）**：`host-boundary.opencode-host-hostsignalbootstrap` 在 `081aabc36` 之后只编译 `HostSignalBootstrap.{fsi,fs}`，残留引用逐名核对 0 命中后剪除（causal-wait 合同/运行时、execution-delegation-fact、persistence-eventstore-surface、interaction-authority-ledger、casebook-model、casebook-bookkeeper），独立编译通过（fp `1d7c626c8102`）。
4. **五个只读设计（下一批实施输入，证据已写进 HOW §3.2 第 9 条）**：`process`、`strength`、`work`、`knowledge` 的剥离设计 + `sphinx↔persistence` 倒置设计。共同结论：剩余 17 元环的每条入边都对应**真实知识依赖**，没有靠改标签可得的诚实剥离；需要 capability 注入、规则注入/依赖倒置、归属裁决这类真实重构。

### 结构结果

| 口径 | 上班前 (`1c82aee29`) | 下班时 | 净变化 |
|---|---|---|---|
| subsystems | 26 | 26 | 0 |
| compile shards | 221 | **222** | +1（sphinx-serve-runtime） |
| production sources | 701 | 701 | 0 |
| ProjectReference | 1825 | **1833** | +8（6 条真实漏报声明 + 9 条新分片引用 − 7 条零符号剪枝） |
| shard DAG | 无环 | 无环 | 保持严格无环 |
| 最大 subsystem SCC | 16 | **17** | +1，**且 17 是真相** |

**SCC 16→17 的性质**：16 建立在 `sphinx-runtime` 声明闭包不完整之上（该分片当时无法独立编译、真实依赖未声明）。补全后 sphinx 与 `host`/`interaction`/`persistence` 双向可达（`host-boundary.sphinx-host-adapter`→`sphinx-mcp`、`action-affordance.opencode-tools-statictools`→`sphinx-mcp`、`sphinx-serve-runtime`→persistence 都是真实引用）。**含义：本仓此前的 SCC 数字只在闭包完整时才有意义；17 从现在起是可信基线，后续任何下降都必须同时给出闭包完整证据。**

### 验证

- `node scripts/checks/subsystems.mjs` → 26 subsystems / 222 shards / 701 sources / 1833 refs / shard DAG / cycle=17，exit 0
- `node --test requirements/structured-workflow/tests/` → 264/264，exit 0
- `node scripts/check.mjs` → 全绿 exit 0（仅既有 ENF-015/016 proof gap）
- 本班 focused Fable compile 共 12 条，全部 exit 0（见上）
- 全表 standalone 复扫：冷缓存吞吐约 3 shard/min，跑至 **41/222 全部通过、0 失败** 时为准时收班取消；**未完成项**=全表 222 分片复扫（不得宣称已完成）
- 未运行：`npm run build`、`npm run format-build-test`、Long Stroke、dist-backed 全量语义套件（与既往班次同口径）

### 剩余缺口

1. 最大 subsystem SCC=17（真相基线）。下一批实施项（HOW §3.2 第 9 条末段）：`work` 的 capability 注入与 MagicTodo 归属裁决（14 条入边）、`knowledge` 的 BookkeeperRuntime 领域纯化 + ToolRegistry/CanonicalIntegrator 规则倒置（9 条入边，其中 3 条 hostsignalbootstrap 残留已剪）、`strength` 的 durabilityport 归属与 PluginRuntimeScope 解耦、`process` 的 Pty 反向引用与 time-capability 分片归属、sphinx↔persistence 规则注入倒置。
2. 全表 standalone 复扫未完成（41/222 全过）。
3. `knowledge-reuse.runtime`（`casebook-runtime-surface`，6 个 Surface 文件）在生产源码 0 引用、仅 JS 契约测试经 dist 路径加载——需确认是否仍在交付面，不得凭此删除。
4. ENF-015/016 证明缺口仍 OPEN。

### Git

- 分支：`master`；本班修改：4 个源文件（删遗留 open）+ 6 个 fsproj（含 1 个新增）+ HOW.md + 本交接
- 提交并推送至 `origin/master`（无 force）


## 再下一班追加（接续 GAP-033 · work/strength/process 知识剥离落地；全表独立编译复扫 222/222；SCC 仍为 17）

基准：`fbc9b5076`（26 subsystems，222 shards，701 sources，1833 refs，SCC=17）。

### 做了什么

1. **work-record 物化注入**：Change Host / SyncDelegate / ToolRuntimeScope 不再就地调用 `LifecycleWorkRecordProjection`；组合层与 JS 入口（`SyncDelegate.Surface.create`、`Fork/OpenCode/ToolSurface.createRuntime`）显式注入真实能力。行为回归（EXEC-031）证明：delegation JS 运行时表面确实组合 work-record——该声明依赖保留。
2. **strength 窄端口 + durabilityport 归属纠正**：ToolRegistry `isReplicaSession`；PluginRuntimeScope 去掉 `member Strength`（改中性钩子）；HostTurnObserver/HostSessionDeletion/Wire/Speculate 改为端口/直接接收；`strength-persistence-durabilityport` 标签 `persistence→strength`。恢复被静默掉的 `Diagnostic.fatal "strength-semantic-cut"`（DURABLE-EVENTS-021）。
3. **process Pty 中性化**：`PtyExitEvent` + `exitListeners`；delegation 侧映射回 `PtyJoinItem`；剪除 process→delegation 三条引用；物理 listener 登记表同步。
4. **零符号剪枝 / 真实 provider 重定向 5 条**（含跨子系统 dispatch→context、work→host）。
5. **全表 standalone 复扫完成**：222/222 全过、0 失败（关闭上一班未完成项）。
6. **契约测试迁移**：pty-port、dry-run-shadow、durable-events semantic-cut、host-boundary/time-capability m6（后两者为基准已失败的期望漂移，本班诚实收口）、workflow-constitution 物理 listener。

### 结构结果

| 口径 | 上班前 (`fbc9b5076`) | 下班时 | 净变化 |
|---|---|---|---|
| subsystems | 26 | 26 | 0 |
| compile shards | 222 | 222 | 0 |
| production sources | 701 | 701 | 0 |
| ProjectReference | 1833 | **1820** | −13 |
| shard DAG | 无环 | 无环 | 保持 |
| 最大 subsystem SCC | 17 | **17** | 0（预期：本班切的是偶然就地耦合，不是反馈弧） |

subsystem 对级入边（节选）：`work` 6→4、`strength` 5→3；`session-lifecycle` 出边 14→12、`context` 出边 12→11、`interaction` 出边 19→18；`host` 出边 14→15（HostSignalBootstrap 显式装配 strength——下一步可上移到 plugin-composition）。

### 验证

- `node scripts/checks/subsystems.mjs` → 26/222/701/1820，cycle=17，exit 0
- `node --test requirements/structured-workflow/tests/` → 264/264
- `npm run build` → exit 0（impact 1402 items）
- `node requirements/verification-system/tests/run.mjs` → **3970 passed / 0 failed**，exit 0
- `node scripts/check.mjs` → 全绿 exit 0（仅既有 ENF-015/016 proof gap）
- 全表 standalone 复扫：222/222 全过
- 未运行：`npm run format-build-test`、Long Stroke

### 剩余缺口

1. 最大 subsystem SCC=17。下一批：`CanonicalIntegrator`/`EventVocabulary` 领域规则倒置（设计已完成，见 HOW §3.2 第 10 条）；MagicTodo 归属；`BookkeeperRuntime` 宿主调度抽出；HostSignalBootstrap→strength 闭包上移到 plugin-composition。
2. ENF-015/016 证明缺口仍 OPEN。
3. `knowledge-reuse.runtime`（casebook-runtime-surface）仍为 JS 契约交付面，勿删。

### Git

- 分支：`master`；本班修改：~50 个源/fsproj/测试/登记表文件 + HOW.md + 本交接
- 提交并推送至 `origin/master`（无 force）


---

## 2026-09-11 第二班：领域规则倒置收口（Integrator 注入 + MagicTodo/Bookkeeper/HostStrength 归属）；新增 4 个 provider 分片，SCC 17→19 为诚实值

基准 `94b16000a`（`git status` 干净，`HEAD == origin/master`）。本班按 HOW §3.2 第 10 条末段的设计，落地四刀并在收官时补齐两条真实缺口（宿主编排 store 规则集、控制金字塔压平），外加继承自基准的格式债清理。

### 本班做了什么

1. **Integrator 领域规则倒置（spine 不再认识任何领域折叠）**：`SphinxIntegration`/`SphinxGenericIntegration`、`StrengthIntegration`、`JsTransactionIntegration`、`CasebookIntegration` 四组规则逐字迁到各自领域 owned 的新 provider 分片——`epistemic-reasoning.sphinx-integration-rules`、`speculative-investigation.strength-integration-rules`、`repository-programming.repository-programming-js-integration-rules`、`knowledge-reuse.knowledge-integration-rules`。构造口改为 `CanonicalIntegrator.createWithRules : IntegrationRule list -> ICanonicalIntegrator`（旧 `create()` 与 builder CE 删除；`requireBaseRules` 对缺失 Structural/Journal fail-closed）；8 个真实调用点按最小规则集装配（5 处 journal-only 用 `baseRules`，JS 表面用全集，Sphinx 服务用 `baseRules @ SphinxIntegrationRules.rules`）。脊线分片 `persistence-eventstore-canonicalintegrator` 清掉 7 条领域引用，现只持 `baseRules`（Structural+Journal）。规则 `Name`/`Accepts`/`FaultScope`/cut-reset 与 `EventVocabulary.fs` 的 21 个冻结常量未改。
2. **宿主编排 store 规则集修复（本班新发现的静默语义丢失）**：`OpenCode/Host/WorkspaceEventStore.acquire` 是全宿主唯一共享 store；`CasebookLifecycle`/`CasebookWorkflow`/`CasebookIndex`/`FetchTool`、`PluginHostInterop`→`JsToolsTransactionStore.createPersistence`、`PluginHostWiring`→`StrengthDurability.create` 都从它读 `TryCurrent "Casebook"/"JsTransaction"/"Strength"`。只给 `baseRules` 会让这些读取静默得空而 append 仍成功。现装配 `hostProgram = baseRules @ StrengthIntegrationRules.rules @ CasebookIntegrationRules.rules @ JsTransactionIntegrationRules.rules`（历史顺序；Sphinx 由独立 MCP 进程自己的 store+程序承担），并如实新增声明引用。
3. **MagicTodo 归属迁移**：`Mission/Obligation/Todo/{Facts,Projection,MagicTodoFactCodec}` → `Composition/Durable/MagicTodo{Facts,Projection,FactCodec}`，新增 `MagicTodoIdentity.{fs,fsi}` 承载 canonical `TodoWriteId`（`Todo/Model.fs` 保留同型别名，11 个非本切片消费者无需改动）。命名空间、codec 字节、event-type 字符串、fold 结果、fail-closed 解码逐字不变；`composition-durable-{fact,fold,projection}`→`todo-model` 三条 persistence→work 边消失。
4. **BookkeeperRuntime 内部分离**：单文件内私有 `Decisions`（纯决策）/`Ledger`（记账）/`Coordination`（唯一触碰 `ISessionHostPort`、`BookkeeperStaging`、`PromptAuthority`），公开 `.fsi` 逐字不变，未增删 ProjectReference。残留：`PromptAuthority`/`BookkeepingStaging` 仍是同分片静态调用。
5. **HostSignalBootstrap strength 闭包上移**：新增 `OpenCode/Plugin/PluginStrengthPorts.{fs,fsi}`（plugin-composition）承接逐字迁移的闭包；`HostSignalBootstrap` 只留中性 `StrengthHostPorts`（`HandlePreTurn`/`ObservePrimaryTurn`/`CancelStrengthOwner`/`OnSessionDeleted`）并剪除 3 条 strength 引用，子系统边 `host → strength` 消失。**实现事实**：wire 时 `StrengthReplicaRuntime` 实为 `None`（`PluginSessionWiring.attach` 在 `PluginHostWiring.create` 之后），eager 捕获与旧行为一致；激活 replica 路径属行为变更，未做。
6. **门禁要求的真实压平**：`PluginStrengthPorts` 拆为 `tripFuse`/`commitAppendResult`/`commitReconciledEvent`/`commitDurablePromotion`/`observePrimaryTurnBody`；`HostTurnObserver` 增私有 `observePrimaryTurnIfPresent`，去掉 `if → match` 链。顺序（干跑关闭→primary 观察→durable 提交→普通观察）与 fuse/raise/`Diagnostic.fatal "strength-semantic-cut"` 语义逐字保持。
7. **测试与登记表重锚（同强度换新家）**：`durable-events/event-store-append`、`speculative-investigation/dry-run-shadow`（→`PluginStrengthPorts.fs`）、`crash-reconciliation/external-effect-reconciliation`（registry 3 条路径 + 负例 fixture）、`obligation-ledger/{obligation-ledger-workflow-contract,magic-todo-provider-boundary}`（→`Composition/Durable/MagicTodo*`）、`scripts/checks/external-effect-contracts.json`（3 条）、`scripts/checks/release-closure-nodes.json`（2 个 node + 新增 Identity）。
8. **格式债清理**：`npm run format:check` 在基准即为红（14 个未格式化文件，实测于 HEAD 干净 worktree）；本班用仓库自有 Fantomas 修正共 19 个（含本批新增 5 个），门禁转绿。

### 结构结果

| 口径 | 上班前 (`94b16000a`) | 下班时 | 净变化 |
|---|---|---|---|
| subsystems | 26 | 26 | 0 |
| compile shards | 222 | **226** | +4（四个领域规则 provider） |
| production sources | 701 | **707** | +6 |
| ProjectReference | 1820 | **1839** | +19 |
| 跨子系统引用 | 1446 | 1455 | +9 |
| shard DAG | 无环 | 无环 | 保持 |
| 最大 subsystem SCC | 17 | **19** | +2（诚实值，成因见下） |

**SCC 归因（实测子系统边差异）**：新增 `repository-programming → interaction`、`repository-programming → persistence`；消失 `host → strength`。前者来自新 provider `repository-programming-js-integration-rules` 需要 `opencode-tools-filemutationtools`——该 fsproj 名字带 `repository-programming.` 前缀，但 `<WanxiangshuSubsystem>` 是 `interaction`，同一分片混合编译 interaction 的 `OpenCode/Tools/FileMutationTools.*` 与 `Repository/Programming/Js/{ToolsBindings,TransactionStore,OpenCode/*,GeneratorSurface}`；后者是领域 provider 依赖持久化的窄 `IntegrationRule` 契约与 `EventEnvelope` 模型。`requirements` 随 repository-programming 并入同一分量。**这不是解环成果**：倒置把"脊柱硬编码领域折叠"换成"组合根显式装配领域 provider"，引用从脊线搬到装配分片并新增 provider→契约的诚实依赖。脊线自身闭包显著收窄（7 条领域引用清零）。

### 验证

- `node scripts/checks/subsystems.mjs` → 26/226/707/1839，cycle=19，exit 0
- `node --test requirements/structured-workflow/tests/` → 264/264
- `npm run build` → exit 0（1414 items；js-surface-manifest 165 surfaces；js-module-linkage 779 modules）
- `node requirements/verification-system/tests/run.mjs` → **3970 passed / 0 failed**
- `node requirements/verification-system/tests/integration/run.mjs` → **279 passed / 0 failed**
- `node scripts/check.mjs` → exit 0（仅既有 ENF-015/016 proof gap）
- `npm run format:check` → exit 0
- `npm pack --dry-run` → OK（2044 files）
- focused Fable compile（每刀各自验证）：`knowledge-integration-rules` `fp:7d9fbfa0df9d`、`persistence-eventstore-canonicalintegrator` `fp:5392caad68bd`、`durable-runtime-surface` `fp:e21240589828`、`persistence-eventstore-surface` `fp:379b829a0908`、`plugin-composition` `fp:8c6cef9ca703`、`opencode-host-turnruntimepreparation` `fp:5f812f7080fb`、以及各 provider/消费分片（见 HOW §3.2 第 11 条）
- **Long Stroke（`e2e/entry.test.mjs`）为偶发停顿，与本批无确定性关联**：本班首次运行失败（`Timed out waiting for expectation coder.3`，WATCHDOG 在 `manager-interrupt.1` 后 60s 静默，阻塞期望 orch.2/coder.1..3，保留世界 `/tmp/oc-e2e-F0GwGR`）；随后在基准 `94b16000a` 的干净 worktree（`/tmp/e2ebase`，完整 build）复跑 **通过**（56 steps），当前树连续复跑 **3/3 通过**（56 steps，4.3–4.8s，journal≈450/699，sse≈2161/3351）。失败签名与 HOW §3.2 第 9 条记录的历史 Long Stroke 停顿（`manager-interrupt.1` 后等 `coder.3`，保留世界 `/tmp/oc-e2e-yqJFT1`）一致；本次仍无因果证据归因于业务逻辑，当前证据只支持“偶发停顿”。基准 worktree 已删除。

### 剩余缺口

1. 最大 subsystem SCC=19。下一批候选（证据见 HOW §3.2 第 11 条）：拆分混合分片 `opencode-tools-filemutationtools`（预期消掉 `repo-prog → interaction`）；评估把 `IntegrationRule`/`EventEnvelope` 契约上移到平台窄分片（消掉领域 provider→persistence 反向边）；`EventVocabulary.fs` 白名单若要由规则派生需 `DeclaredEventTypes` 字段。
2. ENF-015/016 证明缺口仍 OPEN。
3. `knowledge-reuse.runtime`（casebook-runtime-surface）仍为 JS 契约交付面，勿删。
4. `scripts/checks/owner-impact-corpus.json` 中 `baseline_measurement.structural[*].compile_item_identities` 仍含旧 `Mission/Obligation/Todo/{Facts,Projection,MagicTodoFactCodec}` 路径——该文件是 report-only 工具（`scripts/owner-impact-report.mjs`）的历史基线测量记录，不是当前证明锚点，未改写；如需重测须同步再生。
5. `BookkeeperRuntime` 的 `PromptAuthority`/`BookkeepingStaging` 静态调用、`WorkspaceEventStore` 的领域 provider 装配仍是 knowledge/persistence 入边的真实来源。

### Git

- 分支：`master`；本班修改：源/fsproj/测试/登记表 54 个文件 + `requirements/structured-workflow/HOW.md` + 本交接
- 提交并推送至 `origin/master`（无 force）

---

## 2026-09-11 第三班：混合分片拆分（归属真相）+ ENF-015/016 真实行为证明（trace gap 4→0）+ Long Stroke 停顿证据；SCC 保持 19

基准 `6e46f7ae4`（=origin/master，工作区干净）。本班先按上一班记录的两项缺口开工：混合 compile shard 归属与 ENF-015/016 的替代证明。子代理通道整班 429（`RESOURCE_EXHAUSTED`）不可用，四次派发（含换类型重派）均瞬时失败，切片由主代理亲自执行；这属于基础设施问题，不影响切片范围。

### 本班做了什么

1. **拆分混合 compile shard（归属真相）**：`repository-programming.opencode-tools-filemutationtools` 声明 subsystem=`interaction`，却把 `OpenCode/Tools/FileMutationTools.{fsi,fs}` 与 `Repository/Programming/Js/{ToolsBindings,TransactionStore,OpenCode/ToolWorkflow,OpenCode/ToolHost,GeneratorSurface}`（共 10 个 repository-programming 源文件）编译进同一分片，后者因此被计入 interaction 子系统。现拆为：
   - `interaction.opencode-tools-filemutationtools`（仅 FileMutationTools，4 条引用：nodefs、host-tool-adapter、office-capability、provider-language）；
   - `repository-programming.repository-programming-js-toolbindings`（Js 10 源，9 条引用：identity、llm-facing、eventstore-model、eventstore-storetypes、host-tool-adapter、host-fatal-effect、foundation-taskresult、provider-language、js-capability）。
   5 个真实 consumer 按符号重接：`opencode-host-managedagentconfig`（`ToolRegistry` 用 `mv/rm` spec + `JsToolSpec/JsDescriptionAssets`）与 `repository-programming.runtime`（`FileMutationSurface` + `RuntimeSurface/TransactionSurface/WorkflowSurface/ToolHostSurface`）两半都需要；`opencode-host-requirementgrounding-runtime`（`JsGeneratorSurface/JsToolWorkflow`）、`opencode-tools-bookkeepertool`（`JsToolsData`）、`repository-programming-js-integration-rules`（`JsToolsTransactionStore`）只需 Js 半。aggregate 源集合与顺序不变。新增引用全部由编译错误逐条证明（首轮缺少 `IEventStore/AppendReceipt` → storetypes、`FatalProcess` → host-fatal-effect、`TaskValue/taskResult` → foundation-taskresult）。
2. **law 测试按新 owner 重锚（同强度）**：`requirements/structured-workflow/tests/owner-project-boundaries.test.mjs` 的 `WHAT[STRUCTURED-WORKFLOW-014]` 原先断言旧分片名存在并引用 NodeFs 端口。现断言两个新分片的**精确源集合**（`.fsi` 先于 `.fs`，与 `compileItems` 口径一致）与真实边界：FileMutationTools 分片引用 NodeFs 端口且不引用 Js 半；Js 半既不引用已删旧名、也不继承 NodeFs 端口。
3. **ENF-015/016 真实行为证明（trace gap 4→0）**：新增
   - `requirements/capability-enforcement/tests/authority-subject-binding.test.mjs`（`WHAT[ENF-015] AUTHORITY_001_...`）
   - `requirements/capability-enforcement/tests/authority-stale-admission.test.mjs`（`WHAT[ENF-016] AUTHORITY_002_...`）
   两条测试驱动注册的 ChatExecution surface，实际执行 `ManagedChatAcceptance`/`ManagedChatProviderLifecycle` 准入（dist 构建产物，非重写 oracle）：witness 携带精确 subject 与版本字段；外来主体（`AttemptKeyMismatch`）、旧版本证据（`EstablishedEvidenceConflict`）、终态后的旧准入（`ProviderStartedAfterTerminal`）、竞争 provider run（`ProviderRunConflict`）均 typed fail closed，且 append 计数与 projection 证明零效果；同一准入在 Accepted 态成功、终态后失败 ⇒ 决定来自当前读取，而非缓存能力。
   **可失败性实测**：对 `dist/.../Acceptance.js` 定点变异（subject guard / evidence guard / 终态 guard）分别使断言翻红（`FactRejected` / `FactRejected` / ENF-016 红），随后按 sha256 还原并复跑全绿。
   gate 要求声明消费边：`scripts/lib/test-surface-scan.mjs` 的 `SURFACE_CONSUMERS` 为 `Execution/Session/ChatExecution/Surface.js` 增加 `capability-enforcement`。
   `requirements/capability-enforcement/HOW.md`：表格新增 ENF-015/016 行，并把原「替代证明尚未闭合」段落改写为已落地证明 + **覆盖边界**（只证明 chat-execution 一个消费边界；`RecoveryReceipt`/`RecoveryPermit`/`OwnerIdentityWitness`/`AppendReceipt`/`ChatAdmissionBindingReceipt` 等行的运行时消费点 screening 未逐项证明）。
4. **Long Stroke 停顿证据（未定位根因，如实交接）**：本班共 10 次运行，1 次失败（失败发生在构建后首次运行；其后 6/6 串行复现实验全过）。失败世界 `/tmp/oc-e2e-F0GwGR` 的证据：mock 不是阻塞方（唯一 hold 已被 harness 在 interrupt prompt 被接受后释放；失败运行 mock 服务 52 次请求，通过的运行 71–75 次）；宿主在 10:13:59.764 后只发 `server.heartbeat`（业务事件停止）；harness 自己的 causal frontier 报 `BrokenCausalEdge: OrchestratorWorkflow/OrchestratorJob waits for ManagerWorkflow: but no active wait exists for that producer`，同时 `JoinTool:{session=ses_f700a536…}` 的 `agent-join` 仍等 `external:child-completion`；停顿前宿主日志里 manager 会话出现 scripted `declared provider-error fault (retryable=false)`（primary `test` 与回退 `opencode`），而持久 journal 只有 2 条 `FailureRecorded` + 1 条 `SuccessRecorded`。结论只支持“宿主侧在 hard provider fault 后停止推进”，**不能**认定产品缺陷或脚本分支，需下次按 frontier dump 继续。

### 结构结果

| 口径 | 上班前 (`6e46f7ae4`) | 下班时 | 净变化 |
|---|---|---|---|
| subsystems | 26 | 26 | 0 |
| compile shards | 226 | **227** | +1（拆一为二） |
| production sources | 707 | 707 | 0 |
| ProjectReference | 1839 | **1842** | +3（均为编译证明过的真实依赖） |
| 跨子系统引用 | 1455 | 1457 | +2 |
| shard DAG | 无环 | 无环 | 保持 |
| 最大 subsystem SCC | 19 | **19** | 0（不得读作收益） |

**边的真相变化**：新增 `repository-programming → host`（Js 半真实需要 `ToolHostCodec` 与 `FatalProcess`），删除 `repository-programming → interaction`（10 个 Js 源不再与 interaction 工具面同分片）。SCC 保持 19 是因为该环由其它多条边维持——本刀的价值是**归属不再说谎**，不是解环。

### 验证

- `node scripts/checks/subsystems.mjs` → 26/227/707/1842，cycle=19，exit 0
- `node --test requirements/structured-workflow/tests/` → 264/264（含重锚后的边界 law）
- `npm run build` → exit 0（1414 items；165 surfaces；779 modules linked）
- `node requirements/verification-system/tests/run.mjs` → **3972 passed / 0 failed**（含本班 2 条新证明）
- `node requirements/verification-system/tests/integration/run.mjs` → 279 passed / 0 failed
- `node scripts/check.mjs` → exit 0（**trace proof gap 0**，上一班为 4 条 ENF-015/016）
- `npm run format:check` → exit 0
- focused Fable compile：两个新分片 + 5 个 consumer 全部 OK（`fp:35973a9436a1`、`fp:cca03ef495be`、`fp:531de968db50`、`fp:ad8af0900040`、`fp:5c02ecc77edf`、`fp:7c48647df50e`、`fp:31a2344b591a`）
- Long Stroke：本班 10 次运行 1 次失败（签名与 HOW §3.2 第 9 条历史记录一致）；失败世界保留 `/tmp/oc-e2e-F0GwGR`

### 剩余缺口

1. 最大 subsystem SCC=19 未变。下一批输入（本轮已收集证据、未执行）：(a) 领域 `IntegrationRules` provider → `persistence-eventstore-model` 的边应**保留**（真实使用 `EventEnvelope`，声明于 `Persistence/EventStore/Model.fs:26`；`IntegrationRule` 签名本身引用该类型，搬走 envelope 即搬走 persistence 事件模型，属伪解耦）；(b) `EventVocabulary.AuthoritativeEventTypes` 仍硬编码 Job 字面量与 JsTransaction/Inspector 事件名（strength/sphinx 已由窄 vocabulary contract 分片提供），若改由规则派生需给 `IntegrationRule` 增 `DeclaredEventTypes` 且 accepted set 逐字不变；(c) `host -> sphinx`、`dispatch -> strength`、`strength -> host` 等边在取得符号级证据前保留。
2. Long Stroke 偶发停顿根因未定位（见上）。
3. ENF-015/016 的证明覆盖边界已在 HOW 声明：其余已登记 authority 合同的运行时消费点 screening 未逐项证明；ENF-017 的 multiplicity 仍由 manifest 文本门禁证明。
4. 子代理通道整班 429：本班两次只读审计（下一批切割审计、Long Stroke 诊断）由主代理直接完成，未产出独立 Worker 报告。
5. `knowledge-reuse.runtime`（casebook-runtime-surface）仍为 JS 契约交付面，勿删；`scripts/checks/owner-impact-corpus.json` 的历史基线路径仍未再生（report-only）。

### Git

- 分支：`master`；本班修改：2 个新 fsproj + 1 个删除 fsproj、5 个 consumer fsproj、`scripts/lib/test-surface-scan.mjs`、`requirements/capability-enforcement/{HOW.md,tests/authority-*.test.mjs}`、`requirements/structured-workflow/tests/owner-project-boundaries.test.mjs`、`requirements/structured-workflow/HOW.md`、本交接
- 提交并推送至 `origin/master`（无 force）

---

## 2026-09-11 第四班：分片归属真相第二刀 + 领域事件名自有词汇（新增 2 个 contract 分片）+ Long Stroke 双失败模式定位；SCC 保持 19

基准 `9e40a3ea3`（=origin/master，工作区干净）。子代理通道再次整班 429（唯一子代理模型 `RESOURCE_EXHAUSTED`，一次 4 任务批量派发 11s 内全部失败），四个切片由主代理亲自执行。本班按上一班 HOW §3.2 第 12 条记录的下一批输入开工。

### 本班做了什么

1. **混合分片归属修正（第二处）**：`Wanxiangshu.Owner.repository-programming.runtime.fsproj` 声明 subsystem=`interaction`，却编译 8 个 `Repository/Programming/Js/**Surface.{fsi,fs}`（repository-programming 知识）+ `OpenCode/Tools/FileMutationSurface.{fsi,fs}`（interaction 工具面）。现：
   - `FileMutationSurface` 移入 `interaction.opencode-tools-filemutationtools`（与 `FileMutationTools` 同属 `OpenCode/Tools` 工具面知识）；
   - runtime 分片子系统改为 `repository-programming`（另 8 个源全是 Js surface，`<WanxiangshuSemanticOwner>` 本来就是 repository-programming）；
   - 随后零符号剪枝：逐条删除 `host-tool-adapter`、`persistence-eventstore-storetypes`、`repository-programming-js-capability` 三条引用（每次删后 focused compile 通过 = 无符号消费），只留 `repository-programming-js-toolbindings`。
   `WHAT[STRUCTURED-WORKFLOW-014]` law 按新 owner 重锚（同强度：精确源集合 + 不得含 Js 源 + runtime 分片 subsystem/源集合/引用精确断言）。`FileMutationSurface` 无 F# consumer，故无消费者迁移；HOST-BOUNDARY 工具面闭包 law 未改且 8/8 通过。
2. **零符号剪枝的事实边界（记录）**：`Requirement/Grounding/Catalog.fs` 的 `open Wanxiangshu.Repository.Programming.Js` 经编译证明**仍在用**（`JsGlobFs.matchesPathPattern`，Catalog.fs:90）→ `requirements → repository-programming` 是真实知识依赖，删除已撤回（文件与 fsproj 均按 sha256 还原）。`requirements` 留在 19 分量里是事实，不是遗漏。
3. **领域事件名自有词汇**：新增两个零引用窄 contract 分片（与既有 strength/sphinx 同形态）：
   - `knowledge-reuse.repository-knowledge-casebook-event-vocabulary-contract`（`Repository/Knowledge/Casebook/EventVocabulary.{fsi,fs}` → `CasebookEventTypes`）；
   - `repository-programming.repository-programming-js-event-vocabulary-contract`（`Repository/Programming/Js/EventVocabulary.{fsi,fs}` → `JsTransactionEventTypes`）。
   `CasebookStore`（4 个 `InspectorCase*`）与 `JsToolsTransactionStore`（2 个 `JsTransaction*`）改为消费这些常量（对外常量名与字符串逐字不变）；`Persistence/EventStore/EventVocabulary.fs` 改为 `yield! <Owner>.all` 组装，spine 只保留自己的 `JournalEnvelope`/`ProjectionCutTail` 与 4 个 legacy `Job*`（注释记录：`src/` 无生产者/消费者，仅 durable-convergence law 仍用）。accepted set 逐字不变（21 名）。`DURABLE-EVENTS-022` 编译边界 law 纳入两个新 contract 分片并把闭包白名单扩到 knowledge／repository-programming 的 contract 分片，法律含义不变。**未新增行为测试**（理由写入 HOW：领域名漂移已由真实 store-driven 测试覆盖，新增测试无额外可失败性）。
4. **Long Stroke 双失败模式定位（本班 28 次有效运行 7 次失败 ≈ 25%）**：
   - **模式 A（60s 停顿，5 次）**：给 `releaseHeldChild` 与 `respondStream.waitUntil` 加临时日志后复现两次，均打印 `wait-begin entry=coder.0` → `release-called` → `wait-end entry=coder.0 destroyed=false ended=false` ⇒ **harness 释放正常、被 hold 的 SSE 存活、mock 行为与通过运行一致，mock/harness hold 不是阻塞源**（临时日志已按 sha256 还原，两个 support 文件 `git status` 无改动）。宿主侧证据：失败世界里子会话日志只有 `loop step=0` → `process` → `stream providerID=test modelID=test-model agent=coder` → **`loop step=1` 之后没有 `process`/`stream`**，既没有 provider 请求也没有该次尝试的 `Terminal`，随后父 join 一直等 `external:child-completion` 直到 60s watchdog（两个独立失败世界同一签名）。
   - **模式 B（12s 快速失败）**：`[MOCK-FATAL] first script mismatch: reason=no-declared-turn session=... lane=manager kind=chat step=0`，候选集为其它 lane 条目 ⇒ 宿主发出了脚本未声明的 turn。与模式 A 不同源。
   - **推翻旧解释**：失败不集中在「构建后第一次运行」（批次一 run 1 与 run 12、批次二 run 3/6/10 混杂）；「冷启动」不再是证据支持的假设。另：hold 批次里有 5 次运行因本班并发 `npm run build` 重写 dist 而报 `ERR_MODULE_NOT_FOUND`，属自伤污染，已从统计中剔除并记录。

### 结构结果

| 口径 | 上班前 (`9e40a3ea3`) | 下班时 | 净变化 |
|---|---|---|---|
| subsystems | 26 | 26 | 0 |
| compile shards | 227 | **229** | +2（两个领域词汇 contract） |
| production sources | 707 | **709** | +2 |
| ProjectReference | 1842 | **1842** | 0（剪 4 条 / 新增 4 条窄 contract 引用） |
| shard DAG | 无环 | 无环 | 保持 |
| 最大 subsystem SCC | 19 | **19** | 0（不得读作收益） |

新增引用：（词汇分片 → 两个新 contract 各 1）+（casebook-model → casebook contract）+（js-toolbindings → js contract）；删除引用：runtime 分片 4 条（filemutationtools 1 + 上面 3 条零符号）。

### 验证

- `node scripts/checks/subsystems.mjs` → 26/229/709/1842，cycle=19，exit 0
- `node --test requirements/structured-workflow/tests/` → 264/264
- `node --test requirements/durable-events/tests/event-store-compile-boundary.test.mjs` → 2/2；`requirements/host-boundary/tests/m6-slice-boundary.test.mjs` → 8/8
- `npm run build` → exit 0（165 surfaces，781 modules linked）
- `node requirements/verification-system/tests/run.mjs` → **3972 passed / 0 failed**
- `node requirements/verification-system/tests/integration/run.mjs` → 279 passed / 0 failed（all suites passed）
- `node scripts/check.mjs` → exit 0（trace proof gap 0）
- `npm run format:check` → exit 0
- `npm pack --dry-run` → OK（2046 files，含两个新 dist 模块）
- focused Fable compile：casebook contract `fp:26c297bee20f`、js contract `fp:bcae5c07f3fb`、casebook-model `fp:bb8fc99fcb07`、js-toolbindings `fp:d9011dfb9a7b`、词汇 contract `fp:e367bfd70295`、工具面分片 `fp:466d78b2cc7d`、runtime 分片 `fp:54cffdf98097`、以及词汇链上的既有分片全部 OK
- e2e（最终字节）：通过（56 steps in 4.1s）；复现实验另见上（失败世界保留）

### 剩余缺口（下一班）

1. **Long Stroke 模式 A 的下一刀在宿主侧**：子会话 step 1 的 pre-turn/会话门禁为何不发出 provider 请求、也不写 `Terminal`（`OpenCode/Host/TurnRuntimePreparation.fs` 一带与子会话 drain/interrupt 路径）；模式 B 需给 manager lane step 0 补脚本声明或确证是产品越界请求。不要把这两类混成一个 issue。
2. 最大 subsystem SCC=19 未拆；本班再次确认 `requirements → repository-programming` 是真实依赖，已有互反弧（`strength ↔ persistence`、`work ↔ context`）仍缺符号级证据，不得按计数剪。
3. 归属审计负结果：namespace 前缀/目录支配度不是所有权证据（会产出 109 个假阳性），只用逐文件知识判定 + 兄弟分片约定 + 消费者符号语义。
4. `composition-durable-{fact,fold,projection}` 是 composition 根，保持现状；`knowledge-reuse.runtime`（casebook-runtime-surface）仍为 JS 契约交付面，勿删。
5. 子代理通道整班 429：四个切片均由主代理执行，未产出独立 Worker 报告。

### Git

- 分支：`master`；本班修改：`src/Wanxiangshu/Wanxiangshu.fsproj`、7 个 fsproj、3 个源文件、4 个新源文件 + 2 个新 fsproj、2 个 law 测试、`requirements/structured-workflow/HOW.md`、本交接
- 提交并推送至 `origin/master`（无 force）

---

## 2026-09-11 第五班：Long Stroke 停顿根因定位到插件 capacity demand 永不获授；结构侧三条负结果（无删改）

基准 `7622eaf49`（=origin/master，工作区干净）。子代理通道再次整班 429（4 个只读切片批量派发、11s 内全部失败），四个切片全部由主代理执行。

### 本班做了什么

1. **Long Stroke 模式 A 根因（本班主成果，含可复现证据链）**：上一班证伪了 mock hold 并把停顿收敛到"宿主侧 step 1"，本班用**临时插桩**把它进一步收敛到插件 `OpenCode/Host` 的真实代码路径。
   - 三处插桩（交付前全部还原，`git status` 与 sha256 证明无残留）：①`PluginHostInterop.policyAwareHook` 包装层记录每个 hook 的 enter/exit；②`PluginTransforms.normalTransform` 17 步流水线逐步记录；③`ModelCapacity/Borrowing` 记录 demand 入队/授予/拒绝/token 生命周期/step 结束。
   - 复现世界保留：`/tmp/oc-e2e-moltHP`、`/tmp/oc-e2e-0Rqjug`、`/tmp/oc-e2e-3fI7Qr`；追踪 `/tmp/hook-trace-fail.log`、`-fail2.log`、`-fail5.log`。
   - 事实链（以 `/tmp/oc-e2e-3fI7Qr` 为例；子会话 `…eI0jUTdP`，charge `msg_…H5kmiDe1HI`，target `test`）：通过运行的对照是同一子会话 `[XSTEP] 1..16` 全过并 `[HOOK-EXIT] messages-transform`；失败运行在 step 1 只留下 `[XSTEP] 1 begin-physical`，之后再无 XSTEP、也无 `HOOK-EXIT`。对应 capacity 追踪：step 0 `[CAP] add seq=43 … fence=0` → `grant seq=43` 正常；tool 边界 `[TB] end msg=… run=…` → `[CAP] endstep … inflight fence=0 inFence=false owner=test`（谓词命中，本应 `finishStep` 归 Idle）；step 1 `[CAP] add seq=49 … fence=1` → **持续 `nogrant … owned=true ordinary=true queue=1 sources=-1 tokens=1 token=inflight/test`**，直到 60s watchdog，全程没有任何后续 `grant`。宿主日志 `message=loop … step=1` 之后再无 `process`/`stream`；mock 收不到任何 provider 请求；父 join 停在 `external:child-completion`。
   - 机制：`tryGrantDemand = 自有 || 借用 || 普通`。执行仍"拥有"该 execution key 的 token（`owned=true`）但该 token 不是可授予的 Idle 时：自有失败、借用无 credit source（`sources=-1`）、而 `tryGrantOrdinary` 的第一条守卫 `if demandOwnsToken demand then false` 又拒绝再 acquire 普通容量——三者皆不可行，demand 永久挂起。`drain()` 只在 add/release/endstep/cancel 时重算，没有超时、重算或 typed fail-closed 出口，因此插件永不返回该 transform，宿主永不发出该 provider 请求。
   - **最后一跳未闭合（下一班第一件事）**：tool 边界 EndStep 已按 `inFence=false` 命中并 `finishStep`，为什么 step 1 入队时该 token 仍是 `inflight`。三个候选：(a) 同一 execution key 的 token 在 EndStep 之后被别的路径重新置为 InFlight；(b) 同会话不同 execution key 的第二个 token（`tokens=1` 只按 `OwnerSessionId` 计数，不区分键）；(c) `Retiring` 分支 `releaseToken` 之后 `ownedTokenByExecution` 残留键，使 `demandOwnsToken` 为真而 `tryGrantOwned` 又找不到可授予 token。插桩点已写进 HOW，可直接复用。
   - **修复约束**（不得为绿灯放宽）：EMR-010（tool 边界必须用冻结 `PhysicalUserMessageId` 结束 provider step，使 token 回到可借用 idle）、EMR-004（同一 physical message 复用 target 与 capacity fence）、EMR-014（队列/结算只返回封闭 outcome，token/waiter/owner 必须可追溯到同一 exact identity）；禁止用超时、sleep、默认成功或重试兜底。
2. **Long Stroke 模式 B（`no-declared-turn`）证据固定**：`/tmp/lst2-6.log:205` 显示一条 inspector/review charge 的 provider 请求（`lastUser="# Establish read-only evidence about the current delivery through the entitled offices. Judge it independently on all eight dimensions, then submit the review tool once."`）落在 `lane=manager kind=chat step=0` 的会话（`ses_f6fd9d29cffe…`，parent `ses_f6fd9d370f…`）上，候选集全部是 `g2-inspector-*`／`protocol-repair.0`／`manager-t1-*` 的 step-0 条目。是产品把 charge 路由到了错误会话，还是 mock 对产品新建会话缺 lane 绑定，需要带 `MOCK_TRACE=1` 保留世界后取一次 `session` 表证据再判定（不要两边同时改）。
3. **结构侧三条负结果（本班未删改任何引用；GAP-033 保持 PARTIAL）**：
   - `sphinx → persistence` 6 条引用逐条有真实符号消费：`EventEnvelope`/`EventStreamId`/`normalize`、`CanonicalIntegrator.createWithRules`/`baseRules`、`IntegrationRule`、`IEventStore`/`AppendError`/`SemanticCut`、`EventStore.createLocal` → 诚实知识依赖，保留。
   - `opencode-host-hostsignalbootstrap`（2 源 / 33 引用）逐 ref 用提供方源码声明符号核对消费者，未发现零符号候选（命中多为 `SessionExecutionBinding`、`ChatAdmissionIntent`、`IEventStore` 这类真实类型名）。未做 compile 探针，故只作"没有明显假引用"的负结果，不作为"无引用可删"的证明。
   - HOW 第 13 条点名的互反弧 `dispatch ↔ strength`、`work ↔ context`、`strength ↔ persistence`：跨子系统被消费的提供方分片全部是 `<WanxiangshuOwnerLocalityKind>contract` 的窄模型分片（`context-trace-cursor` 0 ref、`dispatch-model-contract` 等），消费的符号是 `XTraceOpeningEvidence`、`LifecycleWorkRecord`、`ReconciledTurn`/`ReconcileProgram`/`TurnOutcome`、`AwaitMode`/`PromptDispatcher` 这类纯词汇类型；`dispatch → strength` 的 4 条全部来自 `opencode-plugin.plugin-composition`（composition 装配）。按 STRUCTURED-WORKFLOW-013/014 这正是被认可的窄合同形态，故判定为诚实依赖，**本批不切**；下一批应换目标（如 `delegation → runtime-platform` 42 条、`interaction → runtime-platform` 38 条的符号级审计）。

### 结构结果

| 口径 | 上班前 (`7622eaf49`) | 下班时 | 净变化 |
|---|---|---|---|
| subsystems / compile shards / sources / refs | 26 / 229 / 709 / 1842 | 26 / 229 / 709 / 1842 | 0 |
| shard DAG | 无环 | 无环 | 保持 |
| 最大 subsystem SCC | 19 | 19 | 0 |

本班没有结构改动（三次审计结论都是"保留"），交付为文档与交接；这一点如实记录，不用审计次数冒充结构进展。

### 验证（最终字节，临时插桩全部还原后）

- `npm run build` → exit 0（165 surfaces，781 modules linked）
- `node scripts/checks/subsystems.mjs` → 26/229/709/1842，shard DAG，cycle=19，exit 0
- `node --test requirements/structured-workflow/tests/` → 264/264
- `node --test requirements/durable-events/tests/event-store-compile-boundary.test.mjs` → 2/2；`requirements/host-boundary/tests/m6-slice-boundary.test.mjs` → 8/8
- `node requirements/verification-system/tests/run.mjs` → **3972 passed / 0 failed**
- `node requirements/verification-system/tests/integration/run.mjs` → all suites passed（279/0）
- `node scripts/check.mjs` → exit 0（requirement-trace 0 proof gap）
- `npm run format:check` → exit 0
- `npm pack --dry-run` → OK
- e2e（最终字节）→ 通过（56 steps in 4.5s）
- 复现实验统计（本班）：instrumented 批次 8/8 通过未复现；此前批次分别 6 次中 1 次、3 次中 1 次失败；失败世界见上。

### 剩余缺口（下一班）

1. Long Stroke 模式 A 最后一跳（token 为何仍是 inflight）+ 随之而来的修复与性质证明；修复必须保持 EMR-004/010/014。
2. Long Stroke 模式 B 的产品/脚本归属判定。
3. GAP-033：本班只有负结果；下一个候选目标见上（`delegation → runtime-platform`、`interaction → runtime-platform`），仍要求符号级证据 + focused compile 证明，禁止按计数剪。
4. 子代理通道整班 429，四个切片 0 产出。

### Git

- 分支：`master`；本班交付：`requirements/structured-workflow/HOW.md` 第 14 条 + 本交接（产品源码与 harness 的临时插桩全部还原，工作区无残留）
- 提交并推送至 `origin/master`（无 force）

---

## 2026-09-11 第六班：EMR-010 属主 transform 入口回收（Long Stroke 模式 A 危墙拆除）

基准 `a20406fc7`（=origin/master）。用户要求不得避重就轻：根因已钉死却只写证据不修不行；语义钉死为「回合内后代借用可阻塞属主，属主 `messages.transform` 一旦触发必须回收」。并确认这本来就是 transform 入口拦截。

### 本班做了什么

1. **结构修复（不是测一测）**：`BorrowingCapacity.reconcileFence` 补齐属主再入回收——按 `ownedTokenByExecution` 找到属主 credit，若当前 InFlight/Retiring step 属于外来会话/消息（descendant borrow），在仲裁前 `finishStep` 回收。Helper：`tryDictionaryValue` / `stepBelongsTo` / `foreignBorrowedStepToken`（压平嵌套 match；`fsharp-control-pyramid` 仍 0 nested decisions）。
2. **法律对齐**：`EMR-010` WHAT/HOW 写明「显式 release/retire 仍等借用方；属主 transform 触发即回收；等待中的 borrower 仍按单调序号优先」。
3. **能失败的旧实现证明**：`EMR_010_owner_transform_entry_reclaims_foreign_inflight_borrow`（descendant 留 InFlight → 属主再入必须立刻获授、不得挂 waiter）。既有 `EMR_010_older_borrowed_step_precedes_later_owned_step` 仍绿。
4. **e2e 证据**：16 次串行 Long Stroke → **15 通过 + 1 次模式 B**（11s，`[MOCK-FATAL] no-declared-turn` inspector charge/`lane=manager`）；**0 次 60s 模式 A capacity 卡死**。模式 B 独立，下一批处理。

### 结构结果

| 口径 | 上班前 | 下班时 | 净变化 |
|---|---|---|---|
| subsystems / shards / sources / refs | 26 / 229 / 709 / 1842 | 26 / 229 / 709 / 1842 | 0 |
| shard DAG / 最大 SCC | 无环 / 19 | 无环 / 19 | 保持 |

本班不拆 SCC；拆除的是 capacity 语义危墙（君子不立危墙）。

### 验证（最终字节）

- focused compile `strength-policy` → OK `fp:10465f12ccc8`
- `npm run build` → exit 0（165 surfaces，781 modules）
- EMR suite → **78/0**（含新回收证明）
- `subsystems.mjs` → 26/229/709/1842，cycle=19
- structured / durable / host-boundary → 264/264、2/2、8/8
- verification → **3973/0**（+1）
- integration → 279/0
- `check.mjs` / `format:check` / `npm pack --dry-run` → exit 0 / exit 0 / OK（2046 files）
- Long Stroke 16× → 15 pass + 1 mode-B；0 mode-A

### 剩余缺口

1. Long Stroke **模式 B**：inspector charge 落在 `lane=manager` 且脚本 `g2-inspector-*` 候选 `@undefined`——产品路由 vs 脚本 lane 绑定，需保留世界后判归属，再结构修。
2. GAP-033 SCC=19 未拆；下一候选仍要符号级证据。
3. 子代理通道本班未依赖（主代理执行）。

### Git

- 修改：`Borrowing.fs`、EMR WHAT/HOW、`model-routing-runtime.test.mjs`、`structured-workflow/HOW.md` §3.2 第 15 条、本交接
- 提交并推送 `origin/master`（无 force）

---

## 2026-09-11 晚班 — Long Stroke 模式 B（脚本欠绑定）

基准：上班时 `master` = `2c98ecab8`（mode A reclaim 已推送，工作区干净）。结束时本批 harness 修复已提交并推送。

### 本班做了什么

1. **根因定性（三路 scout 一致）**：`[MOCK-FATAL] no-declared-turn` 不是产品把 inspector charge 误送到 manager。`lastUser` 正是 `runtime/manager-assess`（`resources/provider/runtime/manager-assess/en.md`），由 Manager workflow 合法投递到 Manager 会话；工具集也是 Manager 套件。
2. **结构缺口**：`long-stroke.toml` 的 `manager-reopened-loop` 只声明了 `runtimeStep = 1`（suicide）。当 assess 以 fresh turn 到达 `step=0`（msgs=`system user assistant user`）时，`resolveEntry` 过滤掉该 turn → fatal。常见竞态仍是 assess@step=1，只打到 suicide。
3. **修复**：
   - 补 optional `runtimeStep = 0` → `review`（PERFECT 默认；oracle 按 incarnation 改分）
   - 保留 optional `runtimeStep = 1` → `suicide`
   - `bindManagerLoopSequence`：reopened step0 推进 `latestManagerAuditAttempt`，与 `manager-loop.0` 用 `Math.max` 对齐，避免覆盖失步
   - `internal` 仍禁 `lane`（schema）；不把 `@undefined` 候选当误路由去“修产品”
4. **能失败的证明**：`e2e-event-ceiling.test.mjs` 钉死 assess@0 → `manager-reopened-loop.0`（review）、assess@1 → `.1`（suicide）。

### 验证

| 项 | 结果 |
|---|---|
| `node --test …/e2e-event-ceiling.test.mjs` | **6/0** |
| code review | APPROVE（无 findings） |
| Long Stroke ×8（`MOCK_TRACE=1`，`/tmp/ls-modeb-{1..8}.log`） | **8/0**；**0× no-declared-turn** |
| 本批 trace | 均命中 `manager-reopened-loop.1`（常见 step=1 路径）；step=0 竞态由单元回归钉死，未在本 8 次中再现 |

未重跑全仓 `format-build-test` / `npm run build`（本班仅 harness/scenario/oracle/测试，无 F# 产品字节变更）。

### 剩余缺口

1. GAP-033 SCC=19 仍 PARTIAL；下一刀需符号级证据，勿追虚荣数字。
2. Mode B step=0 物理竞态未在本 8 次中打出；若再现而单元仍绿，先保留世界日志再判是否另有绑定洞。

### Git

- 修改：`long-stroke.toml`、`long-stroke-oracles.mjs`、`e2e-event-ceiling.test.mjs`、`verification-system/HOW.md`、本交接
- 提交并推送 `origin/master`（无 force）

---

## 2026-09-11 白班 — GlobMatch 纯核抽取，requirements 如实出环（SCC 19→18）

基准：上班时 `master` = `c374bbdb0`（工作区干净，26/229/709/1842，cycle=19）。本班只做 GAP-033 一刀。

### 本班做了什么

1. **选刀**：SCC-19 里 `requirements` 出度唯一（`→ repository-programming`），符号审计确认整条边只为 `Catalog.fs` 一个纯函数 `JsGlobFs.matchesPathPattern`（第 13 项留下的已证事实）；`Model/Facts/Surface` 均不沾。
2. **结构修复（纯/物理分离）**：通配符→正则编译是无领域知识的纯字符串函数，`node:fs` 扫描才是物理能力。新窄分片 `runtime-platform/globmatch`（`Foundation/GlobMatch.{fsi,fs}`，零引用，自带 `GlobPatternError`，不拖 `JsFailure` 下沉）；`GlobFs.fs` 删实现留委托（`JsFailure.AnchorInvalidPattern` 映射保留，对外形状不变）；`Catalog.fs` 改吃新模块；model 分片删旧引用、加新引用。
3. **修两次翻车**：B 切片误删 `Capability.fsi` 行（门禁当场红，已恢复）；新模块裸 `jsNative` 跨模块被擦除（linkage 红）——按 Digest 先例包实函数体；reviewer 抓到 `Foundation` 纯核禁 `JsInterop`（arch gate 5）——删 open；三文件 Fantomas 已格式。

### 结构结果

| 口径 | 上班前 | 下班时 | 净变化 |
|---|---|---|---|
| subsystems / shards / sources / refs | 26 / 229 / 709 / 1842 | 26 / 230 / 710 / 1843 | +1 分片 / +1 源 / +1 引用（净：-1 域边 +2 平台边） |
| 最大 subsystem SCC | 19（含 requirements） | 18（requirements 出度归零出环） | -1，诚实缩小 |

GAP-033 仍 **PARTIAL**。本班只动真实知识归属，未碰数字游戏。

### 验证（最终字节）

- focused compile：globmatch `fp:6361416fd6a4`、js-capability `fp:9baefea59f7a`、model `fp:85bd5731b844` + host-fact/fold/requirementgrounding-runtime/guidelineprojection 四消费者全绿
- `subsystems.mjs` → 26/230/710/1843，cycle=18，exit 0
- structured 264/264；grounding 16/16（搬运函数的行为证明）；durable/host-boundary 10/10
- `npm run build` → 1420 items / 782 modules，exit 0
- verification **3973/0**；integration 279/0
- `check.mjs` / `format:check` → exit 0 / exit 0（含 arch gate 5 与 control-pyramid）
- e2e（最终字节）→ 1/1 通过
- code review：REQUEST_CHANGES → 已修（JsInterop 移除 + 格式），无残留 findings

### 剩余缺口

1. SCC=18 剩余互反弧仍需符号级证据；`persistence`（16 出边）是下一刀的富矿，但它是 composition 根地带，每条引用都要按消费者符号逐条过，不得批量删。
2. `composition-durable-fact → requirement-grounding-model` 疑似零符号（8 源均无 Grounding 消费），可作下一班第一候选探针（删后 focused compile 见分晓；若红则如实保留）。

### Git

- 新增：`Foundation/GlobMatch.{fsi,fs}`、`Wanxiangshu.Owner.runtime-platform.foundation-globmatch.fsproj`
- 修改：`GlobFs.fs`、`Catalog.fs`、2 个 fsproj 引用、`Wanxiangshu.fsproj`（+2 行）、HOW §3.2 第 16 条、本交接
- 提交并推送 `origin/master`（无 force）

---

## 2026-09-12 白班 — 全仓零符号跨分片引用剪枝（7 分片 11 条引用收敛，SCC 保持 18）

基准：上班时 `master` = `25675fbf2`（工作区干净，26/230/710/1843，cycle=18）。

### 本班做了什么

1. **审计候选依赖与真实符号**：
   - 全仓扫描调用方与目标分片符号使用情况，发现 7 个分片存在历史遗留或过宽的 ProjectReference，其内部 `.fs`/`.fsi` 对目标项目符号零消费。
   - 逐个分片核对 AST、类型定义与导出模块，确认完全无实际知识依赖，不涉及伪解耦。
2. **实施引用清理与逐片验证**：
   - `persistence/persistence-journal-promptfactcodec`：切除未引用的 `chat-execution/foundation-outcome`、`host/host-fatal-effect` 与 `runtime-platform/foundation-async-support`（3 条）；
   - `sphinx/sphinx-integration-rules`：切除未引用的 `runtime-platform/foundation-identity` 与 `persistence/persistence-eventstore-model`（2 条）；
   - `knowledge/knowledge-integration-rules`：切除未引用的 `persistence/persistence-eventstore-model`（1 条）；
   - `session-lifecycle/opencode-host-turnruntimepreparation`：切除未引用的 `runtime-platform/foundation-async-support`（1 条）；
   - `repository-programming/repository-programming-js-integration-rules`：切除未引用的 `persistence/persistence-eventstore-model`（1 条）；
   - `strength/strength-integration-rules`：切除未引用的 `runtime-platform/foundation-identity` 与 `persistence/persistence-eventstore-model`（2 条）；
   - `process/foundation-temporal`：切除未引用的 `participant/foundation-roles`（1 条）。
   - 7 个分片各自独立执行 focused Fable compile，全部成功通过并产出新鲜指纹。
3. **独立 Code Review**：
   - 派发独立 `reviewer` subagent 对 7 个修改文件及 11 条删除引用进行逐一核实。
   - Review 结论：`overall_correctness: "correct"`，`findings: []`，确认 11 条均为真实 0 符号引用，无任何字符串反射、动态加载或伪解耦。

### 结构结果

| 口径 | 上班前 | 下班时 | 净变化 |
|---|---|---|---|
| subsystems / shards / sources / refs | 26 / 230 / 710 / 1841 | 26 / 230 / 710 / 1830 | 引用净减 11 条（其中 6 条为环内跨子系统引用） |
| 最大 subsystem SCC | 18 | 18 | 结构稳定，引用收敛 |

### 验证（最终字节）

- focused compile：7 个分片全部 OK（promptfactcodec `fp:923c51bc3cdc`、sphinx-integration-rules `fp:1a52067f1dae`、knowledge-integration-rules `fp:4b643d16a62b`、turnruntimepreparation `fp:c21a317ba66b`、js-integration-rules `fp:c04d681c55e2`、strength-integration-rules `fp:340234c49ebd`、foundation-temporal `fp:2c33b5a7852b`）
- `subsystems.mjs` → 26/230/710/1830，cycle=18，exit 0
- structured / durable / host 边界测试 → 14/14 全绿
- `npm run build` → 1420 items / 782 modules，exit 0
- verification **3973/0**；integration 279/0
- `check.mjs` / `format:check` / `npm pack --dry-run` → exit 0 / exit 0 / exit 0
- e2e（Long Stroke）→ 1/1 通过
- code review → 独立 reviewer 确认无违规

### 下一班

1. 环内单引用边分析显示 `sphinx -> persistence` 是打破当前 18 节点环使 `sphinx` 出环（SCC 18→17）的关键瓶颈，需深入重构 `Sphinx/GenericDurability.fs` 与 `CanonicalIntegrator` 的耦合关系。
---

## 2026-09-12 白班（第二轮）— 零符号引用剪枝第二轮 + 剪切面穷尽结论

基准：上班时 `master` = `1f49438d9`（26/230/710/1830，cycle=18，工作区干净）。

### 本班做了什么

1. **全仓分片级零符号扫描（230 个分片全遍历）**：以目标分片的 namespace / module / type 标识符集合与调用方 `.fs`/`.fsi` 全文做词边界比对，筛出两条真实零符号直接引用（阈值两轮复核，第二轮把短标识符与 type 名一并纳入，仍只剩这两条）。
2. **逐条源码核对后切除**：
   - `runtime-platform/participant-provider-language` 切除 `runtime-platform-assemblyinfo`：调用方 `SessionLanguage` / `Prose` / `ProviderLanguageBinding` 只消费同分片的 `ProviderResources`；`ProviderResources` 对 `ProviderResourceBytes` 的依赖由仍保留的 `runtime-platform/language` 传递，未切断。
   - `enforcer/enforcer-codec` 切除 `host/host-session-runtime`（quiescencegate 分片）：8 个源文件对 `Wanxiangshu.OpenCode` / `ISessionQuiescenceGate` / `SessionSnapshotPort` / `HostMessageProjection` / `HostSessionContext` / `HostBoundarySurface` 的引用计数全为 0；`Enforcer/Surface.fs` 的 `open Wanxiangshu.Resources` 由保留的 `distribution/resources-enforcercatalogresource` 提供；目标分片无模块级 `do`，删除不丢注册行为。
   - 两处均只改 fsproj，未动任何 `.fs`/`.fsi`；各自 focused compile 通过。
3. **独立 code review**：`ReviewTwoPrunes`（reviewer）结论 `overall_correctness: "correct"`、`findings: []`，确认两条删除均为真实零符号且无被动态加载/副作用掩盖的隐藏依赖。

### 结构结果

| 口径 | 上班前 | 下班时 | 净变化 |
|---|---|---|---|
| subsystems / shards / sources / refs | 26 / 230 / 710 / 1830 | 26 / 230 / 710 / **1828** | 引用净减 2 条 |
| 最大 subsystem SCC | 18 | 18 | 未变（两条删除都不改变 subsystem 边集合） |

### 验证（最终字节）

- focused compile：participant-provider-language `fp:6fe5046a764c`、enforcer-codec `fp:af46444848fe`
- `subsystems.mjs` → 26/230/710/1828，cycle=18，exit 0
- `npm run build` → 1420 items / 782 modules，exit 0
- verification **3973/0**；integration 279/0；e2e（Long Stroke）1/1
- `check.mjs` / `format:check` / `npm pack --dry-run` → 全 exit 0
- structured-workflow / durable / host 边界测试与 provider-language 行为测试（21/21）全绿

### 剪切面穷尽结论（重要）

分片级标识符比对启发式在剩余 228 个分片上**零候选**：靠“删掉没有人用的 ProjectReference”已经无可删。要让 SCC 继续下降必须做真实知识倒置，本班把两条已核实的事实写进了 `requirements/structured-workflow/HOW.md` §3.2 第 18 条：

1. `persistence/eventstore-event-vocabulary-contract` 的 `AuthoritativeEventTypes` 静态 `open` 了 sphinx / strength / knowledge / repository-programming 四个域词汇契约 —— persistence 知道全部署事件名。倒置方案（`IntegrationRule` 自带 `Vocabulary`，integrator 从注册规则求并集，`Store.validateVocabulary` 改问注入的 integrator）可行，但会改变只注册 `baseRules` 的 store（delegation / journal / verification 面）的接受集，动手前必须先证明这些 store 不追加域事件。
2. `persistence/durable-runtime-surface` 分片（`Persistence/EventStore/Surface.fs` 的 full-program 装配）在分片图上**零消费者**，但它是 `dist/Persistence/EventStore/Surface.js`，被六个包的测试直接 import，路径与行为是现存测试合同 —— 不能凭“零消费者”删除或改路径；它同时是 persistence→strength/sphinx/knowledge/repository-programming 四条环内边的实际来源。

### 下一班

1. 先做第 1 条的前置证明（`baseRules` 单程序 store 追加的事件类型是否全为 `JournalEnvelope`），再决定是否实施词汇倒置。
2. 若词汇倒置成立，第 2 条的 full-program 装配才是真正要迁移的知识：它现在让 persistence 持有“全部署事件程序”的装配权。
---

## 2026-09-12 夜班 — 业务/纯决策分片不再直读外部物理适配器（013/014 边界倒置三刀）

基准：上班时 `master` = `35916e5ce`（26/230/710/1828，cycle=18，工作区干净）。

### 本班做了什么

先做了一次**全仓分片级违规审计**：统计"非 composition 分片 → 外部子系统 runtime/adapter 分片"的引用（5 条），再逐条读源码判定是合同/纯数据使用还是实现读。结果三刀全部落在真实现读上：

1. **delegation 计时能力注入**（`execution-delegation-fork-runtime`、`delegation-host-adapter`）：
   - 删除 `defaultArg clock (NodeTiming.nodeClockPort ())`——这是"缺省回退到外部物理适配器"的写法；`ForkRuntimeBackendState` 是 private 且唯一构造点总是传入 clock，故回退在生产不可达；
   - `NodeTiming.raceExit` 由直读改为 composition 注入的必需能力 `raceExit: Task -> int -> Task<bool>`；`HostForkRuntime` 的 clock/raceExit 也从可选默认改为必需；
   - 两个分片的计时引用由 `process-node-timing-adapter` 换成窄合同 `foundation-temporal-contract`（`IClockPort`）。接线点在 `Change/Host/Host.fs`、`Fork/Host/HostForkPtySurface.fs`、`Fork/OpenCode/JoinSurface.fs`、`OpenCode/Tools/ToolRuntimeScope.fs`（2 处），全部是 composition 分片。
2. **enforcer/repair 恢复纯决策**：`Enforcer/Cycle/Decode.fs` 里的 `Diagnostic.emit`（host 诊断运行时）与 `Enforcer/Cycle/BloggerProbe.fs` 里的 `ProviderWireCapture.tryPhysicalParentOfProviderRun`（provider 线解码适配器）改为必需注入参数，装配在 `enforcer-continuation` 与 `context-companion-fact`；随后删除 `host-diagnostics-runtime`、`opencode-codec-providerwiredecode` 两条引用。诊断文本与 CTX-014 白名单不变。
3. **relay 工作区快照的 git 能力注入**：`WorkspaceSnapshot.capture` 不再直接调用物理 git 子进程适配器，改为消费 relay 侧窄能力 `WorkspaceSnapshotGitCapability`（六操作镜像 `Git/Subject.fsi`），由 `mission-relay-reviewtool`、`mission-relay-suicidetool`、`composition-turn-workflow`、`plugin-composition` 四个 composition 分片用 `Wanxiangshu.GitSubject` 装配。git 命令与哈希语义不变。

### 结构结果

| 口径 | 上班前 | 下班时 | 说明 |
|---|---|---|---|
| subsystems / shards / sources / refs | 26 / 230 / 710 / 1828 | 26 / 230 / 710 / **1829** | 净 +1：composition 接线 +6，纯分片 −3，delegation 物理计时适配器 −2 |
| 最大 subsystem SCC | 18 | 18 | 本刀移除的是"实现读"而非合同/装配边，环由合同边维持 |
| 非 composition 分片直读外部 runtime/adapter | 5 条 | **1 条** | 剩余为 `sphinx-serve-runtime → eventstore-core-runtime`（方案已勘察）；另一条（`resources-runtimeresources → resources-promptresources`）经读码判定只是旧 locality 标签偏差，实际只消费纯数据 |

### 验证

- focused Fable compile 通过 10 个分片（含 relink 后的 `execution-delegation-fork-runtime` `fp:2c7c1fc65f39`、`delegation-host-adapter` `fp:40e53d492ce3`）
- `requirements/time-capability/tests/m6-slice-boundary.test.mjs`：`process-node-timing-adapter` 消费者白名单移出两个 delegation 分片，TIME-008 性质收紧，5/5 通过
- `npm run build`（1420 items / 782 modules）、`node scripts/check.mjs`（requirement-trace 814 WHAT / 4016 tests，0 proof gap）、verification **3973 passed / 0 failed**、integration 全部通过、e2e（Long Stroke）exit 0、`format:check` 与 `npm pack --dry-run`（2047 files）通过

### 下一班

1. 唯一剩下的 runtime→runtime 直读是 sphinx 持久化装配：把 `serveDurableDir` 里的 `CanonicalIntegrator.createWithRules (baseRules @ SphinxIntegrationRules.rules)` + `EventStore.createLocal` 移到 host 侧 composition 入口，并把 `SphinxMcpConfig.fs:37` 的 spawn 入口指过去。硬约束：`dist/Sphinx/McpServer.js` 必须继续存在（build 有断言），且 `requirements/epistemic-reasoning/tests/mcp-stdio.test.mjs:543,579` 目前直接 spawn 该路径，迁入口要同步改这两处并保持 EPI-019/030 的跨进程恢复证明。
2. 环内 147 条跨子系统边中，绝大多数是合同/装配边：单边割集分析显示只有 `sphinx → persistence` 能单独把 SCC 降到 17，而它由合同边（`eventstore-model-contract`、`eventstore-port-contract`）与这条 runtime 装配共同构成，所以即使做完第 1 条也不足以破环；进一步降环需要合同分层（把 spine 合同与实现分层），不要用删引用或改标签凑数。
---

## 2026-09-12 第二班 — sphinx 进程入口与 durable store 装配迁出 runtime 分片（013/014 最后一条 class-(c)）

基准：上班时 `master` = `729113037`（26/230/710/1829 refs，cycle=18，工作区干净）。

### 本班做了什么

1. **反向审计**（判据先行）：把全部 13 个 `runtime-platform` 分片对 domain subsystem 的引用逐个核对 → **0 条**，平台规则未被违反；再把 14 个 domain `runtime` 分片的跨 subsystem 引用逐符号分成三类：纯数据/合同 (a)、领域 decision (b)、物理实现 (c)。结论：`sphinx-serve-runtime` 是唯一"runtime 分片自建外部物理实现"的 (c) 类倒置，优先修它。
2. **搬迁**：`Sphinx/McpServer.fs:1219-1248` 的 `serveDurableDir`（`CanonicalIntegrator.createWithRules (baseRules @ SphinxIntegrationRules.rules)` + `EventStore.createLocal`）与 `argv[1]` 入口守卫、`SPHINX_COMMON_DIR` 读取、失败退出路径，逐字迁到新文件 `Sphinx/ServeEntry.fs`，由 sphinx 子系统**既有的 composition 分片** `sphinx-integration-rules` 编译（该分片本就持有 sphinx 侧持久化桥 `IntegrationRules`，故装配留在 sphinx 内部，不新增跨 subsystem 边、不让 host 侧 adapter 越界做 domain 装配）。`McpServer.fsi` 以 `bootDurable: IEventStore -> Result<SessionStore, string>` 取代 `serveDefault`；`serveStdio`/`serveDurable`/`create`/`createDurable` 不变。
3. **引用账（符号证据）**：`sphinx-serve-runtime` 剪除 `sphinx-integration-rules`、`persistence-eventstore-canonicalintegrator`、`persistence-eventstore-processeventlog`、`foundation-llm-facing`；`sphinx-integration-rules` 增补 `sphinx-serve-runtime`、`canonicalintegrator`、`processeventlog`、`persistence-eventstore-storetypes`。净 refs 不变。
   **教训**：scout 报 `foundation-taskresult` 是死引用——大写 `TaskResult` 零命中，但小写 `taskResult { ... }` 计算表达式（`McpServer.fs:793/813/846`）真实使用，照搬会编译失败；"零符号"结论必须大小写双向 grep，并以真实 focused compile 互相印证。
4. **入口路径单一真值**：`Sphinx/Mcp.fs` 的 `relativeServerEntry` → `"dist/Sphinx/ServeEntry.js"`；宿主 `SphinxMcpConfig.fs` 改为 `pathJoin (packageRoot (), SphinxMcp.relativeServerEntry)`（不再自写字面量）；`scripts/build.mjs` 与 `owner-compile.mjs` 的 `essentialOutputs` 断言新入口产物；`mcp-stdio.test.mjs` 的 spawn 路径由该数据派生，`kernel.test.mjs` 断言 build 脚本包含该数据。

### 结构结果

| 口径 | 上班前 | 下班时 | 说明 |
|---|---|---|---|
| subsystems / shards / sources / refs | 26 / 230 / 710 / 1829 | 26 / 230 / **711** / 1829 | +1 source = `Sphinx/ServeEntry.fs`（含 `.fsi`）；refs 净 0 |
| 最大 subsystem SCC | 18 | 18 | 只改"谁装配"，sphinx→persistence 仍由合同引用维持 |
| 非 composition 分片直读外部 runtime/adapter 实现 | 1（sphinx） | **0** | 该类别（013/014）在本仓清空 |

### 验证

- focused compiles：`sphinx-integration-rules`（`fp:d7a02059e065`）、`sphinx-serve-runtime`、`durable-events.runtime`、`host-boundary.sphinx-host-adapter`
- `npm run build` 1421 items / 783 modules；`node scripts/check.mjs` 全绿（control-pyramid clean、requirement-trace 814 WHAT / 4016 tests / 0 proof gap）
- verification **3973 passed / 0 failed**；integration 全部通过；e2e（Long Stroke）exit 0
- `format:check`、`npm pack --dry-run`（2048 files）、边界测试 21/21、`mcp-stdio.test.mjs` 12/12
- 冒烟：`node dist/Sphinx/ServeEntry.js` 在 `SPHINX_COMMON_DIR` 下回应 `initialize`（`serverInfo {name:"sphinx",version:"0.9.0"}`，stderr 空）；旧 `dist/Sphinx/McpServer.js` 不再自执行
- 独立审查 `ReviewSphinxEntrySplit`：`correct`、0 findings（含 .fsi 与 flatten 小改动的复核确认）

### 三条被门禁拦下的施工约束（后来者照做）

1. 分片清单法要求每个实现文件有 sibling `.fsi`（`scripts/lib/compile-shards.mjs:113`）——新文件必须同时给 `.fsi`，且 `.fsi` 在 shard fsproj 与聚合 fsproj 中都要排在 `.fs` 之前。
2. `fsharp-control-pyramid` 拒绝 `try { ... match ... }`（depth≥2）：启动逻辑要把 match 抽成独立 helper，再在 `try` 中调用（即原来的 `serveBootOutcome` 形状）。
3. `js-surface-manifest` 按包法禁止跨包导入未声明消费者边之 surface：`host-boundary` 测试不得导入 `Sphinx/Surface.js`；不为冗余一致性断言新开消费者边。

### 下一班（按本班审计结果排序，均有符号证据）

1. **(c)** `delegation/delegation-recovery-runtime → persistence/persistence-journal-agentjournal`：`AgentJournal.appendAgent`（`Handle/Controller.fs:56,231`、`Handle/JoinDrain.fs:84`）、`durable.Writer.BlobWriter.Read`（`JoinDrain.fs:374`、`CompletionCodec.fs:277`）、`HandleProjection.*`（`Controller.fs:217,249`、`JoinDrain.fs:93-95`）。journal 句柄已由参数注入，但类型是对方的 composition 句柄而非窄 capability；需要先把"appendAgent/读 blob/读 projection"抽成 delegation 侧窄 capability（composition 用 `AgentJournal` 装配），注意 `JoinDrain` 与 EMR-010/mode-B 自杀绑定相关，改动必须重跑 Long Stroke（禁止并发跑两个世界）。
2. **(c)** `enforcer/resources-runtimeresources → resources/resources-promptresources`：`PromptResources.loadForLanguage`/`instructionTextsForRole` 经 `ProviderResources.readText` 读资源文件，调用面宽（`ManagedAgentConfig`、`ProviderSystemTransform`、`LanguageSurface`、`BookkeeperRuntime`、`EnforcerCatalogResource`、`PromptSurface`），需先定"资源读取"的 capability 边界再动。
3. **(b)** `session-attachment-runtime → dispatch/dispatch-model-contract` 的 `SessionRecovery.validateClosurePure` 是合同分片暴露的纯 decision，按 013 允许；`resources-runtimeresources → enforcer-catalog-resource` 同 subsystem，不在 013 约束内。二者都**不需要**处理。
4. SCC 降环仍需要合同分层（本班已确认：`sphinx→persistence` 的 5 条引用里 3 条是合法合同/纯数据，删不得），不要用删引用或改标签凑数。
---

## 2026-09-12 第三班 — enforcer 资源装配移出 runtime 分片；PromptCatalog 独立为纯数据 contract shard

基准：上班时 `master` = `a81212a06`（26/230/711/1829 refs，cycle=18，工作区干净）。

### 判据先行：脚本化行为依赖审计

写了分片级审计（遍历 kind ∈ {runtime, pure, contract} 的消费者 → 外部子系统 {composition, adapter, runtime} 分片，统计**带限定名符号使用** module/type 名 + `.`），全仓只剩 7 对：
- 6 对是 `delegation → persistence` 的 journal/projection 族（`AgentJournal`/`HandleProjection`/`AgentFact`，含 `composition-durable-fact`/`-projection`）——需要合同分层，不是一刀能收；
- 第 7 对即本班目标：`enforcer/resources-runtimeresources [runtime] → resources/resources-promptresources [runtime]`（9 处 `PromptResources.*`）。

**更正上一班的错误结论**：上一班把这条判为"只消费纯数据的标签偏差（不修）"。读完调用链后不成立——`RuntimeResources.loadFor` → `PromptResources.loadForLanguage`/`instructionTextsForRole` → `ProviderResources.readText` → `ProviderResourceBytes.readText` → `PackageResources.readText` → `[<Import("readFileSync","node:fs")>]`，即业务 runtime 分片经外部 runtime 分片取得**文件 authority**（014 违规）。教训已写进 HOW：分片级符号扫描只能证明"用了哪些符号"，判物理效果必须把调用链读到 `node:fs` / 子进程 / 网络边界。

### 本班做了什么

1. `PromptCatalog`（9 个 string 字段的纯数据 record）从 runtime 分片 `resources-promptresources` 拆到新 contract 分片 `resources-promptcatalog`（**零 ProjectReference**、无 open、无 I/O）。
2. `RuntimeResources.loadFor`/`load` 逐字搬到 `Resources/RuntimeResourceAssembly.{fsi,fs}`，由 enforcer 既有 composition 分片 `enforcer-catalog-resource` 编译（该分片本就持有 enforcer 规则书装载与 Blogger 规则书合成）→ 装配留在拥有该知识的 subsystem 内，不新增跨 subsystem 知识边。
3. runtime 分片 `resources-runtimeresources` 只剩 record + `install`/`current`/`enforcerRulesFor`；三处调用点（`PromptSurface` 的 3 个 JS 面函数、`ManagedAgentConfigSurface`、`PluginBoot`）改为调 `RuntimeResourceAssembly`。

### 结构结果

| 口径 | 上班前 | 下班时 | 说明 |
|---|---|---|---|
| subsystems / shards / sources / refs | 26 / 230 / 711 / 1829 | 26 / **231** / **713** / **1834** | +1 contract shard、+2 source；refs 净 +5（装配分片 +2、contract 分片 +1、三个调用方各 +1，runtime 分片 −3/+1） |
| runtime 分片前向闭包 | **8** 分片 | **5** 分片 | 移出 `resources-promptresources`（文件读取实现）与 `distribution.resources-enforcercatalogresource`；余下 5 个全是类型/纯数据 |
| 最大 subsystem SCC | 18 | 18 | 本刀搬装配与数据契约，不动合同边；环数不变是预期 |
| runtime/pure/contract 分片 → 外部物理实现（带限定名符号使用） | 7 对 | **1 对**（6 对 delegation→persistence 属 backlog，本质是 `AgentJournal` 句柄合同分层，非"实现读"） | 审计脚本可重跑 |

闭包前后测量方法：同一闭包脚本分别跑在 `git worktree add /tmp/wxs-before HEAD` 的旧树与当前树（worktree 已清理）。

### 验证

- 8 个受影响分片 focused Fable compile 全过：`resources-promptcatalog` `fp:965d1e88a062`、`resources-promptresources` `bbfa1ca4cd65`、`resources-runtimeresources` `62e183726cc4`、`resources-promptsurface` `e1a36473409f`、`enforcer-catalog-resource` `e7f7ae76104f`、`opencode-host-managedagentconfig` `61d9cb42e185`、`plugin-composition` `1f52589cb190`、`provider-system-transform` `ce93eb770411`
- `npm run build` 785 emitted modules；`node scripts/check.mjs` 全绿（814 WHAT / 4016 tests / 0 proof gap）；verification **3973 passed / 0 failed**；integration 全过；e2e（Long Stroke）exit 0；`format:check`；`npm pack --dry-run` 2050 files
- 冒烟（真实 JS 面）：`dist/Resources/PromptSurface.js` 的 `runtimeLoad()` → 120 条规则 + 双语规则书 + `ProviderLanguageRootsReady=true`；`runtimeLoadForLanguage('zh-CN')` → 中文目录；`runtimeInstallFromPackage()` 后 `runtimeCurrent()` 可读；Blogger 提示词含规则书合成结果。

### 过程教训（给后来者）

1. 并行刀必须由父任务的 focused compile 收口：implementer 按批次规则未跑编译，漏了装配分片对 record 所在 runtime 分片的引用，`RuntimeResourceAssembly.fs(37,19): The type 'RuntimeResources' is not defined` 由父任务拦下补上。
2. 新 contract shard 的 `.fs`/`.fsi` 同样要登记进聚合 `Wanxiangshu.fsproj`（否则 `owner-compile` 直接报 "Closure compile item absent from aggregate project"）。
3. 分片清单法要求每个实现文件有 sibling `.fsi`（上一班已记录，本班再次生效）。

### 下一班（均有符号证据）

1. **(主候选)** `delegation → persistence` 的 journal/projection 6 对：需要"合同分层"而不是逐文件换类型——为 `AgentJournal`（含 `HandleProjection`/`AgentFact` 读取）定义 persistence 侧窄合同（append + 投影查询 + blob 读），60 个消费者分片重定向；涉及 `Handle/Controller.fs`、`Handle/JoinDrain.fs`、`CompletionCodec.fs`、`ChildRecoveryWorkflow.fs` 与 mode-B/EMR-010 路径，必须重跑 Long Stroke（禁止并发两个世界）。
2. `resources-promptresources`（runtime kind）与 `runtime-platform-language`（含 `ProviderResources` 文件读取）自身的 kind/归属仍是旧标签：016 明确 locality kind 只是兼容读取数据，不得作为判据；若要继续收，应按"文件 authority 的单一物理边界"重新设计，而不是改标签。
---

## 2026-09-12 第四班 — 落实 DELEG-029 未实现面：delegation 运行时改吃自有 capability port；`AgentFact` 包装回到 durable composition

基准：上班时 `master` = `44cb22259`（26/231/713/1834 refs，cycle=18，工作区干净）。

### 判据先行：条款要求与实现相反，且证明是空转

翻既有条款发现 `DELEG-029`（`requirements/delegation/WHAT.md:115`）明写「Host runtime、recovery、fork、fold与sync 只消费 delegation-owned append/query/wait/clock/PTY capability types」「durable composition 是把这些 case 包入 `AgentFact` 并组合 projection 的唯一位置」，而实现恰好相反：

- `delegation-recovery-runtime`（runtime 分片）形参直接是 persistence composition 的具体类 `AgentJournal`，调 `AgentJournal.appendAgent` / `AgentJournal.handleProjection`，并伸手进 `durable.Writer.BlobWriter.Read`（窥视外部实现内部）；
- `delegation-fold`（runtime 分片）持有把 delegation case 包成 `AgentFact` 的桥接模块 `Execution/Delegation/Fact.{fsi,fs}`；
- 该条款注册的证明 `m6-slice-boundary.test.mjs::WHAT[DELEG-029]` 只调用 `m6-boundary-proof.mjs` 的自证同义反复（校验器喂内联字面量，与仓库无关），等于没有证明。

### 本班做了什么

1. 新增 delegation 自有 contract 分片 `delegation-journal-port`：`AgentJournalPort` 四个成员（`AppendExecutionFact`/`HandleProjection`/`ReadBlob`/`WriteBlob`）——数量由 grep 穷举运行时对 journal 的全部使用得出，不多不少。
2. 新增 persistence composition 分片 `durable-journal-port-adapter`：`AgentJournalPortAdapter.fromAgentJournal` 实现 port（`AgentFact.Execution` 包装、失败文案展平、`WriteBlob` receipt 投影成身份对）；`Execution/Delegation/Fact.{fsi,fs}` 桥接位置不动但改由 `composition-durable-fact` 编译 → 「包装进 `AgentFact`」只剩 durable composition 一处。
3. 运行时（`Controller`/`JoinDrain`/`CompletionCodec`/`ChildRecoveryWorkflow` 及其 .fsi）改吃 port；边界分片（host-adapter 的 Agent/ChildDispatch/Join/RunLifecycle、runtime surface 的 Restart/JournalSurface、fission 的 `Execution/Fission/OpenCode/Host.fs`）在持有 journal 处适配一次再传入。
4. DELEG-029 证明换成仓库事实检查（源码禁用外部句柄/routing union、跨 subsystem 引用禁用 durable handle/codec、port 必须 delegation-owned contract、运行时必须消费 port），实测在旧树为红、新树为绿。

### 结构结果

| 口径 | 上班前 | 下班时 | 说明 |
|---|---|---|---|
| subsystems / shards / sources / refs | 26 / 231 / 713 / 1834 | 26 / **233** / **715** / **1842** | +2 分片（delegation port、durable 适配器）、+2 source；refs 净 +8（含 `durable-journal-port-adapter`、`delegation-journal-port` 传播与桥接消费者重定向） |
| runtime/pure/contract 分片 → 外部物理实现的带限定名符号使用 | 6 对 | **3 对**（+1 条命名空间前缀假阳性） | 干掉的是 journal 句柄对（112）、`AgentFact` 包装（100/33）与 `durable.Writer` 伸手 |
| 最大 subsystem SCC | 18 | 18 | 搬能力形状与所有权，不动合同边 |
| 分片 DAG | OK | OK | 中途出现过一次真实环（`persistence-journal-agentjournal → promptfactcodec → 新适配器 → agentjournal`），已把桥接从适配器分片移回 `composition-durable-fact` 解掉；这正是「环由共享 spine 产生」的实例 |

### 验证

- 12 个受影响分片 focused Fable compile 全过：`delegation-journal-port` `fp:f8b870e647c8`、`delegation-recovery-runtime` `57be944dc780`、`durable-journal-port-adapter` `ec5a7d9d9037`、`composition-durable-fact` `a9f085296b3e`、`delegation-fold` `e1888633d7ac`、`delegation-ledger` `e1afb3eb9cd8`、`persistence-journal-promptfactcodec` `52601a3d0fd9`、`durable-runtime-surface` `5a6816e5bf2f`、`persistence-journal-agentjournal` `bac3b7447dc2`、`delegation-host-adapter` `751952a513f7`、`delegation-runtime-surface` `9460ce2893ec`、`execution-fission-opencode-host` `f07f39d40f2b`
- `npm run build` 787 emitted modules（`js-surface-manifest` 165 surfaces closed）；`node scripts/check.mjs` 全绿（814 WHAT / 4016 tests / 0 proof gap）；verification **3973 passed / 0 failed**；integration 全过；e2e Long Stroke exit 0（journal 449/699，仅诊断口径）；`format:check`；`npm pack --dry-run` 2052 files；新 DELEG-029 证明单跑 2/2 绿。

### 过程教训

1. 新分片引用了外部类型时，先确认该类型声明在哪个分片：`StreamId` 其实声明在 `Persistence/Journal/Envelope.fs`（`persistence-journal-promptfactcodec`，composition），因此 port 改用 `SessionId`（`Foundation.Identity`）并让适配器负责 `StreamId.Session` 包装——顺带把 port 收得更窄。
2. `open FSharp.Control.Tasks.Affine` 在本仓不存在（`FSharp.Control.Tasks` 未引用）；`task { }` 由 FSharp.Core 提供，不需要 open。
3. 调用方若未 `open Wanxiangshu.Composition.Durable`，必须写全限定名（fission 的 `Host.fs` 即此类），不要为一行调用新增 open，以免命名污染。
4. 真实工作区里「分片环」往往由共享 spine 反向依赖引起：改 ref 前先算一整条回路（`subsystems.mjs` 只报一个环头，不报路径）。

### 下一班（均有符号证据）

1. **(主候选)** projection spine / fold 组合：`delegation-recovery-runtime` 与 `delegation-fold` 仍引用 `composition-durable-projection`（117/42 命中），因 `HandleProjection.*` 决策与 `AgentLinkageProjection` 类型编译在该 spine（`2ee76f2dc` 为打断 fold 环而移入）。闭合方式是让 aggregate fold 按域切片组合：spine 不再调用 delegation 决策、由 delegation fold 拥有 linkage 迁移；同一方案顺带解决 `delegation-fold` 调用 authority 的 `PromptAuthorityRun.closeCompletedAgentOwnerChildWork`（13 命中）。
2. DELEG-029 的 PTY 半句仍未实现：`Execution/Delegation/Fork/Host/Pty.fs` 扩展 `HostForkRuntime` 并使用 `Dictionary`，需要把 PTY 物理边界收成窄能力。
3. 上一班留的 delegation→persistence journal 家族已在本班收口（`AgentJournal`/`AgentFact` 两条对已消失），审计脚本可重跑核对。
---

## 2026-09-12 第五班 — 把 delegation 自有词汇从 persistence spine 收回 delegation contract 分片

基准：上班时 `master` = `d49df9d9f`（26/233/715/1842 refs，cycle=18，工作区干净）。

### 判据与病象

`requirements/delegation/WHAT.md` DELEG-028 写明 `Delegation.Contract` 拥有 presentation/协议词汇、`Delegation.Fold` 只实现纯投影；`DELEG-029` 要求 linkage/estimate 决策由 delegation-owned pure fold/decision 拥有。实现不是这样：

- `Execution/Delegation/LinkageProjection.{fsi,fs}`（`AgentLinkageProjection`、`HandleRecord`、`HandleLifecycle`、`module HandleProjection`）和 `Execution/Delegation/DelegatedToolEstimateProjection.{fsi,fs}` 是纯函数 + 纯数据类型（只开 `Wanxiangshu.Foundation(.Identity)`），却编译在 persistence composition 分片 `composition-durable-projection` 里；
- 原因是 `2ee76f2dc` 为打断 fold 环把 `LinkageProjection`/`FoldRejection` 放进 spine，并留下「durable projection spine must own LinkageProjection」的测试钉扎与「LinkageProjection owned by durable projection spine」的提交说明；
- 后果：delegation 运行时读自己领域的类型时，声明依赖指向 persistence composition；审计里 4 对「runtime/contract → 外部 composition」有 2 对是这条造成的。

### 本班做了什么

1. 新增 delegation contract 分片 `delegation-linkage-projection`（只引用 `identity`、`foundation-roles`、`delegation-sync-contract`）编译那 4 个文件；spine 去掉这 4 个 Compile 项，改为声明引用新分片。源码一字未动、路径未变，聚合 fsproj 无需修改。
2. 15 个消费者改用它声明的引用：其中 8 个删掉对 spine 的直接引用（`delegation-recovery-runtime`、`delegation-journal-port`、`delegation-ledger`、`delegation-host-adapter`、`execution-delegation-hostturnobservedsurface`、`execution-fission-opencode-host`、`execution-session-opencode-horizontool`、`git-integrationgate`），6 个真用 `AgentProjection`/`FoldRejection` 的保留。删除一律以 focused compile 为准；`delegation-ledger`、`delegation-host-adapter`、`git-integrationgate` 的「剩余使用」实为注释。
3. 证明更新：DELEG-028 的旧钉扎换成现行不变量（LinkageProjection 归 delegation contract 分片、spine 必须声明引用它），并把 `delegation-linkage-projection`、`delegation-journal-port` 纳入 contract ≤100 预算；DELEG-029 去掉 `pureProjectionSpine` 例外，改为「recovery runtime 的跨 subsystem 引用必须全部 contract」。

### 结构结果

| 口径 | 上班前 | 下班时 | 说明 |
|---|---|---|---|
| subsystems / shards / sources / refs | 26 / 233 / 715 / 1842 | 26 / **234** / 715 / **1852** | 文件只是换编译者，sources 不变；refs +10（15 加 − 8 删 + spine 1，含并行迁移中的读数） |
| 行为符号审计对 | 4（含 1 条命名空间假阳性） | **2** | 剩 `delegation-fold → composition-durable-projection`（27 处 `AgentProjection`/`FoldRejection`）与 `delegation-fold → authority`（13 处） |
| contract 分片 `delegation-journal-port` 闭包 | 110 `.fs` | **12** | spine 曾是唯一路径，删掉后整条 durable spine 离开它的闭包 |
| `delegation-linkage-projection` 闭包 | — | 11 | 新 contract 分片，纳入 ≤100 预算 |
| `delegation-recovery-runtime` 闭包 | 136 | 136 | **没变小**：它经 `delegation-fold` 仍到达 spine。直接引用变干净 ≠ 闭包变小，报告如实 |
| 最大 subsystem SCC | 18 | 18 | 不动合同边 |

### 验证

- 17 个受影响分片 focused Fable compile 全过：新分片 `90c60d3e539d`、spine `cf33987e49da`、recovery `bd638522d266`、port `a01660146c80`、fold `03de6db1395d`、ledger `b003f4f80cb8`、runtime-surface `c33a292d0812`、host-adapter `de98b6650008`、hostturnobserved `e0d413349f80`、fission host `d7ae0a9d40a9`、horizontool `64123db662aa`、recoveryclosure `c916e7a9d962`、pluginruntimescope `1656490f0e54`、git-integrationgate `f8ffca93e078`、chatadmission `e6309b868771`、agentjournal `ebcb55adef14`
- `npm run build` 787 emitted modules；`node scripts/check.mjs` 全绿（814 WHAT / 4016 tests / 0 proof gap）；verification **3973 passed / 0 failed**；integration 全过；e2e Long Stroke exit 0（journal 444/699，诊断口径）；`format:check`；`npm pack --dry-run` 2052 files；DELEG-028/029 单测 4/4 绿（含新断言）

### 过程教训

1. 「删掉声明依赖」和「闭包变小」是两件事：本班 8 个分片删了对 spine 的直接引用，但只有 spine 是唯一路径的 `delegation-journal-port` 闭包从 110 掉到 12。汇报时两个数字都要给，不然就是拿引用数冒充收益。
2. 纯数据类型移动时先查它的真实依赖：`HandleOwnership`/`HandleCompletionKind`/`HandleAbandonReason` 在 `Execution/Delegation/Facts.fs`（`delegation-sync-contract`）、`Role` 在 `Foundation/Roles.fs`（`foundation-roles`），所以新分片只需要 3 个引用就能独立编译。
3. 判断「某分片是否还需要一条引用」以编译为准：注释里出现 `AgentProjection` 之类的名字会让 grep 计数虚高。

### 下一班（均有符号证据）

1. **(主候选)** durable aggregate fold 按域切片：`delegation-fold` 仍直接引用 `composition-durable-projection`（27 处 `AgentProjection`/`FoldRejection`）并调用 authority 的 `PromptAuthorityRun.closeCompletedAgentOwnerChildWork`（13 处）。闭合需要让 aggregate fold 不再调用 delegation/authority 域的迁移、由各域 fold 拥有自己的迁移段；这也是让 `delegation-recovery-runtime` 闭包真正脱离 spine 的唯一路径。
2. DELEG-029 的 PTY 半句仍未实现：`Execution/Delegation/Fork/Host/Pty.fs` 扩展 `HostForkRuntime` 并使用 `Dictionary`，需要把 PTY 物理边界收成窄能力。
3. `persistence-journal-agentjournal` 仍是 60 个消费者的共享 spine，可在投影切片方案里一并评估。

