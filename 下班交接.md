# 下班交接

基准：本班开始时 `master` 在 `82d5c2513`（相对 `origin/master` ahead 1，工作区干净）。结束时已把本批结构剪枝与交接文档提交并推送 `master`。

## 本班做了什么

继续 GAP-033（STRUCTURED-WORKFLOW-011..016）下的跨 subsystem 声明依赖收敛：先只读审计稀疏边与符号消费，再按分片并行剪未使用的 `ProjectReference`，聚焦 Fable 编译验证，失败时改挂真实窄合同而不是恢复宽伞引用。

### 结构结果

| 口径 | 上班前（含未推送的 DistillationSurface 提交后） | 下班时 |
|---|---|---|
| subsystems | 26 | 26 |
| compile shards | 213 | 213 |
| production sources | 700 | 700 |
| ProjectReference | 1873 | 1846 |
| shard DAG | 无环 | 无环 |
| 最大 subsystem SCC | 22 | 22 |

相对 `1873` 净减 **27** 条声明引用。SCC 未缩小；GAP-033 保持 **PARTIAL**。事实记录已写入 `requirements/structured-workflow/HOW.md` §3.2。

### 主要剪枝（均经符号复核 + focused Fable compile）

1. `process/opencode-tools-ptytool` ← 去 `execution-delegation-fact`
2. `process/foundation-temporal` ← 去 syncdelegate-model／composition-durable-projection／journal-promptfactcodec／provider-language（4）
3. `repository-programming/opencode-tools-filemutationtools` ← 去 outcome／durabilityport／bounded-parallel（3）
4. `knowledge/repository-knowledge-casebook-model` ← 去 durabilityport；补 `host-fatal-effect` + `host-tool-adapter`（`FatalProcess`／`ToolHostCodec` 真实直接消费）
5. `relay/mission-relay-reviewtool` ← 去 sessionquiescencegate
6. `work/mission-obligation-todo-magictodosemanticsurface` ← 仅去 bounded-parallel（保留 host-digest）
7. `authority/interaction-authority-fold`、`enforcer/enforcer-continuation`、`enforcer/execution-session-loopdetector` ← 各去 outcome
8. `authority/interaction-repair-interactionrepair` ← 去 outcome／ingresscodec／fallback-ledger／requestkind（4）
9. `change/git-integrationgate` ← 去 handle-surface／durabilityport／sharedstatesurface／bounded-parallel／provider-language／node-timing-adapter（6）；清理 `Host.fs` 未使用 `open`；**保留**仍消费的 `foundation-llm-facing`
10. `strength/strength-opencode-settings` ← 去 managedagent／eventstore-storetypes
11. `strength/strength-prediction-predictor` ← 去 managedagent／outcome／strength-policy；删除 7 处无符号 `open Wanxiangshu.Host.Contract`

上班前已存在、本班一并推送的提交：`82d5c2513` 将 DistillationSurface 从误挂 companion runtime 收窄（1874→1873）。

### 明确拒绝／回滚的假阳性

- `context-prefix-wire` 的 outcome／host-digest：仍用 `AttemptOutcome`／`HostDigest`
- `magictodo`／`requirementgrounding` 的 host-digest：仍用 `HostDigest.sha256Hex`
- `provider-languagesurface` 的 foundation-identity：仍用 `SessionId.create`
- 取消中的 worker 曾写坏 `context-prefix-wire.fsproj`（重复 PropertyGroup／误删引用），已 `git checkout --` 还原，未纳入提交

## 验证

已运行：

- `node scripts/checks/subsystems.mjs` → OK，1846 refs，SCC=22
- `node --test requirements/structured-workflow/tests/subsystem-boundaries.test.mjs` → 4/4
- `node scripts/check.mjs` → 各静态 gate 绿；requirement-trace 仍报既有 4 个 proof gap（ENF-015／ENF-016），与本批无关
- 代表性 focused compile：ptytool、foundation-temporal、filemutationtools、casebook-model（修复后 274）、reviewtool、magictodo、fold／continuation／loopdetector、interaction-repair、git-integrationgate、strength-settings、strength-predictor 均 OK

未运行：

- 全量 `npm run build`／`npm run format-build-test`
- Long Stroke／正式 semantic suite 全量

## 剩余缺口（下一班）

1. **GAP-033 仍 PARTIAL**：22 节点 SCC 未拆；优先真实知识归属迁移（HOW §3 重力井：`foundation-roles`、Identity cohorts、provider projection、Host／Delegation／Persistence 多原因分片），不要只追引用计数。
2. 继续稀疏边审计时必须 focused compile；传递闭包碰巧带上的符号要改挂窄合同，禁止伪解耦（动态加载／tag／复制实现）。
3. ENF-015／ENF-016 证明缺口仍 OPEN（capability-enforcement），不在本轮结构授权内自动关闭。
4. 并行剪枝时注意同一 fsproj／Git index 冲突；半截编辑要先还原再继续。

## Git

- 分支：`master`
- 本班提交：结构剪枝 + HOW 更新 + 本交接文件（与先前 ahead 的 DistillationSurface 提交一并 push）
- 推送：`origin/master`（无 force）

---

## 下一班追加（接续 GAP-033）

基准：`bce851f5f`，工作区干净，1846 refs，SCC=22。

### 做了什么

继续只读审计未覆盖的稀疏 SCC 边与大型 composition／persistence 分片；多数 1–2 边为真实 KEEP。对确认零符号消费的引用并行剪枝，并清理残留无用 `open Wanxiangshu.Host.Contract`。

### 结构结果

| 口径 | 上班前 | 下班时 |
|---|---|---|
| ProjectReference | 1846 | **1834** |
| 最大 subsystem SCC | 22 | 22 |
| subsystems／shards／sources | 26／213／700 | 不变 |

相对 `1846` 净减 **12** 条声明引用。GAP-033 仍 **PARTIAL**。HOW §3.2 已同步。

### 本班剪枝

1. `resources/resources-promptsurface` ← `foundation-roles`
2. `delegation/execution-fission-fold` ← `persistence-journal-promptfactcodec`
3. `dispatch/plugin-composition` ← `opencode-tools-filemutationtools`
4. `persistence/persistence-journal-promptfactcodec` ← `execution-delegation-fact`／`context-trace-cursor`／`context-trace-model`（3）；删 `ProjectionUpdate.fs` 无用 Trace open
5. `session-lifecycle/opencode-host-pluginruntimescope` ← `delegation-recovery-runtime`
6. `context/context-companion-companionfactfold` ← `strength-policy`
7. `provider/composition-turn-ordinaryturnworkflow` ← outcome／identityseed／chatadmission-intent／context-trace-capture（4）
8. 清理 12+1 处无用 `open Wanxiangshu.Host.Contract`（不含仍用 `ToolResultBound` 的 ToolHostCodec／Casebook Surface）

### 验证

- `subsystems.mjs` → 1834 refs，SCC=22
- `subsystem-boundaries.test.mjs` → 4/4
- `check.mjs` → 绿（ENF-015／016 既有 proof gap 仍在）
- focused compile：promptsurface、fission-fold、plugin-composition、journal codec、pluginruntimescope、companionfactfold、ordinaryturnworkflow、managedagentconfig、hostsignalbootstrap 均 OK
- 未跑全量 build／Long Stroke

### 剩余缺口

1. SCC=22 未拆；稀疏剪枝收益递减，下一班应优先重力井知识归属迁移。
2. ENF-015／016 仍 OPEN。
3. 剪枝后若 focused compile 暴露传递闭包碰巧提供的符号，改挂窄合同，禁止伪解耦。

---

## 再下一班追加（接续 GAP-033 · SharedState 拆分）

基准：`c82dc213b`，工作区干净，1834 refs，SCC=22，213 shards。

### 做了什么

1. **真实知识归属迁移：** 将 `SharedState` 从混装的 `host/opencode-host-providerrunbinding` 抽到新建 `host/opencode-host-sharedstate`；providerrunbinding 去掉 companion 依赖，只保留 ProviderRunBinding／Surface；SharedState 消费方改挂新分片。聚焦编译后为 `sessionquiescencegate` 补窄合同 `foundation-taskresult`（原靠传递闭包碰巧提供）。
2. **零符号剪枝（+清理无用 open）：** 去误挂的 providerrunbinding／foundation-roles／managedagent／host-signal-contract（host-digest 名）／host-session-contract／outcome／chatadmission-intent／requestkind／async-support 等；foldsurface 的 PluginScope 删 3 处无用 Delegation／Wait open。

### 结构结果

| 口径 | 上班前 | 下班时 |
|---|---|---|
| compile shards | 213 | **214** |
| ProjectReference | 1834 | **1821** |
| 最大 subsystem SCC | 22 | 22 |
| subsystems／sources | 26／700 | 不变 |

相对 `1834` 净减 **13** 条声明引用。GAP-033 仍 **PARTIAL**。HOW §3.2 已同步。

### 验证

- `subsystems.mjs` → 214 shards，1821 refs，SCC=22
- `subsystem-boundaries.test.mjs` → 4/4
- `check.mjs` → 绿（ENF-015／016 既有 proof gap 仍在）
- focused compile：sharedstate、providerrunbinding、sharedstatesurface、pluginruntimescope、companion runtime、strength-opencode-settings、sessionquiescencegate、foldsurface、durabilityport、modelroutingsurface、chatexecution-fold、sessionstartedatledger、fallback-ledger、providerprojectionsurface、delegation-host-adapter、delegation-recovery-runtime 等 OK
- 未跑全量 build／Long Stroke

### 剩余缺口

1. SCC=22 未拆；继续重力井归属（Role↔Persona 分拆、ordinaryturnworkflow 编排归属、HostSignalBootstrap／DurabilityPort 多原因分片等），勿只追 ref 计数。
2. Identity／provider／Host-Delegation 审计已列出更多 move candidates，下一班择一可独立验证的归属迁移。
3. ENF-015／016 仍 OPEN。

### Git

- 分支：`master`
- 本班提交：SharedState 分片 + 剪枝 + HOW + 本交接
- 推送：`origin/master`（无 force）

---

## 再下一班追加（接续 GAP-033 · Persona 拆分与重力井剪枝）

基准：`cd0ad1b49`，1821 refs，SCC=22，214 shards。

### 做了什么

1. **知识归属迁移：** 新建 `participant-persona` 分片，将 Persona 词汇（`Participant/Persona/{Catalog,ManagedCatalog,Identity,Surface}.fs/.fsi`）从 `foundation-roles` 中拆出，精简 `foundation-roles` 至基础 Role 合同；36 个仅消费 Persona 的消费者改挂 `participant-persona`；`prefix-wire` 补齐直接 `participant-persona` 依赖。
2. **重力井与多余引用剪枝：**
   - `hostsignalbootstrap`：仅剪去未直接引用的 `provider-system-transform` 与 `host-tool-adapter` 2 条引用；审计确认 `enforcer-guidance-tip` 必须保留（KEEP，`PairProgrammingThoughtSurface` 直接消费其 `ThoughtTransform` 符号）。
   - `ordinaryturnworkflow`（OTW）：剪去 `context-companion-fact`、`interaction-authority-fact`、`execution-fission-fact` 3 个未直接引用的 fact。
   - `interaction-attention-fold`：剪去未直接引用的 `intra-participant-parallelism/foundation-parallel`。
   - `casebook-bookkeeper`：剪去未直接引用的 `durabilityport`。
   - `casebook-lifecyclesurface`：剪去未直接引用的 `durabilityport` 与 `persistence-journal-promptfactcodec`，改挂实际消费其 `RuntimePath` 类型的 `change-integration/change-fact`。

### 结构结果

| 口径 | 上班前 | 下班时 |
|---|---|---|
| compile shards | 214 | **215** |
| ProjectReference | 1821 | **1815** |
| 最大 subsystem SCC | 22 | 22 |
| subsystems／sources | 26／700 | 不变 |

相对 `1821` 净减 **6** 条声明引用，新增 1 个 persona compile shard。GAP-033 仍 **PARTIAL**。HOW §3.2 已同步。

### 验证

- `subsystems.mjs` → 26 subsystems，215 compile shards，700 sources，1815 refs，shard DAG，largest subsystem cycle=22
- `subsystem-boundaries.test.mjs` → 4/4 通过
- `check.mjs` → 绿（ENF-015／016 既有 proof gap 仍在）
- 各受影响分片均经符号复核与聚焦编译通过
- 未跑全量 build／Long Stroke

### 剩余缺口

1. 最大 subsystem SCC=22 仍未拆开，GAP-033 保持 PARTIAL。
2. 下一刀继续重力井物理分片剥离，优先推进 `DurabilityPort` 等关键分片，禁止将引用数下降写成 SCC 已拆。
3. ENF-015／016 仍 OPEN。

### Git

- 分支：`master`
- 本班提交：Persona 分片 + hostsignal/OTW/attention/casebook 剪枝与窄合同改挂 + HOW + 本交接
- 推送：`origin/master`（无 force）

---

## 再下一班追加（接续 GAP-033 · DurabilityPort 4 路拆分与 SyncDelegateRole 正名）

基准：`a8d51cff8`，26 subsystems，215 compile shards，700 sources，1815 refs，SCC=22。

### 做了什么

1. **DurabilityPort 物理 4 路拆分（本班主刀）：**
   - 拆解混装了 Strength 持久化、Durable Fold、EventStore 日志写入器及 EventStoreHandle 的 `durable-events.strength-persistence-durabilityport` 重力井；
   - 新建分片 `durable-events.composition-durable-fold.fsproj`（`HostFactFold.fs/.fsi`，`Fold.fs/.fsi`）；
   - 新建分片 `durable-events.persistence-journal-eventstorewriter.fsproj`（`JournalEventStoreBoot.fs/.fsi`，`EventStoreJournalWriter.fs/.fsi`）；
   - 吸收归并：将 `EventStoreHandle.fs/.fsi` 移入 `durable-events.persistence-eventstore-storetypes.fsproj`（将 `Store`、`Dispose`、`Create` 设为 public 以便跨分片可见）；
   - 精简 `strength-persistence-durabilityport.fsproj`，仅保留 Strength 持久化类型（`DurabilityPort`、`Store`、`Durability`），其自身的 ProjectReferences 从 31 降至 7；
   - 重定向 ~17 个原消费分片至细粒度窄分片（如 `runtime.fsproj`、`canonicalintegrator`、`foldsurface`、`context-compression.runtime`、`plugin-composition` 等）。
2. **SyncDelegateRole 知识所有权正名与解环：**
   - 将 `SyncDelegateRole` 与 `delegateRoleToAttachment` 从 `Execution/Delegation/SyncDelegate/Model.fs` 移至新建分片 `session-ontology.execution-session-syncdelegaterole.fsproj`（`Execution/Session/SyncDelegateRole.fs/.fsi`）；
   - 切断环路边：`session-ontology.execution-session-association` 不再反向引用 `syncdelegate-model`（改为引用 `syncdelegaterole`）；
   - 在 `Model.fs` 中将 `SyncDelegateRole` 保留为类型别名 `Wanxiangshu.Execution.Session.SyncDelegateRole`，保持完全向下兼容；
   - 更新 `scripts/checks/session-ownership-matrix.json` 中 SyncInspector 与 SyncCoder 的 evidencePath，指向真实所有者 `src/Wanxiangshu/Execution/Session/SyncDelegateRole.fs`。
3. **经符号复核的稀疏引用剪枝（共 16 条）：**
   - 9 条安全剪枝：涵盖 `delegation-sync-runtime`、`ingresscodec`（4 条）、`chatadmission`、`managed-chat-execution`、`strength-predictor`、`delegation-host-adapter`；
   - 3 条 durability 伴随剪枝：`foldsurface`（去 `composition-durable-fact`）、`context-compression.runtime`（去 `host-session-contract`）、`plugin-composition`（去 `foundation-outcome`）；
   - 1 条经审计明确保留（KEEP）：`fallback-ledger` 对 `ingresscodec` 的引用（因需要 `ModelRouting` 类型）；
   - 3 条 HostSignal Stage 1 剪枝：在 `opencode-host-hostsignalbootstrap.fsproj` 上切断 `foundation-outcome`、`interaction-authority-identityseed`、`execution-failure-model`（经符号核实零使用，聚焦编译 32.8s 通过）；
   - 清理 `PluginScope.fs` 中 5 处未使用的 Strength 命名空间 open。

### 结构结果

| 口径 | 上班前 | 下班时 |
|---|---|---|
| subsystems | 26 | 26 |
| compile shards | 215 | **218** (+3) |
| production sources | 700 | **701** (+1) |
| ProjectReference | 1815 | **1818** (+3 净增，新分片必要接线 vs 16 剪枝与 DurabilityPort 31→7) |
| shard DAG | 无环 | 无环 |
| 最大 subsystem SCC | 22 | 22 |

GAP-033 保持 **PARTIAL**。事实记录已写入 `requirements/structured-workflow/HOW.md` §3.2。

### 验证

- `node scripts/checks/subsystems.mjs` → 26 subsystems, 218 shards, 701 sources, 1818 refs, SCC=22，exit 0
- `node --test requirements/structured-workflow/tests/subsystem-boundaries.test.mjs` → 4/4 通过，exit 0
- `node scripts/check.mjs` → 全部门禁通过，exit 0（仅既有 ENF-015/016 证明缺口仍在）
- 全部 14+ 个受影响分片均通过 focused Fable 聚焦编译验证
- 未运行：全量 `npm run build`／Long Stroke

### 剩余缺口

1. 最大 subsystem SCC 保持为 22，GAP-033 仍为 PARTIAL。
2. HostSignal Stage 2（叶子 surface 抽取）与 Stage 3（PluginBoot 移至 plugin-composition）排期至下一班。
3. OTW（OrdinaryTurnWorkflow）子系统重新分类至 dispatch。
4. ENF-015/016 证明缺口仍为 OPEN。

### Git

- 分支：`master`
- 本班修改：DurabilityPort 4 路拆分 + SyncDelegateRole 正名与解环 + 16 条剪枝 + HOW + 本交接

---

## 再下一班追加（接续 GAP-033 · OTW 子系统重分类与 HostSignal Stage 2 叶子 Surface 拆分）

基准：`bca46531d`，26 subsystems，218 compile shards，701 sources，1818 refs，SCC=22。

### 做了什么

1. **OTW 子系统归属修正（重分类至 dispatch）：**
   - `composition-turn-ordinaryturnworkflow.fsproj`（OTW）作为 turn observation 与 dispatch 调度的核心编排器，原归属于 `provider` 子系统系历史遗留；
   - 将其 `<WanxiangshuSubsystem>` 声明从 `provider` 修正为 `dispatch`；
   - 消除跨子系统反向编排倒挂，聚焦编译通过，各上游下游引用关系更加自洽。
2. **HostSignal Stage 2 叶子测试 Surface 归属迁移：**
   - **ChatParamsSurface**：从 `host-boundary.opencode-host-hostsignalbootstrap` 中抽离，移入其天然归属且已包含 `ChatParamsHook` 的 `execution-model-routing.opencode-host-modelroutingsurface` 分片中；
   - **JoinResultRendererSurface**：从 `hostsignalbootstrap` 中抽离，移入拥有 `JoinResultRenderer` 实现的 `delegation.execution-delegation-hostturnobservedsurface` 分片中，并补齐直接依赖 `foundation-roles`；
   - **ExplicitResumeSurface**：从 `hostsignalbootstrap` 中抽离，移入已编译 `ExplicitSessionResume` 的 `host-boundary.opencode-host-fissionhostsurface` 分片中。
3. **HostSignalBootstrap 零符号 ProjectReference 剪枝（2 条）：**
   - 剪除 `hostsignalbootstrap.fsproj` 中经符号复核完全未使用的 2 条声明引用：
     * `Wanxiangshu.Owner.managed-chat-execution.execution-session-chatexecution-fold.fsproj`
     * `Wanxiangshu.Owner.dispatch-protocol.composition-turn-reconcilesurface.fsproj`

### 结构结果

| 口径 | 上班前 | 下班时 |
|---|---|---|
| subsystems | 26 | 26 |
| compile shards | 218 | 218 |
| production sources | 701 | 701 |
| ProjectReference | 1818 | **1817** (净减 1 条：剪枝 2 条，`hostturnobservedsurface` 补 1 条 `foundation-roles`) |
| shard DAG | 无环 | 无环 |
| 最大 subsystem SCC | 22 | 22 |

GAP-033 保持 **PARTIAL**。事实记录已同步写入 `requirements/structured-workflow/HOW.md` §3.2。

### 验证

- `node scripts/checks/subsystems.mjs` → 26 subsystems, 218 shards, 701 sources, 1817 refs, SCC=22，exit 0
- `node --test requirements/structured-workflow/tests/subsystem-boundaries.test.mjs` → 4/4 通过，exit 0
- `node scripts/check.mjs` → 全部门禁通过，exit 0（仅既有 ENF-015/016 证明缺口仍在）
- `node --test requirements/interaction-authority/tests/chat-params-hook.test.mjs` → 7/7 通过
- `node --test requirements/provider-projection/tests/join-result-renderer-entry-comment.test.mjs` → 2/2 通过
- 受影响分片聚焦编译（`node scripts/compile-owner.mjs`）验证均通过：
  * `composition-turn-ordinaryturnworkflow.fsproj`
  * `opencode-host-modelroutingsurface.fsproj`
  * `execution-delegation-hostturnobservedsurface.fsproj`
  * `opencode-host-fissionhostsurface.fsproj`
  * `opencode-host-hostsignalbootstrap.fsproj`
- 未运行：全量 `npm run build`／Long Stroke

### 剩余缺口

1. 最大 subsystem SCC 保持为 22，GAP-033 仍为 PARTIAL。
2. HostSignal Stage 3（PluginBoot / PluginHostWiring 等移至 plugin-composition）留待后续推进。
3. ENF-015/016 证明缺口仍为 OPEN。

### Git

- 分支：`master`
- 本班修改：OTW 重分类至 dispatch + HostSignal Stage 2 三个叶子 Surface 抽取 + 2 条零符号剪枝 + HOW + 本交接

---

## 再下一班追加（接续 GAP-033 · HostSignal Stage 3 与稀疏剪枝）

基准：`bccee6d39`，26 subsystems，218 compile shards，701 sources，1817 refs，SCC=22。

### 做了什么

1. **PairProgrammingThoughtSurface 归属迁移：**
   - 将 `PairProgrammingThoughtSurface.fs/.fsi` 从 `host-boundary.opencode-host-hostsignalbootstrap` 抽离移入 `guidance-delivery.enforcer-guidance-tip`；
   - 为 `enforcer-guidance-tip` 补充直接依赖 `semantic-trace.context-trace-model` 与 `runtime-platform.digest`；
   - HSB 剔除 PPT 专用依赖（`enforcer-guidance-tip`、`guidelineprojection`、`context-trace-model`、`companion-fact`、`companionfactfold`）；
   - 符号复核确认保留 `companion-runtimesurface`，供 `HostCompactionObserver` 使用。
2. **HostSignal Stage 3 插件接线迁移至 PluginComposition：**
   - 将 Plugin Cluster B（`PluginHost.fs/.fsi`、`PluginBoot.fs/.fsi`、`PluginHostWiring.fs/.fsi`、`PluginSessionWiring.fs/.fsi`、`PluginRecoveryWiring.fs/.fsi`）从 `hostsignalbootstrap` 整体移入 `opencode-plugin.plugin-composition`（位于 `BloggerChronicleText` 之后、`PluginTransforms` 之前）；
   - `plugin-composition` 补齐所需依赖（包括 `delegation-host-adapter`、`statictools`、`managedagent`、`opencodeport`、`opencodeport-adapter`、`wait-diagnostic-adapter`、`host-root-workspace-runtime`、`session-attachment-contract`、`foundation-temporal-contract`、`process-foundation-temporal`、`syncdelegate-model`）；
   - 剪除 `plugin-composition` 中未使用的 `git-hook-sync`；
   - HSB 彻底剔除 Cluster B 专用及多余引用（`syncdelegate-model`、`wait-*`、`opencodeport*`、`statictools`、`managedagent`、`session-attachment-contract`、`foundation-temporal`、`host-root-workspace-runtime`）；
   - HSB 依赖改挂 narrow 的 `host-boundary.host-root-workspace-contract`（仅满足 `IRootWorkspaceReader`）；
   - 至此 HSB 仅编译单一核心入口 `HostSignalBootstrap.fs/.fsi`，且 `plugin-composition` → `hostsignalbootstrap` 依赖方向严格保持。
3. **独立符号复核稀疏剪枝：**
   - `delegation.execution-delegation-hostturnobservedsurface`：剪除 7 条未使用的 ProjectReference（`eventstore-surface`、`durable-fold`、`identityseed`、`handle-surface`、`wait-completion-mailbox`、`host-digest`、`ingresscodec`）；
   - `managed-session-lifecycle.opencode-host-pluginruntimescope`：剪除未使用的 `sharedstatesurface`；
   - `speculative-investigation.strength-opencode-settings`：剪除未使用的 `sessionquiescencegate`。

### 结构结果

| 口径 | 上班前 | 下班时 |
|---|---|---|
| subsystems | 26 | 26 |
| compile shards | 218 | 218 |
| production sources | 701 | 701 |
| ProjectReference | 1817 | **1809** (净减 8 条) |
| shard DAG | 无环 | 无环 |
| 最大 subsystem SCC | 22 | 22 |

GAP-033 保持 **PARTIAL**。事实记录已同步写入 `requirements/structured-workflow/HOW.md` §3.2。

### 验证

- `node scripts/checks/subsystems.mjs` → 26 subsystems, 218 shards, 701 sources, 1809 refs, SCC=22，exit 0
- `node --test requirements/structured-workflow/tests/subsystem-boundaries.test.mjs` → 4/4 通过，exit 0
- `node scripts/check.mjs` → 全部门禁通过，exit 0（仅既有 ENF-015/016 证明缺口仍在）
- 受影响 6 个分片全部通过验证
- 未运行：全量 `npm run build`／Long Stroke

### 剩余缺口

1. 最大 subsystem SCC 保持为 22，GAP-033 仍为 PARTIAL。
2. ENF-015/016 证明缺口仍为 OPEN。

### Git

- 分支：`master`
- 本班修改：6 个 fsproj（HostSignal Stage 3、PPT 迁移、稀疏剪枝）+ HOW.md + 本交接
- 提交并推送至 `origin/master`（无 force）

---

---

## 再下一班追加（接续 GAP-033 · 剥离 output 出环、RuntimeResources 抽取与零符号剪枝）

基准：`081aabc36`，26 subsystems，218 compile shards，701 sources，1809 refs，SCC=22。

### 做了什么

1. **剥离 `output` 子系统出环（SCC 22→21）：**
   - 将 `output-distillation.process-largegatesurface.fsproj` 的 `WanxiangshuSubsystem` 归属从 `output` 修正为 `process`（`WanxiangshuSemanticOwner` 保持 `output-distillation` 不变）；
   - 原先唯一导致 cycle 的 `process/opencode-tools-ptytool` → `output/process-largegatesurface` 边转变为 `process` 子系统内部引用；
   - `output` 子系统入度降为 0，成功从全局最大强连通分量中剥离，最大 subsystem SCC 成功由 22 降至 21；
   - 同步更新 `scripts/checks/authority-contracts.json` 中 `RequirePermit DistillationRuntime` 的 owner 为 `process`。
2. **Rank-2 resources 细粒度抽取：**
   - 新建分片 `cognitive-environment.resources-runtimeresources.fsproj`（归属 `resources` 子系统），承接从 `distribution.resources-enforcercatalogresource` 抽出的 `RuntimeResources.fs/.fsi` 编译；
   - 重定向 6 个仅需 `RuntimeResources` 的消费分片（`promptsurface`、`enforcer-repair`、`enforcer-guidance-tip`、`opencode-host-managedagentconfig`、`opencode-tools-executortoolsurface`、`strength-opencode-settings`），切断其对 `resources-enforcercatalogresource` 的引用；
   - 切断了 `promptsurface` → `enforcercatalogresource` 命名分片依赖，并消除了 `strength` → `enforcer` 经由 settings 的唯一跨子系统引用；
   - 为同时使用两者的 `provider-system-transform.fsproj` 补齐对 `resources-runtimeresources` 的显式引用；
   - 剩余遗留：`resources` ↔ `enforcer` 仍通过 `runtimeresources` → `enforcer-catalog*` 相互依赖；`resources` → `provider` 仍通过 `provider-language` 存在跨子系统引用。
3. **经严格符号复核的零符号剪枝（共 6 条）与格式收敛：**
   - `strength-persistence-durabilityport`：剪除未使用的 `foundation-outcome`；
   - `composition-turn-ordinaryturnworkflow`：剪除未使用的 `execution-session-recovery-model`；
   - `opencode-host-hostsignalbootstrap`：剪除未使用的 `wait-diagnostic-adapter`、`composition-durable-fact`、`session-attachment-runtime` 3 条引用；
   - `repository-knowledge-casebook-model`：剪除未使用的 `foundation-roles`；
   - KEEP 保留确认：`opencode-host-modelroutingsurface` 对 `fissionhostsurface` 仍需消费 `ExplicitResumeSuppression`，确认保留；
   - 格式清理：收敛 `modelroutingsurface` 与 `enforcercatalogresource` 的紧凑单行 XML 格式。
4. **后续设计就绪项：**
   - Rank-3（Language → runtime-platform）与 AgentJournal split 仅完成设计，未实施修改，留待下一班推进。

### 结构结果

| 口径 | 上班前 | 下班时 |
|---|---|---|
| subsystems | 26 | 26 |
| compile shards | 218 | **219** (+1) |
| production sources | 701 | 701 |
| ProjectReference | 1809 | **1809** (持平：新建分片依赖与剪枝相抵) |
| shard DAG | 无环 | 无环 |
| 最大 subsystem SCC | 22 | **21** (output 已剥离出环) |

GAP-033 保持 **PARTIAL**。事实记录已同步写入 `requirements/structured-workflow/HOW.md` §3.2。

### 验证

- `node scripts/checks/subsystems.mjs` → 26 subsystems, 219 shards, 701 sources, 1809 refs, SCC=21，exit 0
- `node --test requirements/structured-workflow/tests/subsystem-boundaries.test.mjs` → 4/4 通过，exit 0
- `node scripts/check.mjs` → 全部门禁通过，exit 0（仅既有 ENF-015/016 证明缺口仍在）

### 剩余缺口

1. 最大 subsystem SCC 保持为 21，GAP-033 仍为 PARTIAL。
2. `resources` ↔ `enforcer` 及 `resources` → `provider` 环路仍存。
3. Rank-3（Language → runtime-platform）设计就绪但单凭自身无法解环；AgentJournal split 设计就绪（约 52 处 retarget），留待后续实施。
4. ENF-015/016 证明缺口仍为 OPEN。

### Git

- 分支：`master`
- 本班修改：15 个已改文件 + 1 个新建 fsproj + HOW.md + 本交接
- 提交并推送至 `origin/master`（无 force）

---

## 再下一班追加（接续 GAP-033 · Rank-3 Language 下沉、AgentJournal 分拆、连续解三环至 SCC=18 与安全剪枝）

基准：`68ab2a8cc`，26 subsystems，219 compile shards，701 sources，1809 refs，SCC=21。

### 做了什么

1. **Rank-3 Language 合同与资源读取下沉到 runtime-platform：**
   - 新建 `runtime-platform.language.fsproj`（归属 `runtime-platform`，仅依赖 `distribution.assemblyinfo.fsproj`），承接 `Language.fs/.fsi` 与 `ProviderResources.fs/.fsi`；
   - `participant-provider-language` 移出上述 4 个源码文件，并引用 `runtime-platform.language`；
   - 将 `resources-promptresources`、`resources-runtimeresources`、`resources-enforcercatalogresource` 全部改挂 `runtime-platform.language`，彻底斩断 `resources → provider` 知识边；保留 `languagesurface` 对 `promptresources` 的反向真实测试消费。
2. **AgentJournal 运行时句柄独立分片拆分（治理持久化最大重力井）：**
   - 新建 `persistence-journal-agentjournal.fsproj`（归属 `persistence`），独立编译 `Writer.fs/.fsi`、`AgentJournal.fs/.fsi`、`SharedAgentJournal.fs/.fsi`，声明 9 条直接最小依赖；
   - `persistence-journal-promptfactcodec` 剔除上述 6 个文件，保留事实编解码及投影状态；
   - 严格审查 64 个消费分片：51 个仅需句柄的工程改挂 `persistence-journal-agentjournal`，4 个编解码/折叠工程保留 `promptfactcodec`，9 个双重消费分片挂载双依赖；消除了数十个下游对 FactCodec 内部细节的无谓依赖。
3. **剥离 `resources` 子系统出环（SCC 21→20）：**
   - 结合 Rank-3 切断 `resources → provider` 的成果，将组合 EnforcerRule 与审计资源的 `resources-runtimeresources.fsproj` 以及对 JS 暴露的 `resources-promptsurface.fsproj` 的 `WanxiangshuSubsystem` 修正归入 `enforcer`；
   - `resources` 在环内出度降为 0，成功剥离出全局最大环（SCC 21→20）。
4. **剥离 `repository-investigation` 子系统出环（SCC 20→19）：**
   - 将作为 ToolSpec 装配的 `opencode-tools-inspectortool.fsproj` 重分类至 `interaction`（`action-affordance` 工具面）；
   - 将已无领域依赖的 `participant-provider-language.fsproj` 重分类至 `runtime-platform`；
   - `repository-investigation` 仅保留 `semble-mcp` 纯接收汇，在 SCC 内出度归 0，成功剥离出环（SCC 20→19）。
5. **剥离 `repository-programming` 子系统出环（SCC 19→18）：**
   - 将其 3 个工具与运行时分片（`opencode-tools-codertool`、`opencode-tools-filemutationtools`、`runtime`）的子系统声明重分类至 `interaction`（其中 `codertool` 所有者设为 `action-affordance`）；
   - `repository-programming` 仅保留 `js-capability` 纯汇分片，在 SCC 内出度归 0，成功剥离出环（SCC 19→18）。
6. **经严格符号复核的零符号安全剪枝（5 条）：**
   - `composition-durable-fold`：剪除 `context-companion-facts` 与 `execution-fission-facts`；
   - `opencode-tools-bookkeepertool`：剪除 `foundation-roles`；
   - `casebook-lifecyclesurface`：剪除 `persistence-eventstore-storetypes`；
   - `execution-session-recovery-model`：剪除 `host-digest` 与 `foundation-outcome`；
   - 符号复核 KEEP 确认：`repository-programming.runtime` 保留 `host-fatal-effect`（`TransactionStore` 依赖 `FatalProcess`）；`provider-system-transform` 保留 `enforcercatalogresource`（依赖 `composeBloggerSystemPromptFor`）。

### 结构结果

| 口径 | 上班前 (`68ab2a8cc`) | 下班时 | 净变化 |
|---|---|---|---|
| subsystems | 26 | 26 | 0 |
| compile shards | 219 | **221** | +2 (`runtime-platform.language` + `persistence-journal-agentjournal`) |
| production sources | 701 | 701 | 0 |
| ProjectReference | 1809 | **1824** | +15 (新增 2 个分片前置依赖及双依赖消费方，抵扣剪枝后净增) |
| shard DAG | 无环 | 无环 | 保持严格无环 |
| 最大 subsystem SCC | 21 | **18** | **-3** (连续剥离 resources、repository-investigation、repository-programming) |

GAP-033 保持 **PARTIAL**。事实记录已同步写入 `requirements/structured-workflow/HOW.md` §3.2。

### 验证

- `node scripts/checks/subsystems.mjs` → 26 subsystems, 221 shards, 701 sources, 1824 refs, SCC=18，exit 0
- `node --test requirements/structured-workflow/tests/subsystem-boundaries.test.mjs` → 4/4 通过，exit 0
- `node scripts/check.mjs` → 全部门禁通过，exit 0（仅既有 ENF-015/016 证明缺口仍为 OPEN）
- 全部受影响的定向分片聚焦编译均验证通过
- 未运行：全量 `npm run build`／Long Stroke

### 剩余缺口

1. 最大 subsystem SCC 保持为 18，GAP-033 仍为 PARTIAL。
2. 下一刀候选已就绪：`relay`（reviewtool / suicidetool 迁移工具面）、`strength` 或 `requirements`。
3. ENF-015/016 证明缺口仍为 OPEN。

### Git

- 分支：`master`
- 本班修改：74 个已改文件 + 2 个新建 fsproj + HOW.md + 本交接
- 提交并推送至 `origin/master`（无 force）

---

---

## 再下一班追加（接续 GAP-033 · 剥离 relay 与 requirements 出环至 SCC=16、零符号剪枝与漏报依赖补齐）

基准：`d140e9503`，26 subsystems，221 compile shards，701 sources，1824 refs，SCC=18。

### 做了什么

1. **剥离 `relay` 子系统出环（SCC 18→17）：**
   - `Mission/Relay/OpenCode/` 三个宿主集成分片的 `WanxiangshuSubsystem` 移出 relay：`mission-relay-reviewtool`、`mission-relay-suicidetool` 归 `interaction`（ToolSpec 可供性，由 capability-enforcement.opencode-host-managedagentconfig 消费）；`mission-relay-narrative-transform` 归 `dispatch`（plugin transform 阶段，由 opencode-plugin.plugin-composition 调用；与既有 `relay-retirement.composition-turn-workflow` 归 dispatch 的先例一致）。
   - relay 只余领域核心（Contract/Fold/Assessment/Projection/Retirement/WorkspaceSnapshot），SCC 内出度归 0。
   - 只改现行权威字段 `WanxiangshuSubsystem`；legacy `WanxiangshuSemanticOwner`/locality 保持原值（STRUCTURED-WORKFLOW-016：legacy 字段只作兼容读取，不参与 verdict）。
2. **剥离 `requirements` 子系统出环（SCC 17→16）：**
   - `requirement-grounding.host-fact` → `persistence`（durable host fact 词汇与 grounding projection state，被 composition-durable-fact/-fold/-projection 与 promptfactcodec 消费）；
   - `requirement-grounding.opencode-host-requirementgrounding-runtime` → `interaction`（面向模型的 grounding runtime/gate/transform 及 Surface，由 managedagentconfig 与 plugin-composition 消费）；
   - requirements 只余纯 model/catalog 分片，SCC 内出度归 0。
3. **独立编译暴露的漏报依赖补齐（2 处，均为改动前既存）：**
   - `mission-relay-narrative-transform` 独立编译缺 `PromptDispatcher` → 补 `dispatch-protocol.dispatch-runtime`，并补直接使用的 `interaction-authority-fact`、`interaction-authority-ledger`；
   - `context-companion-fact` 独立编译缺 `LlmFacing`（`Context/Companion/Blogger/Toml.fs/.fsi`）→ 补 `dispatch-protocol.foundation-llm-facing`；
   - 两者补齐后独立 Fable 编译通过（fingerprint `2087d81b6d49`、`3407b794d9e9`）。subsystem 标签不参与编译，这些失败在改动前就存在，只有 standalone focused compile 能暴露。
4. **经符号复核的零符号剪枝（4 条）与引用移位（1 条）：**
   - `repository-programming.runtime` 的 `host-fatal-effect` 引用零使用（provider 公共符号 `FatalProcess`/`kill`/`trip` 逐名核对），真实消费者 `opencode-tools-filemutationtools`（`TransactionStore.fs` 两处 `FatalProcess.trip`）反而漏报 → 引用从前者移除、补到后者；
   - 剪除 `interaction-authority-ledger`、`delegation-sync-runtime`、`execution-delegation-syncdelegate-model` 三条 `participant-persona` 引用（以 `.fsi` 公共符号逐名核对为零使用；命中的 `role`/`roleLabel`/`origin` 均为消费方本模块自有符号）；
   - 五个受影响分片独立编译全部通过（`077c60862481`、`4c54f590ce87`、`e10c2f546bda`、`0566c6359dfd`、`efb267cc6a0e`）。
5. **审计方法警示（零符号审计刀部分误报）：**
   - 审计刀 7 个候选中 `mission-workrecord-materialize-runtime → host-digest` 为误报（`Materialize.fs` 实际 3 处 `HostDigest` 使用，且其 provider 描述错误）；本批所有剪枝都在 provider `.fsi` 公共符号表上逐名复核后才执行，低置信 2 项未采纳。
   - 结论：零符号候选必须以 provider 公共签名逐名核对，不能只按 `.fs` 文本命中判断。

### 结构结果

| 口径 | 上班前 (`d140e9503`) | 下班时 | 净变化 |
|---|---|---|---|
| subsystems | 26 | 26 | 0 |
| compile shards | 221 | 221 | 0 |
| production sources | 701 | 701 | 0 |
| ProjectReference | 1824 | **1825** | +1（-4 零符号剪枝，+1 真实消费者补齐，+1 漏报依赖补齐，+3 漏报闭包补齐） |
| shard DAG | 无环 | 无环 | 保持严格无环 |
| 最大 subsystem SCC | 18 | **16** | **-2**（relay、requirements 出环） |

GAP-033 保持 **PARTIAL**。事实记录已同步写入 `requirements/structured-workflow/HOW.md` §3.2（第 8 条）。

### 验证

- `node scripts/checks/subsystems.mjs` → 26 subsystems, 221 shards, 701 sources, 1825 refs, SCC=16，exit 0
- `node --test requirements/structured-workflow/tests/` → 264/264 通过，exit 0
- 受影响行为套件（relay-context-projection / relay-assessment / relay-incumbency / relay-retirement / requirement-grounding）→ 55/55 通过，exit 0
- `node scripts/check.mjs` → 全绿，exit 0（仅既有 ENF-015/016 proof gap）
- 独立 Fable 编译：relay 三片（`198b1daa1d4c`、`30c42de88f7d`、`2087d81b6d49`）、requirements 两片（`39458fa41d13`、`f60386db1274`）、剪枝/移位五片（见上）全部 exit 0
- 未运行：全量 `npm run build`、Long Stroke、全表独立编译普查（见下）

### 剩余缺口

1. 最大 subsystem SCC=16，GAP-033 仍为 PARTIAL；下一刀候选 `process`、`work`、`strength`，但都需真实知识边界（停止用改标签冒充，见 AGENTS.md 警告）。
2. **独立 compile closure 尚未系统闭合（本班给出精确清单）**：本批先实证两处漏报依赖（narrative-transform、context-companion-fact）并修复；随后对全部 221 个 production shard 各跑一次 standalone focused compile（并发 6，702s）：**213 通过、8 失败**，全部 missing-declaration 且均为本批前既存——`change-facts`（`Mission`/`WorkspaceSnapshotId`/`QualityCertificateId`）、`delegation-pty-adapter`（`Strength`）、`dispatch-model-contract` 与 `session-attachment-runtime`（`Host`，同源 `Composition/Turn/{Program,Observation}.fs`）、`sphinx-host-adapter` 与 `sphinx-runtime`（`Foundation`/`CanonicalJson`）、`eventstore-merge-runtime`（`CanonicalEventCodec`）、`repository-investigation-semble-mcp`（`TaskValue`）。修复方式按本班先例：为每个直接使用的符号补最窄 provider 引用并重跑该分片独立编译，禁止动态回捞或复制实现。
3. ENF-015/016 证明缺口仍为 OPEN。

### Git

- 分支：`master`
- 本班修改：10 个 fsproj + HOW.md + 本交接
- 提交并推送至 `origin/master`（无 force）

---

## 再下一班追加（接续 GAP-033 · 全表独立编译闭包补全 8/8、sphinx 重力井拆分；SCC 16→17 为真相值）

基准：`1c82aee29`（26 subsystems，221 shards，701 sources，1825 refs，SCC=16）。

### 做了什么

1. **关闭上一班普查的 8 个 missing-declaration 分片（逐个 focused compile 通过）**：
   - 真实漏报 → 补最窄 provider：`change-facts`（补 `relay-incumbency.mission-relay-core`）、`durable-convergence.eventstore-merge-runtime`（补 `persistence-eventstore-processeventlog`）、`repository-investigation.repository-investigation-semble-mcp`（补 `foundation-taskresult`）。
   - 经编译证明的遗留 open 清理（**不加引用**，删除死 open）：`Execution/Delegation/Fork/Host/Pty.fs:19` 的 `open Wanxiangshu.Strength`、`Composition/Turn/{Program,Observation}.fs` 的 `open Wanxiangshu.Host`、`OpenCode/Host/SphinxMcpConfig.fs:8` 的 `open Wanxiangshu.Foundation`。
   - 全部 fingerprint：`6d658993f69e`、`5ba3c992efa8`、`a633203104c0`、`c44ce09283ea`、`0806e4a5c24c`、`a37a61737881`、`de6882e1831b`。
2. **sphinx 重力井拆分（93 文件单体分片 → 纯领域内核 + 服务/持久化集成）**：
   - 新建 `Wanxiangshu.Owner.epistemic-reasoning.sphinx-serve-runtime.fsproj`，承接 `Sphinx/{GenericDurability,McpServer,Surface}.{fsi,fs}` 共 6 个文件；领域分片只保留 `sphinx-mcp`/`runtime-platform.digest`/`foundation-canonical-json`/`foundation-identity`/`sphinx-event-vocabulary-contract`。
   - 触发原因（证据）：只有这 6 个文件需要 persistence 的 `EventEnvelope`（`GenericDurability.fs:27-31`）、`IEventStore`/`AppendError`（`McpServer.fsi:8-10`）、`CanonicalIntegrator.create`（`McpServer.fs:1221`）、`EventStore.createLocal`（`McpServer.fs:1224`）；而 persistence 的 `Persistence/EventStore/CanonicalIntegrator.fs` 反向持有 sphinx 知识（`open Wanxiangshu.Sphinx`、`SphinxIntegration`/`SphinxGenericIntegration` 规则、第 253-254 行注册；`EventVocabulary.fs:36-40` 枚举 5 个 `SphinxEventTypes.*`）。`scripts/lib/owner-compile.mjs` 拒绝 shard 级 ProjectReference 环，**直接补声明不可行，必须先拆**。
   - 编译证据：`sphinx-runtime` `274675fa13b3`、`sphinx-serve-runtime` `db6978431264`、消费方 `canonicalintegrator` `1f62c475c962`。
3. **零符号剪枝 7 条（编译证明）**：`host-boundary.opencode-host-hostsignalbootstrap` 在 `081aabc36` 之后只编译 `HostSignalBootstrap.{fsi,fs}`，残留引用逐名核对 0 命中后剪除（causal-wait 合同/运行时、execution-delegation-fact、persistence-eventstore-surface、interaction-authority-ledger、casebook-model、casebook-bookkeeper），独立编译通过（fp `1d7c626c8102`）。
4. **五个只读设计（下一批实施输入，证据已写进 HOW §3.2 第 9 条）**：`process`、`strength`、`work`、`knowledge` 的剥离设计 + `sphinx↔persistence` 倒置设计。共同结论：剩余 17 元环的每条入边都对应**真实知识依赖**，没有靠改标签可得的诚实剥离；需要 capability 注入、规则注入/依赖倒置、归属裁决这类真实重构。

### 结构结果

| 口径 | 上班前 (`1c82aee29`) | 下班时 | 净变化 |
|---|---|---|---|
| subsystems | 26 | 26 | 0 |
| compile shards | 221 | **222** | +1（sphinx-serve-runtime） |
| production sources | 701 | 701 | 0 |
| ProjectReference | 1825 | **1833** | +8（6 条真实漏报声明 + 9 条新分片引用 − 7 条零符号剪枝） |
| shard DAG | 无环 | 无环 | 保持严格无环 |
| 最大 subsystem SCC | 16 | **17** | +1，**且 17 是真相** |

**SCC 16→17 的性质**：16 建立在 `sphinx-runtime` 声明闭包不完整之上（该分片当时无法独立编译、真实依赖未声明）。补全后 sphinx 与 `host`/`interaction`/`persistence` 双向可达（`host-boundary.sphinx-host-adapter`→`sphinx-mcp`、`action-affordance.opencode-tools-statictools`→`sphinx-mcp`、`sphinx-serve-runtime`→persistence 都是真实引用）。**含义：本仓此前的 SCC 数字只在闭包完整时才有意义；17 从现在起是可信基线，后续任何下降都必须同时给出闭包完整证据。**

### 验证

- `node scripts/checks/subsystems.mjs` → 26 subsystems / 222 shards / 701 sources / 1833 refs / shard DAG / cycle=17，exit 0
- `node --test requirements/structured-workflow/tests/` → 264/264，exit 0
- `node scripts/check.mjs` → 全绿 exit 0（仅既有 ENF-015/016 proof gap）
- 本班 focused Fable compile 共 12 条，全部 exit 0（见上）
- 全表 standalone 复扫：冷缓存吞吐约 3 shard/min，跑至 **41/222 全部通过、0 失败** 时为准时收班取消；**未完成项**=全表 222 分片复扫（不得宣称已完成）
- 未运行：`npm run build`、`npm run format-build-test`、Long Stroke、dist-backed 全量语义套件（与既往班次同口径）

### 剩余缺口

1. 最大 subsystem SCC=17（真相基线）。下一批实施项（HOW §3.2 第 9 条末段）：`work` 的 capability 注入与 MagicTodo 归属裁决（14 条入边）、`knowledge` 的 BookkeeperRuntime 领域纯化 + ToolRegistry/CanonicalIntegrator 规则倒置（9 条入边，其中 3 条 hostsignalbootstrap 残留已剪）、`strength` 的 durabilityport 归属与 PluginRuntimeScope 解耦、`process` 的 Pty 反向引用与 time-capability 分片归属、sphinx↔persistence 规则注入倒置。
2. 全表 standalone 复扫未完成（41/222 全过）。
3. `knowledge-reuse.runtime`（`casebook-runtime-surface`，6 个 Surface 文件）在生产源码 0 引用、仅 JS 契约测试经 dist 路径加载——需确认是否仍在交付面，不得凭此删除。
4. ENF-015/016 证明缺口仍 OPEN。

### Git

- 分支：`master`；本班修改：4 个源文件（删遗留 open）+ 6 个 fsproj（含 1 个新增）+ HOW.md + 本交接
- 提交并推送至 `origin/master`（无 force）


## 再下一班追加（接续 GAP-033 · work/strength/process 知识剥离落地；全表独立编译复扫 222/222；SCC 仍为 17）

基准：`fbc9b5076`（26 subsystems，222 shards，701 sources，1833 refs，SCC=17）。

### 做了什么

1. **work-record 物化注入**：Change Host / SyncDelegate / ToolRuntimeScope 不再就地调用 `LifecycleWorkRecordProjection`；组合层与 JS 入口（`SyncDelegate.Surface.create`、`Fork/OpenCode/ToolSurface.createRuntime`）显式注入真实能力。行为回归（EXEC-031）证明：delegation JS 运行时表面确实组合 work-record——该声明依赖保留。
2. **strength 窄端口 + durabilityport 归属纠正**：ToolRegistry `isReplicaSession`；PluginRuntimeScope 去掉 `member Strength`（改中性钩子）；HostTurnObserver/HostSessionDeletion/Wire/Speculate 改为端口/直接接收；`strength-persistence-durabilityport` 标签 `persistence→strength`。恢复被静默掉的 `Diagnostic.fatal "strength-semantic-cut"`（DURABLE-EVENTS-021）。
3. **process Pty 中性化**：`PtyExitEvent` + `exitListeners`；delegation 侧映射回 `PtyJoinItem`；剪除 process→delegation 三条引用；物理 listener 登记表同步。
4. **零符号剪枝 / 真实 provider 重定向 5 条**（含跨子系统 dispatch→context、work→host）。
5. **全表 standalone 复扫完成**：222/222 全过、0 失败（关闭上一班未完成项）。
6. **契约测试迁移**：pty-port、dry-run-shadow、durable-events semantic-cut、host-boundary/time-capability m6（后两者为基准已失败的期望漂移，本班诚实收口）、workflow-constitution 物理 listener。

### 结构结果

| 口径 | 上班前 (`fbc9b5076`) | 下班时 | 净变化 |
|---|---|---|---|
| subsystems | 26 | 26 | 0 |
| compile shards | 222 | 222 | 0 |
| production sources | 701 | 701 | 0 |
| ProjectReference | 1833 | **1820** | −13 |
| shard DAG | 无环 | 无环 | 保持 |
| 最大 subsystem SCC | 17 | **17** | 0（预期：本班切的是偶然就地耦合，不是反馈弧） |

subsystem 对级入边（节选）：`work` 6→4、`strength` 5→3；`session-lifecycle` 出边 14→12、`context` 出边 12→11、`interaction` 出边 19→18；`host` 出边 14→15（HostSignalBootstrap 显式装配 strength——下一步可上移到 plugin-composition）。

### 验证

- `node scripts/checks/subsystems.mjs` → 26/222/701/1820，cycle=17，exit 0
- `node --test requirements/structured-workflow/tests/` → 264/264
- `npm run build` → exit 0（impact 1402 items）
- `node requirements/verification-system/tests/run.mjs` → **3970 passed / 0 failed**，exit 0
- `node scripts/check.mjs` → 全绿 exit 0（仅既有 ENF-015/016 proof gap）
- 全表 standalone 复扫：222/222 全过
- 未运行：`npm run format-build-test`、Long Stroke

### 剩余缺口

1. 最大 subsystem SCC=17。下一批：`CanonicalIntegrator`/`EventVocabulary` 领域规则倒置（设计已完成，见 HOW §3.2 第 10 条）；MagicTodo 归属；`BookkeeperRuntime` 宿主调度抽出；HostSignalBootstrap→strength 闭包上移到 plugin-composition。
2. ENF-015/016 证明缺口仍 OPEN。
3. `knowledge-reuse.runtime`（casebook-runtime-surface）仍为 JS 契约交付面，勿删。

### Git

- 分支：`master`；本班修改：~50 个源/fsproj/测试/登记表文件 + HOW.md + 本交接
- 提交并推送至 `origin/master`（无 force）

