# 人工巡检：从一个 Coder 到完整协作

编制日期：2026-09-13。调查基线：c079a8fcd。当前 requirements/ 下有 56 个含 WHAT.md 的包；下文每包安排一次主审。这是巡检路线，不是已完成记录，也不是新的产品规范。

## 1. 推荐路线

先让一个 Coder 在真实 Host 中完成一件小事，再逐步增加运行能力、记忆、故障、协作、管理与发布。不要第一天就让 Manager 带着所有机制跑一个大任务：一旦不对，既不知道是哪层错，也分不清是用户根本不喜欢这个设计，还是代码没有实现它。

| 段落 | 主审序号 | 增加什么 | 主要面对用户的角色 |
|---|---|---|---|
| A．校准判断 | 01—04 | 谁决定产品意志，什么算证据 | 人主导阅读，不启动复杂任务 |
| B．最小工作者 | 05—14 | 一个真实入口的身份、权限与认知 | coder |
| C．小工作闭环 | 15—21 | 查事实、改小文件、实际运行、观察等待 | coder / inspector / devops，分开建根会话 |
| D．记忆与反馈 | 22—30 | 事实落盘、历史、压缩、记录与诊断 | coder；内部 Blogger 自然工作 |
| E．执行与恢复 | 31—41 | 同一工作在排队、失败和重启后仍说真话 | coder / devops，逐项加入受控故障 |
| F．有限协作 | 42—49 | 同步委托、通信、分身、复用与可选增强 | coder / devops / browser / inquiry |
| G．管理循环 | 50—54 | 义务、独立评估、换任与退休 | manager |
| H．整体交付 | 55—56 | 多道路进入共享目标，再从实际安装包重验 | orchestrator，最后再用已安装的 coder |

一笔画主线：校准 → coder 单人 → 人工接力的读写与运行 → 长会话 → 故障恢复 → 小协作 → manager 单道路 → orchestrator 多道路 → 安装后的普通用户。

这里的“弱到强”首先指责任范围、并发范围和不可逆后果，不是要求依次换更贵的模型。初始模型应是用户真正准备日用的模型；换模型、换角色、加并发、加故障不要同时发生。

### 一笔画不等于禁止回头

requirements/INDEX.md 存在 execution-model-routing 与 managed-chat-execution 等循环依赖，不能把这张图硬排成先决条件全部满足的拓扑序。包的语义依赖也不是用户使用产品的顺序。

本路线区分三件事：

- 主审：在所属站完整读该包 WHY、WHAT、HOW，追代码并裁决设计。
- 借用：前站正常使用后站的机制，但不因此宣布后站已经通过。例如 coder 的第一次请求已经借用了调度、持久化、Blogger。
- 回归：后站改动影响已审过的能力时，重跑那条最小场景，不把全部包从头重新审一遍。

每包主审一次，不代表每种行为只观察一次。局部完成是“此包的意志、实现和已执行证据对齐”；整体完成还需要 G、H 两段的组合验证。

## 2. 三个判断分开做

每站都按以下顺序，不先看实现替它找理由。

| 判断 | 要回答的问题 | 不接受的替代品 |
|---|---|---|
| 用户意志 → WHY / WHAT | 用户究竟需要什么后果？哪些限制是用户要的，哪些是工程师自加的？ | “现在就是这么写的”“现有测试要求如此” |
| WHAT → 代码 / HOW | 条款的判断与副作用由谁拥有？真实入口是否走到了它？ | 函数存在、注释提到条款、Surface 自己测得很漂亮 |
| 代码 → 真机体验 | 日用模型在真实 Host 中能否做对？失败时人能否理解并接手？ | 模型说“已完成”、mock 全绿、日志出现一个成功词 |

先请用户用一两句话说出本包希望保住的价值与不想付的代价。将这句话与现行 WHY、WHAT 比较。没有直接用户裁决的地方标“设计待确认”，不要把本路线的建议包装成用户原话。

WHAT 是当前实现的验收合同，但不是拒绝用户修订产品的理由。用户不接受 WHAT 时，先确定修订，再同步 WHY、WHAT、HOW、代码和测试。不能只改测试让旧实现变绿，也不能先改代码把新行为偷偷变成既成事实。

### 每站固定读法

1. 读 WHY：删掉历史施工背景后，还能否说清它为何存在？它的代价是否值得？
2. 逐条读 WHAT：主体、触发、前提、允许后果、禁止后果、失败后果是否明确？硬阈值和永久限制有无用户依据？
3. 读 HOW：是否在讲当前机制？链接的函数、文件、测试还存在吗？有没有旧分支、空实现或绕行入口？
4. 沿真实入口追一条正向路径，再追一条失败路径。查看 .fsi 与 .fs、装配、资源、事件提交、Host 回调，不止读 Surface。
5. 做最小现场场景。先普通成功，再一个有区分力的反例；涉及取消、未知后果或恢复时，再做对应边界。

本文件的“代码入口”只是开卷位置，不是完整审查范围。源路径未带前缀时，均相对于 src/Wanxiangshu/；测试均从对应 requirements/<包>/HOW.md 与 tests/ 继续追踪。

## 3. 真机准备：保留真实机制，隔离真实损失

### 3.1 使用隔离的练习仓库

不要拿万象术源码仓库、用户工作目录或正式远端做破坏性场景。建立独立 Git 仓库，使用可丢弃的本地 bare remote 作为后期发布目标。不要只建同仓 worktree 后就称完全隔离：worktree 可能共享 Git common directory 与插件持久化资料。

练习工程不需要业务复杂度。推荐一个无第三方依赖的 Node 小项目，包含两个可独立修改的模块：

| 文件 | 人预先确定的需求 | 作用 |
|---|---|---|
| src/tags.mjs | normalizeTags：去首尾空白、丢空项、去重且保持首次出现顺序；非字符串元素拒绝 | 单 Coder 修复、歧义编辑、Inspector 取证 |
| src/total.mjs | sumCents：只接受非负安全整数分值；结果也必须是安全整数 | 第二条独立道路、边界验证 |
| tests/tags.test.mjs、tests/total.test.mjs | 由人确认的固定正例与反例；运行命令 node --test | 区分写测试与实际运行 |
| requirements/tag-normalization/ | WHY、WHAT、HOW 及可选 APPLIES-TO | 测 grounding，不给万象术新增第 57 个包 |

例如 [" b ", "a", "b", " "] 的正确标签结果应为 ["b", "a"]，而不是排序后的 ["a", "b"]。人可预先放入一个错误排序实现，形成知道为什么红的起点。不要让同一个被测模型同时发明需求、写实现、改 expected 并宣布通过。

练习包的 APPLIES-TO 可以使用 /src/tags.mjs 与 /tests/tags.test.mjs；包自己的 requirements/tag-normalization/** 天然覆盖，不在 APPLIES-TO 中重复声明。正式语法以 requirement-grounding 的当前 WHAT 为准。

### 3.2 固定环境，不复制日常隐患

保存本次源码提交、构建或包版本、实际插件入口、Host 版本、路由配置版本与所用模型。调查时本机 OpenCode 为 1.18.29；将来执行时重新读取，不能把这个版本写成永远正确。

使用独立 HOME、USERPROFILE、XDG 配置/数据/状态/缓存目录。只给这个 profile 配置获准的 provider 凭证和必要 MCP，不复制全部私人配置。凭证不写进仓库、不导出到巡检记录，不打印整个环境变量表。

模型调度配置实际由 OpenCode/Host/ModelRouting.fs::configPath 定位到 home 下的 .config/opencode/wanxiangshu.mjs。只改 XDG_CONFIG_HOME 不等于已经隔离这份路由。保留当前已验证的路由结构，换成用户获准使用的目标；不要照抄 resources/wanxiangshu.mjs 中恰好存在的模型名来碰运气。

单 Coder 也可能自然使用 Blogger、Inspector、Distiller 或其它内部执行。因此须给实际启用的内部角色配置可用目标，留出嵌套执行的正常容量。不要用“所有角色只许一个全局槽位”意外制造等待死锁，再误判主角色不工作。真正的容量边界留到第 34 站。

初期只通过正式设置关闭可选 speculation：WANXIANGSHU_STRENGTH_MODE=off；第 48 站再单独打开。不要改源码关闭 Companion、权限、grounding、持久化或失败策略来制造一个“简单模式”。最小子集指任务负担小，不是运行时机制被掏空。

### 3.3 Coder 可以直接面对用户

已核对 StaticTools.fs：coder、inspector、devops、browser、inquiry、manager、orchestrator 都经 primary 配置进入 Host；Blogger 等内部角色另有隐藏配置。ManagedAgentConfig.fs 在加载时投影这些非模型字段。

已核对本机 opencode --help，存在 [project] 与 --agent。当前官方 CLI 文档也列出 --agent；官方 agents 文档说明 primary 可切换。现场优先为各角色新建根会话，不在同一条正在执行的 authority 中频繁换角色，以免混淆身份与角色切换问题。

假定 OC_BIN 指向已核对的 OpenCode 可执行文件，LAB 指向练习目录，下面命令应在上述隔离 profile、已挂载正确插件且已配好路由的 shell 中运行：

```bash
export WANXIANGSHU_PROVIDER_LANGUAGE=zh-CN
export WANXIANGSHU_STRENGTH_MODE=off
"$OC_BIN" "$LAB/project" --agent coder

# 后面各站按需要启动新的根会话，不是同时全部启动。
"$OC_BIN" "$LAB/project" --agent inspector
"$OC_BIN" "$LAB/project" --agent devops
"$OC_BIN" "$LAB/project" --agent manager
"$OC_BIN" "$LAB/project" --agent orchestrator
```

不要加 --pure，它会关闭外部插件；不要加 --auto 绕过权限确认。不要把 WANXIANGSHU_NO_FATAL_EXIT=1、TESTS_MJS_FILES 或自动化测试专用的 fake provider 环境带进日用实测。

Coder 直接面对用户仍是 Coder。按 OFF-008，它负责写源码与静态测试，不负责执行项目并认证运行证据。早期运行由人亲自做，或切到独立 DevOps 会话。不能把“无法自己跑 npm test”一概记为 Coder 缺陷；先问用户是否仍然认同这个 office 边界。

### 3.4 三种证据明确分开

“真实 Host + 真实 provider + 自然任务”用于体验、可理解性与通常行为；“真实 Host + 受控故障端点”用于指定拒绝、延迟、重复和取消；“确定性测试”用于精确竞态与不变量。三者各有用途，不互相冒名。

真机人工巡检不等于用真人替代自动测试。不要求人靠不断断网撞出 exact race。没有现成且可控的故障注入能力时，该项记“未实测”，用已有契约测试承接已知范围；发现实际缺陷后补成正式可重放回归。

模型自述“我读过规范”“我没有重复执行”“我没泄露信息”都不是证据。必要时由巡检人核对实际工具记录、provider 请求、持久化事件、文件 diff 与进程退出。TUI 看不到 provider 全部输入；拿不到请求级观测就不能宣布 prefix、horizon 或隐私隔离通过。

所有观察只读；不手改 journal、伪造 accepted 事件、把内部 SessionId 塞进 provider 提示词。原始日志留在受控证据目录，进入项目记录前去掉凭证、私人路径、个人消息和无关请求正文。

## 4. A 段：先把尺子摆正

以下各站的现场预期来自当前条款或明确标注的巡检建议，均须接受用户裁决。没有做过的场景不预填通过。

### 01 requirement-system：用户的话如何成为合同

意志问题：用户说“顺序保持首次出现”，WHY 是否解释其价值，WHAT 是否直接约束结果，HOW 是否只是实现说明？是否把一次脚手架决定写成了永远不能改变的产品教条？

入口：requirements/README.md、INDEX.md、各包 WHY/WHAT/HOW；重点看 REQUIREMENT-SYSTEM-007、008、011、014、015。用户裁决和包所有权有冲突时，不让实现者自行选边。

最小检查：以标签需求完整走一遍“用户一句话 → 条款 → 可观察例子”；再挑一条用户明确不想要的限制，确认能经正式修订删除，而不是被 HOW 或旧测试反向否决。此站主要是人工裁决，不冒充真机功能测试。

退出：意志未决、合同冲突与实现缺陷能分开记录；不新增全仓登记表或测试数量门槛。

### 02 verification-system：绿灯是否诚实

意志问题：日常验证是否只付必要成本？日志能否区分执行失败、基础设施失败、尚未运行和范围外？用户要的是真实结论，不是好看的通过率。

入口：scripts/verify.mjs、scripts/check.mjs、requirements/verification-system/tests/run.mjs、tests/support/ 与 tests/e2e/support/supervise-node-test.mjs。结合当前 HOW 选已有的失败传播、进程挂死、流未完成反例。

最小检查：运行一个现成成功例和一个现成必败例，观察实际退出码、失败明细与最终摘要；校对声明范围是否与实际执行范围一致。后面每站只跑受影响套件，不把完整发布链复制 56 次。

退出：能说明每种绿色证明了什么、没证明什么。完整发布验收留在第 56 站。

### 03 js-semantic-surface：测试有没有绕开真实边界

意志问题：独立领域合同是否值得有稳定入口？是否为了测试方便暴露了一批并无独立意义的内部接口，或又造了一张证明权威登记表？

入口：scripts/lib/test-surface-scan.mjs、实际保留的 JS 边界检查、相应领域的 *Surface.fs/.fsi 与调用方。以当前 HOW 为准，不恢复已退役的 trace 治理。

最小检查：从一条真实标签工具调用追到领域实现，再对照测试走的入口。用现成非法输入例观察完整拒绝；确认测试不是只测一个适配器自己造出的返回值。Fable 内部表示与模块链接的专属验证另算。

退出：真实入口、转换边界和错误传播能连起来；入口存在、函数可调用不再冒充业务正确。

### 04 structured-workflow：流程是否被实现细节绑架

意志问题：代码是在顺着问题写直接流程，还是为了“规范”再造 Stage、Phase、注册框架和业务程序计数器？所谓简洁是否把必要边界也删掉了？

入口：任一短业务入口及其 .fsi/.fs、Composition/ 装配、scripts/lib/owner-compile.mjs、编译分片 .fsproj。首例选一次小文件修改，后续在恢复与退休站复验。

最小检查：人工沿输入、纯判断、副作用、提交、返回逐步定位；用已有小型 Fable 边界 fixture 验证缺依赖或隐藏实现真的失败。只用 Fable，不运行 dotnet build。

退出：业务步骤直接可读，权限由类型/边界承载，不用代码外形检查替代行为证明。

## 5. B 段：一个真实 Coder，先不组队

### 05 host-boundary：插件真的接上了 Host

意志问题：用户是否能直接选 coder，而不用先接受 Manager 的全部管理方式？普通发言、工具结果、取消与完成是否可理解？

入口：OpenCode/Plugin/Plugin.fs、OpenCode/Host/、OpenCode/Signals/、Host/Contract/。从实际加载的插件入口追 hooks，不只看注册表。

现场：新建 coder 根会话，发一条只读请求，观察一次工具调用及完整结果；再取消一项明确正在进行的动作。核对 Host 的真实 terminal，不把 SSE 有流量或 HTTP 204 当作业务完成。

退出：确实加载本次插件，选中的角色、实际请求、工具执行与终态一致；没有把纯 Host 会话误当万象术。

### 06 session-ontology：会话不是人，也不是职责

意志问题：用户看到的工作者身份是否稳定？系统的 Work / InternalLeaf、独立 / attached 分类是否仍有现实用途，而不是历史命名包袱？

入口：Execution/Session/Ownership.fs、Execution/Session/Attachment/、managed-session-lifecycle 的装配调用；对照 SESSION-ONTOLOGY 的正交分类。

现场：观察 coder 根会话及自然出现的 Blogger；随后在第 42 站加入同步 Inspector。人能分清主工作、附属执行与受托工作，但模型不应因此收到一堆机器拓扑。

退出：一个辅助会话出现不等于出现第二个人或第二份用户任务；后站借用关系明确。

### 07 participant-identity：换执行者不换人

意志问题：同一工作遇到模型切换、附属会话或执行分身时，原来的职责、人格和授权是否应保持？新任务又应在哪个边界建立新身份？

入口：Participant/Persona/Identity.fs、Participant/Persona/、Interaction/Authority/IdentitySeed.fs；重点区分 LogicalRun、Role、Persona、ExecutionBinding。

现场：先记录当前同一工作的身份与实际模型；第 34、37、44 站分别复验路由变化、恢复与分身。不要用“在另一个会话重新问同一句话”伪造身份连续性测试。

退出：身份事实不从当前模型名、session 数量或助理自述猜测；未发生切换的部分保留待复验。

### 08 provider-language：语言完整、技术标识不变

意志问题：中文环境是否确实自然、准确，而不是只把系统提示词翻译了？固定语言绑定是否符合用户日用方式？

入口：Participant/Provider/Language.fs、Participant/Provider/SessionLanguage.fs、OpenCode/Host/ProviderLanguageBinding.fs、resources/provider/ 的语言对。

现场：用 zh-CN 新根会话完成一次读取、一次可理解的失败；再用 en 新根会话执行同一小任务。比较真实 system、工具描述、后果文本；工具名、路径、代码和用户原文不被翻译。恢复旧会话时核对旧绑定不随未来偏好改变。

退出：语言一致性不仅是文件成对存在；英文自动化 runner 通过不能替中文真机作证。

### 09 office-capability：先问职责分工值不值得

意志问题：用户是否仍希望 Coder 只写、Inspector 只取既有证据、DevOps 执行、Browser 查外部事实、Inquiry 做推理？这个边界带来的协作成本是否值得？

入口：Foundation/OfficeCapability.fs、Foundation/Roles.fs、resources/provider/role/；逐条对照 OFF-002、007—014，尤其 Coder 不认证运行结果。

现场：分别给 coder“修标签函数”、inspector“指出现有缺陷”、devops“运行已写测试”。再给各自一件越职责请求，观察实际权限而非只听解释。先由人接力，不引入 Manager。

退出：职责解释、可产生的后果与用户认可一致。用户不认可时是产品裁决，不把 office 放宽藏在工具补丁里。

### 10 capability-enforcement：看得见、调得动、拒得住

意志问题：权限保护是否真实，又是否给日常操作增加了不必要的问答与失误成本？

入口：OpenCode/Tools/StaticTools.fs、OpenCode/Tools/ToolRegistry.fs、OpenCode/Host/ManagedAgentConfig.fs、Repository/Programming/Js/Capability.fs 与当前 attempt profile。

现场：比较 coder 与 inspector 的实际 provider 工具面；在练习目录用已有受控反例验证 inspector 的写操作被执行层拒绝，且文件未变。模型主动拒绝不等于 runtime gate 已测过。

退出：描述、schema、SDK 与执行门读取同一能力；primary 身份和模型档次都不扩大权限。

### 11 cognitive-environment：把任务留给用户，把工具留给工具

意志问题：Common Law、Role Law 和继承知识是否帮助工作，还是淹没了用户的一句话？assume 是否只是推演画布，而不是偷偷改写事实或义务？

入口：Resources/PromptResources.fs、resources/provider/world/、role/、library/，以及当前认知工具的注册与执行位置。

现场：给 coder 一个完整、很小的任务，不额外教授工具。看它是否直接做事，是否凭 Role Prompt 擅自扩大范围。显式提出一个假设后，再要求区分假设与已观察事实。

退出：稳定身份与瞬时任务分层；文案即使“符合规范”，让日用模型持续迷路也要回到 WHY/WHAT 讨论。

### 12 action-affordance：模型能否自己选对下一步

意志问题：工具说明是否让普通模型知道第一步怎么做、何时停止、失败后怎么恢复，而不是堆砌危险告示与术语？

入口：resources/provider/tool/、Repository/Programming/Js/Description.fs、OpenCode/Tools/ToolRegistry.fs 及实际输出描述。

现场：先只说“修复保持首次出现顺序”，观察工具选择；再安排一个歧义编辑例，观察能否读反馈、重新定位、保守修复。第二次才给有限指引，分别记录无提示与有提示表现。

退出：不是模型背出契约，而是正确选择和恢复；不能由人逐次填写参数后宣布工具易用。

### 13 participant-horizon：只看该看的，但不能少看关键事实

意志问题：内部细节是否只给巡检人看？模型是否既不被机器拓扑轰炸，也不会因过度精简失去决策依据？

入口：Execution/Session/OpenCode/HorizonTool.fs、Execution/Session/OpenCode/HorizonSurface.fs、Participant/Provider/Projection/ 以及各工具后果 renderer。

现场：查看当前 coder 的实际输入与工具结果；不应把句柄表、诊断 DTO、内部会话 ID 当业务材料。之后在双参与者场景放入两个无敏感性的不同标记，验证只沿获准路径传播。

退出：区分“日志里能看到”与“provider 真收到了”；未拿到请求观测时不宣称隔离已证实。

### 14 provider-projection：同一意思经过表示后不能变质

意志问题：模型看到的表示是否准确、稳定、节省注意力？用户指令、工具观察、辅助提示有没有被混成同一种权威？

入口：Participant/Provider/Projection/、OpenCode/Plugin/PluginTransforms.fs、OpenCode/Codec/ 与相应资源 renderer。

现场：对一次读取和一次失败保留 typed 结果、最终 provider 消息的对应片段；核对没有凭渲染增加任务、成功事实或空 user 消息。专门检查只有 system 而没有真实用户意图的非法续轮不能被脚本宽松匹配掩盖。

退出：投影保持语义，失败仍是失败；后续 prefix、grounding、Manager 切段都使用这条已看懂的链。

## 6. C 段：先完成一件小工作

### 15 repository-investigation：先看见，再下结论

意志问题：调查是否以最少、充分的真实观察建立结论？Inspector 有没有把猜测说成证据，或者为了显得严谨一直查下去？

入口：Repository/Investigation/、Execution/Delegation/SyncDelegate/、实际 read/grep/glob 工具及相应结果投影。

现场：Inspector 直接面对用户，只调查标签函数为何改变顺序；要求指出可定位的实现与现有测试，不修复、不运行。再问一个仓库里没有答案的问题，观察是否明确说不知道，而不是编出文件或执行结果。

退出：结论能回到实际路径和观察；“读过测试源码”不能等同“测试已通过”。

### 16 requirement-grounding：补知识，不接管操作

意志问题：用户是否认可现在的弱介入语义？当前 WHY/WHAT 明确 grounding 不阻断 mutation；INDEX 还残留“首次 mutation 必须先 grounding”的旧叙述，这两者须在本站裁决对齐。

入口：Requirement/Grounding/、OpenCode/Host/RequirementGrounding/、真实工具读取/修改的 hook 与投影；重点读 REQUIREMENT-GROUNDING-006—012。

现场：在练习包中先 grep/glob，确认没有全量注入；再读 tags 源码，检查返回尾部规范及来源路径；主动读过同版本规范后再次读源码，不重复补入。更新一份练习规范后只追加新版本。另用允许的修改操作确认缺 grounding 不成为拒绝、重发或回滚理由。

退出：工具族一致、内容版本去重、历史字节不改写；规范不会伪装成人的新授权。NUL+BOM 等字节契约由实际请求/持久化观察核对，不靠屏幕肉眼猜。

### 17 repository-programming：一次真实、保守的小修改

意志问题：编程面是否让普通改动容易、错误恢复清楚，并保住用户已经写好的内容？是否为了抽象让模型承担了整文件重建的负担？

入口：Repository/Programming/Js/ 的生成、sandbox、编辑规划、preflight、事务与提交实现；结合 js-edit、js-workflow、js-transaction 系列现存测试。

现场：Coder 修复标签顺序，只动授权范围。再做三类反例：匹配不唯一；同一 program 中第二个文件编辑失败；人于读取后修改了目标。分别观察零错误写入、整次 staging 丢弃、第三方修改保留。实际竞态使用正式夹具或可控屏障，不靠手速。

退出：最终 diff 与用户要求一致；近似匹配只提供诊断，不能偷偷取得写权限；Coder 没有顺便替用户修改需求以通过测试。

### 18 time-capability：时间只做它有权做的事

意志问题：哪些等待真的需要期限？哪些业务判断不应受机器快慢支配？设置的限时是否代表物理保护，而不是新的业务状态？

入口：Process/NodeTiming.fs、Process/VirtualTiming.fs、Process/Deadline.fs，相关等待调用方及注入位置。

现场：人或 DevOps 跑一个短命令和一个明确超过已配置物理期限的练习进程，观察完成/超时区别。精确临界时刻、时区和取消次序用现有虚拟时间测试验证，不要求真人精准卡毫秒。

退出：超时含义、终止请求、真实退出分开；后续压缩、恢复不能因“等了够久”就制造成功。

### 19 process-execution：写了命令不等于跑完命令

意志问题：终端控制是否足够直接？运行结果有没有保留 stdout/stderr、退出、取消的真实含义？

入口：Process/ProcessRunner.fs、Process/NodeProcessHost.fs、Process/NodeProcessWait.fs、Process/Pty*.fs，OpenCode/Tools/ 的实际执行工具。使用当前可见工具，不从 README 的旧名称猜入口。

现场：DevOps 运行练习项目测试，先看真实失败，再看 Coder 修复后的成功；额外运行一个非零退出命令和一个可交互、可终止的短进程。发出 signal 后等待真实 backend exit，不把 signal 发送成功当作进程已死。

退出：运行证据能由人重放；进程确实回收，输出不会把错误包装成成功。用户第一次获得完整“写代码—运行—看到结果”的子集。

### 20 output-distillation：少看一些，但不被误导

意志问题：输出精简是否保留决定下一步所需的信息？是否只是把重要错误切掉后省了 token？

入口：OpenCode/Tools/Distillation.fs、OpenCode/Tools/DistillationRuntime.fs、Process/ProcessOutput.fs、Process/Spool.fs、Process/LargeGate.fs。

现场：用一个有界练习命令产生超过当前输出阈值的内容，把不同错误放在首段和尾段，并保留原始输出供人核对。比较模型得到的压缩结果是否诚实说明范围、遗漏和失败。不要用无上限输出压测真实模型。

退出：尾部没报错不被推成整体成功；没有因输出变大无端创建一群摘要执行者。

### 21 causal-wait：卡住时知道在等什么

意志问题：等待诊断是否帮助人定位问题，而不会反过来干预职责、重试或完成？

入口：Execution/Session/Wait/、相关 wait capability 与诊断 reader/writer；从刚才的进程等待和后续同步委托选两条路径。

现场：有意保留一个练习进程或受控 child 未完成，观察等待对象、owner、最后因果进展与取消后的清理。普通日志不断输出时，业务等待不应因此被宣布完成。

退出：人知道该等、取消还是排障；诊断没有写进业务事实或 provider 决策。此站结束后不再用“卡住了”作为无定位描述。

## 7. D 段：让同一个工作者持续工作

### 22 durable-events：保存发生过的事，不保存想象的进度

意志问题：哪些事实值得持久保存，保留范围与隐私代价是否符合用户预期？当前 retained writer 边界是否清楚，而不是泛称“永远保存一切”？

入口：Persistence/EventStore/、Persistence/Journal/、Composition/Durable/；重点看提交、canonical identity、fold、writer 保留与损坏处理。

现场：借用前面成功修改和失败修改，核对实际事件与文件后果；正常关闭后重开同一练习仓库。写入失败、半行与损坏恢复用独立副本及现存正式回归，不手改仍运行的 journal。

退出：没有事实却推进 Current、失败却宣称提交、同 ID 冲突被覆盖等情况。暂不据正常重开宣称所有 crash cut 都已验证。

### 23 effect-accounting：不知道就说不知道

意志问题：工具已经可能执行、回执却丢失时，用户需要怎样的后果说明与接手办法？系统会不会为了给一个爽快结果而擅自重复执行？

入口：相关领域的 Requested/Claimed/Accepted/Published 类型及提交链，Interaction/Dispatch/、Change/、Repository/Programming/Js/ 的消费者。

现场：用现成可控故障路径让执行端已接受、调用端尚未确认，记录真实副作用与返回后果。随后只核对、恢复，不主动重复这项工作。没有受控注入时保留“unknown 场景未实测”。

退出：requested 不等于没发生，accepted 不退回 requested；unknown 不被包装成零效果或成功。

### 24 semantic-trace：原始语义历史没有被二次编写

意志问题：用户指令、真实工具观察和可见推理如何保存，才能供重放而不泄漏更多内容？辅助注入是否有清楚来源？

入口：Context/Trace/、Context/Trace/SemanticTraceSurface.fs、Host 的 typed capture 与 durable append。

现场：选择含用户原话、一次成功工具与一次失败工具的短会话，逐一核对 trace。测试重复或晚到回调时，只接受因果上相同的幂等，不接受靠文本相同去掉不同发生。

退出：trace 是发生记录，不是事后摘要；后续压缩只能替换表示，不能重写这些审计事实。

### 25 context-compression：记得必要事实，不强迫主模型写总结

意志问题：用户是否认可失败驱动的压缩，而不是提前估计窗口？压缩之后哪些任务事实绝不能丢？Blogger 的成本与收益是否可接受？

入口：Context/Companion/Blogger/、Context/Prefix/、Enforcer/Cycle/；读 CONTEXT-COMPRESSION-001—011、017—025，区分 delta 输入合同与上下文容量。

现场：持续做几项相关小工作，观察 Blogger 正常追平；在已有受控恢复条件下观察 prefix probe/squash。核对真实 Opening、最近工作、todowrite 相关消息与失败后的 coverage。一次没有压缩机会的普通对话不能证明压缩已通过。

退出：不读取窗口容量、不提前预测溢出、不向主模型索要压缩总结；候选失败不改变已提交 epoch，Blogger busy/失败不推进覆盖。不要为了触发场景灌入无限历史。

### 26 prefix-stability：已经呈现的前缀不反复改

意志问题：用户修改新需求和系统保持历史稳定之间，边界是否清楚？省成本有没有以丢原话为代价？

入口：Context/Prefix/、Participant/Provider/Projection/、实际 transform 顺序与 cold-boundary 提交。

现场：保留同一 epoch 下相邻两次 provider 输入，核对原前缀与新增后缀；再在合法冷边界发生后检查新 epoch。旧 grounding/guidance 被正常退休，新材料正常出现，不把所有变动都当违规。

退出：按实际协议位置和身份比较，不按屏幕聊天文字猜；没有“先改前缀，失败再补解释”。

### 27 work-record：交接的是这段工作，不是整个会话

意志问题：接手者得到的信息是否足够且不重复？用户能否知道本次做了什么、还没做什么，而不被固定汇报模板拖累？

入口：Mission/WorkRecord/、Context/Companion/ 的物化边界、Execution/Delegation/ 的 handoff frontiers。

现场：同一个 worker 完成两次不同 charge，第二次的返回只描述第二段因果窗口；父给子的背景与子给父的结果各按其方向呈现。同步复用在第 42 站完整复验。

退出：reuse 保留记忆，但不把上一任务包装成本次成果；Opening、Chronicle 与 Recent work 的范围可由实际 trace 对上。

### 28 behavior-diagnosis：指出真实病因，不靠相似词处罚

意志问题：这条规则是否抓住用户确实在意的机制？是否有负例与区分条件？规则库是否让模型把精力用于干活而非不断自我训诫？

入口：Enforcer/、Enforcer/Cycle/、resources/enforcer/ 与 live Rulebook 装载；读当前 BD-001—019，不把固定内置数量当永恒产品目标。

现场：在练习代码中给一个证据明确的病理例和一个外观相似但合法的反例；比较实际诊断是否引用本次证据、是否命中正确机制。一次随机模型漏诊与系统把无效诊断正式提交须分别记账。

退出：有效诊断与误诊边界能说明；不要求真人背完整规则表，也不靠命中规则名数评质量。

### 29 guidance-delivery：纠正一次，不反复念经

意志问题：第一次反馈是否足以行动？同一问题再次出现时，该补新知识还是只提醒身份？

入口：Enforcer/Guidance/、相关 occurrence/frontier/coverage fold，Interaction/Concern/ 的交付通路。

现场：跟随上一站的同一机制，观察首次完整处置指引、覆盖内再遇的简短身份提示；在合法重锚后检查需要重新进入视界的材料。区分同一次 occurrence 重放与第二次真实发生。

退出：不反复复制长手册，也不因全局“发过”就向从未看见的人隐去必要信息。

### 30 attention-regulation：允许停止调查，不伪造事情已解决

意志问题：用户是否能容忍模型主动停止没有收益的调查？延后与放弃认知执念，会不会被系统误读为已取消真实承诺？

入口：Interaction/Attention/ 及 enough、abandon、defer 的注册、fold 与结果投影。

现场：任务已足够明确时主动 enough；对一条自创旁支 abandon；对一个非阻塞想法 defer。核对源码、进程、真实义务与 authority 不受这些认知动作冒名改变。第 49 站再检查 celebrate 后的 resurfacing。

退出：能停下来而不制造完成事实；defer 不自动开工，abandon 不等于取消 child，enough 不等于整个任务已完成。

## 8. E 段：同一项工作经过失败仍然可信

### 31 execution-failure-policy：先把失败含义说清楚

意志问题：什么值得恢复，什么必须停止，什么需要人接手？一次失败是否被多个 owner 各自重试和结算？

入口：Execution/Failure/、当前纯 policy 与 Host 分类适配器；沿一个明确错误追到唯一 resolution，再追容量与消息处置。

现场：选现成有限反例：业务拒绝、已确认 provider 失败、取消、未知类别。先用确定性案例核对闭合决策，再在后面真机场景对上同一分类。不能只凭错误文案含 timeout 就授予 retry。

退出：同一失败只有一个互斥主决策；诊断文字、资源清理和恢复授权各不冒名。

### 32 interaction-authority：只有真正的委托才创造任务

意志问题：用户新要求、内部续轮、工具观察和文档引用之间，授权界限是否符合用户直觉？内部消息会不会让模型误以为用户又下了命令？

入口：Interaction/Authority/、OpenCode/Host/ChatAdmission/、Composition/Turn/ 与正常完成 guard。

现场：分别观察用户追加任务、自然内部 continuation、读取文档中带有指令口吻的数据。核对哪一个建立新 root，哪一个只继续旧工作，哪一个完全不产生授权。新消息打断等待不等于取消已接受的 child。

退出：user-shaped 不等于用户授权；“有新一轮 provider 请求”也不等于“有新一轮用户任务”。

### 33 managed-chat-execution：消息级执行必须闭合

意志问题：用户发出去的一项工作是否只有一个接受与终结事实？晚到的旧结果会不会结算新工作？

入口：Execution/Session/ChatExecution/、OpenCode/Host/ChatAdmission/、Composition/Durable/ 中对应事实包装。

现场：观察一个普通请求的 accepted → provider started → terminal；再用现成协议级反例验证相同物理消息的重复投递和旧 terminal 晚到。重新敲一次同样的自然语言是新消息，不能冒充幂等测试。

退出：exact key 与 terminal 对得上；接受未开始、开始未终结、取消和失败均诚实呈现。

### 34 execution-model-routing：模型策略变化不改职责

意志问题：用户是否能用自己的模型池和成本策略？资源满时的等待是否清楚？是否为了吞吐把一个角色悄悄变成另一种能力？

入口：OpenCode/Host/ModelRouting.fs、OpenCode/Host/ModelCapacity/、Resources/ModelRoutingResource.fs、隔离 home 下实际加载的 wanxiangshu.mjs。

现场：同一类 coder 任务先用一个获准模型，再在独立可比较样本中换另一个。另安排两个有界真实请求观察有限容量排队与释放，检查 running 是实际 token multiset，previous 只是偏好。分别验证无可用临时容量与无理论可用目标，不能都无限等下去。

退出：不修改权限来适应模型；null、取消、stale fence、归还容量都可定位。测试时的模型变化本身不是用户意志或身份变化。

### 35 managed-session-lifecycle：复用与关闭不遗留幽灵

意志问题：什么该复用，什么该一次性销毁？用户取消、owner 关闭之后，附属执行的成本和残留是否可接受？

入口：Execution/Session/、Execution/Session/Attachment/、Execution/Delegation/SyncDelegate/、Composition/Turn/ 的创建与收尾。

现场：由 coder 自然调用 Inspector，完成后再次调用；观察复用时本次 work unit 与历史范围。另取消一条正在进行的受控委托，核对子级关闭/保留的实际合同和句柄清理。不要手工启动隐藏 Blogger 来替代正常创建路径。

退出：旧完成不完成新任务；关联先于首 prompt；OneShot 与 Reusable 不混用。第 42、44、54 站复验组合关闭。

### 36 dispatch-protocol：发送成功不是唯一完成证据

意志问题：跨不可靠 Host 的调用能否告诉用户“已放置”“未发送”与“可能已接受”的区别？是否会因回执不够漂亮而重复副作用？

入口：Interaction/Dispatch/、Interaction/Dispatch/OpenCode/、PromptDispatcher 与实际 Host acceptance 绑定。

现场：选已有受控协议路径分别截在发送前、Host 接受后而回执未知、真实物理消息身份确认后；观察 claim 和返回。测试恢复时保留同一个语义请求，不另起一项替代工作。

退出：transport receipt 不伪造 physical identity；未知后果不自动重发；后置 bookkeeping 失败不把已发送改写成未发送。

### 37 provider-attempt-recovery：失败后继续原来的工作

意志问题：恢复在用户看来是否可理解、有界、不重复劳动？新 physical attempt 是否保留原工作、角色、语言和必要记忆？

入口：Participant/Provider/Attempt/、Fallback/、Execution/Failure/，以及与 prefix/Blogger 的交接。

现场：在受控 provider 端安排有限的“失败 → 成功”，再安排预算耗尽；核对每次失败只记一次、有效业务成功才清零、失败的恢复材料不被当主任务成功。真实日用 provider 的自然样本另留一份，不能由脚本结果代替。

退出：没有重复 authority root、旧终态串入新 attempt 或无限循环；无法自然触发的预算边界保留确定性证明而不冒称真人覆盖。

### 38 host-provider-failure-ownership：别让 Host 和插件各试一遍

意志问题：用户究竟希望错误如何出现？恢复期间不重复打扰，与保证隐藏所有 Host 原生错误，是两件不同的事。

入口：OpenCode/Host/ManagedAgentConfig.fs、OpenCode/Host/ProviderFailurePresentation.fs、Execution/Failure/；读当前 HOSTFAIL-001—007，尤其呈现能力边界。

现场：对同一受控上游失败，数清每个 physical provider run 的实际请求；Host retry 与万象术新 attempt 分开数。再制造不归万象术认领的错误，确认仍能被人看到。

退出：Host 与插件不双重重试，插件不额外重复报错；不得根据旧 README/旧条款承诺已超出 Host 能力的 UI 抑制。

### 39 degeneration-guard：制止退化，不惩罚正常长思考

意志问题：这项保护抓的是真正无收益输出，还是把模型风格差异当病？终止与改写请求会不会反而形成放大循环？

入口：Execution/Session/LoopDetector.fs、相关退化处置，scripts/lib/derive-loop-detector-envelope.mjs 与 repository corpus 选择。

现场：正常代码解释作为反例；受控有限重复流和异常多样性流作为目标样本。只对已允许的文本流检测，不把没响应、流量小或工具调用密集自动判成退化。禁止让真实模型无上限重复来凑场景。

退出：停止与后续改写有因果身份和有限后果，普通工作未被误杀；保护自身不会制造更多污染历史。

### 40 crash-reconciliation：重启不是重新编造一个世界

意志问题：工作中断后，哪些应恢复，哪些应明确说无法证实？用户是否愿意承担自动补做的风险？

入口：Execution/Session/Recovery/、Persistence/、各未决 effect 的实际 reconcile 与普通 workflow 入口。

现场：在独立练习环境记录一次已完成操作和一次未决操作，然后仅终止本次已确认 PID 的 Host，再重启。不能按通用进程名批量 kill。核对已完成项不重做、未决项不被猜为成功；真实重启与同 PID 中一次 provider 恢复分别记录。

退出：恢复依据是 durable facts 与可信物理观察，不是内存或“文件似乎存在”。遇到 unknown 明确保留，不能靠重跑到绿通过。

### 41 durable-convergence：两份现实合并，不按时间猜赢家

意志问题：两个合法工作副本同时发展后，用户如何理解保留、冲突和过期 writer？保留政策是否可解释？

入口：Persistence/EventStore/、Git/Hook/、writer-stream merge 与 retention；读 DURABLE-CONVERGENCE 当前作用域，不把 retained union 说成无限历史全保留。

现场：用两个独立 clone 和本地 bare remote 各产生事实，按两个方向同步，再重复一次。核对相同事件不重复、独立事件保留、真正领域矛盾为 typed conflict；停用副本和 retention 的边界由现成可控测试补足。

退出：收敛不依赖 last-write-wins 或墙钟排序；此站是事实/replica 收敛，不是第 55 站业务代码发布的替代品。

## 9. F 段：只增加一种协作负担

### 42 delegation：先同步小委托，再验完整异步道路

意志问题：交出去的究竟是一件新工作，还是同一个人的续做？返回是“已经承接”，还是“已经完成”？同步委托的收益能否抵扣上下文与模型成本？

入口：Execution/Delegation/Fork/、SyncDelegate/、Handle/，相关工具与 handoff frontier。特别核对 DELEG-002、010 中 fast/deep 旧措辞是否与现行 PID/OFF 的同角色语义一致，不能擅自认定哪份过期。

现场先做 coder → Inspector、devops → Coder 的正常同步边，再做同一次调用中的小批次、复用下一次 charge、取消与旧 completion。同步返回观察 bounded WorkRecord，不重复广播全文。

异步 fork/resume/join 的代码在本站主审；真实 Manager/Orchestrator 场景挂到第 54、55 站收证。fork/resume 成功只表示承接，不应等子级完成；同一道路续做用 resume，不新建另一个 worker 遗失责任。异步部分未执行前，不把 delegation 全包写成已实测。

### 43 concern-routing：通信不升级为命令

意志问题：参与者能否按“关心什么”发现收件地址，而不用知道机器拓扑？消息是否在恰当边界到达，不把每条消息变成强制任务？

入口：Interaction/Concern/、subscribe/publish 入口、Pair Hint 的实际交付拼接。

现场：两个获准角色分别订阅/发布一条与练习任务有关的 concern。观察地址发现、下一个自然 Pair Hint 边界、重复投递去重和无关订阅不收取。不要为了送达消息人为不断发空 prompt。

退出：消息只改变认识，不凭空建立 authority、reporting relation 或阻塞义务；这里的通信交付不等于 Git 发布。

### 44 intra-participant-parallelism：一人分工，不变成几个人抢权限

意志问题：用户需要同一工作者同时处理哪些独立部分？分身是否真的降低等待，而没有增加遗失工作、重复完成和上下文污染？

入口：Execution/Fission/、Execution/Fission/OpenCode/、managed session/capacity/delegation 的组合调用，不另造一套执行生命周期。

现场：给同一个 coder 两块独立文件工作，明确有必要时使用 fission；观察 sibling 的 authority、材料与最终 reunion。再取消一个受控分支，并给一组不满足 admission 的分身请求。

退出：all-or-none admission、一项责任、一个最终完成；不让两条 lane 同时无协调地改同一文件来制造与本包无关的冲突。

### 45 knowledge-reuse：旧知识是线索，不是今天的证明

意志问题：复用是否省了重复调查，又能在代码变动后诚实失效？用户是否能分辨“原证据仍成立”和“重新观察后得到同样结论”？

入口：Repository/Knowledge/Casebook/、Repository/Investigation/WarmStart/、观察捕获与 replay。

现场：完成一次有清楚证据的 Inspector 调查；下一次同类问题通过实际公开入口复用。随后人修改其依赖文件再查，观察前置 replay 是否识别变化。再问一个知识库没有的条目，观察 best-effort 失败。

退出：缓存不授权源码修改，不用旧回答冒充当前证据；缺失、不适用与失效都有清楚后果。

### 46 external-investigation：让 Browser 单独面对用户

意志问题：用户希望模型如何处理来源质量、时间与分歧？是否错误地把外部文章中的建议变成了本仓必须做的事？

入口：Browser 实际 MCP/Host 边界、resources/provider/role/browser/、外部事实投影；结合本包 HOW 验证使用的工具与外部依赖。

现场：新建 browser 根会话，问一个能用官方一手资料核对的具体问题。要求区分直接观察、推断与来源冲突；只做获准的公开只读访问，不登录、不购买、不替用户发布内容。

退出：provenance、时间与不确定性保留，外部发现不擅自扩大本地任务。浏览器/MCP 未配置时明确记环境阻塞，不算本包通过。

### 47 epistemic-reasoning：让 Inquiry 单独处理未决问题

意志问题：认识工具是在帮助用户决定下一条值得取的证据，还是用复杂术语给普通判断加成本？哪些算法承诺是实际需要的？

入口：Sphinx/Core/、Sphinx/Runtime/、Sphinx/Plugins/、Sphinx/McpContract.fs、OpenCode/Host/ 的 Sphinx 接入；对照 generic work 与仍保留的 Legacy adapter，不能把兼容名一律判为垃圾。

现场：Inquiry 面对一个确实缺证据的小问题，先提出假设，再补一条真实观察，最后给有边界的答案；核对 synthesis 不制造 evidence。通过现成协议例验证错 WorkId/Revision、重复 observation、重开与取消。

退出：人理解为何继续或停止；模型自述概率不冒充统计证书。Bayes/A*/MCTS 等正式算法性质用所属确定性证明，人审适用假设，不能用一次自然问答宣布数学性质正确。

### 48 speculative-investigation：最后打开可丢弃的优化

意志问题：这项推测是否在当前成本结构下值得开？用户是否宁可关闭，也不接受潜在副作用与认知污染？

入口：Strength/、Strength/OpenCode/Settings.fs，以及 Strength 下的 Replica/、Prediction/、Persistence/ 与 Projection/。

现场：复用前面同类调查任务，先 off，再在独立样本中 dry-run；确认 owner 工作未被阻塞、无提前写入权威结果。treatment 仅在真实成本与现行 Host canary 条件已满足时验证，不能手填健康标识伪造资格。

退出：收益、额外请求与用户等待分开记录；关闭不改变产品正确性。真实模型输出有随机性，不能要求两次自然回复逐字相同；零影响的严格证明使用固定输入的正式回归。

### 49 institutional-learning：经验要减少未来负担

意志问题：这次经验是否值得成为长期规则？ABSORB/DISCARD 是否真正可发生，还是每次后悔都会再加一条禁令？

入口：Enforcer/InstitutionalLearning/、live Rulebook 的准入、Interaction/Attention/ 的 deferred resurfacing。

现场：仅在隔离规则库中提交一段真实的小成功与小教训，查看 disposition 及理由。用现有受控案例补齐 ABSORB/BIRTH/DISCARD 与提交失败。检查 celebrate 的延后事项只在学习闭合后提示一次，regret 不触发同样弹出。

退出：局部命令和私人偏好不被直接升成永久规则；不为了凑 BIRTH 持续调用付费模型。本站会改变未来环境，故安排在小规模认知与协作验完之后。

## 10. G 段：Manager 管一条道路

这一段才让 Manager 直接面对用户。先只管理标签模块这一件小事；不同时引入第二道路、真实网络故障、speculation 和大规模分身。

### 50 obligation-ledger：账本写现在还欠什么

意志问题：账本是否帮助用户与 Manager 看清当前工作，还是把 phase/status 换个名字又做了一遍流程引擎？未完成事项会不会自己不断繁殖？

入口：Mission/Obligation/Todo/、Magic Todo 的 Host hook、账本 fold 与投影。

现场：Manager 接到一个有明确完工条件的练习任务。看账本是否写真正待办，随着调查和修复准确推进；用户明确取消一项范围后观察语义变化。把已完成事项、延后想法、当前承诺和质量问题分开。

退出：没有为了满足模板凭空创造任务；模型写出的账本不是运行事实认证，账本的 open 条目也不直接成为退休 blocker。

### 51 relay-incumbency：每轮独立审视当前现实

意志问题：用户是否接受这种 Manager 工作方式：独立判断后接责，下一轮再独立审视，而不是同一个评审反复修自己的结论？

入口：Mission/Relay/Contract.fs、Mission/Relay/Facts.fs、Mission/Relay/Fold.fs、Mission/Manager/Workflow.fs 及 Composition/Durable/ 装配。

现场：直接新建 HumanRoot manager，面对一个已有明确缺陷的小项目。观察评估前看到的材料、取得证据的委托方式、唯一 active 迭代及接责后的动作。不要告诉模型“下一轮会发生什么”，否则测到的是人工导演而不是产品引导。

退出：评审前的认知只读要求与从一开始拥有管理权限不混淆；不是靠临时删除所有管理工具来伪造独立评估。

### 52 relay-assessment：审的是真质量，不是分数仪式

意志问题：八个维度是否都必要、定义是否可操作？PERFECT、REVISE、N/A 的界限是否符合用户预期？N/A 会不会变成跳过实质审查的捷径？

入口：Mission/Relay/OpenCode/ReviewTool.fs、Mission/Relay/Assessment/Model.fs、Mission/Relay/Fold.fs 中的 decideAssessment / validateFreshAssessment、Mission/Relay/Contract.fs；核对实际 provider schema 与评审前公开文本绑定。

当前重要漂移：ASSESS-001 标题仍写“0..10 整数”，正文与代码已经是 PERFECT / REVISE / N/A；ASSESS-008 仍有“全 10”措辞。本站必须消除这个歧义。本路线不使用旧的八个数字满分脚本。

现场：一份人确认有缺陷的实现应得到有证据的 REVISE；一份已经满足需求的实现允许相应 PERFECT/N/A。使用真实工具 schema 验证非法值、缺项、同任第二次不同 assessment、精确重放与陈旧快照；用正式反例精确驱动，不能要求自然模型主动生成所有错误协议。

退出：一任一个 accepted assessment，证据在 tool call 前，证书绑定正确现实。是否允许某维度 N/A 由用户认可的 WHAT 决定，不能临时修改判据逼某个样本变绿。

### 53 relay-context-projection：给下一轮事实，不塞上一轮答案

意志问题：审计历史完整保留而 provider 只看权威消息与本轮内容，这个隔离是否既能独立评估又不丢必要工作事实？

入口：Mission/Relay/OpenCode/NarrativeTransform.fs、OpenCode/Plugin/PluginTransforms.fs、retirement cut 与实际消息选择。

现场：让一次合法 Continue 自然发生，比较前后 provider 请求。上一轮退休后多出的尾部输出不能混进下一轮；真实权威请求、当前工作区与本轮材料应可得。不能用 system-only 请求冒充合法新开局。

退出：provider context 与全量审计历史分开；没有为了“盲评”丢用户要求，也没有把全部旧回答重新塞回新轮。

### 54 relay-retirement：退出、续任和任务成功是三件事

意志问题：用户是否认可目前只有递归 live resources 阻塞退休？未做完、测试红、dirty tree 应引出什么 outcome，而不是被隐藏成工具无法退出？

入口：Mission/Relay/OpenCode/SuicideTool.fs、Mission/Relay/Retirement/、Mission/Manager/Workflow.fs、Mission/Relay/OpenCode/NarrativeTransform.fs、OpenCode/Tools/ToolRuntimeScope.fs。

现场先覆盖“未 review 不能直接 suicide”；再让一轮有真实修复责任的 Manager 完成当前动作后退出并 Continue，下一轮自然独立审查。另用一个尚存活的受控 child 或 PTY 观察 freeze-before-check、清理与重复退休。

把第 42 站的异步语义接到这里：fork 只返回承接，join 等当前 owner 的 completion；等待时新增用户消息只中断等待；同一工作续做使用 resume，不能重复放置。资源归零后 Accepted 必须持有效且快照匹配的证书。

退出：退休不等于项目成功；dirty、测试成败和 open obligations 不被偷偷追加为 suicide blocker，但 Accepted 的证书/快照校验仍然有效。同物理 SessionId 的下一迭代不被旧 abort 误杀。

## 11. H 段：整体成立后，再回到普通用户

### 55 change-integration：Orchestrator 最后登场

意志问题：独立道路能否真正并行而不互相覆盖？用户面对的是清楚的交付结果，还是一堆管理者状态？同一目标后续工作应续做还是开新道路？

入口：Change/、Change/Orchestrator/、Change/Host/、Git/ 与共享 ref 的短 CAS 临界区；一并观察 Orchestrator 的公开 fork/resume/join 实际语义，不用旧 fork-manager/commission 名称推测当前 schema。

现场：Orchestrator 面对练习项目的标签与金额两个独立目标，各委任 Manager，人工检查每条道路确有独立 charge。先正常并行交付；另开一个明确的受控案例，在候选形成后推进本地目标 ref，触发陈旧证书、rebase 或 CAS 冲突，再观察同一道路继续。

补齐两个不同入口：G 段已经测 HumanRoot manager；此处测 Orchestrator 委任后的 AgentOwnerRoot manager。两种 authority 路径不能用其中一条替代另一条。

退出：只有共享 ref 修改短暂串行，长调查/修复不被全局锁住；冲突不丢用户或他人已提交内容，旧证书不授权发布。Orchestrator 不越职直接写源码，也不把同一路的续做变成重复 Manager。

### 56 distribution：从交付物重新开始，才算走完

意志问题：普通用户拿到包后能否照说明使用，不需要懂编译目录、仓库布局或隐藏开发依赖？升级、资源缺失与失败是否说得清楚？

入口：package.json、scripts/build.mjs、scripts/verify-package.mjs、Resources/PackageResources.fs、实际包产物与 README 安装说明。

现场：所有改动稳定后运行一次正式 verify:release，保留真实测试范围与结果；再在仓库之外的独立消费者环境安装同一份已验证包，不能链接回源码仓库 node_modules。检查包携带完整 runtime resources，公开入口与 manifest 一致，无开发文件混入。

用安装后的 coder 重跑最早的标签小任务，再跑一个简短的 Manager/Orchestrator 交付。此处不是重审 56 包，而是证明前面依赖的能力确实随产品交到用户手里。源码开发挂载通过与安装包通过分开记录。

退出：用户能从新环境到真实结果；运行的就是被验证的那份 bytes。完整 E2E、受控模型场景与真实 provider 人工体验各记其证据边界，不互相替代。

## 12. 现场可直接使用的任务串

下面是最小任务样本，不是要求模型按固定台词演出。第一次不要同时给出正确算法、工具参数和预期答案；正确答案由人留作对照。

| 使用点 | 给模型的任务 | 人核对什么 |
|---|---|---|
| B / C 开始，coder | “请读取标签函数和它的规范，指出目前哪里不满足要求。先不修改，也不要声称运行过测试。” | 真实读取、grounding、事实与推测分离 |
| C，coder | “修复标签顺序问题，保留首次出现顺序。只修改必要源码和测试，不改变需求，不执行项目。” | 授权范围、diff、错误恢复，不把测试源码当运行证据 |
| C，devops 或人 | “在当前练习项目运行已提供测试，报告真实命令、结果和未验证部分。不要直接修改源码。” | 原缺陷红、修改后绿；真正退出和未完成项 |
| D，coder | “这次只处理另一个边界：非法元素必须拒绝。上一项已经完成，不要重报成这次的新成果。” | 本次窗口、记录连续性、重复诊断控制 |
| F，coder | “先让 Inspector 核对这两处事实，再决定是否需要改代码。不要把调查建议当成新任务。” | 同步批次、边界、返回范围；必要时复用 |
| F，inquiry | “这两个方案目前还缺哪条证据才能比较？请区分已有事实、假设和值得继续调查的部分。” | 认识状态与证据不混淆，没有无意义递归 |
| G，manager | “请判断并完成标签规范所要求的交付。不要扩展到金额模块。” | 自然的证据委托、当前职责、独立评估、退出；不预告循环内部机制 |
| H，orchestrator | “交付标签规范与金额规范两项独立改动，进入这个练习仓库约定的目标分支；不要扩大范围。” | 独立道路、合并结果、续做与新建区分 |

“模型没有调用某工具”不自动等于工具失败，也不等于已验证。先判断任务是否真的需要它；确需覆盖时补一个明确使用该能力的有界样本，并注明这不是自然选择样本。

## 13. 每段结束只保留一条锚定场景

不要审一包就重新跑完整世界。保存能快速说明这一段仍然成立的最短场景：

| 段落结束 | 保留的锚定场景 | 后续何时回归 |
|---|---|---|
| B | 新 coder 根会话完成一次读取，语言/角色/工具正确 | Host、身份、权限、投影或模型路由改动 |
| C | Coder 修小缺陷，人或 DevOps 得到真实测试结果 | 工具、进程、grounding、事务改动 |
| D | 连续两项小工作，历史与本次记录都准确 | trace、压缩、反馈、记录改动 |
| E | 一次受控失败后原工作继续；一次已完成操作重启后不重做 | dispatch、recovery、capacity、持久化改动 |
| F | 一次同步委托与一次有界并行，单一最终责任 | delegation、session、fission、通信改动 |
| G | 一个直接 Manager 经必要迭代到达合法退出 | assessment、cut、retirement、义务改动 |
| H | 同一实际包在新环境完成小任务与有限整体交付 | 分发、依赖、资源或正式发布 |

真机发现的产品缺陷，必须缩成包内正式自动回归，再修复、重跑对应现场。真人日志用于发现与说明问题，不是“只跑不提交的探针”替代正式证明。

不要预设所有包都能靠普通对话完整验证。故障注入、trace/prefix 比较、精确重复投递、容量竞争属于需专门证据的场景；缺条件就记清缺什么。

## 14. 记录要轻，但四项结论不能揉成一个绿色

每包一条短记录即可，放在该包现有 HOW 的审查说明或现有 GAP 体系。不要另造清单数据库、统一打分系统或第二套 proof registry。原始日志不全文贴进规范。

```text
包 / 提交 / 入口角色：
用户裁决：认可哪些行为，修订哪些行为，仍未决定什么。
实现核对：真实入口、判断属主、提交点；发现的偏差。
现场证据：环境与请求标识、实际动作、结果、最短日志位置。
剩余与回归：尚未触发的分支、要到哪站收证、正式回归位置。
```

分别回答“意志是否确认”“合同是否自洽”“代码是否贯彻”“哪些场景已实测”。其中一项未闭合，不能用其它三项的绿色替它背书。

允许的结论是：此范围已通过、发现缺陷、设计待裁决、环境阻塞、未实测。模型输出不确定但系统诚实暴露，也可能符合合同；系统输出确定但证据不足，应当算问题。

人工停止条件由人预先约定：本次费用与请求上限、受控并发规模、允许写入的目录/分支/远端、哪些进程可以终止。触及上限就停止并记录未完成，不把预算直接写成新的产品 hard gate，也不通过放宽时间、重复运行或临时关机制得到绿灯。

## 15. 现在就该带进巡检的问题

这些是本次阅读直接看到的候选，不是已经替用户作出的裁决。

| 位置 | 已看到的现状 | 在哪站处理 |
|---|---|---|
| requirements/INDEX.md、README.md | 索引开头称 54 张 card，实际包目录 56；README 仍有 48 包、旧验证链等叙述 | 01、02、56；不把旧数量改成新的数量门禁 |
| README 的角色/工具说明 | 仍列 fork-manager、fork-pty 等旧名称；代码中多个工作角色确为 primary | 05、09、42、55；按当前契约核对，不先按文档试错 |
| requirement-grounding 的导航与合同 | INDEX 强调 effect 前，现行 WHY/WHAT 已明确弱介入、不阻断 mutation | 16；优先让用户确认意志，再消除叙述分叉 |
| delegation 与 participant/office | DELEG-002、010 保留 fast/deep/tier 措辞，OFF-003 等已强调单角色、不以 tier 扩权 | 07、09、42；查清当前绑定语义，不凭关键词批量删除 |
| relay-assessment | 标题和部分措辞仍为数字评分，正文与代码是三种 grade | 52；统一 schema、说明、资源与测试 |
| relay-assessment/HOW.md 的源码落点 | 仍引用已不存在的 Assessment/Admission.fs；当前准入判断位于 Mission/Relay/Fold.fs 与 ReviewTool.fs | 52；修正文档指向，不据失效路径推定实现不存在 |
| 人工真机与自动化 | 测试 runner 会固定部分环境；真实体验需要另行确认实际 profile 与 provider | 02、03、08、56；不把测试环境顺利当普通安装顺利 |

本次没有通读并裁决所有包的全部条款；已经核对完整包清单、关键依赖与角色入口，并精读了影响路线的核心合同。每站的完整精读是接下来要做的工作。

## 16. 依据与执行边界

本路线的仓库依据：requirements/INDEX.md 及实际目录集合；各包 WHY/WHAT/HOW；Foundation/OfficeCapability.fs、Foundation/Roles.fs；OpenCode/Tools/StaticTools.fs；OpenCode/Host/ManagedAgentConfig.fs、OpenCode/Host/ModelRouting.fs；Participant/Provider/Language.fs；Strength/OpenCode/Settings.fs；Mission/Relay/Contract.fs、Mission/Relay/Assessment/Model.fs、Mission/Relay/Fold.fs、Mission/Relay/OpenCode/ReviewTool.fs；当前 package.json 与验证入口。

Host 操作依据：本机 opencode 1.18.29 的 --help；OpenCode 官方 CLI 文档 `https://opencode.ai/docs/cli/` 与 Agents 文档 `https://opencode.ai/docs/agents/`，查阅于 2026-09-13。外部文档只用于确认 Host 操作方式，不替代万象术的角色权限合同；执行时以实际安装版本和本仓代码为准。

本次交付仅新增本路线。没有启动真实 provider 任务，没有改角色、规范、代码或测试，没有替用户执行上述破坏性场景，也没有把上轮已完成清理重新列为施工任务。

本文纳入 Git 后属于 repository 文档语料，可能使已派生的构建输入凭据失效；下一次正常 build 应刷新对应产物。文档结构检查不等于重新验证了最新产物，更不能据上轮通过记录宣布本次全链已通过。

终点不是“56 包全部勾完”，而是用户能够说：我确实要这些行为；文档没有把我的意志改写成脚手架礼仪；代码确实做到了；一个普通 Coder 能独立有用，复杂协作也只是逐步增加能力，而不是强迫所有人从完整管理体系起步。
