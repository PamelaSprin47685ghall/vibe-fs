# 万象术重构 交接文档

## 时间点状态

根据《大规模保姆级重构指南》执行六 Phase 迁移，**当前已部分完成，存在编译错误，不可直接构建**。

## 一、已完成（编译通过 + 全测绿）

### Phase 1：Domain.fs 时钟注入化 ✅
- `src/Kernel/Domain.fs` 第100行 `nowMs` 已删除
- 两处调用者 `Mux/SlashCommands.fs`、`Opencode/CommandHooks.fs` 改为内联 `DateTimeOffset.UtcNow`
- 架构测试 `kernelBoundary` 补时钟扫描 guard（UtcNow/DateTimeOffset/Date.now）

### Phase 2A：StateHolder 归位 ✅
- 新建 `src/Shell/SerialStateHolder.fs`（9行，StateHolder SSOT）
- `Opencode/NudgeEffect.fs`、`Shell/NudgeRuntime.fs`、`Opencode/SessionLifecycleObserver.fs` 删除本地定义，改 `open Shell.SerialStateHolder`

### Phase 2B：KGTestHooks 归位 ⚠️ 文件已建，未验收
- 新建 `src/Shell/KnowledgeGraphTestHooks.fs`（32行，KgTestOps record + 4函数）
- Opencode/Mux KGTestHooks 瘦身为 thin wrapper
- **未跑编译验证**

## 二、文件已拆分但编译未通（当前阻塞）

### Phase 6-1：PtyTools(234行) → PtySpawn(124行) + PtyIo(123行) ⚠️
- 新文件已建，内容完整
- `wanxiangshu.fsproj` 编译项已切
- `PluginCore.fs` 改 `open PtyTools` → `open PtySpawn`
- `Tools.fs` 改 `open PtySpawn` + `open PtyIo`
- `getManager`/`checkExecPerm`/`dynImport`/`newRegex` 改为 public
- **旧壳 `PtyTools.fs` 仍在磁盘（3行注释），fsproj 已无编译项**

### Phase 6-2：HookSchema(257行) → HookSchemaCore(130行) + HookSchemaDecode(134行) ⚠️
- 新文件已建
- `HookSchema.fs` 变为 4行 facade（open Core + open Decode）
- **跨模块 private 函数风险**：`extendZodTaskSchema` 等原为 private，现放 Decode。需确认 facade 模式下符号可达性
- **未跑编译验证**

### Phase 6-3：SessionIoSubagent → SubagentSpawn(115行) + SubagentIo(89行) ❌ 编译必断
- **致命问题**：`SubagentSpawn.fs:10` 引用 `Wanxiangshu.Shell.OpopenClientCodec`（幽灵 namespace）
- **致命问题**：`SubagentIo.fs:59` 使用 `DOMException`，定义在 `SubagentSpawn.fs:16`——编译顺序倒置
- **致命问题**：`SessionIo.fs:11-13` 引用 `Opopen.MessagingCodec`、`Opopen.SubagentSpawn`、`Opopen.SubagentIo`
- **致命问题**：`SessionIoSubagent.fs:4` 引用 `Opopen.SubagentIo`
- `SubagentIo.fs` 中 `buildPromptBody` 参数类型与 `SubagentSpawn.fs` 调用处不匹配（record vs anonymous record）
- 缺失多个 `open`：`OpencodeClientCodec`、`OpencodeSessionSpawnCodec`、`OpencodeSessionPromptCodec`、`ErrorClassify`

### Phase 6-4：SessionLifecycleHooks(207行) → NudgeHooks(57行) + TodoHooks(125行) ⚠️
- 新文件已建
- `SessionLifecycleHooks.fs` 变为 31行 facade
- **问题**：第27行 `clearTypedIteratorScope globalIteratorStore sessionId`——`globalIteratorStore` 未定义
- 需补 `open Wanxiangshu.Shell.FuzzyIteratorStore` + 定义 `let private globalIteratorStore = createTypedIteratorStore 200`

### 未拆分文件
- `Shell/NudgeRuntime.fs`（148行）：Phase 3 后自然瘦身，已 <200，无需拆分
- `Opencode/SessionIoSubagent.fs`：拆分后原文件为 4行 facade，清理遗留即可

## 三、时钟收敛未完

- 新建 `src/Shell/Clock.fs`（4行，getTimestampMs）已存在
- `Opencode/CommandHooks.fs` 仍未改引 Clock
- `Mux/SlashCommands.fs` 仍未改引 Clock
- `Shell/ReviewRuntime.fs:78` 调用 `getTimestampMs()` 但未 `open Wanxiangshu.Shell.Clock`

## 四、幽灵目录清理

- `src/Opopen/` 目录含 `NudgeEffect.fs` + `SessionLifecycleObserver.fs`（git untracked）
- 应该直接删除，这两个文件已迁移至 `src/Opencode/`

## 五、恢复性操作建议（按顺序）

1. **先回滚 Subagent 拆分**——问题太多，不如回退到原始 `SessionIoSubagent.fs`
   ```
   rm src/Opencode/SubagentSpawn.fs src/Opencode/SubagentIo.fs
   git checkout -- src/Opencode/SessionIoSubagent.fs src/Opencode/SessionIo.fs
   ```
   相应地回退 fsproj 中 SessionIoSubagent 编译项

2. **修复 SessionLifecycleHooks 拆分**：补 `globalIteratorStore` 定义

3. **删除幽灵目录**：
   ```
   rm -rf src/Opopen/
   ```

4. **删除 PtyTools 旧壳**：
   ```
   rm src/Opencode/PtyTools.fs
   ```

5. **时钟收敛**：所有 `DateTimeOffset.UtcNow.ToUnixTimeMilliseconds()` 替换为 `Wanxiangshu.Shell.Clock.getTimestampMs()`

6. **每步后跑 `npm run build-and-test`**（超时 60s）

7. **OMP Nudge 接入**：`Omp/NudgeRuntime.fs` + `Omp/SessionLifecycleHooks.fs` 目前走独立 `Kernel.Nudge.Coordinator` 路径，未接入 Shell 统一骨架。指南声明"若差异过大则不强行合并"——可加架构测试豁免

## 六、文件清单

| 文件 | 状态 | 行数 | 说明 |
|------|------|------|------|
| `src/Shell/SerialStateHolder.fs` | 新建 ✅ | 9 | |
| `src/Shell/Clock.fs` | 新建 ✅ | 4 | |
| `src/Shell/KnowledgeGraphTestHooks.fs` | 新建 ✅ | 32 | |
| `src/Opencode/HookSchemaCore.fs` | 新建 ⚠️ | 130 | 待编译验证 |
| `src/Opencode/HookSchemaDecode.fs` | 新建 ⚠️ | 134 | 待编译验证 |
| `src/Opencode/PtySpawn.fs` | 新建 ⚠️ | 124 | 待编译验证 |
| `src/Opencode/PtyIo.fs` | 新建 ⚠️ | 123 | 待编译验证 |
| `src/Opencode/SubagentSpawn.fs` | 新建 ❌ | 115 | 编译必断 |
| `src/Opencode/SubagentIo.fs` | 新建 ❌ | 89 | 编译必断 |
| `src/Omp/NudgeHooks.fs` | 新建 ⚠️ | 57 | 待编译验证 |
| `src/Omp/TodoHooks.fs` | 新建 ⚠️ | 125 | 待编译验证 |
| `src/Opencode/HookSchema.fs` | 修改 | 4 | facade |
| `src/Opencode/SessionIoSubagent.fs` | 修改 | 4 | facade(坏) |
| `src/Opencode/PtyTools.fs` | 修改 | 3 | 待删除 |
| `src/Opencode/SessionIo.fs` | 修改 | - | Opopen→Opencode 需修复 |
| `src/Kernel/Domain.fs` | 修改 ✅ | - | nowMs已删 |
| `src/Mux/SlashCommands.fs` | 修改 ✅ | - | |
| `src/Opencode/CommandHooks.fs` | 修改 ✅ | - | |
| `src/Opencode/NudgeEffect.fs` | 修改 ✅ | - | StateHolder归位 |
| `src/Opencode/SessionLifecycleObserver.fs` | 修改 ✅ | - | StateHolder归位 |
| `src/Shell/NudgeRuntime.fs` | 修改 ✅ | 148 | runNudgeFlowCore |
| `src/Shell/ReviewRuntime.fs` | 修改 ⚠️ | - | 缺Clock open |
| `src/Mux/Plugin.fs` | 修改 | - | UtcNow替换 |
| `src/Omp/ReviewToolsLoop.fs` | 修改 | - | UtcNow替换 |
| `src/Opencode/PluginMimoTui.fs` | 修改 | - | UtcNow替换 |
| `src/Shell/KnowledgeGraphPortLock.fs` | 修改 | - | UtcNow替换 |
| `src/Opencode/PluginCore.fs` | 修改 | - | PtyTools→PtySpawn |
| `src/Opencode/Tools.fs` | 修改 | - | PtyTools→PtySpawn+PtyIo |
| `src/Mux/KnowledgeGraphTestHooks.fs` | 修改 | - | thin wrapper |
| `src/Opencode/KnowledgeGraphTestHooks.fs` | 修改 | - | thin wrapper |
| `wanxiangshu.fsproj` | 修改 ⚠️ | - | 含Opopen路径需修复 |
| `tests/ArchitectureTestsFoundation.fs` | 修改 ✅ | - | 时钟 guard |
| `tests/TestsArchitectureRegistry.fs` | 修改 ✅ | - | |

## 七、幽灵目录

```
src/Opopen/NudgeEffect.fs           <- 删除
src/Opopen/SessionLifecycleObserver.fs <- 删除
```
