# 保姆式 0.5.0 编程指南

| 字段       | 内容                                    |
| -------- | ------------------------------------- |
| 版本       | `0.5.0`                               |
| 性质       | 破坏性架构升级                               |
| 配置 SSOT  | OpenCode 宿主最终解析后的 `opencode.json`     |
| Agent 命名 | 所有工作 Agent 必须显式使用 `fast-*` 或 `deep-*` |
| Fallback | `A/A/B/B/A/A/B/B/...` 无限循环            |
| 兼容性      | 不兼容旧 Agent 名称，不兼容旧模型环境变量              |
| 核心原则     | Agent 决定模型；万象术不再自行解析、保存或覆盖模型          |

---

# 一、0.5.0 最终产品语义

## 1.1 一句话定义

0.5.0 将每个 Agent 拆成两个真实 OpenCode Agent：

```text
fast-ROLE
deep-ROLE
```

每个名字都在用户的 `opencode.json` 中直接绑定模型。

万象术只选择 Agent 名称：

```text
Agent = fast-reviewer
```

OpenCode 宿主自然使用：

```text
opencode.json.agent["fast-reviewer"].model
```

万象术不得再：

* 读取模型环境变量；
* 保存自己的模型目录；
* 根据角色解析模型；
* 在发送 prompt 时覆盖 `model`；
* 将 Host 已解析的 Agent 再映射为另一个模型。

---

## 1.2 A 和 B 的新含义

0.5.0 中：

```text
A/B 不再是模型槽位。
A/B 是一对 OpenCode Agent 名称。
```

用户选择：

```text
fast-reviewer
```

则：

```text
A = fast-reviewer
B = deep-reviewer
```

用户选择：

```text
deep-reviewer
```

则：

```text
A = deep-reviewer
B = fast-reviewer
```

因此：

```text
fast-ROLE 与 deep-ROLE 互为 fallback peer。
```

OpenCode 根据每个 Agent 在 `opencode.json` 中的配置，自行选择实际模型。

---

## 1.3 无限 AABBAABB

Fallback 序列永久循环：

```text
A → A → B → B → A → A → B → B → ...
```

没有第四次失败后死亡的规则。

没有：

```text
FallbackDead
LogicalRunDead because retry count reached four
SessionDead because B failed twice
maximum provider failures
```

现有蓝图规定第四次累计失败后 Logical Run Dead；0.5.0 明确删除这条语义。

Fallback 本身永远不终止 Logical Run。

以下外部事件仍然可以结束运行：

* 用户主动取消；
* 父 Agent 取消；
* session 被删除；
* OpenCode 宿主关闭；
* 进程崩溃；
* Host Contract 不受支持；
* 非 retry 类型的不可恢复基础设施错误。

“无限循环”表示：

> 万象术不再因为 provider retry 次数达到某个数字而主动判死。

---

## 1.4 成功不推进循环

Fallback cursor 只在唯一合法信号：

```text
session.status = retry
```

到达时推进。

成功的 provider request 不推进 cursor，也不自动重置到 A。

例如当前 cursor 位于 B：

```text
deep-reviewer 失败两次
→ 当前 EffectiveAgent = fast-reviewer
→ fast-reviewer 成功并调用工具
→ 下一次同 Logical Run 的 provider request 仍使用 fast-reviewer
```

只有再次收到 retry，cursor 才继续前进。

---

## 1.5 新 Logical Run 重置到 A

每个新 Authority Root 创建新的 Fallback cursor：

```text
Cursor = 0
EffectiveSide = A
```

例如：

```text
用户显式选择 deep-coder
```

本次 Logical Run：

```text
A = deep-coder
B = fast-coder
```

即使上一个 deep-coder Logical Run 最后运行在 `fast-coder`，新 Logical Run 仍从：

```text
deep-coder
```

开始。

---

# 二、唯一配置来源：opencode.json

## 2.1 SSOT 的精确定义

唯一配置事实源不是：

* 环境变量；
* 万象术内部 JSON；
* journal 中保存的 model ID；
* `ModelResolver` 默认值；
* package 内硬编码模型；
* 测试服务器的 `test-model`；
* 某个额外的 `wanxiangshu.json`。

唯一事实源是：

> OpenCode 宿主完成所有用户级、项目级配置合并后，实际交给插件的最终 `opencode.json` 配置对象。

万象术应读取 Host 已解析的：

```text
config.agent
```

不要自行猜测用户配置文件路径，也不要第二次读取磁盘。

这样可以保证：

```text
万象术看到的 Agent 配置
=
OpenCode 实际使用的 Agent 配置
```

---

## 2.2 两类 SSOT 必须分清

### 用户配置 SSOT

`opencode.json` 负责：

* Managed Agent 是否存在；
* Agent 的准确名称；
* Agent 对应哪个模型；
* 用户希望 fast/deep 分别绑定什么模型。

### 万象术源码 SSOT

万象术静态源码继续负责：

* Canonical Role；
* 工具权限；
* system prompt；
* 哪些 Agent 对 LLM 可见；
* 哪些 Agent 是内部 Agent；
* Manager、Orchestrator 的调用边界；
* Reviewer 双 PERFECT；
* Inspector 只能使用 Executor Tool；
* Blogger 和 Executor Agent 无工具。

当前架构已经明确要求角色能力使用静态表，并且未授权工具必须在 schema 层不可见。0.5.0 保留这条安全边界。

因此，准确表述是：

> `opencode.json` 是 Agent inventory 和 Agent→Model 绑定的唯一 SSOT；万象术源码是角色语义、工具权限和系统 prompt 的唯一 SSOT。

---

# 三、完整 opencode.json 配置

## 3.1 必须存在的 Agent

0.5.0 要求以下 20 个 Agent 全部存在：

```text
fast-orchestrator
deep-orchestrator

fast-manager
deep-manager

fast-coder
deep-coder

fast-inspector
deep-inspector

fast-devops
deep-devops

fast-browser
deep-browser

fast-meditator
deep-meditator

fast-reviewer
deep-reviewer

fast-blogger
deep-blogger

fast-executor
deep-executor
```

缺少任意一个，万象术启动失败。

---

## 3.2 完整配置模板

```json
{
  "$schema": "https://opencode.ai/config.json",
  "agent": {
    "fast-orchestrator": {
      "model": "provider/fast-orchestrator-model"
    },
    "deep-orchestrator": {
      "model": "provider/deep-orchestrator-model"
    },

    "fast-manager": {
      "model": "provider/fast-manager-model"
    },
    "deep-manager": {
      "model": "provider/deep-manager-model"
    },

    "fast-coder": {
      "model": "provider/fast-coder-model"
    },
    "deep-coder": {
      "model": "provider/deep-coder-model"
    },

    "fast-inspector": {
      "model": "provider/fast-inspector-model"
    },
    "deep-inspector": {
      "model": "provider/deep-inspector-model"
    },

    "fast-devops": {
      "model": "provider/fast-devops-model"
    },
    "deep-devops": {
      "model": "provider/deep-devops-model"
    },

    "fast-browser": {
      "model": "provider/fast-browser-model"
    },
    "deep-browser": {
      "model": "provider/deep-browser-model"
    },

    "fast-meditator": {
      "model": "provider/fast-meditator-model"
    },
    "deep-meditator": {
      "model": "provider/deep-meditator-model"
    },

    "fast-reviewer": {
      "model": "ai.kw92.cyou/devin/swe-1-7-medium"
    },
    "deep-reviewer": {
      "model": "ai.kw92.cyou/devin/swe-1-7"
    },

    "fast-blogger": {
      "model": "provider/fast-blogger-model"
    },
    "deep-blogger": {
      "model": "provider/deep-blogger-model"
    },

    "fast-executor": {
      "model": "provider/fast-executor-model"
    },
    "deep-executor": {
      "model": "provider/deep-executor-model"
    }
  }
}
```

除用户已经指定的 Reviewer 模型外，上述 `provider/...` 均为占位符，实施时必须替换为真实模型。

---

## 3.3 启动时验证

插件启动时必须逐项验证：

1. `config.agent` 存在；
2. 20 个 Managed Agent 全部存在；
3. 每个 Agent 的 `model` 是非空字符串；
4. 每个 fast/deep pair 都完整；
5. Agent 名称大小写完全匹配；
6. 不存在任何需要万象术推测的模型；
7. 公开 Agent 与内部 Agent 分类正确。

建议要求同一 pair 的模型字符串不同：

```text
fast-reviewer.model != deep-reviewer.model
```

若相同则启动失败，并明确提示：

```text
Managed agent pair fast-reviewer/deep-reviewer resolves to the same model.
Configure two distinct models or correct opencode.json.
```

这样可以在启动阶段发现复制粘贴错误，而不是运行后才发现 fast/deep 没有区别。

---

## 3.4 禁止的旧配置

删除并拒绝以下环境变量：

```text
WANXIANGSHU_MODEL_A
WANXIANGSHU_MODEL_B
WANXIANGSHU_BLOGGER_MODEL
WANXIANGSHU_EXECUTOR_MODEL
WANXIANGSHU_AGENT_MODELS
```

也删除：

* `ModelConfig`；
* `AgentModelCatalog`；
* role-based model override；
* hard-coded fallback model；
* production 中的 `test-model` fallback；
* model provider/model ID 的 journal 持久化；
* “新配置缺失时使用旧环境变量”的兼容代码。

环境中即使存在这些变量，也必须完全不起作用。

建议启动日志输出一次：

```text
Wanxiangshu 0.5.0 model configuration source: OpenCode config.agent
Legacy model environment variables are ignored.
```

不要逐轮重复输出。

---

# 四、Agent 命名与解析

## 4.1 Canonical Role 不复制

不要创建：

```fsharp
type Role =
    | FastReviewer
    | DeepReviewer
    | FastCoder
    | DeepCoder
```

仍然保留原始 Canonical Role：

```fsharp
type Role =
    | Orchestrator
    | Manager
    | Coder
    | Inspector
    | DevOps
    | Browser
    | Meditator
    | Reviewer
    | Blogger
    | Executor
```

新增：

```fsharp
[<RequireQualifiedAccess>]
type AgentTier =
    | Fast
    | Deep
```

以及：

```fsharp
type ManagedAgent =
    { Name: string
      Role: Role
      Tier: AgentTier
      Visibility: AgentVisibility }
```

其中：

```fsharp
type AgentVisibility =
    | Public
    | Internal
```

---

## 4.2 精确名称解析

建议新增：

```text
next/OpenCode/ManagedAgent.fs
```

核心解析：

```fsharp
module ManagedAgent =

    let private publicRoles =
        Map.ofList
            [ "orchestrator", Role.Orchestrator
              "manager", Role.Manager
              "coder", Role.Coder
              "inspector", Role.Inspector
              "devops", Role.DevOps
              "browser", Role.Browser
              "meditator", Role.Meditator
              "reviewer", Role.Reviewer ]

    let private internalRoles =
        Map.ofList
            [ "blogger", Role.Blogger
              "executor", Role.Executor ]

    let tryParse (value: string) =
        let parts = value.Split('-', 2)

        if parts.Length <> 2 then
            None
        else
            let tier =
                match parts.[0] with
                | "fast" -> Some AgentTier.Fast
                | "deep" -> Some AgentTier.Deep
                | _ -> None

            let roleName = parts.[1]

            match tier, publicRoles.TryFind roleName, internalRoles.TryFind roleName with
            | Some tier, Some role, _ ->
                Some
                    { Name = value
                      Role = role
                      Tier = tier
                      Visibility = Public }

            | Some tier, _, Some role ->
                Some
                    { Name = value
                      Role = role
                      Tier = tier
                      Visibility = Internal }

            | _ ->
                None
```

生产解析应区分：

```text
UnknownManagedAgent
LegacyAgentName
ExistingHandleId
```

不要把拼错的 Agent 名称静默当成 handle。

---

## 4.3 Peer 计算

```fsharp
let peer agent =
    let tier =
        match agent.Tier with
        | AgentTier.Fast -> AgentTier.Deep
        | AgentTier.Deep -> AgentTier.Fast

    { agent with
        Name =
            match tier with
            | AgentTier.Fast -> "fast-" + roleName agent.Role
            | AgentTier.Deep -> "deep-" + roleName agent.Role
        Tier = tier }
```

示例：

```text
peer(fast-inspector) = deep-inspector
peer(deep-inspector) = fast-inspector
```

Peer 名称必须在启动配置验证阶段证明存在。

---

# 五、彻底删除旧名称

## 5.1 不再支持的名称

以下名称全部非法：

```text
orchestrator
manager
build
plan
coder
inspector
devops
browser
meditator
reviewer
blogger
executor
```

也不接受：

```text
fast
deep
reviewer-fast
reviewer-deep
fast_reviewer
deep_reviewer
```

只接受准确形式：

```text
fast-reviewer
deep-reviewer
```

---

## 5.2 不提供 alias

禁止编写：

```fsharp
| "reviewer" -> Some "fast-reviewer"
| "manager" -> Some "fast-manager"
| "build" -> Some "fast-manager"
```

禁止在 Host config hook 中自动创建：

```text
manager → fast-manager
```

禁止在缺少 `fast-*` 时寻找旧名称。

这是 0.5.0 的有意破坏性设计。

---

## 5.3 用户必须显式选择

每个新的公开 Authority Root 必须携带准确 Agent：

```text
fast-coder
deep-coder
fast-manager
deep-manager
...
```

插件不得执行：

```text
agent omitted → default fast
agent omitted → inherit LastAuthority
agent omitted → read session role
agent omitted → inspect last assistant
```

当 Host 未提供准确 Agent 时：

```text
HostContractUnsupported:
Managed Wanxiangshu authority root requires an explicit fast-* or deep-* agent.
```

OpenCode UI 中用户已经选中的 Agent，可以由 Host 作为解析后的 Agent 名称提供。这属于显式选择。

---

# 六、权限和 Prompt

## 6.1 同一角色的 fast/deep 权限完全一致

```text
tools(fast-reviewer) = tools(deep-reviewer)
tools(fast-coder) = tools(deep-coder)
tools(fast-manager) = tools(deep-manager)
```

fast/deep 只表示模型绑定不同。

不得出现：

```text
fast-coder 只能读
deep-coder 可以写
```

或者：

```text
deep-inspector 多一个 grep
```

当前静态能力矩阵包括：Manager 只有 `fork/join/list`，Inspector 只有 `executor`，DevOps 独占 PTY，Blogger 和 Executor Agent 无工具；0.5.0 必须保持这些边界。

---

## 6.2 不复制 Prompt 文件

保留：

```text
manager-system.md
coder-system.md
inspector-system.md
devops-system.md
browser-system.md
meditator-system.md
reviewer-system.md
orchestrator-system.md
blogger-system.md
executor-system.md
```

不要创建：

```text
fast-reviewer-system.md
deep-reviewer-system.md
```

两者共同使用：

```text
reviewer-system.md
```

否则 fallback 切换时会同时改变：

* model；
* system prompt；
* provider-visible prefix；
* 行为协议。

0.5.0 的档位差异应完全来自 `opencode.json` 中绑定的模型。

---

## 6.3 Config hook 的权限

OpenCode config hook 可以：

* 验证 Managed Agent 是否存在；
* 为 pair 绑定相同的 Wanxiangshu system prompt；
* 设置 Wanxiangshu 管理的静态权限；
* 设置描述和模式等 Wanxiangshu-owned 字段；
* 验证 fast/deep pair 的非模型语义一致。

Config hook 不得：

* 创建缺失的 Agent；
* 填充默认模型；
* 修改用户配置的 `model`；
* 从环境变量覆盖 `model`；
* 将旧名称复制成新名称；
* 把 deep 的模型写给 fast；
* 把 fast 的模型写给 deep。

---

# 七、Prompt Authority 重构

现有 Authority 设计区分 Authority Root 与 Continuation，并规定只有 Authority Root 能改变执行档案；Fallback 只改变 attempt 层，不应改写 Last Authority。0.5.0 保留这套边界，但把“模型 fallback”改成“Agent fallback”。

## 7.1 新 Authority profile

删除：

```fsharp
BaseModel
Variant
ModelA
ModelB
```

目标：

```fsharp
type AuthorityExecutionProfile =
    { SessionId: SessionId
      LogicalRunId: LogicalRunId
      AuthorityRootUserMessageId: MessageId
      AuthorityKind: RootAuthorityKind

      SelectedAgent: string
      PeerAgent: string
      CanonicalRole: Role
      SelectedTier: AgentTier }
```

例如用户选择：

```text
deep-reviewer
```

则：

```text
SelectedAgent = deep-reviewer
PeerAgent = fast-reviewer
CanonicalRole = Reviewer
SelectedTier = Deep
```

这里不保存任何 model ID。

---

## 7.2 Attempt profile

```fsharp
type AttemptExecutionProfile =
    { Authority: AuthorityExecutionProfile
      PhysicalUserMessageId: MessageId
      ProviderAttempt: int64
      EffectiveAgent: string
      Origin: PromptOrigin }
```

Fallback 只能改变：

```text
AttemptExecutionProfile.EffectiveAgent
```

不得改变：

```text
AuthorityExecutionProfile.SelectedAgent
AuthorityExecutionProfile.PeerAgent
AuthorityExecutionProfile.CanonicalRole
AuthorityExecutionProfile.SelectedTier
LastAuthorityProfile
AuthorityRootUserMessageId
LogicalRunId
```

---

## 7.3 Host 负责模型解析

发送 Prompt 时：

```fsharp
{ Agent = Some effectiveAgent
  Model = None
  Directory = directory
  Metadata = metadata }
```

禁止：

```fsharp
{ Agent = Some effectiveAgent
  Model = Some resolvedModel }
```

当前 Blogger 路径同时保存 `Model` 并向 Host 发送 `"blogger"` 与显式 model；0.5.0 应删除该依赖，改成只发送 `fast-blogger` 或 `deep-blogger`。

Host 收到：

```text
Agent = deep-reviewer
Model = None
```

应根据实际配置自动使用：

```text
config.agent["deep-reviewer"].model
```

这是 0.5.0 最重要的 Host Contract。

---

## 7.4 禁止独立 model override

在 Managed Wanxiangshu Agent 上，不再允许：

```text
agent = fast-coder
model = another-model
```

用户更换模型的唯一方法是修改：

```json
{
  "agent": {
    "fast-coder": {
      "model": "another-model"
    }
  }
}
```

Authority 不再接受“显式 model 比 agent 优先”的旧语义。

Host message 中可能带有已经解析完成的 model 信息，但它只能作为诊断事实，不能成为万象术的选择输入。

---

## 7.5 新 Root 和 Continuation

### 新 Human Root

必须提供准确公开 Agent：

```text
fast-reviewer
deep-reviewer
...
```

创建：

```text
新 LogicalRunId
新 AuthorityRootUserMessageId
新 fallback cursor = 0
SelectedAgent = 用户所选 Agent
PeerAgent = 对侧 Agent
```

### 新 AgentOwnerRoot

Manager、Orchestrator、同步 Inspector/Coder 等创建新工作时，必须显式传入准确 Agent。

### Continuation

以下 continuation 继承原 Authority：

```text
InteractionRepair
ManagerGuard
ReviewerGuard
ReviewConfirmation
BusyAgentNudge
ProviderRetryAttempt
HostCompactionContinue
```

但其物理请求使用当前 fallback cursor 对应的：

```text
EffectiveAgent
```

因此：

```text
SelectedAgent = deep-reviewer
当前 side = B
EffectiveAgent = fast-reviewer
```

并不意味着 Authority 被改成了 fast-reviewer。

---

# 八、AABBAABB 循环实现

## 8.1 不保存 Side 和 Dead

最简单的持久状态只需要：

```fsharp
type FallbackCursor =
    { Offset: byte
      LastProviderAttempt: int64 option }
```

`Offset` 只能是：

```text
0
1
2
3
```

映射：

```fsharp
let side offset =
    match offset with
    | 0uy
    | 1uy -> ModelSide.SideA
    | 2uy
    | 3uy -> ModelSide.SideB
    | _ -> invalidOp "Fallback offset must be in range 0..3"
```

推进：

```fsharp
let advance offset =
    byte ((int offset + 1) % 4)
```

---

## 8.2 完整序列

新 Logical Run：

```text
Offset = 0
Request = A
```

第一次 retry：

```text
Offset = 1
Next request = A
```

第二次 retry：

```text
Offset = 2
Next request = B
```

第三次 retry：

```text
Offset = 3
Next request = B
```

第四次 retry：

```text
Offset = 0
Next request = A
```

第五次 retry：

```text
Offset = 1
Next request = A
```

如此永久循环。

---

## 8.3 EffectiveAgent

```fsharp
let effectiveAgent authority cursor =
    match side cursor.Offset with
    | ModelSide.SideA -> authority.SelectedAgent
    | ModelSide.SideB -> authority.PeerAgent
```

示例一：

```text
SelectedAgent = fast-inspector
PeerAgent = deep-inspector

Offset 0 → fast-inspector
Offset 1 → fast-inspector
Offset 2 → deep-inspector
Offset 3 → deep-inspector
Offset 0 → fast-inspector
```

示例二：

```text
SelectedAgent = deep-inspector
PeerAgent = fast-inspector

Offset 0 → deep-inspector
Offset 1 → deep-inspector
Offset 2 → fast-inspector
Offset 3 → fast-inspector
Offset 0 → deep-inspector
```

---

## 8.4 Retry 去重

唯一 durable retry identity 保持：

```text
logicalRunId
+ AuthorityRootUserMessageId
+ providerAttempt
```

同一 identity 重复到达时：

* 不重复推进 cursor；
* 不重复发 continuation；
* 不创建新 Logical Run；
* 不重置 repair；
* 不改变 SelectedAgent。

建议持久事实改为：

```fsharp
FallbackCursorAdvanced
    { LogicalRunId
      AuthorityRootUserMessageId
      ProviderAttempt
      PreviousOffset
      NextOffset }
```

Fold 必须验证：

```text
NextOffset = (PreviousOffset + 1) mod 4
```

---

## 8.5 删除死亡路径

删除或改写：

```text
FallbackDead
SessionDead because retry count >= 4
LogicalRunDead because retry count >= 4
FourthFailureKillsSession
NoFifthRequest
```

删除所有测试断言：

```text
fourth retry → dead
fifth request must not happen
```

替换为：

```text
fourth retry → A
eighth retry → A
twelfth retry → A
```

---

## 8.6 宿主能力门禁

Wanxiangshu 不应再设 retry 上限，但 OpenCode Host 也必须允许后续物理请求继续产生。

必须增加真实 Host Contract 测试：

```text
retry #1 之后有 request #2
retry #2 之后有 request #3
...
retry #12 之后有 request #13
```

如果 Host 自身在某个次数停止 retry：

* 不得伪称无限 AABB 已实现；
* 必须在 PromptDispatcher 中使用 `ProviderRetryAttempt` continuation 延续同一 Logical Run；
* continuation 仍使用同一 AuthorityRoot；
* continuation 不得创建新 completion；
* continuation 不得重置 cursor。

不要凭空添加未经 Host 支持的 `opencode.json.retry` 配置字段。

---

# 九、公开 Agent 调用协议

## 9.1 Manager fork

Manager 的 `fork` 只接受以下新 Agent：

```text
fast-coder
deep-coder
fast-inspector
deep-inspector
fast-devops
deep-devops
fast-browser
deep-browser
fast-meditator
deep-meditator
fast-reviewer
deep-reviewer
```

Schema 描述：

```json
{
  "agent": "fast-coder | deep-coder | fast-inspector | deep-inspector | fast-devops | deep-devops | fast-browser | deep-browser | fast-meditator | deep-meditator | fast-reviewer | deep-reviewer | <existingAgentId>",
  "prompt": "string"
}
```

创建新 Agent 时：

```json
{
  "agent": "deep-inspector",
  "prompt": "全面调查并验证所有可能的竞态路径"
}
```

不得省略 `agent`。

---

## 9.2 Existing handle

继续允许：

```json
{
  "agent": "a1b2c3",
  "prompt": "继续检查新出现的日志"
}
```

规则：

* Busy handle：同一 Logical Run、同一 Authority、同一 fallback cursor；
* Idle handle：新 Logical Run，SelectedAgent 使用该 session 原本的准确 Agent 名称；
* 新 Logical Run cursor 重置为 0；
* handle 不允许被转换成另一档。

例如：

```text
fast-inspector handle
```

不能通过 nudge 改成：

```text
deep-inspector
```

要使用 deep，必须新建 deep-inspector。

---

## 9.3 解析顺序

```text
1. 当前 runtime 中是否存在准确 handle ID
2. retired handle 中是否存在
3. 是否为准确公开 Managed Agent
4. 是否看起来像 handle ID
5. 返回 UnknownAgent
```

错误示例：

```text
deep-inspecter
```

返回：

```text
Unknown managed agent 'deep-inspecter'.
Use 'fast-inspector' or 'deep-inspector'.
```

不得把它当成一个不存在的 handle。

---

## 9.4 fork 返回值

```json
{
  "agentId": "a1b2c3",
  "agent": "deep-inspector",
  "role": "inspector",
  "tier": "deep",
  "fallbackPeer": "fast-inspector"
}
```

`list()` 和 `join()` 也必须包含准确 Agent 名称。

---

# 十、Orchestrator

## 10.1 只能 fork 显式 Manager

Orchestrator fork schema：

```json
{
  "agent": "fast-manager | deep-manager",
  "prompt": "string"
}
```

示例：

```json
{
  "agent": "deep-manager",
  "prompt": "完成 Prompt Authority 重构并通过完整发布门禁"
}
```

不提供默认 Manager。

不支持：

```json
{
  "prompt": "..."
}
```

不支持：

```json
{
  "agent": "manager",
  "prompt": "..."
}
```

---

## 10.2 ManagerJob 持久化

ManagerJob 必须记录：

```fsharp
ManagerAgent: string
```

例如：

```text
deep-manager
```

不要只记录：

```text
Role.Manager
```

否则重启后无法恢复用户明确选择的档位。

---

## 10.3 发布复审

Orchestrator Host 自己创建 post-rebase Reviewer 时，必须在源码中显式写：

```text
deep-reviewer
```

这是 Host-owned 明确策略，不是默认推断。

不得写：

```text
reviewer
```

也不得省略 Agent 后依赖 LastAuthority。

Post-rebase 的双 PERFECT、tree witness 和新 review barrier 规则保持不变。

---

# 十一、同步 Inspector Tool

Coder、Meditator、Reviewer、DevOps 使用的同步 Inspector Tool 必须要求显式 Agent。

Schema：

```json
{
  "agent": "fast-inspector | deep-inspector",
  "prompt": "string",
  "prompts": ["string"]
}
```

规则：

* `agent` 必填；
* `prompt` 和 `prompts` 仍然二选一；
* 一个 `prompts` batch 使用同一个 Inspector Agent；
* child session 记录准确 Agent；
* A/B pair 根据所选 Agent 建立。

示例：

```json
{
  "agent": "fast-inspector",
  "prompt": "运行目标测试并返回直接失败原因"
}
```

```json
{
  "agent": "deep-inspector",
  "prompt": "调查失败、竞态、重启恢复和资源泄漏路径"
}
```

禁止：

```json
{
  "prompt": "调查问题"
}
```

禁止默认 fast。

---

# 十二、同步 Coder Tool

DevOps 的同步 Coder Tool：

```json
{
  "agent": "fast-coder | deep-coder",
  "prompt": "string",
  "prompts": ["string"]
}
```

`agent` 必填。

示例：

```json
{
  "agent": "fast-coder",
  "prompt": "修复一个明确的构建脚本路径错误"
}
```

```json
{
  "agent": "deep-coder",
  "prompt": "重构 DurableFallback 并完成 journal migration 删除"
}
```

两者权限完全一致，均由 Coder 静态能力表控制。

---

# 十三、Blogger 双档

## 13.1 不向 LLM 暴露

以下名称不得进入：

* Manager fork schema；
* Orchestrator fork schema；
* Inspector/Coder 工具 schema；
* `list()` 的可创建类型提示；
* 任何模型可调用的 Agent enum；
  -用户公开 Agent 选择说明。

```text
fast-blogger
deep-blogger
```

仅由 Companion Host 内部使用。

Blogger 在现有角色表中本来就是无工具的内部工作记录 Agent；0.5.0 继续保持这一边界。

---

## 13.2 固定方向

Blogger 不允许选择 deep 作为初始档位。

每个新的 Blogger Logical Run 固定：

```text
A = fast-blogger
B = deep-blogger
```

序列：

```text
fast-blogger
fast-blogger
deep-blogger
deep-blogger
fast-blogger
fast-blogger
...
```

---

## 13.3 每次 Blog step 重置

每次正式的 Companion blog 请求是一个新的内部 AgentOwnerRoot：

```text
新 delta
→ 新 Blogger Logical Run
→ cursor = 0
→ 从 fast-blogger 开始
```

同一个 Blogger session 可以长期复用，但每个 blog step 都有自己的 Logical Run。

这样正常情况下始终优先使用快速廉价 Blogger，只有本次 blog step 的 provider retry 才切 deep。

---

## 13.4 Blogger Prompt 发送

目标代码：

```fsharp
SendAgentOwnerRoot
    sessions
    bloggerSessionId
    prompt
    "fast-blogger"
    model = None
```

Fallback 后：

```fsharp
Agent = "deep-blogger"
Model = None
```

删除 `BloggerDeps.Model`。

建议改为：

```fsharp
type BloggerDeps =
    { Sessions: ISessionHostPort
      EnsureBlogger: unit -> Task<SessionId>
      ...
    }
```

模型完全不进入 Companion 模块。

---

# 十四、Executor 双档

## 14.1 区分 Executor Tool 和 Executor Agent

保持：

```text
Executor Tool
```

负责：

* 启动真实命令；
* timeout；
* stdout/stderr；
* spool；
* 200KB chunk；
* process tree cleanup。

内部：

```text
fast-executor
deep-executor
```

是无工具的摘要 Agent，只处理大输出 summarization。

当前架构同样规定 Executor Agent 无工具，而真实命令由 Executor Tool 负责。

---

## 14.2 固定方向

每个新的 summary Logical Run：

```text
A = fast-executor
B = deep-executor
```

循环：

```text
fast-executor
fast-executor
deep-executor
deep-executor
fast-executor
...
```

不允许调用方选择 deep-executor。

不向 LLM 暴露这两个名称。

---

## 14.3 Map/Reduce 任务边界

每个独立摘要请求创建自己的 Logical Run，包括：

* map chunk summary；
* ripple-carry merge summary；
* final summary。

每个新 summary 请求从：

```text
fast-executor
```

开始。

不要把整个进程的所有 map/reduce 请求共用一个无限 fallback cursor，否则早期某一 chunk 的失败会让后续所有正常 chunk 从 deep 开始，破坏“默认 fast”的产品语义。

---

# 十五、Journal 与恢复

## 15.1 持久化 Agent，不持久化 Model

`AuthorityRootAccepted` 保存：

```text
SelectedAgent
PeerAgent
CanonicalRole
SelectedTier
```

不保存：

```text
ProviderID
ModelID
Variant
ModelA
ModelB
```

`AgentLinked` 保存准确 Agent：

```text
deep-inspector
```

而不是只保存：

```text
Inspector
```

ManagerJob、one-shot child、Blogger、Executor summary job 同理。

---

## 15.2 opencode.json 变化的语义

因为 `opencode.json` 是唯一 SSOT：

```text
journal 记录 Agent 名称
Host 根据当前配置解析该 Agent 的模型
```

若运行中进程重启，用户同时修改：

```json
"deep-reviewer": {
  "model": "new-provider/new-model"
}
```

恢复后的下一次 `deep-reviewer` attempt 使用新配置。

这是 0.5.0 的有意语义，不是恢复错误。

万象术不得用 journal 中的旧 model 覆盖用户的新配置。

---

## 15.3 Cursor 恢复

Journal Fold 恢复：

```text
LogicalRunId
AuthorityRootUserMessageId
SelectedAgent
PeerAgent
Fallback Offset
Last processed provider attempt
```

例如崩溃前：

```text
SelectedAgent = deep-coder
Offset = 3
EffectiveAgent = fast-coder
```

恢复后仍然：

```text
Offset = 3
EffectiveAgent = fast-coder
```

下一次 retry：

```text
Offset = 0
EffectiveAgent = deep-coder
```

---

## 15.4 删除旧 journal 兼容

0.5.0 不读取旧的：

```text
BaseModel
SideA model
SideB model
Agent = coder
Agent = reviewer
Fallback Dead
```

启动发现旧 journal schema 时，直接失败并给出迁移提示：

```text
Wanxiangshu 0.5.0 does not support pre-0.5.0 runtime journals.
Archive or remove the old Wanxiangshu runtime journal before starting.
```

不要自动猜测旧 `reviewer` 是 fast 还是 deep。

---

# 十六、必须删除的代码

## 16.1 模型配置

删除：

```text
next/OpenCode/ModelResolver.fs
```

或者将其彻底改名、重写为：

```text
EffectiveAgentResolver.fs
```

删除：

* `ModelConfig`；
* `SideA: OpencodeModel`；
* `SideB: OpencodeModel`；
* 所有 `WANXIANGSHU_MODEL_*` 读取；
* Blogger model env；
* Executor model env；
* role model override；
* model parse fallback；
* model 默认值；
  -测试模型进入生产的路径。

---

## 16.2 Authority model 字段

删除：

```text
AuthorityExecutionProfile.BaseModel
AttemptExecutionProfile.EffectiveModel
LastAuthorityProfile.BaseModel
```

替换为：

```text
AuthorityExecutionProfile.SelectedAgent
AuthorityExecutionProfile.PeerAgent
AttemptExecutionProfile.EffectiveAgent
```

---

## 16.3 旧名称

删除所有：

```text
"manager"
"build"
"plan"
"coder"
"inspector"
"devops"
"browser"
"meditator"
"reviewer"
"orchestrator"
"blogger"
"executor"
```

作为可执行 Agent 名称的用法。

注意：Canonical Role 字符串、prompt 文件名、日志中的 role 名称可以继续是：

```text
reviewer
coder
```

删除的是 Host Agent identity，不是领域角色名称。

---

## 16.4 Fallback 终止

删除：

```text
ModelSide.Dead
FallbackDecision.Die
LogicalRunDeadOnFourthFailure
SessionDeadOnFourthRetry
MaxFallbackFailures
```

删除任何：

```fsharp
if failures >= 4 then ...
```

目标必须是：

```fsharp
offset <- (offset + 1) % 4
```

---

# 十七、建议新增和修改的文件

## 17.1 新增

```text
next/OpenCode/ManagedAgent.fs
next/OpenCode/ManagedAgentConfig.fs
next/OpenCode/EffectiveAgentResolver.fs
```

### ManagedAgent.fs

负责：

* Tier；
* Role；
* Visibility；
* 精确解析；
* peer；
* public/internal 分类。

### ManagedAgentConfig.fs

负责：

* 从 Host 最终 config 对象读取 `agent`；
* 验证 20 个 Agent；
* 验证 model；
* 验证 pair；
* 拒绝旧名称；
* 不保存模型目录。

### EffectiveAgentResolver.fs

纯函数：

```text
Authority profile + fallback cursor
→ EffectiveAgent
```

不处理 model。

---

## 17.2 重点修改

```text
next/OpenCode/ManagerConfig.fs
next/OpenCode/Plugin.fs
next/OpenCode/PluginHost.fs

next/OpenCode/PromptAuthority.fs
next/OpenCode/PromptAuthorityAccept.fs
next/OpenCode/PromptAuthorityRestore.fs
next/OpenCode/PromptAuthoritySend.fs
next/OpenCode/PromptAuthorityService.fs
next/OpenCode/PromptDispatcher.fs

next/OpenCode/RetrySignalHandler.fs
next/OpenCode/PluginFallbackRetry.fs

next/Journal/AgentFacts.Types.fs
next/Journal/AgentFacts.Authority.fs
next/Journal/AgentFacts.Fallback.fs
next/Journal/AgentFacts.Linkage.fs
next/Journal/FactCodec.fs
next/Journal/Fold.fs
next/Journal/Boot.fs

next/Session/Fallback.fs
next/Session/DurableFallback.fs
next/Session/ForkTypes.fs
next/Session/HostForkAgentOwner.fs
next/Session/HostForkBusyNudge.fs
next/Session/HostForkChildDispatch.fs
next/Session/HostForkRestart.fs
next/Session/HostForkRunLifecycle.fs
next/Session/HostForkRuntime.fs

next/OpenCode/ToolSurfaceFork.fs
next/OpenCode/ToolSurfaceOrchestrator.fs
next/OpenCode/InspectorTool.fs
next/OpenCode/CoderTool.fs

next/Session/CompanionHostBlogger.fs
next/OpenCode/ExecutorSummarize.fs

next/OpenCode/OrchestratorHost.Types.fs
next/OpenCode/OrchestratorManagerJob.fs
next/OpenCode/OrchestratorHostReview.fs
```

---

# 十八、推荐编程顺序

## Phase 0：先冻结规范

修改：

```text
AGENTS.md
next/Doc/SSOT.md
MIGRATION.md
CHANGELOG.md
docs/E2E_RELEASE_GUIDE.md
```

先删除文档中的：

```text
A/A/B/B 第四次失败 Dead
BaseModel
EffectiveModel
旧 Agent 名称
旧环境变量配置
```

新增本指南冻结文本。

出口：

* 搜索仓库，不再有“第四次失败判死”的规范；
* 不再有无前缀 Agent 的合法示例；
* 配置 SSOT 只有 `opencode.json`。

---

## Phase 1：Managed Agent 和 Config Gate

实现：

* `ManagedAgent`；
* 精确解析；
* peer；
* 20 Agent 验证；
* legacy name rejection；
* model non-empty；
* pair completeness。

暂时不改 fallback。

出口：

* 新配置测试全绿；
* 缺任意 Agent 启动失败；
* 旧名称启动失败；
* 环境变量无效。

---

## Phase 2：移除模型解析

删除：

* ModelResolver；
* ModelConfig；
* env；
* Authority model 字段；
* BloggerDeps.Model；
* Executor model 参数。

所有 Prompt 暂时只发送准确 Agent：

```text
Agent = fast-...
Model = None
```

出口：

* 全仓生产代码不再主动设置 `PromptOptions.Model`；
* Host Contract 测试证明 Agent 对应正确模型；
* Reviewer 示例准确使用用户配置中的模型。

---

## Phase 3：Authority 改为 Agent pair

实现：

```text
SelectedAgent
PeerAgent
CanonicalRole
SelectedTier
EffectiveAgent
```

所有新 Root 要求显式 Agent。

Continuation 继承 Authority，只按 cursor 选择 EffectiveAgent。

出口：

* Human Root；
* AgentOwnerRoot；
* Busy nudge；
* Guard；
* repair；
* review confirmation；
* restart；

全部保持同一 Authority。

---

## Phase 4：无限循环 Fallback

实现 modulo-4 cursor。

删除 Dead。

先完成纯函数测试：

```text
0 A
1 A
2 B
3 B
4 A
5 A
6 B
7 B
8 A
```

再接 durable facts 和 retry signal。

出口：

* 12 次 retry 后仍活跃；
* duplicate retry 不推进；
* restart 后 cursor 不丢；
* 新 Root 重置为 0。

---

## Phase 5：公开 Agent 工具面

修改：

* Manager fork；
* Orchestrator fork；
  -同步 Inspector；
* 同步 Coder；
* list；
* join；
* handle restart。

所有新创建操作都要求显式 fast/deep。

出口：

* schema 中没有旧名称；
* schema 中没有 blogger/executor；
* 缺 agent 参数直接 validation error；
* fast/deep 权限完全相同。

---

## Phase 6：Blogger 与 Executor

先 Blogger：

```text
fast-blogger → fast-blogger → deep-blogger → deep-blogger → ...
```

再 Executor：

```text
fast-executor → fast-executor → deep-executor → deep-executor → ...
```

每个内部新 Logical Run 重置到 fast。

出口：

* 不向 LLM 暴露；
* 不传 model；
* retry 可切 deep；
* 新任务重新 fast；
* 失败不杀主 Agent 或真实进程。

---

## Phase 7：删除兼容代码

删除：

* 旧 parser；
* alias；
  -旧 env；
* old journal codec；
* model migration；
* fourth failure dead；
* unprefixed canary script；
* legacy README 示例。

出口：

```bash
grep -R '"manager"\|"coder"\|"reviewer"' next testkit tests-next
```

剩余命中必须逐条判断：

* Canonical Role 或 prompt 文本：允许；
* Host Agent identity：禁止。

---

## Phase 8：全量测试与发布

进入：

```text
0.5.0-rc.1
```

任何生产代码修复都切新的 RC。

最终在真实 `0.5.0` 版本号上重新完成完整门禁。

---

# 十九、测试矩阵

## 19.1 Config Gate

必须覆盖：

```text
20 Agent 全部存在
每个缺失一次
model 缺失
model 为空
fast/deep 相同
未知额外 Agent
旧名称存在
环境变量存在但无效
Host config 对象与磁盘路径无关
```

---

## 19.2 Agent 解析

对每个公开角色测试：

```text
fast-role
deep-role
peer(peer(x)) = x
legacy role rejected
大小写错误 rejected
下划线 rejected
后缀顺序错误 rejected
拼写错误 rejected
```

内部：

```text
fast-blogger parsed as Internal
deep-blogger parsed as Internal
fast-executor parsed as Internal
deep-executor parsed as Internal
```

---

## 19.3 权限矩阵

```text
permissions(fast-role) = permissions(deep-role)
prompt(fast-role) = prompt(deep-role)
canonicalRole(fast-role) = canonicalRole(deep-role)
```

重点断言：

```text
fast-inspector tools = executor
deep-inspector tools = executor

fast-manager tools = fork/join/list
deep-manager tools = fork/join/list

fast-orchestrator tools = fork/join
deep-orchestrator tools = fork/join

fast-devops 和 deep-devops 才能操作 PTY

fast-blogger/deep-blogger 无工具
fast-executor/deep-executor 无工具
```

---

## 19.4 Fast 起点循环

选择：

```text
fast-inspector
```

真实 provider request 应为：

```text
1 fast-inspector model
2 fast-inspector model
3 deep-inspector model
4 deep-inspector model
5 fast-inspector model
6 fast-inspector model
7 deep-inspector model
8 deep-inspector model
9 fast-inspector model
```

每次 request 都要验证实际 model 与 `opencode.json` 对应。

---

## 19.5 Deep 起点循环

选择：

```text
deep-inspector
```

真实请求：

```text
1 deep-inspector model
2 deep-inspector model
3 fast-inspector model
4 fast-inspector model
5 deep-inspector model
6 deep-inspector model
7 fast-inspector model
8 fast-inspector model
```

---

## 19.6 无限性

至少测试 12 次连续 retry：

```text
A A B B A A B B A A B B
```

断言：

* 没有 LogicalRunDead；
* 没有 SessionDead；
* 没有第五次禁止；
* completion 未被错误完成；
* cursor 始终在 0..3；
* journal 可恢复。

建议再做一个 10,000 次纯函数/Journal Fold 压力测试，验证：

* cursor 不越界；
* 不出现整数相关错误；
* 不生成无界状态机对象。

---

## 19.7 Success 行为

```text
A 失败
A 成功 tool call
下一 request 仍 A

A 失败两次
B 成功 tool call
下一 request 仍 B
```

成功不推进、不重置。

新 Authority Root 才重置到 A。

---

## 19.8 Restart

在以下 offset 崩溃：

```text
0
1
2
3
```

恢复后 EffectiveAgent 必须准确。

修改 `opencode.json` model 后重启：

```text
Agent 名称不变
下一请求使用新 model
```

这是 SSOT 语义的直接证明。

---

## 19.9 Explicit-only

必须失败：

```text
fork(agent="coder")
fork(agent="inspector")
fork(agent="reviewer")
fork(prompt="...")
inspector(prompt="...")
coder(prompt="...")
fork-manager(prompt="...")
```

必须成功：

```text
fork(agent="fast-coder", ...)
fork(agent="deep-coder", ...)
inspector(agent="fast-inspector", ...)
inspector(agent="deep-inspector", ...)
coder(agent="fast-coder", ...)
coder(agent="deep-coder", ...)
fork-manager(agent="fast-manager", ...)
fork-manager(agent="deep-manager", ...)
```

---

## 19.10 内部 Agent 隐藏

读取所有公开 tool schema，断言不存在：

```text
fast-blogger
deep-blogger
fast-executor
deep-executor
```

同时 Host integration 证明内部代码可以创建和调用这些 Agent。

---

# 二十、Canary 改造

## 20.1 所有旧脚本破坏性更新

当前测试脚本仍广泛使用 `manager`、`coder`、`inspector` 等旧 lane 和 fork 参数；0.5.0 必须全部改成准确 Agent 名称，不保留双轨脚本。现有 strict mock 中也包含 `agent: 'coder'` 一类旧示例，必须整体迁移。

建议测试默认显式选择：

```text
fast-manager
fast-coder
fast-inspector
fast-devops
fast-browser
fast-meditator
deep-reviewer
fast-orchestrator
```

但每个选择必须写在 scenario 中，不能由测试框架隐式填入。

---

## 20.2 新增场景

```text
agent-config-gate.json
agent-explicit-only.json
fallback-fast-cycle.json
fallback-deep-cycle.json
fallback-infinite.json
fallback-restart.json
blogger-fallback-cycle.json
executor-fallback-cycle.json
managed-agent-model-binding.json
```

对应：

```text
agent-config-gate-canary.mjs
agent-explicit-only-canary.mjs
fallback-fast-cycle-canary.mjs
fallback-deep-cycle-canary.mjs
fallback-infinite-canary.mjs
fallback-restart-canary.mjs
blogger-fallback-cycle-canary.mjs
executor-fallback-cycle-canary.mjs
managed-agent-model-binding-canary.mjs
```

---

## 20.3 Strict Mock lane

lane 不应只记录 canonical role。

建议：

```js
function lane(agent, turn = 1, session = agent) {
  return {
    scenario: 'gate',
    session,
    agent,
    role: canonicalRole(agent),
    tier: tier(agent),
    turn,
    requestKind: 'chat'
  };
}
```

Mock 必须同时验证：

```text
agent name
canonical role
actual model
tools
messages
request kind
```

否则无法证明：

```text
deep-reviewer Agent
确实使用 deep-reviewer 配置的模型
```

---

# 二十一、版本和发布

## 21.1 版本文件

同步修改：

```text
package.json
build-package.json
next/package.json
```

版本：

```json
{
  "version": "0.5.0"
}
```

开发过程：

```text
0.5.0-rc.1
0.5.0-rc.2
...
0.5.0
```

---

## 21.2 CHANGELOG 必须醒目标记

```markdown
## 0.5.0

### Breaking changes

- All Wanxiangshu agents now require explicit `fast-*` or `deep-*` names.
- Unprefixed agent names are no longer supported.
- `build` and `plan` aliases have been removed.
- Agent-to-model bindings are read exclusively from OpenCode `opencode.json`.
- All Wanxiangshu model environment variables have been removed.
- Wanxiangshu no longer persists or overrides model IDs.
- Provider fallback now cycles A/A/B/B indefinitely.
- Provider retry count no longer kills a Logical Run.
- Blogger and Executor Agent are now internal fast/deep pairs.
- Pre-0.5.0 runtime journals are not supported.
```

---

## 21.3 MIGRATION 只写断裂步骤

迁移说明应要求用户：

1. 停止 OpenCode；
2. 归档或删除旧 runtime journal；
3. 删除全部模型环境变量；
4. 在 `opencode.json` 添加 20 个 Managed Agent；
5. 将自定义调用中的旧名称全部改为 fast/deep；
6. 修改所有 Manager/Orchestrator 脚本；
7. 重新启动；
8. 检查 Config Gate；
9. 运行 smoke test；
10. 不尝试混用 0.4.x journal 或 config。

不要提供自动迁移器。

---

## 21.4 最终证据

```text
docs/evidence/0.5.0/
├── ENV.txt
├── OPENCODE-CONFIG-REDACTED.json
├── managed-agent-config-gate.txt
├── model-binding-trace.txt
├── explicit-agent-only.txt
├── fallback-fast-12round.txt
├── fallback-deep-12round.txt
├── fallback-restart.txt
├── blogger-fallback.txt
├── executor-fallback.txt
├── permissions-pair-matrix.txt
├── internal-agent-visibility.txt
├── canary-3round-clean.txt
├── npm-pack.txt
├── install-out.txt
├── import-out.txt
└── TARBALL.sha256
```

配置证据必须脱敏，不得提交真实 token、API key 或私有 endpoint 凭据。

---

# 二十二、0.5.0 发布 No-Go

出现任一项不得发布：

```text
仍支持 manager/coder/reviewer 等旧 Agent 名称

仍支持 build 或 plan alias

任意公开创建操作可以省略 fast/deep

万象术仍从环境变量读取模型

万象术发送 Prompt 时仍设置 Model

Authority journal 仍保存 model ID

Fallback 第四次失败仍判死

Fallback 在成功后擅自重置

fast/deep 同角色工具权限不同

fast/deep 使用不同 system prompt

Blogger 或 Executor 名称进入 LLM tool schema

Blogger 不是从 fast-blogger 开始

Executor summary 不是从 fast-executor 开始

重启后 fallback cursor 丢失

重启后 journal 旧 model 覆盖新 opencode.json

Host 收到 Agent 后没有使用该 Agent 对应的模型

12 次 retry 后不再继续物理请求

拼错 Agent 被静默当作新 handle

旧 journal 被猜测性迁移
```

---

# 二十三、建议写入 SSOT 的冻结文本

> Wanxiangshu 0.5.0 使用 OpenCode Managed Agent identity 作为模型选择的唯一入口。每个公开工作角色必须拥有两个准确命名的 Agent：`fast-ROLE` 与 `deep-ROLE`。用户和 LLM 创建新工作时必须显式选择其中之一；无前缀旧名称、`build`、`plan` 以及任何隐式默认均不受支持。
>
> OpenCode 宿主最终解析后的 `opencode.json.agent` 是 Agent inventory 和 Agent→Model 绑定的唯一事实源。Wanxiangshu 不读取模型环境变量，不维护模型 catalog，不持久化模型 ID，不覆盖 Prompt 的 model 字段。Wanxiangshu 只向 Host 提供 EffectiveAgent，实际模型由 Host 根据该 Agent 的配置解析。
>
> 对公开 Agent，用户选择的 Agent 为 Side A，其同角色相反 tier Agent 为 Side B。选择 `fast-ROLE` 时，`A=fast-ROLE, B=deep-ROLE`；选择 `deep-ROLE` 时，`A=deep-ROLE, B=fast-ROLE`。Fallback cursor 按 `A/A/B/B/A/A/B/B/...` 无限循环。Provider retry 只推进 modulo-4 cursor，不存在因累计 retry 数而产生的 Dead 状态。
>
> `fast-blogger/deep-blogger` 与 `fast-executor/deep-executor` 是 Host 内部 Agent，不向任何 LLM 工具 schema 暴露。每个新的 Blogger 或 Executor summary Logical Run 固定从 fast Agent 开始，以 deep Agent 为 B，并使用相同的无限 AABBAABB 循环。
>
> Fast 与 Deep 只改变 OpenCode Agent identity 及其在 `opencode.json` 中绑定的模型。它们不改变 Canonical Role、system prompt、工具权限、Review 协议、Companion eligibility、Logical Run、Authority Root 或 completion 语义。Fallback 只能改变 AttemptExecutionProfile.EffectiveAgent，不能改写 AuthorityExecutionProfile.SelectedAgent 或 LastAuthorityProfile。

---

# 二十四、最终目标结构

```text
opencode.json
    │
    ├── fast-reviewer → medium model
    ├── deep-reviewer → full model
    ├── fast-coder    → fast model
    ├── deep-coder    → deep model
    ├── fast-blogger  → cheap model
    ├── deep-blogger  → stronger model
    ├── fast-executor → cheap summary model
    └── deep-executor → stronger summary model

Authority Root
    │
    ├── SelectedAgent
    ├── PeerAgent
    ├── CanonicalRole
    └── SelectedTier

Fallback cursor
    │
    ├── 0 → A
    ├── 1 → A
    ├── 2 → B
    ├── 3 → B
    └── retry → (offset + 1) mod 4

Attempt
    │
    └── EffectiveAgent
            │
            └── OpenCode Host
                    │
                    └── opencode.json resolves model
```

## 最终不变量

```text
Agent 决定模型。
配置决定 Agent 的模型。
万象术不决定模型。

新工作必须显式选择 fast 或 deep。
Continuation 不改变用户选择。
Fallback 只改变当前 EffectiveAgent。

AABBAABB 永久循环。
没有 retry 次数死亡。

公开工作 Agent 可选择 fast/deep。
Blogger 和 Executor 内部固定 fast 起步。

角色权限仍然由静态源码控制。
模型绑定只由 opencode.json 控制。
```
