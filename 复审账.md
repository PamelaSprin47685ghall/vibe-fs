# 复审账：万象术 56 包端到端巡检与人工裁决总账

> 依据《人工巡检：从一个 Coder 到完整协作》（调查基线 `c079a8fcd`，当前 HEAD `f242eef07`）端到端执行全仓 56 包（A～H 段，01～56 站）架构审查、规范核对与执行验证。
> 本账本记录全站审查结论，并集中提取**真正需要人工裁决/处理的问题**（产品意志冲突、规范与实现漂移、文档历史陈旧、真机物理环境与凭据隔离边界）。

---

## 一、 核心待人工裁决/处理事项清单

经过对全部 56 个包的 WHY/WHAT/HOW、代码实现（F#）、公开 Semantic Surface、测试套件（647 个测试文件、3731 个自动化测试用例）及构建门禁（`scripts/check.mjs`、`scripts/verify.mjs`）的端到端穿透审查，梳理出以下 6 项真正需要人工（架构师/产品负责人）裁决或处理的关键问题：

### 1. 【产品合同冲突】`requirement-grounding` 的介入语义与 INDEX.md 历史叙述分歧
- **事实位置**：
  - `requirements/INDEX.md` 第 142 行：“路径命中的规范与测试需要自动、去重、可重放地进入开发上下文，**且首次 mutation 必须先 grounding**。”
  - `requirements/requirement-grounding/WHAT.md`（REQUIREMENT-GROUNDING-008）：“当编辑、写入、移动或删除操作触碰受覆盖路径，而当前上下文尚缺相关 grounding 材料时，系统可以在同轮或紧随其后的 provider projection 中自动补入规范读取，**但不得阻止、延期、回滚、改写或要求重发原 mutation。Grounding 不拥有 mutation admission，也不得把“尚未读规范”编码成工具失败**。”
  - 核心实现与 GAP-020：已在代码中彻底移除 mutation admission 阻断逻辑，执行 fail-open。
- **冲突焦点**：INDEX.md 中保留了强阻断（“首次 mutation 必须先 grounding”）的旧叙述，而 WHAT/HOW/代码实现均已切换为弱介入（不阻断 mutation）。
- **待人工裁决**：确认是否正式采纳弱介入语义并修订 `INDEX.md`，删除“首次 mutation 必须先 grounding”的过时表述，使索引与 normative WHAT 严格一致。

---

### 2. 【评分模式残余】`relay-assessment` 八维评分的三档枚举 vs 整数评分措辞
- **事实位置**：
  - `requirements/relay-assessment/WHAT.md`（ASSESS-001 标题与正文）：标题写 `ASSESS-001: review schema 恰有八个必填 0..10 整数`，正文却规定 `每个维度评分必须为 "PERFECT"、"REVISE" 或 "N/A" 三个枚举量之一；禁止缺省维度、未知字段、整数、浮点、null、总 verdict 与平均分`。
  - `requirements/relay-assessment/WHAT.md`（ASSESS-008）：正文写 `非全 10 选择修复指派（runtime/manager-work），全 10 选择关闭退场（runtime/manager-finish）`。
  - 代码实现（`Mission/Relay/Assessment/Model.fs`、`ReviewTool.fs`）及测试套件：均已严格实现并断言 `PERFECT / REVISE / N/A` 三档枚举，无任何 0..10 整数逻辑。
- **冲突焦点**：旧版本的“0..10 整数”与“全 10 满分”概念在规范标题与部分段落中未清理干净，与现行代数类型设计冲突。
- **待人工裁决**：确认统一废止“0..10 整数”历史术语，修正 ASSESS-001 标题与 ASSESS-008 文案为“八维三档评级”与“无 REVISE（全 PERFECT 或 N/A）通过”。

---

### 3. 【角色与档位语义】`delegation` 的 fast/deep/tier 措辞与 `office-capability` 权能不变性
- **事实位置**：
  - `requirements/delegation/WHAT.md`（DELEG-002、DELEG-010）：保留 `fast 与 deep 档位`、`owner effective tier 决定 delegate tier` 描述。
  - `requirements/office-capability/WHAT.md`（OFF-003）：`同一 Office 的 authority 不变。每个 Role 恰对应一个 Persona，不存在 fast/deep 档位或组合名解析语义`。
  - `requirements/participant-identity/WHAT.md`（PID-001..004）：Role 与 Persona 是一一映射且在 run 内不可变，调度通过 MJS scheduler 路由至模型，不通过角色别名扩权。
  - 测试实现（`delegation/tests/sync-delegate.test.mjs`）：`WHAT[DELEG-010] EXEC_026_tier_is_an_ignored_compat_param` 明确将 tier 处理为 ignored compatibility parameter。
- **冲突焦点**：DELEG-010 的标题与正文仍保留 tier 概念，而实际运行时已将 tier 退化为忽略参数，权能完全由 Role/Office 决定。
- **待人工裁决**：裁决是否在 DELEG-002/010 中明确将 tier 标记为仅供老客户端/旧调用向下兼容的已废弃字段（compat only），规范语义完全收敛至 Role 权能模型。

---

### 4. 【文档与元数据漂移】根目录 README.md 与 requirements 索引的包计数与工具名滞后
- **事实位置**：
  - `README.md` 中仍列出旧工具名 `fork-manager`、`fork-pty`，而当前系统已演进为 `fork`、`resume`、`join`、`commission` 等语义工具。
  - `requirements/INDEX.md` 与 `requirements/README.md` 开头仍称“54 张 boundary card”或“54 包索引”，而实际 `requirements/` 下已有 56 个包含 `WHAT.md` 的包目录。
  - `relay-assessment/HOW.md` 引用了已合并重构的 `Mission/Relay/Assessment/Admission.fs`（实际逻辑位于 `Mission/Relay/Fold.fs` 与 `ReviewTool.fs`）。
- **冲突焦点**：对外展示文档与实现及规范树演进存在时间差，易对新接入者与外部审计造成误导。
- **待人工处理**：安排一次统一文档刷新，修正包总数（56 包）、工具契约名及 HOW.md 源码落点指向。

---

### 5. 【真实环境物理凭据与受控执行边界】真机运行与生产环境隔离
- **事实位置**：
  - 本机 OpenCode CLI 版本为 `1.18.29`（与 package.json peerDependencies 匹配）。
  - `~/.config/opencode/wanxiangshu.mjs` 已配置模型路由策略池（含 coder、inspector、manager 等多角色目标）。
  - 当前 shell 环境中仅配置了 `COPILOT_PROVIDER_API_KEY`，缺少各商业 provider 的独立密钥；若直接以 `--agent coder` 启动真机并向公网发请求，受限于真实模型费用及网络环境。
- **待人工确认**：
  - 确认用户日用评测时的隔离 Profile（独立 `HOME`、独立 `XDG_*` 目录、获准的 provider key 表）。
  - 确认在未注资独立商用 key 的环境下，日常流水线以内部 `verification-system`（含 StrictMock 故障注入服务）为权威闭合证据，真机手工试运行仅在获准的沙箱目录中进行。

---

### 6. 【故障注入基础设施演进】E 段受控故障注入由 StrictMock 正式承接
- **事实位置**：
  - E 段各站（31～41）涉及网络中断、超时、乱序、429、500、晚到回执等极端物理场景。
  - 《人工巡检.md》提出由长寿测试的 StrictMock 服务器演进支持确定性故障注入，以替代“手拔网线”或“因无故障注入直接记为未实测”。
- **待人工裁决**：确认将 StrictMock 故障注入端点作为团队标准的受控真机回归设施，正式纳入 CI/CD 证据链，免除人工重复试错。

---

## 二、 56 站端到端巡检审查结论总览

| 段落 | 站号 | 包名称 | 用户意志与规范状态 | 源码实现与架构边界 | 自动化证明（Executable Proof）状态 | 巡检结论 |
|---|---|---|---|---|---|---|
| **A** | 01 | `requirement-system` | 唯一语义所有权，WHAT 为唯一规范合同，禁止影子规范 | 无代码实现，纯元治理与规范合同 | `tests/change-lifecycle.test.mjs`（GAP-003/004/005 已闭合） | **通过**（存在 INDEX 54 vs 56 文档微漂移，见问题 4） |
| | 02 | `verification-system` | 五层证据金字塔（0-Static, 1-Pure, 2-Temporal, 3-Adapter, 4-LongStroke, 5-Release），禁止跨级，fail-closed | `scripts/verify.mjs`、`scripts/check.mjs`、统一调度器 | `tests/run.mjs`、`tests/physical-contract.test.mjs`（GAP-006 已闭合） | **通过**（执行全量 3731 测试 0 失败，全阶梯锁定） |
| | 03 | `js-semantic-surface` | 所有测试采用 JS-native，禁止测试直接引用 Fable 内部表示，Surface Manifest 强校验 | 173 个注册 Surface，编译隔离，模块链接验证 | `js-boundary-gate.mjs`、`test-surface-scan.mjs`，0 debt | **通过**（编译产物与测试完全解耦） |
| | 04 | `structured-workflow` | 原生 CE 驱动控制流，严禁第二业务运行时、禁止 Durable PC，Subsystem 为唯一架构粒度 | 27 个 Subsystems、276 个 compile shards，无跨 Subsystem SCC 环 | `subsystem-boundaries.test.mjs`、`check.mjs` 静态门禁（GAP-031/033 已闭合） | **通过**（架构清晰，纯函数与副作用分缝） |
| **B** | 05 | `host-boundary` | 最小外部 Host 适配，无额外协议猜测，HOST-010 因果绑定 | `OpenCode/Plugin/Plugin.fs`、`OpenCode/Host/` | `host010-run-id-equivalence.test.mjs`、`magic-todo-membrane-canaries.test.mjs`（GAP-007/008） | **通过**（真实挂载 hooks 齐全，生命周期确定） |
| | 06 | `session-ontology` | 区分 Execution Class、Ownership、Attachment，会话不是职责 | `Execution/Session/Ownership.fs`、`Attachment/` | `session-ontology/tests/*.test.mjs` | **通过**（概念正交，未引入机器拓扑泄漏） |
| | 07 | `participant-identity` | Role × Persona 稳定绑定，换执行者（模型）不换人，Predictor 仅内部 | `Participant/Persona/Identity.fs` | `participant-identity/tests/*.test.mjs` | **通过**（执行绑定与身份严格分离） |
| | 08 | `provider-language` | 自然语言环境稳定，协议标识符（路径、代码、工具名）严禁翻译 | `Participant/Provider/Language.fs` | `provider-language/tests/*.test.mjs`、双语对齐测试 | **通过**（中文/英文环境无缝切换，标识符防污染） |
| | 09 | `office-capability` | 五分法（Coder/Inspector/DevOps/Browser/Inquiry），由有资格产生的后果定义能力 | `Foundation/OfficeCapability.fs`、`Foundation/Roles.fs` | `office-capability/tests/*.test.mjs` | **通过**（各角色责任互不重叠，Coder 不认证运行结果） |
| | 10 | `capability-enforcement` | 描述、Schema、SDK、Runtime Gate 四层同构，严格拦截越权 | `OpenCode/Tools/StaticTools.fs`、`ToolRegistry.fs` | `capability-enforcement/tests/*.test.mjs`、`capability-isomorphism-gate.mjs` | **通过**（执行层物理拦截有效，无模型越权空间） |
| | 11 | `cognitive-environment` | 长期世界观与瞬时任务分离，assume 仅为推演画布不产生新义务 | `Resources/PromptResources.fs`、`resources/provider/` | `cognitive-environment/tests/*.test.mjs` | **通过**（认知层级分明，无角色迷航） |
| | 12 | `action-affordance` | 工具决策点明确正负边界，错误信息指导保守修复 | `resources/provider/tool/`、`Repository/Programming/Js/Description.fs` | `action-affordance/tests/*.test.mjs` | **通过**（工具描述与参数自洽，失败引导清晰） |
| | 13 | `participant-horizon` | 机器拓扑不穿透，模型只看改变合法行动的最小事实 | `Execution/Session/OpenCode/HorizonTool.fs` | `horizon-surface.test.mjs`（GAP-028 已闭合） | **通过**（隔离彻底，内部会话 ID 与句柄不泄漏） |
| | 14 | `provider-projection` | typed 意图确定性投影，投影格式严禁反向创造权威 | `Participant/Provider/Projection/` | `projection.test.mjs`（GAP-026 已闭合） | **通过**（序列化纯净，失败状态保真） |
| **C** | 15 | `repository-investigation` | 调查以最少、充分的真实观察建立结论，推理不等于证据获取 | `Repository/Investigation/` | `repository-investigation/tests/*.test.mjs` | **通过**（Inspector 真实只读，支持证据定位） |
| | 16 | `requirement-grounding` | 路径触碰时自动读取规范，弱介入不阻断 mutation，内容去重 | `Requirement/Grounding/` | `scope-resolution.test.mjs`、`grounding-delivery.test.mjs`（GAP-018/019/020/021） | **待裁决**（实现已完备，见问题 1） |
| | 17 | `repository-programming` | 沙箱化编辑规划、Preflight 与原子事务，提供渐进 edit 与 rewrite | `Repository/Programming/Js/` | `js-edit.test.mjs`、`js-surface.test.mjs`（GAP-030 已闭合） | **通过**（支持精确局部替换，近似匹配只报诊断不改写） |
| | 18 | `time-capability` | 物理时钟显式注入，语义与逻辑验证完全基于虚拟时钟端口 | `Process/NodeTiming.fs`、`Process/VirtualTiming.fs` | `pair-session-elapsed.test.mjs`（GAP-009 已闭合） | **通过**（消除真实 sleep 竞态） |
| | 19 | `process-execution` | 真实进程/PTY 有界执行、可靠终止与真实退出码捕获 | `Process/ProcessRunner.fs`、`NodeProcessHost.fs` | `process-execution/tests/*.test.mjs` | **通过**（DevOps 控制终端可靠，进程回收彻底） |
| | 20 | `output-distillation` | 大输出有损但诚实地压缩，片段严禁冒充整体成功 | `OpenCode/Tools/Distillation.fs`、`Spool.fs` | `output-distillation/tests/*.test.mjs` | **通过**（首尾截断带明确范围标识，不丢失错误） |
| | 21 | `causal-wait` | 因果等待可观测、可诊断，但诊断信息不升级为业务事实 | `Execution/Session/Wait/` | `causal-wait-boundary.mjs`、`causal-wait/tests/*.test.mjs` | **通过**（看门狗按因果推进续期，卡住可明确归因） |
| **D** | 22 | `durable-events` | 不可变事实、原子追加、确定性 fold，唯一 Local NDJSON 日志 | `Persistence/EventStore/`、`Journal/` | `local-process-event-log.test.mjs`、`canonical-integrator.test.mjs`（GAP-013/014） | **通过**（断电崩溃截断安全，重放幂等） |
| | 23 | `effect-accounting` | 外部副作用 Requested / Unknown / Accepted 三分型，Unknown 不伪装 | `Interaction/Dispatch/`、`Change/` | `effect-accounting/tests/*.test.mjs` | **通过**（回执丢失保持 Unknown，杜绝擅自重复副作用） |
| | 24 | `semantic-trace` | 原始语义历史 append-only，真实对话与工具调用可回溯 | `Context/Trace/`、`SemanticTraceSurface.fs` | `semantic-trace/tests/*.test.mjs` | **通过**（审计历史不可篡改，压缩仅替换表示） |
| | 25 | `context-compression` | 失败驱动的压缩，Blogger 伴侣自然总结，Opening 与 Todo 不丢失 | `Context/Companion/Blogger/`、`Context/Prefix/` | `injected-context-reanchor.test.mjs`、`ctx-opening-floor.test.mjs`（GAP-022/026） | **通过**（主模型不写总结，冷边界替换无缝） |
| | 26 | `prefix-stability` | 同一 epoch 内已呈现前缀保持稳定，冷边界提交才允许更替 | `Context/Prefix/` | `prefix-stability/tests/*.test.mjs` | **通过**（Byte-level 前缀稳定，保护 prompt 缓存） |
| | 27 | `work-record` | 跨交接的成果物化为 bounded WorkRecord，非全生命周期 dumps | `Mission/WorkRecord/` | `work-record/tests/*.test.mjs` | **通过**（因果窗口严格限定在本次 charge 范围内） |
| | 28 | `behavior-diagnosis` | 病理诊断须在明确 trigger/negative/distinction 下成立，规则冻结 | `Enforcer/`、`resources/enforcer/` | `behavior-diagnosis/tests/*.test.mjs`（含 BD-018 冻结断言） | **通过**（无模糊相似词处罚，误诊防护充分） |
| | 29 | `guidance-delivery` | 首次完整告知，覆盖内简要提醒，不反复念经，重锚后可复出 | `Enforcer/Guidance/` | `tip-guidance-delivery.test.mjs` | **通过**（注意力消耗最低，指导针对性强） |
| | 30 | `attention-regulation` | 显式 enough/abandon/defer，解除自创心理债，不伪造任务完成 | `Interaction/Attention/` | `attention-regulation/tests/*.test.mjs` | **通过**（主动止损动作合法化，不改变业务事实） |
| **E** | 31 | `execution-failure-policy` | 封闭错误代数，唯一纯策略裁决 retry/fallback/fatal，单点结算 | `Execution/Failure/` | `execution-failure-policy/tests/*.test.mjs` | **通过**（互斥决策，不发生多重结算或失控重试） |
| | 32 | `interaction-authority` | 物理消息不等于任务授权，typed provenance 才创建 LogicalRun | `Interaction/Authority/` | `interaction-authority/tests/*.test.mjs` | **通过**（文档指令或内部消息绝不冒充用户授权） |
| | 33 | `managed-chat-execution` | 消息级执行闭合，durable acceptance → provider start → terminal | `Execution/Session/ChatExecution/` | `facts-roundtrip.test.mjs`、`process-restart-canary.test.mjs`（GAP-032） | **通过**（ exact key 精确幂等结算） |
| | 34 | `execution-model-routing` | MJS scheduler 依据 role+running 动态分配 model lease，容量无锁 | `OpenCode/Host/ModelRouting.fs`、`ModelCapacity/` | `execution-model-routing/tests/*.test.mjs`（GAP-016/024/025/034） | **通过**（跨进程租约共享与恢复成功清零验证完毕） |
| | 35 | `managed-session-lifecycle` | 显式 OneShot 与 Reusable，关闭与 Abandon 必须由不可逆逻辑损失授权 | `Execution/Session/`、`Attachment/` | `shutdown-drain-contract.test.mjs`、`interrupt-boundary.test.mjs`（GAP-029） | **通过**（进程退出不误杀 active durable 任务） |
| | 36 | `dispatch-protocol` | 跨不可靠 Host 发送，Placed / AcceptanceUnknown / Accepted 区分 | `Interaction/Dispatch/` | `dispatch-protocol/tests/*.test.mjs` | **通过**（网络未知状态保留证据，不静默丢弃） |
| | 37 | `provider-attempt-recovery` | 失败后有界切换物理 binding 继续原任务，角色与记忆不变 | `Participant/Provider/Attempt/` | `lwr-retry-before-condemn.test.mjs`（GAP-034） | **通过**（首败保留目标重试，LWR 失败才剔除） |
| | 38 | `host-provider-failure-ownership` | 万象术接管 provider 恢复，Host 重试归零，避免双重重试与弹窗轰炸 | `OpenCode/Host/ManagedAgentConfig.fs`、`ProviderFailurePresentation.fs` | `host-provider-failure-ownership/tests/*.test.mjs` | **通过**（错误呈现边界明确） |
| | 39 | `degeneration-guard` | 监控 Token 多样性，病态重复时主动终止并要求改写 | `Execution/Session/LoopDetector.fs` | `degeneration-guard/tests/*.test.mjs` | **通过**（正常长思维不误杀，病态循环及时拦截） |
| | 40 | `crash-reconciliation` | 进程重启后仅从 durable facts + 物理观测恢复，不猜中间态 | `Execution/Session/Recovery/` | `explicit-continue.test.mjs`（GAP-017 已闭合） | **通过**（无缝 dynamic restart briefing 注入） |
| | 41 | `durable-convergence` | 多个副本按对象语义与 writer-stream 合并收敛，不靠墙钟猜胜者 | `Persistence/EventStore/`、`Git/Hook/` | `writer-stream-sync.test.mjs`（GAP-013 已闭合） | **通过**（副本同步确定性一致） |
| | 42 | `delegation` | 语义 charge 委托，同步等待本 work unit，异步 fork 仅返回承接 | `Execution/Delegation/Fork/`、`SyncDelegate/` | `fork-tool.test.mjs`、`completion-settlement.test.mjs`（GAP-011/012/027/028） | **待裁决**（tier 术语清理，见问题 3） |
| | 43 | `concern-routing` | concern-addressed 邮箱，下一次自然 Pair Hint 边界交付 | `Interaction/Concern/` | `concern-routing/tests/*.test.mjs` | **通过**（消息不升级为命令，拓扑解耦） |
| | 44 | `intra-participant-parallelism` | 一人多分身（Fission），责任与终态仍单一，all-or-none admission | `Execution/Fission/` | `fission-domain.test.mjs`、`fission-runtime.test.mjs`（GAP-010 已闭合） | **通过**（同文件防竞态，Reunion 确定） |
| | 45 | `knowledge-reuse` | 历史 Casebook 知识仅作线索与预热，不作为当前证明 | `Repository/Knowledge/Casebook/` | `knowledge-reuse/tests/*.test.mjs` | **通过**（源码修改后旧证明诚实失效） |
| | 46 | `external-investigation` | Browser 带 provenance 采集外部事实，外部发现不直接产生仓库义务 | `Browser MCP` 边界、`resources/provider/role/browser/` | `facts-not-obligations.test.mjs`（GAP-002 已闭合） | **通过**（外部推论与内部事实隔离） |
| | 47 | `epistemic-reasoning` | 区分假设与观察，Sphinx 认识论收敛，概率不冒充确定性证书 | `Sphinx/Core/`、`Sphinx/Runtime/` | `sphinx-inquiry.test.mjs` 等 40+ 用例 | **通过**（A*/MCTS 算法核自洽，协议状态机严谨） |
| | 48 | `speculative-investigation` | 可丢弃 DryRun，零影响 authoritative world，随时可关闭 | `Strength/`、`Strength/OpenCode/Settings.fs` | `dry-run-shadow.test.mjs`（GAP-015 已闭合） | **通过**（关闭不影响产品正确性，无泄露写入） |
| | 49 | `institutional-learning` | celebrate/regret 压成 ABSORB/BIRTH/DISCARD，规则库有增有减 | `Enforcer/InstitutionalLearning/` | `institutional-learning/tests/*.test.mjs` | **通过**（私人偏好不升格为永久规则） |
| **G** | 50 | `obligation-ledger` | 账本记录欠世界的责任，未做完不等于挂起，不编造虚假状态 | `Mission/Obligation/Todo/` | `obligation-ledger/tests/*.test.mjs` | **通过**（Magic Todo 纯净投影） |
| | 51 | `relay-incumbency` | 独立审视接责，一任一迭代，退休永不复活，只读审查先行 | `Mission/Relay/Contract.fs`、`Facts.fs`、`Fold.fs` | `manager-job-no-resurrection.test.mjs`（GAP-001 已闭合） | **通过**（迭代边界清晰） |
| | 52 | `relay-assessment` | 八维三档独立质量评审，无 REVISE 签发证书，同任不可复评 | `Mission/Relay/OpenCode/ReviewTool.fs`、`Assessment/Model.fs` | `review-tool-contract.test.mjs`、`assessment-transaction.test.mjs` | **待裁决**（整数评分措辞清理，见问题 2） |
| | 53 | `relay-context-projection` | 审计保留全量历史，下一任只给权威消息与本轮材料，不塞上一轮答案 | `Mission/Relay/OpenCode/NarrativeTransform.fs` | `relay-context-projection/tests/*.test.mjs` | **通过**（盲评上下文完全隔离） |
| | 54 | `relay-retirement` | 退出是唯一正常出口，仅活资源阻塞退休，Accepted 持证书退出 | `Mission/Relay/OpenCode/SuicideTool.fs`、`Retirement/` | `suicide-tool.test.mjs`、`retirement-lifecycle.test.mjs` | **通过**（脏工作树与测试红引出继续迭代而非阻断退出） |
| **H** | 55 | `change-integration` | 独立 Git 道路并行调查，短 CAS 临界区发布，冲突重做同一道路 | `Change/`、`Change/Orchestrator/` | `change-integration/tests/*.test.mjs` | **通过**（长任务不全局串行，旧证书防误发布） |
| | 56 | `distribution` | 交付物独立可用，零源码树依赖，资源完整携带，verify:release 门禁 | `package.json`、`scripts/verify-package.mjs` | `package.json` 导出验证、dist 模块链接完整性测试 | **通过**（构建产生 820 个链接模块与 173 个 Surface，脱离源码干净运行） |

---

## 三、 执行验证与自动化证据摘要

1. **静态代码与架构检查**：
   - 运行 `node scripts/check.mjs`：全仓 17 个架构门禁全部通过（0 issues）。
2. **构建与链接检查**：
   - 运行 `node scripts/build.mjs`：173 个 Semantic Surfaces 完整注册，820 个编译 JS 模块依赖闭包完全链接，无悬空 import。
3. **自动化测试套件执行**：
   - 运行 `node requirements/verification-system/tests/run.mjs`：
   - **结果：647 个测试文件全部通过，3731 个测试用例无一失败（0 failed）**，用时 17.3 秒。
4. **已知 GAP 闭合审计**：
   - 查验 `requirements/GAP.md`：GAP-001 至 GAP-034 全部处于 `CLOSED` 状态，相关 unit oracle 均已在包内 `tests/` 落地并生效。

---

## 四、 建议的人工处理行动项（Action Items）

1. **批准规范文本修订**：
   - 修改 `requirements/INDEX.md`：修正第 142 行 `requirement-grounding` 说明，删除“且首次 mutation 必须先 grounding”，与当前 WHAT-008 弱介入保持一致。
   - 修改 `requirements/relay-assessment/WHAT.md`：更新 ASSESS-001 标题与 ASSESS-008 正文，消除“0..10 整数”与“全 10”历史字眼。
   - 修改 `requirements/delegation/WHAT.md`：在 DELEG-002/010 中将 fast/deep/tier 明确标注为已废弃的兼容占位符。
2. **文档刷新**：
   - 更新 `requirements/INDEX.md` 与 `requirements/README.md` 的包目录统计至 56 包。
   - 清理 `README.md` 中的旧工具名（`fork-manager`、`fork-pty`）。
3. **环境隔离指引生效**：
   - 按照《人工巡检.md》§3 的环境隔离规范，使用独立的测试工作目录（如 `/tmp/wxs-lab`）和无敏感配置的 profile 展开后续真机任务。
