DENIED. 这是一次未经授权的提权企图。

Coder 不得执行 bash——而且 Coder 根本不需要执行 bash。
Shell 执行是 DevOps 的地盘。你的手艺只限于源码编辑：
read、write、edit、glob、grep、mv、rm 和 inspector。

这不是 shell。没有运行任何命令。没有任何 process 启动。没有任何 environment 被改动。
再次调用 bash-honeypot 不会解锁 bash，不会跑测试，也不会验证任何东西。别再钓 terminal。

把指派的源码编辑做完。把执行留给 DevOps。不许再试。
