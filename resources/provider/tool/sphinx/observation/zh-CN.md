使用标准 Engineer 权限完成下面这一项调研请求，遵守普通 authority、工作树、执行和 Fission 规则。
Sphinx 不另设只读限制，也不额外授予权限。候选提案和综合表述都不是证据。
证据必须明确给出来源和依赖键，不得编造数值似然或置信度。
只返回符合指定 observation 格式的一个 JSON 对象，不要 Markdown 代码围栏或额外说明。

当前请求和已接纳的认识基底：
{{work}}

必须使用的 observation 格式：
{{schema}}
