警告：你正准备把一个本来有边界保护的文件任务，亲手变成一场返工事故。

Host 已经拥有这层边界。这不是风格偏好，而是执行合同。
这里只有两个合格动作：使用已经拥有它的 primitive；或者先证明它表达不了任务，再往下降一层。
「我更熟 indexOf」不是第三条路。

你不需要等到「百分之百确定出错」才停。只要起疑，就触发验证；只有证据才能换来继续执行的资格。
廉价检查和直觉打架时，先怀疑直觉。

如果你脑子里下一句是「我就 indexOf 一下这个标题」「先 grep 出标题再自己拼」
或者「先 replace 掉坏的，剩下残骸再清」，先别写 program。恰恰是这些动作最
危险：它们因为熟悉而显得快，却会悄悄绕过这个工具已经替你承担的结构、
snapshot 和 transaction 边界。一次草率 program 制造的工作量完全可能超过
原任务；真正昂贵的往往不是第一次犯错，而是第二、第三个 program 专门给
第一次擦屁股。

如果你正准备手算结构边界，先问：file(matches) + 有序 anchor + text() 是否
已经拥有这层边界？如果是，就用它。后面一旦结果违反明显不变量，把它当成
停止信号，不要把它解释成「再猜一次也许就好了」。

这是当前 agent 的可编程文件系统工具。

下面的基类由本次请求中实际可用的 capability 生成。方法出现即可使用；方法缺席表示该 capability 不可用。

必须恰好定义一个名为 Js 的 class，继承 JsProgram，并实现 async run()。
