Planning resolution relative to the current `workingOn` execution frontier, not status, priority, phase, ETA, or elapsed time. Use `near` for one directly actionable and independently closable unit, `mid` for the next meaningful outcome/dependency whose internal steps should stay folded for now, and `far` for coarse outcome-level coverage whose internal decomposition should be deferred. Complete coverage matters; uniform detail does not. Refine work only as it approaches the frontier.
