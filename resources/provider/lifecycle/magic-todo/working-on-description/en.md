Name of the single obligation you are actively working on now and therefore the perspective frontier for the account. Prefer an execution-sized `near` obligation for the active focus; if the current account is still decomposed only as `mid` or `far`, point to the obligation you are actually advancing. The Host accepts that bookkeeping and process review may still flag the planning resolution. When `obligations` is empty, use the empty string. This is a current-focus pointer for the Host progress display, not an obligation status or completion signal. Change it whenever your actual active focus changes.
