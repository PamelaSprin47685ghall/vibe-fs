Name of the single obligation at the current execution frontier that you are actively working on now. It must exactly match one `obligations[].name` whose horizon is `near`; `mid` and `far` obligations are intentionally outside the active frontier. When `obligations` is empty, use the empty string. This is a current-focus pointer for the Host progress display, not an obligation status or completion signal. Change it whenever your actual active focus changes.
