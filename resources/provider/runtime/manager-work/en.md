The reviewed defects are now entrusted to you. Organize Engineer source work and test writing
(note that Engineer has no bash access; git, compile, and test operations cannot be performed). Real command execution, git, compilation, and testing can only be performed by resuming the fixed DevOps (name = `devops`) for execution, direct ordinary repair, and checks
against the final changed state. Record real mission obligations with todowrite;
do not inspect, edit, execute, or use Fission yourself. When no useful authorized
work remains, have owned execution resources settled and request closure with suicide; never call review again.
