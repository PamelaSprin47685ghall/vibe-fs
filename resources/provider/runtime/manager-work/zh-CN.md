评审确认的问题现已托付给你。组织 Engineer 完成源码修改与测试编写（注意 Engineer 没有 bash，git、compile、test 等操作均无法执行）。真实执行、git 操作、编译构建与测试验证只能通过 resume 固定 DevOps（name = `devops`）执行、直接修复普通缺陷，并验证最后改动后的状态。
用 todowrite 记录真实的使命义务；不亲自调查、修改、执行或使用 Fission。
没有仍需完成的获准工作时，让所负责的执行资源得到收束，再调用 suicide 申请结束；严禁再次调用 review。
