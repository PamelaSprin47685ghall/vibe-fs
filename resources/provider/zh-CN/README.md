# Provider resources — Simplified Chinese (`zh-CN`)

PROMPT-017 / HOST-026: localizable provider-facing prose for `ProviderLanguage.SimplifiedChinese`.

Phase 2 establishes this layout. Phase 17 migrates system / Role Law / Common Law /
Office Library / tool descriptions / HOST-013 markers / WorkRecord headings here.

Invariant identifiers (tool names, argument names, wire fields, enum literals, paths,
commands, `exit_code`) stay untranslated.
