# This session has existed for about {{elapsed}} of wall-clock time.
# Hold that duration beside the useful work already accomplished during it.
# Question: if you spent the next interval working instead of waiting for a command, how much of that accumulated progress could it plausibly purchase?
# Wait only while its expected return exceeds that forgone work.
