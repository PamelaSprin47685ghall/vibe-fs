ToolOutputInfo 结构化输出：
ToolOutputMessage = { info: InfoItem list; body: string }
InfoItem = Hint | Syntax | Iterator | Status | ExitCode | Signal | TimeoutMs | BodyRef
ToolOutputBodyRef = SeeBelow | SeeBelowTruncated | NoChangeSincePreviousReadWrite
render: YAML front-matter (info:) + body。
tryParse: 从文本解析回 ToolOutputMessage，用于去重/提 hint。
noChangeEnvelope: 只含 NoChangeSincePreviousReadWrite 的空 body。
seeBelowEnvelope: 含 SeeBelow 的 body 包装。
withBookkeepingHints: 追加 todo refresh hint。
todoWriteOutput: todowrite 工具返回值，含 methodology hint + meditator hint。
