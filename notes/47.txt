Shell MessageTransformPipeline:
MessageTransformPlan = { SessionID; Agent; Directory; Excluded; Cleaned }
runMessageTransformPipeline:
  1. applyBacklogProjection: 回溯折叠
  2. encodeMessages: Message<obj> list → obj array
  3. dedupFn: 读取去重（非 excluded 时）
  4. loadCaps: 加载能力文件
  5. loadKgPrelude: 加载知识图谱前注
  6. buildCaps: 注入合成消息
runHostMessagesTransform:
  先 replayReviewForMode（审查状态恢复）
  再 runMessageTransformPipeline
ReviewReplayMode = IfStoreEmpty | Always
CapsLoadPolicy = RequireDirectory | AllowEmptyDirectory
