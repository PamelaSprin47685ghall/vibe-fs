Shell KnowledgeGraphSubmit 提交流程：
buildEntriesFromDrafts: 读取投影 → normalizeDraftIds → allocateRandomHexId → applyDrafts。
submitEntriesForKind: AppendAfterWork → appendDrafts, DailyRewrite → rewriteDayUnderLock。
submitDraftsForKind: buildEntriesFromDrafts + submitEntriesForKind。
BookkeeperLaunch 工作流 (KnowledgeGraphWorkflow):
BackgroundJobSink = { Jobs; RecordResult }
trackBackgroundJob: 添加后台 job 并 fire-and-forget。
drainPendingJobs: 清空并返回所有 job，用于测试等待。
queueBackgroundLaunch: 检查 session API 可用 → 创建子 session → prompt → 记录结果。
queueMuxBackgroundLaunch: Mux 路径 — 通过 delegateToSubAgent 而非子 session。
