# 困惑

## OrchestratorHost 真实 canary：reviewer 永远不出现

### 现象
- `orchestrator-publish-canary.mjs` 中 orchestrator fork manager → manager fork coder → coder write → manager terminal 后，reviewer child 的 mock 请求**从未到达**。
- 所有 worktree 停在 `git log = init`，`publish_proof.txt` 存在但 **untracked**——说明 `finalizeWorktree`（`git add -A` + `git commit`）**根本没有执行**。

### 已排除的原因
1. ~~`IsDirty` 拒绝~~：fork 工具返回了 `Ok worktree`，说明 worktree 创建成功。
2. ~~`SubscribeTerminal` 没注册~~：fake port 验证 `subscribeCallCount = 1`，真实 canary 中 manager child 的 terminal subscription 也注册了。
3. ~~`run.Ready = false`~~：JS 编译确认 `markReady` 在 `SendPrompt` 之前同步执行。
4. ~~`SendPrompt` 失败~~：fork 返回 Ok，manager child 的 mock 请求全部到达。
5. ~~`reviewState` 因 `Journal = None` 失败~~：真实 canary 中 journal 是 Some（PluginHost.createJournal 创建）。
6. ~~completion 被 `JoinPublished` 抢走~~：`JoinPublished` 调 `engine.JoinPublished()`，有独立 mailbox/waiters，不调 `runtime.Join()`。
7. ~~Manager guard 拦截~~：已加 `not (sessionParents.ContainsKey sessionId)`，manager child 有 parent → guard 跳过。

### 核心困惑
`HostForkRuntime.Fork` 的 `None` 分支中：
- `installRun` → `sessions.SubscribeTerminal(childId, callback)` → callback 存入 `eventPort` 的 listener list
- `runtime.Fork(agentId, role, runWork)` → `launch()` → 后台 `runTask` 等 `run.Source.Task`
- `markReady run` → `run.Ready = true`
- `SendPrompt` → opencode 创建 prompt → LLM 处理 → `session.idle` 事件

当 `session.idle` 到达 `HostEventPort.Observe` → `notify childId (Completed)` → 遍历 listeners → 找到 manager child 的 listener → `complete gate pendingRuns sessions run (Completed msgId)` → `run.Source.SetResult(Ok(outputSince...))` → 后台 `runTask` 继续 → completion 进 `runtime` mailbox → `awaitAgent` 的 `runtime.Join()` 取走 → `finalizeWorktree` 执行。

但 `publish_proof.txt` 仍然 untracked——说明 `finalizeWorktree` 没跑。

### 可能的根因（未验证）
1. **`HostEventPort.Observe` 的 `session.idle` 事件没有触发 `notify`**：可能 opencode 对 child session 的 idle 事件格式不同，`sessionIdOf` 提取失败。
2. **`complete` 的 lock 检查失败**：`pendingRuns.TryGetValue run.AgentId` 没找到——可能 `agentId` 不匹配（OrchestratorHost 的 `runtime` 用 `managerId` 作为 agentId，但 ForkRuntime 内部 `agents` dict 的 key 和 `pendingRuns` 的 key 可能不同步）。
3. **`TerminalOutcome` 的 Fable 序列化问题**：`HostEventPort.Observe` 构造 `Completed(MessageId.create ...)` 但 Fable 编译后 `outcome.tag` 可能不匹配 `complete` 里的 `switch (outcome.tag)`。
4. **opencode 对 child session 的 `session.idle` 事件不发给全局事件流**：`trySubscribeHostEvents` 可能只订阅 parent session 的事件，child session 的 idle 事件走了不同的 SSE 流。
5. **`awaitAgent` 的 `runtime.Join()` 永远等待**：如果 completion 没进 mailbox（因为 `complete` 没调 `Source.SetResult`），`Join` 永远阻塞，`finalizeWorktree` 永远不执行。

### 为什么 L1 测试无法覆盖
L1 fake port 直接调 `TriggerTerminal` → listener → `complete`。真实 Host 依赖 opencode SSE 事件流 → `HostEventPort.Observe` → `notify` → listener。这个中间层有 Fable 运行时行为差异，纯逻辑测试无法复现。

### 下一步建议
- 在 `HostEventPort.Observe` 的 `session.idle` 分支加临时日志，确认 `notify` 是否被调用，`sessionId` 是否匹配。
- 检查 opencode v1.18.4 对 child session 的 idle 事件是否走全局 SSE 流。
- 用 `OPENCODE_BIN` 指定的真实 binary 运行一次 canary，捕获完整 SSE 事件日志，确认 child session idle 事件的格式。