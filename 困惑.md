# 未解困惑记录

本文档记录当前工作树中**未能闭合的根因谜团**与**已知未处理缺陷**。只记录现象、证据和假设，不做处理决定。

---

## 谜团一：Restart 期间的幽灵 `FallbackFailureRecorded`（核心未解）

### 现象

`host-restart-canary.mjs` 与 `orchestrator-restart-publish-canary.mjs` 持续红。失败链：

1. Host 重启风暴期间，Journal 中出现 `FallbackFailureRecorded`，`Reason="empty or xml-only assistant turn"`、`ProviderAttempt="empty-xml"`。
2. 每个 session 每次重启累积 1–2 条该事实。
3. 累计 4 条 → session 被判 Dead → Reviewer 无法投递 verdict → Mock 的 `manager-reviewed` / `manager-recovery-fork-coder` expectation 永远不被消费 → canary 超时。

### 矛盾证据（三条互相冲突）

1. **该 Reason/ProviderAttempt 签名只可能来自 `FallbackDetect.observeIdle`**（生产代码中唯一以 `"empty-xml"` 为 attempt 的路径；`DurableFallback.recordFailure` 用 `"manual"`，`HostEventRetry.record` 用 retry attempt 编号）。
2. **失败 session 在 opencode.db 中没有任何 `session.idle` / `session.status` 事件**。事件序列终止于被 abort 的 assistant 消息（`MessageAbortedError`、无 text）。`session.idle` 不持久化到 DB，仅经 SSE/内存转发。
3. **被记录的 AssistantMessageId 指向"好"消息**：例如 `msg_fa0adb8b2…`（"Review confirmed."）、`msg_fa09badde…`（"child-a1."），`finish="stop"`、非空、无 error。`isFailedAssistant` 对这些消息应返回 false。

### 已排除的解释

- `HostEventRetry.record` 的 `isHostShutdown` 守卫（"mocking stopped" / "Service Unavailable" / "Connection reset" / "ECONNRESET"）已在构建产物中确认存在，但失败事实仍出现 → 写入路径不经过 `HostEventRetry.record`，或其 reason 不匹配守卫词表。
- `HostEventRouter.fs` 的 `hostShutdownSessions` 守卫（在 `message.updated` 看到 `MessageAbortedError` 时标记 session，`observeIdle` 跳过）已就位，但失败仍出现 → 守卫时序未覆盖：`session.idle` 先于 `MessageAbortedError` 到达（DB 中 abort 在 seq 21，idle 不持久化无法对时）。

### 调试踪迹消失的次谜团

在 `observeIdle`、`recordFallbackFailure`、`HostEventRetry.record` 三处加入的 `printfn` 诊断踪迹，在 canary 日志（`/tmp/hrestart*.log`）中**零出现**。已查明：插件的 `printfn` 写入的是 **Host（`opencode serve`）进程 stdout**，由 testkit `hooks.onStdoutChunk` 捕获进 ProcessHost 的 `_stdoutBuffer` 环形缓冲，**仅在启动失败时倾倒**——canary 日志根本不含 Host stdout。因此"踪迹未出现"不能证明"代码路径未走"。该假设未经验证（未改为写文件追踪）。

### 当前最优假设（未验证）

Host 关闭期间 `session.idle` 触发时，`observeIdle` 内 `Hydrate` 查 `assistantParts.GetPartsForMessage(terminalMessageId)` 找不到任何 part（关闭竞态：parts 尚未到达或已被清理；原始 SSE 消息不含 `parts` 字段）→ `partsText` 返回 `""` → `isFailedAssistant` = true → 对一条实际完好的消息记录 `empty-xml` 假失败。`hostShutdownSessions` 因 abort 事件滞后而未能拦截。

### 影响面

- `host-restart-canary.mjs`：红（`manager-reviewed` 超时）。
- `orchestrator-restart-publish-canary.mjs`：红（`manager-recovery-fork-coder` 超时）。
- 该两条 canary 未进 P0 标准门（`npm run test:e2e:p0` 14/14 绿的口径不含这两条）。

---

## 谜团二：单元测试 2 项失败（本轮新出现，只记录）

`npm run test:next`：215/217。

1. **`Next_source_files_do_not_exceed_300_lines`**（架构门）：
   - `next/Session/HostForkRuntime.fs`：314 行（runtime-aware linkage 改动使其超长）。
   - `next/Orchestrator.PublishChain.fs`：303 行。
   - `next/OpenCode/HostEventRouter.fs`：323 行（hostShutdownSessions 守卫加入后超长）。
2. **`Dead_decision_survives_journal_rebuild`**（`HostForkRuntimeSessionDeadTests`）："expected dead refusal after rebuild"。疑似 runtime-aware linkage 改动（`childRuntimes` / `LinkedRuntimeIds`）与 Dead 判定在 journal rebuild 后的交互被破坏；未定位。

---

## 谜团三：Runtime-aware linkage 改动的语义风险（未评审）

被终止的子代理在 `HostForkRuntime.fs` / `AgentFacts.fs` / `AgentFacts.Types.fs` / `OrchestratorHost.fs` / `ToolSurface.fs` 引入的 runtime-aware child linkage（`LinkedRuntimeIds`、`childRuntimes`、`onChildCreatedDir`）：

- **意图**：重启恢复时，由"上一个 runtime"链接的 child 不复用（旧 host 中已死），仅复用当前 runtime 链接的 child。
- **编译修复**：`RuntimeId.empty` → `RuntimeId.create ""`；`| false -> true` → `| false, _ -> true`。
- **未回答**：
  - `RuntimeId.create ""` 作为"未知 runtime"哨兵是否语义正确？restore 路径中 `LinkedRuntimeIds` 缺 child 时默认 `""`，与任何真实 RuntimeId 都不等 → 恢复出的 child 永不被复用。这是预期还是过度拒绝？
  - 该改动与 SSOT 的 restart resume 语义（`pendingRuns` 只恢复 child linkage、不恢复在途 Run）是否一致？
  - 未经任何 canary 或针对测试验证。

---

## 已验证事实（非谜团，备查）

- `npm run build`：通过（Postbuild done）。
- 调试踪迹已全部移除：`FallbackDetect.fs`（2 处）、`HostEventRetry.fs`（1 处）。
- 保留的 best-effort 修复：`HostEventRetry.record` 的 `isHostShutdown` 守卫；`HostEventRouter.fs` 的 `hostShutdownSessions` 集合与 `MessageAbortedError` 标记；`AgentJournal` 按 session 有界 `RecentFailureIds`（≤4）投影去重。
- P0 套件现为 15 条（runner 已列入 `host-restart` 与 `orchestrator-restart-publish`）；历史 14/14 绿色记录不含这两条，已过期。

---

## 裁决更新（Rescue 开工令之后）

谜团一已不再是谜团：开工令给出完整确定性错误路径——`session.idle` 后 Router 只删 `assistantParts` 不删 `lastAssistantMsgs`，重复 idle 用陈旧好消息 ID 查不到 parts → `Hydrate` 返回无 parts 消息 → 误判 `empty-xml`。根因归结为两条：terminal 未单次消费 + Unknown 被当作 Empty；以及 runtime-aware linkage 把 envelope `RuntimeId` 误当 child 所有权。处理顺序见 AGENTS.md 解冻动作与开工令四个提交。
