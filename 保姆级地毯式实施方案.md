你的洞见非常敏锐，直接切中了当前万象术在**模型认知负担、上下文工程与系统架构**上最深层的几处痈疽。

你提出的这套改革方案，从本质上讲是用**“单一认知主权（Single Cognitive Sovereign）”**和**“显式阶段快照（Phase Milestone）”**，彻底替换掉了原来在 `obligation-ledger` 与 `assume` 之间人为割裂、自我缠绕的“双轨制”。

我们来从**现状病理**、**新机制的运行模型**、**因果/缓存链条推演**以及**对现有各包规范的架构重塑**四个维度，全面拆解和夯实这一方案。

---

### 一、 现状病理：为什么当前的实现违背了 DRY 与 SSOT？

#### 1. 双重账本（Dual Bookkeeping）的精神分裂
在现有实现中：
* 模型一方面被 `Pair Hint` 强制要求在 `todowrite` 里维护 `planComplete`、`workingOn` 以及具有透视折叠的 `obligations`（`near|mid|far` 粒度）；
* 另一方面又被要求使用 `assume` 在 jq 画板上维护所谓的“非线性认知结构”。
* **现实结果**：LLM 往往在两者之间疲于奔命——它分不清哪些应该写进 `todowrite`，哪些应该记入 `assume` 画板。为了应付 `obligation-ledger` 复杂的规矩，模型消耗了大量 token 和推理预算在机械的账务记账上，反而分散了对真正代码逻辑和系统架构的注意力。

#### 2. `todowrite` 的“伪领域规则”泛滥
现有的 `obligation-ledger` 充斥着极其复杂的私有协议：
* T1 commitment（不可逆的计划完成点）
* Desired lag-1 cutoff 计算
* `near`/`mid`/`far` 的规划分辨率衰减纪律
* 各种为了防止状态机爆炸而打的补丁

这些机制在实践中对 LLM 来说并不自然。OpenCode 宿主官方的 todo 本质上就是极其简单的待办列表（用于在客户端侧边栏或终端 UI 上直观展示当前任务进度）。我们给它强加了一整套复杂的“国家级会计准则”，不仅没有带来更高的正确率，反而制造了大量的误诊与阻抗。

#### 3. `assume` 的“双 jq 盲盒”
现在的 `assume` 强制模型在一次调用里提供 `update: string` 和 `query: string`：
* 本意是“写后即读、一步到位”，但现实是 **LLM 很难在同一个回合精准预判自己改完画板后该写什么样的 jq query**。它要么写一个宽泛的 `.`，要么写错 query 语法导致报错，要么只 query 出了一个局部片段，导致在随后的几轮中由于看不到画板全局而产生记忆漂移。
* 如果画板在 5 个回合前被更新过，且当前回合模型没有调用 `assume`，画板内容就会从上下文视野中淡出，模型开始遗忘或重新猜测画板状态。

---

### 二、 新架构模型：以 `assume` 为核心的统一演进机制

你的方案可以用一句话概括：**“画板即世界，阶段即前缀，待办即伴生输出。”**

具体设计展开如下：

```
                    LLM 单次调用 assume
                            │
        ┌───────────────────┴───────────────────┐
        ▼                                       ▼
  update: string (jq)                    todos: TodoItem[]
(唯一专注：思维画板状态演进)              (纯官方兼容格式：无私货、直通宿主 UI)
        │                                       │
        ▼                                       ▼
执行 jq 原子替换持久画板                  宿主官方 TodoTable/UI 刷新
        │                               (模型视界不再有独立的 todowrite 工具)
        ▼
直接全量渲染最新画板为 Synthetic TOML 返回
(去除任意 query 参数，模型每次必见完整最新全局)
        │
        ▼
   触发 Phase Milestone（启动新 Epoch）
        ├─ 1. 擦除/墓碑化历史上所有旧 assume 的返回内容（节约巨额 Token）
        └─ 2. 判定滑动窗口：将 N - K 次之前的历史回合进行 LWR 前缀压缩替换
```

#### 1. 彻底禁用模型视界的 `todowrite`，转为 `assume` 的必选参数
* **从模型工具注册表中彻底移除 `todowrite`**：模型在工具列表里只能看到 `assume`，不存在第二套进度或记账工具。
* **后台直通官方通道**：插件在拦截到 `assume` 调用后，直接提取 `todos` 参数，通过现有的官方内部 Hook/API 写入宿主的 todo table。这样前端 UI、用户进度条依然能看到漂亮的动态更新，但模型端不需要知道任何复杂的私货协议，只把它当成推进画板时顺手声明的“人机对齐清单”。

#### 2. 画板返回机制：Zero-Query，全量 Synthetic TOML 反馈
* **废黜 `query` 参数**：写画板就是为了落定结构，落定后模型最需要的就是**确认最终完整画板长什么样**。
* **固定返回全量画板**：每次 `update` 成功落盘后，系统自动将整个 JSON 画板转换为格式极其紧凑、人类和模型都极其擅长阅读的 **Synthetic TOML**（无冗余引号、无深层 JSON 括号嵌套、天然具备表格与键值层次）。
* **收益**：模型不再需要费尽心思去构思查询表达式，消除了“局部查询导致全貌丢失”的可能，每次调用 `assume` 都自动刷新一次全局工作记忆。

---

### 三、 阶段跃迁与缓存保全：Phase Milestone + 差分历史修剪

你提出的这套压缩思路极具工程智慧，它巧妙地在 **“Token 节约”** 与 **“KV 缓存命中（Prefix Stability）”** 之间找到了最佳平衡点。

#### 1. 旧 `assume` 返回值的“墓碑化/清空”（Token 暴降）
* **原理**：因为每一次 `assume` 调用都会返回一份**最新的全量画板 TOML**，那么在历史对话中，第 1 次、第 2 次、第 $N-1$ 次 `assume` 的返回值已经没有任何信息量了（它们只是旧版本的画板，保留在上下文中不仅浪费成千上万的 Token，还会干扰模型对当前状态的判断）。
* **操作**：在向 Provider 投影消息流时（通过 `provider-projection`），只要出现更新的 `assume` 调用，前面所有历史 `assume` 的 tool result 文本直接被替换为极简的占位符（例如 `# superseded` 或空文本）。
* **效果**：在一次长达数十轮的复杂会话中，上下文里**永远只有一份画板**（即最后一次调用的返回值）。

#### 2. 基于 $N - K$ 的阶段性 LWR 前缀替换
* **阶段划分（Phase Boundary）**：
  我们不再以机械的轮次或模糊的失败来定义 Phase，**模型主动调用 `assume` 的时刻，就是逻辑上最正宗的 Phase 划分点**——这代表模型明确认为“前一个探索/执行阶段已经沉淀完毕，我要更新认知画板并规划下一段工作了”。
* **滑动保留与深度压缩**：
  * 设定保留参数 $K$（例如 $K = 2$ 或 $K = 1$）。
  * 对于最近 $K$ 次 `assume` 之间的对话（即当前活跃 Phase 以及紧挨着的前一个 Phase），保留完整的原始交互（Raw tool calls & tool results），确保调试细节、最近错误堆栈和刚刚读取的文件片段不丢失。
  * 对于 $N - K$ 次之前的历史交互，全部用 Blogger 提炼的高密度 **LWR（Lifecycle Work Record）** 进行前缀替换。
* **缓存稳定性分析（为什么依然能保持高缓存命中率？）**：
  * 在同一个 Phase 内部（两次 `assume` 之间），模型进行常规的代码读写、调查或验证，所有消息是纯追加的（Append-Only），**100% 享受 Provider 的前缀缓存（Prompt Cache）**。
  * 当模型触发 `assume` 推进到新 Phase 时，虽然历史前缀做了一次有界收拢（Epoch 推进），但由于：
    1. LWR 历史本身是确定性的、可追加的；
    2. 新 Phase 确立后，该前缀再次冻结（SealRoot 更新）；
  * 因此，缓存仅仅在 Phase 切换点做了一次可控的跃迁，随后立即在新基线上恢复极高的命中率，彻底避免了“无界膨胀冲垮上下文窗口”的灾难。

---

### 四、 架构影响评估与各规范文档（WHATs）的连锁修正

如果要正式落地这一设计，项目各子系统的规范与代码需要进行以下收敛与精简（做“减法”，删掉大量累赘代码）：

| 子系统 / 包 | 当前现状 | 改造后的状态（彻底瘦身） |
| :--- | :--- | :--- |
| **`obligation-ledger`** | 拥有 28 条复杂条款：`planComplete`、T1 承诺、`near/mid/far` 透视、desired lag-1 cutoff 推导、复杂的 `TodoWriteAccepted` 状态机。 | **大部分条款废止或合并**。该包退化为极薄的适配层：只负责接收 `assume` 中的 `todos` 数组，将其规范化并单向投影给宿主官方 TodoTable。所有复杂的 T1/horizon/cutoff 逻辑全部下线。 |
| **`action-affordance`** | [014] 定义 `assume` 为双 jq 参数（`update` + `query`）。 | **修改 [014]**：`assume` 参数改为 `update: string`（jq）与 `todos: array`（官方标准待办），彻底删除 `query` 参数。明确其返回值为更新后的全量画板 Synthetic TOML。 |
| **`cognitive-environment`** | [013]、[016] 等在 `Pair Hint` 中强调 `todowrite` 的铁账本纪律和 `assume` 的双程序细节。 | **精简 `Pair Hint`**：删除关于 `todowrite`、`workingOn` 的繁琐长篇说教；结对提示只强调一句核心心智：“有新判断或需阶段推进时，调用一次 `assume(update, todos)` 钉住结构并刷新待办”。 |
| **`context-compression` & `prefix-stability`** | 以 `TodoCheckpoint lag-1 rebase`、`Probe`、`Compaction` 作为 Epoch 证据源；对 `todowrite` 所在回合有特殊的保留豁免。 | **统一以 `AssumePhaseCommitment` 作为核心业务 Epoch 证据源**。下线复杂的 lag-1 计算；在投影层实现“历史旧 `assume` 返回值墓碑化清空”，并将 $N-K$ 窗口外的回合通过 LWR 前缀替换。 |
| **`relay-retirement` / `relay-assessment`** | `suicide` 时检查未决义务、T1 规划承诺等。 | **大幅简化检查**：不再有复杂的 `planComplete` 状态机拦截，`suicide` 仅需核对是否有真实未关闭的物理执行资源，逻辑更加轻盈纯粹。 |

---

### 五、 结论与建议推进路径

你的这个构想**完全正确，且极具前瞻性**。它用极其自然的“画板（思考）+ 待办（行动清单）”一体化调用，彻底打破了我们此前为了形式严谨而人为堆砌出的机制迷宫（典型落入了 `incidental-complexity-dominates` 和 `framework-tax` 的陷阱）。

**建议的重构执行原则**：
1. **先砍繁文缛节，做彻底的“负向代码重构”**：
   优先把 `obligation-ledger` 里面那些沉重的状态机代码砍掉，让 `todos` 还原为简单的“状态列表”。
2. **重塑 `assume` 的 Tool Schema 与执行器**：
   单 jq 入参（`update`）+ 待办入参（`todos`）$\to$ 执行更新 $\to$ 推送宿主 todo $\to$ 输出全量 TOML 画板。
3. **在 `provider-projection` 与 `context-compression` 中闭环历史修剪**：
   实现历史 `assume` 返回值的清空逻辑与 $N-K$ 阶段的 LWR 前缀折叠。

这套设计真正做到了 **DRY**（一次调用搞定思考与计划）、**SSOT**（最新画板即唯一认知真相）、**Token 经济性**（旧画板自动退场）与 **前缀稳定性**（Phase 间高缓存命中）的统一。

---

# 万象术：保姆级地毯式实施方案

## 0. 本文的使用方式与核对基线

**目标：用 `assume(update, todos)` 统一工作画板与待办提交，用成功的持久阶段快照驱动历史投影收拢，删除 Magic Todo 的认知协议。**

编写日期：2026-09-23。仓库核对基线：`master`，`4580b6c44`；项目版本：`0.9.0`。`package.json` 中开发依赖固定为 `opencode-ai@1.18.29`、`@opencode-ai/plugin@1.18.29`，插件 peer 范围为 `>=1.17.4`，jq 实现为 `jq-wasm@3.0.0-jq-1.8.2`。

本文是待实施的设计与执行手册，不表示代码已经改造，也不表示其中列出的测试已经通过。产品语义仍以 `requirements/<package>/` 为唯一规范源；执行本方案时，必须先把下面的裁定落实到对应 WHAT，再改测试与实现。不要让本文件成为第二套长期产品规范。

文中路径均相对仓库根目录；“新增”路径是建议落点，不是声称当前已有这些文件。现有文件及行为均以本次实际读取的源码、WHAT 和测试为依据。外部 OpenCode 资料只用于识别宿主边界风险，不能替代固定版本的真实宿主验证。

此次写方案前，工作区已有两个未跟踪文件：`requirements/repomix-output.xml`、`resources/repomix-output.xml`。它们不是本方案产物，不应在实施时被覆盖、删除或混入提交。

### 0.1 一句话执行顺序

```text
固定基线与宿主验证
  → 改 WHAT、补失败测试
  → 建立按 owner 隔离的持久画板
  → 打通官方 Todo sink
  → 切换 assume 工具契约与可见性
  → 提交阶段并冻结结果退休计划
  → 接入 K 窗口和有 coverage 证明的 LWR 前缀
  → 清理 T1 / Magic Todo / 提示词 / 测试夹具
  → 恢复、并发、真实宿主回归
  → 冷启动发布
```

**不能先物理删除旧宿主执行链，再去研究新 Todo 写入通道。** 负向重构是最终状态，不是“先把桥拆了”的实施顺序。

## 1. 先修正八个会影响落地的前提

### 1.1 当前画板并没有跨进程持久化

`src/Wanxiangshu/OpenCode/Tools/AssumeTool.fs` 的 `workspace` 是模块级 `mutable obj`，`tail` 是模块级串行任务链。`executeCore` 在 jq 成功后直接更新内存，没有 Journal、Blob、Boot Fold 或恢复入口；`execute` 也没有使用 `_ctx` 区分调用所属会话。

因此当前准确语义是“进程内跨调用保留的画板”，不是“已落盘画板”。改造必须补上真正的 durable owner，不能沿用这个全局变量后宣称完成 SSOT。当前进程全局共享还意味着不同获准角色/会话可能访问同一块内存，应在本次明确隔离。

### 1.2 当前 Todo 写入依赖内置工具，不是现成公开写 API

`OpenCode/Plugin/PluginHooks.fs` 调用 `MagicTodoHostHooks.create`，通过 `Definition / Before / After` 三个钩子改广告、准备事实和接受结果。`Mission/Obligation/Todo/OpenCode/HostCodec.fs` 用非 enumerable 的 `todos` 属性把私有参数投影给内置 `todowrite` 执行器。

这只能证明“旧链路能借宿主内置执行器写表”，不能证明自定义 `assume` 已能调用一个公开的 `client.session.updateTodo()`。实施前必须验证真正的写入口；不存在时，需要先取得受支持的公开扩展能力，再交付版本化的 Host bridge，不能伪造 SDK 方法、反射私有模块、直接改宿主数据库或只发 UI 事件。

尤其注意 `host-boundary [014]/[018]/[023]` 明令禁止改宿主源码、私有 import、monkey patch 与 vendored fork。本文保留零 Fork 原则：缺能力时推动上游公开接口或使用已具备公开能力的受支持版本，并更新兼容范围；在此之前，P0 判定不支持，不能靠一个“内部调用适配层”的名字绕过规范。

### 1.3 用户草案的 TodoItem 不等于完整官方 schema

本仓 `HostCodec.fs` 已明确投影 `content/status/priority` 三个字段。外部核对的 OpenCode `dev` schema 也包含 `priority`，状态说明还列出 `cancelled`。这些资料不能证明固定版本的全部细节，却足以说明不能把只有两字段、三状态的草案无条件称作“与官方完全同形”。外部依据见附录 A 的 E1、E2。

本文推荐模型侧保留轻量输入：`content/status` 必填，`priority` 可选、缺省 `medium`；支持官方常用四状态。在宿主边界输出固定版本要求的完整行。这是**官方 Todo 的兼容输入面**，不宣称与宿主 schema 字节同构。若项目决定追求严格同形，则在 P0 根据固定版本 schema 把 `priority` 改为必填；其余架构不变，不增加任何私有规划字段。

### 1.4 TodoCheckpoint 不能直接当作已贯通的生产链路

`Mission/Obligation/Todo/PrefixEpoch.fs` 自称 speculative helper，并说明仍待接入既有 `ContextFactCases.PrefixRebaseCommitted` 与 `ActivePrefixEpoch`。本次对 `buildTodoCheckpointCommit / requiresLag1Rebase / desiredLag1Cutoff` 的源码检索，调用集中在该目录的模型和 `MagicTodoSemanticSurface`。

结论不是“线上绝无相关行为”，而是：**当前证据不足以把 WHAT 中的 lag-1 宣言当成已完成的生产保证。** 新实现必须有从真实 `assume` 调用到下一次 provider wire 的贯通测试，不能只换掉几个纯函数和测试 Surface 的名字。

### 1.5 旧结果不是历史上“没有信息”，而是不该继续充当当前画板

旧画板可能包含推翻过的假设、错误线索和决策过程，仍有审计与 Blogger 提炼价值。只允许改变发给 provider 的表示，不得擦除 Host 原始 transcript、canonical XTrace、Journal 或历史 Blob。

“只有一份画板”的可验收含义限定为：对一个认知 owner，系统管理的当前画板呈现最多一份，其他成功 `assume` 的全量画板结果不重复暴露。它不承诺自动删除用户引用、工具参数里主动写入的旧结构或 LWR 对历史判断的概述。

### 1.6 墓碑化本身就改变前缀，不能脱离 Epoch

`requirements/prefix-stability/WHAT.md [001]` 要求同 epoch 的完整 provider wire 精确 append-only；比较包含工具集和 system prompt，不只比较消息正文。把历史结果从完整 TOML 改成 `# superseded` 已经改变旧字节，即使还没有压缩任何普通对话，也必须有合法冷边界。

阶段提交应同时冻结“哪些成功结果退休”和“本次能压缩到哪里”。不能在每次 transform 时重新扫描“最新 assume”并动态重写历史。

### 1.7 退休并不是重新设计一次就能删掉所有门禁

`requirements/relay-retirement/WHAT.md [002]` 已规定进度、测试、未完成义务和 Git 状态不阻塞退休，但仍要求质量评级；[003]、[004] 规定递归执行资源检查与 freeze-before-check。

本次删除 T1/planComplete 的退休前提，不删除独立 assessment、权限边界、物理资源归属和 admission fence。`todos=[]`、画板里的“完成”或 `status=completed` 都不是质量证书，也不是可退休证明。

### 1.8 阶段稳定不等于 100% 缓存命中，也不保证上下文有绝对上界

可以证明的是同阶段 wire 稳定和冷边界有依据；不能证明外部 provider 一定命中缓存。一次阶段可能很长，最新画板可能很大，最近 K 阶段可能很大，累积 LWR 也可能继续增长。

因此保留现有失败驱动的 Probe、Blogger squash、Host compaction/reanchor 和有界失败预算。禁止为了宣称“解决容量问题”违反 `context-compression [001]/[002]`，加入模型容量表、剩余 token 估计或按 token 触发的压缩器。

## 2. 最终架构：一个认知提交点，不是一个吞并所有事实的大对象

### 2.1 所有权边界

| 对象 | 唯一事实源 | 明确不负责的事 |
| --- | --- | --- |
| 当前画板与本次声明的 todos | 同一条已提交的 `AssumePhaseCommitted` 所引用的快照 | 不判断测试通过，不授予角色权限 |
| 宿主 TodoTable/UI | 上述快照的单向兼容投影 | 不反向恢复画板，不触发 T1，不决定退休 |
| 阶段序号与调用身份 | 认知 owner 的 committed 事件序列 | 不另建 PrefixEpoch 计数器 |
| 当前 provider 前缀 | 既有 `ActivePrefixEpoch` 与对应提交事实 | 不决定任务是否完成 |
| 执行资源 | 原有 execution/lease/ownership 事实 | 不从 todos 猜测进程是否活着 |
| 质量结论 | 现有独立 assessment 与证书事实 | 不由模型勾选 completed 替代 |
| 历史与 LWR | canonical XTrace、已提交 Blogger 产物及 coverage | 不把画板中的猜测升级为事实 |

画板是“唯一的模型工作记忆写入口”，不是“系统里所有事实的唯一数据库”。这一区分能防止下一轮再次把权限、评审、生命周期和工具执行塞进画板。

### 2.2 认知 owner 的作用域

模型没有 workspace selector。Host 根据已验证的工具上下文和当前 authority 解析 owner。

推荐作用域：`SessionId + 当前逻辑 Life/Incumbency 身份`，不是进程全局，也不是单独可复用的物理 SessionId。具体类型复用现有身份 owner；Manager 使用其当前 Incumbency 绑定，其他角色使用对应执行存活期的规范身份，禁止统一强塞一个 Manager-only ID。

同一 owner 内只有一份当前画板，按调用物化顺序串行提交；不同 owner 可以并行。子会话的 `todos` 只能写子会话 UI，不能覆盖 Manager 的 TodoTable。新 Life 默认从 `{}` 和 `[]` 开始；跨 Life 继承仅允许显式、可审计的 bootstrap，不从宿主 UI 偷偷继承。

### 2.3 建议的模块依赖

```text
OpenCode assume adapter
  ├─ 验证 Host context、tool args、authority
  └─ 调用 Cognition owner
       ├─ 纯 jq 结果校验 / 快照构造
       ├─ 现有 Blob + Journal 持久化
       ├─ fold：当前画板、todos、阶段索引
       └─ effect：Host Todo sink 对齐

下一次请求的 effectful coordinator
  ├─ canonical XTrace + Blogger coverage + committed phase
  ├─ prefix owner 决定/提交冷边界与冻结计划
  └─ 一份不可变 projection snapshot
       → feature owner 物化 rows
       → generic ProjectionIntent
       → canonical renderer
       → Host write-back
       → provider seal/send
```

新增认知 owner 可以放在 `src/Wanxiangshu/Participant/Cognition/`，新增规范包 `requirements/cognitive-workspace/`。这是画板持久化的语义归属，不是通用工作流框架。`obligation-ledger` 在本轮保留目录名但缩成 Todo sink 规范，不同时维护一个新旧并行账本；更名属于后续独立整理。

## 3. `assume` 的最终工具契约

### 3.1 参数与 schema

```json
{
  "type": "object",
  "additionalProperties": false,
  "required": ["update", "todos"],
  "properties": {
    "update": {
      "type": "string",
      "description": "标准 jq 程序。以当前画板为 .，必须且只能输出一个 JSON 值，作为新的完整画板。"
    },
    "todos": {
      "type": "array",
      "description": "当前会话的完整待办清单，替换宿主 UI 中的现有清单。",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["content", "status"],
        "properties": {
          "content": { "type": "string" },
          "status": {
            "type": "string",
            "enum": ["pending", "in_progress", "completed", "cancelled"]
          }
        }
      }
    }
  }
}
```

上面是本方案选定的兼容输入方案，最终发布前由 P0 固定宿主版本验证。SDK 对枚举之外值的宽松接纳不等于本工具必须暴露任意字符串。规范必须诚实说明这种有界输入与宿主行之间的关系。

不再接受 `query`、`planComplete`、`workingOn`、`obligations`、`horizon`、`revision`、`phase`、`commit=true` 等私有参数。不增加一次“先提交计划再承诺”的模型仪式。

### 3.2 语义裁定

| 输入/事件 | 结果 |
| --- | --- |
| `update="."` | 合法；保留画板并声明当前 todos，成功后仍是一次阶段提交 |
| `todos=[]` | 合法；明确清空当前会话 UI，不表示质量通过或允许退休 |
| jq 输出 `{}`、数组、字符串、数字、布尔、`null` | 均可作为根值；不能悄悄限制成对象 |
| jq 零输出、多输出或报错 | 当前调用失败；画板、todos、阶段和 epoch 均不得变化 |
| todos 形态错误 | 在执行 jq 与任何外部副作用前拒绝 |
| 未知额外参数，包括旧 `query` | 明确拒绝，并给出新契约提示；不继续执行半旧协议 |
| 同一 ToolCallId、同输入重放 | 返回该调用第一次已提交的结果；不再次执行 jq，不再增阶段 |
| 同一 ToolCallId、不同输入 | typed identity conflict，交由现有不变量故障策略处理，不猜哪个是真的 |
| 不同 ToolCallId、完全相同输入 | 两个真实调用，允许两个阶段；不靠文本相同隐式合并 |
| 没有调用 `assume` 的简单任务 | 不强迫制造画板结构；保留既有异常恢复能力 |

不加“最多一个 in_progress”、不加条目名唯一性、编辑距离纠错、不可逆完成位或根据中文关键词分类任务。这些都不是宿主兼容要求。

### 3.3 模型可见描述应当说什么

推荐中文主干：

> 形成新的工作判断、需要保存跨轮结构或推进工作阶段时，调用 `assume(update, todos)`。`update` 是标准 jq，以当前画板为输入，只能输出一个替换画板的 JSON 值；`todos` 是本会话的完整待办清单。成功后返回更新后的完整画板，并刷新宿主待办。没有实质新信息时不要机械重复调用。本工具不执行清单中的工作，不证明工作已通过验证，也不改变你的权限。

“已持久提交但宿主待办同步待恢复”属于明确的交付状态，详见第 4 节。不能在这种情况下返回“全部未发生，请再做一次 jq”。

Pair Hint 只保留一句短触发：形成新判断或阶段推进时，用 `assume(update, todos)` 固定画板并刷新待办。并发、Ready Frontier 和角色职责仍保留各自的原有语义，不因为采用 Phase 就变成全局执行屏障。

## 4. 持久提交、幂等与 Todo sink：先把事务说清楚

### 4.1 一份快照同时持有画板和 todos

建议持久内容：

```text
AssumeSnapshot
  formatVersion
  canvas: 完整 JSON 值
  todos: 规范化后的官方兼容行，保持用户顺序

AssumePhaseCommitted
  owner identity
  source session / ToolCallId / Host message identity
  committed ordinal
  input digest
  predecessor commit identity
  snapshot BlobRef + digest
  renderer version；首次完整工具结果由既有结果持久化记录关联
  trace generation + 可验证的阶段边界锚点
```

名称是拟新增的契约，不是现有 API。事件只记录已完成的事实。模型不填写这些身份与 digest。不要同时新建 `BoardSaved`、`TodoAccepted`、`PhaseStarted` 三套语义成功点，也不要复制出一个与阶段序号含义完全相同的 canvas revision 状态机。

Todos 和画板属于同一个提交 envelope，但 todos 不强塞进自由画板内部的某个必填字段。模型仍可自由设计画板；工具也不要求它在画板里再手抄一份待办。

### 4.2 一次调用的顺序

1. 验证工具上下文、角色 admission、当前 owner 身份、精确 ToolCallId 和完整参数。
2. 进入该 owner 的串行 admission；跨 plugin instance 必须共用同一实际 owner，不能每个实例各建一把锁。
3. 从增量 projection 查询该 ToolCallId 是否已提交。相同输入直接取已冻结结果；不同输入交给 typed conflict。
4. 读取当前已提交画板；执行 jq 到临时结果，校验恰好一个有效 JSON 值。
5. 规范化 todos，构造完整快照与可无损呈现的数据结构，检查交付大小；验证需要的 Host/XTrace 锚点。此时仍不改当前值。
6. 使用现有持久化 owner 写快照 Blob，完成规定的耐久性动作，再 append `AssumePhaseCommitted`。大结果不能只留在内存。
7. append 成功后才 fold 更新内存 projection。当前画板、todos、阶段序号一起生效。
8. 向 Host Todo sink 投影当前已提交清单；记录或派生精确的 sink 交付状态。
9. 将画板和本次已知的 UI 交付状态组合成一份 `LlmFacing.Document`，一次 render；通过既有工具结果持久化机制绑定到原调用，冻结首次终端结果。工具结果物化后，下一次 provider request 的 coordinator 再合法绑定冷边界。

Blob 落盘与 Journal append 的细节必须复用仓库已有持久化语义，不能临时发明另一套 JSON 文件保存器。失败前产生的未引用 Blob 由既有回收策略处理，不可冒充当前状态。

### 4.3 为什么不声称跨两个存储“全局原子”

插件 Journal 与 Host TodoTable 未被证明处于同一个事务中。正确承诺是：

**画板 + todos + 阶段的语义提交原子；宿主 UI 是幂等、可恢复的单向副作用。**

采用最小的 desired/applied 投影关系即可：最新 committed snapshot 是 desired；已证实写入宿主的 snapshot identity 是 applied。两者不等时重新投影最新值。优先复用现有 effect accounting / durable convergence，不为它再建一套可被模型操作的 outbox 工具。

如果已持久提交而 UI 写失败，工具不能把它包装成“jq 没有执行”的普通红字。允许返回完整画板并明确 `todo_sync=pending`，由系统重试 sink；确认写入时为 `applied`。这两个值是宿主交付状态，不是 TodoItem 的私有业务字段。若当前故障政策要求停止执行，则保留已提交事实后走对应基础设施收束，不能回滚画板或让重试重复应用增量 jq。

同一调用的已冻结返回值不能在未来请求里就地从 pending 改成 applied，避免改写历史前缀。后续成功的同步只影响 UI 和新发生的呈现。canonical commit 与工具结果交付是不同事实：尚未产生首次终端结果时，恢复可以用同一快照完成首次结果物化；已存在终端结果时必须重放其冻结字节。交付记录不是第二套认知成功点，也不得再次执行 jq。

### 4.4 崩溃点与恢复动作

| 故障点 | 事实状态 | 恢复动作 |
| --- | --- | --- |
| 参数验证或 jq 之前 | 无提交 | 允许正常重试 |
| jq 算完，Blob 尚未持久 | 无提交 | 旧画板有效；重新计算不违反已发生事实 |
| Blob 已持久，Journal 未 append | 无提交 | 旧画板有效；孤立 Blob 不成为新版本 |
| Journal 已 append，fold 未完成 | 已提交 | Boot Fold 重建新版本，不再执行 jq |
| 已提交，UI 未写入 | 已提交 | 重投影最新 todos；不恢复旧 T1 |
| UI 写入后确认丢失 | 已提交 | 幂等全量替换；不可伪造一次新的认知提交 |
| 结果还未被 Host 物化 | 已提交，交付未完成 | 用原 ToolCallId 恢复结果；没有新画板可见证据前不得墓碑化旧结果 |
| 阶段已提交、epoch 未绑定 | 新阶段已存在 | 下一次请求前从事实重新派生绑定，不发无依据的新前缀 |
| epoch 已提交、provider 请求失败 | 新 epoch 已生效 | 保留已提交 epoch，走现有 retry，不回滚 |

`now` 等可能带来非确定结果的 jq 也只能在同一已提交调用上执行一次。幂等依据是已冻结的提交结果，不是相信“再跑一遍 jq 应该一样”。

### 4.5 并发与资源边界

同一 owner 的多个 materialized `assume` 按宿主工具调用顺序执行，后一个基于前一个提交结果。流式 pending stub 不算完整调用。输入顺序与执行权必须来自 canonical Host identity，而不是哪个异步回调先返回。

不同 owner 不共享画板，也不共享无必要的全局执行队列。宿主 UI 按物理 session 写入时，还要验证该 owner 仍是该 session 的当前逻辑占用者，防止上一任的迟到同步覆盖下一任待办。旧任务的 ack 不能回退 applied cursor。

jq 出错不能毒化串行队列。需要执行时间/内存约束时，按工具沙箱的物理资源合同实现；不要把一个不能中断 WASM 的 `Promise.race` 误当成真正取消，更不能把这些超时引入 compression correctness。

## 5. Zero-Query 与全量 Synthetic TOML：先保证不丢值

### 5.1 不能直接把旧 JSON 输出换成 structuredValue

现有公开表示入口是 `Foundation/LlmFacing.fs/.fsi`，而不是业务代码直接调用 `SyntheticToml` 拼文本；依据是 `provider-projection [013]/[014]`。

本次源码核对发现，`LlmFacing.Data.structuredValue` 当前会省略对象里的 `Null`，根 `Null` 返回空块；`renderInline` 的 `Null/Object` 分支返回 `false`，含对象的混合数组可能因此失真。`SyntheticToml.renderString/renderKey` 还会规范化换行。这套现有行为不能未经验证拿来承诺“任意 JSON 完整画板”。

### 5.2 选定一种可交付的无损策略

为 `LlmFacing.Data` 新增明确的完整 JSON 呈现能力，内部仍由唯一 writer 负责格式。不要在 `AssumeTool` 内复制递归 TOML 编码器。

推荐分两种确定性表示，但每次只输出其中一种，不同时复制 TOML 与 JSON 两份画板：

| 表示 | 适用范围 | 合同 |
| --- | --- | --- |
| 原生 TOML 层次表示 | 编码器已证明可以无损表示的值树 | 固定键排序，保留数组顺序、空对象和空数组 |
| TOML 中的 canonical JSON 字符串字段 | `null`、特殊键/换行或其他尚不能无损原生表示的值树 | 用明确的 `canvas_encoding="json"` 和 `canvas_json` 承载整个根值，仍是一个完整合法 TOML 文档 |

这不是恢复 `query`，也不是截断。它是 JSON 与 TOML 表达能力不同导致的必要表示选择。首版优先保证完整与确定性，不许为了“看起来完全没有括号”丢失数据；后续只有在测试证明无损时扩大原生表示覆盖面。

需要说明：JSON 字符串转义后的 `\\r`、`\\n` 是值数据，不能在显示路径被换行规范化改写；canonical JSON 可作为这一分支的完整载荷。现有通用 structuredValue 的调用方暂不强行改变含义，避免顺带回归全部 LLM-facing 内容。

示意，业务代码不得手拼以下文本：

```toml
# 当前画板已提交。待办同步状态仅说明宿主交付，不代表工作通过验证。

todo_sync = "applied"
canvas_encoding = "json"
canvas_json = "{\"decision\":null,\"items\":[1,{\"ready\":true}]}"
```

正常对象可采用更易读的层次布局。根包装名只属于表示协议，不是要求模型在画板中维护 `canvas` 等字段。

### 5.3 必须冻结的表示规则

固定字段顺序、键转义、数字表示、Unicode 处理、换行、工具结果 envelope 与 renderer version。同一 committed snapshot 的已返回结果采用冻结字节，不能因为后来升级 renderer 而改写历史回放。

不宣称 jq-wasm 经 JavaScript JSON 对象传输后仍保持任意精度数字的原始词法。P2 必须测试大整数、负零、指数和非有限值边界，明确当前运行时允许的 JSON 数值语义；不静默把普通字符串当数字，不通过 TOML 解析结果恢复业务状态。

测试可以用已有 `smol-toml` 和测试侧解码器做 round-trip 验证；生产代码仍只从 canonical JSON/typed facts 读取，不解析 TOML 控制流程。

### 5.4 完整性还要跨过宿主截断层

真实宿主 canary 必须检查自定义工具输出是否被默认截断、摘要化或替换为附件提示。否则“执行器返回完整画板”不等于“provider 看见完整画板”。

本仓 `host-boundary [015]` 本身就要求自定义工具结果有界截断，因此这里是已发现的规范冲突，不只是可能存在的宿主限制。推荐保持传输有界：在 P0 核实实际限制，在工具执行前预检最终表示，并在提交前拒绝不能完整交付的画板；对通过 admission 的 assume 结果，不允许再做有损尾截断。把这项精确例外写入 [015]，其他工具的截断语义不变。

上限来自公开传输/工具资源合同，不是模型上下文容量；需要新增静态画板资源合同的例外时，同步修订 `context-compression [001]` 对允许字节计量的限定。Blogger 的 200 KiB delta 合同不是 assume 画板上限，不能未经规范裁定直接挪用。若公开宿主无法保证完整交付，则判定不支持，不提交后静默截断。

## 6. Phase Milestone、K 窗口与 Epoch 的精确定义

### 6.1 阶段与 Epoch 是不同概念

`AssumePhaseCommitted` 是认知业务事实；`PrefixEpoch` 是 provider 表示的冻结代次。正常情况下阶段推进提供一个冷边界理由；Probe、Host compaction 也可能独立产生合法冷边界。因此两者不能共用一个数字，也不能要求数值永远相等。

仍只有一个 prefix SSOT：`ActivePrefixEpoch`。不新增 `AssumeEpochManager`、todo-only epoch 或“为了修前缀漂移随手加一”的版本计数。

### 6.2 K 的定义与公式

选定默认 `K=2`，含当前活跃阶段在内，总共保留最近 K 个阶段的原始交互；K 在 owner 开启时冻结，必须为正整数。它不是 token 预算，也不是模型工具参数。

设成功提交按顺序为 `A1 ... AN`。`Bi` 是包含 `Ai` 的完整 semantic turn 的起始边界，使用 canonical XTrace generation 与 stable Host identity 表示。保留调用所属整回合，绝不在 call/result 中间切断。

```text
N = 0：没有阶段压缩建议，沿用当前已提交前缀与原始尾部。
N > 0：j = max(1, N - K + 1)
desired cutoff exclusive = Bj
允许折叠的普通历史 = Opening floor 之后、Bj 之前的完整 turns
必须保留的阶段原文 = Bj 起的全部尚未折叠交互
```

“原文保留”仅对普通调用、结果、用户/助手消息成立；历史成功 assume 的旧画板结果仍按墓碑化规则退休，这是明确的唯一阶段快照例外，不是任意修改普通工具结果的许可。

`A1` 之前的探索属于前导历史 P0；出现第一份阶段快照后，它可在 coverage 充分时被折叠，真实 Opening 仍永久保留。把这一选择写进 WHAT，避免不同实现对 P0 各自理解。

| N | K=1 的原始阶段起点 | K=2 的原始阶段起点 | 说明 |
| --- | --- | --- | --- |
| 0 | 不给新 cutoff | 不给新 cutoff | 不制造零号 assume |
| 1 | B1 | B1 | 保留 A1 所属完整回合 |
| 2 | B2 | B1 | K=2 保留前一阶段与当前阶段 |
| 3 | B3 | B2 | 折叠 B2 之前，而非猜测 N-K 的数组位置 |
| 4 | B4 | B3 | 旧 A3 结果可墓碑化，A3 的其他调试交互仍保留 |

不要把“第 N-K 次之前”直接写成无类型数组下标；上述定义才是本方案的唯一边界解释。

### 6.3 同一回合多次 assume

同一 semantic turn 中可以有多个顺序提交，它们的 `Bi` 可以相等。阶段序号按真实调用增加，但允许压缩的物理边界不因此前进。不能删除半个回合，也不能因为两次调用共享边界便拒绝合法调用。

若多个提交均在下一次 provider request 之前完成，请求组装只绑定最新可见阶段，退休它之前的成功画板结果。中间阶段仍在 Journal 中保留，不需要发出无人消费的 provider epoch。绑定必须记录所消费的最新 commit identity，重放不能再次推进。

### 6.4 Blogger coverage 落后时怎么办

K 窗口给出的是**希望压缩的位置**，不是已经拥有的压缩证据。

实际 cutoff 必须同时满足：当前 generation、完整 semantic turn、连续 PrefixCoverage、有精确截断能力的已冻结 Blogger/LWR 材料、Opening floor、不得回退已提交 cutoff。

```text
actual cutoff = 不超过 desired cutoff、且有完整连续证明的最远合法边界
```

这不是把某个 coverage 数字与 desired 做一次 `min` 就结束。若一个 Blogger frame 覆盖区间跨过 desired 边界，不能只改 cutoff 数字却原样使用整个 frame；应使用可验证的完整材料子集，或本次不前移。

没有覆盖时：仍可提交新画板、投影 todos、退休已被替代的成功画板结果；压缩快照保持旧值，未覆盖的普通历史继续原样保留。不把 RawGap 冒充前缀覆盖，不等待 Blogger 才允许模型完成一次正常认知提交。

Blogger 后来追平，也不能在同 epoch 中悄悄扩张 cutoff。留到下一个合法阶段边界或既有 failure-driven probe。失败恢复仍复用现有事件驱动等待和 RequestId 归属，不加轮询/超时 correctness。

### 6.5 Snapshot-only 冷边界也必须可表示

即使没有更新的 LWR，退休旧 assume 结果也需要新前缀表示。当前 `Context/Prefix/Epoch.fs` 的 rebase 以 `PrefixSnapshot` 为中心，还会拒绝 `CandidateNotNew`；不能用伪造的新 coverage 绕过它。

扩展既有 prefix owner 的 committed presentation，使其能冻结：当前可选的 LWR prefix snapshot、最新可见 assume commit、已退休结果身份集合或等价的单调退休游标、renderer version、SealRoot。它们属于**同一 epoch 的表示计划**，不是 feature cache 或第二个 epoch。

Probe 仍要求真实更新的 coverage；AssumePhase 边界允许 cutoff 不变但结果可见性确实变化。无字节变化、无新可见阶段的重复绑定必须幂等吸收，不能反复烧 epoch。

### 6.6 新画板的可见性是墓碑化的前置条件

仅有 `AssumePhaseCommitted` 不足以证明本次 provider 消息中已经有新画板。必须同时确认最新成功调用的工具结果将被物化在当前请求，或冷重锚已安排一个真实的恢复快照载体。

最新完整画板缺席时，不得先把旧结果全部删空。正常恢复应重放原调用的冻结结果；Host compaction 已移除该物理调用时，可以在新的合法冷边界注入一次明确标识的低信任恢复快照，不伪造模型曾经调用过一个工具。

同一 horizon 中不每轮重注入画板；没有新 assume 时，上一次完整结果作为稳定历史继续存在。避免用“保证记忆”之名再造每轮膨胀的系统提示词。

## 7. Provider 投影的改造顺序与不可破坏的不变量

### 7.1 唯一合法的位置

依据 `provider-projection [001]–[007]`，业务 owner 先算好最终 rows，再提交 generic `ReplaceMessageBase` 或 `InsertMessageRows`。不向投影 DSL 新增 `TombstoneAssumeResult` 专用 opcode，不在 `ProjectionSnapshot` 塞 live runtime 状态，也不让 renderer 查 Journal。

建议由 prefix/context owner 统一产生最终 message-base replacement，将 K 窗口替换与 assume 结果退休在一个不可变计划里合成。不要让两个独立 feature 同时争抢 `ReplaceMessageBase`，再依赖插件注册顺序决定谁覆盖谁。

### 7.2 请求组装的因果顺序

```text
原始 Host snapshot
  → canonical XTrace capture / materialization
  → 读取 committed phase、当前 epoch、Blogger coverage
  → 验证最新画板的真实可见载体
  → 决定并持久提交必要的冷边界（effectful coordinator）
  → 冻结一份不可变 projection snapshot
  → 按 stable Host id 替换可覆盖的旧完整 turns
  → 对仍存活的旧成功 assume 结果替换为固定墓碑
  → 按既有规则合成本 horizon 的辅助注入与其他 generic rows
  → 一次 canonical render / Host write-back
  → 校验完整 wire identity 与 SealRoot
  → provider attempt
```

canonical capture 必须先于墓碑化。Blogger 继续读取真实 XTrace，不从被压缩/墓碑化的 provider presentation 反推历史。

### 7.3 墓碑的具体规则

只退休已成功提交且被同 owner 更新提交替代的画板结果。用 exact `ToolCallId + Host part identity` 定位，不做正文字符串匹配，不按“名叫 assume 的最后一条文本”猜成功。

保留工具 call/result 配对、ID、顺序和宿主要求的状态 envelope；只更换结果 payload。推荐一个固定、非空、可被宿主接受的短墓碑，例如经 `LlmFacing` 渲染的 `superseded` 说明。空字符串可能被宿主特殊处理，必须以 canary 决定，不能凭感觉节省最后几个字。

不退休失败 assume 的错误结果，也不把一次 jq 失败计成新阶段。最新成功结果之后出现失败调用时，前一个成功画板继续是唯一当前画板。

位于已被 LWR 整体替换区间的 call/result 一起随完整 turn 被移出 provider 视图，不再额外插回墓碑。只有仍在 raw tail 中的历史成功结果才保留短墓碑。这能避免墓碑数量永久线性增长。

`update` 参数与 todos 参数不做单独篡改；它们可能还包含旧画板片段，直到所属完整 turn 可被 LWR 替换。不能声称 tombstone 立即清除了全部历史画板内容。

### 7.4 删除旧 todowrite 永久保留豁免

`OpenCode/Codec/ProjectionMessageEdit.fs` 现有 `isTodoWritePart / retainedTodoRounds` 会从被覆盖前缀中提取并重新保留 todowrite 相关消息。删除 `context-compression [020]` 的旧义务，并在新格式会话路径去掉这种无期限豁免。

不要把豁免对象简单从 todowrite 改成 assume，否则全量画板仍会累积。保留对象是“当前画板载体”和“最近 K 阶段的完整语义回合”，不是“历史上每个认知工具回合”。

### 7.5 Guidance、失败恢复与 SealRoot

沿用 `context-compression [019]` 和 `prefix-stability [010]/[014]`：冷边界原子退休旧辅助可见性；同 horizon 中 occurrence 原位、原字节 replay。不能直接在带 `NUL+BOM` guidance 的工具输出上截个字符串尾巴，造成旧指引丢失、重复或穿越 horizon。

Phase 边界、Probe 成功和 compaction reanchor 都服从同一个前缀 owner。失败 probe 的 attempt-local 计划仍不得变成 durable 事实；已经成功提交的 epoch 不因为下次 provider Failed/Aborted 而回退。synthetic ID 由冻结身份确定性派生，不使用时钟、随机数或新 GUID。

缓存验收看实际 wire prefix 和 provider 返回的缓存计量（仅作诊断）。LWR 生成本身是模型行为，不能假设重跑必同字节；确定性来自**已提交产物被冻结并复用**，不是来自摘要模型天生确定。

### 7.6 Compaction generation 与紧急恢复的额外裁定

Host compaction 后旧 XTrace generation 的 `Bi` 不得继续参与当前 cutoff 计算。认知提交的全局 ordinal 不回退，阶段锚点按 generation 分组；在新 generation 中尚无合法阶段边界时，不给出新的阶段 cutoff，保留重锚后的 raw 历史与一次恢复画板。不能把旧 generation 的序号当新消息数组下标。

K 原文窗口是**正常阶段收拢策略**。本方案保留紧急恢复能力：真实 WorkMain 失败后，既有 Probe 可在完整 coverage 和合法语义边界证明下越过正常 K 窗口，但必须作为 Probe 冷边界明确记录；不能删除当前未完成工具协议单元、真实 Opening 或唯一当前画板。若产品坚持 K 是连紧急恢复也不可打破的硬约束，应明确接受“大阶段可能无候选、最终耗尽失败预算”的后果。本方案选择前者，不能一面允许紧急压缩、一面宣称最近 K 阶段在所有情况下永不折叠。

## 8. WHAT 地毯式修订表

### 8.1 编号和规范迁移纪律

保留一个编号的核心含义时原位改写；彻底删除旧语义时从 active WHAT 中移除，不把旧编号重新分配给无关含义。旧编号的处置记录放在本文迁移表或 Git 历史，不为它们永久保留几十条空壳规范。

新条款从各包当前最大编号之后追加；下列新增编号均是基于本次基线的建议，实施时先检查分支是否已被其他改动占用。测试始终使用 `NNN.test.mjs`，标题带准确 `WHAT[package-NNN]`，不得用一个通过的文案正则代替行为测试。

### 8.2 核心包

| 包/条款 | 必须改成什么 | 不能遗漏的联动 |
| --- | --- | --- |
| `action-affordance [014]` | 单 jq + todos；全量返回；按 owner 持久；明确失败、重放、纯读取也提交阶段 | 中英文 description、arg-update、新增 arg-todos、移除 arg-query |
| `cognitive-environment [013]/[016]` | 删除实时义务账本训诫和双 jq 说明；保留一条阶段触发提醒 | Pair Hint 的 resource、资源目录和文案测试 |
| `obligation-ledger [001]–[006]` | 废止 CurrentObligations、私有 wire、status 禁令、planning 分类、名称连续性规则 | 旧 admission、Levenshtein、horizon 解码与投影 |
| `obligation-ledger [007]–[012]` | 旧 checkpoint 语义退役；其中通用的串行、幂等、错误分型移交 cognitive-workspace | 不是把可靠性规则直接删除 |
| `obligation-ledger [013]/[016]–[019]/[021]–[027]` | 废止 T1、BlindPlan 门禁、lag-1、旧 schema 广告、旧三钩子提交协议和透视粒度 | WorkRecord Opening、提示资源、宿主 hook wiring |
| `obligation-ledger [014]/[020]` | 去掉重复的质量职责声明，引用 relay-assessment 即可 | 不改变实际质量 owner |
| `obligation-ledger [015]/[028]` | 单向 sink 与基础设施故障原则迁到精简后的条款/共享边界 | legacy Journal 解码仍保留必要读取能力 |
| `obligation-ledger` 新增 `[029]–[034]` | 输入规范化；按 session/owner 的完整列表替换；desired/applied 幂等；空列表清除；失败诚实；禁止反向成为语义权威 | 它只作为 Todo sink 规范，不再拥有认知阶段 |
| `prefix-stability [002]/[004]` | 用 AssumePhase 取代 TodoCheckpoint 业务证据，保留 Probe 与 compaction；统一 ActivePrefixEpoch | snapshot-only 冷边界与幂等提交 |
| `prefix-stability [007]` | 移除 T1 作为现行业务概念的描述，保留同 Life system 字节冻结 | 不热切工具 schema 或语言绑定 |
| `prefix-stability` 新增 `[016]–[018]` | 阶段可见性计划；最新快照载体证明；阶段重复绑定与 generation 隔离 | 与同 epoch append-only 一起验收 |
| `context-compression [001]/[002]` | 保留无容量预测；若新增静态画板资源限额，精确列出不参与压缩决策的例外 | 不增加 token-driven policy |
| `context-compression [017]` | 真实 Opening floor 不扩大到任何 assume 阶段点 | includeOpening=false 的 same-session prefix |
| `context-compression [019]` | AssumePhase 冷边界同样原子退休旧 auxiliary visibility | guidance、grounding、Strength 的组合顺序 |
| `context-compression [020]` | 删除 todowrite 永久原文豁免，改为当前快照与正常 K 窗口合同 | `retainedTodoRounds` 退役 |
| `context-compression` 新增 `[028]/[029]` | K 公式、coverage 落后不丢 raw、frame 不跨界冒用、紧急 Probe 例外 | N=0/1、K=1/2、同 turn、compaction generation |
| `provider-projection [001]–[007]` | 保留 generic intent 架构，说明新结果退休由 feature owner 预先物化 | 禁止独立 mutating projector |
| `provider-projection [008]/[012]–[014]` | 增补完整 JSON 画板的确定性、无损表示与单次 render | LlmFacing 与 SyntheticToml 的边界 |
| `provider-projection` 新增 `[015]` | 已提交成功画板的投影退休不改变 canonical history，不破坏协议配对 | 所有 provider adapter 形态 |

### 8.3 宿主、生命周期和横切包

| 包/位置 | 改造内容 |
| --- | --- |
| `host-boundary [014]/[018]/[019]/[023]` | 保留公开接口、零 Fork 与真实 canary；新增 Todo sink 能力探测和版本证明，不用“内部 API”绕过 |
| `host-boundary [015]` | 区分普通工具尾截断与 assume 提交前大小 admission；通过 admission 的完整画板不得被有损截断 |
| `host-boundary [009]/[012]/[017]/[020]/[030]` | assume 所需 session、call、owner、raw JS 类型严格验证；无身份不能落盘 |
| `work-record [009]` 及现行 T1 raw-call 例外 | 删除“首次 planComplete=true 才结束 Opening”；保留真实委托及权威澄清，不把后来的 assume 变成 constitutive Opening |
| `office-capability` 的 Manager 工具条款 | 去掉 todowrite 的可见编排接口；assume 仍是认知 utility，不扩展文件/执行能力 |
| `capability-enforcement` | 更新工具 schema、所有角色可见性与执行拒绝；Blogger/Distiller 不借新参数取得 assume |
| `relay-retirement [002]–[004]` | 不要求 T1/assume/todos 为空；仍有 assessment 要求、freeze-before-check 和 exact recursive resources |
| `relay-assessment` | 删除如存在的 obligation/T1 文案依赖，但不改八维评级或证书语义；覆盖未清空 todos 也可按原准则完成评审 |
| `relay-incumbency` | 清理将 T1 当当前道路纪律的旧解释；身份、证书、退休纪要仍是独立事实 |
| `durable-events / crash-reconciliation / durable-convergence` | 注册新事实家族、codec、fold、恢复路径、幂等 effect；不在 load hook 做业务恢复 |
| `semantic-trace` | 当前 generation 的阶段边界定位；原始 assume 结果进入 canonical trace，墓碑不进入 canonical trace |
| `execution-failure-policy / effect-accounting` | 区分输入拒绝、持久化故障、已提交但 sink 未交付、身份冲突；复用现有 settlement 与 fatal 注入 |
| `provider-language` | assume 参数说明、错误提示、短结果指令中英对等；canonical 数据不因语言切换而变化 |
| `feature-ablation` | 旧 MagicTodo fact/tool/resource 映射退役；新 owner 与 sink 的依赖关闭不能留下半提交路径 |
| `verification-system / distribution` | 更新夹具、tier-gate、owner compile、资源闭包及真实 Host canary；不留已删除资源的打包引用 |

### 8.4 新增 cognitive-workspace 的最小 WHAT 集合

建议仅十条，每条对应一种真实约束，不再引入 twenty-state checkpoint 协议：

| 建议编号 | 内容 |
| --- | --- |
| `[001]` | 单一隐式 owner 画板、不同 owner 隔离、新 Life 初始化 |
| `[002]` | jq 恰好一个输出，所有失败前不改变语义状态 |
| `[003]` | 画板、todos、阶段同一 committed snapshot |
| `[004]` | exact ToolCallId 幂等与输入冲突分型 |
| `[005]` | owner 串行、跨实例一致、不同 owner 可并行 |
| `[006]` | Blob/Journal 先持久后 fold、崩溃恢复不重放 jq |
| `[007]` | 全量、无损、冻结结果交付，输入资源 admission |
| `[008]` | 阶段以成功提交建立，边界是 canonical semantic turn，不是 provider 数组下标 |
| `[009]` | 重锚恢复、代际隔离、当前快照可见性证据 |
| `[010]` | 不接管权限、执行资源、完成判断和质量评审 |

## 9. 逐目录、逐文件施工地图

以下都是“本次已定位的必查入口”与“明确标识的新增落点”。修改 `.fs` 的公开类型/函数时同步 `.fsi`；新增/删除文件必须更新 owner 编译单元、`.fsproj` Compile 顺序和 `APPLIES-TO`，不能只让运行时 import 凑巧可达。

### 9.1 工具表面和宿主配置

| 已有路径 | 实施动作 |
| --- | --- |
| `src/Wanxiangshu/OpenCode/Tools/AssumeTool.fs/.fsi` | 删除 ArgQuery、全局 workspace/tail 和 update→query；注入认知 owner 与 Host 能力，工具适配层只解码与调用 |
| `src/Wanxiangshu/OpenCode/Tools/ToolRegistry.fs/.fsi` | 重接 AssumeTool 构造依赖；维护 role admission；确认最终 provider 广告只有新参数 |
| `src/Wanxiangshu/OpenCode/Tools/StaticTools.fs/.fsi` | 删除 TodoWrite 的 allow 路径；必要时仍保留宿主 todowrite 的显式 deny 名称；区分“注册可调用”与“安全拒绝列表” |
| `src/Wanxiangshu/Foundation/OfficeCapability.fs/.fsi`、`Roles.fs/.fsi` | 查找并移除 `ToolPermission.TodoWrite` 的领域授权与穷尽分支；assume 不升格为业务权能 |
| `src/Wanxiangshu/OpenCode/Plugin/PluginHooks.fs` | 卸下 MagicTodo 三钩子业务路径；保留 grounding、estimate、casebook 等无关 hook；新 owner 恢复不能在 plugin load 执行 |
| `src/Wanxiangshu/OpenCode/Codec/ToolHostCodec.fs` | 复核 ToolCallId/context、array/object schema 和结果截断入口，按明确合同接线 |
| `src/Wanxiangshu/OpenCode/Host/PluginHostInterop.fs` | 清理仅服务旧 ProviderInputRejection 的特判；新失败仍走 closed typed membrane |

工具“完全不可见”以真实发给 provider 的 tools 数组为准。仅删除插件自己的 `tool` 字典条目不够：宿主内置工具可能仍出现。反过来，为阻止内置工具重新出现而留下一个显式 deny 字符串，并不构成保留旧认知工具。

### 9.2 新的认知 owner 与瘦身后的 Todo 适配

建议新增 `src/Wanxiangshu/Participant/Cognition/Workspace.fs/.fsi`（类型与纯提交验证）、`Projection.fs/.fsi`（当前快照和索引）、`JournalPort.fs/.fsi`（最小持久能力）、`Runtime.fs/.fsi`（串行执行与恢复）、`Surface.fs/.fsi`（必要测试/边界出口）。是否拆分以编译所有权为准，不为了表格把一个短函数机械拆成一堆文件。

旧 `src/Wanxiangshu/Mission/Obligation/Todo/` 中：

- `Model`、`ObligationCodec`、`Surface`：删旧义务模型、schema 和规划校验；保留或改写为轻量 Todo 规范化能力，不保留 old/new 两套 runtime mode。
- `MagicTodoMembrane` 及对应 Surface：生产三钩子 Prepared/Accepted/T1 路径退役；通用可靠性需求迁移到新 owner，不搬整个状态机。
- `PrefixEpoch` 和 `MagicTodoSemanticSurface` 的 lag-1 helper：生产替代路径贯通后删除，不保留一个假装兼容的新名字。
- `OpenCode/HostCodec`：删除 non-enumerable obligations→todos 技巧，保留受支持的官方行编解码职责；新 Host sink 的实际实现必须通过 P0。
- 其他 `MagicTodo*Surface`、`MagicTodoIdentity`、locality/recovery 辅助：逐一根据引用判断保留的历史读取与应删除的 active runtime，不能按文件名整目录盲删。

### 9.3 持久化接线与历史兼容

`src/Wanxiangshu/Composition/Durable/MagicTodoFacts.fs`、`MagicTodoFactCodec.fs`、`MagicTodoProjection.fs` 是旧事实、解码和 projection 的核心入口。当前 `Fact/Fold/ProjectionState/ProjectionUpdate/DomainFamilyBridge` 和 `Persistence/Journal/AgentJournal.fs` 的接线也必须沿引用链检查。

新事实定义归认知 owner，Composition 只接线，不再把领域定义继续堆进 Composition。旧日志所需 decoder 保留为明确的 legacy read 路径；旧事件不能在新 Life 激活 planComplete latch、TodoCheckpoint 或 Host 回写。不要为了 grep 零命中破坏历史恢复。

新增认知家族后的恢复顺序：通用 Journal replay → owner 增量 projection → 活跃 authority 校验 → 最新 todo sink 对齐与缺失结果交付 → 下一次请求前 prefix binding。不要在业务热路径重新扫描整个 Journal。

### 9.4 Prefix、Companion 和原始消息写回

| 已有路径 | 实施动作 |
| --- | --- |
| `src/Wanxiangshu/Context/Prefix/Epoch.fs/.fsi` | 表示 snapshot-only 冷边界；保留 sequential epoch、stale absorption、compaction run 去重 |
| `src/Wanxiangshu/Context/Prefix/Candidate.fs/.fsi` | 候选身份冻结新可见性字段；Probe 与阶段证据的合法性分别验证 |
| `src/Wanxiangshu/Context/Prefix/Projection.fs/.fsi` | 从 committed facts 读取稳定 prefix presentation，不按 live 最新值重渲染旧 epoch |
| `src/Wanxiangshu/Context/Prefix/Wire.fs/.fsi`、`WirePort.fs/.fsi` | 协调结果退休、K 窗口、stable Host id 与单一 generic replacement |
| `src/Wanxiangshu/Context/Prefix/ProbeSelection.fs/.fsi` | 保留失败驱动与 coverage proof；实现明确的紧急 K 例外而非隐式越界 |
| `src/Wanxiangshu/Context/Companion/Blogger/` | 沿现有 ContextFactFold、BloggerMainContext 等 owner 接口补证据绑定；不复制第二套 catch-up/retry 算法 |
| `src/Wanxiangshu/Context/Trace/` | 提供完整 turn 起点与 generation/Host identity 对齐；不从投影后墓碑取 digest |
| `src/Wanxiangshu/OpenCode/Codec/ProjectionMessageEdit.fs` | 删除新格式路径上的 `retainedTodoRounds`；支持已经由 owner 决定好的通用结果写回 |
| `src/Wanxiangshu/Participant/Provider/Projection/` | 保持 generic Model/Intent/Planner/Renderer，无新增 feature 专用解释器 |

### 9.5 表示、Opening、语言和资源

`src/Wanxiangshu/Foundation/LlmFacing.fs/.fsi`、`SyntheticToml.fs/.fsi` 承担完整 JSON 呈现能力。先写 null/混合数组/特殊键回归，再接 assume；不能用变更旧 structuredValue 的副作用顺便“修所有输出”。

`src/Wanxiangshu/Mission/WorkRecord/Model.fs/.fsi` 当前含 `FirstPlanCompleteTodoWrite` 和 `BlindPlan`；`Materialize`、`OpeningSemanticSurface` 与 Trace Opening 选择必须一起修改。same-session prefix 只使用不重复 Opening 的已覆盖材料，不能调用带 RawGap 的通用 LWR 后直接宣称 coverage 完整。

资源的明确动作：

| 资源路径 | 动作 |
| --- | --- |
| `resources/provider/tool/assume/description/{zh-CN,en}.md` | 重写单 jq、完整返回、todos 与成功/失败后果 |
| `resources/provider/tool/assume/arg-update/{zh-CN,en}.md` | 保留标准 jq 与单输出约束，去掉搭配 query 的指南 |
| `resources/provider/tool/assume/arg-query/` | 删除，两种语言一起删除 |
| `resources/provider/tool/assume/arg-todos/{zh-CN,en}.md` | 新增，说明完整替换、空清单、priority 默认和普通状态 |
| `resources/provider/host/pair-programming-guideline/{zh-CN,en}.md` | 删除账本与双 jq 的重复训诫，保留短触发与并发纪律 |
| `resources/provider/lifecycle/magic-todo/` | 逐项移除 T1、horizon、working-on、plan-complete、accepted epilogue 等旧资源和所有调用引用 |
| `src/Wanxiangshu/Ablation/{FactMap,ToolMap,Manifest,Gate,Settings,Surface}.fs` 及签名 | 新旧 fact/tool/resource 映射与依赖闭包一致更新 |

最后对其他 Role Law、handbook、book 内容和中文/英文测试做精确检索，不只改上述资源。

## 10. 分批实施剧本：每批必须能独立审查

批次是代码审查与验证单元，不是同时上线两套认知协议的功能开关。无法单独运行的接口切换批次放在同一集成分支，最终以一次冷启动版本切换交付。

### P0：固定事实与证明宿主能力——硬门禁

**改动范围：** 基线记录、Host contract 测试、真实 canary 及必要的能力诊断，不改生产认知协议。

先确认固定 Host build 的公开 Todo schema、写入口、读取验证入口和 `todo.updated` 行为；记录版本/hash、启动命令、公开观察方法。测试从 `assume` 的实际自定义工具执行路径调用公开写能力，而不是让模型补调一次 todowrite。

Canary 必须同时证明：非空全量替换；`[]` 真清空且订阅者收到更新；重启后读回；两个 session 不串表；延迟旧 owner 不覆盖新 owner；模型 tools 中没有 todowrite；大结果在公开传输链上完整到达。

**红灯：** 只能用 mock、只能发事件、只能改数据库、只有私有执行器可用，或真实 provider 工具表仍带 todowrite。此时不得进入“可发布”的切换批次。允许继续纯算法/文档工作，但不能宣称目标在现有 Host 上已支持。

**绿灯：** 有固定版本的公开接口 contract 与真实 canary 双证据，且输出上限已核实。需要上游新版本时，同步缩紧 peer 支持范围；不能继续声称未经验证的 `>=1.17.4` 全部支持新能力。

### P1：先改规范与测试锚点

按第 8 节修改 WHAT/WHY/APPLIES-TO，写出对新行为失败的测试。先关闭三项歧义：owner 作用域；K 包含当前阶段且默认 2；normal K 与 emergency Probe 的关系。

为 legacy 事件仅保留读取明确列条款，为 `host-boundary [015]` 的完整结果 admission 明确列例外。不要把旧的两字段 Todo 草案当官方完整 schema。

**退出条件：** 需求与测试映射闭合，新失败均对应明确目标；没有互相冲突的“所有工具都尾截断”和“assume 永远全量”。

### P2：实现无损画板表示与纯核心

先改 LlmFacing 的完整 JSON 能力、输入规范化、jq 输出计数校验、快照构造与稳定 digest。测试 null、对象混合数组、特殊键、空结构、字符串转义、大数字、根标量与稳定顺序。

禁止业务代码直接拼 TOML，禁止为了通测禁掉合法根值。预检上限必须在语义提交前完成；所有错误明确“不修改当前画板”。

**退出条件：** 完整表示 round-trip/golden/property tests 通过，现有普通 LlmFacing 输出未被无关改动破坏。

### P3：建立 durable owner 与恢复

新增最小认知 owner，接 Blob/Journal/fold、ToolCallId 索引与 owner 串行 admission。替换全局内存状态的方式是注入 durable capability，不是在旧全局变量旁边再加一个“备份文件”。

按第 4.4 节逐个注入故障；覆盖跨 plugin instance、跨 session、同 call 重放、不同输入冲突、恢复后 `.count += 1` 不重复增长。

**退出条件：** 重启后状态由事实恢复；任何已提交更新只应用一次；新 Life 无隐式继承；失败不推进 ordinal。

### P4：接入 Todo sink，不改变认知真相源

通过 P0 已证明的公开入口实现全量行替换；content 与顺序不自作主张修改，priority 仅按规范默认。用 desired/applied 实现最小重投影，复用已有 effect/recovery 机制。

先写“已提交后 UI 写失败”的测试，再写正常成功测试。拒绝靠 UI 读回恢复 canonical 状态；防止上一任迟到写入覆盖下一任。

**退出条件：** UI 可延迟但最终与最新已提交 todos 对齐；失败不会重复 jq、不伪造全局回滚；空数组稳定清空。

### P5：原子切换工具表面与提示词

修改 AssumeTool/ToolRegistry/StaticTools/角色权限与资源；移除 query，模型广告没有 todowrite。卸下旧 MagicTodo 的生产三钩子，不影响其他 hook。

同步更新 `action-affordance/tests/014.test.mjs` 和 `capability-enforcement/tests/010.test.mjs`，后者当前明确断言 assume 参数为 update/query，并实际测试局部 query，必须替换为真实新行为断言。

工具 schema 是 prefix identity 的一部分。只在新 Life/冷启动发布新表面，不能在同一既有 Life 的第二次请求突然换 schema 和 system 文案。

**退出条件：** 所有普通角色工具快照通过；Blogger/Distiller admission 仍正确；旧字段无生产执行路径；中英文契约一致。

### P6：先做结果退休，再做普通历史压缩

先实现 snapshot-only phase cold boundary，在没有 LWR 的情况下跑通成功结果墓碑化。需要真实 call/result identity 与可见性证明，不读模型正文判断成功。

用至少三个阶段和一个失败 assume 验证：第一/第二成功画板退休，第三完整保留，失败错误保留；canonical Host/XTrace/Journal 不变；同 epoch 普通调用精确 append-only。

**退出条件：** 一份当前画板保证可见；协议配对不坏；新阶段只在合法边界改变历史；重放不反复增 epoch。

### P7：接入 K 窗口、Blogger coverage 与生产前缀

把第 6 节的窗口选择写成纯函数；把材料读取、coverage 验证、cold-boundary append 留在 coordinator；把已物化 rows 交给 generic projection。

覆盖 Blogger busy/失败/空输出、frame 跨 cutoff、无新 coverage、同 turn 多 assume、late Blogger、Probe 与 phase 相邻、Host compaction generation。不能为压缩等待 Blogger 让 assume 工具无限挂起。

**退出条件：** actual cutoff 从不越过证据，旧前缀不回退，未覆盖 raw 不丢；phase 内不随 Blogger 进度偷偷改前缀；真实 provider wire 证明全部接线。

### P8：删旧义务协议与修 WorkRecord/retirement

删除旧 active runtime 中的 planComplete、T1、near/mid/far、workingOn、lag-1 与 todo rounds 永久豁免；修改 OpeningPolicy/Materialize 和 Role Law。不能把第一次 assume 换皮成新的 T1。

新增退休测试：没有任何 assume、todos 有 pending、todos 有 completed、画板声明完成等输入，不影响既有 assessment/真实资源裁决。live child、PTY、active lease 和未结算 cancel/join 继续阻塞；缺 assessment 继续走现有提示。

**退出条件：** 旧概念只存在于受控历史 decoder、迁移材料或负向测试，不再影响新会话控制流；评审与资源闭环无回归。

### P9：迁移、消融、长会话与组合故障

实施第 13 节的冷启动迁移；验证旧 Journal 可读、新事实不会被旧 runtime 错消费。检查 ablation 中禁用 sink/认知/prefix 时不存在“提交一半”的配置；不支持的依赖组合启动时明确拒绝。

加入多角色并行和长会话测试；在工具提交、sink 同步、结果交付、phase binding、Blogger commit 和 provider retry 之间注入故障。验证隐私诊断不记录画板正文、todo 内容或凭证。

**退出条件：** 无串板、无回滚假象、无 canonical 历史损坏、无重复 epoch；兼容模式与配置边界清楚。

### P10：最终清理与冷启动发布

跑定向套件，再跑完整 format/build/test 和发布门禁。检查删除资源、owner 编译声明、测试锚点与包闭包；发布说明写清工具破坏性变更、支持的宿主版本和旧会话处置。

禁止自动滚动切换正在执行中的 Life；等待既有执行资源按原协议收束后启用新版本。代码回滚不能直接对已写入新事实的 Journal 启动旧 decoder。

**退出条件：** 第 15 节的清单逐项有证据；只在用户授权时提交/推送，不把方案文档的交付当成执行发布授权。

## 11. 测试矩阵：必须验证行为，而不是验证代码长得像方案

### 11.1 工具、画板与 UI

| 用例 | 关键断言 | 建议测试归属 |
| --- | --- | --- |
| 新 schema | 必填 update/todos；无 query/私有账本字段 | action-affordance 014；capability-enforcement 010 |
| 最终工具集 | provider tools 无 todowrite；伪造调用拒绝；内部 sink 不制造模型调用 | capability-enforcement；host-boundary 019 |
| 单输出更新 | `{}`、数组、标量、null 可替换并完整返回 | cognitive-workspace 002/007 |
| 零/多输出 | `empty`、`1,2` 不改变画板、todos、phase、epoch | cognitive-workspace 002/003 |
| jq 错误 | 原状态保留，下一合法调用仍可执行 | cognitive-workspace 002/005 |
| 参数错误 | 旧 query、缺 todos、非数组、非法 status 在副作用前拒绝 | cognitive-workspace 002 |
| identity replay | `.count += 1` 同 call 重放只增长一次 | cognitive-workspace 004/006 |
| 输入冲突 | 同 call 不同 digest 不覆盖既有事实 | cognitive-workspace 004 |
| 同 owner 并发 | 第二次看到第一次结果，无 lost update | cognitive-workspace 005 |
| 跨 owner 并发 | 相同字段名互不污染，UI 不串表 | cognitive-workspace 001/005 |
| plugin 多实例 | 同 owner 不获得两条独立执行队列 | cognitive-workspace 005 |
| Todo 四状态/priority | 规范化行与固定 Host contract 一致，不附私货 | obligation-ledger 新 029/030 |
| 空 todos | 删表与更新事件俱有证据，重启后仍空 | obligation-ledger 新 032；真实 Host canary |
| sink 失败 | canonical 已提交且不重跑 jq，交付状态诚实 | obligation-ledger 新 031/033 |
| stale sink | 旧 owner / 旧 snapshot 的写和 ack 不覆盖最新值 | obligation-ledger 新 031 |
| 无损输出 | null 不消失，混合数组对象不变 false，特殊键/字符串保真 | provider-projection；cognitive-workspace 007 |
| 过大完整结果 | 提交前明确拒绝，不能“成功但只返一半” | host-boundary 015/019 |

### 11.2 阶段、前缀与覆盖

| 用例 | 关键断言 |
| --- | --- |
| N=0、N=1 | 无凭空阶段；Opening 和第一个 assume 完整回合保留 |
| K=1/2 | 对照第 6.2 表，不差一；K=0/负值拒绝 |
| 同 turn 多 assume | phase ordinal 增加但物理 cutoff 可相等，不拆 call/result |
| 成功 A1 后失败 A2 | A1 完整结果仍可见，A2 错误保留，不推进阶段 |
| A1/A2/A3 成功 | raw 区域仅 A3 完整；更旧成功结果短墓碑；已压缩区不额外插墓碑 |
| 结果尚未物化 | 没有新载体证明就不清空旧结果 |
| snapshot-only epoch | cutoff 不变也能合法冻结结果退休，不伪造新 coverage |
| 同阶段普通调用 | provider/model/variant/tools/system/messages 的 prefix law 均成立 |
| Blogger 晚到 | 下一普通请求保持既有冻结前缀，不在同 epoch 抢先更新 LWR |
| coverage 有 gap | 留住所有未覆盖 raw，不把 RawGap 当 PrefixCoverage |
| frame 越界 | 不拿跨越 desired cutoff 的摘要冒充精确子区间 |
| 前缀重复提交 | 相同 commit identity 不重复 increment epoch |
| Probe 失败 | tentative presentation 不持久化；未提交候选可丢弃 |
| 新 epoch 后 provider 失败 | epoch 不回滚，retry 继续使用已提交前缀 |
| phase + Probe 相邻 | 以 expected previous epoch 裁决，不产生两个互相覆盖的 prefix 真相 |
| compaction 重放 | 同 run 只重锚一次；旧 generation 边界不进入新 cutoff |
| emergency Probe | 只在实际失败路径使用明示例外，保留当前协议单元与当前画板 |
| guidance/NUL+BOM | 同 horizon 原位 replay，冷边界统一退休，canonical trace 不含辅助后缀 |
| provider 差异 | combined tool part 与 split call/result 形态都保持 ID 配对；非文本部分不丢 |
| 历史不可变 | 投影前后 canonical Host/XTrace/Journal 摘要一致 |

### 11.3 退休、迁移与架构负向测试

必须证明：pending todos 不阻塞；没有 assume 不阻塞；缺 assessment 仍提示；资源仍阻塞；freeze 后不能创建新资源；上一 Incumbency fence 不误伤下一任；旧 T1 事件不能激活新协议门禁。

架构负向断言应覆盖：无生产 query 参数、无非 enumerable 私有兼容参数、无动态 renderer 查 Journal、无第二个 epoch owner、无每轮重发完整画板、无基于 token/时钟的正常 phase 决策、无用 Host UI 恢复 canonical board。

同时明确应当允许的残留：公开 deny 列表中的 todowrite；legacy codec 中的历史事件名；描述旧格式拒绝行为的测试；本方案和历史说明。禁止把“全仓 grep 零命中”作为不加区分的验收。

### 11.4 必须有一条真正的贯通测试

```text
启动真实受支持 Host
  → 开启有有效 authority 的会话
  → 经真实模型工具广告/执行路径调用 assume
  → 官方 Todo 读取与事件验证一致
  → 已提交快照与工具完整结果一致
  → 普通工具执行若干次
  → 第二、第三次 assume
  → Blogger 产物与 coverage 提交
  → 捕获下一次 provider wire
  → 检查 latest-only + K/cutoff + call/result + prefix identity
  → 重启并重放同 ToolCallId
  → 状态、UI、epoch 和结果字节不重复变化
```

不要用 `MagicTodoSemanticSurface` 换名后的单元测试来替代这条链。没有真实 Host/公开 API 条件时，报告“canary 未完成”，不是“模拟测试通过所以已支持”。

## 12. 可复制的验证命令与执行纪律

以下命令从仓库根目录执行。它们是后续实施命令，不表示此次编写方案已经运行。不要执行 `dotnet build`；仓库只有 Fable 编译目标。

### 12.1 开工与定向检索

```bash
git status --short
git diff --stat

rg -n 'planComplete|workingOn|TodoCheckpoint|desiredLag1Cutoff|FirstPlanCompleteTodoWrite' \
  src/Wanxiangshu resources/provider requirements \
  --glob '!repomix-output.xml'

rg -n 'ArgQuery|assume\(update, query\)|retainedTodoRounds|defineCompatibilityTodos' \
  src/Wanxiangshu resources/provider requirements \
  --glob '!repomix-output.xml'

rg -n 'MagicTodo|AssumeTool' src/Wanxiangshu --glob '*.fsproj'
```

检索结果逐条分类，不用自动程序批量替换代码。`horizon` 也是现有合法工具名，`query-shell` 和搜索 query 也与本次删除的 assume.query 无关，不能按词全仓删除。

### 12.2 编译与第一组定向测试

```bash
node scripts/build.mjs

node --test \
  requirements/action-affordance/tests/014.test.mjs \
  requirements/cognitive-environment/tests/013.test.mjs \
  requirements/cognitive-environment/tests/016.test.mjs \
  requirements/provider-projection/tests/*.test.mjs

WXS_TIER_INTEGRATION=1 node --test \
  requirements/capability-enforcement/tests/010.test.mjs
```

`requirements/verification-system/tests/support/tier-gate.mjs` 用 `WXS_TIER_INTEGRATION=1` 与 `WXS_TIER_RELEASE=1` 控制 tier。默认运行出现 skip 不等于集成验证已通过。必须看测试总数、skip 数和退出码。

### 12.3 完成 P3–P8 后的测试组

新包创建后再运行其路径；不在包尚不存在时将 glob 失败记作实现失败。

```bash
node --test requirements/cognitive-workspace/tests/*.test.mjs

WXS_TIER_INTEGRATION=1 node --test \
  requirements/obligation-ledger/tests/*.test.mjs \
  requirements/context-compression/tests/*.test.mjs \
  requirements/prefix-stability/tests/*.test.mjs \
  requirements/work-record/tests/*.test.mjs \
  requirements/semantic-trace/tests/*.test.mjs

WXS_TIER_INTEGRATION=1 node --test \
  requirements/host-boundary/tests/*.test.mjs \
  requirements/relay-retirement/tests/*.test.mjs \
  requirements/relay-assessment/tests/*.test.mjs \
  requirements/feature-ablation/tests/*.test.mjs \
  requirements/durable-events/tests/*.test.mjs \
  requirements/crash-reconciliation/tests/*.test.mjs
```

现有 Host canary 的组织可参考 `requirements/host-boundary/tests/support/run-opencode-chat-admission-canary.mjs` 和对应 fixture，但 Todo canary 必须新增真实场景，不把 chat-admission 通过当 Todo 写入通过。新 canary 的具体命令由其正式入口文件提供，本文不虚构一个尚不存在的脚本。

### 12.4 集成与发布门禁

```bash
npm run check
npm run format-build-test
npm run verify:release

git diff --check
git diff --stat
git status --short
```

`scripts/verify.mjs` 当前仅支持 `--release`、`--verbose`、`--profile` 和 help，不存在 `--package`/`--requirement` 定向参数。定向验证用上面的 Node test 入口，不编造仓库 CLI。

不要编辑 `dist/`、`artifacts/` 或生成的编译产物来让测试变绿。新增与删除的 source/resources 必须通过仓库构建生成正确输出，并确认 release 包里没有旧 arg-query/magic-todo 资源残骸。

## 13. 迁移、灰度与回滚

### 13.1 选定的迁移策略：新 Life 冷切，历史只读

本次是工具 schema 和持久事实的破坏性变化，首版不支持在同一活跃 Life 中热升级。先让旧版本已经接受的执行资源按现有协议完成/取消，再开启新版本的新 Life；不强制杀进程来制造一个看似干净的迁移点。

旧 `assume` 画板仅存内存，而且历史 query 可能只返回局部，**不能从最后一次 query 结果伪造完整旧画板**。默认新 owner 从 `{}` 开始，保留合法的任务和历史工作记录；需要继承时显式 bootstrap 并注明来源，不把历史 UI 清单当已验证认知事实。

旧 `TodoWritePrepared/Accepted`、T1 和 PrefixRebaseCommittedV2 等历史事件可被读取和审计，但不驱动新 owner 的 planComplete、前缀切换或 UI 回写。活跃旧协议与新协议不得同时写同一个 canonical owner。

### 13.2 滚动升级与支持范围

先在独立测试存储和真实 Host canary 验证新版本，再部署到新会话；不要在同一 authority/Life 上通过 feature flag 来回切换 tools/schema。新的 supported Host 版本范围由 P0 证明，不沿用一个未经测试的宽 peer range。

灰度是“哪些新会话使用新版本”，不是“同一次 assume 一半走旧账本一半走新画板”。不支持的消融/能力组合在 activation 前报出明确 typed unsupported，不在业务途中偷偷退回 todowrite。

### 13.3 回滚分界

在尚未写入任何新事实时，可以回滚新构建并保留旧 Journal。写入新事实后，不能仅 `git checkout` 旧代码然后启动旧 decoder；这会把合法新事实误判为损坏，或产生错误恢复。

允许的回滚路线只有已验证的新旧 reader 兼容方案，或在执行资源收束后恢复完整、相互一致的迁移前存储快照并明确其数据损失范围。不得只恢复 Host TodoTable、只恢复画板文件或修改事件行来“修平”版本。

Blob GC 与历史 codec 的最终删除另立生命周期政策；不得因为 provider 看不到旧画板就删除审计事实。

## 14. 效果衡量：衡量得到的才写进发布说明

| 维度 | 验收方式 | 不作的承诺 |
| --- | --- | --- |
| 模型负担 | 对照固定场景比较工具 schema、Pair Hint 字节与认知维护调用次数 | 不预先承诺减少某个百分比 token |
| 当前画板重复量 | 每个 owner 的系统管理完整画板载体数为 1；无新载体时旧版不先消失 | 不声称删除了用户自行引用的所有旧内容 |
| 正常前缀稳定 | 同 epoch 的完整 provider wire 通过 `isAppendOnlyPrefix` | 不声称外部缓存一定 100% 命中 |
| 冷边界次数 | 每次都有 phase/probe/compaction 已提交证据；重复绑定为零 | 不按 timer 或模型容量强制换代 |
| 历史压缩 | 实际 cutoff 有 coverage、digest、generation、完整 turn 证明 | 不声称无覆盖也能安全删除 |
| UI 一致性 | 最新 desired 最终等于 applied，旧 owner 不覆盖新 owner | 不把 UI 更新与 Journal 说成同一数据库事务 |
| 恢复质量 | 所有已提交增量只应用一次；失败结果不冒充阶段 | 不靠重试碰运气掩盖错账 |
| 代码减法 | active T1/lag-1/义务校验/旧 hook/资源引用实际删除 | 不把通用恢复保障算作应砍的复杂度 |

诊断只记录允许的身份、摘要和 typed transition；画板正文、待办内容、token/credential/path 等不得随意塞进 `host-boundary [025]` 的受限因果诊断 schema。实验性 payload/缓存指标采用符合规范的专用验证记录，不进入业务分支。

长会话测试至少覆盖：无 assume 的长阶段、频繁 no-op assume、Blogger 长期落后、快照很小但原始工具输出很大、快照逐渐增长、多 owner 并行、compaction 后恢复。这样才能知道“最新画板 + 最近 K 阶段 + LWR”的实际代价，而不是用一个理想演示代替系统边界。

## 15. 最终交付检查单

- [ ] 新工具仅接受 update/todos 兼容输入，query 和私有账本字段被明确拒绝。
- [ ] 最终 provider tools 不含 todowrite；宿主显式 deny 与内部公开 sink 的职责分开。
- [ ] Host Todo 写入、空清单事件、重启读回、隔离和完整结果传输均有真实 canary。
- [ ] 画板按 owner 持久，跨会话不串；新 Life 初始化与显式继承规则已写 WHAT。
- [ ] 同调用重放不重复 jq、不重复阶段；参数/零输出/多输出失败不产生副作用。
- [ ] 任意受支持 JSON 值完整可见；null/混合数组/特殊键/根标量不被悄悄丢弃。
- [ ] 大结果在提交前 admission，不出现“成功写入但返回被截断”的假全量。
- [ ] 最新成功结果可见后才退休旧结果；失败错误保留；工具协议配对不破坏。
- [ ] 只有 provider 表示改变，canonical Host/XTrace/Journal/Blob 历史保持真实。
- [ ] K 公式、同 turn 情况、P0 前导历史和 emergency Probe 例外都有正式测试。
- [ ] Blogger 落后时保留未覆盖 raw，不把 RawGap/frame 越界当完整证明。
- [ ] snapshot-only 冷边界接入既有 ActivePrefixEpoch，无第二个 epoch SSOT。
- [ ] 同阶段 wire 精确稳定；晚到 Blogger 不改当前前缀；compaction generation 不混用。
- [ ] WorkRecord 不再以 T1/第一次 assume 关闭 Opening；真实 Opening 未被误压缩。
- [ ] assessment、执行资源、freeze-before-check 和异常恢复机制均保留。
- [ ] 旧 active MagicTodo 代码/资源/测试断言/ablation 映射已清理；legacy reader 范围明确。
- [ ] `.fsi`、owner 编译、APPLIES-TO、资源闭包和测试锚点同步完成。
- [ ] 定向测试、integration tier、真实 canary、format/build/test、release 门禁均有实际结果。
- [ ] 发布只针对支持的 Host 与新 Life，回滚不会让旧 decoder 误消费新 Journal。

**完成标准不是“把两个工具合成一个”，而是：模型只维护一个认知入口，系统仍能诚实解释每一次持久提交、每一份可见画板、每一次前缀变化和每一个未完成的物理副作用。**

## 附录 A. 核对依据与外部能力边界

### A.1 仓库内主要依据

| 编号 | 已核对位置 | 支撑的关键事实 |
| --- | --- | --- |
| R1 | `AGENTS.md`、`package.json` | 规范所有权、Fable 构建、固定依赖、修改与验证纪律 |
| R2 | `requirements/action-affordance/WHAT.md [014]`；`OpenCode/Tools/AssumeTool.fs` | 双 jq、单进程内存画板、更新后查询、无 durable owner |
| R3 | `requirements/obligation-ledger/WHAT.md`；`Mission/Obligation/Todo/OpenCode/HostCodec.fs`；`OpenCode/Plugin/PluginHooks.fs` | 28 条旧义务规则、三钩子 membrane、官方三字段兼容投影 |
| R4 | `Mission/Obligation/Todo/PrefixEpoch.fs`；`MagicTodoSemanticSurface.fs`；`Context/Prefix/Epoch.fs` | lag-1 helper 的接线边界与现有 prefix SSOT/fold |
| R5 | `requirements/context-compression/WHAT.md`；`requirements/prefix-stability/WHAT.md` | coverage、完整 turn、冷边界、Opening、无容量预测、失败恢复 |
| R6 | `OpenCode/Codec/ProjectionMessageEdit.fs` | todowrite 回合的永久保留实现与 stable Host identity 写回 |
| R7 | `requirements/provider-projection/WHAT.md`；`Foundation/LlmFacing.fs/.fsi`；`SyntheticToml.fs` | generic intent、单次 render、现有结构表示的 null/混合数组边界 |
| R8 | `requirements/host-boundary/WHAT.md [014]–[025]` | 零 Fork、公开接口、真实 canary、有界截断、诊断限制 |
| R9 | `requirements/work-record/WHAT.md`；`Mission/WorkRecord/Model.fs` | T1 constitutive Opening 及原始调用例外 |
| R10 | `requirements/relay-retirement/WHAT.md`；`requirements/cognitive-environment/WHAT.md` | 现行退休条件、Pair Hint 与双 jq 文案 |
| R11 | `requirements/action-affordance/tests/014.test.mjs`；`requirements/capability-enforcement/tests/010.test.mjs` | 现有双 jq 正则与集成执行断言 |
| R12 | `requirements/verification-system/tests/support/tier-gate.mjs`；`scripts/verify.mjs` | 集成/release 环境开关与真实验证 CLI |

表中省略 `src/Wanxiangshu/` 的源码路径均位于该目录下。

### A.2 外部核对资料（2026-09-23 检索）

E1：OpenCode 官方仓库 `dev/packages/schema/src/session-todo.ts`。包含 content/status/priority，状态说明含 cancelled。它是本次取回的开发分支资料，不是固定 v1.18.29 的运行时证明。

```text
https://raw.githubusercontent.com/anomalyco/opencode/dev/packages/schema/src/session-todo.ts
```

E2：OpenCode 官方仓库 `dev/packages/opencode/src/session/todo.ts`。内部实现将 TodoTable 替换与事件发布作为具体宿主职责；**内部存在 update 不代表插件 SDK 公开可调用它**。

```text
https://raw.githubusercontent.com/anomalyco/opencode/dev/packages/opencode/src/session/todo.ts
```

E3：OpenCode 官方 plugin 类型。公开 Hook 的 before 输出主要是 args，definition 修改描述和参数；本次取回的接口没有给出可任意调用内部 Todo service 的工具执行能力，不能从其空白推导一个不存在的方法。

```text
https://raw.githubusercontent.com/anomalyco/opencode/dev/packages/plugin/src/index.ts
```

E4：官方 Plugins 文档列出了 `todo.updated` 事件和自定义工具机制。可订阅事件不等于可以持久更新 TodoTable。

```text
https://opencode.ai/docs/plugins/
```

E5：官方仓库 issue #17499 记录过“提供 Todo 更新 API”的需求；它是历史问题记录，不用来断言所有后续版本都没有该能力。

```text
https://github.com/anomalyco/opencode/issues/17499
```

本次在线获取 v1.18.29 原始源码地址未成功，因此本文不声称已验证固定版本的公开写入口。P0 的源码/类型核对和真实 Host canary 是必须补齐的工程工作，而非可以省略的手续。

## 附录 B. 最容易把新方案重新做重的十种写法

| 错误写法 | 正确替代 |
| --- | --- |
| 新建 BoardLedger、TodoLedger、PhaseLedger 三套模型可见工具 | 一个 assume 提交、一个快照，其他都是不可见技术投影 |
| 给 todos 加 planComplete、horizon、workingOn | 官方字段与静态缺省，不再造义务状态机 |
| 把第一次 assume 定义为“不可逆计划承诺” | 每次成功仅确立当前快照与阶段，不承诺后续不可改判 |
| 用 todos 为空判断工作成功/可退休 | 质量与执行资源由原 owner 裁决 |
| 每次 transform 搜索最新 assume 然后就地删旧文本 | 从 committed epoch presentation 生成稳定 rows |
| 为每个 feature 再建一套 epoch | 既有 ActivePrefixEpoch 接受明确证据 |
| Blogger 没覆盖也先删，反正最新画板应该够用 | 未覆盖 raw 保留，coverage 先于删除 |
| 把完整 JSON 硬转 TOML，null 一律省略 | 明确无损表示能力，必要时用单份 canonical JSON 字符串载荷 |
| 为 UI 同步失败重新执行整段 jq | semantic commit 不回滚，只重投影最新 sink |
| 为宣称全量与高缓存隐瞒截断、长阶段和恢复例外 | 写清 admission、正常窗口、紧急 Probe 和实际测量范围 |
