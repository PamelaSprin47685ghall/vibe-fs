目前 llm 工具时常调用失败，但是已经成为既定事实，无法 amend。或者说 llm 没有走错方向的后悔药吃。
1. 请你在 tool before 钩子中检测任意工具是否被附加了 amend = 1 (可以没有，等同于 0)，如果是，把这个参数过滤掉。
2. 在消息投影钩子中，用一个栈扫描所有消息并处理。对每个消息，先弹掉 amend 个工具调用（中间夹杂的 user/assistant/reasoning 什么的都不要了），再进栈。最后就是 filtered 消息，作为投影。
3. 请你仔细思考这个新机制和旧的投影们的先后次序和相互影响，用最合理的方式作用。
