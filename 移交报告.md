建议叫 **Operator（终端操作员）**。

相比 `Executor` 或 `Runner`，`Operator` 更符合这个角色的职责：它不负责规划和管理任务，而是接受 Manager 的指令，专门操作 PTY、运行命令并返回结果。

### 推荐定义

* **角色名：** `Operator`
* **对外名称：** Terminal Operator
* **内部标识：** `operator`
* **核心能力：** 独占 `pty_executor`
* **职责边界：** 执行，不决策；操作，不管理

调用关系可以变成：

```text
Manager
  ├── 委派代码修改 → Coder
  ├── 委派代码调查 → Inspector
  └── 委派终端操作 → Operator
```

Operator 可以负责：

* 执行构建、测试、Lint 和类型检查
* 启动、观察和终止长时间运行的进程
* 操作交互式 CLI
* 管理 PTY session
* 收集 stdout、stderr 和退出码
* 对超长输出进行截断或摘要
* 将结构化执行结果返回给 Manager

不建议让它负责：

* 自主拆解任务
* 决定产品或代码方案
* 判断任务是否完成
* 直接承担 Reviewer 职责
* 无明确委派地修改代码

可以在角色说明中写成：

```text
Operator is the only role allowed to use the PTY executor.

It performs terminal operations requested by another agent, manages
interactive and long-running processes, and returns structured execution
results. It does not plan projects, make architectural decisions, or declare
tasks complete.
```

几个备选名称的区别：

| 名称                | 评价                                           |
| ----------------- | -------------------------------------------- |
| **Operator**      | 最推荐，适合交互式 PTY、进程和运行环境管理                      |
| Terminal Operator | 最明确，但作为代码中的角色名稍长                             |
| Executor          | 直观，但容易和 `pty_executor`、执行引擎或现有 Executor 概念混淆 |
| Runner            | 简洁，但更像 CI Runner，只适合一次性运行命令                  |
| Shell Agent       | 容易理解，但 PTY 能力不一定只限于 Shell                    |
| Process Operator  | 强调进程管理，但弱化了命令和终端操作                           |
| DevOps Agent      | 范围过大，会让人误以为负责部署、基础设施和运维决策                    |

因此，最终建议是：

> **代码和架构中使用 `Operator`，产品文档中首次出现时写作 `Operator（Terminal Operator）`。**

这样权限模型也会非常清楚：

```text
Manager thinks and delegates.
Coder edits.
Operator executes.
Reviewer verifies.
```
