# 从当前状态到 0.4.0 最终版的完整路线图

## 一、当前状态判断

当前代码版本已经统一到 **`0.4.0-rc.5`**。rc.5 在 rc.4 已通过门禁的基础上，新增了：

* 全角色 system prompt；
* session-wide 正式 assistant A 累积；
* `join.finalText` 改用 session-wide A；
* A/B 语义明确为完整会话正式输出与 Companion 工作记录。

但 rc.5 文档明确标记为 **Release Candidate，不是最终 0.4.0**，并要求在这个版本切点重新完成干净环境的 `npm ci + test:release` 三轮门禁。

rc.4 曾经通过完整干净门禁，包括 276 个 tests-next 测试、29 个 TestKit gate、17 个 canary 连续三轮以及打包安装验证；不过 rc.4 已被 rc.5 取代，这些证据只能作为基线，不能直接证明 rc.5。

从仓库目录看，目前存在 rc.3、rc.4 的证据目录，但没有 `docs/evidence/0.4.0-rc.5/`。因此最合理的判断是：

> **rc.5 已完成代码切点，但尚未被正式 seal 为一个有完整发布证据的候选版本。**

仓库的产品语义和发布阻断条件已经基本冻结，包括 Prompt Authority、Logical Run Fallback、Companion Authority eligibility、双 PERFECT Review、PTY 和 Orchestrator 发布约束。

---

## 二、正式推进前必须解决的两个口径冲突

### 1. A/A/B/B 真实请求证据是否阻断发布

仓库文档存在不完全一致：

* RC 与 CHANGELOG 将“真实 provider request trace”称为可选观察项；
* `E2E_RELEASE_GUIDE.md` 明确写成发行阻断：必须证明同一 Logical Run 内真实请求严格为 `A → A → B → B`，且没有第五次请求。

建议采用更严格口径：

> **在最终 0.4.0 中，将 provider-visible A/A/B/B 请求轨迹视为阻断项。**

Mock canary 只能证明插件内部决策；真正需要证明的是下一次实际 provider request 使用了正确的 EffectiveModel。

完成后应统一修改：

* `docs/E2E_RELEASE_GUIDE.md`
* `CHANGELOG.md`
* `MIGRATION.md`
* 最终发布说明

避免一处写“可选”、另一处写“No-Go”。

### 2. 最终版是私有交付还是公开 npm 发布

目前三个 package manifest 都保持：

```json
{
  "private": true,
  "license": "SEE LICENSE IN LICENSE"
}
```

许可证是临时商业许可证，明确禁止未经书面商业协议的复制、分发或公开发布。

因此必须在最终切版前选定一条路线：

| 路线           | 最终处理                                               |
| ------------ | -------------------------------------------------- |
| 私有 0.4.0     | 保持 `private: true`，生成并交付 tarball，不执行公开 npm publish |
| 公开 npm 0.4.0 | 先确定正式许可证和商业授权，再改为 `private: false` 并完成公开发布审查       |

在没有新的许可证决定时，路线图默认采用 **私有最终版**。

---

# 三、阶段 0：冻结 0.4.0 范围

## 工作

1. 将 rc.5 当前功能定义为 0.4.0 的功能冻结线。
2. 不再加入新角色、新工具、新持久化事实或新 Host 协议。
3. `future/FEATURE.md` 中的 Prefetcher、Enforcer、Sidecar Supervisor 等设计继续留在未来版本，不进入 0.4.0。
4. 允许的代码变化仅包括：

   * 阻断缺陷修复；
   * 测试和诊断补强；
   * 发布脚本修复；
   * 文档与版本同步；
   * 不改变产品语义的确定性修复。

## 退出条件

* 建立一份明确的 `0.4.0 Scope Freeze` 清单；
* 所有未完成想法被归类为：

  * 0.4.0 阻断；
  * 0.4.1；
  * 未来架构；
* 不存在“顺手加入”的新功能。

---

# 四、阶段 1：封存 rc.5

## 1. 确认版本和源树

确认以下三个文件均为 `0.4.0-rc.5`：

```text
package.json
build-package.json
next/package.json
```

同时确认：

* `CHANGELOG.md` 顶部为 rc.5；
* `docs/RC-0.4.0-rc.5.md` 与实际代码一致；
* Git 工作树无 tracked 修改；
* rc.5 对应一个不可变 commit SHA。

## 2. 在全新 checkout 执行门禁

不得使用现有 `build/`、`node_modules/` 或旧测试产物：

```bash
git clean -xfd
npm ci
npm run test:release
```

`test:release` 当前覆盖：

```text
npm run build
npm run test:compile
npm run test:next
npm run test:manager-tools
node testkit/opencode/tests/gate-testkit.mjs
CANARY_REPEAT=3 node scripts/run-canary-staggered.mjs
```

三轮 canary 必须全部通过；任意一轮、任意一个 scenario 失败，整次 rc.5 gate 即失败。

## 3. 打包验证

```bash
npm pack --dry-run
npm pack ./build
```

然后在空目录验证：

```bash
mkdir -p /tmp/wanxiangshu-rc5-install
cd /tmp/wanxiangshu-rc5-install
npm init -y
npm install /absolute/path/to/wanxiangshu-0.4.0-rc.5.tgz
node -e "import('wanxiangshu')"
```

还应检查：

* tarball 内版本确为 `0.4.0-rc.5`；
* `LICENSE` 存在；
* `next/prompts/*.md` 全部进入包；
* 入口为 `next/OpenCode/Plugin.js`；
* 没有源仓库临时文件、测试 workspace、journal 或凭据。

## 4. 建立 rc.5 证据目录

建议固定为：

```text
docs/evidence/0.4.0-rc.5/
├── ENV.txt
├── test-release.txt
├── canary-3round-clean.txt
├── npm-pack-dry-run.txt
├── npm-pack.txt
├── install-out.txt
├── import-out.txt
├── TARBALL.sha256
└── RELEASE-MATRIX.md
```

`ENV.txt` 至少记录：

* commit SHA；
* 操作系统；
* Node、npm、.NET、Fable、OpenCode 版本；
* A/B/Blogger 模型配置；
* canary repeat 数；
* seed；
  -执行时间；
* checkout 是否干净。

## 退出条件

rc.5 只有同时满足以下条件才算被 seal：

* 全新 checkout；
* `npm ci` 成功；
* `test:release` 全绿；
* 三轮 canary 全绿；
* 打包成功；
* 空目录安装和 import 成功；
* tarball hash 已记录；
* 所有证据进入仓库。

如果需要修改生产代码，不应继续复用已经发布或打标签的 rc.5，应修复后切 **rc.6**。

---

# 五、阶段 2：逐项关闭最终版发行阻断

仅有“套件全绿”还不够，需要把测试结果映射到冻结语义。

## 2.1 Prompt Authority 与 Logical Run

必须证明：

* Human Root 可以改变 agent/model/variant；
* repair、Guard、confirmation、busy nudge、provider retry 等 continuation 不能改变 Authority；
* UnknownOrigin fail-closed；
* 所有插件 user-shaped prompt 都通过 `PromptAuthorityService`／`PromptDispatcher`；
* AgentOwnerRoot 使用 claim → send → accepted 两阶段协议；
* restart 后可恢复 profile、Logical Run、claims、repair 和 fallback root。

以下任一情况都是 No-Go：

```text
零宽 repair 更新 LastAuthority
synthetic 成为 Fallback root
synthetic 重置 repair 预算
UnknownOrigin 被当作 Human
Continuation 改写 BaseModel
模块绕过 PromptDispatcher
Companion 从 sessionRoles 推断 eligibility
```

这些阻断项已经在工程蓝图中明确列出。

## 2.2 Fallback A/A/B/B

必须取得 provider request 级别的直接证据：

```text
同一 Logical Run：
request 1 → A
retry 1
request 2 → A
retry 2
request 3 → B
retry 3
request 4 → B
retry 4
不再产生 request 5
```

随后证明：

```text
新 Human Root，省略 model
→ 继承 LastAuthorityProfile.BaseModel
→ 新 epoch
→ Failures=0
→ Side=A
```

还要证明：

* B retry 不写回 LastAuthority；
* continuation 的 physical user message ID 不替换 AuthorityRootUserMessageId；
* cancel、abort、空输出和 XML-only repair 不计 provider failure；
* duplicate retry event 不重复计数。

## 2.3 Companion 和前缀缓存

必须比较真正送到 provider 的字节，而不是 Host metadata：

* eligibility 只读取 `ActiveLogicalRun.Profile.Agent`；
* `LatestB` 增长不修改当前 `FrozenB`；
* 同一 epoch 的 `companion-b-head` ID、内容、顺序逐字节不变；
* replacement 仅覆盖 `LastSuccessfulProjection` 已证明覆盖的完整 semantic turn；
* Blogger 未覆盖的 raw tail 必须保留；
* Blogger busy/failed 不阻塞主 session；
* restart 后恢复同一个 Blogger；
* 外来或旧 epoch synthetic B 不得被错误当作幂等命中。

## 2.4 Review witness

必须保留完整证据链：

```text
ManagerJob
→ Reviewer session
→ review barrier
→ 当前 Git tree
→ ProviderRunIdentity 1
→ PERFECT tool call 1
→ confirmation prompt physical message ID
→ ProviderRunIdentity 2
→ 第二 root user message ID
→ PERFECT tool call 2
```

同时验证：

* 同一 assistant message 两次 PERFECT 无效；
* 没有 confirmation prompt 的第二次 PERFECT 无效；
* REVISE 立即打断确认链；
* tree 变化后清零；
* rebase 后即使 tree hash 相同，也必须建立新 barrier 并重新双 PERFECT；
* 缺 ProviderRunId 时 fail-closed。

## 2.5 Inspector、Executor 和取消传播

必须证明：

* Inspector 工具集合严格等于 `{read, glob, grep, executor}`；
* Inspector 与 Coder 使用同一 worktree；
* 两个并行 Inspector 的结果按输入顺序返回；
* 不污染 Manager completion mailbox；
* 父取消后等待 child abort；
* 没有 session、listener、进程残留；
* Executor 大输出失败时仍返回 partial summary 和最后 raw tail；
* 巨大但有限的 estimate 不发生整数或时间跨度溢出。

## 2.6 PTY

必须覆盖全部 signal：

```text
TERM KILL INT HUP QUIT USR1 USR2
```

并证明：

* 只有 DevOps 可见 `fork-pty`；
* Manager 不拥有 PTY 或 executor；
* Signal 和 Close 不提前产生 completion；
* completion 仅来自 backend `onExit`；
* TERM 五秒 grace 后才 KILL；
* Close 是 stdin EOF；
* read 返回 unread delta；
* UTF-8 半字符跨 read 不损坏；
* parent abort 后 PID、PTY handle、port 均收敛。

角色和工具面属于冻结产品语义。

## 2.7 Orchestrator 发布链

必须证明：

```text
clean gate
→ parallel Manager worktrees
→ serial publish lock
→ current-tree review
→ rebase
→ new review barrier
→ two new PERFECT
→ ff-only
→ durable Published fact
→ cleanup
```

需要注入并恢复以下崩溃点：

* candidate 建立前后；
* pre-review 前后；
* rebase 前后；
* conflict 处理前后；
* post-review 前后；
* ff 前后；
* Published fact 前后；
* worktree 和 branch cleanup 前后。

每个 scenario 结束后必须没有：

```text
child PID
监听端口
SSE 连接
pending request
pending session
PTY
spool
worktree
manager branch
rebase state
publish lock
```

---

# 六、阶段 3：RC 观察期

源文件没有规定观察期必须是几天，因此不应随意发明固定天数。建议采用**事件驱动退出条件**。

## 观察范围

在受控真实环境中运行 rc.5 或后续 rc：

* 普通 Coder 工作；
* Manager 多 child fork/join；
* DevOps PTY 长运行任务；
* Reviewer REVISE 与双 PERFECT；
* provider retry；
* OpenCode 重启；
* Orchestrator 多 worktree 发布；
* Companion context replacement；
* user cancel 和 parent abort。

## 观察指标

必须没有：

* Authority 错配；
* Logical Run 串线；
* duplicate completion；
* join 永久挂起；
* provider 第五次请求；
* Blogger 修改冻结前缀；
* Review witness 错绑；
* 重复 ff；
* Git fail-open；
* PID、PTY、端口、worktree 或 lock 泄漏；
* restart 后把未证明 Busy 的 child 当作仍在运行；
* package 安装后缺失 prompt 或运行资产。

## 观察期变更规则

* 文档或证据变更：可留在同一 RC；
* 测试补强但不改生产行为：重新执行完整 gate；
* 任何生产代码变更：切新 RC；
* 任何冻结语义变更：重新开始观察期；
* 不允许在观察期累积“最后一起测”的变更包。

## 退出条件

* 一个不可变 RC commit；
* 完整 release gate 全绿；
* 真实观察没有未关闭的 P0/P1；
* 所有 P2 均有明确接受、延期或修复结论；
* 最终版不再需要任何生产代码变更。

---

# 七、阶段 4：生成最终 0.4.0 切版提交

最终切版应从已经完成观察的 RC commit 创建，原则上只包含版本和发布文档变化。

## 1. 修改版本

将以下三个文件同步改为：

```text
package.json        → 0.4.0
build-package.json  → 0.4.0
next/package.json   → 0.4.0
```

检查整个仓库，确保不存在仍会进入构建或包产物的 `0.4.0-rc.x`。

## 2. 更新文档

`CHANGELOG.md` 顶部新增正式章节：

```text
## 0.4.0

- 最终功能范围
- 与 rc.5/rc.6 的差异
- 已完成的发布门禁
- 已知限制
- 许可证和分发方式
- 迁移说明
```

同时更新：

* `MIGRATION.md`
* `README.md`
* 最终 release note
* 新建 `docs/RELEASE-0.4.0.md`
* 删除或修正“当前版本为 rc.5”的文字

## 3. 确定发布 manifest

### 私有最终版

保持：

```json
{
  "private": true,
  "license": "SEE LICENSE IN LICENSE"
}
```

这种情况下：

* 可以生成 `wanxiangshu-0.4.0.tgz`；
* 不得公开发布到 npm；
* 分发必须符合书面商业协议。

### 公开最终版

只有在许可证问题解决后才能：

```json
{
  "private": false,
  "license": "<正式许可证标识>"
}
```

并确保：

* tarball 内许可证与 manifest 一致；
* `publishConfig` 与目标 registry 一致；
* 不包含专有或禁止分发的资产。

---

# 八、阶段 5：在最终版本号上执行第二次完整门禁

这是最终版和 RC 之间最重要的发布边界。

必须在新的干净 checkout 上执行，不能复用 rc.5 的：

* `build/`
* `node_modules/`
* tarball
* 测试日志
* install 目录

## 命令

```bash
git clean -xfd
npm ci
npm run test:release
npm pack --dry-run
npm pack ./build
```

空目录安装：

```bash
mkdir -p /tmp/wanxiangshu-040-install
cd /tmp/wanxiangshu-040-install
npm init -y
npm install /absolute/path/to/wanxiangshu-0.4.0.tgz
node -e "
  import('wanxiangshu').then(() => {
    console.log('wanxiangshu 0.4.0 import ok')
  })
"
```

## 最终证据目录

```text
docs/evidence/0.4.0/
├── ENV.txt
├── COMMIT.txt
├── test-release.txt
├── canary-3round-clean.txt
├── provider-aabb-trace.txt
├── review-witness.txt
├── orchestrator-recovery.txt
├── cleanup-checks.txt
├── npm-pack-dry-run.txt
├── npm-pack.txt
├── install-out.txt
├── import-out.txt
├── PACKAGE-CONTENTS.txt
├── TARBALL.sha256
└── RELEASE-MATRIX.md
```

## 最终门禁要求

* 所有测试零失败；
* 没有意外减少测试数量；
* 17 个 canary 连续三轮通过；
* 所有 No-Go 场景有直接 Host 证据；
* tarball 版本为 `0.4.0`；
* tarball hash 固定；
* 空目录安装和 import 通过；
* package 内容和许可证检查通过；
* Git 工作树仍然干净。

---

# 九、阶段 6：正式发布

## 私有发布流程

1. 将最终证据提交到同一 release commit，或建立紧随其后的 evidence-only commit；
2. 确保发布 tag 指向通过最终门禁的准确 commit；
3. 创建：

```text
v0.4.0
```

4. 保存：

   * commit SHA；
   * tag SHA；
   * tarball；
   * SHA-256；
   * release notes；
   * 完整证据目录；
5. 仅通过授权的私有渠道交付 tarball。

## 公开 npm 流程

除上述步骤外，还必须：

* 完成许可证和商业授权；
* 将发布 manifest 改为 `private: false`；
* 在 staging registry 先验证；
* 确认 npm 包安装后的版本、入口和资产；
* 再执行正式 publish；
* publish 后重新从 registry 安装并 import 验证。

当前许可证状态下，不应执行公开发布。

---

# 十、最终 Go / No-Go 清单

只有下面所有项目都是“是”，才能宣布 0.4.0 final：

* [ ] 0.4.0 范围已冻结，future 功能未混入；
* [ ] rc.5 或后续 RC 已完成独立干净门禁；
* [ ] RC 观察期完成；
* [ ] Prompt Authority 所有阻断场景通过；
* [ ] provider request 级别 A/A/B/B 已证明；
* [ ] Companion 前缀逐字节稳定；
* [ ] Review confirmation 完整绑定 physical message 和 ProviderRunIdentity；
* [ ] Inspector、Executor、PTY 的资源和权限边界通过；
* [ ] Orchestrator crash recovery 和 ff-only 发布通过；
* [ ] dispose 后不存在进程、端口、session、PTY、worktree 或 lock 泄漏；
* [ ] 三个 manifest 都是 `0.4.0`；
* [ ] 最终版本号上重新完成三轮门禁；
* [ ] `wanxiangshu-0.4.0.tgz` 空目录安装和 import 成功；
* [ ] 最终证据目录完整；
* [ ] 私有或公开分发政策已经明确；
* [ ] tag 精确指向通过门禁的 commit；
* [ ] 没有发布后再修改同一 0.4.0 产物。

---

## 推荐的最短主线

```text
冻结范围
→ 统一发布阻断口径
→ clean checkout 封存 rc.5
→ 补齐 rc.5 evidence
→ 关闭 A/A/B/B 等直接证据缺口
→ 受控 RC 观察
→ 有生产修复则切 rc.6 并重跑
→ 选定最终 RC
→ 仅修改版本和发布文档为 0.4.0
→ 新 checkout 第二次完整门禁
→ pack + 空目录安装
→ 固化 0.4.0 evidence
→ tag v0.4.0
→ 按许可证选择私有交付或公开发布
```

这里真正的近期关键路径不是继续开发功能，而是：**先把 rc.5 变成有完整证据的不可变 RC，再消除发布口径冲突，最后在真实 `0.4.0` 版本号上进行第二次独立门禁。**
