# 移交报告 — Release Remediation WIP

| Field | Value |
|---|---|
| **Date** | 2026-07-28 |
| **Branch** | `master` |
| **Commit** | `6d751620` — *WIP: release remediation — prompt authority, companion/blogger, review content proof* |
| **Remote** | `origin/master`（已 push） |
| **Release 结论** | **No-Go**（未宣称 RC/final） |

本报告描述当前残局、已落盘进展、设计决策、未完成阻塞与建议下一步。接手者应从本文件 + `AGENTS.md` / `next/Doc/SSOT.md` 继续，勿依赖会话记忆。

---

## 1. 目标与完成定义

Release remediation 的目标是：在**不修改 OpenCode 本体**的前提下，把 KISS 新架构做到可 release 的证据链：

1. SSOT / AGENTS 与实现一致
2. 编译 + unit/Fable tests 全绿
3. P0 canary 全绿（含 event-stagger release gate）
4. Prompt Authority 与 continuation 不污染 human root
5. Companion / Blogger / Epoch / self-rebase 正确
6. Review 双 PERFECT 因果确认
7. Orchestrator worktree / rebase / post-rebase review / FF
8. Fallback A/A/B/B 有 Host 契约可证明
9. 打包 / clean install 可复现

**当前未达成 3、6（部分）、7、8。**

---

## 2. 已落盘进展（`6d751620`）

### 2.1 Prompt Authority

- 新增：
  - `next/OpenCode/PromptAuthority.fs`
  - `next/OpenCode/PromptDispatcher.fs`
  - `next/Journal/AgentFacts.Authority.fs`
  - 相关 tests：`tests-next/OpenCode/PromptAuthorityTests.fs`、`tests-next/Journal/PromptAuthorityFactTests.fs`
- 插件 continuation 经 dispatcher：`PluginPromptClaimed` → 带 metadata 发送 → `PluginPromptAccepted` / `Abandoned`
- Unknown origin 不得冒充 HumanRoot
- Zero-width interaction repair 也走 continuation 路径
- SSOT / AGENTS 已写入 authority 规范

### 2.2 Companion / Blogger

根因与修复：

| 问题 | 修复 |
|---|---|
| Blogger child 未 own/bind → idle reconcile 不 NotifyTerminal → blog Submit 永不完成 → `LatestB` 为空 | 创建 Blogger 时 `RegisterOwned` + `BindActiveRun`；`AgentRole.Blogger` |
| Terminal 先 notify 再 record 输出 → listener 看到空 A | `TerminalPolicies` 在 `NotifyTerminal` 前 `recordOutput` |
| Self-rebase 在 `TransformRaw`/Submit 之后检查 → 非空 delta 永远 busy skip | Self-rebase 评估移到 delta submit **之前** |
| Sticky 32k model budget 压过 fixture 1k override | `bloggerBudgetForPrimary = min(sticky, override)`；canary env 默认 `WANXIANGSHU_BLOGGER_MODEL` / context limit |

相关文件：

- `next/OpenCode/CompanionTransform.fs`
- `next/OpenCode/CompanionTransformHelpers.fs`
- `next/OpenCode/SpikePlugin.fs` / `SpikePluginHelpers.fs`
- `next/Session/CompanionHostBlogger.fs`
- `next/Session/ForkRuntime.fs`（`AgentRole.Blogger`）
- `testkit/opencode/scenario-parallel.js`
- `testkit/opencode/tests/companion-replacement-canary.mjs`

### 2.3 Review 双 PERFECT（内容因果，非 message id）

用户明确决策：**剧本与确认不得依赖 host message id，应完全基于消息内容。**

已开始/部分落盘的迁移：

| 旧 | 新 |
|---|---|
| `ConfirmationPromptMessageId` | `ConfirmationPromptMarker`（内容标记） |
| `ReviewVerdictRecorded.RootUserMessageId` | `ReviewVerdictRecorded.UserPromptText` |
| 二次 PERFECT：`rootId == confirmId` | 二次 PERFECT：user prompt **包含** `"PERFECT requires confirmation"` |

核心文件：

- `next/Journal/AgentFacts.Types.fs`
- `next/Journal/AgentFacts.Review.fs`
- `next/Kernel/Fact.fs`
- `next/Session/ReviewerHost.fs`
- `next/Review/Guard.fs`
- `next/OpenCode/VerdictSurface.fs`（从 `ctx.message.parts` 取 prompt 文本）
- `next/OpenCode/HostReviewGuard.fs`（confirm path 仍发带 marker 的确认文案）
- `next/OpenCode/ReviewerGuardState.fs`（pending confirmation 查询抽出）

**注意：** commit 时该迁移可能尚未完成全仓编译/测试清理；接手后**先 `npm run build && npm run test:compile`**，按编译错误把剩余 `RootUserMessageId` / `ConfirmationPromptMessageId` 调用点改完。

### 2.4 Orchestrator review owner

- `emitReviewBarrier` / `OrchestratorReviewRead` 的 session 必须是 **review owner**（当前 OrchestratorHost 直接 fork reviewer 时 = orchestrator session；Manager 自有 reviewer 时 = manager session）
- 引入 `ReviewState = Confirmed | PendingConfirmation | NeedsReview | RevisionRequired`，避免 pending confirmation 时再 fork 一轮完整 review 抢剧本

文件：

- `next/OpenCode/OrchestratorHost.fs`
- `next/OpenCode/OrchestratorManagerJob.fs`
- `next/OpenCode/OrchestratorReviewRead.fs`

### 2.5 其它

- `LICENSE`（provisional commercial）
- 测试 runner / canary driver 小改动
- 多份 canary fixture 调整了 confirmation 轮次（仍未完全稳定）

---

## 3. 验证证据（commit 前最后一轮）

| 检查 | 结果 |
|---|---|
| `npm run build` / `test:compile` | 内容迁移前曾绿；**迁移后需重验** |
| `npm run test:next` | 曾 **269 passed**（内容迁移前） |
| `companion-canary` | 绿 |
| `companion-replacement-canary` | 绿 |
| `inspector-oneshot-canary` | 绿 |
| `reviewer-verdict-canary` | 绿（内容迁移前） |
| `host-restart-canary` | 绿（内容迁移前） |
| `orchestrator-canary` / publish / restart-publish | **红** |
| `fallback-canary` | **红** / Host 契约阻塞 |
| `npm run test:release` | **No-Go** |

---

## 4. 仍打开的阻塞

### 4.1 Orchestrator confirmation fixtures（P0 bug / 剧本）

现象历史：

1. 第一对 PERFECT 后可能发出第二轮 full review prompt，与仅剩 confirmation / next-perfect 的剧本头冲突 → `no-lane-head-matched`
2. 中间 `*-text` 轮若只 `requiredTools: verdict` 且无 user 约束，会**抢** HostReviewGuard 的 confirmation 请求
3. Review owner / barrier / GuardPromptAccepted 写错 session 时，二次 PERFECT 永不 `IsConfirmed`

用户约束：

> **剧本应完全基于消息内容，不要和 host message id 打交道。**

因此 fixture 与匹配应：

- 用 `match.user` / 正文片段（如 `PERFECT requires confirmation`、`Review the current worktree`）
- **不要**用 physical message id / accepted-id 做因果
- 确认轮与 tool-followup 文本轮要严格区分 content

建议剧本形状（每个 barrier）：

```text
1. tool-call verdict(PERFECT)          // 无 confirmation 文案
2. text after tool                     // 可选：tool-followup 文本（勿匹配 confirmation user）
3. tool-call verdict(PERFECT)          // user 必须包含 "PERFECT requires confirmation"
4. text terminal（可选）
```

相关 fixture：

- `testkit/opencode/scripts/orchestrator.json`
- `testkit/opencode/scripts/orchestrator-publish.json`
- `testkit/opencode/scripts/orchestrator-restart-publish.json`
- 对照已通过的：`testkit/opencode/scripts/reviewer-verdict.json`

### 4.2 Fallback Host contract（能力阻塞，非本地能硬补）

Host 源码证据（`next/Doc/host-docs/04.md` / `06.md`）：

- native retry 走内部 `Effect.retry`
- 不建新 user message
- 不调用 `setAgentModel`
- 复用同一 assistant message

因此插件层**无法保证** provider attempt #2 切 B、#4 后禁发 request。  
`docs/E2E_RELEASE_GUIDE.md` 已要求 Host-contract Spike；`fallback-canary` 不能假绿放行。

需要上游之一：

1. retry 前 model resolution hook；或
2. SDK retry policy / model-selection API；或
3. 正式声明产品语义降级并改 SSOT

### 4.3 内容确认迁移的收尾

`6d751620` 已改类型与 fold，但可能仍有：

- tests 仍传 `RootUserMessageId`
- 某处仍读 `ConfirmationPromptMessageId`
- `VerdictSurface` 在真实 Host 上能否读到 user prompt parts 需 canary 验证

接手第一步：

```bash
npm run build && npm run test:compile && npm run test:next
```

按错误清干净后，再跑：

```bash
node testkit/opencode/tests/reviewer-verdict-canary.mjs
node testkit/opencode/tests/orchestrator-canary.mjs
```

---

## 5. 设计决策（不要回退）

1. **确认因果 = 内容标记**，不是 message id 相等。
2. **Continuation 不创建 Authority Root**；human root 只在 `chat.message` 真人接受时建立。
3. **Blogger 是 owned child**；无 terminal 即无 B。
4. **Self-rebase 在 Submit 之前**评估 LatestB。
5. **PendingConfirmation 时 Orchestrator 不得再 fork 完整 review** 与 HostReviewGuard 抢确认。
6. **不修改 OpenCode 本体**；Fallback 无契约则 No-Go，不伪造实现。
7. **Release 以 `npm run test:release` 为准**，局部 unit 绿不够。

---

## 6. 关键文件地图

```text
规范
  AGENTS.md
  next/Doc/SSOT.md
  docs/E2E_RELEASE_GUIDE.md
  next/Doc/host-docs/04.md, 06.md

Authority
  next/OpenCode/PromptAuthority.fs
  next/OpenCode/PromptDispatcher.fs
  next/Journal/AgentFacts.Authority.fs

Companion
  next/OpenCode/CompanionTransform.fs
  next/Session/CompanionHostBlogger.fs

Review
  next/Journal/AgentFacts.Review.fs
  next/OpenCode/HostReviewGuard.fs
  next/OpenCode/VerdictSurface.fs
  next/OpenCode/TerminalPolicies.fs
  next/OpenCode/ReviewerGuardState.fs

Orchestrator
  next/OpenCode/OrchestratorHost.fs
  next/OpenCode/OrchestratorReviewRead.fs
  next/OpenCode/OrchestratorManagerJob.fs

Canaries
  testkit/opencode/scripts/*.json
  testkit/opencode/tests/*-canary.mjs
  scripts/run-canary-staggered.mjs
```

---

## 7. 建议接手顺序

1. **编译/测试 baseline**  
   `npm run build && npm run test:compile && npm run test:next`  
   完成 UserPromptText / ConfirmationPromptMarker 全仓收尾。

2. **固定 reviewer 内容剧本**  
   先保证 `reviewer-verdict-canary` 在内容确认下仍绿。

3. **对齐 orchestrator 真实请求序列**  
   - 临时 request 顺序（或 `CANARY_DEBUG_FACTS=1`）  
   - fixture **只按 content 匹配**  
   - 消灭 mid-confirmation 的二次 full review，或给它单独 content 剧本

4. **Fallback**  
   保持 Host-contract Spike 为 release gate；无上游契约不宣称完成。

5. **`npm run test:release` 三轮**  
   全绿才可考虑 RC。

---

## 8. 明确不要做的事

- 不要用 `accepted-<session>` / host message id 当二次 PERFECT 因果证明
- 不要为了假绿把 confirmation 改成“任意两次 PERFECT”
- 不要在插件内用假 model-switch 宣称 A/A/B/B 已实现
- 不要改 OpenCode 本体绕过 “不修改宿主” 约束（除非产品明确改约束）
- 不要把 WIP 标成 release complete

---

## 9. 一句话状态

> Prompt Authority、Companion/Blogger、Review 内容确认方向已落盘并 push；Orchestrator 确认剧本与 Fallback Host 契约仍阻塞；release 仍为 **No-Go**。接手请先编译清内容迁移，再按 content-only 剧本修 orchestrator canary。
