建议叫 **DevOps（终端操作员）** 管理 pty 和 executor，进一步减轻 manager 负担。

inspector 继续被允许直接用 executor 执行只读终端操作。但 manager 的终端权限没收。

相比 `Executor` 或 `Runner`，`DevOps` 更符合这个角色的职责：它不负责规划和管理任务，而是接受 Manager 的指令，专门操作 PTY、运行命令并返回结果。

### 推荐定义

* **角色名：** `DevOps`
* **核心能力：** 异步委派 `pty` 并可同步委派 `executor`
* **职责边界：** 执行，不决策；操作，不管理

调用关系可以变成：

```text
Manager
  ├── 委派代码修改 → Coder
  ├── 委派代码调查 → Inspector
  └── 委派终端操作 → DevOps
```

DevOps 可以负责：

* 执行构建、测试、Lint 和类型检查
* 启动、观察和终止长时间运行的进程
* 操作交互式 CLI
* 管理 PTY session

不建议让它负责：

* 自主拆解任务
* 决定产品或代码方案
* 判断任务是否完成
* 直接承担 Reviewer 职责
* 无明确委派地修改代码

可以在角色说明中写成：

```text
DevOps is the only role allowed to use the PTY or executor.

It performs terminal operations requested by another agent, manages
interactive and long-running processes. It does not plan projects, make architectural decisions, or declare tasks complete.
```

Manager thinks and delegates.
Coder edits.
DevOps executes.
Reviewer verifies.
