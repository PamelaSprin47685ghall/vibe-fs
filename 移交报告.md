# Nudge 子系统重构移交报告

## 当前状态
- **构建**: 已通过 Fable 编译
- **测试**: 14241 通过 / 15 失败
- **主要阻塞**: OmpPluginTestsAgentEnd 抛出数组越界异常，其余为行为回归/架构测试失败

## 已完成
- 移除旧 facade 模块：Coordinator.fs（已从磁盘和 fsproj 删除）
- 清理旧测试：NudgeEventHandlerTests.fs、NudgeTransitionsTests.fs、NudgeCoordinatorTests.fs
- 重写 Registry.fs 为最小导出（仅类型别名 + emptyState）
- 清理 SessionLifecycleObserver.fs：移除 holder、NudgeState 引用
- 清理 OpencodeSessionEventNudge.fs：移除 EventHandler 引用
- 修复 Shell/NudgeRuntime.fs 语法错误，HandleEvent 每个 case 显式 `return ()`
- Omp/NudgeHooks.fs 与 Omp/NudgeRuntime.fs 已迁移到 NudgeDerivation
- Mux/EventHook.fs 已迁移到 NudgeRuntime

## 剩余失败（15）

### 1. OmpPluginTestsAgentEnd.run [THREW]
- 异常: `Index was outside the bounds of the array. Parameter name: index`
- 位置: 疑似 agentEndHandler 调用链（entries/decodeEntries/extractHistoryTexts/reviewTaskFromTexts/front-matter 解析）
- 状态: 正在定位根因，需要完整 JS stack trace

### 2-4. Mux fresh assistant / reviewer reject / wip 行为回归
- 测试:
  - `fresh assistant output re-allows todo nudge`
  - `reviewer reject reopens loop nudge on fresh assistant output`
  - `wip submit_review does not permanently suppress loop nudge`
- 原因: 新 `deriveAction` 仅通过 `lastSentAction` 去重，未实现旧 facade 中的 `freshAssistantSnapshots` / `submitReviewWipAcknowledgment` 重激活逻辑

### 5-8. 架构测试: `src/Shell/NudgeSnapshot.fs must not Dyn.str/get`
- NudgeSnapshot.fs 仍在使用 Dyn 访问 host 对象，违反架构约束
- 需要把快照构建逻辑拆到 host-specific 适配层，核心保持纯函数

### 9. `new assistant turn in history re-arms loop nudge on next idle`
- 同 2-4 的去重/重激活逻辑缺失

### 10-15. tool.definition 相关失败
- 涉及 coder _ui property / intents / todo description / executor program/mode schema
- 看起来与本次 Nudge 重构无直接关系，但需确认是否由相关文件改动引入

## 临时调试改动（需还原）
- `tests/Assert.fs` 已临时加入 `console.error(ex)` 与 stack trace 输出，用于定位 OmpPluginTestsAgentEnd 异常根因
- 定位完成后应还原为简洁的 `[THREW]` 标签

## 下一步建议
1. 还原/精简 Assert.fs 临时改动（定位异常后）
2. 修复 OmpPluginTestsAgentEnd 数组越界（优先）
3. 恢复 fresh assistant / reviewer reject / wip 的重激活行为
4. 把 NudgeSnapshot.fs 中的 Dyn 调用下放到 Mux/Opencode 适配层，通过架构测试
5. 检查 tool.definition 失败是否与本次改动相关
