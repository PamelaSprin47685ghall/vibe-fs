# 大规模保姆级重构指南

用宝典六条第一性原理作为度量尺，对 wanxiangshu 做系统性架构审计与重构。

核心判断：项目已大规模贯彻宝典，非推倒重建。三处精确修正 + 六步迁移切片，每步有编译器+架构探针+行为基线三层守卫。

---

## 目录

1. [宝典符合度评估](#一宝典符合度评估)
2. [偏离点清单](#二偏离点清单按优先级降序)
3. [拒绝清单](#三拒绝清单不做什么)
4. [安全网](#四安全网不可破不变量)
5. [执行方案：六 Phase 迁移](#五执行方案六-phase-迁移)
   - [Phase 1：Domain.fs 时钟注入化](#phase-1domainfs-时钟注入化)
   - [Phase 2：泛型容器归位（Slice 1）](#phase-2泛型容器归位slice-1)
   - [Phase 3：NudgeFlow 骨架收敛（Slice 2）](#phase-3nudgeflow-骨架收敛slice-2)
   - [Phase 4：ReviewLoop 骨架收敛（Slice 3）](#phase-4reviewloop-骨架收敛slice-3)
   - [Phase 5：KG Runtime 状态归一（Slice 4）](#phase-5kg-runtime-状态归一slice-4)
   - [Phase 6：拆分黄牌文件](#phase-6拆分黄牌文件)
6. [验收清单](#六验收清单)

---

## 一、宝典符合度评估

以六条第一性原理为度量尺：

| 原理 | 符合度 | 证据 |
|---|---|---|
| 1. 稳定资产是领域规则非宿主API | B+ | Kernel 状态机（Review/Nudge/KG/Todo）纯函数且无 Dyn 泄漏。但 KG Runtime 三处等价拷贝，领域逻辑泄漏到宿主层 |
| 2. 历史是事实内存是投影 | A- | BacklogProjection/WorkBacklog/replay 机制已落地。NudgeEffect 的 StateHolder 需确认是缓存还是不可恢复真理 |
| 3. 副作用压到边界 | B | Dyn/obj 边界严格经 Shell codec。但 `Domain.fs:100` 读 `DateTimeOffset.UtcNow`——Kernel 内直接读时钟，是真侧效泄漏 |
| 4. LLM边界尽快强类型化 | A | 架构测试 126+ 条目强制全工具链经 Decode+Execute，Hook output 禁裸写 Dyn。执行最彻底 |
| 5. 长会话折叠进度可压缩 | A- | completedWorkReport + BacklogSessionCodec 已落地 |
| 6. 并发本质是共享可变状态 | A | PromiseQueue 串行域 + RuntimeScope 派生实例，SessionExecutor 无模块级队列 |

文件体量铁律已守住（0 个超 300 行），非主要矛盾。

---

## 二、偏离点清单（按优先级降序）

### P0：Kernel 纯度缺口——时钟直读

位置：`src/Kernel/Domain.fs:100`

```
System.DateTimeOffset.UtcNow.ToUnixTimeMilliseconds()
```

这是唯一真正的 Kernel 侧效泄漏。违反原理 3"不读时钟"。后果：Kernel 函数不再同入同出，重放依赖墙钟运气。

架构测试 `kernelBoundary` 只拦 `Dyn.` 和 `open Shell`，未守 `DateTimeOffset`/`UtcNow`，是覆盖盲区。

注意区分：
- `KnowledgeGraph/Id.fs:17` 的 `randomInt` 是注入式参数（签名 `randomInt: unit -> int`），合规
- `Yaml.fs`/`PromptFrontMatter.fs` 的 `jsNative`/`Emit` 是纯值 interop（无 IO），可接受

### P1：三宿主知识重复——同一事实多处拷贝

| 重复项 | 位置 | 行数 | 判定 | 合并风险 |
|---|---|---|---|---|
| `StateHolder<'state>` | Opencode/NudgeEffect.fs:22-28 + Shell/NudgeRuntime.fs:18-24 | 6×2 | 真DRY违反 | 零 |
| KGTestHooks | Opencode(33行) + Mux(46行) | 33+46 | 真DRY违反 | 低 |
| collectSnapshot | Opencode + Shell 两处 | 22×2 | 骨架等价，数据源异 | 中 |
| ReviewLoop 循环 | Opencode + Mux + Omp 三处 | ~30×3 | 骨架等价，IO异 | 中 |
| KG Runtime | 三处各 190+190+159 行 | 539 | 字段同构，构造异 | 高 |

判定标准（宝典）："两段像只说明此刻长得像，不说明同一份知识。"逻辑是否依赖宿主API：
- `StateHolder`/`KGTestHooks`：零宿主耦合，纯泛型容器+测试接口。同一知识，必合并
- `collectSnapshot`/`ReviewLoop`/`KG Runtime`：骨架不依赖宿主，差异仅在 IO 调用点（`session.prompt` vs `pi?sendMessage` vs `delegate`）。骨架是同一知识，IO 是宿主领土。应提取高阶函数，非抽象基类

### P2：黄牌文件（200-300行）

| 文件 | 行数 | 拆分方向 |
|---|---|---|
| `Opencode/HookSchema.fs` | 257 | 按 Hook 类型拆为 HookSchemaCore + HookSchemaDecode |
| `Opencode/PtyTools.fs` | 234 | 按职责拆为 PtySpawn + PtyIo |
| `Omp/SessionLifecycleHooks.fs` | 207 | 按事件类型拆为 NudgeHooks + TodoHooks |
| `Shell/NudgeRuntime.fs` | 201 | 提取 StateHolder 后自然下降 |
| `Opencode/SessionIoSubagent.fs` | 200 | 按子代理生命周期拆 |

非设计溃烂，是局部样板挤占。合并 P1 重复后 NudgeRuntime 会自然瘦身。

---

## 三、拒绝清单（不做什么）

| 拒绝项 | 理由 |
|---|---|
| 给三宿主加抽象基类 | 基类内塞 `if host==X` = 偶然复杂度。用高阶函数组合 |
| 把所有 FFI 移出 Kernel | Yaml 解析是纯计算，移出增 codec 往返。只移真侧效（nowMs） |
| 追求零重复 | 若三宿主差异确实需要分居，强行合并制造耦合 |
| 先拆文件再修架构 | 文件体量非主要矛盾。先修 P0/P1 知识问题，黄牌文件自然瘦身 |
| 大批量自动化改代码 | 手工编辑步步为营。每步编译+测试验证 |

---

## 四、安全网（不可破不变量）

重构过程中，以下不变量由三层机制守卫：

| 守卫层 | 机制 | 破坏症状 |
|---|---|---|
| 物理边界 | F#类型系统 + Fable编译 | 编译断：类型不匹配 / 引用未找到 |
| 架构探针 | 126+ 架构测试条目 | 测试红：边界违规被拦截 |
| 行为基线 | 231+ 端到端测试 | 运行崩 / 逻辑漂移 |

最危险 = 逻辑漂移：测试绿但行为变（Nudge 不触发 / Review 状态错 / KG 数据丢）。防范：每个迁移切片后跑 `npm run build-and-test`，确认全绿。

现有 8 个不变量域：

| 不变量域 | 守卫测试 | 重构期风险 |
|---|---|---|
| Kernel 禁引 Dyn/Shell | `kernelBoundary` | 注入参数时若误传 Shell 类型则破坏纯度 |
| Omp 禁引 Opencode/Mux | `ompBoundary` | 合并代码至 Shell 时易引入反向依赖 |
| Opencode↔Mux 禁互引 | `opencodeNoMuxRef` / `muxNoOpencodeRef` | 同上 |
| Hook output 经 codec | `ArchitectureTestsWireHook*` | 新增回调接口时若绕过 Codec 则破坏 |
| 全工具链经 Decode+Execute | `ArchitectureTestsWireToolExec` | 同上 |
| KG 维护经 Shell | `ArchitectureTestsRuntimeKg` | 移动 StateHolder 时未绑 RuntimeScope 则破坏并发 |
| 单文件 ≤300 行 | `fileBodyUnder300` | 拆分文件时需同步更新路径映射 |
| 测试全绿 | 231+ 测试文件 | 任何操作后必须保持 |

重构期新增纪律：
1. 修改 Kernel 注入参数必守纯函数不碰环境
2. 迁移 StateHolder 至 Shell 必绑定 RuntimeScope 派生实例
3. 新增 Shell 抽象与回调接口必同步补全架构测试

---

## 五、执行方案（六 Phase 迁移）

核心原则：用高阶函数 + record 组合，不造类层级，不造 DI 容器。宝典："GoF 翻到末页只剩数据函数类型组合。"

每个 Phase 的标准流程：
```
Step A：读取涉及文件，确认当前结构
Step B：手工修改代码
Step C：npm run build-and-test（编译+全测）
Step D：确认全绿 → 进入下一 Phase
Step E：若红 → 回滚本步修改 → 重新分析
```

### Phase 1：Domain.fs 时钟注入化

目标：消除 Kernel 内唯一真侧效泄漏，还 Kernel 纯度。

涉及文件：
- `src/Kernel/Domain.fs`（修改）
- `tests/ArchitectureTestsFoundation.fs`（修改：补扫描规则）
- 所有调用 `nowMs` 的上游文件（修改：传入注入参数）

操作步骤：

Step 1：在 `src/Kernel/Domain.fs` 找到 `nowMs` 的定义和使用点（约第 100 行）。确认所有调用链。

Step 2：将 `nowMs` 从直接读取改为函数参数注入：
```
原：let nowMs () = System.DateTimeOffset.UtcNow.ToUnixTimeMilliseconds()
改：从签名中移除，改为调用方传入 getTimestamp: unit -> int64 参数
```

Step 3：沿调用链向上推：每个调用 `nowMs` 的 Kernel 函数，签名增加 `getTimestamp: unit -> int64` 参数。直到到达 Shell 边界。

Step 4：Shell 边界函数创建实际的时间戳获取器：
```
let getTimestamp () = System.DateTimeOffset.UtcNow.ToUnixTimeMilliseconds()
```
在调用 Kernel 函数时传入。

Step 5：在 `tests/ArchitectureTestsFoundation.fs` 的 `kernelBoundary` 测试中，增加对 `DateTimeOffset`/`UtcNow`/`DateTime.Now`/`Date.now` 的扫描，断言 `src/Kernel/*.fs` 不含这些字符串（白名单允许 `Fable.Core` interop）。

Step 6：`npm run build-and-test`，确认全绿。

验收标准：
- `src/Kernel/` 下全局搜索 `UtcNow`/`DateTime.Now`/`Date.now` 零匹配（排除 Fable.Core interop 注释）
- 架构测试 `kernelBoundary` 包含新的时钟扫描断言
- 全测绿

回滚策略：`git checkout` 涉及文件，重新分析调用链。

风险提示：
- nowMs 可能被多个 Kernel 模块间接调用，需完整追踪调用链
- 若调用链过深（>5层），考虑在 Kernel 定义 `Clock` 抽象类型，Shell 创建实例传入，减少参数穿透层数

---

### Phase 2：泛型容器归位（Slice 1）

目标：消除两处零宿主耦合的重复定义。最低风险，零回归。

零风险原因：StateHolder 和 KGTestHooks 是纯泛型容器/测试接口，不依赖任何宿主对象。

#### 2A：StateHolder 归位

涉及文件：
- `src/Shell/SerialStateHolder.fs`（新建）
- `src/Opencode/NudgeEffect.fs`（修改：删除 StateHolder 定义，改为 open）
- `src/Shell/NudgeRuntime.fs`（修改：删除 StateHolder 定义，改为 open）

操作步骤：

Step 1：读取 `src/Opencode/NudgeEffect.fs:22-28` 和 `src/Shell/NudgeRuntime.fs:18-24`，确认两个 `StateHolder<'state>` 定义完全一致（字段、方法、行为）。

Step 2：新建 `src/Shell/SerialStateHolder.fs`，内容为 StateHolder 的单一定义。文件应 <30 行。

Step 3：在 `src/Opencode/NudgeEffect.fs` 中删除 StateHolder 定义，改为 `open Wanxiangshu.Shell.SerialStateHolder`。

Step 4：在 `src/Shell/NudgeRuntime.fs` 中删除 StateHolder 定义，改为 `open Wanxiangshu.Shell.SerialStateHolder`（或直接引用同模块，因 NudgeRuntime 已在 Shell 内）。

Step 5：确认 `src/Shell/SerialStateHolder.fs` 已加入 `.fsproj` 编译项列表（在 NudgeRuntime 和 NudgeEffect 引用之前）。

Step 6：`npm run build-and-test`，确认全绿。

#### 2B：KGTestHooks 归位

涉及文件：
- `src/Shell/KnowledgeGraphTestHooks.fs`（新建）
- `src/Opencode/KnowledgeGraphTestHooks.fs`（修改：删除或瘦身为 thin wrapper）
- `src/Mux/KnowledgeGraphTestHooks.fs`（修改：同上）

操作步骤：

Step 1：读取 `src/Opencode/KnowledgeGraphTestHooks.fs`（33行）和 `src/Mux/KnowledgeGraphTestHooks.fs`（46行），逐行对比。确认 `RegisterJob`/`TakeLaunches`/`WaitJobs` 完全等价，Mux 多 `HasJob`。

Step 2：新建 `src/Shell/KnowledgeGraphTestHooks.fs`，定义泛型测试接口（含 `HasJob`），接受 `KnowledgeGraphRuntime` 类型参数。

Step 3：Opencode 版瘦身为：`type KnowledgeGraphTestHooks = Shell.KnowledgeGraphTestHooks<KnowledgeGraphRuntime>`（如果泛型可行）或 thin wrapper。

Step 4：Mux 版同上。

Step 5：确认 `.fsproj` 编译项顺序正确。

Step 6：`npm run build-and-test`，确认全绿。

#### 2C：新增架构测试守新边界

在 `tests/` 下新建或修改架构测试文件：

断言 1：`src/Opencode/` 和 `src/Mux/` 和 `src/Omp/` 下不允许定义 `type StateHolder`（grep 扫描）。

断言 2：`src/Opencode/` 和 `src/Mux/` 下不允许定义 `KnowledgeGraphTestHooks` 类型（应 open Shell 版）。

Phase 2 验收标准：
- StateHolder 在全项目仅 1 处定义（Shell/SerialStateHolder.fs）
- KGTestHooks 在全项目仅 1 处定义（Shell/KnowledgeGraphTestHooks.fs）
- 新增架构测试全绿
- 净减约 70 行重复代码
- 现有 231+ 测试全绿

---

### Phase 3：NudgeFlow 骨架收敛（Slice 2）

目标：消除 collectSnapshot 两处等价实现，用高阶函数统一 NudgeFlow 骨架。

中风险原因：collectSnapshot 骨架等价但数据源不同（Opencode 从 client API，Shell 从 helpers+getChatHistory）。需定义 SnapshotProvider 接口隔离数据源。

涉及文件：
- `src/Shell/NudgeRuntime.fs`（修改：定义高阶函数）
- `src/Opencode/NudgeEffect.fs`（修改：瘦身为提供闭包+接线）
- `src/Omp/SessionLifecycleHooks.fs`（修改：瘦身为提供闭包+接线）
- `tests/ArchitectureTests*.fs`（修改：新增断言）

操作步骤：

Step 1：读取 `src/Opencode/NudgeEffect.fs:30-52`（collectSnapshot）和 `src/Shell/NudgeRuntime.fs:106-129`（collectSnapshot），逐行对比骨架逻辑。确认差异点仅在：
- 获取 todos 的 API 调用（Opencode: client?getTodos / Shell: helpers.getTodos）
- 获取最后 assistant 消息的 API 调用
- getSessionId 的来源

Step 2：在 `src/Shell/NudgeRuntime.fs` 中定义高阶函数：
```
collectSnapshot (getTodos: unit -> Promise<TodoItem list>)
                (getLastAssistantMessage: unit -> Promise<string option>)
                (sessionId: string)
                : Promise<SessionSnapshot>
```
将骨架逻辑（组装 SessionSnapshot）放入此函数，数据获取经参数注入。

Step 3：在 `src/Shell/NudgeRuntime.fs` 中定义高阶函数：
```
runNudgeFlow (snapshot: SessionSnapshot)
             (deps: NudgeDeps)
             (sendNudge: NudgeContext -> Promise<unit>)
             : Promise<NudgeResult>
```
将 runNudgeFlow 骨架（decideNudge → sendNudge → tryRecordSend）放入此函数，发送逻辑经参数注入。

Step 4：`src/Opencode/NudgeEffect.fs` 瘦身：
- 删除 collectSnapshot 实现
- 删除 runNudgeFlow 实现
- 保留 getTodos 闭包（封装 client API 调用）
- 保留 getLastAssistantMessage 闭包
- 保留 sendNudge 闭包（封装 session.prompt）
- 调用 Shell.NudgeRuntime.collectSnapshot 和 runNudgeFlow

Step 5：`src/Omp/SessionLifecycleHooks.fs` 瘦身：
- 保留 getTodos 闭包（封装 pi API 调用）
- 保留 getLastAssistantMessage 闭包
- 保留 sendNudge 闭包（封装 pi?sendMessage）
- 调用 Shell.NudgeRuntime.collectSnapshot 和 runNudgeFlow

Step 6：新增架构测试断言：
- 禁止 Opencode/Mux/Omp 内联 collectSnapshot 实现（应调用 Shell.NudgeRuntime.collectSnapshot）
- 禁止 Opencode/Mux/Omp 内联 runNudgeFlow 循环骨架

Step 7：`npm run build-and-test`，确认全绿。

验收标准：
- collectSnapshot 仅在 Shell/NudgeRuntime.fs 定义
- runNudgeFlow 骨架仅在 Shell/NudgeRuntime.fs 定义
- 三宿主仅提供闭包+接线，不含骨架实现
- 新增架构测试全绿
- Nudge 行为不变（测试覆盖 Nudge 触发/去重/状态转换）

风险提示：
- SnapshotProvider 接口签名不得泄漏宿主类型（如 Opencode 的 client 对象）。接口参数应为 Kernel 类型（TodoItem list / string option）
- 若发现 Omp 的 nudge 路径与 Opencode/Mux 差异过大（不是 collectSnapshot→decideNudge→sendNudge 的同一流程），则不强行合并，标记 deferred 保持分居

---

### Phase 4：ReviewLoop 骨架收敛（Slice 3）

目标：消除 ReviewLoop 三处等价循环骨架，用高阶函数统一。

中风险原因：三宿主循环骨架等价（decideAfterRound + promptParts + maxNudges），但 child session 创建和 prompt 发送机制不同。

涉及文件：
- `src/Shell/ReviewLoopRunner.fs`（新建）
- `src/Opencode/ReviewerLoop.fs`（修改：瘦身为提供回调+接线）
- `src/Mux/ReviewToolsMux.fs`（修改：同上）
- `src/Omp/ReviewLoop.fs`（修改：同上）
- `tests/ArchitectureTests*.fs`（修改：新增断言）

操作步骤：

Step 1：读取三宿主的 ReviewLoop 实现：
- `src/Opencode/ReviewerLoop.fs`（runReviewerLoop，约 L53-86）
- `src/Mux/ReviewToolsMux.fs`（runReviewRound）
- `src/Omp/ReviewLoop.fs`（runReviewLoop）

逐行对比循环骨架。确认共性：
- `Kernel.ReviewSession.decideAfterRound` 调用
- `Kernel.ReviewSession.promptParts` 调用
- maxNudges 循环控制
- verdict 解析

确认差异点：
- createChild：Opencode 用 ChildAgentRegistry / Mux 用 delegate / Omp 用 taskService+子workspace
- sendPrompt：Opencode 用 session API / Mux 用 delegate API / Omp 用 pi API
- resolveVerdict：可能三处等价（均调 Kernel.ReviewVerdict.parseReviewReportMarkdown）

Step 2：新建 `src/Shell/ReviewLoopRunner.fs`，定义高阶函数：
```
runReviewLoop (config: ReviewLoopConfig)
              (createChild: ReviewerPrompt -> Promise<ChildRef>)
              (sendPrompt: ChildRef -> string -> Promise<string>)
              (resolveVerdict: string -> ReviewVerdict)
              : Promise<ReviewResult>
```
将循环骨架（decideAfterRound 循环 + promptParts 拼装 + maxNudges 控制）放入此函数。

Step 3：`src/Opencode/ReviewerLoop.fs` 瘦身：
- 删除循环骨架
- 保留 createChild 闭包（封装 ChildAgentRegistry）
- 保留 sendPrompt 闭包（封装 session API）
- resolveVerdict 直接调 Kernel（如三处等价则放入 Runner 默认实现）
- 调用 Shell.ReviewLoopRunner.runReviewLoop

Step 4：`src/Mux/ReviewToolsMux.fs` 瘦身：同上，保留 delegate 相关闭包。

Step 5：`src/Omp/ReviewLoop.fs` 瘦身：同上，保留 taskService/pi 相关闭包。

Step 6：确认 `src/Shell/ReviewLoopRunner.fs` 已加入 `.fsproj`（在 ReviewerLoop / ReviewToolsMux / Omp.ReviewLoop 引用之前）。

Step 7：新增架构测试断言：
- 禁止三宿主内联 `decideAfterRound` 循环骨架（应经 Shell.ReviewLoopRunner）
- 禁止三宿主内联 `promptParts` 拼装逻辑

Step 8：`npm run build-and-test`，确认全绿。

验收标准：
- ReviewLoop 循环骨架仅在 Shell/ReviewLoopRunner.fs 定义
- 三宿主仅提供 createChild/sendPrompt/resolveVerdict 回调
- Review 行为不变（测试覆盖 review 状态转换/verdict 解析/maxNudges 循环）

风险提示：
- ChildRef 类型需精确定义。不能是 `obj`（泄漏宿主类型），应为 Kernel 层的抽象引用类型，或使用泛型参数 `'childRef`
- 若三宿主的 resolveVerdict 完全等价（均调 Kernel.ReviewVerdict），则直接在 Runner 内调用，不需回调注入

---

### Phase 5：KG Runtime 状态归一（Slice 4）

目标：消除 KG Runtime 三处等价字段定义（state+commandQueue+registeredJobs+writeQueues），用 F# record 组合统一。

高风险原因：三处构造函数签名差异大（Opencode 6参 / Mux `?deps:obj` / Omp `pi:obj`），外部 API 紧耦合。可能引入隐式依赖。

宝典指导："边界先于抽象成熟。"若收益不足可只做部分合并，标记 deferred 保持分居。

涉及文件：
- `src/Shell/KnowledgeGraphRuntimeState.fs`（新建）
- `src/Opencode/KnowledgeGraphRuntime.fs`（修改：持有 record，保留 IO 注入）
- `src/Mux/KnowledgeGraphRuntimeMux.fs`（修改：同上）
- `src/Omp/KnowledgeGraph/Runtime.fs`（修改：同上）
- `tests/ArchitectureTests*.fs`（修改：新增断言）

操作步骤：

Step 1：读取三处 KG Runtime 实现：
- `src/Opencode/KnowledgeGraphRuntime.fs`（190行）
- `src/Mux/KnowledgeGraphRuntimeMux.fs`（190行）
- `src/Omp/KnowledgeGraph/Runtime.fs`（159行）

逐行对比字段定义和操作方法。确认等价部分：
- `mutable state = initialKnowledgeGraphState`
- `commandQueue = SerialQueue()`
- `mutable registeredJobs = Map.empty`
- `mutable writeQueues = Map.empty`
- `applyCmd` 状态更新逻辑
- `ensureSessionSnapshot` 骨架
- `registerJob`/`deleteJob` 逻辑
- `buildPreludeForSession` 逻辑

确认差异部分：
- 构造函数参数（6参 / 0参 / 1参）
- 外部 API 调用（client / deps / pi）
- submit 签名
- tryResolveJobContext 签名
- 测试 hooks 命名

Step 2：新建 `src/Shell/KnowledgeGraphRuntimeState.fs`，定义 F# record：
```
type KnowledgeGraphRuntimeState = {
    mutable state: KnowledgeGraphInternalState
    commandQueue: SerialQueue
    mutable registeredJobs: Map<string, RegisteredJob>
    mutable writeQueues: Map<string, SerialQueue>
}
```
定义纯操作函数：
```
module KnowledgeGraphRuntimeStateOps =
    let create () = { ... }          // 初始化
    let applyCmd (s, cmd) = ...       // 状态更新
    let registerJob (s, job) = ...    // 注册作业
    let deleteJob (s, id) = ...       // 删除作业
```

不造基类。不造 DI 容器。用 record + 纯函数组合。

Step 3：`src/Opencode/KnowledgeGraphRuntime.fs` 修改：
- 删除 state/commandQueue/registeredJobs/writeQueues 字段定义
- 改为持有 `KnowledgeGraphRuntimeState` record
- applyCmd/registerJob/deleteJob 改为调用 Shell.KnowledgeGraphRuntimeStateOps
- 保留构造函数签名（外部 API 调用不变）
- 保留 tryResolveJobContext（client API 不变）

Step 4：`src/Mux/KnowledgeGraphRuntimeMux.fs` 修改：同上。

Step 5：`src/Omp/KnowledgeGraph/Runtime.fs` 修改：同上。

Step 6：新增架构测试断言：
- 禁止三宿主各自定义 `mutable state.*KnowledgeGraph` / `commandQueue.*SerialQueue` / `registeredJobs.*Map` 字段（应持有 record）
- 禁止三宿主各自内联 applyCmd 逻辑

Step 7：`npm run build-and-test`，确认全绿。

验收标准：
- KG Runtime 状态字段仅在 Shell/KnowledgeGraphRuntimeState.fs 定义
- 三宿主仅持有 record + 提供外部 IO 注入
- KG 行为不变（测试覆盖 submit/snapshot/maintenance/bookkeeper）
- 净减约 200+ 行重复代码

风险提示（重要）：
- 这是最高风险 Phase。如果 record 提取后引入隐式依赖（如 Shell 模块需引用宿主类型），立即回滚
- 判定 deferred 的标准：如果三宿主的 KG Runtime 生命周期管理差异导致 record 无法统一持有（如 Omp 的 lifecycle 与 Opencode/Mux 根本不同），则只合并 applyCmd/registerJob/deleteJob 等纯操作函数，状态 record 保持各宿主持有
- 可分两步：先只提取纯操作函数（applyCmd/registerJob/deleteJob），验证无回归后再提取 record

回滚策略：若 Phase 5 引入编译错误或测试失败，立即 `git checkout` 回退至 Phase 4 完成状态。KG Runtime 分居不违反任何宝典原则——边界先于抽象成熟。

---

### Phase 6：拆分黄牌文件

目标：清扫 200-300 行黄牌文件，降至 200 行以下。

前提：Phase 1-5 完成后，部分黄牌文件（如 NudgeRuntime）已自然瘦身。先重新统计行数。

操作步骤：

Step 1：重新统计文件行数：
```
find src -name '*.fs' -exec sh -c 'lines=$(wc -l < "$1"); if [ "$lines" -ge 200 ]; then echo "$lines $1"; fi' _ {} \; | sort -rn
```

Step 2：对仍在黄牌区的文件，按职责拆分：

`src/Opencode/HookSchema.fs`（257行）：
- 拆为 `HookSchemaCore.fs`（类型定义+常量）+ `HookSchemaDecode.fs`（编解码逻辑）
- 或按 Hook 类型拆：`HookSchemaTool.fs` + `HookSchemaChat.fs` + `HookSchemaSession.fs`

`src/Opencode/PtyTools.fs`（234行）：
- 拆为 `PtySpawn.fs`（会话创建/销毁）+ `PtyIo.fs`（读写/列表）

`src/Omp/SessionLifecycleHooks.fs`（207行）：
- 拆为 `NudgeHooks.fs`（nudge 相关事件处理）+ `TodoHooks.fs`（todowrite 后处理）

`src/Opencode/SessionIoSubagent.fs`（200行）：
- 按子代理生命周期拆：spawn 部分 + io 部分

Step 3：每拆一个文件：
- 确认新文件加入 `.fsproj`（顺序正确）
- 确认引用方更新 `open` 声明
- `npm run build-and-test` 确认全绿

验收标准：
- 全项目 0 个文件 ≥200 行（黄牌线以下）
- 新增文件均 <200 行
- 现有测试全绿

---

## 六、验收清单

### 每个 Phase 结束时检查

- [ ] `npm run build-and-test` 全绿
- [ ] 涉及文件无残留重复代码
- [ ] 新增架构测试断言已加入
- [ ] `git diff` 确认变更范围符合预期

### 全部 Phase 完成后检查

- [ ] `src/Kernel/` 下搜索 `UtcNow`/`DateTime.Now`/`Date.now` 零匹配（Phase 1）
- [ ] StateHolder 全项目仅 1 处定义（Phase 2A）
- [ ] KGTestHooks 全项目仅 1 处定义（Phase 2B）
- [ ] collectSnapshot 全项目仅 1 处定义（Phase 3）
- [ ] ReviewLoop 循环骨架全项目仅 1 处定义（Phase 4）
- [ ] KG Runtime 状态字段全项目仅 1 处定义（Phase 5）
- [ ] 全项目 0 个文件 ≥200 行（Phase 6）
- [ ] 架构测试新增断言全绿
- [ ] 231+ 端到端测试全绿
- [ ] 提交时带上 `kg/` 的变化（AGENTS.md 要求）

### 量化目标

| 指标 | 当前 | 目标 |
|---|---|---|
| Kernel 内侧效泄漏点 | 1（nowMs） | 0 |
| StateHolder 定义数 | 2 | 1 |
| KGTestHooks 定义数 | 2 | 1 |
| collectSnapshot 定义数 | 2 | 1 |
| ReviewLoop 骨架数 | 3 | 1 |
| KG Runtime 状态字段重复 | 3处 | 1处 |
| ≥200 行文件数 | 5 | 0 |
| 架构测试断言条目 | 126+ | 130+（新增4+断言） |

---

## 附：宝典原则速查

重构过程中随时对照：

1. 压缩不是合并，复用不是提前抽象。两段像只说明此刻长得像，不说明同一份知识
2. 唯一表示是同一事实多处重复并开始不一致。独立生命周期概念逐字相同也该分居
3. 边界先于抽象成熟。规则网络协议持久化权限语境视图各有领土
4. 类型系统是最便宜边防。概念独立命名在运行时零成本
5. 纯函数是内核：不读时钟不掷骰子不查库不发网不写盘不改入参
6. 命令和事件必分：意图可拒事实不可驳
7. GoF 翻到末页只剩数据函数类型组合
8. 文件体量是梯度刑场：200行亮黄牌，300行亮红牌，触发即新建文件
