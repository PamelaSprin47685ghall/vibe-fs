import test from 'node:test'

test.todo('WHAT[REQUIREMENT-GROUNDING-007] automatic grounding durably anchors ordinary read wire after the pair pseudo-skill and replays it byte-identically without prefix drift')
test.todo('WHAT[REQUIREMENT-GROUNDING-008] defers the first ungrounded mutation with zero file effect and never auto-replays the old call')

