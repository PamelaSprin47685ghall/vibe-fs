# office-capability — GAP

## 2026-09-16 资源大修的证明边界

共同法、角色说明、跨角色工具、生命周期、案例阶段、生成示例、输出说明和完成纪律已按 Engineer 合并重写。公开提示词目录只含五个活跃角色，旧角色不再加载 Engineer 文案。具体路径与接点见 HOW 和方案第 18.1 节。

资源语义与实际提示词装配测试不等于整个角色迁移完成。以下事项保持开放：

1. `Foundation/Roles`、内部 `PromptCatalog` 和停用工具实现仍有旧类型或旧路径。公开入口已拒绝或不再分发，不等于这些实现已从物理编译闭包全部删除。删除审计须保留真实历史解码，不恢复旧活跃角色。
2. 固定 DevOps 的接力、崩溃恢复、重复接收、进程收束，以及 Fission 旧状态恢复，需要各 owner 的完整契约与集成验证。本次提示词测试不能代替这些证据。
3. 普通修复自主完成、产品语义边界停止、只读 Engineer 返回和 Fission 收敛，仍须正式模型 canary；不能由字符串存在推定行为已经出现。

本轮全仓 `node scripts/check.mjs` 实际失败：`fsharp-control-pyramid.mjs` 报告 39 处控制流嵌套，涉及 Relay、执行输出、Casebook、Spool 和 Sphinx；其他静态门禁未报告问题。未放宽规则或将失败标记为通过。此记录描述当时工作树，不把全部问题归因于本次资源改动，也不以「已有问题」免除最终集成责任。

`npm run format-build-test` 与 `npm run verify:release` 的完整成功记录尚未建立。针对性测试及构建结果在本次交付中单独报告；不将它们记作发布验收。
