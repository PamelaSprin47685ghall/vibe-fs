# feature-ablation — GAP

## GAP-ABL-001: 角色与能力目录消融同步闭包（CLOSED）

- **状态**：CLOSED
- **现状与闭合证明**：已完成新角色映射与消融目录同步，并验证 Sphinx 独立开关与门禁。落点 `tests/010.test.mjs` 与 `tests/012.test.mjs`（primary agent 门禁、Sphinx 独立开关、tool-map 同步）。
- **目标合同**：ABL-010 与 ABL-012 保证 Browser MCP 撤销、Sphinx 独立可控以及消融目录严格同步无错位。
- **影响范围**：`resources/ablation/`、`src/Wanxiangshu/Ablation/`。
