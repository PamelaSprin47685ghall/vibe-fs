# migration-ledger — WHAT

本文件是 `migration-ledger` 的**临时施工契约**，非永久产品语义。永久迁移完成状态机由 `requirement-system` 唯一拥有，本包仅引用。

---

## MIGRATION-LEDGER-001: 施工账本引用

本包 `scripts/checks/migration-ledger.json` 的 DAG、节点状态机与门禁 11 项非法态 **引用** `requirement-system:019` 的 `WHAT[REQUIREMENT-SYSTEM-019]` 定义，不在本包重复定义。`requirement-system` 为唯一 `normative owner`，本包 `WHY/HOW` 仅说明临时施工格式与 `--self-test` 落点。
