# Proof status

Unit oracles for `CONCERN-ROUTING-001` through `007`.

The test suite covers:
- Idempotent address subscription and immutable id-to-concern mapping (`001`).
- Sticky-once subscription discovery announcements in Pair Hint (`002`).
- Fail-closed message publishing by exact generation and semantic address (`003`).
- Natural Pair Hint delivery boundary and atomic coverage commits (`004`).
- Low-authority peer message semantics with no obligation vocabulary (`005`).
- Mailbox generation retirement and lifecycle isolation per owner participant (`006`).
- Minimal routing state verification without workflow or organization hierarchy (`007`).
