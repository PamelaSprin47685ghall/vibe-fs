namespace OwnerProjectBoundary

module Runtime =
    let backingValue = "runtime"
    let internal secretValue = "secret-runtime"

module private PrivateRuntime =
    let secretValue = "private-runtime"
