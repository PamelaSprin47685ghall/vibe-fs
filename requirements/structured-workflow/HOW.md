# structured-workflow — HOW

`WHAT.md` 是唯一 normative 合同。本文只记录当前实现入口与迁移方法。

## 1. 当前结构入口

- `scripts/checks/subsystems.json`：迁移期唯一 legacy-owner → subsystem 映射。它只帮助尚未显式迁移的 shard 解析 subsystem，不定义 consumer ACL、exposure 或业务 law。
- `scripts/lib/compile-shards.mjs`：纯构建层。读取 fsproj、source、sibling `.fsi`、ProjectReference 与 aggregate union；不知道业务 owner/slice/exposure。
- `scripts/checks/subsystems.mjs`：唯一 subsystem 结构 release gate。验证 source 唯一归属、compile-shard DAG、aggregate 等价与显式 runtime-platform shard 的依赖倒置，并报告 subsystem SCC。
- `scripts/check.mjs`：只运行 `subsystems.mjs` 作为 source/subsystem/shard 的架构权威；旧 semantic-owner/owner-contract/owner-project gate 不再进入 release verdict。

旧 `WanxiangshuSemanticOwner`、`WanxiangshuOwnerLocality` 与 `WanxiangshuOwnerLocalityKind` 暂留在 fsproj，供未迁工具和历史测试读取。新 shard 同时声明 `WanxiangshuSubsystem`、`WanxiangshuCompileShard`；迁移结束后可机械删除旧字段，但删除本身不是架构收益。

## 2. 拆 compile shard 的固定方法

先按知识而不是文件大小分 cohort：

1. 列出当前 shard 的公开类型、纯 decision、effect/factory、runtime registry、codec 与测试 surface。
2. 按真实 consumer 需要分组；两组若 reason-to-change 不同或 audience 长期不同，拆 shard。
3. 最底层通用 primitive 必须无领域依赖；若需要领域 identity，说明它不是 platform primitive。
4. consumer 直接引用最窄 shard。禁止保留 umbrella reference 作为“保险”；编译失败用于发现遗漏依赖。
5. `.fs` 与 sibling `.fsi` 同迁，aggregate 顺序不变；full build 输入并集不变。
6. 用定向真实 Fable compile 验证 ProjectReference closure，而不是 aggregate 成功或源码 grep 冒充编译证明。

第一批样本：

```text
dispatch/identity            <- Foundation/Identity
chat-execution/outcome       <- Foundation/Outcome + OutcomeSurface
dispatch/runtime-nudge       <- Interaction/Dispatch/Nudge
dispatch/prompt-metadata     <- MetadataCodec
runtime-platform/canonical   <- CanonicalJson + surface

runtime-platform/task-result <- TaskResult + FsToolkit compatibility
runtime-platform/parallel    <- bounded Parallel + surface
runtime-platform/async       <- AsyncSupport
delegation/fission-facts     <- Execution/Fission/Facts
```

`runtime-platform/*` 明确不依赖 domain subsystem。Fission facts 因为携带业务 identity，归 Delegation 而不是为了复用留在 TaskResult 大包。

## 3. subsystem 粒度与后续收敛

调整 subsystem 只看 semantic cohesion、历史 co-change、dependency direction、rewrite independence 与 boundary tax。compile shard 数量不参与 subsystem 数量裁决；下面的观测数量不是 quota。

Subsystem SCC 是首要结构债：双向依赖必须通过移动知识所有权、提取窄纯 contract、capability injection 或删除重复 decision 消解。严禁以 facade、service locator、event bus、shared DTO bucket 隐藏环。

后续优先继续处理 reverse closure 最大的公共重力井：`foundation-roles`、剩余 Identity cohorts、provider projection model，以及仍同时承载多种 reason-to-change 的 Host/Delegation/Persistence shard。

### 3.1 观测基线

2026-09-08，在上游提交 `58aa3d934` 运行 `node scripts/checks/subsystems.mjs`：26 subsystem、199 compile shards、702 production sources、1919 ProjectReference，shard DAG，最大 subsystem SCC 为 23 个节点。口径是 fsproj 声明及其聚合图，不是源码实际调用图或编译耗时测量。后续报告附对应代码版本和工作区差异，不把本次快照写成固定测试期望。

### 3.2 GAP-033 当前缺口与关闭证据

2026-09-12 用户批准按完整消费者边界继续迁移。以下历史批次中的单边删引用实验，只证明当时那个具体改法的结果；第 32／33 条对窄查询与 journal 的否定不适用于同时迁移类型、全部路径及装配的完整方案。具体纪律见当前 AGENTS 第 3 节；不以中间一步闭包未减否定迁移，也不把删边释放量当作最终收益。

本批从 `cc08c286f` 迁移 Attention 工具：`AttentionJournalPort` 由 interaction 自有 facts 分片提供，工具与签名不再消费全局 journal、projection 或 routing union；`ToolRegistry` 只接入 `AgentJournalPortAdapter.forAttention`，由 persistence 的既有 durable composition adapter 保留一次快照、原 session／provider-run 和错误／异常语义，独占 `AgentFact.Attention` 包装。真实声明前向闭包（含自身，生产 `.fs` 不重复计算 `.fsi`）由 **58 分片／152 源码降至 11／27**，其中包括新增的 port 与真实工具 Semantic Surface。装配消费者 `opencode-host-managedagentconfig` 为 181 分片／513→515 源码，`plugin-composition` 为 209／589→591；两者本来就须知道全应用，增加两份源码不能宣称为闭包收益。新边界测试在旧树因包含 `Composition/Durable/MagicTodoIdentity.fs` 失败，迁移后拒绝整个 durable aggregate 与运行时容器进入该工具闭包。全图为 26 subsystem、235 shards、716 production `.fs`、1786 refs、最大 SCC 18；GAP-033 仍为 PARTIAL。

同批修正 DELEG-029 的测试判据：不再用 `legacyKind` 决定合法性，保留自身 port／源码身份及 foreign handle 禁令，改查真实传递源码闭包。移除全部 kind 的正例通过；伪标 contract 的 journal、物理 timing adapter，以及借 delegation port 间接传入 journal 的三个反例均被拒绝。此项只修正退役治理的残留，不宣称扩大 DELEG-029 的 PTY 行为证明。

本节是 STRUCTURED-WORKFLOW-011 至 016 的实现与证明缺口记录，不新增产品条款，也不构成整表开工授权。仅处理用户任务明确覆盖的部分；相关 Worker 应显式读取本节，不依赖路径触发的自动接地。聚合状态见 `requirements/GAP.md` 的 GAP-033，保持 PARTIAL；单项修复不代表整体已可独立替换。

第 3.1 节保留历史测量口径；以下逐项记录当前实现和仍缺的证据，不用历史数量约束后续合法变化。

1. **知识依赖尚未收敛。** subsystem 聚合图仍有 SCC，公共重力井仍待拆解。按真实 consumer 收窄 contract／port，记录被切断的具体知识依赖与前后闭包；对受影响 shard 和 consumer 提供真实 focused Fable 编译及相关行为证明。SCC 数字下降不能独自关闭整体缺口。
2. **结构快照已替换为性质反例。** `subsystem-boundaries.test.mjs` 与 `owner-project-boundaries.test.mjs` 不再限制 subsystem 数量范围或要求 shard 多于 subsystem。前者通过真实临时 fsproj 验证合法增长、缺失／重复 source 归属、sibling 签名与 aggregate 输入完整性；`A1(alpha) → B1(beta) → A2(alpha)` 是合法 shard DAG，但必须准确报告 subsystem SCC，单向对照则无 SCC。测试标题与第 5 节的精确证明边保持不变。
3. **平台解析路径已有正反例，知识归属审计仍未完成。** `scripts/checks/subsystems.mjs` 已按解析后的 `subsystem` 检查全部平台 shard；`subsystem-boundaries.test.mjs` 对 explicit 与 legacy 映射路径分别证明平台依赖合法、域外依赖拒绝。它不证明所有标成平台的源码都没有领域知识，也不替代真实 focused compile；不得据此宣布整个平台隔离已完成。
4. **authority 源码身份已与 WHAT 包归属分离。** `authority-boundary.mjs` 通过 `buildSubsystemInventory` 取得唯一源码 subsystem；manifest 的 declaration、method、issuer `owner` 全部按实际文件归属迁移，`whatOwners` 继续指向 requirement package。legacy 字段只参与既有映射解析，不再作为第二个可接受身份；`scanRepo` 使用传入 repository root。`requirements/capability-enforcement/tests/authority-boundary.test.mjs::WHAT[ENF-014] explicit and legacy-mapped subsystems resolve through the real repository path` 用真实临时工程证明无 legacy 字段与 legacy 映射的合法路径，并拒绝旧 alias、错误源归属及错误 WHAT 包归属。能力发行、一次性消费和持久化检查没有放宽。

2026-09-09，在 merge commit `2c6088fa8` 上叠加本批修改：相关结构、impact 与 authority 测试 40/40 通过；补全实际 consumer 后，subsystem gate 报 26 subsystem、207 shard、702 source、1874 references、最大 SCC 20。相比本批中途的 18，SCC 增长来自显式恢复 Host→Casebook 等已存在的代码依赖，不能把先前漏报当作隔离成果，也不能宣布本 GAP 已关闭。authority cutover 的临时对照使用同一迁移后 manifest，旧 gate 得到 59 个错误，新 gate 零错误；临时模块已删除。

随后恢复 `ToolRuntimeScope → change-integration.git-integrationgate` 已被动态加载隐藏的实际依赖，静态构造 Host 并保留 callback 与 snapshot 类型。scope shard 的 942-source focused Fable compile 通过；subsystem gate 更新为 1875 references，其余上述计数不变，shard 图仍无环。这修复了真实 Long Stroke 首次 publication 的异步调用错误，不代表 subsystem 知识耦合已经消除。

在 `40692d3dc` 上继续迁移 `ToolRuntimeScope` 的 Relay 查询：两个 `emitJsExpr` 布局探针改用既有 `Fold.view` public contract，退休 fence 与 `.fsi` 改为 `IncumbencyId`，并显式引用 `relay-incumbency.mission-relay-core`。真实 `SuicideTool` consumer 连同 scope 的 948-source focused Fable compile 通过，结构／impact 测试 24/24、既有退休／投影／Manager plugin 测试 26/26 通过。当前声明图为 1876 references，其余上述计数不变；本批切断的是对 Relay 内部 JS 表示的知识依赖，不宣称编译闭包或 SCC 缩小。未新增依赖编译器内部表示的测试，也不把已有 pure fence 测试描述为 scope fence 全分支证明；GAP-033 仍为 PARTIAL。

资源校验接缝恢复静态 `EnforcerCatalog.validate` 后，`Wanxiangshu.Owner.cognitive-environment.resources-promptsurface.fsproj` 的真实 focused Fable compile 通过（90 source）；新产物中英文各装载 120 条，移走 scratch 校验器模块时新 Node 进程以 `ERR_MODULE_NOT_FOUND` 失败，随后恢复并清理临时资源链接。随后修复 `enforcer-codec` consumer 的真实闭包：删除无用 namespace 引入，恢复纯 `EnforcementProjection` 静态依赖与 root-workspace contract；将 `SessionNudge`／repair port 从 ingress 大分片移到 `dispatch/session-nudge`，使 Companion 无需反向引用包含自身的 ingress 闭包。原 516-source 编译失败已在 522-source focused Fable compile 中通过；源码总数与 aggregate 顺序不变，没有用动态加载回捞依赖。合入的格式问题由正式 Fantomas 工具修正，整体验收仍使用 `npm run format-build-test`，局部编译不代替该入口。

独立编译原 ingress consumer 又暴露了被 enforcer-codec 根项目遮住的 Companion→repair 依赖。`enforcer/repair` 现单独编译既有 codec、cycle model/decode、repair 和 Blogger probe，Companion 与 enforcer-codec 均静态引用它，不再依赖偶然被聚合根纳入的源码；Host signal adapter 也显式引用实际调用的 `ToolResultBound` 窄合同。`Wanxiangshu.Owner.dispatch-protocol.interaction-dispatch-opencode-ingresscodec.fsproj` 的 564-source focused compile 已通过。这些修复恢复真实声明依赖，references 增长是如实表达知识，不是架构退步的自动判据。

`LanguageSurface` 的 Bookkeeper 资源读取仍需要 `PromptResources`，因此恢复其静态资源引用；真实 Host transform 从 bootstrap 分片移到 `host/provider-system-transform`，由语言验证 Surface 与 bootstrap 共用，公开 API 和源码路径不变。bootstrap 对 `BookkeeperRuntime`／`CasebookLifecycle` 的实际调用分别通过现有 casebook-model／casebook-bookkeeper 分片声明，不再依靠 aggregate 偶然补齐。语言 Surface 与原 bootstrap consumer 各使用自己的 focused compile 入口验证，不能仅用全量构建证明这两条闭包。

旧 M6 计划仅用于追溯退役原因；其 extractor、worksheet、ACL 与 snapshot 清单不形成关闭本节缺口的条件。ENF-015、ENF-016 等独立产品证明缺口仍由所属包 HOW 记录，不因 GAP-031 路线退役或本节记录完成而关闭。

在 `9b56d8ce2` 上继续恢复两条 typed 边界：Coder 静态调用既有 WarmStart 合同，保留查询级 fail-open 而去掉 adapter 的动态加载与 catch-all；Blogger 将唯一 context 构造及其两个纯 helper 收回 MainContext，删除旧 Enforcer Host 与 Recovery 中已无消费者的副本。`coverageBirth` Surface 改用真实 trace fold 和出生门，无法映射的正序列反例旧败新胜；相关出生门、WarmStart 和 JoinGuard 等测试 15/15 通过，不将出生门证据冒充 writer 提交前校验证据。

本批 Coder focused compile 又暴露 `JoinGuardSurface → DispatchSurface` 的漏报依赖。证明 adapter 从运行时分片移入独立 `delegation/join-guard-surface`，显式引用原运行时和 DispatchSurface 所属分片，未复制 Host port adapter，aggregate 源码顺序不变。Coder、JoinGuard Surface、Enforcer continuation 的真实 focused Fable compile 分别通过 982、972、958-source 闭包；声明图为 26 subsystem、208 shard、701 production source、1879 references，shard DAG，最大 subsystem SCC 为 21。SCC 增长如实反映恢复的依赖，不是闭包收敛成果；GAP-033 保持 PARTIAL。

在 `11a221797` 上继续恢复 Fork → WarmStart 与 Prefix → WorkRecord 两条实际依赖。Fork 直接调用既有 typed append 合同，不再解码编译器 union 或吞掉 adapter 异常；Prefix 静态调用唯一纯 materializer，删除动态加载与备用 Chronicle 渲染器。两个真实 consumer shard 的 focused Fable compile 分别通过 980、952-source 闭包，相关行为测试 50/50 通过；声明图为 26 subsystem、208 shard、701 production source、1881 references，shard DAG，最大 subsystem SCC 21。新增引用公开原来隐藏的知识依赖，并不代表 SCC 已缩小；GAP-033 仍为 PARTIAL。删除只能匹配源码注释的 Opening 伪证明，保留 canonical renderer 与真实 writeback 行为证明；完整 frozen 物化及 Fork adapter fault／取消的直接证明限制分别记录于所属 HOW。

在 `21033d90c` 上恢复 Host 四个 consumer 对 `SessionExecutionBinding` 的 typed 调用，以及 SyncDelegate Surface 对真实 Inspector tool 的 typed 构造／调用。移除动态加载、手写 union、缺失模块时的 no-op 和虚假零值；provider 合同与公开 Surface 签名不变。真实 consumer 的影响集合经既有 `compile-impact` 求并集，focused Fable 编译通过 1068 parsed sources／1030 compile items；首次编译暴露并清除了 `JoinSurface` 未使用的旧 Manager namespace 引入，没有用额外引用掩盖问题。新全量产物上的绑定、chat.params、pre-provider settlement、Inspector finalize 与 SyncDelegate lifecycle 测试 38/38 通过。声明图为 26 subsystem、208 shard、701 production source、1885 references，shard DAG，最大 subsystem SCC 21；这些新增引用公开实际知识依赖，不代表 SCC 收敛或 GAP-033 完成。

在 `173887ea9` 上恢复 `ToolRegistry` 的 typed 工具注册及 `PluginHooks` 的 Casebook 接线，同时将唯一 persistence 调用方 `PluginHostInterop` 与 registry 签名迁移到既有 `IJsTransactionPersistence`。删除 foreign module loader、备用权限表、静默漏注册及无类型 factory；provider 合同不变。首次真实 focused compile 暴露 `ExecutorTool` 对 `DistillationRuntime`／`Distillation` 的缺失引用，补回实际 provider 声明后，包含签名反向消费者的影响并集通过 1284 parsed sources／1246 compile items（fingerprint `879e5ddd19b7`）。新全量产物上的工具权限、Casebook、JS Host／事务测试 44/44 通过。声明图为 26 subsystem、208 shard、701 production source、1896 references，shard DAG，最大 subsystem SCC 从 21 增至 22；这是如实暴露既有知识依赖，不是隔离收敛。GAP-033 保持 PARTIAL，完整 hook／composition 的局部分支证明限制记录于所属 HOW。

在 `bd2e9563e` 上恢复 `HostSignalBootstrap` 对 `LoopSensor` 的 typed 构造、装配及 reset／observe／drop 消费，复用 owner 的 continuation 资源映射。真实插件回归先复现 managed child 无物理中断，再确认恢复 exactly-once interruption；不再以注释里的 `LoopSensor.create` token 证明接线。focused Fable 并集通过 1144 parsed sources／1106 items（fingerprint `641bd4fec085`），新全量产物上的 bootstrap 与 sensor 行为测试 19/19 通过。声明图为 26 subsystem、208 shard、701 source、1897 references，shard DAG，最大 subsystem SCC 22；新增引用公开真实依赖，GAP-033 仍为 PARTIAL。

该批首轮 ladder 的 3975 个语义测试及全部 integration suites 通过，但唯一 Long Stroke 在 `manager-interrupt.1` 后等待 `coder.3` 超时（保留世界 `/tmp/oc-e2e-yqJFT1`）。保留证据仅证明 Coder 下一步未进入 provider stream，不能证明其进入了 messages.transform，也不能据此断定容量等待。完整 stderr 的阶段观测实验通过（441 journal／2142 SSE），没有同 fence 重入或 capacity waiter；探针已移除，该实验不替代无探针正式验收。

另一保留世界 `/tmp/oc-e2e-CEqj6W` 的 `seal-undeclared` 已通过 frozen blob 的 wire digest 对应及确定性旧败新胜回归定位：成功后仍保留的 retry row 重新触发 prefix candidate 选择。`XWire.mayProbe` 现在读取成功结算后的连续失败计数，复用已有 frozen plan，零失败只投影 committed epoch，不再构造候选。修复与证据边界见 `context-compression/HOW.md`；focused consumer 并集通过 1286 parsed sources／1248 items（fingerprint `e532369f51e2`），相关测试 38/38。未修改 watchdog、event ceilings 或 seal 声明，原 Coder 停顿仍不归因于该 prefix 缺陷。

在 `47e2d4666` 上恢复 RequirementGroundingTransform → PairProgrammingThoughtTransform 与 PromptResources → ProviderResources 的静态 typed 调用，删除缺失模块时的伪造成功、空资源及跳过校验路径。两个 owning shard 显式声明原有知识依赖，公开签名不变；包含真实 PluginHooks、ManagedAgentConfig、Grounding／Language／Prompt Surface consumer 的 focused Fable 并集通过 1288 parsed sources／1250 items（fingerprint `9cb200792b8c`）。相关投影、资源与语言行为检查通过，结构／impact 测试 14/14；规范重放测试不再比较空 synthetic 列表，而是证明冻结终端结果、journal 重开不重新读取文件以及新 digest 仅追加。删除入口 token 伪证明，物理终止与 OS crash 的证明限制见 requirement-grounding/HOW。声明图为 26 subsystem、208 shard、701 source、1899 references，shard DAG，最大 subsystem SCC 22；新增引用公开隐藏依赖，不代表耦合消除，GAP-033 保持 PARTIAL。

在 `9dbdbf2a8` 上恢复 CanonicalIntegrator 的 Casebook／JsTransaction oracle、GroundingCatalog 的 glob 匹配及 RequirementGroundingRepositorySurface 的生成器／工作流静态调用。删除 foreign runtime loader、编译器 union tag 解码及缺失模块 fallback；只扩大既有 `JsGeneratorSurface.typedRole` 的可见性，`typedFor` 仍为 internal，不复制生成器或装配逻辑。接地 Surface 保留原失败词汇，业务注册、cut/reset、提交与观察语义不变。首轮 focused compile 拒绝事务嵌套 union 模式的缺括号，修正后包含签名反向 consumer 与真实持久化／Host／Delegation consumer 的并集通过 1360 parsed sources／1322 items（fingerprint `2444894e6950`）；新全量产物上的 grounding、事务、canonical Current／重放及结构／impact 测试 44/44 通过。生产工作流 smoke 实际拒绝无效程序与程序异常，分别保持 `invalid_program`、`program_failed`；临时探针已删除。声明图为 26 subsystem、208 shard、701 source、1905 references，shard DAG，最大 subsystem SCC 22。新增六条引用公开既有依赖，不代表知识耦合消除；未新增 Casebook／JsTransaction decoder fault 到物理 fatal 的专门证明，GAP-033 保持 PARTIAL。

在 `1aaf1c43e` 上继续恢复 Prefix Wire 与 Fallback Workflow 的 typed 查询。前者通过既有 `StrengthRuntime.TryFindByReplica` 取得 `StrengthReplicaBinding`，直接比较 Role 与 capability Set，删除私有字典路径、擦除字段与 catch-to-None；后者调用唯一 `SessionAssociationProjection`，删除 shadow module 的 Map 扫描与手写 union tag。两个 owning shard 分别显式引用既有 Strength runtime 和 Session association 合同，公开签名不变。包含真实 `PluginTransforms` 与 `OrdinaryTurnWorkflow` consumer 的 focused Fable 并集通过 1284 parsed sources／1246 items（fingerprint `ba15998515e3`）；新全量产物上的 association、Replica registry／transform／工具门禁、XWire decision、Blogger runtime、retry policy 与结构／impact 测试 105/105 通过。声明图为 26 subsystem、208 shard、701 source、1907 references，shard DAG，最大 subsystem SCC 22；两条引用公开原有依赖，不代表 SCC 或耦合已经收敛。未保留只匹配源码 token 的伪回归：上述 owner／decision Surface 测试不等于直接执行 Wire 的 Replica Authority 分支，也不覆盖 Workflow 的 association 材料等待分支；这两处专门 consumer 行为证明仍缺，GAP-033 保持 PARTIAL。

在 `f40d0b63f` 上移除 `ToolHostSurface` 对 `HostSchema` 的 `.value/.fields[0]` 表示探针，改由既有 `ToolHostCodec` 在同一 shard 内发布 internal typed 解包；私有构造器、公开 Surface 签名与工具注册策略不变。原生 literal schema 在旧 Surface 被错误解成字符串，正式回归先以 `schema.parse is not a function` 失败，再随 typed 解包通过；删除迎合二次解包的虚构 schema fixture，用真实 SDK validator 证明接受／拒绝与 optionality。签名影响及其反向 consumer 的 focused Fable 并集通过 1418 parsed sources／1380 items（fingerprint `9557abcf695f`），新全量产物上的 codec、结构与 impact 测试 40/40 通过。声明图仍为 26 subsystem、208 shard、701 source、1907 references，shard DAG，最大 subsystem SCC 22；没有以表示依赖清理宣称 subsystem 已可独立替换，GAP-033 保持 PARTIAL。

在 `8cddee173` 上将 CompletionMailbox、Change VerdictMailbox 与 HostForkJoin 的 journal／fission 竞争结果改为 typed `Choice`，删除手写 `{ kind, reason }` 协议；不新增调度运行时，不改变单次 `.then`、注册顺序、drain-first 或中断语义。首轮格式检查拒绝未括号化的 `let!` 类型模式，修正后真实 focused Fable 并集通过 950 parsed sources／912 items（fingerprint `21025f5f1239`），新全量产物上的 verdict／join／causal wait 测试 35/35 通过。新增 Change Surface 直接观察真实 VerdictMailbox，证明竞争优先级、旧 waiter 移除及有界 FIFO；公开 mailbox／join 生产签名不变，仅扩充既有证明 Surface。无 journal join probe 不证明 journal／fission 全分支，具体证据边界见 delegation/HOW。声明图仍为 26 subsystem、208 shard、701 source、1907 references，shard DAG，最大 subsystem SCC 22；本批只是类型安全收敛，不是 subsystem 知识依赖减少，GAP-033 保持 PARTIAL。

在 `d087240ac` 上将现有 `Host/Digest.fs/.fsi` 从混有 OpenCode 类型的 host-digest 分片抽到零 ProjectReference 的 `runtime-platform/digest`，公开摘要 API 与 aggregate 顺序不变。原 Host 分片保留全部消息／事件合同并引用原语；Casebook 和 Sphinx 直接声明窄原语引用，删除各自重复的字符串 crypto 实现及 Sphinx 旧导出的全部调用方，保留 canonical JSON、身份输入与 null 语义。二进制 SHA-1／SHA-256 不属于本次统一范围。focused Fable 的签名反向 consumer 并集通过 1402 parsed sources／1364 items（fingerprint `dbdfc4eb9b9d`）；新全量产物上的捕获／重放、Sphinx 身份／承诺／golden 与结构测试 41/41 通过，真实 Capture Surface smoke 另覆盖空串、Unicode、CRLF、NUL、孤立 surrogate 与 null。删除只执行测试内 crypto、却误称 HOST-BOUNDARY-019 生产证明的用例；该删除不宣称补齐 Host canary 缺口。声明图为 26 subsystem、209 shard、701 source、1910 references，shard DAG，最大 subsystem SCC 22。新增原语确实不包含 Host 类型或领域知识，但原宽 Host 消费者未迁移，不宣称其闭包缩小或 GAP-033 完成。

在 `686f3a9c4` 上将 Institutional Learning 事实／Enhancer 分片的摘要引用从宽 Host 分片迁到既有 `runtime-platform/digest`。完整分片只需要 Identity、Enforcer catalog 与字符串摘要；不修改源码、签名或 aggregate，不以动态加载补回 Host。正式 planner 的 forward closure 从 7 项目／38 个 `.fs/.fsi` 输入降至 4 项目／16 个输入，移除 OpenCode Message／OpencodeTypes／EventContract 及其 Outcome／Roles 传递输入；真实 focused Fable 编译通过 54 parsed sources（fingerprint `a731717f814a`）。声明图仍为 26 subsystem、209 shard、701 source、1910 references，shard DAG，最大 subsystem SCC 22。此次证明的是这一 consumer 的闭包收窄，不代表其余宽 Host consumer 已迁移或 GAP-033 完成。

在 `be3054fab` 上并行核对 Grounding model 与 Relay workspace snapshot 的真实源码需求，分别将宽 Host 摘要引用替换为既有 `runtime-platform/digest`，无源码、签名或 aggregate 修改。按声明 ProjectReference 递归闭包计数，Grounding 从 8 项目／60 个 `.fs/.fsi` 输入降至 6／50，Relay snapshot 从 8／38 降至 4／14；前者仍保留 JS capability 所需的 Roles／OfficeCapability，后者仍保留 GitSubject 与 Relay core。两分片独立 Fable 编译分别通过 88、52 parsed sources（`91e59f8a1e03`、`068168b18cec`）；真实 Grounding Runtime consumer 通过 818／780，四条 Relay capture consumer 的并集通过 1284／1246（parsed sources／compile items；`9956ed957c82`、`6d4bccc9b9ff`）。编译按共享产物纪律串行，新隔离产物另实际执行规范材料与 Git snapshot 摘要 smoke，范围见所属 HOW。不将声明闭包计数当作耗时实验，也不以局部 consumer 收窄关闭 GAP-033。

在 `bd99d71e7` 上并行核对 Change 事实分片和 Host message codec。仅前者的宽 Host 引用可替换为既有 `runtime-platform/digest`：六个输入中只有 `RuntimePath` 需要字符串摘要，声明递归闭包从 29 项目／158 个 `.fs/.fsi` 输入降至 27／148；独立 Fable 编译通过 186 parsed sources（`7711a827c1d3`），三个既有签名的保守反向消费者并集通过 1432／1394（parsed sources／compile items，`6f04afa1af0a`）。新隔离 RuntimePath 产物的真实 Git 与非 Git 摘要 smoke 范围见 change-integration/HOW。Host message codec 实际依赖宽分片拥有的 `MessagePart`，不使用摘要，因此保留声明依赖，不按项目名批量替换。源码、签名和 aggregate 均不变；本批不证明全局 SCC 收敛或 GAP-033 完成。

在 `32f66d952` 上并行核对 Provider wire decoder 与 Git hook 的全部源码／签名，分别将宽 Host 引用收窄到既有 `runtime-platform/digest`。两者只需媒体 URL 摘要与 common-dir socket key，不消费 OpenCode 消息／事件类型；保留其余真实引用，源码、签名与 aggregate 均不变。声明递归闭包分别从 10 项目／48 个 `.fs/.fsi` 输入降至 7／26、从 51／250 降至 49／240；独立 Fable 编译分别通过 64、278 parsed sources（`3b3604da2356`、`f39c6d4c6b7b`）。先验证 decoder 的反向 consumer 再修改 hook 引用，最终两分片签名反向消费者的 flat 并集通过 1418 parsed sources／1380 items（`91bced9bf768`），包含真实 decoder 与 HostSignalBootstrap 消费路径。新隔离产物的媒体投影和临时 Git hook 安装 smoke 范围分别记录于 provider-projection/HOW 与 durable-convergence/HOW。上述计数不代表编译耗时改善或全局 SCC 收敛，GAP-033 仍为 PARTIAL。

在 `e5794c8f4` 上并行审查 Companion、Delegation recovery 与 shared-state 三个分片。前两者分别实际消费 `IEventObservationPort`／终端事件词汇和 `MessagePart.Text`，保留宽 Host 引用；三处机械替换摘要引用均不会缩小声明闭包，不作为隔离成果。shared-state 中的 OpenCode `GitTree.create`／`GitTreePort` 已无调用方，因此删除退役适配器、类型及当前编译／适用入口，保留 EventStore 同名模块和 Relay snapshot。

该批首次 shared-state 独立编译暴露 `IJoinAttemptRegistry`／构造器漏报依赖及失效 namespace 引入。将既有 registry 的 sibling 源码移入窄 `delegation/join-attempt-registry` 分片，原 recovery 与 shared-state 显式消费它；只依赖 Identity、causal-wait contract、AsyncSupport，不以 recovery umbrella 或动态加载补回类型。registry 与 repaired shared-state 独立 Fable 编译分别通过 46、578 parsed sources（`5612752d421a`、`cf8198d664c9`），GitSubject／registry／session scope／recovery scope 签名反向消费者并集通过 1430 parsed sources／1392 items（`f31b44cd590f`）。新隔离 shared-state smoke 范围见 host-boundary/HOW。

registry 声明闭包为 4 项目／8 输入；shared-state 从基准 87／538 变为 89／540，增长来自补齐真实 registry／wait contract，而非耦合收敛。结构 gate 为 26 subsystem、210 shard、700 source、1913 references，shard DAG，最大 SCC 22；相关 Host／Delegation／subsystem 结构测试 8/8 通过。删除死适配器与恢复可编译边界不代表全局 SCC 缩小或 subsystem 已可独立替换，GAP-033 保持 PARTIAL。

在 `8e9e6d9f1` 上将既有 `OpenCode/Host/Message.fs/.fsi` 从宽 Host 分片移入零 ProjectReference 的 `host/host-message-contract`，HostMessageCodec 直接引用它，宽 Host 分片仍引用唯一消息定义。未改消息 union、decoder 实现、公开签名或 aggregate 顺序。按同一 production compile-shard inventory 递归计数，codec 声明闭包从 7 项目／32 输入降至 3／8，移除 OpencodeTypes、EventContract、Digest 及其传递输入；消息合同自身为 1／2。首轮 subsystem gate 拒绝将 requirement package 名 `host-boundary` 当作 subsystem，修正为现行映射的 `host`，未新增 alias 或放宽 gate。

`host-boundary/tests/m6-slice-boundary.test.mjs` 的既有 codec audience 证明新增实际 transitive source closure 排除：临时恢复宽引用时以 `message codec must not acquire .../OpencodeTypes.fs` 失败，恢复窄引用后通过。codec 独立 Fable 编译通过 46 parsed sources；Message 与 HostMessageCodec 签名的真实反向消费者并集通过 1426 parsed sources／1388 items。新消费者产物的 `ProviderProjectionSurface.decodeHostParts` smoke 验证 Unicode／CRLF text、reasoning、pending/completed/error tool、canonical args、activity normalization 与未知／空白分段丢弃，不读取 Fable union 布局。它不替代真实 Host 时序 canary。

同批并行审查保留 SyncDelegate runtime 的 `TerminalCompletionListener`／`TerminalStop`／`TerminalOutcome` 依赖，以及 recovery model 的 `ReconciledTurn.Parts`／`OpencodeModel` 和 `QuiescencePermit` 合同；它们不能机械替换为摘要或消息合同。仅 codec 的无关知识闭包已切断，未宣称全局 SCC 缩小或 subsystem 已可独立替换，GAP-033 保持 PARTIAL。

在 `cd6ef0ded` 上将既有 `OpenCode/Codec/OpencodeTypes.fs/.fsi` 独立到零引用的 `host/host-opencode-types`，宽 Host 分片引用唯一 SDK 类型定义，`OpenCodeContract` 与 `strength-policy` 直接消费窄分片。两 consumer 只使用 `OpencodeModel`，不修改源码、签名或 aggregate 顺序。声明递归闭包分别从 7 项目／28 输入降至 5／22、55／338 降至 54／334；policy 仍保留其余真实域依赖及传递摘要原语，不宣称整个 policy 已纯化。

先验证端口隔离编译及实际反向 consumer，再迁移 policy。最终端口、policy 独立 Fable 编译分别通过 60、372 parsed sources（`49421f904449`、`0b1e1c80861a`）；三组 SDK／端口／routing 签名反向 consumer 的 flat 并集通过 1426 parsed sources／1388 items（`dde50e2f81ac`）。既有 HOST-BOUNDARY-026 闭包测试在端口旧引用和 policy 宽引用 mutation 下分别因 EventContract 被编入而失败，窄引用下通过。新产物的公开 SDK prompt projection smoke 范围见 execution-model-routing/HOW。

同批完整审查 guidance-tip：唯一直接宽 Host 符号是 `stableCallId` 的 `HostDigest.sha256Hex`，但 production inventory 实测其余引用仍保留宽闭包。基准 112 项目／770 输入，在 SDK 分片迁移后为 113／770，假设将 guidance 引用替换为 digest 仍为 113／770；因此本批保留，不将直接符号收窄误报为闭包收益。结构 gate 为 26 subsystem、212 shard、700 source、1915 references，shard DAG，最大 SCC 22；Host／subsystem 相关检查 13/13 通过。GAP-033 仍为 PARTIAL，局部闭包下降不证明全局 SCC 收敛或 subsystem 已可独立替换。

在 `26bfed9d0` 上继续审查终端事件 audience。既有 `host-digest` 分片只保留 EventContract 的 identity/outcome 引用，不再转接 SDK types、MessagePart 或 digest；SessionSnapshot 显式引用唯一消息合同。diagnostics 与 message visibility 没有终端知识，移除其引用。声明递归闭包（项目／输入）分别从 terminal 7／26、diagnostics 14／62、visibility 11／36、SyncDelegate 17／66、session contract 9／34 收到 4／20、10／54、5／12、14／60、8／32。

同批 signal adapter 的独立 Fable 编译拒绝既有漏报的 ExecutionFailure、ChatExecutionTerminalDisposition 与 RuntimePath。先补齐实际 provider 引用并独立编译通过，再继续合同收窄；只删除 adapter 与 snapshot sibling 中无用途的 ingress namespace 引入，没有改变业务或公开签名。修复后的 adapter 闭包从漏报的 12／60 增至 35／198，不能把旧缺失输入当作隔离成果；RuntimePath 仍需要 digest。六个相关分片最终独立 Fable 编译分别通过 58、92、50、98、70、236 parsed sources；EventContract／SessionSnapshot／ReliabilityDiagnostics／MessageVisibility 签名反向消费者合并为一次 flat compile，通过 1426 parsed sources／1388 items（`0e6946de9286`）。初次 snapshot 独立编译拒绝失效 namespace，删除无用 open 后通过，没有把 namespace 名重新变成宽引用。

既有 HOST-BOUNDARY-026 回归新增真实闭包性质：三个独立的 terminal 宽引用 mutant 与恢复 adapter 漏依赖的 mutant 均退出 1。新产物的 terminal replay／authority／dispose、diagnostic optional effect 与 visibility signal／deadline smoke 范围见 host-boundary/HOW。结构 gate 为 26 subsystem、212 shard、700 source、1914 references，shard DAG，最大 SCC 22；GAP-033 保持 PARTIAL，不以局部减少抵消漏依赖修复，也不宣称全局 SCC 收敛或编译耗时下降。

在 `26b439645` 上将既有 ToolHostCodec／ToolHostSurface 四个源码与签名输入转入显式 `host/host-tool-adapter` 分片，公开类型、函数、物理实现与 aggregate 顺序不变。完整审查十五个直接 consumer：十三个只直接使用工具知识，改引工具适配器；bootstrap 同时使用 HostIngressCodec 与信号／终端实现，显式引用两侧；SessionExecutionBinding 只使用 ExactProviderStartObservation，保留信号引用。所有其他工具符号消费者的传递闭包仍有唯一 provider，没有复制实现或运行时回捞。

声明递归闭包口径为项目／`.fs/.fsi` 输入。工具 audience 从原宽 adapter 的 35／198 收为 5／26；signal adapter 自身从 35／198 收为 33／186。Attention fold 59／356 → 56／332，Concern fold 58／352 → 55／328，InstitutionalLearning fold 61／362 → 59／340，filemutationtools 99／616 → 97／594，repository-programming runtime 100／628 → 98／606，BookkeeperTool 101／636 → 99／614，FetchTool 99／622 → 97／600；这七条路径不再编入 HostSignal、HostEventCodec、Events 与 SharedTerminalBus。其余直接迁移路径仍经真实依赖保留信号闭包：executor tools 143／986 → 144／986，action runtime 146／1004 → 147／1004，PTY tools 141／968 → 142／968，review 141／964 → 142／964，retirement 141／962 → 142／962，plugin composition 190／1244 → 191／1244，bootstrap 166／1118 → 167／1118。后七项只完成正确 provider cutover，不计源码闭包收益。

tool adapter、signal adapter、Attention consumer 与 repository-programming runtime 分别独立 Fable 编译通过 64、224、370、644 parsed sources；ToolHostCodec／HostEventCodec 签名的真实反向消费者合并为一次 flat compile，通过 1416 parsed sources／1378 items（`bafa41b8e0a7`）。新编译边界回归先在旧混装闭包失败，再分别拒绝工具、consumer 与信号侧重新耦合的三个引用 mutant。新产物的 SDK schema、exact identity、abort disposal、结果尾部界限与异常 smoke 范围见 host-boundary/HOW。结构 gate 为 26 subsystem、213 shard、700 source、1917 references，shard DAG，最大 SCC 22；局部闭包变小不等于全局 SCC 收敛、subsystem 可替换性已证明或实际编译耗时下降，GAP-033 保持 PARTIAL。

在 `8903c729c` 上核对 Host 现行规范时，发现 HOST-BOUNDARY-026 仍将摘要、消息、SDK 类型和工具注册写入旧宽 Host 边界，并将普通业务契约限制为两个旧合同。用户于 2026-09-10 明确批准同步合同；本批仅修订 WHAT/HOW 与导航，保留物理能力隔离及既有证明，不改变实现或以此关闭 GAP-033。另确认 `host-session-contract-closure.test.mjs` 仍依赖 legacy locality/kind、100/185 数量断言及 legacy owner 文件选择；这些检查尚未完全迁到 subsystem inventory，显式 Host 分片的归属仍由全仓 subsystem gate 证明。后续迁移必须保留真实闭包排除、必要 provider 与唯一归属反例，不能恢复旧 ACL 或仅删除测试取得绿色。

在 `a0a710fb2` 上完成上述 Host 验证迁移：闭包测试直接消费既有 compile-shard/subsystem inventory，不再维护 XML parser、legacy kind、数量预算或 legacy owner 文件集合。保留真实 provider 与 composition 引用保护；会话合同排除工具注册、信号订阅和终端总线，新增 Host 分片与摘要原语按实际 subsystem 核对。显式元数据替换旧声明的正例通过；会话误引工具、会话归属错误及显式工具分片归属错误的三个真实工程反例均被拒绝，原工程全部恢复。全仓唯一来源、签名与 aggregate 完整性仍由同一库存机制及既有结构反例承接。本批不改生产编译输入，不宣称新增编译隔离或全局 SCC 收敛，GAP-033 保持 PARTIAL。

在 `46a1c334c` 上将 `owner-project-boundaries.test.mjs` 的生产工程读取统一到既有 `checkSubsystems` 返回的库存，删除重复工程扫描、Compile／ProjectReference XML parser 与 GitGateway 的旧 locality 标签要求。GitGateway 归属核对现行 `change` subsystem，而不是 legacy owner `change-integration`；源码配对、必要／禁止引用和 NodeFs 物理边界仍受原测试保护。只删除库存已经覆盖的非空工程断言及无合同依据的项目数量下限；临时 planner 工程的 XML、顺序、缓存与失败生命周期证明不变，原测试标题及证明注册不变。恢复原字节的实验中，GitGateway 仅保留显式 subsystem／shard 元数据通过，错误归属到 persistence 被拒绝，FileMutationTools 缺失 NodeFs provider 被拒绝。相关 boundary／subsystem／impact 测试 24/24 通过；本批不修改生产工程，不以静态证明迁移宣称新 focused Fable 隔离或业务等价，GAP-033 保持 PARTIAL。

在 `f3479bdf6` 上将 `owner-impact-compile.test.mjs` 的六个临时工程改为显式 subsystem／compile-shard 元数据，去掉夹具的旧 owner／locality／kind 参数。全部分片同属 fixture subsystem，原有精确 source／root 集合断言仍要求实现变更不扩入反向 consumer、签名变更包含全部反向 consumer；合并编译、full fallback、缓存与 CLI 证明及其标题／注册不变。相关测试 24/24 通过；临时将实现变更扩成 reverse closure、将签名变更缩成 owning shard 的两个 planner mutant 均被既有断言拒绝，planner 原字节恢复。本批仅更新证明输入，没有改变生产 planner、工程或公开合同，不是新的真实 focused Fable 编译或知识依赖收敛证据，GAP-033 保持 PARTIAL。

在本次推进中，将 `event-store-compile-boundary.test.mjs` 与 `ctx-capacity-observation-forbidden.test.mjs` 统一迁移至 compile-shard／subsystem inventory。删除自建 readdirSync/XML regex 解析与 legacy kind、legacy owner 属性依赖；按 persistence／strength subsystem 与显式 compile shard 验证闭包排除、生产源码预算及禁止容量词汇。相关边界测试通过；非契约分片侵入契约闭包与错误 subsystem 的两个反例均被拒绝。同时，完成 `causal-wait/tests/m6-slice-boundary.test.mjs`、`time-capability/tests/m6-slice-boundary.test.mjs` 与 `host-boundary/tests/m6-slice-boundary.test.mjs` 向标准 `readCompileShardInventory` + `buildSubsystemInventory` 的迁移，移除了对旧 `readCompileShardInventoryV1` helper 及其 legacy locality/kind 属性的依赖，并彻底删除了该已退役 helper。随后推进 participant、sphinx、process 与 verification 四个子系统全部剩余分片声明显式 `<WanxiangshuSubsystem>` 与 `<WanxiangshuCompileShard>` 元数据，使这四个子系统（含 `foundation-roles` 重力井）全部达成 100% 显式归属。进一步将 change（5分片）、strength（5分片）、resources（2分片）、output（2分片）与 repository-investigation（2分片）共计 15 个分片全部声明显式 `<WanxiangshuSubsystem>` 与 `<WanxiangshuCompileShard>` 元数据，使这五个子系统达成 100% 显式归属。随后推进 repository-programming（3分片）、requirements（3分片）、dispatch（4分片）、knowledge（4分片）与 work（5分片）共计 19 个分片全部补齐显式 `<WanxiangshuSubsystem>` 与 `<WanxiangshuCompileShard>` 元数据，使这五个子系统同样达成 100% 显式归属。进一步推进 authority（7分片）、relay（8分片）、chat-execution（6分片）与 enforcer（7分片）共计 28 个分片全部补齐显式 `<WanxiangshuSubsystem>` 与 `<WanxiangshuCompileShard>` 元数据，使这四个子系统达成 100% 显式归属。紧接着推进 interaction（10分片）与 context（13分片）共计 23 个分片全部补齐显式 `<WanxiangshuSubsystem>` 与 `<WanxiangshuCompileShard>` 元数据，使 interaction 与 context 两个子系统同样达成 100% 显式归属。随后进一步推进 session-lifecycle（19分片）与 delegation（16分片）共计 35 个分片全部补齐显式 `<WanxiangshuSubsystem>` 与 `<WanxiangshuCompileShard>` 元数据，使 session-lifecycle 与 delegation 两个子系统达成 100% 显式归属。全仓显式分片数量提升至 162/213（占比 76%），全仓 26 个子系统中已有 23 个达成全显式归属。随后进一步推进 persistence（17分片）、provider（17分片）与 host（17分片）最后 51 个分片全部补齐显式 `<WanxiangshuSubsystem>` 与 `<WanxiangshuCompileShard>` 元数据，使全仓全部 26 个子系统、213 个编译分片达成 100% 显式归属（213/213，0 legacy-mapped 遗留）。随后审查 interaction 与 process 间的编译引用，发现 `opencode-tools-executortoolsurface` 仅拥有 ChronicleTool、BashHoneypotTool 与 AssumeTool 三个工具实现，源码及签名并未消费 PtyTool 或 ExecutorTool，但多声明了对 `process/opencode-tools-ptytool` 的引用。删除该无用引用后，该分片的前向闭包从 144 个项目缩减为 141 个项目；`opencode-tools-executortoolsurface` 独立 Fable 编译（1012 parsed sources）以及其消费者 `tool-runtime-surface` 独立 Fable 编译（1042 parsed sources）均通过。接着审查 persistence 与 change 间的编译引用，发现 `persistence/git-hook-sync`（`Wanxiangshu.Owner.durable-convergence.git-hook-sync.fsproj`）的实现与签名源码仅消费 GitGateway、EventStoreProcessEventLog 与 EventStoreSyncRuntime 等分片，并未消费 `change/change-fact`（ChangeFact、Projection、RuntimePath），但仍保留该跨 subsystem 引用。删除该无用引用后，`git-hook-sync` 独立 Fable 编译（278 parsed sources）及其真实消费者 `opencode-host-hostsignalbootstrap`（1156 parsed sources）与 `plugin-composition`（1282 parsed sources）均通过独立编译。全仓 ProjectReference 引用数降至 1915，相关结构与边界测试全部通过，全量编译与阶梯验收通过，GAP-033 保持 PARTIAL。

随后进一步系统审查跨 subsystem 的 ProjectReference 依赖，严格依据源码符号使用情况剔除冗余依赖：
1. `interaction/opencode-tools-executortoolsurface`（`Wanxiangshu.Owner.action-affordance.opencode-tools-executortoolsurface.fsproj`）：确认其三个工具源码（ChronicleTool、BashHoneypotTool、AssumeTool）均不消费 `host/host-session-contract`（QuiescencePermit、DegenerationKind、ILoopSensor 等），移除该跨 subsystem 引用。经独立 Fable 编译（1012 parsed sources）及其消费者 `action-affordance.runtime`（1042 parsed sources）编译验证通过。
2. `enforcer/enforcer-institutionallearning-fold`（`Wanxiangshu.Owner.institutional-learning.enforcer-institutionallearning-fold.fsproj`）：源码不使用 `host/host-signal-contract`（TerminalStop、TerminalOutcome、IEventObservationPort）与 `runtime-platform/bounded-parallel`，移除这两个冗余 ProjectReference。经独立 Fable 编译（372 parsed sources）及其直接消费者 `strength-persistence-durabilityport`（618 parsed sources）和 `opencode-host-hostsignalbootstrap`（1156 parsed sources）编译验证通过。
3. `knowledge/repository-knowledge-casebook-bookkeeper`（`Wanxiangshu.Owner.knowledge-reuse.repository-knowledge-casebook-bookkeeper.fsproj`）：源码不使用 `participant/foundation-roles`（Role/Roles）与 `host/host-session-contract`，移除这两个冗余 ProjectReference。经独立 Fable 编译（774 parsed sources）及其消费者 `repository-knowledge-casebook-lifecyclesurface`（780 parsed sources）与 `knowledge-reuse.runtime`（790 parsed sources）编译验证通过。

全仓 ProjectReference 引用总数进一步从 1915 降至 1910。全套静态架构与规范检查 `node scripts/check.mjs`、子系统门禁 `node scripts/checks/subsystems.mjs` 以及结构工作流测试全部保持绿色通过，GAP-033 保持 PARTIAL。

紧接着进一步深度审查 enforcer 子系统内部编译分片的跨 subsystem 冗余依赖：
1. `enforcer/enforcer-codec`（`Wanxiangshu.Owner.behavior-diagnosis.enforcer-codec.fsproj`）：经对 ChronicleExecution、Surface、Commit、Recovery、RepairSurface 与 ObservationSurface 的源码及签名完整审查，确认其未消费 `chat-execution/outcome`、`host/host-digest`、`authority/interaction-authority-fact`、`authority/interaction-authority-identityseed`、`authority/interaction-authority-ledger` 以及 `context/context-trace-cursor`。移除这 6 条冗余引用后，独立 Fable 聚焦编译（542 parsed sources）以及其消费者 `opencode-tools-executortoolsurface`（1012 parsed sources）与 `enforcer-continuation`（1010 parsed sources）均编译通过。
2. `enforcer/enforcer-continuation`（`Wanxiangshu.Owner.behavior-diagnosis.enforcer-continuation.fsproj`）：确认其源码与签名不使用 `persistence/composition-durable-projection`（CompositionDurableProjection 等），移除该跨 subsystem 引用。独立 Fable 编译（1010 parsed sources）通过。
3. `enforcer/repair`（`Wanxiangshu.Owner.behavior-diagnosis.enforcer-repair.fsproj`）：确认其源码与签名不使用 `host/host-digest`（HostDigest），移除该跨 subsystem 引用。独立 Fable 编译（410 parsed sources）以及消费者 `context-companion-companionfactfold`（530 parsed sources）编译通过。

全仓 ProjectReference 引用总数从 1910 降至 1902（减少 8 条冗余跨 subsystem 依赖）。全套静态架构检查 `node scripts/check.mjs` 与子系统门禁 `node scripts/checks/subsystems.mjs` 均保持绿色通过，GAP-033 保持 PARTIAL。

继续推进全仓真实跨 subsystem 知识依赖审查与裁剪，对 7 个编译分片中无符号消费的跨 subsystem 引用进行剪枝：
1. `resources/resources-promptsurface`（`Wanxiangshu.Owner.cognitive-environment.resources-promptsurface.fsproj`）：移除对 `runtime-platform/llm-facing` 的无用引用；
2. `context/context-companion-companionfactfold`（`Wanxiangshu.Owner.context-compression.context-companion-companionfactfold.fsproj`）：移除对 `chat-execution/outcome` 的无用引用；
3. `context/context-compression-runtime-surface`（`Wanxiangshu.Owner.context-compression.runtime.fsproj`）：移除对 `chat-execution/outcome` 的无用引用；
4. `delegation/delegation-fold`（`Wanxiangshu.Owner.delegation.execution-delegation-handle-surface.fsproj`）：移除对 `context/context-trace-cursor` 的无用引用；
5. `delegation/delegation-ledger`（`Wanxiangshu.Owner.delegation.execution-delegation-ledger.fsproj`）：移除对 `persistence/composition-durable-fact` 的无用引用；
6. `persistence/strength-persistence-durabilityport`（`Wanxiangshu.Owner.durable-events.strength-persistence-durabilityport.fsproj`）：移除对 `context/context-prefix-candidate` 的无用引用；
7. `provider/strength-policy`（`Wanxiangshu.Owner.execution-model-routing.strength-policy.fsproj`）：移除对 `chat-execution/outcome` 的无用引用。

上述 7 个分片及代表性消费分片（如 `delegation-ledger`、`delegation-recovery-runtime`、`enforcer-guidance-tip`、`persistence-eventstore-canonicalintegrator`、`opencode-host-modelroutingsurface`）均通过独立 Fable 聚焦编译验证。全仓 ProjectReference 引用总数从 1902 降至 1895（净减 7 条跨 subsystem 依赖），子系统门禁与架构检查全绿。

在此基础上，持续深入审查 22-subsystem SCC 内的跨 subsystem 知识依赖，剔除 6 条无真实符号消费的冗余引用，并补齐 2 处真实编译消费声明：
1. `delegation/execution-delegation-hostturnobservedsurface`（`Wanxiangshu.Owner.delegation.execution-delegation-hostturnobservedsurface.fsproj`）：移除对 `provider/opencode-host-opencodeport` 的冗余引用；
2. `host/host-diagnostics-runtime`（`Wanxiangshu.Owner.host-boundary.host-diagnostics-runtime.fsproj`）：移除对 `chat-execution/outcome` 的冗余引用；
3. `knowledge/repository-knowledge-casebook-lifecyclesurface`（`Wanxiangshu.Owner.knowledge-reuse.repository-knowledge-casebook-lifecyclesurface.fsproj`）：移除对 `host/host-signal-contract` 的冗余引用；
4. `work/mission-obligation-todo-magictodosemanticsurface`（`Wanxiangshu.Owner.obligation-ledger.mission-obligation-todo-magictodosemanticsurface.fsproj`）：移除对 `chat-execution/outcome` 的冗余引用；
5. `process/opencode-tools-ptytool`（`Wanxiangshu.Owner.process-execution.opencode-tools-ptytool.fsproj`）：移除对 `chat-execution/outcome` 的冗余引用；
6. `provider/participant-provider-attempt-planner`（`Wanxiangshu.Owner.provider-attempt-recovery.participant-provider-attempt-planner.fsproj`）：移除对 `chat-execution/outcome` 的冗余引用；
7. 显式补齐 `fetchtool` 对 `casebook-bookkeeper` 及 `sessionexecutionbinding` 对 `opencode-tools-managedagent` 的真实类型声明依赖，并清理 `ManagedAgentConfig.fs` 中未使用的 `Manager` 命名空间 open。

上述分片及其代表性消费分片（如 `join-guard-surface`、`plugin-composition`、`action-affordance.runtime`、`opencode-host-managedagentconfig`、`enforcer-repair`、`sessionexecutionbinding`）均通过独立 Fable 聚焦编译验证。全仓 ProjectReference 引用总数降至 1890，结构与架构门禁全绿通过。

进一步深入清理 10 个源文件中未使用的 `open Wanxiangshu.Host.Contract`、`open Wanxiangshu.Strength` 与 `open Wanxiangshu.Strength.Prediction` 残留语句（如 `Context/Trace/Capture.fs`、`Execution/Delegation/SyncDelegate/Runtime.fs` 及 Fork/Host 运行期源码），彻底消除对宽命名空间的隐式伪依赖。在此基础上，系统审计并移除 12 条无真实符号消费的跨 subsystem ProjectReference：
1. `intra-participant-parallelism/execution-fission-opencode-host`（`Wanxiangshu.Owner.intra-participant-parallelism.execution-fission-opencode-host.fsproj`）：移除对 `execution-model-routing/opencode-host-opencodeport` 的冗余引用；
2. `provider-projection/opencode-codec-providerprojectionsurface`（`Wanxiangshu.Owner.provider-projection.opencode-codec-providerprojectionsurface.fsproj`）：移除对 `host-boundary/host-digest` 的冗余引用；
3. `requirement-grounding/opencode-host-requirementgrounding-runtime`（`Wanxiangshu.Owner.requirement-grounding.opencode-host-requirementgrounding-runtime.fsproj`）：移除对 `dispatch-protocol/foundation-outcome` 的冗余引用；
4. `guidance-delivery/enforcer-guidance-tip`（`Wanxiangshu.Owner.guidance-delivery.enforcer-guidance-tip.fsproj`）：移除对 `host-boundary/host-digest` 的冗余引用；
5. `interaction-authority/interaction-authority-fold`（`Wanxiangshu.Owner.interaction-authority.interaction-authority-fold.fsproj`）：移除对 `host-boundary/host-digest` 的冗余引用；
6. `managed-chat-execution/opencode-host-chatadmission-transaction`（`Wanxiangshu.Owner.managed-chat-execution.opencode-host-chatadmission-transaction.fsproj`）：移除对 `host-boundary/host-digest` 的冗余引用；
7. `participant-horizon/execution-session-opencode-horizontool`（`Wanxiangshu.Owner.participant-horizon.execution-session-opencode-horizontool.fsproj`）：移除对 `host-boundary/host-digest` 的冗余引用；
8. `relay-assessment/mission-relay-reviewtool`（`Wanxiangshu.Owner.relay-assessment.mission-relay-reviewtool.fsproj`）：移除对 `host-boundary/host-digest` 的冗余引用；
9. `relay-retirement/mission-relay-suicidetool`（`Wanxiangshu.Owner.relay-retirement.mission-relay-suicidetool.fsproj`）：移除对 `host-boundary/host-digest` 的冗余引用；
10. `semantic-trace/context-trace-semantictracesurface`（`Wanxiangshu.Owner.semantic-trace.context-trace-semantictracesurface.fsproj`）：移除对 `host-boundary/host-digest` 的冗余引用；
11. `speculative-investigation/strength-opencode-settings`（`Wanxiangshu.Owner.speculative-investigation.strength-opencode-settings.fsproj`）：移除对 `host-boundary/host-digest` 的冗余引用；
12. `speculative-investigation/strength-turnevidence`（`Wanxiangshu.Owner.speculative-investigation.strength-turnevidence.fsproj`）：移除对 `host-boundary/host-digest` 的冗余引用。

上述修改的分片及其代表性消费分片（包含 `delegation-host-adapter`、`managedagentconfig`、`plugin-composition` 等）均通过独立 Fable 聚焦编译验证。全仓 ProjectReference 引用总数降至 1878（净减 12 条跨 subsystem 依赖），结构与架构门禁全绿通过。

进一步审计 `output` 子系统的 `process-largegatesurface`（`Wanxiangshu.Owner.output-distillation.process-largegatesurface.fsproj`）：
- 清理 `DistillationRuntime.fs` 与 `Distillation.fs` 中未使用的 `open Wanxiangshu.Strength` 及 `open Wanxiangshu.Strength.Prediction` 语句；
- 将原对混装了 OpenCode 消息/终端类型的 `host/host-digest` 宽引用收窄至零引用的 `runtime-platform/digest`（源码仅需 `HostDigest.sha256Hex` 进行散列计算）；
- 审计确认其源码（`LargeGateSurface.fs`、`DistillationRuntime.fs`、`Distillation.fs`）完全不消费 `dispatch/execution-session-recovery-model` 与 `process/process-processrequest` 符号，安全剔除这两条冗余跨 subsystem ProjectReference。
经独立 Fable 聚焦编译（610 sources）及其直接消费分片 `opencode-tools-ptytool`（1006 sources）双向验证通过，全仓声明引用总数降至 1876，结构与架构门禁全绿通过。

继续系统审计 22-subsystem SCC 中的稀疏跨子系统边，重点审查 `relay -> chat-execution` 与 `process -> chat-execution` 路径：
1. `relay/mission-relay-suicidetool`（`Wanxiangshu.Owner.relay-retirement.mission-relay-suicidetool.fsproj`）：确认其源码（`SuicideTool.fs`）均不消费 `chat-execution/outcome`，移除该冗余跨 subsystem 依赖，彻底切断 `relay -> chat-execution` 跨子系统边。经独立 Fable 编译（1040 sources）以及消费者 `managedagentconfig`（1034 sources）与 `plugin-composition`（1236 sources）验证通过。
2. `process/foundation-temporal`（`Wanxiangshu.Owner.time-capability.foundation-temporal.fsproj`）：确认其源码（`DeadlineSurface.fs`、`Surface.fs`）完全不消费 `chat-execution/outcome`，移除该冗余跨 subsystem 依赖，彻底切断 `process -> chat-execution` 跨子系统边。经独立 Fable 编译（310 sources）验证通过。

全仓 ProjectReference 引用总数降至 1874（净减 2 条跨 subsystem 边），结构与架构门禁全绿通过。

在 `82d5c2513` 上将 `output/opencode-tools-distillationsurface` 从误挂的 `context-companion-runtimesurface` 收窄：源码不消费 companion runtime 符号，声明图降至 1873 references；其余计数不变，shard DAG，最大 SCC 仍为 22。

本批继续按真实符号消费审计稀疏跨 subsystem ProjectReference，并在聚焦 Fable 编译失败时改挂实际消费的窄合同，而不是恢复宽伞引用：
1. `process/opencode-tools-ptytool`：移除未使用的 `delegation/execution-delegation-fact`（1006 sources）。
2. `process/foundation-temporal`：移除未使用的 `delegation/execution-delegation-syncdelegate-model`、`persistence/composition-durable-projection`、`persistence/persistence-journal-promptfactcodec`、`provider/participant-provider-language`（122 sources）。
3. `repository-programming/opencode-tools-filemutationtools`：移除未使用的 `chat-execution/outcome`、`persistence/strength-persistence-durabilityport`、`runtime-platform/bounded-parallel`（632 sources）。
4. `knowledge/repository-knowledge-casebook-model`：移除仅作传递闭包的 `persistence/strength-persistence-durabilityport`；聚焦编译暴露真实直接消费 `FatalProcess` 与 `ToolHostCodec`，改挂 `host/host-fatal-effect` 与 `host/host-tool-adapter`（274 sources）。
5. `relay/mission-relay-reviewtool`：移除未使用的 `host/opencode-host-sessionquiescencegate`（SessionSnapshot 来自 host-session-contract）（1002 sources）。
6. `work/mission-obligation-todo-magictodosemanticsurface`：仅移除未使用的 `runtime-platform/bounded-parallel`；保留 `host/host-digest`（`HostDigest.sha256Hex`）（792 sources）。
7. `authority/interaction-authority-fold`、`enforcer/enforcer-continuation`、`enforcer/execution-session-loopdetector`：各移除未使用的 `chat-execution/outcome`（426／1010／82 sources）。
8. `authority/interaction-repair-interactionrepair`：移除未使用的 `chat-execution/outcome`、`dispatch/interaction-dispatch-opencode-ingresscodec`、`provider/participant-provider-attempt-fallback-ledger`、`provider/participant-provider-attempt-requestkind`（546 sources）。
9. `change/git-integrationgate`：移除未使用的 `delegation/execution-delegation-handle-surface`、`persistence/strength-persistence-durabilityport`、`host/opencode-host-sharedstatesurface`、`runtime-platform/bounded-parallel`、`provider/participant-provider-language`、`session-lifecycle/process-node-timing-adapter`；清理 Host.fs 中对应未使用 `open`；保留仍消费 `LlmFacing` 的 `foundation-llm-facing`（934 sources）。
10. `strength/strength-opencode-settings`：移除未使用的 `participant/opencode-tools-managedagent` 与 `persistence/persistence-eventstore-storetypes`。
11. `strength/strength-prediction-predictor`：移除未使用的 `participant/opencode-tools-managedagent`、`chat-execution/outcome`、`provider/strength-policy`；并删除七处无符号消费的 `open Wanxiangshu.Host.Contract`（464 sources）。

审计中拒绝的假阳性包括：`context-prefix-wire` 的 outcome／host-digest（`AttemptOutcome`／`HostDigest` 仍用）、`magictodo`／`requirementgrounding` 的 host-digest、以及 `provider-languagesurface` 的 foundation-identity（`SessionId.create`）。取消中的错误编辑曾破坏 `context-prefix-wire.fsproj`，已从 git 还原，未纳入本批。

当前声明图为 26 subsystem、213 shard、700 source、1846 references，shard DAG，最大 subsystem SCC 22；`node scripts/checks/subsystems.mjs` 与 `subsystem-boundaries.test.mjs` 4/4 通过。净减相对上一记录点（1873）为 27 条声明引用；SCC 未缩小，GAP-033 保持 PARTIAL。

在 `bce851f5f` 之后继续审计尚未覆盖的稀疏边与大型 composition 分片。多数 1–2 边分片的跨 subsystem 引用均为真实符号消费（KEEP）；可剪项集中在部分 composition／persistence 分片：
1. `resources/resources-promptsurface`：移除未使用的 `participant/foundation-roles`。
2. `delegation/execution-fission-fold`：移除未使用的 `persistence/persistence-journal-promptfactcodec`。
3. `dispatch/plugin-composition`：移除未使用的 `repository-programming/opencode-tools-filemutationtools`。
4. `persistence/persistence-journal-promptfactcodec`：移除未使用的 `delegation/execution-delegation-fact`、`context/context-trace-cursor`、`context/context-trace-model`；并删除 `ProjectionUpdate.fs` 中无用的 `open Wanxiangshu.Context.Trace`。
5. `session-lifecycle/opencode-host-pluginruntimescope`：移除未使用的 `delegation/delegation-recovery-runtime`。
6. `context/context-companion-companionfactfold`：移除未使用的 `provider/strength-policy`。
7. `provider/composition-turn-ordinaryturnworkflow`：移除未使用的 `chat-execution/outcome`、`authority/interaction-authority-identityseed`、`authority/opencode-host-chatadmission-intent`、`context/context-trace-capture`；并删除无用的 `open Wanxiangshu.Host.Contract`。
8. 另清理 12 个源文件中无符号消费的 `open Wanxiangshu.Host.Contract`（Scheduler、Workflow、PluginScope、DispatchSurface、HostCompactionObserver、HostSignalBootstrap、ManagedAgentConfig、PluginRecoveryScope、PluginSessionScope、PluginHooks、PluginTransforms、ToolRegistry）；`ToolHostCodec`／Casebook Surface 仍保留对 `ToolResultBound` 的真实消费。相关 focused Fable 编译（含 managedagentconfig、hostsignalbootstrap、ordinaryturnworkflow、plugin-composition、promptsurface、fission-fold、journal codec、pluginruntimescope、companionfactfold）均通过。

当前声明图为 26 subsystem、213 shard、700 source、1834 references，shard DAG，最大 subsystem SCC 22；相对 `1846` 净减 12 条声明引用。SCC 未缩小，GAP-033 保持 PARTIAL。下一刀仍应优先真实知识归属迁移（重力井），稀疏边继续剪枝收益递减。

在 `c82dc213b` 上继续 GAP-033：先对重力井做只读审计，再落地真实知识归属迁移，并对确认零符号消费的边并行剪枝。

**知识归属迁移（本批主刀）：** `OpenCode/Host/SharedState.fs/.fsi` 原与 `ProviderRunBinding*` 混编在 `host/opencode-host-providerrunbinding`。SharedState 只依赖 companion Blogger 类型（`BloggerRequestContext`／`BloggerMaterializationAdmission`），与 Role 词汇无关；ProviderRunBinding 只需要 `SessionMessage`／`SessionSnapshotSurface`（经 `opencode-host-sessionquiescencegate`）。本批新建 `host/opencode-host-sharedstate` 承接 SharedState，从 providerrunbinding 删除 SharedState 编译项及其 companion-factfold／companion-fact 引用；SharedState 消费方（sharedstatesurface、companion foldsurface、companion runtime、pluginruntimescope）改挂 sharedstate。聚焦编译暴露 `sessionquiescencegate` 的 `SessionSnapshotPort` 直接消费 `taskResult`，原先靠 providerrunbinding→companion 传递闭包碰巧提供，改挂窄合同 `runtime-platform/task-result`（`foundation-taskresult`）。`strength-opencode-settings` 仍引用 providerrunbinding 以消费 `ProviderRunBinding.bindableRun`，不再被迫闭合 companion SharedState 知识。

**伴随零符号剪枝（均经符号复核 + focused Fable compile）：**
1. 去 `providerrunbinding`：`context-companion-runtimesurface`、`interaction-dispatch-opencode-ingresscodec`、`opencode-host-hostsignalbootstrap`（后者经 pluginruntimescope／PluginSessionScope 使用 SessionParents，不直接消费 SharedState／ProviderRunBinding 模块）。
2. 去 `foundation-roles`：`execution-session-chatexecution-fold`；`execution-session-sessionstartedatledger` 同时去 `opencode-tools-managedagent`。
3. 去误挂的 `host-signal-contract`（fsproj 名仍为 `host-digest`，仅 EventContract）：`context-companion-foldsurface`（并去未用的 `host-session-contract`，清理 PluginScope 三处无用 Delegation／Wait open）、`strength-persistence-durabilityport`、`opencode-host-modelroutingsurface`。HostDigest 仍由既有传递闭包中的 `runtime-platform/digest` 提供，本批未恢复宽 EventContract 伞。
4. 去 `foundation-outcome`：`participant-provider-attempt-fallback-ledger`。
5. 去 `opencode-host-chatadmission-intent`：`opencode-codec-providerprojectionsurface`。
6. 去 `participant-provider-attempt-requestkind`：`delegation-host-adapter`。
7. 去 `foundation-async-support`：`delegation-recovery-runtime`。

当前声明图为 26 subsystem、214 shard、700 source、1821 references，shard DAG，最大 subsystem SCC 22；相对 `1834` 净减 13 条声明引用，并新增 1 个 sharedstate shard。SCC 未缩小，GAP-033 保持 PARTIAL。下一刀继续重力井归属（例如 Role 合同与 Persona／ParticipantIdentity 分拆、ordinaryturnworkflow 编排归属、HostSignalBootstrap 多原因分片），不要把引用计数下降写成 SCC 已拆。

在基准 `cd0ad1b49`（1821 refs, 214 shards, SCC=22）上继续实施重力井知识归属分拆与剪枝：

**知识归属迁移（本批主刀）：**
新建 `participant-identity/participant-persona`（`Participant/Persona/{Catalog,ManagedCatalog,Identity,Surface}.fs/.fsi`），将 Persona 词汇从 `foundation-roles` 中物理剥离，精简 `foundation-roles` 至基础 Role 合同；全仓 36 处仅消费 Persona 的消费者改挂 `participant-persona`；`prefix-wire` 补充直接消费的 `participant-persona` 引用。

**伴随零符号剪枝（均经符号复核与聚焦编译）：**
1. `opencode-host-hostsignalbootstrap`：仅删除未直接引用的 `provider-system-transform` 与 `host-tool-adapter` 边；经符号审计，`enforcer-guidance-tip` 必须保留（KEEP），因其通过 `PairProgrammingThoughtSurface` 实际消费 `ThoughtTransform` 符号。
2. `composition-turn-ordinaryturnworkflow`（OTW）：删除 3 个未直接引用的 fact 边，分别为 `context-companion-fact`、`interaction-authority-fact`、`execution-fission-fact`。
3. `interaction-attention-fold`：删除未直接引用的 `intra-participant-parallelism/foundation-parallel`。
4. `repository-knowledge-casebook-bookkeeper`：删除未直接引用的 `strength-persistence-durabilityport`。
5. `repository-knowledge-casebook-lifecyclesurface`：删除未直接引用的 `strength-persistence-durabilityport` 与 `persistence-journal-promptfactcodec`，改挂实际消费其 `RuntimePath` 类型的 `change-integration/change-fact`。

当前声明图为 26 subsystem、215 compile shards、700 sources、1815 references，保持严格 shard DAG，最大 subsystem SCC 仍为 22；相对 `1821` 净减 6 条声明引用，并新增 1 个 persona shard。SCC 未缩小，GAP-033 依然保持 PARTIAL。下一刀重点推进 DurabilityPort 物理分片剥离等关键重力井，不要把引用计数下降写成 SCC 已拆。

在基准 `a8d51cff8`（26 subsystems, 215 compile shards, 700 sources, 1815 references, 最大 subsystem SCC 22）上继续实施 GAP-033 重力井物理分片剥离、所有权正名及稀疏边剪枝：

**1. DurabilityPort 物理 4 路拆分：**
原 `durable-events.strength-persistence-durabilityport` 分片混装了 Strength 持久化、Durable Fold、EventStore 日志写入器及 EventStoreHandle 资源契约，形成多原因重力井。本批将其解构：
- 新建 `durable-events.composition-durable-fold.fsproj`（`HostFactFold.fs/.fsi`, `Fold.fs/.fsi`），承接纯事实折叠逻辑；
- 新建 `durable-events.persistence-journal-eventstorewriter.fsproj`（`JournalEventStoreBoot.fs/.fsi`, `EventStoreJournalWriter.fs/.fsi`），承接 EventStore 日志写入逻辑；
- 将 `EventStoreHandle.fs/.fsi` 源码及类型归并吸收至底层的 `durable-events.persistence-eventstore-storetypes.fsproj`（将其 `Store`、`Dispose`、`Create` 公开化，支持跨分片安全可见）；
- 精简瘦身 `strength-persistence-durabilityport.fsproj`，仅保留 Strength 持久化类型（`DurabilityPort`, `Store`, `Durability`），其自身的 ProjectReferences 从 31 骤降至 7；
- 将 ~17 个原消费方按精确符号需求重定向改挂至细粒度分片（例如 `runtime.fsproj`、`canonicalintegrator`、`foldsurface`、`context-compression.runtime`、`plugin-composition` 等）。

**2. SyncDelegateRole 知识所有权正名与解环：**
- 将 `SyncDelegateRole` 及 `delegateRoleToAttachment` 从执行侧的 `Execution/Delegation/SyncDelegate/Model.fs` 中抽出，移入会话本体域新建分片 `session-ontology.execution-session-syncdelegaterole.fsproj`（`Execution/Session/SyncDelegateRole.fs/.fsi`）；
- 切断环路边：`session-ontology.execution-session-association` 不再反向引用下游的 `syncdelegate-model`，改为引用同子系统的 `syncdelegaterole`；
- 在 `Model.fs` 中将 `SyncDelegateRole` 保留为对 `Wanxiangshu.Execution.Session.SyncDelegateRole` 的类型别名，保证下游源码完全兼容；
- 同步更新 `scripts/checks/session-ownership-matrix.json` 中 SyncInspector 与 SyncCoder 的 evidencePath，指向真实物理所有权路径 `src/Wanxiangshu/Execution/Session/SyncDelegateRole.fs`。

**3. 经符号复核的稀疏引用剪枝（共 16 条）：**
- 9 条安全剪枝：涵盖 `delegation-sync-runtime`、`ingresscodec`（剪 4 条）、`chatadmission`、`managed-chat-execution`、`strength-predictor`、`delegation-host-adapter`；
- 3 条 durability 拆分伴随剪枝：`foldsurface`（去 `composition-durable-fact`）、`context-compression.runtime`（去 `host-session-contract`）、`plugin-composition`（去 `foundation-outcome`）；
- 1 条经审计确认必须保留的引用（KEEP）：`fallback-ledger` 对 `ingresscodec` 的引用，因其需要 `ModelRouting` 类型；
- 3 条 HostSignal Stage 1 剪枝：在 `opencode-host-hostsignalbootstrap.fsproj` 上切断 `foundation-outcome`、`interaction-authority-identityseed` 与 `execution-failure-model`（确认零符号使用，聚焦编译 32.8s 通过）；
- 清理 `PluginScope.fs` 中 5 处未使用的 Strength 命名空间 open。

**验证结果：**
全部 14+ 个受影响分片均独立通过 focused Fable 聚焦编译验证。静态门禁检查 `node scripts/checks/subsystems.mjs` 输出 26 subsystems / 218 compile shards / 701 sources / 1818 references / 最大 subsystem SCC=22（exit 0）；结构性质测试 `node --test requirements/structured-workflow/tests/subsystem-boundaries.test.mjs` 4/4 通过（exit 0）；全仓门禁 `node scripts/check.mjs` 全部通过（exit 0）。

声明图指标为 26 subsystems、218 compile shards（+3）、701 sources（+1）、1818 references（由于新增 3 个分片的必要接线与 16 条零符号剪枝及 DurabilityPort 内部引用从 31 降至 7，净增 3 条声明引用），保持严格 shard DAG。最大 subsystem cycle 仍为 22，GAP-033 保持 PARTIAL。HostSignal Stage 2（叶子 surface 抽取）与 Stage 3（PluginBoot 移至 plugin-composition）、OTW 子系统重分类为 dispatch，以及 ENF-015/016 证明缺口留在后续推进。

**4. OTW 子系统重分类、HostSignal Stage 2 叶子测试 Surface 归属迁移与零符号剪枝：**
- **OTW 归属修正**：将 `composition-turn-ordinaryturnworkflow.fsproj`（OTW）的子系统声明从 `provider` 修正为 `dispatch`。OTW 作为 turn observation 与 dispatch 调度的编排核心，本属于 `dispatch` 子系统，原归属 `provider` 系历史遗留；重分类后消除跨子系统反向编排倒挂，聚焦编译通过。
- **HostSignal Stage 2 叶子 Surface 抽取**：
  * 将 `ChatParamsSurface.fs/.fsi` 从 `host-boundary.opencode-host-hostsignalbootstrap` 抽离，移入其天然归属并已编译 `ChatParamsHook` 的 `execution-model-routing.opencode-host-modelroutingsurface` 分片中；
  * 将 `JoinResultRendererSurface.fs/.fsi` 从 `hostsignalbootstrap` 抽离，移入实际拥有 `JoinResultRenderer` 的 `delegation.execution-delegation-hostturnobservedsurface` 分片中，并按需补全直接依赖 `foundation-roles`；
  * 将 `ExplicitResumeSurface.fs/.fsi` 从 `hostsignalbootstrap` 抽离，移入已编译 `ExplicitSessionResume` 的 `host-boundary.opencode-host-fissionhostsurface` 分片中。
- **HostSignalBootstrap 零符号剪枝**：经严格符号审查，剪除 `hostsignalbootstrap.fsproj` 中未使用的 `managed-chat-execution.execution-session-chatexecution-fold` 与 `dispatch-protocol.composition-turn-reconcilesurface` 2 条 ProjectReference。
- **指标与验证**：全部分片聚焦编译绿色通过，`node scripts/checks/subsystems.mjs` 输出 26 subsystems / 218 compile shards / 701 sources / 1817 references（净减 1 条）/ 最大 subsystem cycle=22，全部门禁 `node scripts/check.mjs` 绿色通过（exit 0）。GAP-033 保持 PARTIAL。

**5. PairProgrammingThoughtSurface 迁移、HostSignal Stage 3 插件接线移入 PluginComposition 及稀疏剪枝：**
在基准 `bccee6d39`（1817 refs）上推进 HostSignal 重构收口与零符号稀疏剪枝：
- **PairProgrammingThoughtSurface 归属迁移**：将 `PairProgrammingThoughtSurface.fs/.fsi` 从 `host-boundary.opencode-host-hostsignalbootstrap` 移入 `guidance-delivery.enforcer-guidance-tip`，并为其补充直接依赖 `semantic-trace.context-trace-model` 与 `runtime-platform.digest`；HSB 随之剪除仅供 PPT 消费的 `enforcer-guidance-tip`、`guidelineprojection`、`context-trace-model`、`companion-fact`、`companionfactfold` 5 条引用，并经严格符号复核保留 `companion-runtimesurface` 以供给 `HostCompactionObserver`。
- **HostSignal Stage 3 插件接线迁移至 PluginComposition**：将 Plugin Cluster B（`PluginHost.fs/.fsi`、`PluginBoot.fs/.fsi`、`PluginHostWiring.fs/.fsi`、`PluginSessionWiring.fs/.fsi`、`PluginRecoveryWiring.fs/.fsi`）整体移入 `opencode-plugin.plugin-composition`（物理位置排列于 `BloggerChronicleText` 之后、`PluginTransforms` 之前）；PC 补齐所需直接依赖（包含 `delegation-host-adapter` 等），同时剪除 PC 中未使用的 `git-hook-sync`；HSB 彻底移除 Cluster B 专用依赖及无用引用（`syncdelegate-model`、`wait-*`、`opencodeport*`、`statictools`、`managedagent`、`session-attachment-contract`、`foundation-temporal`、`host-root-workspace-runtime`），并将工作区依赖精确重定向为 narrow 的 `host-root-workspace-contract`（仅满足 `IRootWorkspaceReader`）。至此 HSB 分片仅编译唯一宿主信号入口 `HostSignalBootstrap.fs/.fsi`，且保持 `plugin-composition` → `hostsignalbootstrap` 单向依赖不变。
- **独立符号复核稀疏剪枝**：
  * `delegation.execution-delegation-hostturnobservedsurface`：经符号审计剪除 7 条未使用的 ProjectReference（`eventstore-surface`、`durable-fold`、`identityseed`、`handle-surface`、`wait-completion-mailbox`、`host-digest`、`ingresscodec`）；
  * `managed-session-lifecycle.opencode-host-pluginruntimescope`：剪除未使用的 `sharedstatesurface`；
  * `speculative-investigation.strength-opencode-settings`：剪除未使用的 `sessionquiescencegate`。
- **指标与门禁**：声明图指标变为 26 subsystems、218 compile shards、701 sources、1809 references（净减 8 条，从 1817 降至 1809）；shard 保持无环 DAG；最大 subsystem SCC 仍为 22。相关门禁 `subsystems.mjs`、`subsystem-boundaries.test.mjs` 与 `check.mjs` 全部通过，GAP-033 保持 PARTIAL。

**6. 剥离 output 子系统出环（SCC 22→21）、RuntimeResources 细粒度分片抽取与零符号剪枝：**
在基准 `081aabc36`（26 subsystems, 218 compile shards, 701 sources, 1809 refs, 最大 subsystem SCC 22）上继续实施 GAP-033 核心解环与知识细粒度解耦：
- **解环 output 子系统（SCC 22→21）**：
  * 将 `output-distillation.process-largegatesurface.fsproj` 的 `WanxiangshuSubsystem` 归属由 `output` 修正为 `process`（`WanxiangshuSemanticOwner` 保持 `output-distillation` 不变）；
  * 消除跨子系统反向依赖：原先 `process/opencode-tools-ptytool` 对 `output/process-largegatesurface` 的引用转为 `process` 子系统内部引用；`output` 子系统入度变为 0，成功从全局最大强连通分量中剥离，最大 subsystem SCC 成功由 22 降至 21；
  * 同步更新 `scripts/checks/authority-contracts.json` 中 `RequirePermit DistillationRuntime` 的 owner 为 `process`，`authority-boundary` 检查保持通过。
- **RuntimeResources 细粒度分片抽取（Rank-2 解耦）**：
  * 新建分片 `cognitive-environment.resources-runtimeresources.fsproj`（归属 `resources` 子系统），承接从 `distribution.resources-enforcercatalogresource` 抽出的 `RuntimeResources.fs/.fsi` 编译；
  * 重定向 6 个仅需 `RuntimeResources` 的消费分片（`promptsurface`、`enforcer-repair`、`enforcer-guidance-tip`、`opencode-host-managedagentconfig`、`opencode-tools-executortoolsurface`、`strength-opencode-settings`），切断其对 `resources-enforcercatalogresource` 的引用，消除 `promptsurface` → `enforcercatalogresource` 跨分片直接依赖，并顺带消除了 `strength` → `enforcer` 经由 settings 的唯一跨子系统引用；
  * 为同时使用两侧的 `provider-system-transform.fsproj` 补充对 `resources-runtimeresources` 的显式引用；保留 `resources` ↔ `enforcer` / `provider` 的真实必要依赖。
- **零符号剪枝与格式收敛**：
  * 剪除 6 条经符号复核零使用的跨分片引用：`strength-persistence-durabilityport` 去除 `foundation-outcome`；`composition-turn-ordinaryturnworkflow` 去除 `execution-session-recovery-model`；`opencode-host-hostsignalbootstrap` 去除 `wait-diagnostic-adapter`、`composition-durable-fact`、`session-attachment-runtime`；`repository-knowledge-casebook-model` 去除 `foundation-roles`；经严格符号复核保留（KEEP）`opencode-host-modelroutingsurface` 对 `fissionhostsurface` 的引用（消费 `ExplicitResumeSuppression`）；
  * 格式整理：收敛 `modelroutingsurface` 与 `enforcercatalogresource` 的单行紧凑 XML 格式。
- **指标与门禁**：声明图指标变为 26 subsystems、**219** compile shards（+1）、701 sources、**1809** references、shard DAG 保持严格无环；最大 subsystem SCC 成功降至 **21**。全套门禁 `node scripts/checks/subsystems.mjs`、`subsystem-boundaries.test.mjs` 与 `node scripts/check.mjs` 均绿色通过。单子系统剥离不代表全表独立替换就绪，GAP-033 保持 **PARTIAL**。

**7. Language 下沉平台（Rank-3）、AgentJournal 独立分片拆分、三子系统连续剥离（SCC 21→18）与零符号安全剪枝：**
在基准 `68ab2a8cc`（26 subsystems, 219 shards, 701 sources, 1809 refs, 最大 subsystem SCC 21）上继续推进 GAP-033 知识解耦与环路消解：
- **Rank-3 Language 合同与资源读取下沉**：
  * 新建 `runtime-platform.language.fsproj`（归属 `runtime-platform`，引用 `assemblyinfo`），承接 `Language.fs/.fsi` 与 `ProviderResources.fs/.fsi` 编译；
  * `participant-provider-language` 移出上述 4 文件并反向引用该平台分片；
  * `resources-promptresources`、`resources-runtimeresources`、`resources-enforcercatalogresource` 引用全部改挂 `runtime-platform.language`，彻底消解 `resources → provider` 知识边；保留 `languagesurface` 对 `promptresources` 的反向读取；
- **AgentJournal 运行时句柄独立分片拆分（重力井治理）**：
  * 新建 `persistence-journal-agentjournal.fsproj`（归属 `persistence`），独立编译 `Writer.fs/.fsi`、`AgentJournal.fs/.fsi`、`SharedAgentJournal.fs/.fsi`，包含 9 项最小前置依赖；
  * `persistence-journal-promptfactcodec` 移出上述 6 个文件，专心承载 FactCodec／Envelope／ProjectionUpdate 序列化与折叠能力；
  * 64 个消费分片严格按符号分类重定向：51 个句柄唯一消费方全部改挂 `persistence-journal-agentjournal`，4 个编解码/折叠方保留 `promptfactcodec`，9 个双重消费方保留两条依赖；
- **剥离 resources 子系统出环（SCC 21→20）**：
  * `RuntimeResources` 深度组合规则与审计目录，属于 enforcer 组合知识；将 `resources-runtimeresources.fsproj` 与配套暴露给 JS 的 `resources-promptsurface.fsproj` 的 `WanxiangshuSubsystem` 归入 `enforcer`；
  * `resources` 唯一余留出边仅指向非环节点（`participant` 与 `runtime-platform`），其在 SCC 内的出度降为 0，成功剥离出环；
- **剥离 repository-investigation 子系统出环（SCC 20→19）**：
  * 将 `opencode-tools-inspectortool.fsproj` 重分类至 `interaction`（`action-affordance` 工具面）；
  * 将已无领域依赖的 `participant-provider-language.fsproj` 重分类至 `runtime-platform`；
  * `repository-investigation` 在 SCC 内出度归 0（仅余内部与平台依赖），成功剥离出环；
- **剥离 repository-programming 子系统出环（SCC 19→18）**：
  * 将其 3 个工具/运行时分片（`opencode-tools-codertool`、`opencode-tools-filemutationtools`、`runtime`）重分类至 `interaction`，仅保留纯接收汇 `js-capability`；
  * `repository-programming` 在 SCC 内出度归 0，成功剥离出环；
- **经符号复核的零符号安全剪枝（5 条）**：
  * `composition-durable-fold`：剪除 `context-companion-facts` 与 `execution-fission-facts`；
  * `opencode-tools-bookkeepertool`：剪除未消费的角色依赖 `foundation-roles`；
  * `casebook-lifecyclesurface`：剪除未消费的底层类型 `persistence-eventstore-storetypes`；
  * `execution-session-recovery-model`：剪除未消费的 `host-digest` 与 `foundation-outcome`；
  * 经编译与符号复核确认 KEEP：`repository-programming.runtime` 依赖 `host-fatal-effect`（`TransactionStore` 依赖 `FatalProcess`）〔第 8 条复核修正：该引用当时未被 `repository-programming.runtime` 分片消费，`TransactionStore.fs` 由 `opencode-tools-filemutationtools` 编译，引用已移位到实际消费者〕；`provider-system-transform` 依赖 `enforcercatalogresource`（调用 `composeBloggerSystemPromptFor`）；

**8. 剥离 relay 与 requirements 子系统出环（SCC 18→16）、零符号引用修复与独立编译暴露的漏报依赖补齐：**
在基准 `d140e9503`（26 subsystems, 221 shards, 701 sources, 1824 refs, 最大 subsystem SCC 18）上继续推进 GAP-033：
- **剥离 relay 子系统（SCC 18→17）**：将 `Mission/Relay/OpenCode/` 三个宿主集成分片的 `WanxiangshuSubsystem` 移出 relay——`mission-relay-reviewtool`、`mission-relay-suicidetool` 归 `interaction`（ToolSpec 可供性，被 capability-enforcement.opencode-host-managedagentconfig 消费），`mission-relay-narrative-transform` 归 `dispatch`（plugin transform 阶段，由 opencode-plugin.plugin-composition 调用，与既有 `relay-retirement.composition-turn-workflow` 归 dispatch 的先例一致）；relay 只余领域核心（Contract/Fold/Assessment/Projection/Retirement/WorkspaceSnapshot），SCC 内出度归 0。
- **剥离 requirements 子系统（SCC 17→16）**：`requirement-grounding.host-fact` → `persistence`（durable host fact 词汇与 requirement-grounding projection state，被 composition-durable-fact/-fold/-projection 与 promptfactcodec 消费）；`requirement-grounding.opencode-host-requirementgrounding-runtime` → `interaction`（面向模型的 grounding runtime/gate/transform 及 Surface，被 managedagentconfig 与 plugin-composition 消费，与 guidance-delivery 家族同族）；requirements 只余纯 model/catalog 分片，SCC 内出度归 0。两刀只改现行权威字段 `WanxiangshuSubsystem`，legacy `WanxiangshuSemanticOwner`/locality 保持原值（按 STRUCTURED-WORKFLOW-016 只作兼容读取，不参与 verdict）。
- **独立编译暴露的漏报依赖补齐（2 处）**：`mission-relay-narrative-transform` 独立编译失败于未声明的 `PromptDispatcher`（实际 provider `dispatch-protocol.dispatch-runtime`），`context-companion-fact` 失败于未声明的 `LlmFacing`（实际 provider `dispatch-protocol.foundation-llm-facing`）；两者均按现有 consumer 约定补齐直接 provider 引用，不复制实现、不用动态回捞。补齐后独立 Fable 编译分别通过（436 source closure，fingerprint `2087d81b6d49`；fingerprint `3407b794d9e9`）。前者不新增 subsystem 边（dispatch 内部引用 + 既有 dispatch→authority），后者只新增 context→runtime-platform。
- **经符号复核的零符号剪枝（4 条）与引用移位（1 条）**：`repository-programming.runtime` 上的 `host-fatal-effect` 引用经 provider 公共符号表（`FatalProcess`/`kill`/`trip`）逐名核对为零使用，而真实消费者 `opencode-tools-filemutationtools`（`TransactionStore.fs` 两处 `FatalProcess.trip`）漏报该 provider——引用因此从前者移除、补到后者；另剪除 `interaction-authority-ledger`、`delegation-sync-runtime`、`execution-delegation-syncdelegate-model` 三条 `participant-persona` 引用（以 `.fsi` 公共符号逐名核对为零使用；命中的 `role`/`roleLabel`/`origin` 均为消费方本模块自有符号）。五个受影响分片的独立 Fable 编译全部通过（`077c60862481`、`4c54f590ce87`、`e10c2f546bda`、`0566c6359dfd`、`efb267cc6a0e`）。
- **指标与门禁**：声明图指标变为 26 subsystems、221 compile shards、701 sources、**1825** references（-4 零符号剪枝，+1 真实消费者补齐，+1 漏报依赖补齐，+3 漏报闭包补齐），shard DAG 保持严格无环；最大 subsystem SCC 成功降至 **16**。门禁：`node scripts/checks/subsystems.mjs` OK；`node --test requirements/structured-workflow/tests/` 264/264；受影响 relay 与 requirement-grounding 行为套件 55/55；`node scripts/check.mjs` 全绿（仅既有 ENF-015/016 proof gap）。GAP-033 保持 **PARTIAL**：subsystem 环数下降不证明知识依赖已收敛，也不证明全表 focused compile closure 已闭合。
- **全表独立编译普查（本班新增证据）**：对全部 221 个 production shard 各跑一次 standalone `node scripts/compile-owner.mjs`（并发 6，耗时 702s）：**213 通过、8 失败**，全部为 missing-declaration，且经复核均为本批改动前既存（subsystem 标签不参与编译）：`change-facts`（`Mission`/`WorkspaceSnapshotId`/`QualityCertificateId`）、`delegation-pty-adapter`（`Strength`）、`dispatch-model-contract` 与 `session-attachment-runtime`（`Host`，同源 `Composition/Turn/{Program,Observation}.fs`）、`sphinx-host-adapter` 与 `sphinx-runtime`（`Foundation`/`CanonicalJson`）、`eventstore-merge-runtime`（`CanonicalEventCodec`）、`repository-investigation-semble-mcp`（`TaskValue`）。这份清单是下一批“补齐直接 provider 声明”的输入；本班不把普查当做已完成修复，也不为凑数仓促修改尚未读透的分片。

**9. 全表独立编译闭包补全（8/8）、sphinx 重力井拆分与零符号剪枝（SCC 16→17，并给出 17 为真值的证据）：**
在基准 `1c82aee29`（26 subsystems, 221 shards, 701 sources, 1825 refs, 最大 subsystem SCC 16）上继续推进 GAP-033。本班把上一班普查给出的 8 个 missing-declaration 分片全部关闭，并按知识边界拆分了 sphinx 重力井分片。**SCC 由 16 变 17 不是回退，而是此前数字被漏声明压低的暴露**——见本条第 4、5 小节。
- **8 处漏报依赖全部按"声明真实最窄 provider（或用编译证明清除遗留 open）"关闭**：
  * `change-integration.change-facts`：`Change/Facts.fsi` 真实使用 `Mission/Relay/Contract.fsi` 的 `WorkspaceSnapshotId`/`QualityCertificateId`/`AuthorityRevision` → 补最窄 provider `relay-incumbency.mission-relay-core`（fp `6d658993f69e`）。
  * `delegation.delegation-pty-adapter`：`Execution/Delegation/Fork/Host/Pty.fs:19` 的 `open Wanxiangshu.Strength` 经逐名核对（含 AutoOpen 与运算符）为零使用 → 删除遗留 open，编译即通过（fp `c44ce09283ea`）。
  * `dispatch-protocol.execution-session-recovery-model` 与 `managed-session-lifecycle.session-attachment-runtime`：`Composition/Turn/{Program,Observation}.fs` 的 `open Wanxiangshu.Host` 同为零使用遗留 open → 删除后两个分片独立编译通过（`0806e4a5c24c`、`a37a61737881`；后者此前失败是继承了前者的构建错误）。
  * `host-boundary.sphinx-host-adapter`：`OpenCode/Host/SphinxMcpConfig.fs:8` 的 `open Wanxiangshu.Foundation` 同为零使用遗漏 open → 删除（fp `de6882e1831b`）。
  * `durable-convergence.eventstore-merge-runtime` 与 `repository-investigation.repository-investigation-semble-mcp`：真实使用 `CanonicalEventCodec`、`TaskValue` → 分别补 `persistence-eventstore-processeventlog`、`foundation-taskresult`（fp `5ba3c992efa8`、`a633203104c0`）。
- **sphinx 重力井拆分（shard 级环先拆才能诚实声明）**：`epistemic-reasoning.sphinx-runtime` 原编译 90+ 个 `Sphinx/**` 文件，其中 `Sphinx/{GenericDurability,McpServer,Surface}.*` 真实需要 persistence 的 `EventEnvelope`（`GenericDurability`）、`IEventStore`/`AppendError`（`McpServer.fsi:8-10`）、`CanonicalIntegrator.create`（`McpServer.fs:1221`）与 `EventStore.createLocal`（`McpServer.fs:1224`）（该构造已在 2026-09-12 班次迁至 `Sphinx/ServeEntry.fs`，见 §3.2 第 20 条）；而 persistence 侧 `Persistence/EventStore/CanonicalIntegrator.fs` 又反向持有 sphinx 知识（第 14 行 `open Wanxiangshu.Sphinx`，第 79-124/125-188 行的 `SphinxIntegration`/`SphinxGenericIntegration` 规则，第 253-254 行注册；`Persistence/EventStore/EventVocabulary.fs:36-40` 还枚举 5 个 `SphinxEventTypes.*`）。`scripts/lib/owner-compile.mjs` 明确拒绝 shard 级 ProjectReference 环，因此"直接补声明"不可行——本班按知识边界新建 `epistemic-reasoning.sphinx-serve-runtime.fsproj`（承接上述 6 个服务/持久化文件，refs 指向领域 shard、事件词汇、persistence 的 model/storetypes/canonicalintegrator/processeventlog 与平台词汇），领域 shard 只保留 `sphinx-mcp`/`digest`/`foundation-canonical-json`/`foundation-identity`/`sphinx-event-vocabulary-contract`。四条 focused compile 全绿：`sphinx-runtime` `274675fa13b3`、`sphinx-serve-runtime` `db6978431264`、消费方 `canonicalintegrator` `1f62c475c962`、`sphinx-host-adapter` `de6882e1831b`。这同时把 90+ 文件的单体分片拆成"纯领域内核 / 服务与持久化集成"两个可独立编译的分片。
- **零符号剪枝（7 条，编译证明）**：`host-boundary.opencode-host-hostsignalbootstrap` 在 `081aabc36` 已把 12 个文件迁出，现仅编译 `HostSignalBootstrap.fsi/.fs`；其残留 7 条引用（`causal-wait.execution-session-wait-contract`、`causal-wait.execution-session-wait-runtime`、`delegation.execution-delegation-fact`、`durable-events.persistence-eventstore-surface`、`interaction-authority.interaction-authority-ledger`、`knowledge-reuse.repository-knowledge-casebook-model`、`knowledge-reuse.repository-knowledge-casebook-bookkeeper`）逐名核对为 0 命中后剪除，独立编译通过（fp `1d7c626c8102`）。其中两条与 `PeelKnowledge` 设计报告独立得到同一结论（host→knowledge 残留引用）。
- **指标与口径说明**：26 subsystems、**222** compile shards（+1：`sphinx-serve-runtime`）、701 sources、**1833** references（+8 = 6 条真实漏报 provider 声明 + 9 条新 shard 引用 − 7 条零符号剪枝）；shard DAG 严格无环。最大 subsystem SCC **16→17**。**17 才是当前声明图的真值**：16 建立在 `sphinx-runtime` 闭包未声明之上；补全后 sphinx 与 `host`/`interaction`/`persistence` 双向可达（`host-boundary.sphinx-host-adapter`→`sphinx-mcp`、`action-affordance.opencode-tools-statictools`→`sphinx-mcp` 为真实主机/工具面引用，而 `sphinx-serve-runtime`→persistence 是真实持久化词汇依赖），本轮已用 focused compile 与 `owner-compile` 的环检测双重证实。这提示：**SCC 数字只在闭包完整时才有意义**；本班之前的所有 SCC 报告都受此条漏声明影响。
- **下一批设计输入（本班派发 5 个只读设计，结论摘要与关键证据）**：
  * `process`：7 条环内入边（`change.git-gateway`/`git-integrationgate`→`processrequest` 的 `ProcessRunner.run`/`ProcessEstimate.DefaultHardLimit`；`delegation.{host-adapter,pty-adapter,runtime}`→`processrequest` 的 `PtyBackend`/`Pty.registerParentAbort`；`action-affordance.runtime`/`capability-enforcement.managedagentconfig`→`ptytool`）都是真实知识；剥离需从出边侧做 capability 注入（`Process/Pty.fs` 反过来引用 delegation `ManagedAgent`/`PtyJoinItem`，以及 `process-*`/`time-capability.*` 分片对 session-lifecycle 的 9 条引用）。
  * `strength`：四类倒置——`durable-events.strength-persistence-durabilityport`（Strength 的 EventStore 适配器被归 persistence，形成 2 条 persistence→strength）、`opencode-host-pluginruntimescope` 硬持有 `PluginStrengthScope`、`interaction`（ToolRegistry）与 `context`（ContextPrefixWire）直查 `StrengthRuntime.TryFindByReplica` 物理状态、`CanonicalIntegrator` 硬编码 Strength 投影 + `opencode-plugin.plugin-composition` 归属待裁决。
  * `work`：14 条入边 = persistence 硬编码 MagicTodo 事实/投影 7 条 + change/delegation/session-lifecycle 就地调用 `LifecycleWorkRecordProjection` 4 条 + context/dispatch 等；需先做 capability 注入（`change/Host/Types.fs:34-35` 已有 `ParentWorkRecordFor`/`ChildWorkRecordFor` 注入槽位可复用）与 MagicTodo 归属裁决。
  * `knowledge`：9 条入边中 3 条 hostsignalbootstrap 残留已由本班剪除；根因是 `Casebook/BookkeeperRuntime.fs` 把宿主调度/权限派生揉进领域分片，且 `interaction`（ToolRegistry 静态内联 `FetchTool.admission`/`JsBookkeeperTool.admission`）与 `persistence`（`CanonicalIntegrator.fs:191-206,255` 内联 `CasebookIntegration.rule`）在底层硬编码领域规则。
  * `sphinx↔persistence`：拆分已按设计落地；进一步倒置需要把 `SphinxIntegration`/`SphinxGenericIntegration` 规则移回 sphinx 侧并改注册为显式注入（`CanonicalIntegrator.create` 签名或注册口），以及处理 `EventVocabulary.fs` 对领域事件类型的枚举——两者都会改动公开签名与 4 个真实调用方（`delegation.runtime`、`durable-events.persistence-eventstore-surface`、`durable-events.runtime`、`verification-system.verification-eventstorewritersurface`），需要单独设计与验证。
- **全表独立编译复扫（未完成，如实记录）**：对 222 个 production shard 重新发起 standalone `node scripts/compile-owner.mjs` 普查（并发 6）；冷缓存下吞吐约 3 shard/min，跑到 **41/222 全部通过、0 失败** 后为按时收班取消。未跑完的分片沿用上一班结论（213 通过）与本班逐条 focused compile 修复证据；**不得**把本次部分复扫当作“全表已复扫”。
- **门禁**：`node scripts/checks/subsystems.mjs` OK（26/222/701/1833，shard DAG 无环，cycle=17）；`node --test requirements/structured-workflow/tests/` 264/264；`node scripts/check.mjs` 全绿（仅既有 ENF-015/016 proof gap）。GAP-033 保持 **PARTIAL**：闭包完整不等于知识依赖已收敛；本轮暴露的 17 才是后续解环的诚实基线。

**10. work/strength/process 知识剥离落地（capability 注入 + Pty 中性化 + durabilityport 归属纠正）、零符号剪枝与全表独立编译复扫（222/222）；SCC 保持 17：**
在基准 `fbc9b5076`（26 subsystems, 222 shards, 701 sources, 1833 refs, 最大 subsystem SCC 17）上继续推进 GAP-033。本班按第 9 条末段的只读设计，落地了 `work`/`strength`/`process` 三条剥离开刀，并完成上一班未完成的全表 standalone 复扫。
- **work-record 物化注入（切断就地调用）**：`Change/Host/{Types,Host,Surface}.*` 增加 `ChildWorkRecordForRun` 注入槽并删除就地 `LifecycleWorkRecordProjection`；`Execution/Delegation/SyncDelegate/Surface.fs` 与 `OpenCode/Tools/ToolRuntimeScope.*` 改为消费注入的 `DelegationWorkRecordCapability`/`childWorkRecordForRun`，不再从 journal 自建；`git-integrationgate`/`opencode-host-pluginruntimescope` 的 `mission-workrecord-materialize-runtime` 引用随之剪除。行为回归（`knowledge-reuse`/`managed-session-lifecycle`/`prefix-stability`/`delegation` 的 EXEC-031 失败）暴露 JS 入口漏配：`SyncDelegate.Surface.create` 与 `Fork/OpenCode/ToolSurface.createRuntime` 必须在表面层显式构造真实能力并注入——这两处恢复/补齐后，`delegation.runtime` 对 materialize 的声明依赖是真实知识（JS 运行时表面组合 work-record），不是漏声明。`intra-participant-parallelism.execution-fission-opencode-host` 的 fission Host 就地调用本班未切（级联到 `HostTurnObserver`，已由 strength 切片占用）。
- **strength 窄端口与归属纠正**：
  * `durable-events.strength-persistence-durabilityport` 的 `<WanxiangshuSubsystem>` `persistence→strength`（源码全在 `Strength/Persistence/**`，命名空间 `Wanxiangshu.Strength.Persistence`；两条 persistence→strength 边变为子系统内边）。
  * `ToolRegistry`：`strengthRuntime: StrengthRuntime option` → `isReplicaSession: (SessionId -> bool) option`；`FetchTool.admission` 内联为同义的 `ToolAdmission.OfficeRole` 谓词（字节级等价），剪除 `strength-prediction-predictor` 与 `opencode-tools-fetchtool`；`js-bookkeeper` 准入保留 `JsBookkeeperTool.admission`（ENF-006 表面语义不可静默降级）。
  * `ManagedAgentConfig.fs` 删除 4 行零符号 `open Wanxiangshu.Strength*`。
  * `PluginRuntimeScope`：删除 `member Strength: PluginStrengthScope`，改为 `AttachSessionCleanup`/`AttachScopeDispose` 中性钩子；`HostTurnObserver`/`HostSessionDeletion` 改为注入 `handlePreTurn`/`observePrimaryTurn`/`onSessionDeleted` 端口；`Context/Prefix/Wire.applyTransform` 增加 `isReplicaSession` 谓词；`Strength/OpenCode/Speculate` 改为直接接收 `PluginStrengthScope`（不再经 `PluginRuntimeScope.Strength`）。组合层（`plugin-composition` + `HostSignalBootstrap`）显式持有 `PluginStrengthScope` 并装配端口。**时序与 fail-closed 语义**：初版把 `StrengthDurableAppend.SemanticRejected` 静默忽略（`()`），违反 DURABLE-EVENTS-021；已恢复为与旧实现逐字同义的 `Diagnostic.fatal "strength-semantic-cut" [ "result", error ]`，并迁移源码契约测试到新家（`HostSignalBootstrap.fs`）。
- **process Pty 反向依赖切断**：`Process/{Pty,PtyTypes,Surface}.*` 不再引用 delegation 业务类型；引入中性 `PtyExitEvent`（`Exited`/`Failed`/`Aborted`）与 `PtyPort(?exitListener, ?handler)`/`AddExitListener`/`Fork(command, agentName: string, ...)`/`List() -> PtyHandle list`；delegation 侧（`Fork/Host/{Pty,Runtime}.*`）把 `PtyExitEvent` 映射回 `PtyJoinItem`。`process-processrequest` 剪除 `opencode-tools-managedagent`/`execution-delegation-fact`/`execution-delegation-syncdelegate-model`。物理 listener 登记表（`scripts/checks/semantic-decorator-invariant.mjs` 的 `PHYSICAL_LISTENER_CONTRACTS`）与 STRUCTURED-WORKFLOW-008 宪法测试同步到 `exitListeners` 绑定。
- **零符号剪枝 / 真实 provider 重定向（5 条）**：`interaction-dispatch-opencode-ingresscodec`→`context-companion-companionfactfold`（跨子系统）；`mission-workrecord-materialize-runtime`→`mission-obligation-todo-model`；`mission-workrecord-materialize-runtime`→`host-digest` 重定向为 `runtime-platform.digest`（跨子系统 work→host 边消失）；`mission-workrecord-surface`→`mission-workrecord-model-contract`；`git-gateway`→`change-fact` 重定向为 `runtime-platform-git-subject`（`GitSubject` 的真实最窄 provider）。全部经 remove-ref + focused compile 证明。
- **指标与口径**：26 subsystems、222 shards、701 sources、**1820** references（1833→1820，净 −13）；shard DAG 无环；最大 subsystem SCC **仍为 17**。subsystem 对级入边变化（本班前后）：`work` 6→4、`strength` 5→3；`session-lifecycle` 出边 14→12、`context` 出边 12→11、`interaction` 出边 19→18；`host` 出边 14→15（`HostSignalBootstrap` 显式装配 strength 端口——组合根诚实持有，下一步可把闭包构造上移到 `plugin-composition` 以去掉 host→strength）。**SCC 未缩小是预期**：本班切断的是偶然就地耦合，不是 17 元环的反馈弧；下一刀仍是 `CanonicalIntegrator`/`EventVocabulary` 的领域规则倒置与 MagicTodo/BookkeeperRuntime 归属裁决。
- **全表独立编译复扫（本班完成）**：对全部 222 个 production shard 各跑一次 standalone `node scripts/compile-owner.mjs`（并发 8）：**222 通过、0 失败**。上一班“未完成项=全表复扫”就此关闭。
- **行为证明与测试迁移**：`npm run build` 后 `node requirements/verification-system/tests/run.mjs` → **3970/0**；`node --test requirements/structured-workflow/tests/` → 264/264；`node scripts/check.mjs` 全绿（仅既有 ENF-015/016 proof gap）。本班迁移/修复的契约测试：`process-execution/pty-port`（新 PtyPort API）、`speculative-investigation/dry-run-shadow`（dry-run 关闭标记迁到 `HostSignalBootstrap`）、`durable-events/event-store-append`（`strength-semantic-cut` 致命边界迁到新家）、`host-boundary/m6-slice-boundary` 与 `time-capability/m6-slice-boundary`（消费者清单与历史物理切分对齐——后者两处期望在基准 `fbc9b5076` 已失败，本班一并诚实收口）、`structured-workflow/workflow-constitution`（物理 listener 登记）。
- **下一批设计输入（本班完成的只读设计，未落地）**：`CanonicalIntegrator`/`EventVocabulary` 领域规则倒置——领域规则迁回各自子系统导出 `IntegrationRule`，`create (rules: IntegrationRule list)` 显式注入；8 个 F# 调用点按最小规则集装配（sphinx 服务只需 Structural+Sphinx×2；delegation journal 只需 Structural+Journal；JS `EventStore/Surface.create(commonDir, writerId)` 保持 2 参并在表面层组合全集）；`unified-store-gate.mjs` 的控制流断言可保持。MagicTodo 归属与 `BookkeeperRuntime` 宿主调度抽出仍待实施。
- **门禁**：`node scripts/checks/subsystems.mjs` OK（26/222/701/1820，cycle=17）；structured-workflow 264/264；verification-system 全量单测 3970/0；`node scripts/check.mjs` 全绿。GAP-033 保持 **PARTIAL**。


**11. CanonicalIntegrator/EventVocabulary 领域规则倒置（显式规则注入）+ MagicTodo 归属迁移 + BookkeeperRuntime 内部分离 + HostSignalBootstrap strength 闭包上移；SCC 17→19 是诚实值：**
在基准 `94b16000a`（26 subsystems, 222 shards, 701 sources, 1820 refs, 最大 subsystem SCC 17）上推进 GAP-033 设计末段的四刀。
- **领域规则倒置（spine 不再认识任何领域折叠）**：`SphinxIntegration`/`SphinxGenericIntegration`、`StrengthIntegration`、`JsTransactionIntegration`、`CasebookIntegration` 四组规则逐字迁到各自领域 owned 的新 provider 分片：`epistemic-reasoning.sphinx-integration-rules`（`Sphinx/IntegrationRules.*`）、`speculative-investigation.strength-integration-rules`（`Strength/IntegrationRules.*`）、`repository-programming.repository-programming-js-integration-rules`（`Repository/Programming/Js/IntegrationRules.*`）、`knowledge-reuse.knowledge-integration-rules`（`Repository/Knowledge/Casebook/IntegrationRules.*`）。构造口改为 `CanonicalIntegrator.createWithRules : IntegrationRule list -> ICanonicalIntegrator`（旧 `create()` 与 builder CE 删除；`requireBaseRules` 对缺失 Structural/Journal fail-closed，无默认成功 fallback）；8 个真实调用点按最小规则集显式装配（`Execution/Delegation/{Fork/OpenCode/ToolSurface,Handle/JournalSurface,SyncDelegate/Surface}`、`Persistence/Journal/Surface`、`Verification/TemporalSurface` 用 `baseRules`；`Persistence/EventStore/Surface` 用全集；`Sphinx/McpServer` 用 `baseRules @ SphinxIntegrationRules.rules`）。脊线分片 `persistence-eventstore-canonicalintegrator` 剪除 7 条领域引用（`strength-persistence-durabilityport`、`sphinx-event-vocabulary-contract`、`sphinx-runtime`、`opencode-tools-filemutationtools`、`repository-programming-js-capability`、`strength-prediction-predictor`、`knowledge-reuse.repository-knowledge-casebook-model`），现只剩 `baseRules`（Structural+Journal）。规则 `Name`/`Accepts`/`FaultScope`/cut-reset 语义与 `EventVocabulary.fs` 的 21 个冻结常量均未改。
- **宿主编排 store 的规则集必须覆盖其消费者真实读取的 Current（本轮发现并修复的静默语义丢失）**：`OpenCode/Host/WorkspaceEventStore.acquire` 是全宿主唯一共享 store，`CasebookLifecycle`/`CasebookWorkflow`(+`CasebookIndex`)/`FetchTool`、`PluginHostInterop`→`JsToolsTransactionStore.createPersistence`、`PluginHostWiring`→`StrengthDurability.create` 都从它读 `TryCurrent "Casebook"/"JsTransaction"/"Strength"`。若只给 `baseRules`，这些读取会静默得空（`StrengthDurability.LoadProjection`→empty、`TransactionSurface.pending`→`[||]`、`CasebookIndex.project`→`Map.empty`）而 append 仍成功——即无报错的语义丢失。现装配为 `hostProgram = baseRules @ StrengthIntegrationRules.rules @ CasebookIntegrationRules.rules @ JsTransactionIntegrationRules.rules`（历史注册顺序；Sphinx 由独立 MCP 进程自己的 store+程序承担），并新增 3 条声明引用（`durable-events.runtime` 新增 4 条：sphinx/strength/js/knowledge）。
- **MagicTodo 归属迁移**：`Mission/Obligation/Todo/{Facts,Projection,MagicTodoFactCodec}` 迁到 `Composition/Durable/MagicTodo{Facts,Projection,FactCodec}`，新增 `Composition/Durable/MagicTodoIdentity.{fs,fsi}` 承载 canonical `TodoWriteId`（`Todo/Model.fs` 保留同型别名以免 11 个非本切片消费者改动）。命名空间、codec 字节、event-type 字符串、fold 结果与 fail-closed 解码逐字不变；persistence 不再硬编码 work 域事实（`composition-durable-{fact,fold,projection}`→`todo-model` 三条边消失，改由 work 消费新 persistence fact/projection 分片）。
- **BookkeeperRuntime 内部分离**：单文件内引入私有 `Decisions`（纯决策：prompt 成型/证据渲染/receipt 分类）、`Ledger`（记账：runtime 槽、live 绑定、prompt 授权、completion 句柄）、`Coordination`（唯一触碰 `ISessionHostPort`、`BookkeeperStaging`、`PromptAuthority` 派生与终态订阅的协调层）。公开 `.fsi` 逐字不变，未增删 ProjectReference；残留：`PromptAuthority`/`BookkeepingStaging` 仍是同分片静态调用，进一步净化需 capability 注入。
- **HostSignalBootstrap strength 闭包上移**：新增 `OpenCode/Plugin/PluginStrengthPorts.{fs,fsi}`（plugin-composition）承接逐字迁移的闭包；`HostSignalBootstrap` 只留中性 `StrengthHostPorts`（`HandlePreTurn`/`ObservePrimaryTurn`/`CancelStrengthOwner`/`OnSessionDeleted`）并剪除 3 条 strength 引用，subsystem 边 `host → strength` 消失。**实现事实如实记录**：wire 时 `StrengthReplicaRuntime` 实际为 `None`（`PluginSessionWiring.attach` 在 `PluginHostWiring.create` 之后），eager 捕获与旧行为一致；激活 replica 路径属行为变更，未做。
- **门禁要求的真实压平（不得抑制）**：`PluginStrengthPorts` 拆为 `tripFuse`/`commitAppendResult`/`commitReconciledEvent`/`commitDurablePromotion`/`observePrimaryTurnBody` 命名决策；`HostTurnObserver` 增加私有 `observePrimaryTurnIfPresent`，去掉 `if → match` 链；顺序（干跑关闭→primary 观察→durable 提交→普通观察）与 fuse/raise/`Diagnostic.fatal "strength-semantic-cut"` 语义逐字保持。
- **测试与登记表重锚（同强度、仅换新家）**：`durable-events/event-store-append`（semantic-cut 边界 `HostSignalBootstrap.fs`→`PluginStrengthPorts.fs`）、`speculative-investigation/dry-run-shadow`（`CloseDryRunAtTargetTerminal` 同）、`crash-reconciliation/external-effect-reconciliation`（registry 3 条路径 + 负例 fixture 指向 `Composition/Durable/MagicTodoFacts.fs`）、`obligation-ledger/{obligation-ledger-workflow-contract,magic-todo-provider-boundary}`（Todo→Composition/Durable）、`scripts/checks/external-effect-contracts.json`（3 条路径）、`scripts/checks/release-closure-nodes.json`（两个 node 的 MagicTodo 文件路径 + 新增 Identity）。断言强度未改；未按源码形状新增伪回归。
- **指标与口径**：26 subsystems、**226** compile shards（+4 provider）、707 sources、**1839** references（1820→1839，+19）、shard DAG 严格无环；跨子系统引用 1446→1455。最大 subsystem SCC **17→19**。实测子系统边差异：新增 `repository-programming → interaction`、`repository-programming → persistence`，消失 `host → strength`。前者因 `repository-programming-js-integration-rules` 需要 `opencode-tools-filemutationtools`（该 fsproj 名带 repository-programming 前缀，但 `<WanxiangshuSubsystem>` 是 `interaction`，同一分片混合编译 interaction 的 `OpenCode/Tools/FileMutationTools.*` 与 `Repository/Programming/Js/{ToolsBindings,TransactionStore,OpenCode/*,GeneratorSurface}`）；后者是领域 provider 依赖持久化的窄 `IntegrationRule` 契约与 `EventEnvelope` 模型。`requirements` 随 repository-programming 并入同一分量。**这不是解环成果**：倒置把“脊柱硬编码领域折叠”换成“组合根显式装配领域 provider”，引用只是从脊线搬到装配分片并新增 provider→契约的诚实依赖；19 是当前声明图的真值。
- **下一批设计输入**：(a) 拆分混合分片 `opencode-tools-filemutationtools`（`Repository/Programming/Js/**` 归 repository-programming，`OpenCode/Tools/FileMutationTools.*` 归 interaction）——预期消掉 `repo-prog → interaction`；(b) 评估把 `IntegrationRule`/`EventEnvelope` 契约上移到平台窄分片，去掉领域 provider → persistence 的反向边；(c) `EventVocabulary.fs` 仍枚举 Job 控制面与 5 个领域事件常量（冻结字符串），若要让白名单由规则派生需给 `IntegrationRule` 增加 `DeclaredEventTypes` 字段；(d) `BookkeeperRuntime` 的 `PromptAuthority`/`BookkeepingStaging` 静态调用仍需 capability 注入才能清 knowledge 入边。
- **门禁**：`node scripts/checks/subsystems.mjs` OK（26/226/707/1839，shard DAG 无环，cycle=19）；`node --test requirements/structured-workflow/tests/` 264/264；`npm run build` 全绿（1414 items，165 surfaces，779 modules linked）；`node requirements/verification-system/tests/run.mjs` **3970/0**；`node scripts/check.mjs` exit 0（仅既有 ENF-015/016 proof gap）；`npm run format:check` 转绿（修正继承自基准的 14 个未格式化文件 + 本批新增 5 个）。GAP-033 保持 **PARTIAL**。

**12. 混合 compile shard 拆分（归属真相）+ ENF-015/016 真实行为证明落地；SCC 保持 19：**
在基准 `6e46f7ae4`（26 subsystems, 226 shards, 707 sources, 1839 refs，最大 subsystem SCC 19）上做两刀。
- **拆分混合分片**：`repository-programming.opencode-tools-filemutationtools` 声明 `<WanxiangshuSubsystem>interaction</WanxiangshuSubsystem>`，却把 `OpenCode/Tools/FileMutationTools.{fsi,fs}`（interaction 工具面）与 `Repository/Programming/Js/{ToolsBindings,TransactionStore,OpenCode/ToolWorkflow,OpenCode/ToolHost,GeneratorSurface}`（repository-programming 知识，与 `repository-programming-js-capability` 中的 `Capability/Utf8Fs/GlobFs/MutationFs` 同类）编译进同一分片，10 个 repo-prog 源文件因此被计到 interaction 子系统。现拆为 `interaction.opencode-tools-filemutationtools`（仅 FileMutationTools，4 条引用）与 `repository-programming.repository-programming-js-toolbindings`（Js 10 源，9 条引用），5 个真实 consumer 按符号使用重接：`opencode-host-managedagentconfig`（`ToolRegistry` 用 `mv/rm` spec + `JsToolSpec/JsDescriptionAssets`）与 `repository-programming.runtime`（`FileMutationSurface` + `RuntimeSurface/TransactionSurface/WorkflowSurface/ToolHostSurface`）两半都需要；`opencode-host-requirementgrounding-runtime`（`JsGeneratorSurface/JsToolWorkflow`）、`opencode-tools-bookkeepertool`（`JsToolsData`）、`repository-programming-js-integration-rules`（`JsToolsTransactionStore`）只需 Js 半。aggregate 的源集合与顺序不变（无增删）。实测：shards 226→227、refs 1839→1842（新增的都是由编译错误逐条证明的真实依赖：`storetypes`/`host-fatal-effect`/`foundation-taskresult`），加入 `repository-programming → host`，删除 `repository-programming → interaction`；**SCC 仍为 19，不得把本刀读作解环收益**（该环由其它多条边维持）。
- **law 测试按新 owner 重锚（同强度）**：`requirements/structured-workflow/tests/owner-project-boundaries.test.mjs` 的 `WHAT[STRUCTURED-WORKFLOW-014]` 原先断言旧分片名存在且引用 NodeFs 端口。现断言两个新分片的精确源集合与真实边界：FileMutationTools 分片引用 NodeFs 端口且不引用 Js 半；Js 半不引用已删旧名、也不继承 NodeFs 端口。
- **ENF-015/016 证明闭合（trace gap 4→0）**：新增 `requirements/capability-enforcement/tests/authority-subject-binding.test.mjs` 与 `authority-stale-admission.test.mjs`，驱动注册的 ChatExecution surface，实际执行 `ManagedChatAcceptance`/`ManagedChatProviderLifecycle` 准入：witness 携带精确 subject（`ChatExecutionKey`）与版本字段；外来主体（`AttemptKeyMismatch`）、旧版本证据（`EstablishedEvidenceConflict`）、终态之后的旧准入（`ProviderStartedAfterTerminal`）、竞争的 provider run（`ProviderRunConflict`）均 typed fail closed 且 append 计数与 projection 证明零效果；同一准入在 Accepted 态成功、终态后失败，说明决定来自当前读取而非缓存能力。**可失败性已实测**（对 dist 定点变异后还原，sha256 一致）：去掉 subject guard → `FactRejected`，去掉 evidence guard → `FactRejected`，去掉终态 guard → ENF-016 红。`capability-enforcement/HOW.md` 表已加 ENF-015/016 行并声明覆盖边界（仅证明 chat-execution 一个消费边界，其余登记行的运行时 screening 未逐项证明）。gate 要求声明消费边：`scripts/lib/test-surface-scan.mjs` 的 `SURFACE_CONSUMERS` 为 `Execution/Session/ChatExecution/Surface.js` 增加 `capability-enforcement`。
- **Long Stroke 停顿证据（未定位根因）**：本班 10 次运行中 1 次失败（失败后 6/6 串行复现实验全过，失败发生在构建后的首次运行）。失败世界 `/tmp/oc-e2e-F0GwGR` 证据：失败后宿主只发 `server.heartbeat`（最后业务事件 `file.watcher.updated` @10:13:59.764），mock 共服务 52 次请求而通过的运行是 71–75 次（**mock 不是阻塞方**，其唯一 hold 已由 harness 在 interrupt prompt 被接受后释放）；harness 自己的 causal frontier 在停顿时报 `BrokenCausalEdge: OrchestratorWorkflow/OrchestratorJob waits for ManagerWorkflow: but no active wait exists for that producer`，同时 `JoinTool:{session=ses_f700a536...}` 的 `agent-join` 仍在等 `external:child-completion`；停顿前宿主日志里 manager 会话出现 scripted `declared provider-error fault (retryable=false)`（primary `test` 与回退 `opencode` 两个 provider），而持久 journal 只记录到 2 条 `FailureRecorded` + 1 条 `SuccessRecorded`。当前证据只支持“宿主侧在 hard provider fault 后停止推进”，不能认定是产品缺陷还是脚本分支未覆盖；下次复现入口：串行循环运行 e2e 并保留世界，重点比较该 frontier dump 与第三条 provider-failure 事实是否存在。
- **下一批设计输入（本轮未执行）**：(a) 领域 `IntegrationRules` provider → `persistence-eventstore-model` 的边应保留：它们真实使用 `EventEnvelope`（声明于 `Persistence/EventStore/Model.fs:26`），而 `IntegrationKernel` 的 `IntegrationRule` 签名本身就引用该类型；把 envelope 搬进 contract shard 等于搬走 persistence 的事件模型，属伪解耦，拒。(b) `EventVocabulary.AuthoritativeEventTypes` 仍硬编码 Job 字面量与 JsTransaction/Inspector 事件名（strength/sphinx 两个领域已通过窄 vocabulary contract 分片提供）；若要由规则派生需给 `IntegrationRule` 增 `DeclaredEventTypes` 并保持 accepted set 逐字不变。(c) 建议保留 `host -> sphinx`、`dispatch -> strength`、`strength -> host` 等边直到有符号级证据。
- **门禁**：`node scripts/checks/subsystems.mjs` OK（26/227/707/1842，cycle=19）；`node --test requirements/structured-workflow/tests/` 264/264；`npm run build` 全绿（1414 items，165 surfaces，779 modules）；verification **3972/0**；integration **279/0**；`node scripts/check.mjs` exit 0（**trace proof gap 0**）；`npm run format:check` exit 0。GAP-033 保持 **PARTIAL**。

**13. 归属真相二刀（混合分片 + 领域事件名自有词汇）+ Long Stroke 双失败模式定位；SCC 保持 19：**

在基准 `9e40a3ea3`（26 subsystems, 227 shards, 707 sources, 1842 refs，最大 subsystem SCC 19）上做两刀，并按 Long Stroke 缺口继续复现。

- **混合分片归属修正**：`Wanxiangshu.Owner.repository-programming.runtime.fsproj` 声明 `<WanxiangshuSubsystem>interaction</WanxiangshuSubsystem>`，实则编译 8 个 `Repository/Programming/Js/**Surface.{fsi,fs}`（repository-programming 知识）加 `OpenCode/Tools/FileMutationSurface.{fsi,fs}`（interaction 工具面）。现把 `FileMutationSurface` 移入 `interaction.opencode-tools-filemutationtools`（与 `FileMutationTools` 同属 `OpenCode/Tools` 工具面知识），该分片子系统改为 `repository-programming`（`<WanxiangshuCompileShard>js-programming-runtime-surface` 与文件名不变）。随后按零符号剪枝逐条删除 `host-tool-adapter`、`persistence-eventstore-storetypes`、`repository-programming-js-capability`（每次删除后 focused compile 均通过，证明无符号消费），只保留 `repository-programming-js-toolbindings`。`FileMutationSurface` 全树无 F# consumer（grep 证实），故无消费者迁移；`requirements/host-boundary/tests/m6-slice-boundary.test.mjs`（HOST-BOUNDARY 工具面闭包 law）未改且 8/8 通过。`WHAT[STRUCTURED-WORKFLOW-014]` law 重锚为新 owner：断言工具面分片的精确源集合（`FileMutationTools` + `FileMutationSurface` 四文件）、该分片不含任何 `Repository/Programming/Js/**` 源、runtime 分片的 `subsystem === 'repository-programming'` 且源集合恰为 8 个 Js surface、其引用恰为 `js-toolbindings`。
- **零符号剪枝的事实边界**：`Requirement/Grounding/Catalog.fs` 的 `open Wanxiangshu.Repository.Programming.Js` 经编译证明**仍在用**（`JsGlobFs.matchesPathPattern`，`Catalog.fs:90`），因此 `requirements → repository-programming` 是真实知识依赖——撤回删除并记录（文件与 fsproj 均按 sha256 还原）。`requirements` 留在 19 分量里不是遗漏，而是事实；不得再用处删该边。
- **领域事件名自有词汇（spine 不再复述领域名）**：新增两个零引用窄 contract 分片——`knowledge-reuse.repository-knowledge-casebook-event-vocabulary-contract`（`Repository/Knowledge/Casebook/EventVocabulary.{fsi,fs}` → `CasebookEventTypes`）与 `repository-programming.repository-programming-js-event-vocabulary-contract`（`Repository/Programming/Js/EventVocabulary.{fsi,fs}` → `JsTransactionEventTypes`），形态与既有 `Strength/EventVocabulary.fs`／`Sphinx/EventVocabulary.fs` 一致。`CasebookStore`（4 个 `InspectorCase*`）与 `JsToolsTransactionStore`（2 个 `JsTransaction*`）改为消费这些常量（对外常量名与字符串逐字不变），`Persistence/EventStore/EventVocabulary.fs` 的 builtins 改为 `yield! <Owner>.all` 组装；spine 只保留自己拥有的 `JournalEnvelope`／`ProjectionCutTail` 与 4 个 legacy `Job*` 名（后者注释记录：`src/` 内无生产者与消费者，仅 durable-convergence 的 law 仍持久化这些名）。accepted set 逐字不变（21 名）。`DURABLE-EVENTS-022` 编译边界 law 相应把两个新 contract 分片纳入 `CONTRACT_SHARDS` 与闭包白名单（`ALLOWED_CONTRACT_CLOSURE_SHARDS`），并把子系统断言扩到 `knowledge`／`repository-programming` 的 contract 分片；法律含义（contract 闭包不得含 physical/runtime 分片）不变，仍逐分片验证闭包。未新增行为测试：领域名漂移已由真实 store-driven 测试覆盖（`requirements/repository-programming/tests/js-*`、`requirements/speculative-investigation/tests/store.test.mjs` 等经 `dist/Persistence/EventStore/Surface.js` 追加真实域事实），再加一条只复述同一集合的测试不增加可失败性。
- **Long Stroke：本班 28 次有效运行 7 次失败（25%），并首次把失败分成两种模式**（失败世界保留 `/tmp/oc-e2e-pJLUig`、`/tmp/oc-e2e-VLgl1z`、`/tmp/oc-e2e-Qqwtu9`、`/tmp/oc-e2e-W2MkG2`、`/tmp/oc-e2e-q0ufTV`）：
  - **模式 A（60s 停顿；本班 5 次）**：`coder.0`（被 `holdChildC1UntilLabor` 挂住未发 `[DONE]` 的子会话首个响应）被 mock 派发一次后，子会话再无 `coder.1/2/3`；子会话只有 `Accepted`+`ProviderStarted`、**没有 `Terminal`**，而 manager 侧继续推进（`manager-interrupt.1` 与 `manager-current-action.0/1` 都被服务），最终 `Timed out waiting for expectation coder.3`。**判别式已实测（不再是猜测）**：给 `releaseHeldChild` 与 `respondStream` 的 `waitUntil` 加临时日志后复现两次（`/tmp/oc-e2e-W2MkG2`、`/tmp/oc-e2e-q0ufTV`），两次都打印 `wait-begin entry=coder.0` → `release-called` → `wait-end entry=coder.0 destroyed=false ended=false`，即 **harness 的释放正常、被 hold 的 SSE 仍存活、mock 与通过运行行为一致——mock/harness hold 不是阻塞源**（临时日志已按 sha256 还原，`git status` 证实两个 support 文件无改动）。真正停住的点在宿主侧：失败世界的宿主日志对子会话只有 `message=loop step=0` → `message=process` → `message=stream providerID=test modelID=test-model agent=coder` → **`message=loop step=1` 之后再无 `process`/`stream`**（两个独立失败世界同一签名），对应不到任何 provider 请求，也没有为该次尝试写下 `Terminal`；随后父 join 一直等 `external:child-completion`。因此定位为「被 hold 的子会话在被释放并完成后，宿主/插件在 step 1 未发出 provider 请求、也未落 terminal」的宿主侧停顿——下一刀应在此处取证（子会话 step 1 的 pre-turn 路径/会话门禁为何未推进），不要在 mock 侧继续追。
  - **模式 B（12s 快速失败；本班 2 次，其中 1 次为并发重建污染，见下）**：`[MOCK-FATAL] first script mismatch: reason=no-declared-turn session=... lane=manager kind=chat step=0`，候选集列出其它 lane 的条目（`protocol-repair.0`／`g2-inspector-*`）。即宿主在某 manager 会话上发出了脚本未声明的 turn，strict mock 直接 fatal。与模式 A 不同源：一个是「被 hold 的子会话不推进」，一个是「脚本对该 turn 无覆盖」。
  - 分布：批次一（无 instrumentation）2/12；批次二（`MOCK_TRACE=1`）3/11；hold 批次有效 5 次里 2 次失败，其余 5 次因与 `npm run build` 并发导致 dist 被重写而报 `ERR_MODULE_NOT_FOUND`，**不计入统计**（这是本班自己的操作污染，记录以免误读）。失败不集中在「构建后第一次运行」（批次一 run 1 与 run 12、批次二 run 3/6/10 混杂），此前「冷启动」解释被本班数据推翻。
- **门禁**：`node scripts/checks/subsystems.mjs` OK（26/**229**/709/1842，shard DAG 无环，cycle=19）；`node --test requirements/structured-workflow/tests/` 264/264；`node --test requirements/durable-events/tests/event-store-compile-boundary.test.mjs` 2/2；`node --test requirements/host-boundary/tests/m6-slice-boundary.test.mjs` 8/8；`npm run build` 全绿（165 surfaces，781 modules linked）；verification **3972/0**；integration **279/0**（all suites passed）；`node scripts/check.mjs` exit 0（trace proof gap 0）；`npm run format:check` exit 0；`npm pack --dry-run` OK（2046 files）。GAP-033 保持 **PARTIAL**。
- **下一批设计输入**：(a) **归属审计的负结果**：`namespace` 前缀与目录支配度都不是所有权证据——`Wanxiangshu.OpenCode`／`Wanxiangshu.Execution`／`Wanxiangshu.Repository` 是多子系统共用的集成命名空间，按 namespace 支配度会扫出 109 个「不符」，其中绝大多数是 composition 根与命名空间重合的假阳性；只有逐文件知识判定 + 兄弟分片约定 + 消费者符号语义才是证据（本班据此只改了一处真实混合分片）。(b) `strength ↔ persistence`、`work ↔ context` 等互反弧仍缺符号级证据，不得按计数剪。(c) `composition-durable-{fact,fold,projection}` 是 composition 根（`Fact.fs` 的 `AgentFact` 联合与投影注册），保持现状。(d) Long Stroke 模式 A 的下一刀在宿主侧 step 1 的 pre-turn/门禁路径取证；模式 B 需要给 manager lane step 0 的 turn 补脚本声明或确证这是产品的越界请求。

**14. Long Stroke 停顿根因：子会话下一步 provider 请求在插件 capacity 队列入队后永不获授（观测证据；修复未落地）：**

上一班已证伪 mock hold 并指出"宿主侧 step 1 停顿"，本班用三处临时插桩把它收敛到 `OpenCode/Host` 的真实代码路径，并保留了可复现的世界与追踪。

- **插桩与复现**：临时给 ①`PluginHostInterop.policyAwareHook` 包装层加 hook 进出追踪、②`PluginTransforms.normalTransform` 17 步流水线逐步追踪、③`ModelCapacity/Borrowing` 的 demand 入队/授予/拒绝/step 结束加追踪，重建 dist 后串行重跑 Long Stroke。失败世界 `/tmp/oc-e2e-moltHP`（hook 追踪）、`/tmp/oc-e2e-0Rqjug`（步级追踪）、`/tmp/oc-e2e-3fI7Qr`（capacity 追踪），追踪文件 `/tmp/hook-trace-fail.log`、`-fail2.log`、`-fail5.log`。全部插桩在交付前按 sha256 还原（`git status` 证明三个 support/产品文件无残留改动）。
- **决定性事实链**（以 `/tmp/oc-e2e-3fI7Qr` 为例，子会话 `…eI0jUTdP`，charge `msg_…H5kmiDe1HI`，target `test`）：
  1. 通过运行的对照：同一子会话 `[XSTEP] 1..16` 全过并 `[HOOK-EXIT] messages-transform`；失败运行在 step 1 只留下 `[XSTEP] 1 begin-physical`，之后没有任何 XSTEP、也没有 `[HOOK-EXIT]`——即卡在 `SessionExecutionBinding.beginPhysicalProviderAttemptForTransform` 的第 1 步内部。
  2. 该步内部是 `SessionExecutionBinding.beginSessionPhysicalProviderAttempt` → `ModelRouting.enterProviderStep` → `BorrowingCapacity.EnterStep`：demand 入队后返回一个 `TaskCompletionSource`，只有 `grant` 会完成它。
  3. step 0：`[CAP] add seq=43 … fence=0` → `[CAP] grant seq=43`（正常）。
  4. tool 边界：`[TB] end msg=msg_…H5kmiDe1HI run=msg_…slzQypiSPc` → `[CAP] endstep … inflight fence=0 inFence=false owner=test`（谓词命中，本应 `finishStep` 使 token 回到 Idle）。
  5. step 1：`[CAP] add seq=49 … fence=1` → **`[CAP] nogrant seq=49 … owned=true ordinary=true queue=1 sources=-1 tokens=1 token=inflight/test`**，该行在整个 60s watchdog 期间反复出现且没有任何后续 `grant`。
  6. 宿主日志与 SQLite 佐证：子会话 `message=loop … step=1` 之后再无 `process`/`stream`；mock 收不到任何 provider 请求；父 join 停在 `external:child-completion` 直到 watchdog（与上一班的宿主侧签名一致）。
- **机制**：`BorrowingCapacity.tryGrantDemand = tryGrantOwned || tryGrantBorrowed || tryGrantOrdinary`。当执行仍"拥有"该 execution key 的 token（`owned=true`）但该 token 不是可授予的 Idle 状态时，`tryGrantOwned` 失败、`tryGrantBorrowed` 无 credit source（`sources=-1`），而 `tryGrantOrdinary` 的第一条守卫 `if demandOwnsToken demand then false` 直接拒绝再 acquire 普通容量——三者皆不可行，demand 只能无限等待；`drain()` 只在 add/release/endstep/cancel 时重算，没有超时、重算或 typed fail-closed 出口。于是插件永不返回该 transform，宿主永不发出该 provider 请求，Long Stroke 以 60s watchdog 收场。
- **未闭合的最后一跳（下一班第一件事）**：step 0 的 tool 边界 EndStep 明明按 `inFence=false` 命中，为什么 step 1 入队时该 token 仍是 `inflight`。候选：(a) 同一 execution key 的 token 在 EndStep 之后被别的路径重新置为 InFlight（需要 token id 级追踪）；(b) 存在第二个 execution key/token（`tokens=1` 只统计 `OwnerSessionId` 相同的 token，同会话不同键不会被区分）；(c) `finishStep` 在 `Retiring` 分支走 `releaseToken` 后 `ownedTokenByExecution` 残留键，使 `demandOwnsToken` 为真而 `tryGrantOwned` 找不到可授予 token。诊断手段已具备：`Borrowing` 的追踪钩子可直接复用（临时插桩，交付前还原）。
- **修复必须守住的合同**（不得为让图变绿而放宽）：EMR-010「Host 开始执行 managed tool 时必须在任何 gate/tool body 之前用冻结的 `PhysicalUserMessageId` 结束该 provider step，使 token 进入可借用 idle」；EMR-004「同一 PhysicalUserMessageId 的执行与重试复用已有 target 与 capacity fence」；EMR-014「queue/release/commit 只返回封闭 outcome，duplicate 不得二次递减，token/waiter/map owner 必须可追溯到同一 exact identity」。修 token 生命周期时不得用超时、sleep、默认成功或重试兜底。
- **模式 B（`no-declared-turn`）证据**（与模式 A 不同源）：`/tmp/lst2-6.log` 第 205 行 `[MOCK-FATAL] first script mismatch: reason=no-declared-turn session=ses_f6fd9d29cffeLBoixBb72dFqHl parent=ses_f6fd9d370ffee2nA54RvB7263i lane=manager kind=chat step=0 … lastUser="# Establish read-only evidence about the current delivery through the entitled offices. Judge it independently on all eight dimensions, then submit the review tool once."`，候选集全部是 `g2-inspector-*`/`protocol-repair.0`/`manager-t1-*` 的 step-0 条目。即：一条 inspector/review charge 的 provider 请求落到一个被解析为 `manager` lane 的会话上，而脚本只为 inspector 条目声明了绑定；要么产品把该 charge 路由到了错误会话，要么 mock 的 lane 解析对该产品新会话缺失绑定。下一步：带 `MOCK_TRACE=1` 复现并在失败时保留世界，用 `session` 表确认 `ses_f6fd9d29…` 的 parent/agent 与产品侧 prompt 路由记录，二者取一后再决定改产品还是补脚本（不得两边同时动）。
- **结构侧负结果（本班）**：`sphinx → persistence` 的 6 条引用逐条有真实符号消费（`EventEnvelope`/`EventStreamId`/`normalize`、`CanonicalIntegrator.createWithRules`/`baseRules`、`IntegrationRule`、`IEventStore`/`AppendError`/`SemanticCut`、`EventStore.createLocal`），属诚实知识依赖，**不得删**；`opencode-host-hostsignalbootstrap` 的 33 条引用按声明符号逐条检查也未发现零符号候选（本班未做 compile 探针）。

15. **EMR-010 属主 transform 入口回收（Long Stroke 模式 A 危墙拆除）；SCC 保持 19**：

上一班把模式 A 钉到「属主 owned credit 借给 companion blogger 后，blogger 的 `EndStep` 因 `inFence=true` 拒结束 → token 永久 InFlight → 属主下一步 `owned=true` 又被 `demandOwnsToken` 堵死 ordinary」。本班按用户钉死的语义做结构修复，而不是再测一轮：

- **语义**：回合内后代借用可阻塞属主；一旦属主自己的 `experimental.chat.messages.transform` 为同一 owned credit 再次进入后续 provider step，必须在仲裁前回收该 credit 上任何外来 InFlight/Retiring step。这本来就是 HOW 写明的「messages.transform = 容量仲裁 + Step Fence 拦截」；缺口是 `reconcileFence` 只按 step 的 `(session,msg)` 匹配，借出后 step 已是后代，属主再入捞不到自己的 token。
- **落地**：`BorrowingCapacity.reconcileFence` 在自有 fence 推进之外，按 `ownedTokenByExecution[ownerKey]` 找到属主 credit，若其 step 属于外来会话/消息则 `finishStep` 回收；helper 拆成 `tryDictionaryValue` / `stepBelongsTo` / `foreignBorrowedStepToken`，控制金字塔扫描仍为 0 nested decisions。
- **法律**：`EMR-010` WHAT/HOW 同步写明「显式 release/retire 仍等借用方 step 结束；属主 transform 触发即回收；等待中的 borrower 仍按单调序号优先，不得被该回收饿死」。
- **能失败的旧实现证明**：`EMR_010_owner_transform_entry_reclaims_foreign_inflight_borrow`——descendant borrow 把属主 credit 留在 InFlight，属主再入 `enterProviderStep` 必须立刻获授、不得留下 waiter；旧实现会在此处挂死。既有 `EMR_010_older_borrowed_step_precedes_later_owned_step` 仍绿（借用优先序未伤）。
- **e2e 证据**：修复后 16 次串行 Long Stroke = 15 通过 + 1 次 11s 模式 B（`[MOCK-FATAL] no-declared-turn` inspector charge 落在 `lane=manager`）；**0 次 60s 模式 A capacity 卡死**。模式 B 是独立缺口（脚本 lane 绑定 / 产品路由），不得与模式 A 混修。

验证：`subsystems` 26/229/709/1842 cycle=19；EMR suite 78/0；verification **3973/0**（+1 回收证明）；integration 279/0；`check.mjs`/`format:check` exit 0。GAP-033 仍 PARTIAL（本班未拆 SCC，是 capacity 语义危墙拆除）。

16. **切断 requirements → repository-programming：纯 glob 模式匹配下沉 runtime-platform narrow shard；SCC 19 → 18**：

按第 13 项记录的事实边界做诚实结构下沉，将纯算法与物理能力分层，切断 `requirements` 留在 SCC 环内的唯一跨子系统出边：

- **依赖成因与历史证据**：第 13 项在尝试对 `requirement-grounding-model` 做零符号剪枝时，编译探针证明 `Requirement/Grounding/Catalog.fs` 真实调用了 `open Wanxiangshu.Repository.Programming.Js` 的 `JsGlobFs.matchesPathPattern`（用于接地目录中文件模式匹配），证明 `requirements → repository-programming` 是由单一消费者带来的真实知识依赖，此前按真实性原则予以保留并记录事实。
- **纯算法与物理分界（Pure vs. Physical Split）**：审计证明 `matchesPathPattern` 及底层正则转换（`compilePathPattern`/`wildmatchRegex`/`takeClass`/`regexTest`）是完全脱离领域与 IO 的纯字符串/正则匹配（string → regex → bool），并不依赖文件系统状态；而 `node:fs` 物理扫描、忽略规则树与目录遍历等物理能力才真正属于 `repository-programming`。将纯算法与物理文件操作混合在同一分片，导致仅需模式判定的 `requirements` 被迫拉入重量级的 `repository-programming-js-capability`。
- **新 Narrow Shard 合同与双向接线**：
  - **新分片与合同**：新增 `runtime-platform` 子系统的零引用窄分片 `Wanxiangshu.Owner.runtime-platform.foundation-globmatch.fsproj`（`<WanxiangshuSubsystem>runtime-platform</WanxiangshuSubsystem>`，`<WanxiangshuCompileShard>globmatch</WanxiangshuCompileShard>`，0 ProjectReferences），包含 `src/Wanxiangshu/Foundation/GlobMatch.{fsi,fs}`。导出 `namespace Wanxiangshu.Foundation`、`module GlobMatch`、`type GlobPatternError = | InvalidPattern`，以及三个逐字迁移的纯函数：
    - `val compilePattern: pattern:string -> Result<obj, GlobPatternError>`
    - `val testCompiled: regex:obj -> text:string -> bool`
    - `val matchesPathPattern: pattern:string -> path:string -> Result<bool, GlobPatternError>`
  - **分片接线（Rewire）**：
    1. `Repository/Programming/Js/GlobFs.fs`：移除纯匹配实现，内部委托给 `GlobMatch`（将 `GlobPatternError.InvalidPattern` 映射保留为 `JsFailure.AnchorInvalidPattern`），对外的 `JsGlobFs` 公共形状与 `glob` 等物理扫描逻辑完整保留。
    2. `Requirement/Grounding/Catalog.fs`：改为 `open Wanxiangshu.Foundation` 并调用 `GlobMatch.matchesPathPattern`（保持 `Error _ -> invalidOp` 错误处理分支）；从 `Wanxiangshu.Owner.requirement-grounding.requirement-grounding-model.fsproj` 中彻底移除 `<ProjectReference ...repository-programming-js-capability.../>`。
  - **拓扑收益**：`requirements` 子系统（唯一分片 `requirement-grounding-model`）的所有跨子系统出边归零，彻底退出 SCC 最大强连通分量。
- **验收与门禁指标**：
  - 聚焦编译：`runtime-platform.foundation-globmatch`、`repository-programming.repository-programming-js-capability`、`requirement-grounding.requirement-grounding-model` 三个分片独立编译全部通过（focused compile exit 0）。
  - 环解耦：`node scripts/checks/subsystems.mjs` 证实 `requirements` 退出 SCC，最大强连通分量由 19 降至 18，拓扑规模 26/230/710/1843。
  - 回归证据：接地范围解析等领域行为测试保持全绿（`requirements/grounding/...` 保持通过，由 parent 在 rebuild 后统一跑完门禁确认）。

验证：`subsystems` 26/230/710/1843 cycle=18；verification 3973/0；integration 279/0；`check.mjs`/`format:check` exit 0。GAP-033 保持 **PARTIAL**。

17. **全仓零符号跨分片引用剪枝（persistence、rules 与 host/runtime 边界收敛）；SCC 保持 18**：
   - 依赖与真实知识核对：经全仓 AST/符号审计与源码比对，以下 7 个分片中的 11 条 ProjectReference 经证明在调用方 `.fs`/`.fsi` 中零符号消费，系历史重构残留或过宽父项目引用；切除后各分片均独立 focused compile 通过，未引入任何伪解耦（无反射、无动态查找、无字符串标签）：
     1. `persistence/persistence-journal-promptfactcodec`：切除未引用的 `chat-execution/foundation-outcome`、`host/host-fatal-effect` 以及 `runtime-platform/foundation-async-support`（3 条）；
     2. `sphinx/sphinx-integration-rules`：切除未引用的 `runtime-platform/foundation-identity` 与 `persistence/persistence-eventstore-model`（2 条）；
     3. `knowledge/knowledge-integration-rules`：切除未引用的 `persistence/persistence-eventstore-model`（1 条）；
     4. `session-lifecycle/opencode-host-turnruntimepreparation`：切除未引用的 `runtime-platform/foundation-async-support`（1 条）；
     5. `repository-programming/repository-programming-js-integration-rules`：切除未引用的 `persistence/persistence-eventstore-model`（1 条）；
     6. `strength/strength-integration-rules`：切除未引用的 `runtime-platform/foundation-identity` 与 `persistence/persistence-eventstore-model`（2 条）；
     7. `process/foundation-temporal`：切除未引用的 `participant/foundation-roles`（1 条）。
   - 结构影响：`subsystems: OK — 26 subsystems, 230 compile shards, 710 sources, 1830 refs, shard DAG, largest subsystem cycle=18`。引用总数从 1841 净减 11 条至 1830 条（其中 6 条为环内跨子系统引用）。
   - 验证：7 个分片 focused Fable compile 全部通过（指纹与耗时：promptfactcodec `fp:923c51bc3cdc`, sphinx-integration-rules `fp:1a52067f1dae`, knowledge-integration-rules `fp:4b643d16a62b`, turnruntimepreparation `fp:c21a317ba66b`, js-integration-rules `fp:c04d681c55e2`, strength-integration-rules `fp:340234c49ebd`, foundation-temporal `fp:2c33b5a7852b`）；`npm run build`（1420 items / 782 modules）；`verification`（3973/0）；`integration`（279/0）；`e2e`（1/1 通过）；`check.mjs` / `format:check` / `npm pack --dry-run` 全部通过。

18. **全仓零符号跨分片引用剪枝第二轮（provider-language、enforcer-codec）；SCC 保持 18**：
   - 依赖与真实知识核对：以全仓分片级标识符扫描（目标分片的 namespace/module/type 与调用方 `.fs`/`.fsi` 全文比对）遍历全部 230 个分片，再次筛出两条零符号直接引用，并逐条以源码核对确认：
     1. `runtime-platform/participant-provider-language`：切除未引用的 `runtime-platform-assemblyinfo`（`Wanxiangshu.Resources.PackageMetadata` / `PackageResources` / `ProviderResourceBytes` / `ModelRoutingResource`）。调用方 `SessionLanguage` / `Prose` / `ProviderLanguageBinding` 只消费同分片的 `ProviderResources`；`ProviderResources` 自身对 `ProviderResourceBytes` 的依赖仍由保留的 `runtime-platform/language` 引用传递，未切断。
     2. `enforcer/enforcer-codec`：切除未引用的 `host/host-session-runtime`（`Wanxiangshu.OpenCode.ISessionQuiescenceGate` / `SessionQuiescenceGate` / `SessionSnapshotPort` / `QuiescenceSurface` / `HostMessageProjection` / `HostSessionContext` / `HostBoundarySurface` / `HostSessionContextSurface`）。`Enforcer/Surface.fs` 的 `open Wanxiangshu.Resources` 由保留的 `distribution/resources-enforcercatalogresource` 提供；目标分片无模块级副作用，删除不会丢失注册行为。
   - 该启发式在其余 228 个分片上零候选，说明“静态零符号引用”剪切面已穷尽；更深层的跨子系统知识耦合不属于删 XML 行的范围，见下条事实记录。
   - 结构影响：`subsystems: OK — 26 subsystems, 230 compile shards, 710 sources, 1828 refs, shard DAG, largest subsystem cycle=18`。引用总数 1830 净减 2 条至 1828 条；两条删除均不改变 subsystem 边集合（`enforcer → host` 仍由其余 6 条分片引用构成），SCC 未变属预期。
   - 验证：两个分片 focused Fable compile 全部通过（participant-provider-language `fp:6fe5046a764c`、enforcer-codec `fp:af46444848fe`）；`npm run build`（1420 items / 782 modules）；`verification`（3973/0）；`integration`（279/0）；`e2e`（1/1 通过）；`check.mjs` / `format:check` / `npm pack --dry-run` 全部通过。
   - 本班确认的未闭合事实（供后续批次定位，不构成已完成的解耦）：
     a. `persistence/eventstore-event-vocabulary-contract` 的 `AuthoritativeEventTypes` 静态 `open` 了 `sphinx` / `strength` / `knowledge` / `repository-programming` 四个域词汇契约，使 persistence 持有全部署事件名知识。倒置方案（`IntegrationRule` 自带 `Vocabulary`，integrator 由注册规则求并集，`Store.validateVocabulary` 改问注入的 integrator）能把该知识移到装配点；但它同时改变只注册 `baseRules` 的 store（delegation / journal / verification 面）的接受集，实施前须先证明这些 store 不追加域事件。
     b. `persistence/durable-runtime-surface` 分片（`Persistence/EventStore/Surface.fs` 的 full-program 装配：`baseRules @ Strength @ Sphinx @ Casebook @ JsTransaction`）在分片图中零消费者，但该模块落成 `dist/Persistence/EventStore/Surface.js`，被 durable-events / durable-convergence / effect-accounting / epistemic-reasoning / knowledge-reuse / repository-programming 六包测试直接 import，其路径与行为是现存测试合同，不能仅凭“零消费者”删除或改路径。
19. **业务/纯决策分片不再直读外部物理适配器（delegation 计时、enforcer 修复、relay 快照）；SCC 保持 18**：
   - 判据与复现：分片级审计"非 composition 分片 → 外部子系统 runtime/adapter 分片"的引用（`buildSubsystemInventory()` 的 `legacyKind` 分类 + `references`），并逐条读源码判定是合同/纯数据使用还是实现读。倒置前 5 条，本刀后剩 2 条，其中 1 条为标签偏差、1 条为已知未闭合方案（见下）。
   - 1. `delegation/execution-delegation-fork-runtime`、`delegation/delegation-host-adapter`：删除 `defaultArg clock (NodeTiming.nodeClockPort ())` 这类"缺省回退到外部物理适配器"的分支（`ForkRuntimeBackendState` 为 private 且唯一构造点始终传入 clock，回退在生产不可达），并把 `NodeTiming.raceExit` 改为 composition 注入的必需能力 `raceExit: Task -> int -> Task<bool>`；`HostForkRuntime` 同样把 clock 与 raceExit 收成必需参数，调用方从 injectable 默认参数变为显式装配。接线点均为 composition 分片：`Change/Host/Host.fs`、`Fork/Host/HostForkPtySurface.fs`、`Fork/OpenCode/JoinSurface.fs`、`OpenCode/Tools/ToolRuntimeScope.fs`（两个构造点）。两个分片的计时引用由 `process-node-timing-adapter`（物理适配器）换成 `foundation-temporal-contract`（`IClockPort` 窄合同），行为不变（注入的仍是 `NodeTiming.nodeClockPort ()` / `NodeTiming.raceExit`）。
   - 2. `enforcer/repair`（纯决策分片，无 runtime/composition 身份）恢复纯净：`Enforcer/Cycle/Decode.fs:141` 的 `Diagnostic.emit`（host 诊断运行时，读 `WANXIANGSHU_DIAG` 并写 `console.error`）与 `Enforcer/Cycle/BloggerProbe.fs:62` 的 `ProviderWireCapture.tryPhysicalParentOfProviderRun`（provider 线解码适配器）改为必需注入的能力参数（`emitDiagnostic`、`tryPhysicalParent`，镜像原函数签名），装配点在同子系统的 composition 分片 `enforcer-continuation` 与 `context/context-companion-fact`；随后删除 `host-diagnostics-runtime`、`opencode-codec-providerwiredecode` 两条引用。CTX-014 白名单与诊断文本不变。
   - 3. `relay/mission-relay-workspace-snapshot`：不再直接调用物理 git 子进程适配器（`Git/Subject.fs` 使用 `child_process.execFileSync`）。新增 relay 侧窄能力 `WorkspaceSnapshotGitCapability`（六个 git 操作逐个镜像 `Git/Subject.fsi` 签名），`capture` 改为必需参数；由四个 composition 分片用 `Wanxiangshu.GitSubject` 函数装配：`mission-relay-reviewtool`、`mission-relay-suicidetool`、`composition-turn-workflow`、`plugin-composition`。删除 `runtime-platform-git-subject` 引用，git 命令与哈希语义不变。
   - 结构影响：`subsystems: OK — 26 subsystems, 230 compile shards, 710 sources, 1829 refs, shard DAG, largest subsystem cycle=18`。引用数 1828 → 1829（净 +1：composition 侧接线 +6，纯分片 −3，delegation 物理计时适配器 −2；新增的接线引用落在 composition 分片，正是 013 允许的装配点）。SCC 仍为 18：本刀移除的是"实现读"，不是合同或装配边，环由合同边维持，环数不变是预期结果而非失败。
   - 边界证明更新：`requirements/time-capability/tests/m6-slice-boundary.test.mjs` 的 `process-node-timing-adapter` 消费者白名单移出 `delegation-fork-runtime`、`delegation-host-adapter`（二者改为只消费 `foundation-temporal-contract`），TIME-008 的物理适配器消费者集合据此收紧；该断言仍逐分片冻结允许直连物理适配器的分片集合。
   - 验证（最终字节）：受影响分片 focused Fable compile 全部通过（`execution-delegation-fork-runtime` `fp:2c7c1fc65f39`、`delegation-host-adapter` `fp:40e53d492ce3`、`enforcer-repair` `fp:38365c832323`、`enforcer-codec` `fp:a85346385b2f`、`enforcer-continuation` `fp:e478c5c17f3f`、`context-companion-fact` `fp:a87bd8940dd1`、`context-companion-companionfactfold` `fp:090a5960791d`、`mission-relay-workspace-snapshot` `fp:8c871faac5bf`、`mission-relay-reviewtool` `fp:698232bc881d`、`mission-relay-suicidetool` `fp:db034fb9d80a`、`composition-turn-workflow` `fp:2c0989af7e65`、`plugin-composition` `fp:eca5e8192925`、`git-integrationgate` `fp:97f1d643f980`、`delegation-runtime-surface` `fp:e034ba67e244`、`opencode-host-pluginruntimescope` `fp:a01b6ff38ac3`）；`npm run build`（1420 items / 782 modules）；`node scripts/check.mjs`（requirement-trace 814 WHAT / 4016 tests，0 proof gap）；verification `3973 passed, 0 failed`；integration 全部套件通过；e2e（Long Stroke）exit 0，1/1；`format:check` 与 `npm pack --dry-run`（2047 files）通过；边界测试 21/21。
   - 未闭合（下一刀依据，已勘察）：`sphinx/sphinx-serve-runtime(runtime) → persistence/eventstore-core-runtime(runtime)`。事实：`Sphinx/McpServer.fsi` 已暴露注入面（`create: SessionStore -> obj`、`createDurable: SessionStore -> IEventStore -> obj`、`serveStdio`、`serveDurable`），只有进程入口 `serveDefault`→`serveDurableDir` 自己构造 `CanonicalIntegrator.createWithRules (baseRules @ SphinxIntegrationRules.rules)` 与 `EventStore.createLocal`；`McpServer` 只需要 `IEventStore`。方案：把这两处构造移到 host 侧 composition 入口，并把 `OpenCode/Host/SphinxMcpConfig.fs:37` 的 spawn 模块路径改指该入口。约束：`dist/Sphinx/McpServer.js` 仍须存在（`scripts/build.mjs:153` 断言），且 `requirements/epistemic-reasoning/tests/mcp-stdio.test.mjs:543,579`（EPI-030/EPI-019 跨进程恢复证明）目前直接 spawn 该路径，迁入口必须同步改这两处测试且不得削弱跨进程恢复性质。
   - **更正（第 21 条）**：本条曾把 `enforcer/resources-runtimeresources(runtime) → resources/resources-promptresources(runtime)` 判为"只消费纯数据的标签偏差（不修）"。该结论有误：`RuntimeResources.loadFor` 当时直接调用 `PromptResources.loadForLanguage`/`instructionTextsForRole`，其调用链终点是 `PackageResources.readText` 的 `node:fs.readFileSync` 文件读取，属于业务 runtime 分片经外部 runtime 分片取得文件 authority 的 014 违规，已由第 21 条修复。教训：分片级标识符扫描只能证明"用了哪些符号"，判定是否物理效果必须顺手读完调用链到 node:fs / 子进程 / 网络边界。

20. **sphinx 服务进程入口与 durable store 装配迁到 composition 分片（013/014 最后一条 class-(c) 倒置）；SCC 保持 18**：
   - 判据与复现：对全部 `runtime-platform` 分片与全部 domain `runtime` 分片做反向审计（引用目标 subsystem 的 `legacyKind` + 逐符号读源码分三类：纯数据/合同 (a)、领域 decision (b)、物理实现 (c)）。结果：`runtime-platform` 的 13 个分片对 domain subsystem 的引用数为 **0**；domain runtime 分片里 `sphinx-serve-runtime` 是唯一"自建外部物理实现"的 (c) 类倒置。
   - 违规事实：`Sphinx/McpServer.fs:1219-1248` 的 `serveDurableDir` 自己执行 `CanonicalIntegrator.createWithRules (CanonicalIntegrator.baseRules @ SphinxIntegrationRules.rules)` 与 `EventStore.createLocal commonDir (Guid.NewGuid().ToString("N")) integrator`；`McpServer.fsi` 早已暴露 `createDurable`/`serveDurable`（注入 `IEventStore`）这一注入缝，缺的只是把"选哪个 store、用哪些规则、进程如何启动"移出 runtime 分片。
   - 迁移：新增 `Sphinx/ServeEntry.fs`（`module ServeEntry`），逐字搬迁 `argv[1]` 入口守卫、`SPHINX_COMMON_DIR` 读取（含 `WHAT[EPI-030]` 注释）、store 装配与 `[sphinx-mcp] durable boot failed:` → `exit(1)` 的失败路径；`McpServer.fsi` 以 `bootDurable: IEventStore -> Result<SessionStore, string>` 取代 `serveDefault`（`serveStdio`/`serveDurable`/`create`/`createDurable` 不变）。入口文件由 sphinx 子系统既有的 composition 分片 `sphinx-integration-rules` 编译——该分片本就持有 sphinx 侧的持久化桥（`IntegrationRules`），因此装配留在 sphinx 子系统内部，不新增跨 subsystem 边，也不让 host 侧 adapter 越界承担 domain 装配。
   - 引用账（符号证据）：`sphinx-serve-runtime` 剪除 4 条——`sphinx-integration-rules`、`persistence-eventstore-canonicalintegrator`、`persistence-eventstore-processeventlog`、`foundation-llm-facing`；`sphinx-integration-rules` 增加 4 条——`sphinx-serve-runtime`、`persistence-eventstore-canonicalintegrator`、`persistence-eventstore-processeventlog`、`persistence-eventstore-types`（`IEventStore` 合同分片）。净引用数不变。
     **教训（零符号剪枝必须双向 grep）**：scout 报告 `foundation-taskresult` 为死引用是错的——大写 `TaskResult` 无命中，但 `taskResult { ... }` 计算表达式（`Wanxiangshu.Foundation`，`McpServer.fs:793/813/846`）真实使用它；照搬报告剪枝会导致编译失败。任何"零符号"结论必须大小写不敏感并与真实编译互相印证（本次由 implementer 的 focused compile 拦下）。
   - 入口路径单一真值：`Sphinx/Mcp.fs` 的 `relativeServerEntry` 改为 `"dist/Sphinx/ServeEntry.js"`，host 侧 `SphinxMcpConfig.fs` 不再自写 `dist/Sphinx/McpServer.js` 字面量，改为 `pathJoin (packageRoot (), SphinxMcp.relativeServerEntry)`。三处断言同步收紧为一致性性质而非并列字面量：`scripts/build.mjs` 断言存在的是**发布数据所指**的入口产物（`owner-compile.mjs` 的 `essentialOutputs` 同步），`kernel.test.mjs` 读 `dist/Sphinx/Surface.js` 的 `relativeServerEntry` 再断言 build 脚本包含它，`mcp-stdio.test.mjs` 的 `serverEntry` 亦由该数据派生。宿主侧不再需要一条"启动路径以数据结尾"的断言：`SphinxMcpConfig.fs` 已改为直接由该数据构造路径（单一真值，漂移在类型层面不可能），且 `host-boundary` 包按 surface manifest 法不允许导入 `Sphinx/Surface.js`（曾试图加该断言，被 `js-surface-manifest` 门禁以"no law or declared consumer edge"拒绝——不为一条冗余断言新开消费者边）。
   - 行为不变性：`SPHINX_COMMON_DIR` 的语义（缺失/空白 → 内存态 `Session.defaultStore`）、store 构造参数、integrator 规则集、stderr 文案与退出码、入口守卫全部逐字保留；EPI-019/EPI-030 的跨进程重启证明（`mcp-stdio.test.mjs` 直接 spawn `dist/Sphinx/ServeEntry.js`）覆盖新入口。
   - 同批发现的排序 backlog（本轮未改，均有符号证据）：(c) `delegation/delegation-recovery-runtime → persistence/persistence-journal-agentjournal`：`AgentJournal.appendAgent`（`Handle/Controller.fs:56,231`、`Handle/JoinDrain.fs:84`）/`Writer.BlobWriter.Read`（`JoinDrain.fs:374`、`CompletionCodec.fs:277`）——journal 句柄已由参数注入，但类型是对方的 composition 句柄而非窄 capability，且 `HandleProjection.*`（`Controller.fs:217,249`、`JoinDrain.fs:93-95`）仍从该句柄读取。(c) ~~`enforcer/resources-runtimeresources → resources/resources-promptresources`~~（**已由第 21 条关闭**：装配搬出 runtime 分片、`PromptCatalog` 拆为纯数据 contract 分片）：原记录是 `PromptResources.loadForLanguage`/`instructionTextsForRole` 经 `ProviderResources.readText` 读资源文件，需先定资源读取的 capability 边界再动。(b) `session-attachment-runtime → dispatch/dispatch-model-contract` 的 `SessionRecovery.validateClosurePure` 属合同分片暴露的纯 decision，按 013 允许，不需处理；`resources-runtimeresources → enforcer-catalog-resource` 为同 subsystem 内引用，不在 013 约束内。
   - 过程约束（本轮被门禁拦下三次，后来者直接照做）：① 分片清单法要求每个实现文件都有 sibling `.fsi`（`scripts/lib/compile-shards.mjs:113` "compile shard must carry its sibling .fsi"），新入口因此补 `Sphinx/ServeEntry.fsi` 且只暴露 `serveDefault`；② `fsharp-control-pyramid` 禁止 `try { ... match ... }` 这种 depth≥2 形状，启动逻辑必须像原来的 `serveBootOutcome` 一样把 match 抽成独立 helper 再在 `try` 里调用；③ `js-surface-manifest` 按包法禁止 `host-boundary` 包导入 `Sphinx/Surface.js`（"no law or declared consumer edge"），故不为一条冗余一致性断言新增消费者边——宿主路径已由数据构造，漂移在类型层面不可能。
   - 指标：26 subsystems、230 compile shards、**711** sources（+1 = `Sphinx/ServeEntry.fs`）、**1829** refs（净 0：serve-runtime −4、integration-rules +4）、shard DAG 无环、最大 subsystem SCC **18**（不变：入口搬迁只改"谁装配"，sphinx→persistence 仍由合同引用维持）。
   - 验证（最终字节）：focused compiles `sphinx-integration-rules`（fp `d7a02059e065`）、`sphinx-serve-runtime`、`durable-events.runtime`、`host-boundary.sphinx-host-adapter` 全部通过；`npm run build`（1421 items / 783 modules）；`node scripts/check.mjs` 全绿（control-pyramid clean、requirement-trace 814 WHAT / 4016 tests / 0 proof gap）；verification **3973 passed, 0 failed**；integration 全部通过；e2e（Long Stroke）exit 0；`format:check` 与 `npm pack --dry-run`（2048 files）通过；边界测试 21/21；`mcp-stdio.test.mjs` 12/12。冒烟：`node dist/Sphinx/ServeEntry.js` 在 `SPHINX_COMMON_DIR` 下回应 `initialize`（`serverInfo = {name:"sphinx",version:"0.9.0"}`，stderr 空），旧 `dist/Sphinx/McpServer.js` 不再自执行（exit 0、无输出）。独立审查结论 `correct`、0 findings。

21. **enforcer 运行时资源分片只持数据与访问器，物理装配回到 composition；`PromptCatalog` 成为 resources 的纯数据 contract shard；SCC 保持 18**：
   - 判据与复现（脚本化，可重跑）：遍历全部 kind ∈ {runtime, pure, contract} 的分片，统计其对外部子系统 {composition, adapter, runtime} 分片的**带限定名符号使用**（目标分片声明的 module/type 名 + `.`，全文计入 `.fs`/`.fsi`）。全仓命中 7 对：6 对是 delegation→persistence 的 journal/projection 族（仍是 backlog），第 7 对即本条：`enforcer/resources-runtimeresources [runtime] → resources/resources-promptresources [runtime]`（9 处 `PromptResources.*`）。
   - 物理效果事实链（读完调用链，不只数符号）：旧 `Resources/RuntimeResources.fs` 的 `loadFor` 调 `PromptResources.loadForLanguage`/`instructionTextsForRole` → `ProviderResources.readText` → `ProviderResourceBytes.readText` → `PackageResources.readText` → `[<Import("readFileSync","node:fs")>]`；即业务 runtime 分片靠外部 runtime 分片取得文件 authority（014：「业务代码不得因为复用 helper 而获得…具体 Host implementation」）。
   - 迁移：`PromptCatalog`（9 个 string 字段的纯数据 record）从 runtime 分片拆到新 contract 分片 `resources/resources-promptcatalog`（**零 ProjectReference**，无 open、无 I/O）；`RuntimeResources.loadFor`/`load` 逐字搬到 `Resources/RuntimeResourceAssembly.{fsi,fs}`，由 enforcer 既有 composition 分片 `enforcer-catalog-resource` 编译——该分片本来就持有 enforcer 规则书装载与 Blogger 规则书合成，装配因此留在拥有该知识的 subsystem 内部，没有新增跨 subsystem 知识边。runtime 分片只剩 record + `install`/`current`/`enforcerRulesFor`（进程级资源槽的 DSL-MUTABLE 语义未动）。
   - 引用账：`resources-runtimeresources` −3（`resources-promptresources`、`participant-identity.foundation-roles`、`distribution.resources-enforcercatalogresource`）/+1（`resources-promptcatalog`）；装配分片 +2（`resources-promptcatalog`、`resources-runtimeresources`，后者是 record 类型的真实来源）；`resources-promptresources` +1（新 contract 分片）；三个直接调用方各 +1（`resources-promptsurface`、`opencode-host-managedagentconfig`、`plugin-composition`，它们原本靠传递可见，现在直接调用必须声明）。净 refs 1829 → 1834。
   - 闭包证据（前/后各测一次，方法：同一闭包脚本分别跑在 `git worktree add HEAD` 的旧树与当前树）：runtime 分片前向闭包 **8 → 5** 个分片，被移出的是 `resources-promptresources`（文件读取实现）与 `distribution.resources-enforcercatalogresource`；剩余 5 个为 record 所需的类型/纯数据（`enforcer-catalog` 合同、`resources-promptcatalog`、`runtime-platform.language` 及两个传递合同分片）。最大 subsystem SCC 仍 18——本刀搬的是装配与数据契约，不是合同边，环数不变是预期。
   - 行为不变性：两次规则书 locale 装载、`rules` 选择、`PromptResources.loadForLanguage`、`instructionTextsForRole Role.Blogger` 与 `composeBloggerSystemPromptFor` 的调用次序、`ProviderLanguageRootsReady = ProviderResources.languageRootsPresent ()`、`load = loadFor English`、`PromptSurface` 四个 JS 面函数与 `RuntimeResources.current ()` 的异常文案全部逐字保留。
   - 过程约束：装配分片必须显式声明对 record 所在 runtime 分片的引用（`RuntimeResourceAssembly` 返回 `RuntimeResources`）；implementer 按并行批次规则未跑编译，该缺失引用由父任务的 focused compile 拦下（`RuntimeResourceAssembly.fs(37,19): The type 'RuntimeResources' is not defined`）并补上——平行刀必须以 focused compile 收口。
   - 验证（最终字节）：8 个受影响分片 focused Fable compile 全通过（`resources-promptcatalog` fp `965d1e88a062`、`resources-promptresources` `bbfa1ca4cd65`、`resources-runtimeresources` `62e183726cc4`、`resources-promptsurface` `e1a36473409f`、`enforcer-catalog-resource` `e7f7ae76104f`、`opencode-host-managedagentconfig` `61d9cb42e185`、`plugin-composition` `1f52589cb190`、`provider-system-transform` `ce93eb770411`）；`npm run build` 785 emitted modules；`node scripts/check.mjs` 全绿（814 WHAT / 4016 tests / 0 proof gap）；verification **3973 passed / 0 failed**；integration 全部通过；e2e（Long Stroke）exit 0；`format:check`、`npm pack --dry-run` 2050 files；结构 `26 subsystems / 231 shards / 713 sources / 1834 refs`、cycle=18；冒烟 `dist/Resources/PromptSurface.js`：`runtimeLoad()` 返回 120 条规则 + 双语规则书 + `ProviderLanguageRootsReady=true`，`runtimeLoadForLanguage('zh-CN')` 返回中文目录，`runtimeInstallFromPackage()` 后 `runtimeCurrent()` 可读，Blogger 提示词含规则书合成结果。
   - 未闭合 backlog（更新）：`delegation/delegation-recovery-runtime → persistence/persistence-journal-agentjournal`（含 `composition-durable-fact`/`-projection` 的 6 对符号使用）仍是下一刀候选：`AgentJournal` 是对方的 composition 句柄而非本侧窄 capability，涉及 60 个消费者分片，需要合同分层而不是逐文件换类型。

22. **delegation 业务运行时改用 delegation 自有 durable capability port；`AgentFact` 包装回到 durable composition；空转证明换成仓库事实证明（DELEG-029 未实现面落地）**：
   - 判据来源是既有条款，不是本班自造：`requirements/delegation/WHAT.md` DELEG-029 要求「Host runtime、recovery、fork、fold与sync 只消费 delegation-owned append/query/wait/clock/PTY capability types」，并规定「durable composition 是把这些 case 包入 `AgentFact` 并组合 projection 的唯一位置」。实现却相反：`delegation-recovery-runtime`（runtime）形参是 `AgentJournal`（persistence composition 的具体类），直接调用 `AgentJournal.appendAgent` / `AgentJournal.handleProjection`，并伸手进 `durable.Writer.BlobWriter.Read`；`delegation-fold`（runtime）还持有把 delegation case 包成 `AgentFact` 的桥接模块 `Execution/Delegation/Fact.{fsi,fs}`。
   - 审计口径（脚本化、可重跑）：遍历 kind ∈ {runtime, pure, contract} 的消费者 → 外部 composition/adapter/runtime 分片的带限定名符号使用。本班前命中 6 对（`delegation-recovery-runtime` → agentjournal 112 / durable-fact 100 / durable-projection 129；`delegation-fold` → durable-projection 52 / durable-fact 33 / authority-fact 23），其中前 5 对正是本条；改动后剩 3 对真实命中（见下）加 1 条命名空间前缀假阳性。
   - 迁移：新增 delegation 自有 contract 分片 `delegation-journal-port` 声明 `AgentJournalPort`（`AppendExecutionFact: SessionId -> ExecutionFactCases -> Task<Result<unit, string>>`、`HandleProjection: SessionId -> AgentLinkageProjection`、`ReadBlob: BlobRef -> Task<Result<string, string>>`、`WriteBlob: string -> Task<Result<BlobRef * BlobDigest, string>>`；四个成员覆盖运行时对 journal 的全部使用，grep 穷举得出），由 `delegation-recovery-runtime` 及其 .fsi 消费；新增 persistence composition 分片 `durable-journal-port-adapter` 用 `AgentJournalPortAdapter.fromAgentJournal` 实现该 port（`AgentFact.Execution` 包装、`JournalAppendFailure.describe` 展平、`WriteBlob` receipt 投影为身份对）；桥接模块位置不变但改由 `composition-durable-fact` 编译，因此「包装进 `AgentFact`」只发生在 durable composition。delegation 边界分片（host adapter 的 `Fork/Host/{Agent,ChildDispatch,Join,RunLifecycle}.fs`、runtime surface 的 `Fork/Host/Restart.fs` 与 `Handle/JournalSurface.fs`、fission 的 `Execution/Fission/OpenCode/Host.fs`）在持有 journal 处适配一次，port 传入运行时。
   - 结构数字：`26 subsystems / 231→233 shards / 713→715 sources / 1834→1842 refs`，shard DAG 保持、最大 subsystem SCC 仍 18（本刀搬能力形状与所有权，不动合同边）。
   - 行为不变性：替换是逐字等价的——append 的 `Ok ProjectionSet` 载荷此前无人消费（`Result.map ignore` / 直接丢弃），失败文案仍是 `JournalAppendFailure.describe`；`WriteBlob` 先于 fact 的 PERSIST-007 顺序由调用点保留；CAS、drain 排序（`stableJoinKey`）、降级分支（`None` journal）与错误串未动。运行时源码不再出现 `AgentJournal`、`AgentFact`、`Wanxiangshu.Persistence`、`Composition.Durable`、`.Writer.`。
   - 证明：DELEG-029 原注册证明是 `m6-boundary-proof.mjs` 的自证同义反复（校验器只喂内联字面量，与仓库无关），本班换成仓库事实证明：运行时源码无外部句柄/routing union、跨 subsystem 引用不得命中 `persistence-journal-agentjournal`/`persistence-journal-promptfactcodec`/`persistence-journal-eventstorewriter`/`composition-durable-fact` 且其余必须是 contract（唯一例外是 durable projection spine，原因见下）、`AgentJournalPort` 声明者必须属 delegation 且为 contract 分片、运行时实现必须消费 port 且不再调用外部 `AgentJournal.appendAgent|handleProjection`。该证明在改动前的树上为红（实测逐条失败），改动后转绿。
   - 未闭合残余（有证据，不冒充完成）：(a) `delegation-recovery-runtime`（117 命中）与 `delegation-fold`（42 命中）仍引用 `composition-durable-projection`，因为 `HandleProjection.*` 纯决策与 `AgentLinkageProjection` 类型声明编译在该 spine shard——`2ee76f2dc` 的提交说明写明是为打断 fold 环而移入，硬搬回去会把分片 DAG 重新变成环；闭合需要把 aggregate fold 组合改成按域切片（spine 不再调用 delegation 决策、由 delegation fold 拥有 linkage 迁移）。(b) `delegation-fold` 仍调用 `PromptAuthorityRun.closeCompletedAgentOwnerChildWork`（authority 域决策跑在 delegation fold 里，13 命中），需按同一 fold 组合方案分配 owner。(c) DELEG-029 的 PTY 半句（PTY adapter 不得读 `HostForkRuntime`/`Dictionary` 等）仍未实现，已在 delegation HOW 记录。
   - 验证（最终字节）：12 个受影响分片 focused Fable compile 全通过（`delegation-journal-port` fp `f8b870e647c8`、`delegation-recovery-runtime` `57be944dc780`、`durable-journal-port-adapter` `ec5a7d9d9037`、`composition-durable-fact` `a9f085296b3e`、`delegation-fold` `e1888633d7ac`、`delegation-ledger` `e1afb3eb9cd8`、`persistence-journal-promptfactcodec` `52601a3d0fd9`、`durable-runtime-surface` `5a6816e5bf2f`、`persistence-journal-agentjournal` `bac3b7447dc2`、`delegation-host-adapter` `751952a513f7`、`delegation-runtime-surface` `9460ce2893ec`、`execution-fission-opencode-host` `f07f39d40f2b`）；`npm run build` 787 emitted modules；`node scripts/check.mjs` 全绿（814 WHAT / 4016 tests / 0 proof gap）；verification **3973 passed / 0 failed**；integration 全过；e2e Long Stroke exit 0；`format:check`；`npm pack --dry-run` 2052 files。

23. **把 delegation 自有词汇从 persistence spine 里收回 delegation contract 分片；8 个分片的直接声明依赖落地到窄合同（DELEG-028/029 的实现面补齐）**：
   - 判据来源：`requirements/delegation/WHAT.md` DELEG-028 写明 `Delegation.Contract` 拥有 presentation/协议层面的词汇、`Delegation.Fold` 只实现纯投影；DELEG-029 要求 linkage/estimate 这类决策由「delegation-owned pure fold/decision」拥有。实际不是：`Execution/Delegation/LinkageProjection.{fsi,fs}` 与 `Execution/Delegation/DelegatedToolEstimateProjection.{fsi,fs}`（纯函数 + 纯数据类型，只开 `Wanxiangshu.Foundation(.Identity)`）编译在 persistence composition 分片 `composition-durable-projection` 里，因为 `2ee76f2dc` 当年把 `LinkageProjection`/`FoldRejection` 一起挪进 spine 来打断 fold 环。于是 delegation 运行时消费自己领域的类型时，声明依赖指向的是 persistence composition。
   - 迁移：新增 delegation contract 分片 `delegation-linkage-projection`（只引用 `identity`、`foundation-roles`、`delegation-sync-contract`）编译这 4 个文件；spine 改为声明引用它并保留 `AgentProjectionSet`/`AgentProjection`/`MagicTodoProjection`/`FoldRejection`。改动仅限 fsproj 归属与 `ProjectReference`，源码一字未动，聚合 fsproj 因路径未变无需修改。
   - 消费者迁移（8 删 spine、6 留 spine，全部 focused compile 验证）：删掉直接 spine 引用的分片是 `delegation-recovery-runtime`、`delegation-journal-port`、`delegation-ledger`、`delegation-host-adapter`、`execution-delegation-hostturnobservedsurface`、`execution-fission-opencode-host`、`execution-session-opencode-horizontool`、`git-integrationgate`；保留的是真正使用 `AgentProjection`/`FoldRejection` 的 `delegation-fold`、`delegation-runtime-surface`、`execution-session-recoveryclosureprojection`、`opencode-host-pluginruntimescope`、`opencode-host-chatadmission-intent`、`persistence-journal-agentjournal`。删除判定用编译为准：注释里出现符号名不算使用（`delegation-ledger`、`delegation-host-adapter`、`git-integrationgate` 的「剩余使用」都是注释）。
   - 结构数字：`26 subsystems / 233→234 shards / 715 sources（不变，只是换了编译者）/ 1842→1852 refs`，shard DAG 保持、最大 subsystem SCC 仍 18。
   - 真实收益（可重跑）：审计里 `delegation-recovery-runtime → composition-durable-projection` 与 `delegation-journal-port → composition-durable-projection` 两对消失（4 → 2 对，剩下的是 fold→spine 27 处与 fold→authority 13 处）；contract 分片 `delegation-journal-port` 的编译闭包 **110 → 12** 个 `.fs`，`delegation-linkage-projection` 为 11；DELEG-028 的预算现在覆盖这两个新 contract 分片（≤100），而这在迁移前不可能通过。反面证据同样记录：`delegation-recovery-runtime` 的闭包仍是 136，因为它经 `delegation-fold` 到达 spine——闭包变小只在 spine 原本是唯一路径的地方成立，不能拿直接引用数冒充。
   - 证明：`delegation-compile-boundary.test.mjs` 里「`durable projection spine must own LinkageProjection`」这条 2026-09-02 的临时钉扎换成现行不变量（LinkageProjection 归 delegation 的 contract 分片；spine 必须声明引用它）；`m6-slice-boundary.test.mjs` 的 DELEG-029 去掉 `pureProjectionSpine` 例外，改为「recovery runtime 的跨 subsystem 引用必须全部是 contract kind」，并断言 linkage 词汇由 delegation contract 提供。
   - 验证（最终字节）：17 个受影响分片 focused Fable compile 全通过（如 `delegation-linkage-projection` fp `90c60d3e539d`、`composition-durable-projection` `cf33987e49da`、`delegation-recovery-runtime` `bd638522d266`、`delegation-journal-port` `a01660146c80`、`execution-fission-opencode-host` `d7ae0a9d40a9`）；`npm run build` 787 emitted modules；`node scripts/check.mjs` 全绿（814 WHAT / 4016 tests / 0 proof gap）；verification **3973 passed / 0 failed**；integration 全过；e2e Long Stroke exit 0；`format:check`；`npm pack --dry-run` 2052 files。

24. **delegation fold 交出聚合投影，改由 durable composition 唯一装配（DELEG-029 / DURABLE-EVENTS-023 的实现面闭合）**：
   - 判据来源：DELEG-029 原文要求「linkage、estimate、handoff frontier、child handle index 与 closed rejection 由 delegation-owned pure fold/decision 拥有；durable composition 是把这些 case 包入 `AgentFact` 并组合 projection 的唯一位置」；DURABLE-EVENTS-023 同义要求「领域 constructor 只能产生 owner-owned fact case、intent、fold state 与 closed rejection；`AgentFact` outer union、跨 projection 组合及 journal append 只由 durable composition routing 拥有」。
   - 病象：`ExecutionFactFold.fold` 与 `DelegationFactFold.fold` 的签名是 `AgentProjectionSet -> … -> Result<AgentProjectionSet, FoldRejection>`，即 delegation 的纯决策接收并返回全仓聚合投影；fold 体内直接写 `HandleByChildSession`、`DelegatedToolEstimate`、`DelegationCompletedHandoffs`（跨 projection 组合），并在 `closeCompletedChildAuthority` 里调用 authority 的 `PromptAuthorityRun.closeCompletedAgentOwnerChildWork`（把 authority 的 run 关闭内联进 delegation fold）。行为符号审计里这两条就是仅剩的两对：fold → persistence composition（27 处）与 fold → interaction-authority（13 处）。
   - 迁移：delegation contract 分片 `delegation-linkage-projection` 新增 `Execution/Delegation/DelegationProjection.{fsi,fs}`，声明 `DelegationSessionState`、四态纯变更 `DelegationProjectionChange`（`ReplaceSessionState`/`IndexChildHandle`/`MoveHandoffFrontier`/`TerminatedChildHandle`）与闭合拒绝 `DelegationFoldRejection`（含 `fact`/`message` 渲染）；两个 fold 改为接收 `(SessionId -> DelegationSessionState option)`（delegation fact 再加 `(string -> int64 option)` 的 handoff frontier 读取）并返回 `Result<DelegationProjectionChange list, DelegationFoldRejection>`。新增 composition 分片文件 `Composition/Durable/DelegationProjectionBridge.{fsi,fs}`（`composition-durable-fold`）：把聚合切成切片、按序应用变更（`ReplaceSessionState` 保留 `AgentProjection.update` 的缺失创建语义、`IndexChildHandle` 保持 PERSIST-008 键索引、`TerminatedChildHandle` 是原 authority 关闭逻辑的原样搬迁），并把闭合拒绝渲染成 `FoldRejection.reject(fact, message)`；`Fold.fs` 的两个分派分支与 JS 面 `Handle/FoldSurface.foldApply` 都改走 bridge。`delegation-fold` 删除对 `composition-durable-projection` 与 `interaction-authority-fact` 的声明引用。
   - 等价性：这是一次动词未变的搬迁——吸收/拒绝判定、`canonicalByname`、childId 取自调用前 state、索引取自更新后 state、变更顺序（replace → index → terminate）、缺失 session 的创建语义与全部诊断文本都与搬迁前逐条一致；JS 面 `foldApply` 的错误对象仍是 `{| Fact; Reason |}`。
   - 真实收益（2026-09-12，owner compile 的 production `.fs` 闭包）：行为符号审计对 2 → **0**（两班前是 4，含 1 条命名空间假阳性）；`delegation-fold` 闭包从「包含整个 durable spine」降到 **20**；`delegation-recovery-runtime` **193/136 → 47**（它此前正是经 `delegation-fold` 到达 spine）；`delegation-linkage-projection` 12、`delegation-journal-port` 13。结构数字：26 subsystems / 234 shards / 717 sources / 1853 refs，shard DAG 保持、最大 subsystem SCC 仍 18。
   - 需求侧记录同步：DELEG-028 的 adapter ratchet 按实测收紧（`requirements/delegation/WHAT.md` 记 2026-09-12：host 278、pty 279、recovery 47），测试 `delegation-compile-boundary.test.mjs` 新增断言：delegation fold 不得声明 foreign composition/adapter/runtime 引用、其闭包不得包含 `Composition/Durable/{Projection,FoldRejection,Fold}.fs`、其实现源不得出现 `AgentProjectionSet`/`PromptAuthority`/`FoldRejection`，且 `DelegationProjection.fs` 必须由 delegation contract 分片编译、`DelegationProjectionBridge.fs` 必须由 composition 分片编译。DELEG-029 侧不变（recovery 的跨 subsystem 引用仍全部是 contract kind）。
   - 验证（最终字节）：5 个受影响分片 focused Fable compile 全过（`delegation-linkage-projection` fp `15d4981040c6`、`delegation-fold` `735962c83305`、`composition-durable-fold` `bc017f5f5bd2`、`delegation-runtime-surface` `5b14d1badee8`、`composition-durable-projection` `1ced07e36486`）；`npm run build`、`node scripts/check.mjs`、verification suite、integration、e2e Long Stroke 与 `format:check` 全绿（见本班交接记录）。
   - 未闭合残余（有证据）：其它 8 个域 fold 分片仍以 `AgentProjectionSet` 为签名（`change-fold`、`interaction-concern-fold`、`interaction-attention-fold`、`enforcer-institutionallearning-fold`、`execution-fission-fold`、`interaction-authority-fold`、`context-companion-companionfactfold`、`participant-provider-attempt-planner`），`FoldRejection` 这个两字段报告类型仍由 spine 拥有并被约 16 个分片引用；两者按同一判据继续收敛，不在本刀内。

### 3.3 语义词汇与证明义务注册

此表保留既有业务词汇 proof edge。第二列中的旧模块身份只用于定位已有源码，不恢复 owner 作为治理粒度。

| 词汇 | subsystem / legacy module / path | WHAT law | 允许的 trace relation | executable proof |
|---|---|---|---|---|
| `ManagerWorkflow.observe` | relay / Mission.Manager / Mission/Manager/Workflow.fs | `STRUCTURED-WORKFLOW-007` | one admission → one settled/no-effect outcome | `requirements/structured-workflow/tests/semantic-vocabulary.test.mjs::WHAT[STRUCTURED-WORKFLOW-007] every vocabulary binds owner_law_relation_and_executable_proof` |
| `ManagerWorkflow.observeIdle` | relay / Mission.Manager / Mission/Manager/Workflow.fs | `STRUCTURED-WORKFLOW-007` | one idle observation → at most one encouragement | `requirements/structured-workflow/tests/semantic-vocabulary.test.mjs::WHAT[STRUCTURED-WORKFLOW-007] every vocabulary binds owner_law_relation_and_executable_proof` |
| `FallbackLedger.recordAuthorizedFailure` | provider / Participant.Provider / Participant/Provider/Attempt/Fallback/Ledger.fs | `STRUCTURED-WORKFLOW-007` | one policy licence + duplicate observation → one durable cursor advance | `requirements/structured-workflow/tests/semantic-vocabulary.test.mjs::WHAT[STRUCTURED-WORKFLOW-007] every vocabulary binds owner_law_relation_and_executable_proof` |
| `ProviderRecoveryWorkflow.continueAfterConfirmedFailure` | provider / Participant.Provider / Participant/Provider/Attempt/Fallback/Workflow.fs | `STRUCTURED-WORKFLOW-008` | `R_fallback`: confirmed failure → bounded ordinary CE re-entry | `requirements/structured-workflow/tests/workflow-constitution.test.mjs::WHAT[STRUCTURED-WORKFLOW-008] decorator_owner_WHAT_and_exact_proof_are_authoritative` |
| `OrchestratorProgram.run` | change / Change / Change/Program.fs | `STRUCTURED-WORKFLOW-008` | `R_publish`: finite retry → one accepted or typed failed result | `requirements/structured-workflow/tests/workflow-constitution.test.mjs::WHAT[STRUCTURED-WORKFLOW-008] decorator_owner_WHAT_and_exact_proof_are_authoritative` |

### 3.3.1 词汇约束

这些 proof 约束业务 trace；subsystem 迁移不得通过改变测试标题、降低 multiplicity 或放宽 failure/cancel 语义取得绿色。

25. **单字段事实族的 fold 交出聚合投影：域只剩自己的切片，composition 独占写回**：
   - 病象：8 个域 fold 仍以 `AgentProjectionSet` 为签名；其中 5 个（`Change`→`Orchestrator`、`Fission`、`Concern`、`Attention`、`InstitutionalLearning`）只读写自己那一个顶层字段，包装体是纯装配。判据同 DELEG-029/DURABLE-EVENTS-023：域拥有切片决策，durable composition 是写回聚合与渲染 fail-closed 报告的唯一位置。
   - 改动：删除 `Execution/Fission/Fold.{fs,fsi}`、`Interaction/Concern/Fold.{fs,fsi}`、`Interaction/Attention/Fold.{fs,fsi}`、`Enforcer/InstitutionalLearning/Fold.{fs,fsi}` 四个纯装配包装；`Change/Fold.{fs,fsi}` 改成 `OrchestratorProjection -> OrchestratorFactCases -> Result<OrchestratorProjection, OrchestratorFoldRejection>`，`Change/Facts.{fs,fsi}` 新增闭合 `OrchestratorFoldRejection`（`fact` = `PublishClaimed`，两条 message 逐字照搬）；写回由 `Composition/Durable/ProjectionUpdate.apply{Fission,Concern,Attention,AttentionLearning,InstitutionalLearning}` 与 `Fold.foldAgentFact` 的 `Orchestrator` 分支承担（learning→attention 的先后顺序不变）。`OrchestratorFactFold` 与 `ProjectionUpdate.updateOrchestrator` 的旧包装删除。注意 `ProjectionUpdate` 不能调用 `Change/Fold`：后者经 journal 分片持有 `ProjectionSet`，会在 `composition-durable-projection → change-fold → persistence-journal-promptfactcodec` 上成环，因此 Change 族的装配留在 dispatcher 侧。
   - 真实收益（2026-09-12，owner compile 的 production `.fs` 闭包）：`execution-fission-fold` 115 → **63**；`composition-durable-fold` 254 → **239**（少拉四个已清空的分片）；concern/attention/institutional 各 −1（它们的 Tools 文件仍真实读聚合，保留引用）；`change-fold` 与 `composition-durable-projection` 闭包不变。结构数字：26 subsystems / 234 shards / **713** sources / **1846** refs，shard DAG 保持、最大 subsystem SCC 仍 18；行为符号审计对保持 0（本批的收益是知识方向与局部闭包，不是审计对数）。
   - 需求侧记录同步：`requirements/durable-events/HOW.md` 第 4 节补单字段族装配条目；`requirements/institutional-learning/HOW.md` 记录本分片删掉的包装与保留的聚合读取；`durable-events/tests/m6-slice-boundary.test.mjs` 新增边界断言（四个包装不得回归、`change-fold` 不得声明 `composition-durable-projection`、其 fold 源不得出现 `AgentProjectionSet`/`FoldRejection`）。删除 `institutional-learning.test.mjs` 的 007 用例：它只 match 两个文件的源码文本（第一条断言甚至由注释满足），不是行为证明，按「不得把实现文本重新钉成断言」处理，该条款的 JS 可驱动证明缺口已记入包内 HOW。
   - 验证（最终字节）：7 个受影响分片 focused Fable compile 全过（`composition-durable-projection` fp `42f77ee6127c`、`composition-durable-fold` `c48212384e3d`、`change-fold` `f63383df8b96`、`interaction-concern-fold` `33e18d89bebb`、`interaction-attention-fold` `c31dee0a6858`、`execution-fission-fold` `92ea93a13fe5`、`enforcer-institutionallearning-fold` `38ab2eef7f3d`）；`npm run build`、`node scripts/check.mjs`、verification suite、integration、e2e Long Stroke 与 `format:check` 全绿（见本班交接记录）。
   - 未闭合残余（有证据）：`interaction-authority-fold`、`context-companion-companionfactfold`、`participant-provider-attempt-planner` 三个族仍以 `AgentProjectionSet` 为签名，且都不是单字段（authority 跨 session + 写 `ProviderFailures`，companion 写 `Associations`+两片，provider failure 是 per-session 写），需要 knife 6 的 change-list 形态；`FoldRejection` 仍由 spine 拥有；四个已清空的分片因其 Tools 文件读取聚合而保留引用。
26. **多切片事实族的 fold 也交出聚合写入：窄查询 + 变更列表，桥归 composition**：
   - 病象：`PromptFactFold`（`Interaction/Authority/Fold.fs`，authority 子系统）、`ProviderFailureFactFold`（provider）、`CompanionFactFold`（context）三个域 fold 仍以 `AgentProjectionSet` 为签名，并且直接调用 composition 的写入代数（`ProjectionUpdate.updateSession`/`updateAuthority`/`updateCompanion`、`AgentProjection.tryUpdate`）与 spine 的 `FoldRejection`；它们的唯一调用方是 `Composition/Durable/Fold.fs` 的对应分支。判据同 DELEG-029 / DURABLE-EVENTS-023。
   - 改动：三个 fold 改为只接收窄查询与自己要用的切片值（`authorityOf`/`runtimeStartCount`；`providerFailuresOf`；`associations`/`companionOf`/`xTraceOf`），返回闭合变更列表（每个 case = 一次写入，空列表 = 旧代码的 `Ok projection`）与各自的闭合拒绝（`fact`/`message` 把旧诊断文本逐字搬走：`ClaimOriginRejected`/`AuthorityRootSeedRejected` 保留 `%A` 渲染，schema 与 ledger 文本原样，association 用 `describe`）。新增 `Composition/Durable/DomainFamilyBridge.{fs,fsi}`（分片 `composition-durable-fold`，与 `DelegationProjectionBridge` 同址、同形状）：唯一的写回与拒绝渲染点，位于三个 domain fold 下游，因此不会与 journal 路径成环——`ProjectionUpdate` 不能承担这件事，理由同第 25 条。`Fold.foldAgentFact` 只做一行分派。
   - 真实收益（2026-09-12 第二刀，owner compile 的 production `.fs` 闭包）：`participant-provider-attempt-planner` refs 10 → 9（去掉 `composition-durable-projection`），闭包 132 **不变**（该分片仍经 `persistence-journal-promptfactcodec` 到达聚合，如实记录而非宣称收益）；`interaction-authority-fold` 183 与 `context-companion-companionfactfold` 224 的闭包与引用都不变（`Interaction/Authority/Child.fs` 与 Blogger 的 runtime/Host/Coordinator 真实读聚合）。本批的真实收益是知识方向：三个域 fold 不再出现聚合模型、写入代数与 `FoldRejection`，`Composition/` 之外不再有任何地方调用 session 级写入辅助。`composition-durable-fold` 闭包 239 → **240**（多一个桥文件）。结构数字：26 subsystems / 234 shards / **714** sources / **1845** refs，shard DAG 保持。仍是聚合签名的域 fold 只剩 `ContextFactFold`（写 6 个切片，需另行设计）。
   - 需求侧与测试：`requirements/durable-events/HOW.md` 第 4 节补多切片族条目，并把两条新断言列入 DURABLE-EVENTS-023 证明行；`m6-slice-boundary.test.mjs` 断言这三个 fold 源不得出现 `AgentProjectionSet|AgentProjections|AgentProjection.|FoldRejection|ProjectionUpdate|Composition.Durable`，且 `Composition/` 之外不得调用 `ProjectionUpdate.updateSession|updateAuthority|updateCompanion`（对 HEAD 字节可证伪：三个文件分别有 9 / 15 / 7 行命中，断言新字节全过）。
   - 验证（最终字节）：`composition-durable-fold` fp `d7606111cfc8`、`composition-durable-projection` `2a5bbc40d151`、`participant-provider-attempt-planner` `401aeb4084f4`（去掉引用后仍绿）；`interaction-authority-fold` 与 `context-companion-companionfactfold` 的 focused 编译错误集合与 HEAD 基线**完全相同**（只剩 HEAD 已有的 `Wanxiangshu.Execution.Delegation.Fork` 命名空间缺失，见交接记录的既有缺口），无新增错误。`npm run build`、`node scripts/check.mjs`、verification suite、integration、e2e Long Stroke、`format:check` 与 `npm pack --dry-run` 全绿（见本班交接记录）。

27. **最后一个聚合签名的域 fold（context/Blogger）交出聚合写入；前缀吸收策略归 prefix 投影**：
   - 病象：`ContextFactFold`（`Context/Companion/Blogger/Fold`，context 子系统）仍以 `AgentProjectionSet -> Result<AgentProjectionSet, FoldRejection>` 为签名，一次事实里用 `AgentProjection.tryUpdate` 写 `BloggerCycles`／`Enforcement`／`Blog`／`PrefixEpoch`，用 `tryUpdateBlog` 与 `ProjectionUpdate.retireAuxiliaryInjectionVisibility` 做写回，并在 `prefixOutcome` 里渲染前缀拒绝；唯一调用方是 `Composition/Durable/Fold.fs:89`。判据同 DELEG-029 / DURABLE-EVENTS-023。
   - 改动：fold 只接收四个窄查询（`bloggerCyclesOf`／`enforcementOf`／`blogOf`／`prefixEpochOf`），返回 `ContextProjectionChange` 六种变更（每 case 一次写入；`BlogReanchored` 让桥保留 `Blog = None` 不写、`AuxiliaryVisibilityRetired` 让桥调用 composition 侧的退役策略）与闭合 `ContextFoldRejection`（七种；`BlogFoldRejection` 六段文本与 `fact` 名逐字搬入，判定顺序仍是 enforcement／收据先于 blog）。`ContextProjectionBridge` 加进 `Composition/Durable/DomainFamilyBridge.{fs,fsi}`（第四个桥）。**吸收策略不再复制**：`StalePrefixEpoch`／`CandidateNotNew`／`CompactionAlreadyReanchored` 的「吸收为零写入」与另两个 case 的 PERSIST-010／CTX-011 文本归 `Context/Prefix/Epoch.fs` 的 `PrefixEpochProjection.describe`，`ProjectionUpdate.prefixOutcome`（`Fold.foldMagicTodo` 仍在用）与 context fold 共用它，原先散在 `prefixOutcome` 的 CTX-012／CTX-011 幂等理由也随实现搬到 `Epoch.fs`。同批删掉三条已无调用方的写入辅助 `updateCompanion`／`updateAuthority`／`tryUpdateBlog`（`ProjectionUpdate.fs` +6/−51、`.fsi` −21），并修掉 `Context/Companion/Projection.fs` 里指向已删除 `Fold.updateCompanion` 的陈旧注释。
   - 真实收益（2026-09-12 第三刀，owner compile 的 production `.fs` 闭包）：`context-companion-companionfactfold`（闭包 224）、`composition-durable-fold`（240）、`composition-durable-projection`（110）与 `change-fold`（138）的 owner-compile production `.fs` 闭包与引用集合都与上一刀逐字相同（该分片的 runtime／Host／Coordinator 文件仍真实读聚合），收益是知识方向与接口面：域 fold 不再出现 `AgentProjectionSet`／`AgentProjection.`／`ProjectionUpdate`／`FoldRejection`／`Composition.Durable`（对 HEAD 字节可证伪：旧文件 14 行命中），写入代数只剩 composition 自己在用，`ProjectionUpdate` 的公开面少三条死辅助。结构数字不变：refs 仍是 **1845**、sources 仍是 **714**——本刀没有增删声明引用或源码文件（第 28 条删掉的是死 `open`，不是 ProjectReference）。
   - 需求侧与测试：`requirements/durable-events/HOW.md` 第 4 节补第三批条目并把改名后的断言列入 DURABLE-EVENTS-023 证明行；`requirements/context-compression/HOW.md` 记录调用形态；`m6-slice-boundary.test.mjs` 的域 fold 集合加入 context fold，写入辅助禁令加入 `retireAuxiliaryInjectionVisibility`；`scripts/checks/authority-contracts.json` 里 `BloggerCycleReceipt` 的两条 issuer 锚点随构造点从被删除的 `applyEnforcementAndReceipt`／`applyReceiptAndTips` 改指 `fold`（锚点文本本身不变，仍是 `Kind = BlogFrameKind.Entry`／`Squash` 的收据字面量）。
   - 验证（最终字节）：`context-companion-companionfactfold` fp `446c0261ea70`、`mission-obligation-todo-prefixepoch` `cc622648a868`、`composition-durable-projection` `2465bc401c12`、`composition-durable-fold` `ab7cf2d76e5d`；行为由 provider／interaction-authority／durable-events 既有 suite 与 e2e Long Stroke 覆盖（见本班交接记录）。

28. **修掉 HEAD 既有的声明引用缺口：四处文件里五行死 `open` 让若干分片 focused 编译全红**：
   - 病象：`Interaction/Dispatch/Dispatcher.fs:17`、`Interaction/Dispatch/Send.fs:16`、`Interaction/Repair/CompletedTurn.fs:10,30`、`Interaction/Authority/Child.fs:13` 都 `open Wanxiangshu.Execution.Delegation.Fork`，但这些分片的声明引用看不到 `Execution/Delegation/Fork/*`，`compile-owner` 因此报 `The namespace 'Fork' is not defined`；aggregate 因为包含全部源码一直是绿的。这是「aggregate 绿灯掩盖分片缺陷」的实例，也是第八班把 `interaction-authority-fold`／`context-companion-companionfactfold` 的 focused 编译记成既有红的根因。
   - 判定（不是猜）：对 4 个文件逐个核查该命名空间下的全部 module 与 type 名（`ChildRecovery`、`ChildRecoverySurface`、`ChildRun`、`ChildRunProgram`、`ChildRunProjection`、`CleanBreakSurface`、`CompletionCell`、`ForkChildPayload`、`ForkLifecycleSurface`、`ForkRecovery`、`ForkRuntimeBackend`、`ForkRuntime`、`IForkRuntimeBackend`、`ForkError`、`ForkResult`、`ForkChildAssignment`、`ForkChildInstructions`、`ForkCompletionMailbox`、`AgentRecord`、`AgentStatus`、`PtyRecord`、`ForkChildPayloadSurface`）——**全部 0 命中**，所以这 5 行是死 `open`（类型迁移遗留），**不补声明引用**：没有真实符号使用就不该有依赖。
   - 结果：`dispatch-protocol.dispatch-runtime` fp `ade8394546cc`、`interaction-authority-fold` `45a222b3b0a7`、`context-compression.context-companion-companionfactfold` `446c0261ea70` 三个分片的 focused 编译全部转绿（此前三片皆红）。教训：分片出现「命名空间未定义」时，先分辨是「缺声明」还是「死代码」，两者修法相反；禁止靠 aggregate 编译或旧 dist 掩盖。

29. **让声明面与已断的知识依赖对齐：删掉 28 条「引用方零提及、闭包不变」的 shard ProjectReference**：
   - 病象：分片 `.fsproj` 的声明引用不等于真实知识依赖。1845 条声明引用里有一批的目标分片导出在引用方源码里零使用（第九班顺手修掉的 `Fork` 死 `open` 是同一本账的另一面：该有声明的地方缺失）。纪律：知识依赖还在就必须留着声明；知识依赖确已切断，残留声明才清掉——不得靠 aggregate 绿灯或动态加载掩盖任何一面。
   - 判据（两条同时成立才删）：ⓐ 引用方全部源码对目标分片声明的任何类型／模块／联合 case 名**零整词提及**。这里必须用朴素整词匹配：任何「点号保护」（`(^|[^.\w])Name\b`）都会把 `Wanxiangshu.Foundation.CanonicalJson.canonicalJson` 这类**全限定引用**误判成字段访问，本班第一版正是因此删掉了 19 条真实依赖，被独立审查拦下（见下）。ⓑ 目标分片仍能从引用方**其余**引用可达——引用闭包集合不变，消费者的编译输入因此不变。
   - 证明方式（本刀的机制修正，务必沿用）：`compile-owner` 的 scratch 工程会把一个分片闭包里的源码放进**同一个**工程，`generateFlatProjectXml` 剥掉全部 `<ProjectReference>`，fable 参数里没有 `--ref`，`<Compile>` 顺序只来自 aggregate；因此声明引用只决定「闭包里有谁」，删引用的正确验证是**闭包变化的量测**：`compile-owner --plan-only` 逐分片比较删除前后的计划文件清单，相同即证明 scratch 工程相同、无需为验证重编该分片（本班 24/24 逐条相同，用时 2 秒）。把「每个改过的分片都重编一遍」当稳妥是错的：那是 Σ(闭包) 的 O(n²)，也违反本仓「一次增量构建的多项变化交 impact 流程求并集」的纪律。闭包确实变化时，只重编受影响闭包的**极大元**分片；改到源码（例如删 `open`）时，源码所属分片必须真实编译一次，不能靠图论。
   - 结果（最终字节）：删除 **28 条**声明引用，覆盖 **24 个分片**、**14 个目标分片**、**16 个消费者 subsystem**；`refs` 1845 → **1817**，`sources` 714、`compile shards` 234、`subsystems` 26、最大 subsystem SCC 18 均不变（闭包不变，故本刀只动声明面，不宣称编译局部性或独立性提升）。其中 6 条指向 `durable-events.composition-durable-projection`（同一命令 `grep -l` 在 HEAD 与 WORK 两侧分别得 49 与 43，差值 6 与删除数一致）。抽检 focused 编译 OK：`attention-regulation.interaction-attention-fold` fp `c7c9b1720808`、`durable-events.composition-durable-fold` `a1e3c97ad066`、`session-ontology.opencode-host-sessionexecutionbinding` `17d3a945bc14`、`interaction-authority.interaction-authority-fold` `c695b85c3107`、`verification-system.verification-eventstorewritersurface` `1cc145810fe4`。
   - 失败与纠正（记账，防后人重犯）：第一版检测器对「同名被多个分片声明」只认限定用法、又看不见 `static member` 扩展成员，给出 181 条／88 分片，照单删除后 86 个分片转红（闭包丢掉 `SessionId` 等），整批回滚。第二版改为「任何提及都算使用」＋「闭包必须保持」，给出 55 条／43 分片，但**点号保护**保留了全限定引用的漏检；独立审查逐条复算后指出 19 条实际上在使用目标分片（`CanonicalJson.canonicalJson`、`SharedState.clearBloggerFlightsForTests`、`Fold.foldEnvelope`、`PromptAuthorityLedger.closeCompletedHumanRootManager`、`HostDigest.sha256Hex`、`StrengthIntegrationRules.rules`、`open Wanxiangshu.Foundation.AsyncSupport` 等），已按朴素整词口径把它们全部恢复，最终收敛到 28 条。同批还回滚了 11 行「孤儿 `open`」删除：删 `open` 是源码改动，`trySetResult`／`trySetCanceled` 这类扩展成员正是名字检测看不见的，只能由拥有该源码的分片真实编译来判。
   - 下一段的已量测入口：**9 对**「零提及但闭包会变」的引用（删它们会真正缩小闭包，必须连带重编闭包的极大元分片，是编译局部性收益的首选）；25 个仍真实读 `AgentProjectionSet`／`SessionAgentProjection` 的消费者分片（列表见第九班交接「发现」一节），需先按 STRUCTURED-WORKFLOW-013／014 把「消费者收到什么」收窄成窄查询／纯数据；（这些存量红已在本班第 30 条一并修掉，不再挂着）。

30. **见红就修：清掉全仓「命名空间未定义」的既有红（5 个分片）**：
   - 病象：`compile-owner` 的 focused 编译在 HEAD 上就有一批红，病因是分片源码里留着指向「没有任何可达声明者」的命名空间的 `open`（`Fork`、`Repository.Programming.Js` 等，类型迁移与旧 owner 拆分的遗留），或者真在用某命名空间的符号却没声明对应引用。aggregate 全量构建看不见这一类——它把全部源码放进一个工程、没有 `ProjectReference`。
   - 找红的两级方法（本班实测）：① **覆盖集**：闭包工程把分片的整条闭包源码放进同一个工程，所以只要编译「极大元」分片（不出现在任何其它分片闭包里的那些；本仓 22 个）就能覆盖全部源码，2.6 分钟拿到「源码自身固有」的红——本班全仓只剩 1 个红；② **补漏**：覆盖集会**掩盖**分片自身的「命名空间缺声明」缺陷（超集工程里源码更全，缺的命名空间可能被补上），所以要再按静态判据筛「`open Wanxiangshu.*` 的目标在该分片闭包内没有声明者」的候选，**只编译被点名的那几个分片**（本仓 8 个）做裁决。禁止一上来就对 234 个分片逐个重编（那是 Σ(闭包) 的重复劳动）。
   - 修法按证据分流：删死 `open`（该命名空间的类型/模块/联合 case 名在文件里整词零提及，删完该分片真实编译转绿），或补声明（错误点名命名空间/符号 ⇒ 找到声明它的分片，把 `<ProjectReference>` 补进引用方，直到编译转绿）。
   - 结果（本班）：4 个既有红转绿 —— `output-distillation.opencode-tools-distillationsurface` fp `08df00816fd5`（删 `OpenCode/Tools/Distillation.fs:52` 的 `open Wanxiangshu.Repository.Programming.Js`）、`semantic-trace.context-trace-capture` `d541bf262b2f`（删 `Context/Trace/Capture.fs:12,42` 的 `open …Fork`）、`context-compression.context-companion-foldsurface` `1701b504864f`（删 `Context/Companion/Blogger/OpenCode/PluginScope.fs:16,33` 的 `open …Fork`）、`host-boundary.opencode-host-sharedstatesurface` `ee00bb86ee94`（删 `OpenCode/Host/PluginSessionScope.fs:11,31` 与 `PluginRecoveryScope.fs:12,33` 的 `open …Fork`）（第 5 个见下）。改动面：8 个源码文件共删 30 行 `open`（其中 21 行属本分片；`JoinDrain.fs` 只含受阻塞依赖的 `open Wanxiangshu.Host`，保持不动），fsproj 引用集合保持与 HEAD 一致。
   - **明确记录一个受合同阻塞的红**：`delegation-recovery-runtime` 要彻底转绿必须让 `HostDigest` 可达。实测两条路都被现行合同挡住：① 直接补 `runtime-platform.digest` 引用 —— 该分片没有 `<WanxiangshuOwnerLocalityKind>`，`WHAT[DELEG-029]` 的「跨 subsystem 引用必须是 contract」直接失败（`cross-subsystem reference digest () is not a contract`）；② 换引用（丢 `host-boundary.host-digest`〔实际装的是 `OpenCode/Signals/EventContract`〕补 `runtime-platform.digest`）——闭包仍超 `WHAT[DELEG-028]` 的记录棘轮 47（`delegation-recovery-runtime grew beyond its recorded ratchet 47`），而该测试注释明确要求增长必须**对 WHAT[DELEG-028] 裁决**。可选出路（都需裁决，未在本班动手）：把 digest 作为 delegation 自有 port 注入；把 `runtime-platform.digest` 升为 contract kind；或按 WHAT[DELEG-028] 修订棘轮。本班保留 21 行死 `open` 的删除并回滚引用改动，`requirements/delegation/tests` 125/125 保持通过。
   - 教训：分片出现「The namespace 'X' is not defined」时先分清「死代码」与「缺声明」——前者删 `open`、后者补引用，两者修法相反；判据是「该命名空间声明的名字在文件里有没有真实使用」，最终一律以该分片自己的 focused 编译为准，不能靠 aggregate、覆盖集或静态脚本宣布修好。

## 4. 编译与验证

日常结构验证：

```sh
node scripts/checks/subsystems.mjs
node --test requirements/structured-workflow/tests/subsystem-boundaries.test.mjs
```

涉及 shard ProjectReference 或 `.fsi` 时，先跑对应定向 compiler-boundary/impact tests，再运行：

```sh
node scripts/check.mjs
node scripts/build.mjs
```

禁止 `dotnet build`。aggregate full build 只证明最终完整输入可编译；缺失 ProjectReference 必须由定向 shard compile/canary 发现。

删 shard ProjectReference 的验证入口：先用 `compile-owner --plan-only` 比较删除前后的计划文件清单（闭包集合），清单不变即证明该分片编译输入未变、无需重编；清单变化时只重编受影响闭包的极大元分片。同批改到源码（例如删 `open`）时，源码所属分片必须真实编译一次。

## 5. proof map

| WHAT | 当前 proof |
|---|---|
| STRUCTURED-WORKFLOW-001 | `requirements/structured-workflow/tests/direct-ce-contract.test.mjs::WHAT[STRUCTURED-WORKFLOW-001] FLOW_001_direct_task_workflow_is_allowed` |
| STRUCTURED-WORKFLOW-002 | `requirements/structured-workflow/tests/direct-ce-contract.test.mjs::WHAT[STRUCTURED-WORKFLOW-002] FLOW_006_second_runtime_patterns_are_rejected` |
| STRUCTURED-WORKFLOW-003 | `requirements/structured-workflow/tests/workflow-surface.test.mjs::WHAT[STRUCTURED-WORKFLOW-003] SW_002_workflow_modules_export_no_program_counter_shaped_names` |
| STRUCTURED-WORKFLOW-004 | `requirements/structured-workflow/tests/fsharp-control-pyramid.test.mjs::WHAT[STRUCTURED-WORKFLOW-004] CONTROL_PYRAMID_nested_match_is_RED_at_the_inner_decision` |
| STRUCTURED-WORKFLOW-005 | `requirements/structured-workflow/tests/dsl-ownership.test.mjs::WHAT[STRUCTURED-WORKFLOW-005] DSL_OWNERSHIP_negative_mutable_goes_red` |
| STRUCTURED-WORKFLOW-006 | `requirements/structured-workflow/tests/direct-ce-contract.test.mjs::WHAT[STRUCTURED-WORKFLOW-006] FLOW_017_composition_keeps_domain_results_and_rejects_child_program_counters` |
| STRUCTURED-WORKFLOW-007 | `requirements/structured-workflow/tests/semantic-vocabulary.test.mjs::WHAT[STRUCTURED-WORKFLOW-007] every vocabulary binds owner_law_relation_and_executable_proof` |
| STRUCTURED-WORKFLOW-008 | `requirements/structured-workflow/tests/semantic-vocabulary.test.mjs::WHAT[STRUCTURED-WORKFLOW-008] SW_015_no_anonymous_middleware_framework_in_workflow_vocabulary` |
| STRUCTURED-WORKFLOW-009 | `requirements/structured-workflow/tests/reconcile-program.test.mjs::WHAT[STRUCTURED-WORKFLOW-009] operator abort is a control-plane wake, never a business outcome` |
| STRUCTURED-WORKFLOW-010 | `requirements/structured-workflow/tests/parallel.test.mjs::WHAT[STRUCTURED-WORKFLOW-010] ARCH_009_results_follow_input_order_not_completion_order` |
| STRUCTURED-WORKFLOW-011 | `requirements/structured-workflow/tests/subsystem-boundaries.test.mjs::WHAT[STRUCTURED-WORKFLOW-011] subsystem is the only semantic governance identity`；`requirements/structured-workflow/tests/owner-project-boundaries.test.mjs::WHAT[STRUCTURED-WORKFLOW-011] subsystem ownership and compile-shard graph are complete and acyclic` |
| STRUCTURED-WORKFLOW-012 | `requirements/structured-workflow/tests/owner-impact-compile.test.mjs::WHAT[STRUCTURED-WORKFLOW-012] implementation changes exclude reverse consumers`；`requirements/structured-workflow/tests/owner-project-boundaries.test.mjs::WHAT[STRUCTURED-WORKFLOW-012] flat Fable projection planner produces exact closure and canonical aggregate order` |
| STRUCTURED-WORKFLOW-013 | `requirements/structured-workflow/tests/subsystem-boundaries.test.mjs::WHAT[STRUCTURED-WORKFLOW-013] reusable platform shards depend on no domain subsystem`；`requirements/structured-workflow/tests/owner-project-boundaries.test.mjs::WHAT[STRUCTURED-WORKFLOW-013] GitGateway exposes a narrow dependency-inverted compiler boundary`；`requirements/structured-workflow/tests/attention-compile-boundary.test.mjs::WHAT[STRUCTURED-WORKFLOW-013] attention tools consume their own port without durable aggregate or runtime containers` |
| STRUCTURED-WORKFLOW-014 | `requirements/structured-workflow/tests/owner-project-boundaries.test.mjs::WHAT[STRUCTURED-WORKFLOW-014] NodeFs physical port and tool contracts have isolated compiler boundaries` |
| STRUCTURED-WORKFLOW-015 | `requirements/structured-workflow/tests/generated-module-relation.test.mjs::WHAT[STRUCTURED-WORKFLOW-015] generated artifact binds tracked inputs bytes lineage traversal and import` |
| STRUCTURED-WORKFLOW-016 | `requirements/structured-workflow/tests/subsystem-boundaries.test.mjs::WHAT[STRUCTURED-WORKFLOW-016] release architecture has one subsystem authority` |

31. **把摘要能力交回能力端口：受合同阻塞的红转绿；并让「闭包缩小」的删除同时暴露反向消费者的未声明依赖**（2026-09-12 第十一班）：
   - 病象一（续第 30 条）：`delegation.delegation-recovery-runtime` 仍红——`CompletionCodec.fs:258` 与 `JoinDrain.fs:374` 直接用 `HostDigest.sha256Hex`，而 `HostDigest` 只由 `runtime-platform.digest` 声明。补该引用被 `WHAT[DELEG-029]`（跨 subsystem 引用必须是 contract kind）与 `WHAT[DELEG-028]`（棘轮 47，量的是闭包源码数）双双挡住——但**两条路都不必走**：摘要本来就该由能力端口供给。
   - 修法：`AgentJournalPort` 增一个字段 `Sha256: string -> string`（`.fs`／`.fsi` 同步；doc 写明「composition 必须供给与存储写 blob 时同一个适配器，第二个实现会作废已存证据」），由唯一构造点 `Composition/Durable/AgentJournalPortAdapter.fromAgentJournal` 填 `HostDigest.sha256Hex`，并给该适配器补上 `runtime-platform.digest` 的**直接声明**（此前只在闭包里隐式可达）。两个调用点改为经端口取摘要：`assertBlobDigest` 收下 `sha256: string -> string` 参数（错误文案 `completion blob digest mismatch: %s` 与判定顺序逐字不变），`JoinDrain` 的守卫改 `durable.Sha256 body`；两处 `open Wanxiangshu.Host` 随之删除。对 `delegation-recovery-runtime` **不新增任何引用**⇒棘轮不变、DELEG-029 不触发：闭包 24 → **21**，分片转绿 fp `65fbdb1545ec`。适配器闭包加声明前后逐字相同（52 → 52），所以那条声明只补了声明面、没有扩大编译输入。
   - 病象二：第 29 条留下的 9 对「零提及但闭包会变」的引用里，有 4 对其实**不能删**——`interaction-attention-fold`、`delegation-sync-runtime`、`sphinx-serve-runtime`、`enforcer-institutionallearning-fold` 的消费者源码都在用 `taskResult` 计算表达式。`intra-participant-parallelism.foundation-taskresult` 导出的是**值层 builder**（`let taskResult = ...`），第 29 条的检测器只收集 `type`／`module`／联合 case 名，看不见它 ⇒ 判据必须扩到值层 `let`、扩展成员与 `.fsi` 的 `val`（命中证据：`OpenCode/Tools/AttentionTools.fs:76`、`Execution/Delegation/SyncDelegate/Workflow.fs:178`、`Sphinx/McpServer.fs:793,813,846`、`OpenCode/Tools/InstitutionalLearningTools.fs:79`）。4 条引用保持不动。
   - 病象三（本班的新发现）：删掉一条出边会让**反向消费者**的闭包一起缩小，而它们可能一直在借这条边。验证方式不是把反向闭包全部重编（Σ 又回来了），而是：算释放集 R → 取 R 各分片**公开面**的名字（有 `.fsi` 只看 `.fsi`）→ 扫每个反向消费者的源码是否提及这些名字 → 命中的再查它在删后引用图里是否仍能到达 R。本班因此抓到两个**既有未声明依赖**：`epistemic-reasoning.sphinx-serve-runtime` 在用 `SphinxMcp`、`durable-events.strength-persistence-durabilityport` 在用 `StrengthEventTypes`，两者都只借道被删的那条边。真实 focused 编译先复现红（`The value, namespace, type or module 'SphinxMcp'/'StrengthEventTypes' is not defined`），再给它们各补一条**直接声明**（不是回滚）后转绿。这两片删前删后的闭包不变，收益在它们自己：`sphinx-runtime` 5 → 4、`strength-prediction-predictor` 73 → 72。
   - 结果（最终字节）：refs 1817 → **1815**（删 5 条、补 3 条声明），`sources` 714、`shards` 234、`subsystems` 26 不变；闭包收益：`delegation-recovery-runtime` 24 → 21、`session-ontology.opencode-host-sessionexecutionbinding` 71 → 69、`strength-prediction-predictor` 73 → 72、`sphinx-runtime` 5 → 4。focused 编译（全部真实执行）：`delegation-recovery-runtime` `65fbdb1545ec`、`delegation-journal-port` `da43dce4f304`、`durable-journal-port-adapter` `b4306db265e7`、`delegation-host-adapter` `b689c9453f74`、`epistemic-reasoning.sphinx-runtime` `d1227db6e959`、`sphinx-serve-runtime` `b1133c57b2d7`、`strength-persistence-durabilityport` `359344e7c2b6`、`sessionexecutionbinding` `fcef8cce8314`、`strength-prediction-predictor` `eccc963e1112`。
   - 已量测入口（下一段）：`host-boundary.host-digest` 这个名字与实际内容不符（它编译的是 `OpenCode/Signals/EventContract.fs`，声明 `Wanxiangshu.OpenCode` 的 `IEventObservationPort` 等，本分片零使用）——该引用对该分片已是纯残留，但删除要按上面的反向扫描流程单独取证。投影聚合消费者盘点：**121 个文件 / 63 个分片**整词提及 `AgentProjectionSet`／`SessionAgentProjection`／`AgentProjection`／`MagicTodoProjection`（其中 15 个文件 / 14 个分片把「查单个 session 的 `PromptAuthority`」与直接访问聚合写在一起——这是同一段重复知识，是最像第一簇的收窄对象）。

32. **把声明面审到全图，并用编译器当 oracle 裁决「同名误判」；同时明确否决两条伪装成进展的切法**（2026-09-12 第十二班）：
   - 病象（续第 31 条）：第 29／31 条只审了「已断的知识依赖」那一批；本班先把残留的两类补齐——`host-boundary.host-digest` 这条名不符实的引用（它编译的是 `OpenCode/Signals/EventContract.fs`），以及第 29 条检测器里被排除、从未审过的 1140 条边。
   - 判据（第四版，比第 31 条更严）：① 目标分片公开名集合按「有 `.fsi` 只取 `.fsi`」取 `val`／`type`／`module`／`member`／`static member`／联合 case，无 `.fsi` 才退回列 0 声明与 `and`；② 提及判据仍用朴素整词（注释算使用，全限定形式不得被排除）；③ 删除只允许两种结论：目标仍可达（闭包不变、纯声明面）或释放集里每个分片都零提及且反向第三方消费者不掉可达。
   - 全图复算的结果：1812 条边里符合「整词零提及」的只剩 **4 条**（1 条真释放分片），说明第 29／31 条的清理已基本到头；剩下的收益在「提及其实是同名误判」这一类——本班用**编译器 oracle** 裁决：删边 → `--plan-only` 测闭包 → 真实 focused 编译，绿则保持删除、红则回滚并留存错误原文。24 条候选里 23 条绿、1 条红：`durable-events.persistence-journal-eventstorewriter ← durable-events.composition-durable-fold` 在 `EventStoreJournalWriter.fs:269` 报 `The value, namespace, type or module 'Fold' is not defined`，如实回滚——这正是「名字出现过、但确实解析到目标分片」的反例，说明提及判据只能做筛子，最终裁决必须是编译器。
   - 结果（最终字节）：两刀合计删 **30 条**声明引用（切片 A 3 条、切片 B 27 条），`refs` 1815 → **1785**，`sources` 714、`shards` 234、`subsystems` 26、最大 subsystem SCC 18 不变。闭包真缩小的四处：`interaction-authority.interaction-authority-fact` 27 → 26（sources 124 → 116）、`dispatch-protocol.interaction-dispatch-opencode-ingresscodec` 81 → 79（450 → 444）、`host-boundary.opencode-host-hostsignalbootstrap` 167 → 165（972 → 964）、`relay-retirement.composition-turn-workflow` 155 → 154（916 → 908）。反向消费者扫描（公开名含联合 case 与 `static member`）对 5 个被释放分片零命中；被删引用的分片全部真实编译过（删除当时 fp 见 `/tmp/s12-oracle.json`，最终字节抽检 fp：`interaction-dispatch-opencode-ingresscodec` `cc21620c24a4`、`opencode-host-hostsignalbootstrap` `f8e46ffc0876`、`composition-turn-workflow` `18cbd3da2e2d`、回滚后的 `persistence-journal-eventstorewriter` `3441c0239b33`）。
   - **明确否决一：把 PromptAuthority 窄查询挂到 `AgentJournal` 上**。实测 `persistence-journal-agentjournal` 的闭包**已经包含** `interaction-authority-ledger`，反向加边直接成环；更要紧的是它违反 STRUCTURED-WORKFLOW-013——物理日志边界不得获得领域知识。
   - **明确否决二：把 15 文件／14 分片的聚合读取改成「窄查询」**。消费者即便改调窄函数，其编译闭包仍通过同一条路径可达同一批分片，闭包一分不减；按 STRUCTURED-WORKFLOW-013 末段「只换文件名、标签、manifest 而保持同一宽依赖闭包不算完成」，这不是进展，只是改调用形态。同类的无效切法一并记录备用：同闭包 facade 转发、`obj`／反射抹类型（破坏强类型且闭包不减）、单个 fsproj 内部多文件拆分（闭包以 fsproj 为粒度，不变）。
   - 已量测入口（下一段，journal 重力井）：`persistence-journal-agentjournal` 闭包 **51 个分片 / 272 个 source**，其中聚合 `composition-durable-projection` 及其领域依赖占 **36 分片 / 215 source**，纯写入与唤醒核心只有 **15 分片 / 57 source**；引用它的 **60** 个分片里 **34 个只传句柄**（源码不调 `snapshot`／不读 `ProjectionSet`），典型如 `delegation-host-adapter` 现闭包 52 分片，切分后约 **16**。可行的切法只有**类型级依赖倒置**：`AgentJournal` 收窄为低层句柄契约（写入／唤醒／blob），快照读取留在高层；F# 项目粒度下「把 `Snapshot` 成员搬到另一个分片」不成立，因为成员签名必须由持有 `ProjectionSet` 的分片编译。

33. **先量后切：journal 重力井被实测否掉，声明面收尾于 0，改按「窄使用落差」排名推进窄契约**（2026-09-12 第十三班）：
   - 病象（接第 32 条末的入口）：上一班把 `persistence-journal-agentjournal` 的重力井（闭包 51 分片／272 源码、60 个引用者中 34 个「只传句柄」、`delegation-host-adapter` 52 → 约 16）列为下一段。本班先量后切，结论是**这条完全没有收益**，不要再做：
     - journal 的公开签名本来就引用 `ProjectionSet`（定义在 codec 分片，闭包 47）、`FoldRejection`、`MagicTodoFact`（`obligation-ledger.mission-obligation-todo-model`，闭包 44）、`AgentLinkageProjection`（闭包 6）——它天生是投影枢纽的一部分，不是可以「收窄」的物理边界。
     - 逐引用算「独占贡献」：摘掉聚合引用后 journal 闭包 51 → 51，60 个直接引用者的闭包下降**全部为 0**（聚合经 codec 仍可达）；摘掉 todo 引用同样全 0；唯一有独占贡献的引用只有 2 个分片。
     - 消费者普查：60 个引用者里 **52 个调用 `snapshot`／`snapshotWithRevision`／`handleProjection`**（真读投影），只有 8 个不读，而它们经别的路径已可达整个胖闭包，收益 ≤1。所以「把 handle 契约下沉」「把 `Snapshot` 成员搬走」都只是改调用形态：`ProjectionSet`／`AgentProjectionSet` 才是枢纽，动 journal 一分不减。
   - 声明面这一线（第 29／31／32 条）**收尾**：全图 1785 条边按「目标公开名在消费者源码整词零提及」复算，可删边为 **0**。反方向（提及但不声明）不可机械推进：唯一名口径仍产出 1540 条候选，噪声来自成员名与同名类型，必须逐条用编译器裁决——判据只能做筛子（续第 32 条结论）。
   - 新判据：**窄使用／删边落差排名**。对每条边 `C → T` 计算 `drop = |closure(C)| − |closure(C 去掉该边)|`；并算「可归因名字」＝ `T` 的公开名 ∩ `C` 的源码整词提及，且剔除 `C` 的闭包里其它分片也能提供的名字（这消除了同名碰撞）。全图 1785 条边里 `drop ≥ 5` 的有 **105** 条，其中可归因名字 ≤3 的 **68** 条——这是本主线下一个可复用入口：找「为了少数几个名字拖着胖分片」的边，把它换成窄契约。注意 `drop` 只是**上界**，落地方案必须复算拆完后的真实闭包（见下条教训）。
   - 本班落地（最终字节，闭包口径含自身）：
     - `BloggerMaterializationAdmission`／`BloggerMaterializationLease` 这两个自足类型从 `context-compression.context-companion-companionfactfold` 下沉到低层 `context-companion-fact`，`host-boundary.opencode-host-sharedstate` 闭包 83 → **29**（−54）；三个原有使用者分片本就声明了 fact 分片，无需补引用。
     - `ProviderSystemTransform.create: AgentJournal option -> …` 端口化为 `createWith: role: (SessionId -> Role option) -> …`，journal 读取逻辑原样移入 composition（`PluginHooks`），删除该分片的 journal／ledger 引用 ⇒ 该分片 71 → **51**、`participant-provider-languagesurface` 72 → **52**（各 −20）。
     - `Sphinx/ServeEntry.{fsi,fs}` 从 `epistemic-reasoning.sphinx-integration-rules` 拆进新分片 `epistemic-reasoning.sphinx-serve-entry`（进程入口与领域规则本不该同包）⇒ 规则分片闭包 102 → **9**（−93），新分片 103 且无人引用它。
     - `PromptIngressCodec.decode` 端口化为 `decodeWith: tryResolveAgent: (SessionId -> string option) -> …`：纯 wire 解码与「缺 agent 时查会话绑定」的回退职责分离，codec 分片不再引用 `opencode-host-sessionexecutionbinding` ⇒ 80 → **71**。**四个调用点全部显式传入原来那次回退**（`SessionExecutionBinding.tryAgent`），可观测行为逐字不变；其中两个调用点的分片因此如实补上该引用（闭包不变，这是正确所有权，不是收益）。删除死导出 `decode`。
   - **实测否掉的（防重做）**：
     - runtime-scope 裂变（把 `pluginruntimescope` 拆成 Scheduler／ToolRuntimeScope／PluginRuntimeScope 三片）：`ToolRuntimeScope` 实现 `PluginRuntimeScope` 定义的 `ISessionRuntimeOwner`，拆出的新分片闭包仍为 **147**；17 个消费者改动后闭包 +2…+36（只有只依赖 Scheduler 的一路 −25）⇒ **净负，已整体回滚**。教训：侦察给的「剥离后应掉 40–90」是单边删除上界，`ToolRuntimeScope` 与其宿主互相需要，拆开只是把同一批依赖换了条路径。
     - `XWireSurface`「把 6 个计划桥接函数搬给唯一消费者」：`XWireSurface.fs` 紧耦合 `Wire.fs` 的 `XWire`（`Wire.fs` 带 journal／`PluginRuntimeScope`／`TurnRuntimePreparation`），且这 6 个函数被 `XWireSurface.pendingPlan`／`bindProviderRun`（公开面，被 `requirements/provider-attempt-recovery/tests/freeze-admission.test.mjs` 当夹具）使用 ⇒ 需要独立的低层 bridge 模块；而 bridge 若落在 host-boundary 一侧会让 `prefix-stability ⇄ host-boundary` 成子系统环。留作下一班设计项。
     - `ExplicitResumeSuppression`（`ChatParamsHook` 的真实依赖，不是注释里的 `NudgeSent`）：模块持有进程级可变注册表（`markedPhysicalBySession`／`pendingBriefingBySession`／`gate`），下沉要复制状态、端口化要改公开签名与测试夹具 ⇒ 停手。附带发现：`requirements/context-compression/tests/companion-ordinary-material-surface.test.mjs` 用 `doesNotMatch`／文本包含断言源码形状（要求指定文件包含指定函数名），按测试质量条款应先改成行为断言，再谈这条边。
   - 下一段（排名入口，均带`文件:行`级侦察）：`pluginruntimescope` 集群改走**逐消费者 capability 端口**（侦察已给形状：`ISessionTerminator` 给 `opencode-tools-executortoolsurface`、`IBloggerRuntimeHost` 给 `enforcer-continuation`、PTY fork／目录解析端口给 `opencode-tools-ptytool`）；`durable-events.persistence-eventstore-surface` 的 `JournalHandle`／`JournalSurface`／`WorkspaceEventStore` 三个名字被 4 个消费者使用（各 −41…−45）；`host-boundary.opencode-host-fissionhostsurface`（须先解决上面的源码文本断言）；XWire bridge（须先解决环）。

34. **词汇注入打穿 spine 对域词汇的编译时认知；5 个消费族按能力端口迁出宽容器；四张 journal 侧切片各得域属端口**（2026-09-12 第十四班，复审主报告落地）：
   - 本班把「戳审计报告的剩余结构主线」落成了一批经正式验收的迁移批次：外围审计 + `FatTypeScout` 的消费者盘点给出§6 ranking 里排首的四个 journal 单读切片与 report§4 的词汇装配问题，按照灯泡样板（`AgentJournalPortAdapter` + 域属 `*Port` 类型）实现。
   - **词汇批次（源自主报告 §4）**：`durable-events.eventstore-event-vocabulary-contract` 不再整词引用四个领域词汇分片——域词汇配对整体迁进新装⟶配分片 `eventstore-authoritative-vocabulary`（`AuthoritativeEventTypes.isKnown` 形状逐字保持）。`ICanonicalIntegrator` 公布 `IsEventTypeKnown`：`createWithRules` 增栏位参数；`Store` 验证改走 `integrator.IsEventTypeKnown`（`EventStore.createLocal` 签名不变）；全部「接受集」与「未知事件拒写拒读」语义逐字不变，全部 8 个 `createWithRules` 构造点传同一个 `AuthoritativeEventTypes.isKnown`。`eventstore-event-vocabulary-contract` 4→0 域词汇引用，`durable-convergence.persistence-eventstore-processeventlog` 已如数摘除。证明：`event-store-append.test.mjs`（未知事件拒、正常写）、`unified-store-gate` 27 过、`event-store-compile-boundary` 重定边界（零域词挂在 vocabulary 分片、装&配分片携带全部 4 个）。
   - **运行时容器消费收窄**：`HostCompactionObserver`.probe 形参由 `PluginRuntimeScope` 起为自身字段函子记录（probe/gap 记录为函数保留眈读）；`Companion Transform.{fsi,fs}` 实参改窄为 `satellites: SatelliteRuntime`+`bloggerHost: IBloggerRuntimeHost`；`Enforcer/BlogSurface` 封 `IBloggerRuntimeHost` 免 scope；`Enforcer/Continuation` 的三个续传函数改收 `IBloggerRuntimeHost`；`XWire` 以自定 `AttemptPlanCapability` 记录接收 `TryAttemptPlan`/`TryBindAttemptPlan`/`ConsumeAttemptPlan`/`FreezePendingAttemptPlan`/`TryPendingAttemptPlan`（调用方照原语义结构）——摘要：`PluginRuntimeScope` 的「整套运行环境」局部让位于测得的最小环境。分片休整：`context-compression.context-companion-runtimesurface`、`behavior-diagnosis.enforcer-continuation`、`prefix-stability.context-prefix-wire`（Wire 经普通调用点 admitted scope）、`speculative-investigation.strength-opencode-settings`、`action-affordance.opencode-tools-executortoolsurface`、`repository-programming.opencode-tools-codertool`、`repository-investigation.opencode-tools-inspectortool`、`participant-horizon.execution-session-opencode-horizontool` 等 8 个消费分片对 `opencode-host-pluginruntimescope` 的声明引用摘除；`persistence-eventstore-canonicalintegrator`/`processeventlog` 恢复 `model` 论边界。
   - **报警工具批次**：`ChronicleTool`/`SyncDelegateBatching`/`CoderTool`/`InspectorTool`/`PtyTool`/`HorizonTool` 的 `ToolRuntimeScope`/`ISessionRuntimeOwner` 观测沙雕分别窄化为 `ISessionTerminator`、port-optin `snapshot`、本地目录 + snapshot 参数、`PtyRuntimeContext` 记录（`IsDevOps`/`ManagedAgentFor`/`RuntimeFor`），与 `HorizonRuntimeContext`（`RuntimeFor`/`LogicalOwnerFor`/`Journal option `）；`ToolRegistry` 装配面覆衾被保留;测试配套构造选 `PluginBloggerScope`。
   - **journal 单读切片批次**（名片级自审）：`DelegatedToolEstimateLedger`/`SessionStartedAtLedger`/`ProviderFailureLedger+Evidence`/`RequirementGrounding Runtime` 各改收自身 `*Port`（`DelegatedToolEstimatePort`/`SessionStartedAtPort`/`ProviderFailureJournalPort`/`RequirementGroundingPort`），同时 `Evidence.fs` 的 `mayRetry`/`currentState` 不再收 `ProjectionSet`。adapter 起新 `forDelegatedToolEstimate`/`forSessionStartedAt`/`forProviderFailure`/`forRequirementGrounding`；宿主相关调用点（`Wire.fs`、`OrdinaryTurnWorkflow.fs`、`PluginHooks*.fs`、`PluginTransforms.fs`、`RequirementGrounding*.fs`、`Host.fs` 等）改为经 adapter 挡。迁移中上一个被损的现事实：**`participant-provider-attempt-fallback-ledger` 在 HEAD 就缺 `execution-model-routing.strength-policy` 声明（`Workflow.fs` 引用 `ModelRouting.*`）**——已如实补上原实物边（`fallback-ledger` 自加 `@strength-policy`），同子系统内纯恢复正确所有权。
   - **门禁合法变更**：`p0-recovery-join` 的 `workflow-main-session-failure-owner` 是“权威 journal 方可 append”的守卫——迁移后 token `durable` 让位于 `port = forProviderFailure durable`；词禁抬高为「委托 adapter `+` 传 `port`+`ownerSessionId`」。`delegation-compile-boundary` 中 `delegation-host-adapter` (278→280)、`delegation-pty-adapter` (279→281) 棘轮反映共享 duraable-port spine 就算全型移植的尺寸：`m6-slice-boundary` 账内 `sessionstartedatprojection` 现在含 `SessionStartedAtPort.fs`（该分片的真实职责多了 port）。
   - 验证：静态门禁全绿；`source fable full impact` 1442 items `ok`；`npm run format-build-test` H級：`format:check`+`check`+`npm run build`+semantic 3975+integration+e2e+pack dry-run 全数绿。
   - 未做事项与下一入口：四个消费者批次全部按序落成，但 `pluginruntimescope` 三文件仍同片未拆——本批证明的是消费者不再需要整只容器；若下一步拆片，须先把 `ISessionRuntimeOwner` 移进独立契约分片，再拆 `host`/`tool` scope。迁移中如实还回缺失声明边；`participant-provider-attempt-fallback-ledger` 在 HEAD 就使用 `execution-model-routing.strength-policy`，缺这条引用的 focused 编译红，已补上；`action-affordance.opencode-tools-executortoolsurface` 补回 `host-tool-adapter`＋`delegation.runtime`，`participant-horizon` 补回 `delegation-host-adapter`＋`host-tool-adapter`——原先都靠 scope 分片转式可达，现在如实陈出。共享 flat 投影的聚合顺序含禁闭区：`RequirementGroundingPort` 的 `.fs/.fsi` 必须在 `AgentJournalPortAdapter` 之前出现，`dsl-ownership` 的 `Requirement/`＋`open Wanxiangshu.OpenCode.Host` 闭锁仍继续生效——同分片下文件名冲突须以限定标签消歧。下一批预留：`ConcernTools`＋`InstitutionalLearningTools` 走同一 journal-port 模式，装配线都在 `ToolRegistry.fs`，同时并行会撞共享文件——优先后合并同一 worker。

35. **把 `ISessionRuntimeOwner` 独立成契约分片切出最后的 scope 商口；两个剩余工具照同模式迁出；fission surface 的 AgentJournal 由 SessionResumeJournalPort 承载**（2026-09-12 第十五班）：
   - **契约抽取**：`ISessionRuntimeOwner`（只依赖 BCL）从 `PluginRuntimeScope.{fs,fsi}` 搬到新文件 `OpenCode/Host/SessionRuntimeOwner.{fs,fsi}`，宿在既有 `host-boundary.host-session-contract` 分片（闭包 12→13）；namespace 与成员名不变。全部 15 个 scope 分片引用审计：`context-compression.runtime` 摘除 pluginruntimescope（闭包 **441→263**）、`opencode-plugin.plugin-composition` 摘除直接边（闭包 596→599，改经 child surfaces 转达这是记录，不算减）；其余 13 个确认真需要 `ToolRuntimeScope`/`Scheduler`/`PluginRuntimeScope`，保持不动。
   - **工具 port 化**：`ConcernJournalPort`（`ReadState`+`Append`）落在 `concern-routing.interaction-concern-facts`；`InstitutionalLearningJournalPort`（`ReadState`+`PendingAttentionWorkPairs`+`Append`）落在 `institutional-learning.enforcer-institutionallearning-facts`；`ConcernTools`/`InstitutionalLearningTools` 全改成 `port option`（其 fold 分片在此齐摘 stage）；`AgentJournalPortAdapter` 起 `forConcern`/`forInstitutionalLearning`，adapter 分片如实增这两个真边。
   - **fission surface（测试文本断言的迁出）**：`companion-ordinary-material-surface.test.mjs` 原先断言源码文本（要求 `ExplicitResumeSuppression.fs` 含给定 `let` 名）——先改为行为断言（被标记抑制的 path 不再发 double-send、未绑定的 binding 不被 suppress、普通的 `CompanionTransform` 仍进料）；`SessionResumeJournalPort`（`TryResumeProfile`+`CandidateRecords`）住在 `intra-participant-parallelism.execution-fission-fact`，`forSessionResume` 进 adapter；`fissionhostsurface` 摘除 `persistence-journal-agentjournal`，调用方 `PluginHooks.fs`+`HostSignalBootstrap.fs` 改 `journal |> Option.map forSessionResume`。
   - **门禁与文档如实调整**：`shutdown-drain-contract`、`interrupt-boundary` 两组断言原本对 `PluginRuntimeScope.fs` 做源码文本检查——接口迁出后同条款的断言改读 `SessionRuntimeOwner.fs`；`host-session-contract-closure` 的 compile 列表断言同理纳入 `SessionRuntimeOwner.fs`；`ADAPTER_RATCHET` 如实调到实测 `280/285/280`。
   - 验证：`npm run format-build-test` 绿到底（fantomas＋check＋build＋semantic 3975＋integration＋e2e＋pack dry-run）；所有被改分片 focused-compile 列出如上。
   - 下一班入口：`ReconcileSurface`/`HostSignalBootstrap`/`TemporalSurface` 的 `Scheduler` 消费与 scope 拆分是同次排序顺序；`persistence-eventstore-surface` 的 `JournalHandle`/`JournalSurface`/`WorkspaceEventStore` 4 个外部调用约各减 41–45，可同批量同方式去证。

36. **`Scheduler` 与 `ToolRuntimeScope` 各自单开分片——先迁合同再拆项目的完整收尾**（2026-09-12 第十六班）：
   - `Scheduler.{fs,fsi}` 从 `opencode-host-pluginruntimescope` 移到新片 `dispatch-protocol.composition-turn-scheduler`；所依赖实型走其真实 owner（`TurnBinding`/`ReconcileProgram`/`HostSignal`/`QuiescenceGuard`/`ISessionSnapshotPort` 等）。`Composition/Turn/Program.fs` 不动——它按 namespace 消费同层类型。
   - 之后 `pluginruntimescope` 仍任一COPE丢两处：`PluginRuntimeScope`（仅靠 `ISessionRuntimeOwner` 接线）与 `ToolRuntimeScope`（真实依赖 `Change.Host`/`Fork.Host`/`Recovery`/`Mission.Relay`/`Persistence.Journal`…）；双向界线不显 `Tool` → `Plugin` 类型。抽 `PluginRuntimeScope` 留 `opencode-host-pluginruntimescope`，`ToolRuntimeScope` 抽走 `opencode-tools-toolruntimescope`。
   - 15 个消费分片审计（按文件逐行验证）：六个 tool-surface 分片全部只看 `ToolRuntimeScope` 而切到新片（每分片 .fs 数齐降 **−26**）；`fissionhostsurface` 此前已提走 scheduler；`hostsignalbootstrap`、`turnruntimepreparation`、`eventstorewritersurface` 真的需要 `PluginRuntimeScope`（或兼需 scope+scheduler），保留原边。
   - 门禁合法变更：`causal-wait/m6-slice-boundary` 的 `mailbox must declare` 断言原指 `pluginruntimescope` 所谓「物理 mailbox 商口」——真实使用方是 `ToolRuntimeScope.fs:188/615` 的 `CompletionMailboxRuntime.create`，断言改指 `opencode-tools-toolruntimescope`；`host-session-contract-closure` 的契约编译清单承接 `SessionRuntimeOwner.fs` 由前批接入。
   - 验证：`npm run format-build-test` 全管道绿；scheduler 新片与其余分片 focused 编译全绿（`pluginruntimescope` 870→858；`toolruntimescope` 844）。refs 1803→1811、shards 236→238 净出双枚。「`pluginruntimescope` 今以 147 层闭包坐在 SCC 边缘」的中山路已打通，但 18 节点 SCC 本身没有按本批可以解决——它仍跨了其他子系统。
