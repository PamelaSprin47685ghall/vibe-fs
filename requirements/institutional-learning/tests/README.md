# Proof status

Unit oracles for `institutional-learning-001` through `008`.

The test suite covers:
- Raw natural-language experience input for `celebrate` and `regret` (`001`).
- Single bounded disposition (`ABSORB`, `BIRTH`, `DISCARD`) per evaluation (`002`).
- Enhancer bounded to input experience and live rulebook snapshot (`003`).
- Behavior-rule admission gate enforcement for `BIRTH` candidates (`004`).
- Degradation to `DISCARD` when reusable trigger or distinction is absent (`005`).
- Equal non-punitive handling of positive and negative experiences (`006`).
- Resurfacing unconsumed deferred work at the tail of `celebrate` (`007`, GAP closed).
- Idempotent occurrence replay with frozen disposition (`008`).
