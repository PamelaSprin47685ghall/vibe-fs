# Manager 评审期只读工具：保姆级实现方案

> 适用项目：`wanxiangshu`  
> 核对日期：2026-09-24  
> 核对基线：`master`，`1e91c88fe`，工作区当时干净。  
> 本文是实施方案，不是已经完成的改动或测试报告。新模块名为建议名称；现有文件和符号均尽量指向已核对的位置。实施时若 HEAD 已改变，先复核对应位置，不照搬行号。

## 0. 最终要做成什么

Manager 在当前独立评审被系统接纳之前，可以亲自只读调查，也可以委派只读 Engineer。评审被接纳之后，Manager 不再亲自读取、检索或处理仓库代码，回到原有的组织、委派和收尾职责。

工具面从第一轮起固定为并集，不因 Review 改变。具体新增、开放：

```text
read-manager
grep-manager
glob-manager
js-manager
```

前三个是插件注册的 Manager 专用工具，不是运行时把原生工具改名。`js-manager` 沿用现有按角色生成的命名方式。

Manager 始终不能使用原生 `read / grep / glob`，也不能借 `js-engineer / js-devops` 绕路。Engineer、DevOps 继续使用自己的原有工具。

四个工具的模型可见 schema 均增加：

```json
{
  "contract": {
    "type": "string",
    "enum": ["do-not-use-except-for-review"]
  }
}
```

`contract` 加进 `required`，但本地对它采用乐观处理：上游校验了就接受上游结果；已经进入插件的调用，不因为该字段缺失或填错而被插件额外拒绝。真正的权限判断依据当前权威事实，与 `contract` 的值无关。

参数处理采用本次确定的办法：**before 把字段藏到参数对象的私有位置，再删除公开字段；after 原样恢复并清理私有位置。** 异常路径由同一个恢复函数收口，不另写第二套恢复逻辑。

本次不做：新增 Manager 角色或 Persona、切换工具列表、改变 Host 源码、fork Host、复制整个 Engineer 权限集、创建第二个 Review 状态机、引入 `allowRepair`、把 Engineer 变成命令执行器。

---

## 1. 先固定六条边界

### 1.1 Review 的分界是“已接纳”，不是“被调用过”

使用当前任期的：

```text
RoadView.AcceptedAssessmentTransport.IsSome
    → ManagerCapabilityFacts.HasAssessment
```

已有生产落点：

- `src/Wanxiangshu/OpenCode/Tools/ToolRuntimeScope.fs`：`managerFactsOfSession`。
- `src/Wanxiangshu/Foundation/OfficeCapability.fs`：`permissionsForManagerFacts`。

不要解析对话中的“我完成评审了”，不要检查最近有没有 `review` 字样，也不要新增内存布尔量 `reviewCompleted`。

由此得到：无效 Review 未被接纳，不关闭只读；Review 已落盘但回复丢失，仍然关闭只读；重启、压缩上下文不能重新打开权限。新的合法任期是否有评审，由当前任期自己的事实决定，不能继承上个任期的临时开关。

### 1.2 固定可见工具，与动态执行准入分开

```text
角色能力上限：由 CanonicalRole 决定。
请求工具定义：由角色与既有 RequestKind 约束决定，同一前缀内保持固定。
本次执行准入：再根据当前任期、Review、证书、清理和退任事实收窄。
```

不要把 `permissionsForManagerFacts` 的结果拿去逐轮生成 Manager 工具列表。否则评审后即使只是删除工具，也会改变工具面。

同时保留已有 RequestKind 收窄。例如 StrengthReplica 的原生只读工具范围不能因为本次增加 `js-manager` 而被扩大。内部 leaf、Bookkeeper 也不能被误当成普通 Manager。

### 1.3 权限表

以下表格只描述合法、活跃、没有进入既有收尾限制的 Manager；失去当前权威、清理阻塞、有效证书和退任冻结仍按原门禁优先处理。

| 动作 | 当前 Review 未接纳 | 当前 Review 已接纳 |
|---|---|---|
| `read-manager / grep-manager / glob-manager` | 允许只读调查 | 拒绝 |
| `js-manager` | 允许；注入的实际 API 也必须只读 | 拒绝整个工具 |
| 原生 `read / grep / glob` | 拒绝 | 拒绝 |
| 文件写改移删、真实命令、终端 | 拒绝 | 拒绝 |
| fork Engineer | 可做只读调查，不得提前修理评估对象 | 沿用已有工作门禁 |
| resume 已有 Engineer | 可续做只读调查 | 沿用已有工作门禁 |
| resume 固定 DevOps | 拒绝派工 | 沿用已有工作、忙碌与收尾门禁 |
| `review` | 允许一次有效接纳 | 拒绝重复接纳 |
| 其他原有 Manager 工具 | 保持原有语义，不额外提权 | 保持原有语义，不额外提权 |

“Review 后允许 DevOps”不是无条件放行。如果已有证书或清理规则只允许 `join / suicide`，仍按该收尾限制执行。

### 1.4 禁止 DevOps 派工，不是禁止整个 resume

`resume` 同时支持已有 Engineer 和固定 DevOps。必须按**解析后的目标身份与现有绑定**拒绝评审前的 DevOps assignment，不能写成“Review 前一律禁止 resume”。

拒绝要发生在 `EnsureRoadDevOpsBound`、更新 charge、创建 PendingRun、发送执行请求之前。已经存在的固定绑定不是一次派工；本次也不应为了“禁止派工”删除合法的持久绑定。

### 1.5 contract 只是提醒

| 当前事实 | contract 正确 | contract 缺失 | contract 填错 |
|---|---|---|---|
| 可以做当前评审调查 | 本地门禁允许 | 本地门禁允许 | 本地门禁允许 |
| Review 已接纳 | 本地门禁拒绝 | 本地门禁拒绝 | 本地门禁拒绝 |

本表描述插件自己的执行门禁，不承诺绕过 provider、SDK 或 Host 已经发生的 schema 拒绝。业务参数如 `filePath` 缺失、offset 类型错误，仍应正常报错。

### 1.6 并发采用“执行准入”作为边界

本次默认语义：禁止 Review 接纳后新获得准入的调用，不追溯取消此前已合法开始的只读调用。

before 的通过不是永久通行证。进入实际执行器前必须再读当前事实。before 通过后、实际执行前若 Review 已接纳，必须拒绝，且不能产生文件读取。

已经开始的只读调用可以正常结束；不为这次改动增加全局锁、轮询、在途任务强杀或“Review 必须等待一切读结束”的新协议。测试需要固定这两种次序，不能依赖运气或 sleep。

---

## 2. 已核对的代码落点与容易漏掉的地方

下表中的路径均相对仓库根目录。它们也是本文判断当前实现的依据。

| 文件或规范 | 当前可见事实 | 本次动作 |
|---|---|---|
| `src/Wanxiangshu/Foundation/OfficeCapability.fs/.fsi` | Manager 基础集合没有 Read/Glob/Grep；当前事实投影已存在 | 扩大静态上限；评审接纳后删除评审只读能力 |
| `src/Wanxiangshu/OpenCode/Tools/StaticTools.fs/.fsi` | 工具名、角色 permission、request tool map 集中在这里 | 加入专用名，并保持原生名对 Manager 为 deny |
| `src/Wanxiangshu/OpenCode/Tools/ToolRuntimeScope.fs` | 当前任期、评审、证书事实已有查询 | 复用，不复制状态推导 |
| `src/Wanxiangshu/OpenCode/Tools/ToolRegistry.fs` | 自定义工具通用执行门禁；生成各角色 JS 工具 | 注册三个专用读查工具；给四个工具接当前事实门禁 |
| `src/Wanxiangshu/OpenCode/Plugin/PluginHooks.fs` | `toolDefinition` 当前为空；before/after 已做 grounding 等工作 | 接 schema 装饰、参数隐藏与恢复；维护正确顺序 |
| `src/Wanxiangshu/OpenCode/Codec/ToolHostCodec.fs` | `register` 是 rawArgs → ToolSpec 的插件入口 | 作为异常恢复边界的候选接点，不扩散 raw JS 操作 |
| `src/Wanxiangshu/OpenCode/Plugin/PluginSessionWiring.fs` | `requestToolMap` 的生产调用之一在同步委派接线 | 跟进工具名投影接口，防止请求 map 又把工具关掉 |
| `src/Wanxiangshu/Execution/Delegation/Fork/OpenCode/Tool.fs` | `executeManagerResumeAfterGuards` 区分固定 DevOps，并调用 EnsureRoadDevOpsBound | 在副作用前拒绝评审前的 DevOps 派工 |
| `src/Wanxiangshu/Repository/Programming/Js/Surface.fsi` | `JsSurface` 已有 `Capabilities` | 将它传至真实执行 API，不另算第二套权限 |
| `src/Wanxiangshu/Repository/Programming/Js/OpenCode/ToolHost.fs` | JS 工具当前只声明 `program` 业务参数 | `js-manager` 的 contract 只由 definition hook 添加 |
| `src/Wanxiangshu/Repository/Programming/Js/ToolsBindings.fs` | `createApi` 当前直接构造 read/glob/grep/edit/write | 改为按已授权 JsCapability 构造实际 API |
| `src/Wanxiangshu/Repository/Programming/Js/OpenCode/ToolWorkflow.fs` | runCore 当前创建 API，运行后按 staging 提交修改 | 传递 capability；只读执行不得产生 mutation |
| `src/Wanxiangshu/Process/JsSandbox.fs` | 注入的 `api` 在模型程序所在的执行上下文中 | 不能只隐藏 SDK 方法而仍注入写 API |
| `src/Wanxiangshu/OpenCode/Host/RequirementGrounding/Gate.fs` | 原生 read 按工具名记录观察；JS 有程序观察入口 | 新名称必须进入正确观察链，但不得重复计数 |
| `requirements/office-capability/WHAT.md` | [006]/[007]/[011] 禁止 Manager 亲自检视 | 重写相关条款，不仅在文末补例外 |
| `requirements/relay-assessment/WHAT.md` | [009] 指向只读 Engineer 支持独立评估 | 改为 Manager 可直接取证，也可委派只读 Engineer |
| `requirements/prefix-stability/WHAT.md` | [001] tools 完全一致；[007] 不因 Review 改 system | 保留；增加本场景的实测证明 |
| `requirements/host-boundary/WHAT.md` | [014]/[018] 零 Host 修改；[019]/[023] 需要公开合同与 canary | 不导入 Host 私有工具，不修补 node_modules |

### 三个不能漏的结论

**不能只把 Manager 的基础集合加上 Read。** 那样原生 read 可能被意外允许，JS 工具也可能只在表面上只读。

**不能只改 Hook。** 直接调用注册执行器必须同样被拒绝，不能把 Host before 当成唯一安全门。

**不能只改中文提示词。** 共同法、角色能力、评估请求、评估后工作指令及中英文工具说明必须一致。

---

## 3. 先修改规范，再写失败测试

### 3.1 既有条款的修改

在 `requirements/office-capability/WHAT.md` 中：

- [003] 保留同 Office 身份与能力上限一致，不以机器档位改变权限；明确当前执行事实可以收窄一次动作的准入。
- [006] 删除“Manager 不是文件检视者”的绝对说法，改成“只允许当前独立评审中的直接只读取证；不承担直接源码实现”。
- [007] 重写整条。不能保留旧标题“Manager 无普通工具：不读文件……”再补一句例外。
- [011] 保留管理、编排与 Fission 禁止，加入评审前取证与评审后禁用的边界；澄清完整管理权不等于任何时刻都能向 DevOps 派工。

在 `requirements/relay-assessment/WHAT.md [009]` 中：

> Manager 可以亲自使用评审专用只读工具，也可以委派只读 Engineer 建立代码事实。评分仍由当前评估者独立作出，实现者报告只能作为待核实线索。评审期不得修改评估对象。

保留该包 [008] 的信息隔离：评审前不向模型透露低分后接手修复、全优后结束、下一轮接力等流程。新增工具提醒只说当前用途和失效条件，不解释隐藏循环。

在 `requirements/capability-enforcement/WHAT.md` 中，调整 [002]/[012] 对“同一能力真相”的表述：schema 是固定上限投影，执行门禁是当前事实投影；两者同源，不意味着每次动态删工具。原有 [007]/[008]/[009]/[010] 的跨入口一致、JS 同构、名字唯一和身份未决拒绝仍需满足。

### 3.2 本次新增条款建议

在已核对的基线中，`capability-enforcement` 最后为 [024]，`host-boundary` 最后为 [031]。可按以下编号新增；若实施时已被占用，顺延并同步测试锚点。

| 新条款 | 所属规范 | 内容 |
|---|---|---|
| [025] | capability-enforcement | Manager 评审专用只读工具固定可见；当前事实收口；原生及跨角色绕路拒绝 |
| [026] | capability-enforcement | Review 接纳前不得向固定 DevOps 派工；已有 Engineer 的合法 resume 不受误伤 |
| [032] | host-boundary | contract 仅为 provider 提示字段；私有暂存、原样恢复、异常与历史完整性 |

JS 真实 API 的过滤测试优先补到已有 `capability-enforcement [008]` 及 JS 所属规范的相应条款，不为同一权限规则再立一个语义所有者。

新增模块记入相关 `APPLIES-TO`；必要时同步对应 WHY 和规范索引。先读该包的规则，不自行创造另一种测试编号体系。

---

## 4. 公共结构：一份目录，一份权限判定

### 4.1 只声明一次四个工具

建议新增一个很小的纯模块：

```text
src/Wanxiangshu/OpenCode/Tools/ManagerReviewTools.fsi
src/Wanxiangshu/OpenCode/Tools/ManagerReviewTools.fs
```

它只拥有名字、用途映射和 contract 常量，不读 journal，不读文件，不决定当前阶段：

```text
read-manager → { Read }
grep-manager → { Grep }
glob-manager → { Glob }
js-manager → { Read, Glob, Grep }

contract 字段名 = contract
contract 唯一值 = do-not-use-except-for-review
```

对外建议只暴露：

```fsharp
val contractValue: string
val requiredPermissions: toolName: string -> Set<ToolPermission> option
val isReviewTool: toolName: string -> bool
```

这里的集合是工具对既有语义权限的映射，不是第二套角色权限矩阵。不要增加 `ReadManager`、`GrepManager` 一类重复权限，也不要用 `EndsWith("-manager")` 或 `StartsWith("js-")` 自动授权未知工具。

静态工具名投影、schema 装饰、before、after、注册执行器均引用这份目录。三个简单文件工具的具体 schema 和程序仍由其业务 owner 定义，别把所有东西塞进这个纯模块。

### 4.2 修改 Manager 静态能力上限

在 `OfficeCapability.permissions Role.Manager` 原集合上只增加：

```fsharp
ToolPermission.Read
ToolPermission.Glob
ToolPermission.Grep
```

不要授予 Write、Edit、Move、Remove、Exec、Pty、BashHoneypot、Fission。Fetch、Sphinx 等非纯文件能力，不因为“Engineer 有”就顺便复制；已有 Manager 能力照旧。

定义一个纯的评审只读集合，供当前事实投影做集合差。

### 4.3 修改当前事实投影

保留已有分支优先级：

```text
无活跃任期                  → 空集合
有清理阻塞                  → 原有 Join + Finality
有匹配当前任期的有效证书     → 原有 Join + Finality
已有 AcceptedAssessment     → Manager 静态集合 - ReviewAssessment - 评审只读集合
否则                        → Manager 静态集合
```

退任冻结仍由现有执行门禁处理，不能因为本次新增三个权限而绕过。

不要通过 `not HasAssessment` 单独放行。没有活跃任期也可能没有 assessment；不存在评审记录，不等于拥有评审权。

### 4.4 before 与注册执行器共用判定

建议共享的判断过程：

```text
精确识别专用工具
    ↓
确认当前调用的权威主体确为 Manager
    ↓
从 ToolRuntimeScope 取得当前任期事实
    ↓
沿用退任冻结与现有权限投影
    ↓
requiredPermissions ⊆ 当前允许权限？
    ├─ 是：准入
    └─ 否：返回稳定的拒绝结果，不进入文件或 JS 执行器
```

Hook 不再自行拷贝 `RoadView → ManagerCapabilityFacts` 的推导。`PluginHooks.fs` 在完成 `toolRegistration` 之后再创建需要 runtime 的 before 闭包，直接注入现有查询能力。当前文件已经先取得 `toolRegistration`、再注册 hooks，可以沿这个接点调整定义位置，不必引入全局 `currentManager`。

`toolRegistration = None`、身份不明或事实不可用时，对专用工具拒绝。其他工具仍走原有行为。

---

## 5. 工具面：注册、permission 和请求投影一起改

### 5.1 真正注册三个专用工具

在 `ToolRegistry` 的生产 `baseSpecs` 中加入三个新 ToolSpec。它们必须有独立名字、明确业务 schema、Manager-only 的角色准入和实际执行函数。

`tool.definition` 只装饰已经存在的定义，不能凭空注册一个工具。before 也不能通过把 `input.tool` 改成 `read` 就假装完成可靠转发。

### 5.2 StaticTools 的角色映射

`knownToolNames` 增加 `read-manager / grep-manager / glob-manager`。`js-manager` 已存在，不重复添加。

对普通 Manager 请求：

```text
allow: read-manager, grep-manager, glob-manager, js-manager
 deny: read, grep, glob, js-engineer, js-devops，以及原有所有写入/终端入口
```

对 Engineer、DevOps：保留原有文件工具与自己的 JS 工具；拒绝三个 Manager 专用名和 `js-manager`。

不要把全局 `Read → read` 改成 `Read → read-manager`。应在 role-aware 的工具名投影中处理 Manager 别名，其他角色仍映射到原生名称。

### 5.3 请求级 tools map 不能遗漏

`StaticTools.requestToolMap` 当前只接受权限集合。不要仅凭集合里存在 Read 就猜这是 Manager。

如果该接口需要输出 Manager 的工具面，就显式传入角色；若还需要区分普通请求与 replica/leaf，继续携带既有 RequestKind 或沿用该请求层的收窄入口。同步修改 `.fsi`、`PluginSessionWiring.fs` 等生产调用和测试调用，不留旧接口兼容壳。

必须测试最终发给 provider 的完整工具定义，不仅测 agent permission 对象。请求 map 中的一个 false 仍可能把已经注册的工具挡掉。

### 5.4 前缀稳定的范围

同一 Manager Life 内，Review 前后以下内容全部不变：

- 工具名字、顺序和数量。
- description、JSON schema、properties、required、enum。
- JS 基类说明、示例和可见能力。
- system prompt。

当前指令通过已有的合法追加投影交付，不能回写历史系统提示或历史工具说明。不能为掩盖本次工具漂移强行切换 PrefixEpoch。

部署新版本带来的工具定义变化，与同一运行中提交 Review 是两回事。已有活跃 Life 不应被热补丁原地换工具面；沿项目现有升级、重建机制处理，不伪造一个 Review 触发的冷边界。

---

## 6. schema 装饰：只影响模型看见的定义

建议新增一个小的 Host 互操作模块，集中处理 schema 和私有字段：

```text
src/Wanxiangshu/OpenCode/Host/ManagerReviewContract.fsi
src/Wanxiangshu/OpenCode/Host/ManagerReviewContract.fs
```

它引用第 4 节的目录，但不自行决定权限。建议接口：

```fsharp
val decorateDefinition: toolInput: obj -> toolOutput: obj -> unit
val hide: args: obj -> unit
val restore: args: obj -> unit
```

### 6.1 装饰过程

在 `tool.definition` 中读取 `toolID`。不是四个专用工具，原样返回；是专用工具，则：

1. 确认参数 schema 是当前公开接口提供的 object schema；结构不支持时明确报错，不猜测递归改写。
2. 在当前输出的独立 schema 上加入 `properties.contract`。不要改动被其他工具共享的底层 Zod 对象或 schema 模板。
3. 在原有 required 顺序之后追加一次 `contract`。重复调用不得追加第二次。
4. enum 恰好一个值，type 为 string，不随阶段变化。
5. 保留业务字段、原有默认值、additionalProperties 等既有设置。
6. description 只追加一次固定的用途提醒，或把提醒稳定放在字段说明中。不要每次调用累加文案。

推荐字段说明：

> 仅用于当前尚未被接纳的独立评审。评审一旦被系统接纳，不得再次调用本工具。固定填写 do-not-use-except-for-review。

执行器的 `ToolSpec.Arguments` **不增加 contract 的必填校验**。否则 before 删除它之后，执行器又会因缺字段失败。上游 provider schema 检查和插件自己的业务参数检查是两层，不能混为一谈。

### 6.2 只按专用工具名判断

本地已读取的 `@opencode-ai/plugin` 类型定义中，definition hook 输入仅给出 `toolID`，没有 sessionID/agent。因此本次使用专用名即可精确装饰，不再要求 Host 新增角色字段。

实际输入输出结构仍以安装版本及其 canary 为准；类型声明不能证明运行时对象是否会被复制。

---

## 7. 私有字段：怎样藏，怎样恢复

### 7.1 保存位置

采用模块内部创建的 `Symbol` 作为私有键，并以 `enumerable: false, configurable: true` 挂在**本次参数对象**上。

```text
args
├─ filePath / pattern / program / 其他业务参数
└─ [模块私有 Symbol]       // 不进公开 schema，不作为普通 JSON 字段输出
    ├─ contractDescriptor // 原来的属性描述；undefined 表示原本缺失
    └─ originalKeyOrder   // 仅在实际序列化路径需要保持原键序时使用
```

不用可由模型猜出的 `_contract` 普通参数作为授权凭据；也不用 `Symbol.for` 的全局名字注册表。私有暂存不授予任何权限，拿不到或没有这个暂存也不能绕过阶段门禁。

模块内的 Symbol 是字段标识，不存放“当前调用”的共享值。不同调用的值存在不同 args 上，因此天然按对象分开。不要再设 `lastContract`，不要用 sessionID 存一份会被并发覆盖的参数。

Symbol 只是内存中的私有键，不是加密或持久化方案。它不会自动穿过 JSON、对象展开、schema parse 或 Host 的对象重建；这些路径必须由真实合同测试核实。相关语言行为见文末 MDN 参考。

### 7.2 必须保存“原样”，不是保存一个正确字符串

至少区分：

```text
没有 contract 属性
有 contract: undefined
有 contract: null
有 contract: 正确字符串
有 contract: 错误字符串或其他错误 JSON 值
```

JSON 模型调用不会自然产生 undefined，但边界单测仍应覆盖它，以免实现把“缺失”和“存在但无值”混成同一种状态。

恢复时不能统一补成正确枚举值。这会篡改模型的原始调用，也与乐观处理相矛盾。

### 7.3 互操作语义示例

下面只说明字段保存、恢复的最小语义；生产实现放在 `.fs/.fsi` 的互操作边界，不为这段示例另建一套 TypeScript 生产模块。

```javascript
const savedContractKey = Symbol("manager-review-contract");

function hideContract(args) {
  if (args === null || typeof args !== "object" || Array.isArray(args)) {
    throw new TypeError("Tool arguments must be an object");
  }
  if (Object.hasOwn(args, savedContractKey)) return;

  const descriptor = Object.getOwnPropertyDescriptor(args, "contract");
  if (!Object.isExtensible(args) || (descriptor && !descriptor.configurable)) {
    throw new TypeError("Tool arguments cannot hold the review contract");
  }

  Object.defineProperty(args, savedContractKey, {
    value: { descriptor },
    enumerable: false,
    configurable: true,
  });
  try {
    if (!Reflect.deleteProperty(args, "contract")) {
      throw new TypeError("Review contract could not be hidden");
    }
  } catch (error) {
    delete args[savedContractKey];
    throw error;
  }
}

function restoreContract(args) {
  if (args === null || typeof args !== "object") return;
  if (!Object.hasOwn(args, savedContractKey)) return;

  const saved = args[savedContractKey];
  if (saved.descriptor === undefined) {
    delete args.contract;
  } else {
    Object.defineProperty(args, "contract", saved.descriptor);
  }
  delete args[savedContractKey];
}
```

这段示例不包含 tool 名判断、权威门禁、异常收口和历史序列化。它也不保证删除再添加后属性枚举顺序不变，不能把它单独通过单测就当成整个协议完成。

### 7.4 before 顺序

```text
1. 从真实 Host 上下文解码调用身份。
2. 专用工具执行当前事实门禁；不通过时直接拒绝，尚未改动 args。
3. 完成必要的业务参数检查和既有 before 工作。
4. 隐藏 contract，作为本插件 before 的最后一个操作。
5. 返回，不在隐藏后继续执行可失败的异步工作。
```

业务参数检查复用执行器自己的 decoder，不能在 hook 里重写第二套 filePath/pattern/program 规则。检查不包括 contract 的枚举合规性。

隐藏操作要么完整成功，要么保持原状；不能在私有字段未建立时先删公开字段。

### 7.5 after 顺序

```text
1. 若是专用工具，先同步恢复 contract，清理私有字段。
2. 再执行 RequirementGrounding 的 after。
3. 再执行 Casebook 等原有观察。
```

恢复不能依赖当前仍处于评审期。读取开始后可能恰好发生 Review 接纳，恢复历史参数仍应完成。after 是清理，不是重新授权。

恢复必须幂等：执行器异常路径已经恢复过，after 再来应无事发生。重复 before 也不能覆盖第一次保存的原值。

### 7.6 异常路径，按位置处理

| 出错位置 | 处理 |
|---|---|
| provider/Host 在 before 前拒绝 schema | 尚未隐藏，无需恢复；不拦截该拒绝强行重试 |
| 权威门禁或业务参数检查失败 | 在隐藏前报错，args 未改动 |
| 本插件隐藏操作失败 | 原子回退自身改动后报错，不执行工具 |
| 已进入插件拥有的 raw 执行边界后失败 | 在该边界的异常/finally 路径调用同一个 restore，再保留原错误 |
| 正常返回，包括正常返回的领域失败结果 | after 首先恢复 |
| after 中后续 grounding/观察失败 | 因恢复在第一步，公开参数已经还原 |
| Host 在 before 后、插件执行器前失败或取消 | 必须由实际 canary 找到其终态/历史形成路径，证明参数不会以缺损状态进入后续上下文 |
| 进程被直接终止 | 不要求已死亡进程运行 finally；要求持久化的原始调用仍完整，恢复后不依赖 Symbol |

执行边界首选 `ToolHostCodec.register` 的 rawArgs 包装层，或该层注入的专用清理闭包。只作用于四个工具，不改变所有工具的异常政策。成功时仍由 after 恢复；发生异常或取消导致栈展开时，执行边界负责提前恢复。

**前提是这个边界取得的 args 确为保存过私有字段的对象，或持有由公开、可证明接线传来的原始对象引用。** 不能因同一个 callID 就假装对象没有被复制。

如果实际 Host 会复制或剥离该字段，先在公开插件接点内解决对象所有权和清理，再继续。不得补一个“猜参数”的恢复器，不得修改 Host 私有源码。无法证明某条路径时，记录具体失败的 canary，不能宣称全路径恢复已经实现。

### 7.7 历史和前缀不能被参数清理污染

原始工具调用和暂供执行器使用的 args 是不同职责。原始调用的持久化记录应保留模型提交的原值，不应被去掉 contract 的临时执行视图覆盖。

重点检查两件事：

**对象身份。** before、执行器、after、历史持久化是否引用同一个对象？哪些地方会 clone 或 serialize？必须在实际 Host 路径上观察，不用 mock 自己编出一个理想顺序。

**键顺序。** 删除再添加 `contract` 可能改变 JSON 字段顺序。若历史保存的是原始调用文本，保持该文本原样，不从临时 args 重建。若真实路径确实重新序列化 args，则在私有暂存中保存原键序，恢复时按原键序恢复；其他 hook 新增的字段追加，其他业务字段的合法改动不得被旧快照覆盖。

验收比较真实 provider wire 和历史原文，不只比较解析后的对象相等。不能通过重写历史、排序整段消息或切换 PrefixEpoch 来掩盖参数恢复引入的漂移。

---

## 8. 三个专用读查工具怎样实现

### 8.1 不是转调 Host 私有 read

官方插件入口支持注册自定义工具，但“可以注册工具”并不等于“能通过公开接口调用任意原生工具”。本次不从 `opencode` 内部目录导入 ReadTool/GrepTool，不借客户端发一条新的模型调用模拟读取，也不用 shell 包装器实现读查。参见文末 OpenCode 插件文档及仓库 host-boundary 规范。

建议新增：

```text
src/Wanxiangshu/Repository/Programming/Js/OpenCode/ManagerReadTools.fsi
src/Wanxiangshu/Repository/Programming/Js/OpenCode/ManagerReadTools.fs
```

它们注册三个普通 ToolSpec，复用本仓现有只读文件能力。代码归属放在与现有 JS/OpenCode 工具 owner 一致的边界，具体 owner project 以当前编译清单为准。

### 8.2 复用方式：固定程序调用同一 JS 工作流

优先采用一条小而明确的路径：

```text
专用 ToolSpec 的业务参数
    ↓ 类型与路径检查
程序生成的固定只读 JsProgram
    ↓
与 js-manager 相同的 JsToolWorkflow + 只读 capability
    ↓
现有文件操作、访问观察、结果渲染与有界截断
```

这里的程序由系统生成，不是让 Manager 通过 `read-manager` 提交任意代码。参数只作为数据进入固定模板，字符串必须使用 JSON 编码或等价可靠字面量编码，不直接拼进代码、正则字面量或模板字符串。

不要通过调用另一个注册工具的 execute 来套娃，这会重复门禁、用量记账和 after 观察。复用的是 workflow/业务函数，不是 LLM 工具调用链。

如果固定模板复用确实造成不必要的运行开销，可把现有 read/glob/grep 的薄业务函数下沉后供双方直接调用；不要复制实现。首版不同时保留两条执行路径。

### 8.3 建议的业务参数合同

为减少模型换工具的负担，沿用熟悉的字段名；以下是新工具的建议合同，需要写入规范与测试，而不是声称已经与原生工具逐字节兼容。

| 工具 | 参数 | 行为 |
|---|---|---|
| `read-manager` | `filePath: string`，`offset?: positive integer`，`limit?: positive integer` | 读取 UTF-8 文本；offset 从第 1 行起；返回路径、行号、片段和明确截断信息 |
| `glob-manager` | `pattern: string`，`path?: string` | 在获准目录内按现有 glob 规则列出路径；不把路径列表冒充已读取内容 |
| `grep-manager` | `pattern: string`，`path?: string`，`include?: string` | 按现有 grep/anchor 语义搜索文本，返回路径、行号、匹配内容；非法模式报错 |
| `js-manager` | `program: string` | 自定义只读调查程序，复用现有 JsProgram 合同 |

`contract` 由 schema hook 追加，不列入这些业务 decoder。

`read-manager` 的行片段默认值、最大值和输出上限优先复用当前已定义的工具界限。若现有 JS 只返回完整 FileView，则只在固定程序内做行片段转换，不另建文件读取器。不要随意发明数个不一致的“安全阈值”。

原生 Host read 可能还有目录、图片或其他扩展行为。本次已核对的复用文件 API 是 UTF-8 文本路径；因此首版应明确写出文本范围，目录调查使用 glob，不把不支持的二进制/图片当作空文件返回，也不声称与 Host read 所有扩展能力等价。

### 8.4 目录归属

根目录应来自当前调用的权威绑定和实际工作树。沿用 `ToolRuntimeScope` 的目录解析，不把插件启动时的全局 workspaceDirectory 无条件当成所有 Manager 的工作树。

参数中的 path/filePath 是获准工作区中的目标，不是一个能自由切换 owner 的 workspace selector。绝对路径、父目录、符号链接、被忽略文件等规则要与既有文件授权规范对齐，并在 schema 描述中说清楚。

如果复用路径只支持获准根内访问，不能靠字符串前缀比较判定；规范化路径与链接行为需有测试。不要因为 Host 的 `external_directory=allow` 就默默扩大自定义执行器的权限，也不要把这种配置等同于已完成自定义工具的路径检查。

### 8.5 错误和观察

文件不存在、非 UTF-8、模式错误、路径越界、结果过大都要有可判断的错误，不能变成空字符串、空列表或假成功。

复用 `JsToolWorkflow` 时，优先由其 `fileAccessObservation` 记录实际读到的路径。别再让 alias after 按 filePath 记录第二次。若采用直接读查 owner，则由那个 owner 记录实际访问，Hook 名称映射只用于必要的分类。

被门禁拒绝的调用不能留下“已读某文件”的证据。glob 只有列目录，不应误记为每个结果文件都已阅读。正常读取、grep 扫描、工具调用计数和 Casebook 观察各自保持原来语义，不重复计数。

---

## 9. js-manager 必须在实际 API 层只读

这是本次最容易漏掉的实质问题。

当前 `JsToolGenerator.generate` 能按权限生成不同基类；但 `JsToolWorkflow.runCore` 调用的 `JsToolsBindings.createApi` 当前直接构造读写方法。`JsSandbox` 又把 api 放入模型程序运行的上下文。因此，只生成一个没有 write 方法的 JsProgram，并不足以证明实际调用面只读。

### 9.1 传递已有 Capabilities，不重新推断

`JsSurface` 已有 `Capabilities: Set<JsCapability>`。沿以下链传递它：

```text
JsSurface.Capabilities
    ↓
JsToolSpec / ManagerReadTools
    ↓
JsToolWorkflow.run / runWithFileAccessObservation / runCore
    ↓
JsToolsBindings.createApi(capabilities, root, staging, readSnapshots)
```

同步修改 `.fsi` 与所有实际调用者。不要通过扫描 baseClassSource 文本猜权限，也不要增加一个默认 true 的 `allowWrite` 兜底。

### 9.2 API 只构造已授予的成员

Manager 的 runtime capability 恰为 `{ Read, Glob, Grep }`。生成的真实 API 只含对应功能，不能把 edit/write 以另一条对象路径暴露出去。

其他角色按自身原有 capability 构造。拥有 Grep 不等于自动获得公开的 Read；grep 自身内部读取选定文件属于该搜索操作，不应借此多开放 file()。

将 capability 作为同一来源传至描述、基类、示例和实际 API，避免模型看到的边界与执行边界分叉。

### 9.3 staging 也要守住只读不变量

在任何提交前检查本次 capability 是否允许产生对应 mutation。对于 Manager 只读执行，任何非空 mutation 都是拒绝条件，不能落盘，也不能写入已提交变更事实。

这是对 capability 与实际效果一致性的检查，不代替 API 过滤。不能先写磁盘再检查，也不能把“只读”解释为“先修改，最后还原”。

### 9.4 必测绕路

测试不仅调用 `this.write`，还要尝试直接使用当前上下文中的底层 API、枚举成员以及调用不存在的写方法。断言文件内容、文件集合和提交事实均不变。

另外，Node 官方明确说明 `node:vm` 本身不是安全机制。不能把“放进 vm 了”当作任意模型代码已经被隔离的证明。要检查既有沙箱关于宿主函数、构造器链、import、process、文件和进程入口的边界测试；若能绕过 API 直接获得宿主能力，必须视作开放 `js-manager` 前的阻塞缺陷，而不是靠 contract 文案掩盖。[参考：Node VM 文档](https://nodejs.org/api/vm.html)

本次不凭这份方案宣称整个沙箱已完成安全审计。

---

## 10. DevOps 门禁和提示词一起收口

### 10.1 DevOps 门禁代码落点

在 `Execution/Delegation/Fork/OpenCode/Tool.fs` 的 resume 路径中，先解析目标和当前绑定，再判断是否为固定 DevOps。

```text
解析现有目标
    ↓
目标是 DevOps？
    ├─ 否：走原来的 Engineer 续做与角色检查
    └─ 是：检查当前合法任期已接纳 assessment
               ├─ 否：拒绝，不执行绑定/charge/启动副作用
               └─ 是：继续原有忙碌、归属、收尾与派工检查
```

即使当前实现通过固定名字 `devops` 识别，也必须保留真实目标绑定检查。别把大小写、历史别名或任意一个叫 devops 的非绑定对象当授权。

### 10.2 提示词的最小修改范围

至少检查并同步中英文：

```text
resources/provider/world/common-law/
resources/provider/host/pair-programming-guideline/
resources/provider/runtime/manager-assess/
resources/provider/runtime/manager-work/
resources/provider/runtime/manager-finish/
resources/provider/tool/review/
resources/provider/tool/resume/
```

JS 工具说明通过 `Repository/Programming/Js/Description.fs` 中的 `JsCanonicalDescription.Path` 定位。另查 Manager Role Law、自我模型和生命周期投影中的绝对禁令；具体资源目录通过当前 ProviderProse 绑定定位，不凭相似文件名修改。

建议评审期文字：

> 你可以亲自使用评审专用只读工具，也可以托付只读 Engineer 查清当前交付，包括缺席或未完成的部分。评估时不修改评估对象，不向 DevOps 派工。独立判断八个维度，分清源码主张和实际运行证据，提交一次 review。

建议评审后工作文字：

> 不再亲自读取、搜索或处理仓库代码。通过 Engineer 安排源码工作，通过固定 DevOps 安排获准的真实执行与普通修复。依据返回的证据组织后续工作。

建议 Review 后读取拒绝：

> 当前评审已被接纳，本工具已不可用。未执行读取；后续代码调查交由 Engineer。

建议 Review 前 DevOps 拒绝：

> 当前独立评审尚未提交成功，不能向 DevOps 派工。继续只读调查并完成评审。

这些文案通过现有 ProviderProse/多语言资源机制提供，不把中文字符串直接硬编码进 F# 错误分支。

不要把评分过程变成“先运行 DevOps 再评分”的隐含要求。评审时缺少实际运行证据，就诚实记录缺口，不能冒称测试通过。

---

## 11. 回归测试矩阵

测试先于实现。新用例在旧实现上要因缺少目标行为失败，不能只因为 import 路径还不存在而报错。尽量从现有公开 surface、插件入口和真实 Host harness 驱动；不要导出只供测试使用的生产后门。

测试文件继续使用 `NNN.test.mjs`。用例标题带真实 WHAT 锚点，例如：

```javascript
test("WHAT[capability-enforcement-025] manager review read closes after accepted assessment", ...);
integrationTest("WHAT[host-boundary-032] review contract survives real host lifecycle", ...);
```

这里的省略号只是标题示意，不是可直接运行的测试实现。

### 11.1 角色和阶段门禁

主要放在 `requirements/capability-enforcement/tests/025.test.mjs`，并更新已有角色矩阵测试。

| 编号 | 场景 | 必须断言 |
|---|---|---|
| P01 | 活跃 Manager，未接纳 Review，逐个调用四工具 | 获准并有真实结果 |
| P02 | Review 已接纳，逐个调用四工具 | 拒绝；文件读取器/搜索器/JS runner 未被调用 |
| P03 | Review 参数无效、未接纳 | 只读权限仍在，不得误封 |
| P04 | Review 落盘后模拟回复丢失 | 新读查调用仍拒绝 |
| P05 | 无活跃任期但没有 assessment | 拒绝，不能把“无 assessment”误当授权 |
| P06 | 未知角色、身份未建立、runtime 不可用 | 拒绝，不能按工具后缀猜角色 |
| P07 | 有效证书、清理阻塞、退任冻结 | 保持原收尾限制，新工具不能例外放行 |
| P08 | Manager 调原生 read/grep/glob | 始终拒绝，before 或其他当前语义门禁不得漏掉伪造调用 |
| P09 | Manager 调 js-engineer/js-devops | 始终拒绝；Engineer/DevOps 调 Manager 专用工具同样拒绝 |
| P10 | 直接调用注册执行器，绕开 Host before | P01/P02/P06 的结论不变 |
| P11 | 同一 session 重启、上下文压缩 | 按恢复的当前任期事实判断，不重置评审 |
| P12 | 合法新任期与旧任期调用交错 | 不把旧调用授权借给新任期，也不使用旧 assessment 错封新评审 |
| P13 | before 通过，执行前接纳 Review | 注册执行门禁再次拒绝，零文件读取 |
| P14 | 只读调用已获最终准入，然后接纳 Review | 原调用可结束；后续新调用拒绝 |

P13/P14 用测试控制的 barrier/deferred promise 构造明确顺序。不要增加 sleep 或放宽超时掩盖竞态。

### 11.2 DevOps 目标级门禁

放在 `requirements/capability-enforcement/tests/026.test.mjs`，并回归现有 [024]。

| 编号 | 场景 | 必须断言 |
|---|---|---|
| D01 | Review 前 resume 固定 DevOps | 拒绝；无 charge 更新、无任务启动、无新绑定副作用 |
| D02 | Review 前 resume 合法的已有只读 Engineer | 不被本次 DevOps 门禁误拒 |
| D03 | Review 后合法 resume 固定 DevOps | 进入原有派工流程 |
| D04 | Review 后 DevOps 已忙或当前只能收尾 | 仍按原规则拒绝，不能因为 HasAssessment 就直接放行 |
| D05 | 使用大小写、错误身份或非本路绑定尝试绕过 | 以真实目标与归属为准，拒绝不合法请求 |
| D06 | fork DevOps、创建替代 DevOps | 仍拒绝；本次没有扩大 fork |

### 11.3 schema、私有字段与乐观处理

主要放在 `requirements/host-boundary/tests/032.test.mjs`。表中每种字段情况都应对四工具运行，而不是只验证 read-manager。

| 编号 | 场景 | 必须断言 |
|---|---|---|
| C01 | definition 装饰四专用工具 | contract 为 required string，enum 恰一个值 |
| C02 | definition 装饰原生 read、Engineer JS、无关工具 | 原定义不变 |
| C03 | 同一定义装饰两次或多次 | required/description 不重复；输出一致 |
| C04 | Review 前后生成定义 | 字节或项目 canonical 序列化结果相同 |
| C05 | 正确 contract → before → after | before 业务视图无 contract；after 原值恢复；私有字段消失 |
| C06 | 缺失/错误字符串/null/错误 JSON 类型 | 插件不因 contract 本身拒绝；after 原样恢复 |
| C07 | 存在但 undefined，与完全缺失 | 恢复后的 own-property 状态分别正确 |
| C08 | 重复 before、重复 after | 第一次原值不丢，恢复幂等 |
| C09 | 两个 session 并发、同 session 两个 callID 并发 | 参数不串号、不覆盖 |
| C10 | 模型提交仿造的 `_contract` 或类似字段 | 不作为私有记录，不影响授权与恢复 |
| C11 | 业务执行失败、Promise 拒绝、取消导致栈展开 | 同源恢复执行，保留原始失败，不伪造成功 |
| C12 | after 的 grounding 或观察报错 | 参数已经恢复，不受后续失败影响 |
| C13 | 冻结/不可扩展 args 或不能删除的字段 | 隐藏失败保持原状；不执行文件访问 |
| C14 | 不经过 before 的直接执行调用 | 缺少私有暂存不报恢复错误，但仍执行完整权限检查 |
| C15 | 模型把 contract 放在第一个/中间/最后一个字段 | 历史与 provider 前缀不发生恢复造成的漂移 |
| C16 | 真实 Host 校验启用与测试入口未校验两种路径 | 上游拒绝不被吞；已到本地的缺填/错填不被二次强校验 |

### 11.4 真实 Host canary：必须独立验收

同一个 `032.test.mjs` 可以包含 `integrationTest`，复用现有 Host 合同测试的 harness 和真实插件加载方式，不直接 import Host 私有函数。

用现有测试 provider 或可控 harness 驱动真实工具调用，不要求由真实模型随机“配合”调用某个工具。至少记录以下**测试观察**，不往生产日志泄漏用户参数：

```text
definition 输出
before 入口的 args 身份与字段
实际插件 execute 入口的 args 身份与字段
正常终态与异常终态是否调用 after
after 入口的 args 身份与字段
Host 已持久化的原始工具调用
下一次 provider 请求中的工具定义与历史调用
```

验收问题：

1. 新名字是否真的被注册、可见、可执行？
2. definition 中新增的 contract 是否真的发给 provider，而不是只改到一个不用的对象？
3. before 删除的字段是否从实际执行参数中消失？
4. 同一个私有暂存是否能在 after 或所选异常边界取回？
5. Host 在哪些失败位置不会进入 after？这些位置的原始历史是否仍完整？
6. 缺失/错误 contract 在未被上游拒绝时，插件是否仍可执行合法评审读取？
7. Review 后调用是否在实际 I/O 前被拒绝，而不只是返回一段劝告？
8. 下一次请求的历史参数是否被改写、重排或丢字段？
9. 强制终止后恢复，是否仍有原始调用和正确的已接纳 Review 状态？

**真实 canary 未完成不能写“Host 兼容已验证”。** 函数单测通过只证明辅助函数，不证明宿主调用顺序。特别是失败/取消若会跳过全部可用清理点，要明确列出这个不满足的合同，不能拿成功路径替代证明。

### 11.5 JS 与文件操作

在 `capability-enforcement [008]` 及对应 JS/文件操作规范的现有测试文件中补充：

| 编号 | 场景 | 必须断言 |
|---|---|---|
| J01 | Manager 生成 surface | capability 只有 Read/Glob/Grep；描述和示例不建议写入 |
| J02 | Manager 真实 createApi | 没有 edit/write 等写 API |
| J03 | this.write、this.edit、this.rewrite、原始 api 路径尝试写 | 失败且磁盘不变，无提交事实 |
| J04 | 人为注入不合规 staging 的边界测试 | 提交前拒绝，不能落盘 |
| J05 | Engineer、DevOps 合法编辑和 JS 执行 | 原能力保持，不能被 Manager 的只读收窄误伤 |
| J06 | Bookkeeper/internal leaf/StrengthReplica | 既有专用准入与请求级收窄不被扩大 |
| J07 | 简单工具参数含引号、反斜杠、换行、代码片段 | 被当成数据，不能改变固定程序的意图 |
| J08 | 根目录、相对/绝对路径、父目录、链接边界 | 与规范一致，不读错工作树 |
| J09 | 空文件、多行文件、CRLF、中文、末行无换行、片段越界 | 返回符合行片段合同，不丢失或重复内容 |
| J10 | 缺失文件、非 UTF-8、非法正则、结果超限 | 明确失败或明确截断，不伪装成无结果 |
| J11 | 正常读查与被拒读查的 grounding/Casebook/计数 | 真实成功访问只记一次；拒绝不记成功观察 |
| J12 | 已有沙箱隔离与宿主能力绕路测试 | 不因开放 Manager JS 新增可达写入或执行通道 |

### 11.6 提示词与完整前缀

在 `office-capability`、`relay-assessment` 和 `prefix-stability` 的对应既有测试中补充：

- 中英文实际分发资源均允许评审期直接只读调查，且没有残留相冲突的绝对禁令。
- 评审前可见内容不透露低分后修复、全优后结束或下一任期机制。
- 评审后当前指令明确禁止直接读查；固定 system 和工具 schema 不改变。
- 比较一次真实 Review 前后的完整 provider projection，使用已有 `ProviderProjection.isAppendOnlyPrefix` 判定，不自行简化成“工具名一样”。
- 同一前缀下的历史 tool-call 参数完全稳定；compaction/reanchor 保持原有规则，不成为清理字段的后门。

---

## 12. 实施顺序与每步完成标准

### 第一步：确认基线

```sh
cd ~/Desktop/vibe/wanxiangshu
git status --short --branch
git diff --stat
git diff --cached --stat
git rev-parse HEAD
```

若有用户未提交改动，先逐文件判断重叠范围，保留无关改动；不 reset，不批量替换，不先格式化全仓制造噪音。

读 root AGENTS.md、涉及目录的下级指令、相关 requirement 与现有测试。本文不能代替这些规范。

完成标准：明确本次基线和要改的 owner，列出已有改动，不开始修改业务代码。

### 第二步：修正规范，建立失败测试

按第 3 节改规范，建立 P/D/C 关键用例，先跑旧实现确认缺口。优先做真实 Host canary 的最小链路，因为它决定私有字段方案的实际接点。

完成标准：知道哪些是产品行为缺口，哪些是 Host 合同待证明；测试失败理由正确。

### 第三步：实现纯权限与工具名投影

完成第 4、5 节：基础上限、当前事实集合差、角色别名、request map、精确四工具目录。

此时先把纯权限与投影测试跑通。不要在一个可发布状态中只开放工具，而没有执行门禁；这一阶段的中间代码不单独推到 master。

完成标准：角色矩阵、状态矩阵、静态定义稳定测试通过。

### 第四步：先把 JS 实际 API 收成只读

完成第 9 节的 capability 传递、API 过滤与提交前不变量检查；再接三个简单工具的固定只读程序。

完成标准：Manager 读查正常，写入尝试没有任何磁盘或提交副作用；其他角色未回归。

### 第五步：接两个执行入口与 DevOps 目标门禁

before 与注册执行器共用当前事实判断。增加 DevOps 派工前置检查。保留既有角色未决、退任冻结、证书和 cleanup 分支。

完成标准：绕开 before 的直接调用仍守住权限；before 到实际执行的竞态测试通过；Review 前不能派工 DevOps，但可续做合法 Engineer。

### 第六步：接 schema 与参数生命周期

完成 definition 幂等装饰、before 最后隐藏、after 最先恢复、插件拥有的异常边界恢复。

运行真实 Host canary，确认对象身份、校验位置、失败路径、历史持久化与下一次请求。按照实测结果处理键顺序，不能先假定宿主行为。

完成标准：C 类测试、真实 Host 合同证明和历史稳定证明全部通过。没有“after 一定会来”这种未经证明的假设。

### 第七步：同步资源和语义观察

修改中英文提示词，修正 grounding、Casebook 和工具调用计数的接点。不要把别名带来的名字变化误当成新的业务事实类型。

完成标准：当前阶段指令准确；评审前信息隔离仍在；观察不漏、不重、不把拒绝算成功。

### 第八步：编译、定向回归、综合验证

按第 13 节运行验证。每次失败按因果排查，不靠放大超时、注释断言、跳过测试或手改 dist 过关。

完成标准：测试记录能够指出运行了什么、什么版本、哪些真实 Host 用例实际执行，不把 skipped 写成 passed。

### 第九步：审阅并原子交付

审阅源码、规范、资源、测试、owner 编译清单和 APPLIES-TO 的 diff。删除临时文件，不留下旧文案、失效分支和为了中间阶段保留的兼容壳。

按仓库提交纪律提交并推送，禁止 force push。提交只包含本次明确相关的文件，不用 `git add -A` 把用户无关改动一起带走。

完成标准：规范、权限、实际执行、提示词和测试在同一个最终提交中闭环；交付报告只陈述真实结果。

---

## 13. 编译清单与验证命令

### 13.1 F# 文件和 owner 顺序

新 `.fsi` 放在对应 `.fs` 前。纯工具目录要早于 StaticTools 与消费它的 Host 模块；依赖 JsToolWorkflow 的工具实现要晚于该工作流；PluginHooks 最后接线。

本仓除主编译顺序外还有 owner project。修改时检查：

```text
src/Wanxiangshu/compile-order.txt
src/Wanxiangshu/Wanxiangshu.Owner.*.fsproj
相关 requirements/<package>/APPLIES-TO
```

已核对的 owner 例子包括：

```text
Wanxiangshu.Owner.office-capability.foundation-officecapability.fsproj
Wanxiangshu.Owner.action-affordance.opencode-tools-statictools.fsproj
Wanxiangshu.Owner.capability-enforcement.opencode-host-managedagentconfig.fsproj
Wanxiangshu.Owner.opencode-plugin.plugin-composition.fsproj
Wanxiangshu.Owner.repository-programming.repository-programming-js-capability.fsproj
```

新增文件归属要按实际依赖核对，不把所有新代码塞进 PluginHooks 或 capability-enforcement owner 来逃避边界。不要引入循环依赖，也不要修改生成后的 dist 文件。

### 13.2 最小编译与定向测试

```sh
node scripts/build.mjs
```

禁止 `dotnet build`。测试导入 dist，必须先有新鲜构建。

按本文新增编号落地后，可运行：

```sh
TESTS_MJS_FILES='requirements/capability-enforcement/tests/025.test.mjs,requirements/capability-enforcement/tests/026.test.mjs,requirements/host-boundary/tests/032.test.mjs' \
node requirements/verification-system/tests/run.mjs
```

若条款编号已顺延，替换为实际文件名。然后把本次改过的既有角色矩阵、JS、评审和前缀测试文件加入同一显式列表，逐步扩大验证范围。

这使用的是当前仓库 runner 的 `TESTS_MJS_FILES` 能力。不要假设 `npm test -- <package>` 或不存在的 `--package` 参数可用，也不要用 `--skip-staleness-check` 跳过新鲜度验证。

### 13.3 真实 Host integration

当前集成入口会先执行 OpenCode warmup，并发现各 requirement 顶层测试文件中的 `integrationTest` 或集成标记。用已有 harness 接入，别把新 canary 放进 runner 永远发现不到的目录。

```sh
node requirements/verification-system/tests/integration/run.mjs
```

该入口已核对：它按自己的规则发现集成套件，不把上面的 `TESTS_MJS_FILES` 当成集成套件筛选器。不要沿用一个变量却误报“只跑了指定 integration”。

单元 runner 中的 integrationTest 可能跳过；最终要核对真实集成入口的执行记录。

### 13.4 最终综合检查

此改动跨权限、Host、JS runtime、持久事实与前缀，定向回归通过后再运行一次项目固定流程：

```sh
npm run format-build-test
```

当前脚本覆盖 format:check、check、build、unit 与 integration。它会清理作用域测试环境，不能靠留下 `TESTS_MJS_FILES` 假装完成全流程。

发布打包是否还需 `npm run verify:release`，按实际交付范围与分发规范决定；不要把未运行的发布证明写成已通过。

最终审阅：

```sh
git diff --check
git status --short
git diff --stat
```

再查看各相关文件的完整 diff。不能仅看统计数字。

---

## 14. 最终验收清单

以下清单应由实施者依据实际证据勾选，不预先标绿。

### 产品行为

- [ ] Manager 在当前独立评审期间能亲自读查，不再被迫所有调查都委派。
- [ ] 评审期间仍不修改评估对象，不执行真实命令。
- [ ] Review 接纳前无法向固定 DevOps 派工。
- [ ] 合法已有 Engineer 的只读 resume 未被误伤。
- [ ] Review 接纳后，四个直接读查工具全部拒绝。
- [ ] 无效 Review 不误封；已落盘 Review 不因丢回复或重启解封。
- [ ] 原生工具和跨角色 JS 不能成为绕路。
- [ ] 证书、清理、退任和未知身份门禁未退化。

### 工具面与参数

- [ ] 四工具从第一轮起固定可见，Review 前后定义不变。
- [ ] required contract 恰好一次，单值 enum 恰好一个值。
- [ ] contract 不进入执行器的强制业务校验。
- [ ] before 私有保存后删除；after 原样恢复并清理。
- [ ] 缺填、错填、null 和 own-property 差异均按原样处理。
- [ ] 并发、重复 hook、异常、取消已实际测试。
- [ ] 真实 Host 对象身份、参数校验与 after 可达性有证据。
- [ ] 原始历史完整，私有暂存不进入 provider、持久化或用户日志。
- [ ] 键顺序与真实 provider prefix 比较通过。

### 真实执行与工程交付

- [ ] js-manager 的描述、基类、示例和 runtime API 同源只读。
- [ ] 直接 API 写入、违规 mutation 与已知宿主能力绕路不能落盘。
- [ ] Engineer/DevOps 的合法能力保持，内部角色与 replica 没有提权。
- [ ] 简单工具没有复制 Host 私有实现，没有 shell 转发。
- [ ] 工作树解析正确；路径、编码、截断与异常行为明确。
- [ ] grounding/Casebook/工具计数不漏记、不重复、不伪记成功。
- [ ] 中英文规范和实际分发提示词一致，评审前信息隔离保留。
- [ ] `.fsi`、编译顺序、owner project、APPLIES-TO 已同步。
- [ ] 定向测试、真实 Host 集成、最终固定验证流程有真实结果。
- [ ] 没有 Host fork、node_modules 修补、手改 dist、临时脚本和无关重构。

---

## 15. 给接手实施者的一段任务说明

> 在当前仓库实现 Manager 评审期直接只读调查。新增插件工具 read-manager、grep-manager、glob-manager，并开放只读 js-manager。Manager 始终不能使用原生 read/grep/glob 和其他角色的 JS 工具。工具定义取静态并集，不随 Review 改变；四工具的模型 schema 加 required 单值 enum contract=do-not-use-except-for-review，本地不因该字段缺失或错误额外拒绝。before 把原字段藏入参数对象私有字段后删除，after 原样恢复；失败、取消与历史序列化使用同源恢复并有真实 Host canary 证明。执行权限根据当前任期已接纳的 assessment 收口，不能另设布尔状态。before 与注册执行器共用门禁。Review 前仅禁止向固定 DevOps 派工，不误禁已有 Engineer 的合法 resume。同步修正 JS 实际 API 的 capability 注入，不能只隐藏 write 方法；直接 API 和 mutation 提交也必须只读。更新冲突规范、中英文资源、权限矩阵、前缀回归、owner 编译清单及 APPLIES-TO。先写失败测试，运行 Fable 构建、定向测试和真实 Host 集成；不改 Host、不修补 node_modules、不用 dotnet build、不手改 dist、不提交无关改动。

---

## 16. 依据、核对范围与尚需实证的条件

### 仓库依据

本文第 2 节给出已读源码和规范落点。另核对了：

- `package.json`：当前开发依赖为 `@opencode-ai/plugin` 与 `opencode-ai` 1.18.29；peer 范围不等于每个运行版本已验证。
- 本地 `node_modules/@opencode-ai/plugin` 类型声明：definition、before、after 的公开输入输出。
- `scripts/verify.mjs`：固定验证阶段及作用域测试环境处理。
- `requirements/verification-system/tests/run.mjs`：构建新鲜度门禁和显式文件列表。
- `requirements/verification-system/tests/integration/run.mjs`：warmup 与 integration 发现方式。
- `src/Wanxiangshu/Repository/Programming/Js/FilesystemSurface.fs/.fsi`、`ToolsBindings.fs`、`Capability.fs`、`Surface.fsi`：已有文本读查、能力与 API 结构。

源码事实以本文记录的本地基线为准。路径是定位依据，不表示本次已修改这些文件。

### 外部公开依据

- [OpenCode 插件文档](https://opencode.ai/docs/plugins/)：公开自定义工具与 hook 接入方式。不能由文档中的接口存在，推断失败路径一定调用 after。
- [MDN：Symbol](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Symbol)：私有键的语言行为；Symbol 不构成持久化协议。
- [Node.js：VM](https://nodejs.org/api/vm.html)：vm 上下文与安全边界限制。

在线固定版本的 Host 源码在本次抓取中未成功取得。因此本文没有把该版本的完整校验顺序、对象复制行为或失败后 after 可达性当成已核实事实；这些条件明确落在第 11.4 节的真实 Host canary 中。

本次产物仅为 Markdown 实施方案：未修改远端仓库，未运行该仓库的构建、测试或 git 提交。
