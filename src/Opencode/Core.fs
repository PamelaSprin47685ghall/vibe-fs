module VibeFs.Opencode.Core

let coder = ToolCopy.coder
let reader = ToolCopy.reader
let meditator = ToolCopy.meditator
let browser = ToolCopy.browser
let executor = ToolCopy.executor
let fuzzyFind = ToolCopy.fuzzyFind
let fuzzyGrep = ToolCopy.fuzzyGrep
let websearch = ToolCopy.websearch
let webfetch = ToolCopy.webfetch

module Params =
    let coderIntents = ToolCopy.Params.coderIntents
    let readerIntents = ToolCopy.Params.readerIntents
    let meditatorIntent = ToolCopy.Params.meditatorIntent
    let meditatorFiles = ToolCopy.Params.meditatorFiles
    let browserIntent = ToolCopy.Params.browserIntent
    let executorLanguage = ToolCopy.Params.executorLanguage
    let executorProgram = ToolCopy.Params.executorProgram
    let executorDeps = ToolCopy.Params.executorDeps
    let executorTimeout = ToolCopy.Params.executorTimeout
    let fuzzyFindPattern = ToolCopy.Params.fuzzyFindPattern
    let fuzzyFindPath = ToolCopy.Params.fuzzyFindPath
    let fuzzyFindLimit = ToolCopy.Params.fuzzyFindLimit
    let fuzzyFindIterator = ToolCopy.Params.fuzzyFindIterator
    let fuzzyGrepPattern = ToolCopy.Params.fuzzyGrepPattern
    let fuzzyGrepPath = ToolCopy.Params.fuzzyGrepPath
    let fuzzyGrepExclude = ToolCopy.Params.fuzzyGrepExclude
    let fuzzyGrepCaseSensitive = ToolCopy.Params.fuzzyGrepCaseSensitive
    let fuzzyGrepContext = ToolCopy.Params.fuzzyGrepContext
    let fuzzyGrepLimit = ToolCopy.Params.fuzzyGrepLimit
    let fuzzyGrepIterator = ToolCopy.Params.fuzzyGrepIterator
    let websearchQuery = ToolCopy.Params.websearchQuery
    let websearchNumResults = ToolCopy.Params.websearchNumResults
    let websearchWhatToSummarize = ToolCopy.Params.websearchWhatToSummarize
    let webfetchUrl = ToolCopy.Params.webfetchUrl
    let webfetchExtractMain = ToolCopy.Params.webfetchExtractMain
    let webfetchPreferLlmsTxt = ToolCopy.Params.webfetchPreferLlmsTxt
    let webfetchPrompt = ToolCopy.Params.webfetchPrompt
    let webfetchTimeout = ToolCopy.Params.webfetchTimeout

let toolFactory = Sdk.toolFactory
let schema = Sdk.schema
let call0 = Sdk.call0
let call1 = Sdk.call1
let arr = Sdk.arr
let strMin = Sdk.strMin
let strMinNullish = Sdk.strMinNullish
let strReq = Sdk.strReq
let strOpt = Sdk.strOpt
let intMinNullish = Sdk.intMinNullish
let boolOpt = Sdk.boolOpt
let excludeOpt = Sdk.excludeOpt
let intentsSchema = Sdk.intentsSchema
let uiParam = Sdk.uiParam
let strArrayOpt = Sdk.strArrayOpt
let numOpt = Sdk.numOpt
let enumOpt = Sdk.enumOpt
let obj = Sdk.obj
let define = Sdk.define

let stripUiFromJsonSchema = HookSchema.stripUiFromJsonSchema
let rewriteToolJsonSchema = HookSchema.rewriteToolJsonSchema
let buildMagicTodoSchema = HookSchema.buildMagicTodoSchema
let joinReaderIntents = HookSchema.joinReaderIntents
let joinCoderIntents = HookSchema.joinCoderIntents
let uiLabelForTool = HookSchema.uiLabelForTool
let setUiLabel = HookSchema.setUiLabel
