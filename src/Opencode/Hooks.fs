module VibeFs.Opencode.Hooks

// This module has been split into VibeFs.Opencode.HookExecute and
// VibeFs.Opencode.HookTransform. The file is intentionally empty and is no
// longer referenced from vibe-fs.fsproj; delete on next cleanup.
