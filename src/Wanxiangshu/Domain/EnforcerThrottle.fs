namespace Wanxiangshu.Domain

/// tip v2: score-vector throttle exited product contract (ENFORCER-080..090).
/// Compiled tombstone; exports only `Removed = true`; zero production call sites.
module EnforcerThrottle =

    let Removed = true
