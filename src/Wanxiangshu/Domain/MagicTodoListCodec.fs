// Retired by GrandRewrite clean break.
//
// The provider/durable account codec now lives in MagicTodoObligationCodec.fs.
// Tagged TodoItem `{id,content,status,priority}` decoding is intentionally gone;
// keeping executable legacy status machinery here would create a second semantic
// owner for todowrite.
