namespace Wanxiangshu.OpenCode

/// Legacy SyncDelegate `return` tool deleted (EXEC-031 / AGENT-024).
/// Ordinary assistant completion → bounded WorkRecord. Kept as empty module
/// so call sites that once opened this file compile against a stable path.
module SyncDelegateTools =
    begin end
