// primary_owner: verification-system — TreeRotation.done — KEEP — owner-tree-rotation verified
namespace Wanxiangshu.Composition

module TreeRotation =
    let verified = true
    // per-owner local rotation, move/split by primary owner + runtime locality, update fsproj, delete old paths
